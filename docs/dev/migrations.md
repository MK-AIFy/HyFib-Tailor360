# Database migrations

How schema changes are made, applied, verified and undone. Read this before writing a migration; the
pull-request template links here.

## Principles

1. **One module owns one schema.** A migration for the Orders module may only touch the `orders`
   schema. Architecture rule ARCH-005 fails the build if a context maps a table outside its schema.
2. **Migrations are applied by the command line, never by the application.** During a rolling
   deployment two versions of the application run at once; if either could migrate, they would race.
   `Tailor360.Cli migrate` runs once, before the new version is allowed to serve.
3. **The migrator is not the application.** Migrations run as a role that may change the schema. The
   application role may not, so a defect in the application cannot alter its own tables.
4. **Expand in release N, contract no earlier than N+2.** This is what makes a rollback survivable.

## Expand and contract

A change that removes or narrows anything is split across releases:

| Release | What ships | Why |
| --- | --- | --- |
| N | Add the new column or table. Write to both. Read from the old one. | The previous version still runs and still understands the old shape. |
| N+1 | Read from the new one. Keep writing both. | If N+1 is rolled back, N still works. |
| N+2 or later | Stop writing the old one, then drop it. | By now no supported version reads it. |

Renaming a column is the same three steps, never a single `RENAME`. A rename looks atomic and is the
most common cause of a failed rollback.

## Writing a migration

```bash
dotnet ef migrations add <Name> \
  --project src/Platform/Tailor360.Platform.Persistence \
  --startup-project src/Platform/Tailor360.Platform.Persistence \
  --context PlatformDbContext \
  --output-dir Migrations
```

Substitute the module's project and context for a module migration. Review the generated file before
committing it: the scaffolder is a starting point, not an authority. Add explicit SQL for anything the
model cannot express — partitioning, triggers, functions, grants — as `migrationBuilder.Sql(...)` with
a comment saying why it is there. `platform.audit_events` is built entirely this way, because its
partitioning, hash-chain trigger and append-only trigger are the point of the table.

Every migration needs a working `Down`. An untested `Down` is not a rollback plan.

## Applying migrations

```bash
./scripts/dev migrate          # or:
dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run
dotnet run --project src/Tools/Tailor360.Cli -- migrate
```

The runner holds one PostgreSQL advisory lock for the whole run, so two instances starting together
cannot both migrate. Contexts are applied in a fixed order: `platform`, then `identity`, then the
remaining modules alphabetically. Each schema keeps its own `__ef_migrations_history` table.

## Startup validation

The startup health check compares this build's migrations against the database:

- **A migration this build knows is missing from the database** — the build refuses to serve. Serving
  against a schema that lacks a column this code writes would corrupt data.
- **The database has migrations this build does not know** — the build serves and reports *degraded*.
  This is the normal state during a rollback to release N while the database is still at N+1. Comparing
  the two sets for equality instead would turn every rollback into an outage, which is why the check is
  deliberately one-directional.

## Rolling back

1. **Prefer a forward fix.** A new migration that corrects the problem is almost always safer than
   reversing one, because it does not have to guess what the reversed migration did to the data.
2. **If the schema change did not touch data**, deploy the previous application version. Expand and
   contract means it still understands the current schema.
3. **If data was mutated and is wrong**, restore from backup to a point before the migration and replay
   what happened since. This is the case the restore drill in issue #60 rehearses.

Never edit a migration that has been applied anywhere but a developer machine. Add another one.

## Compatibility check against the previous release

`tests/fixtures/db-snapshots/` holds the schema snapshot of release N-1 only. The migration test
applies this build's migrations to an empty database and to that snapshot; both must succeed. Older
snapshots are deleted, and migrations older than two releases may be squashed.

## Register

Every migration is recorded here when it merges, with its phase and the evidence that it was applied.
Conventions section 6.4 requires this alongside the pull-request evidence, so that a reviewer looking at
a schema six months later can see what each change was for without reading the code.

| Migration | Schema | Phase | Issue | Applied and verified |
| --- | --- | --- | --- | --- |
| `20260904185922_InitialPlatformSchema` | `platform` | Expand — initial | #21 | Empty database; re-run reports up to date |
| `20260904185927_AuditTrailStructure` | `platform` | Expand — initial | #21 | Empty database; re-run reports up to date |
| `20260904195746_AuditPartitionSafety` | `platform` | Expand | #21 | Empty database; re-run reports up to date |
| `20260905155743_InitialIdentitySchema` | `identity` | Expand — initial | #23 | Applied to an empty PostgreSQL 16 database; re-run reports `identity: already up to date`; `Down` reverted to an empty schema and the migration re-applied cleanly |
| `20260905162158_DataProtectionKeyRing` | `platform` | Expand | #23 | Applied to an empty PostgreSQL 16 database (`tailor360_dp_check`); re-run reports `platform: already up to date`; `Down` dropped `platform.data_protection_keys` and the migration re-applied cleanly |
| `20260905170018_AddRecoveryTokens` | `identity` | Expand | #23 | Applied to an empty PostgreSQL 16 database (`tailor360_mig_check`); both check constraints present afterwards — `ck_recovery_tokens_token_hash_is_digest` and `ck_recovery_tokens_lifetime`, the second of which is the domain's one-hour cap restated in the schema; `Down` dropped the table and the migration re-applied cleanly |
| `20260905193715_AddSessionPendingStep` | `identity` | Expand | #23 | Applied to `tailor360_mig_check`; `identity.sessions.pending_step` is `character varying(30) NOT NULL DEFAULT 'None'` — the default is `None` rather than the scaffolder's empty string, because rows written by the previous build were issued only after the first factor and an empty string parses as no enum member at all; `Down` dropped the column and the migration re-applied cleanly |
| `20260906121513_AddRolesBranchesAndAssignments` | `identity` | Expand | #24 | Applied to an empty PostgreSQL 16 database (`tailor360_mig26`) by `./scripts/dev migrate`; re-run reports `identity: already up to date`. Recorded here retrospectively in #26: the row was missed when the migration merged |
| `20260906170048_IdempotencyInFlightLease` | `platform` | Expand | #53 | Applied to `tailor360_mig26`; re-run reports `platform: already up to date`. Recorded here retrospectively in #26 for the same reason |
| `20260907044327_AddMfaConcurrencyTokens` | `identity` | Expand | #25 | Applied to `tailor360_mig26`; re-run reports `identity: already up to date`. Recorded here retrospectively in #26 for the same reason |
| `20260907090340_AddBranchMasterData` | `identity` | Expand | #25 | Applied to `tailor360_mig26`; re-run reports `identity: already up to date`. Recorded here retrospectively in #26 for the same reason |
| `20260907104054_AddRoleConcurrencyToken` | `identity` | Expand | #25 | Applied to `tailor360_mig26`; re-run reports `identity: already up to date`. Recorded here retrospectively in #26 for the same reason |
| `20260907160822_InitialCustomersSchema` | `customers` | Expand — initial | #26 | Applied to an empty PostgreSQL 16 database (`tailor360_mig26`): `platform: applied 5`, `identity: applied 7`, `customers: applied 1`. Creates `customers.customers`, `customers.customer_aliases` and `customers.customer_branch_visibility`, the three check constraints `ck_customers_phone_is_e164`, `ck_customers_alternate_phone_is_e164` and `ck_customers_deactivated_at_matches_status`, and — in explicit SQL, because EF Core's model cannot express an operator class — `CREATE EXTENSION IF NOT EXISTS pg_trgm` and the three GIN trigram indexes the counter search reads through. Re-run reports `customers: already up to date`; `Down` emptied the schema to the history table alone and deliberately left the extension in place, because dropping a shared extension would break anything else in the database using it; re-applying was clean, and `dotnet ef migrations has-pending-model-changes` reports none. **Deployment note:** `CREATE EXTENSION` needs a role permitted to create extensions, so the migration fails loudly on a database whose user cannot, rather than quietly leaving the search without its indexes |
| `20260908003058_AddConsentAndCommunicationPreferences` | `customers` | Expand | #26 | Applied to an empty PostgreSQL 16 database (`tailor360_mig26b`) after the initial customers schema; re-run reports `customers: already up to date`. Adds `consent_purposes`, `consent_wordings`, `consent_records` and `communication_preferences`. Three things the model cannot express are explicit SQL: the `consent_records_no_update` trigger that makes the consent trail append-only in the database and not only in the domain type (the pattern `platform.audit_events` already uses), and the check constraints `ck_consent_records_decision_is_known` and `ck_communication_preferences_quiet_hours_are_whole`. **All four were proved to bite against the live database**: `UPDATE` and `DELETE` on a consent record are refused with "consent_records is append-only", a decision of `Maybe` is refused, a quiet-hours start without an end is refused, and a whole window is accepted — with the consent record still reading `Granted` afterwards. `allowed_channels` is `character varying(20)[]` holding the channel *names*, so reordering the enumeration cannot re-point a stored preference. `Down` dropped the trigger, the four tables and the function, leaving the schema at the initial migration; re-applying was clean and `dotnet ef migrations has-pending-model-changes` reports none |
| `20260908022906_ModuleOutbox` | `identity` | Expand | #77 | Applied to an empty PostgreSQL 16 database (`tailor360_mig77`): `platform: applied 5`, `identity: applied 8`, `customers: applied 3`; re-run reports `already up to date` for all three. Adds `identity.outbox_messages` and `identity.inbox_messages` with the partial index `ix_outbox_messages_pending` the dispatcher's claim reads through. **The tables are mapped by `ModuleDbContext`, not by this module**, so every module has them by construction and a module added later gets them without anybody remembering — which is the point: a module's write and its event are then on one context and commit together. `Down` dropped both tables and re-applying was clean; `dotnet ef migrations has-pending-model-changes` reports none |
| `20260908022913_ModuleOutbox` | `customers` | Expand | #77 | The same pair in `customers`, from the same `ModuleDbContext` mapping, applied and reverted in the same run on `tailor360_mig77`. **Additive and nothing is moved:** `platform.outbox_messages` and `platform.inbox_messages` keep their shape and their rows — the platform reports `already up to date` because its model did not change, only where the mapping lives — and they are now the platform module's own pair rather than every module's. Both tables were empty in every environment beforehand, because nothing published, which is what made the change a pure expand rather than a data move |
| `20260911141153_InitialOrdersSchema` | `orders` | Expand — initial | #133 | **The evidence came from CI, because there is no Docker daemon and no PostgreSQL on the machine this was written on.** Run `34628648096` applied this migration to real PostgreSQL: unit 1673, architecture 34, contract 151, integration 532 of which 531 passed and exactly one failed — the test written to be answered rather than to pass, which settled the cascade question recorded below. `OrdersMigrationTests.AppliesRollsBackToTheHistoryTableAloneAndReAppliesCleanly` passed in that run against its own namespaced database, so the `Down` has now been executed at least once, which `src/Modules/CLAUDE.md` section 6 requires before this may merge. **The `DEFERRABLE` change that failure produced was made after that run, so it is proved by the next green one and not by `34628648096`.** What was produced locally: the solution builds warning-free in Release, `dotnet format --verify-no-changes` is clean, `dotnet ef migrations has-pending-model-changes` reports none, the unit, architecture and contract tiers are green (1673 / 34 / 151), and both directions were generated as SQL and read through — `dotnet ef migrations script` and `dotnet ef migrations script 20260911141153_InitialOrdersSchema 0` — which proves the DDL is emitted and ordered, and proves nothing about whether PostgreSQL accepts it. `migrate --dry-run` cannot stand in for that here: it compares what the build knows against `__ef_migrations_history`, so it opens a connection and fails with `Failed to connect to 127.0.0.1:5432`. Creates the twelve `orders` tables — `order_drafts`, `order_draft_garments`, `order_draft_garment_dependencies`, `estimates`, `orders`, `order_revisions`, `garment_jobs`, `job_dependencies`, `measurement_snapshots`, `design_snapshots`, plus the module's own `outbox_messages` and `inbox_messages` from `ModuleDbContext`. Five things the model cannot express are explicit SQL, each with a comment saying why: `job_snapshots_are_immutable` over the two snapshot tables and `garment_job_price_is_immutable` over the `price_…` columns of `garment_jobs` (INV-JOB-01, and both deliberately permit the replacement `Order.Revise` makes while every job is still `Confirmed`); `order_revisions_append_only` and `job_dependencies_append_only`; `garment_jobs_ready_is_gate_only` (INV-JOB-07); and `order_number_is_immutable`, `estimate_number_is_immutable` and `garment_job_number_is_immutable`, one per numbered table (INV-ORD-03). **The scaffolder's output needed no hand correction to its generated part, and the five `HAND-EDITED` sites this row used to list are gone with the `job_ready_state` fragment.** `GarmentJob` is no longer split across two tables — Entity Framework Core 10.0.11 cannot read an entity that is both split and holds a complex property, and the frozen price copy is a complex property, so `OrderStore.FindAsync` and `IOrderSnapshotQuery.GetPricedAsync` both threw `InvalidOperationException: Sequence contains more than one element` at query-compilation time. The gate's outcome is `is_ready_for_delivery`, `ready_state_computed_at` and `ready_state_blocks` on `garment_jobs`; `docs/architecture/module-ownership.md` section 5.5 is amended in the same pull request, as section 9 of that document requires. With the fragment gone the scaffolder names the primary key once and resolves every foreign key to `garment_jobs` on its own, and the two ready-state check constraints and the delivery-queue index are ordinary model declarations rather than hand-written SQL — they belong on `garment_jobs` now, which is where Entity Framework puts them anyway. The delivery-queue index changed shape with the table: `ix_job_ready_state_ready` over the fragment's key, with branch scoping left to a join, became `ix_garment_jobs_ready` over `branch_id` filtered on `is_ready_for_delivery`, and there is no join left to make. `garment_jobs_ready_is_gate_only` keeps the insert arm of the old fragment trigger verbatim and drops its delete arm, because on `garment_jobs` the same rule would say a garment job can never be deleted at all and would refuse the cascade from `orders`. `garment_job_price_is_immutable` reads `OLD.status` **and** `NEW.status`: reading `OLD` alone would have let the `StartProduction` update carry a rewritten price out of `Confirmed` with it, which INV-JOB-01 forbids and which no legitimate path does — `GarmentJob.CheckRevision` refuses any status but `Confirmed` and `Revise` never moves it, so both sides read `Confirmed` on the only path that re-prices. `Tailor360.Cli`'s `CliHost` gained `AddOrdersModule`, without which this migration compiles and is unreachable; `AddCatalogModule` and `AddMediaModule` went in beside it, because they were missing too and the resulting unresolvable `ICatalogAvailabilityQuery` made `migrate` throw while building its container, before it reached a database at all. **Three corrections from the second review, all in the trigger bodies and none in the scaffolded part.** *(a)* The `DELETE` arms of `job_snapshots_are_immutable`, `order_revisions_append_only` and `job_dependencies_append_only` refused unconditionally, which made every `ON DELETE CASCADE` in the schema unexecutable and made two of the messages say the opposite of what the trigger did: PostgreSQL deletes the parent row first and then fires the child's `BEFORE DELETE` trigger, so deleting an order or a garment job aborted on a `restrict_violation` whose own text said that case was allowed. Each now refuses only while the parent row still exists — `NOT EXISTS (SELECT 1 FROM …)` against a parent already gone from this transaction's view — and returns `OLD` otherwise. *(b)* `garment_job_price_is_immutable` compared two whole-row `to_jsonb()` documents on every `UPDATE` of the most-written table in the schema; it is an explicit `IS DISTINCT FROM` chain over the twenty-two `price_…` columns, which keeps the property the column list was chosen for — a column added later has to be considered here — without serialising the row. *(c)* the one generic `display_number_is_immutable` read its column through `to_jsonb(NEW) ->> …`, serialising the row twice to read one text column, and is three three-line functions instead. Two model changes came with them: `estimates` gained `updated_at`/`updated_by`, because it carries `xmin` and is rewritten by `Supersede`, `Convert` and `RecordArtefact` — `src/Modules/CLAUDE.md` section 5 asks for the pair on an editable row, and without it the row could not say when it last changed; and `ck_garment_jobs_delivered_is_consistent` was weakened from `(delivered_at IS NOT NULL) = (status IN ('Delivered', 'Closed'))` to `status <> 'Delivered' OR delivered_at IS NOT NULL`, because the converse is the naive shape the `orders` table deliberately omits one aggregate level up and would have to be contracted out at N+2 when issue #34's post-delivery alteration lands. **To verify on CI:** that deleting an order cascades through its garment jobs, revisions, dependencies and both snapshot tables without a `restrict_violation` — `job_dependencies` holds two foreign keys into `garment_jobs`, `garment_job_id` cascading and `prerequisite_garment_job_id` not, and run `34628648096` settled which way PostgreSQL resolves the two: with the prerequisite key `ON DELETE RESTRICT` it refused the whole-order cascade with `23503: update or delete on table "garment_jobs" violates foreign key constraint "fk_job_dependencies_garment_jobs_prerequisite_garment_job_id"`, because `RESTRICT` is checked against the prerequisite garment before the dependent garment's own cascade has removed the row naming it — so an order carrying any declared dependency could not be hard-deleted at all. The key is now `NO ACTION DEFERRABLE INITIALLY DEFERRED`, which moves the check to `COMMIT`, where the question is decidable: either both rows have gone together, or a prerequisite has been deleted while its dependent survives, and that second case is still refused — `OrdersCascadeTests.RefusesToDeleteAGarmentThatAnotherSurvivingGarmentIsWaitingFor` asserts it, so the deferral cannot read as the constraint being weakened until the cascade stopped complaining. Nothing else in this schema defers. The path is reachable only from a hard delete, which no business path performs — an order is cancelled, never deleted, and that deleting a snapshot row, a revision or a dependency on its own is still refused; that the two immutability triggers accept `Order.Revise` on a confirmed order and refuse it afterwards, that an insert into `garment_jobs` carrying `is_ready_for_delivery = true` is refused, that the two ready-state check constraints refuse a ready row carrying blocking reasons and a ready row with no recorded evaluation, that a display number cannot be updated on any of the three tables, that a garment job with `status = 'Delivered'` and no `delivered_at` is refused while one carrying `delivered_at` in another status is accepted, and that `Down` empties the schema to the history table alone and the migration re-applies cleanly |
| `20260912041738_DesignCatalogue` | `catalog` | Expand | #137 | Applied to local PostgreSQL 16.13 by the integration fixture and by `CatalogMigrationTests.AppliesRollsBackTheDesignTablesAndReAppliesCleanly`, which migrates a scratch database to this migration, down to `20260910215657_CatalogReferenceBreaches` and up again, so `Down` has been executed at least once. `dotnet ef migrations has-pending-model-changes` reports none. Creates `design_option_groups`, `design_option_group_branches`, `design_options` and `design_rules` (`docs/prd/design-options.md` sections 2 to 4; `docs/architecture/module-ownership.md` section 5.3), each with the same `…_key` column the categories carry so that a code change on a published row is detectable across versions. `design_rules` stores each operand flattened — group code, form and a `text[]` of option codes — rather than as a serialised expression, so a rule is queryable and its printed form is always derivable. Four things the model cannot express are explicit SQL, each with a comment saying why: the existing `catalog.published_entries_are_immutable()` trigger attached to `design_option_groups` and `design_rules`, which name their version directly; `catalog.published_design_options_are_immutable()`, a copy permitting the wider list of words an option may correct in place (`name`, `name_tamil`, `help_text`, `illustration_alt`, `display_order`); and `catalog.published_design_group_branches_are_immutable()`, which finds the version through the group because a branch row names none. The scaffolder's output needed no hand correction to its generated part. No personal data: a design option is business configuration (`docs/prd/design-options.md` section 1) |
| `20260912050924_DesignOptionVersionKey` | `catalog` | Expand | #137 | Follow-up to `20260912041738_DesignCatalogue` from the review of #143, carried as its own migration because the first had already merged and `src/Modules/CLAUDE.md` section 6 says an applied migration is never edited. Before it, `design_options` held two independent foreign keys — one to its group and one to its version — so a row naming a published group beside a draft `catalog_version_id` satisfied both, and `published_design_options_are_immutable()` checked the draft version and let the write through. Now `design_option_groups` carries the alternate key `(id, catalog_version_id)` and the option's foreign key is the composite `fk_design_options_design_option_groups_version` over `(design_option_group_id, catalog_version_id)` against it; the single-column key to `catalog_versions` is dropped because the composite implies it. The constraint is named by hand in `CatalogDbContext` because the conventional name exceeds PostgreSQL's 63-character limit and was truncated to one ending in an underscore. The scaffolder's output needed no hand correction. Applied to local PostgreSQL 16.13 by the integration fixture and by `CatalogMigrationTests`, which now migrates a scratch database to this migration, down two steps to `20260910215657_CatalogReferenceBreaches` and up again, so `Down` has been executed at least once; `dotnet ef migrations has-pending-model-changes` reports none. No data movement: the two tables were empty in every environment |
| `20260912061752_InitialBillingSchema` | `billing` | Expand — initial | #145 | Applied to local PostgreSQL 16.13 by the integration fixture and by `BillingMigrationTests.AppliesRollsBackToTheHistoryTableAloneAndReAppliesCleanly`, which migrates a scratch database up, down to nothing (the history table alone, no function left) and up again, so `Down` has been executed at least once. `dotnet ef migrations has-pending-model-changes` reports none. Creates `gst_registrations`, `tax_configuration_versions`, `tax_codes` and `tax_components` (`docs/architecture/module-ownership.md` section 5.8; `docs/architecture/conventions.md` section 1.3), plus the module's own `outbox_messages` and `inbox_messages` from `ModuleDbContext`. Five things the model cannot express are explicit SQL, each with a comment saying why: `ux_tax_configuration_versions_one_published`, a **deferred** exclusion constraint over `(organisation_id) WHERE status = 1` rather than the catalogue's partial unique index — publishing retires the outgoing version and publishes the draft in one save, the two updates run in primary-key order, and a draft older than the version it supersedes would trip a per-statement index before the retirement had landed, so the constraint is judged at commit; `btree_gist` and the exclusion constraint `ex_gst_registrations_one_in_force` over `(branch_id, daterange(effective_from, effective_to, '[]'))`, so that two registrations of one branch are never in force on the same day even when two administrators record them at once; `billing.tax_configuration_versions_are_immutable()`, the version-row trigger (draft → published → retired, one step at a time and never back, nothing but the retirement columns after publication); `billing.published_tax_codes_are_immutable()` and `billing.published_tax_components_are_immutable()`, which refuse every insert, update and delete on a published version's codes and components and judge an update against the parent it leaves as well as the parent it joins, so a row cannot be moved into a draft. The scaffolder's output needed no hand correction to its generated part; the one foreign key whose conventional name exceeded 63 characters is named `fk_tax_codes_tax_configuration_versions` in the model. No personal data: a tax code is configuration and a GSTIN is the business's own registration, printed on every invoice (Public in `docs/nfr/data-classification.md`) |
| `20260912064102_PriceLists` | `billing` | Expand | #146 | Applied to local PostgreSQL 16.13 by the integration fixture and by `BillingMigrationTests`, which migrates a scratch database up through both Billing migrations, down to nothing and up again, so `Down` has been executed at least once. `dotnet ef migrations has-pending-model-changes` reports none. Creates `price_lists`, `price_list_versions`, `price_list_version_branches`, `price_list_items` and `discount_rules` (`docs/architecture/module-ownership.md` section 5.8; `docs/prd/glossary.md` price list; `docs/architecture/conventions.md` section 1.1 — rates are `numeric(18,4)`, the override threshold `numeric(6,3)`). Four things the model cannot express are explicit SQL, each with a comment saying why: `ux_price_list_versions_one_published`, a deferred exclusion constraint over `(price_list_id) WHERE status = 1`, for the same reason the tax configuration's is deferred; `billing.price_list_versions_are_immutable()`, the version-row trigger (draft → published → retired, one step at a time and never back, nothing but the retirement columns after publication); `billing.published_price_list_rows_are_immutable()`, one function on the items, discount rules and branch rows of a published version, refusing every insert, update and delete and judging an update against the parent it leaves as well as the parent it joins; and `ex_price_list_version_branches_one_published`, a deferred exclusion constraint over `(branch_id) WHERE published` on the branch rows — a branch is priced by at most one published version across every list — over a `published` column the model does not map, denormalised from the parent's status by the parent's own `AFTER UPDATE OF status` trigger (`billing.price_list_version_branches_follow_status()`), because an exclusion constraint cannot reach across the join and the application-side check alone is a read-then-write race between two lists. The scaffolder's output needed no hand correction to its generated part; the branch table's foreign key, whose conventional name exceeded 63 characters, is named `fk_price_list_version_branches_price_list_versions` in the model. No personal data: a rate is Confidential business configuration (`docs/nfr/data-classification.md`), which is why every route is on the administration permission |
| `20260912082804_CalculationSnapshots` | `billing` | Expand | #147 | Applied to local PostgreSQL 16.13 by the integration fixture and by `BillingMigrationTests`, which migrates a scratch database up through every Billing migration, down to nothing and up again, so `Down` has been executed at least once. `dotnet ef migrations has-pending-model-changes` reports none. Creates `calculation_snapshots` (`docs/architecture/module-ownership.md` section 5.8): the request and the result of one calculation as `jsonb` with a `schema_version`, the price-list, tax configuration and registration identifiers it used as foreign keys (`RESTRICT`, because a published version is never deleted and a snapshot must stay recomputable), under the caller's reference, unique per organisation. The table deliberately departs from the column convention: no `updated_at`/`updated_by` and no `xmin` token, because a row is written once with `calculated_at`/`calculated_by` and never changes — one thing the model cannot express is explicit SQL with its reason: `billing.calculation_snapshots_are_append_only()`, refusing every `UPDATE` and `DELETE` (`INV-INV-03`, plan D10) in the same shape as `platform.audit_events`. No personal data: a line is keyed by the caller's reference and an item code, never by a customer; the reasons an override or discount carries are staff-typed free text, classed and handled as the audit trail's reason is |
| `20260912160123_Receipts` | `billing` | Expand | #169 | Applied to local PostgreSQL 16.13 by the integration fixture and by `BillingMigrationTests`, which migrates a scratch database up through every Billing migration, down to the history table alone and up again, so `Down` has been executed at least once. `dotnet ef migrations has-pending-model-changes` reports none. Creates `receipts` (`docs/architecture/module-ownership.md` section 5.8): one per payment (a unique foreign key to `payments`, `RESTRICT`), the number `RCPT-<branch>-<FY>-000001` drawn from the gapless `receipt` sequence inside the payment's transaction and unique within the organisation, the opaque `R-` barcode payload unique within the organisation, the financial year, the branch-local date of issue, and the figures frozen at issue — the amount, what was allocated, what was held, what the order still owed — with a check that the amount is allocated plus held. Deliberately departs from the column convention as the payments do: no `updated_at`/`updated_by` pair and no `xmin` token, because a row is written once with `issued_at`/`issued_by` and never changes. What the model cannot express is explicit SQL with its reason: the append-only trigger, which reuses `billing.financial_records_are_append_only()`. No personal data: a receipt names its payment and its branch by identifier and carries no customer detail |
| `20260912151015_Payments` | `billing` | Expand | #162 | Applied to local PostgreSQL 16.13 by the integration fixture and by `BillingMigrationTests`, which migrates a scratch database up through every Billing migration, down to the history table alone and up again, so `Down` has been executed at least once. `dotnet ef migrations has-pending-model-changes` reports none. Creates `payments`, `payment_allocations` and `advances` (`docs/architecture/module-ownership.md` section 5.8): the payment with its mode, amount, the reference the mode required, the idempotency key it arrived with and the open session it was recorded in (a foreign key to `cashier_sessions`, `RESTRICT`); the allocation of an amount to an invoice with its kind (automatic, advance applied, manual) and the advance it came from where it did; and the advance, one per payment at most, the remainder that found no posted invoice. Money is mapped as the invoices map it. Two partial unique indexes are the transactional guard beside the idempotency record — `(organisation_id, mode_code, reference) WHERE reference IS NOT NULL` and `(organisation_id, cashier_id, client_key) WHERE client_key IS NOT NULL` — and a check on each table keeps an amount positive. The three tables deliberately depart from the column convention: no `updated_at`/`updated_by` pair and no `xmin` token, because a row is written once with `recorded_at`/`recorded_by`, `allocated_at`/`allocated_by` or `received_at` and never changes (INV-PAY-01); what an advance still holds is computed from the allocations that name it. What the model cannot express is explicit SQL with its reason: the append-only triggers on the three tables, which reuse `billing.financial_records_are_append_only()` from `InvoicePosting`. No personal data: a payment names its customer and its cashier by identifier; the reference is the terminal's or the bank's transaction identifier, never a card number (refused by the validator before it reaches a row) |
| `20260912134831_CashierSessions` | `billing` | Expand | #161 | Applied to local PostgreSQL 16.13 by the integration fixture and by `BillingMigrationTests`, which migrates a scratch database up through every Billing migration, down to the history table alone and up again, so `Down` has been executed at least once. `dotnet ef migrations has-pending-model-changes` reports none. Creates `payment_modes` and `payment_mode_branches` (configuration: code, name, the reference, provider and refund flags, the active state, and the branches a mode is restricted to — none meaning every branch), unique per organisation and code, seeded by `init-reference-data`; and `cashier_sessions`, `cashier_session_counts` and `cashier_session_mode_totals` (`docs/architecture/module-ownership.md` section 5.8): the float, the status, the count sheet and expected against counted per mode, with a partial unique index over `(branch_id, cashier_id) WHERE closed_at IS NULL` for INV-CSH-01 and a check that a closed row carries its close. `cashier_sessions` deliberately departs from the column convention: no `created_by`/`updated_by` pair, because `cashier_id` is who opened it and `closed_by` who closed it, and no other write exists; the two count tables carry no timestamps and no row version, because a row is written once at the close. Two things the model cannot express are explicit SQL with their reasons: `billing.cashier_sessions_are_immutable()`, which lets a row be inserted open and closed once — the close moving nothing the session was opened with — and refuses every other change and every delete (INV-CSH-05), and the append-only triggers on the two count tables, which reuse `billing.financial_records_are_append_only()`. No personal data: a session names its cashier by identifier |
| `20260912115140_DocumentArtifacts` | `billing` | Expand | #155 | Applied to local PostgreSQL 16.13 by the integration fixture and by `BillingMigrationTests`, which migrates a scratch database up through every Billing migration, down to the history table alone and up again, so `Down` has been executed at least once. `dotnet ef migrations has-pending-model-changes` reports none. Creates `document_artifacts` (`docs/architecture/module-ownership.md` section 5.8): one row per posted document and version, with the opaque object key, the media type, the size, the SHA-256 and the bounded attempt count, a check constraint that a completed row carries all of them and a pending or failed one none, unique over `(kind, document_id, version)` and over the object key, and an index over `(status, requested_at)` for the worker's poll. Four hand-written statements ride with it, each with its reason in the file. (1) `invoices.supplier_legal_name` and `supplier_trade_name`, the supplier's names as issued, frozen with the GSTIN so a registration amended after posting cannot change a rendered statutory document: added nullable, backfilled from the registration the invoice names with `posted_invoices_are_immutable` disabled for that one statement, then the legal name made `NOT NULL`. (2) `adjustment_notes.posted_on` (`date`, the note's business date in the branch calendar, which the credit and debit notes print): added nullable, backfilled as `(posted_at AT TIME ZONE 'Asia/Kolkata')::date` — the one timezone OD-06 has recorded for the launch branches — with `adjustment_notes_are_append_only` disabled for that statement, then made `NOT NULL`. (3) A pending artefact row for every invoice and note posted before the table existed, whose posting events were consumed before the artefact handlers were; the identifier is a UUIDv7 composed in SQL. (4) A repair of the draft revision stamp `InvoicePosting` applied blindly: a draft keeps its order's current revision only where the order fact has not moved since the draft was last priced, otherwise it is set to zero and must be re-priced. `BillingMigrationTests.UpgradesADatabaseThatAlreadyHoldsPostedRecords` applies the migration to a database that already holds a posted invoice, a credit note and two drafts and asserts all four. No trigger for the artefacts, because the row moves from pending to completed by design and the bytes it names are immutable in the store |
| `20260912103135_InvoicePosting` | `billing` | Expand | #154 | Applied to local PostgreSQL 16.13 by the integration fixture and by `BillingMigrationTests`, which migrates a scratch database up through every Billing migration, down to the history table alone and up again, so `Down` has been executed at least once. `dotnet ef migrations has-pending-model-changes` reports none. Adds the posting columns to `invoices` (`invoice_number`, `barcode_payload`, `financial_year`, `posted_on`, `posted_at`, `posted_by`, and `order_revision_number`, the order's revision the draft was last priced against, which posting checks the order is still at), the number and the payload unique within the organisation over the posted rows, and widens `ck_invoices_lifecycle_is_consistent` so a posted row carries them and nothing else does; creates `invoice_cancellations`, `adjustment_notes`, `adjustment_note_lines` and `adjustment_note_taxes`. What the model cannot express is explicit SQL with its reason: `billing.posted_invoices_are_immutable()` (a draft moves to posted or discarded and nowhere else; a posted row never changes, not even its last-touched pair, and is never deleted), `billing.posted_invoice_rows_are_immutable()` on the three line tables (nothing inserted, changed or deleted once the invoice is no longer a draft, the invoice's own status trigger at depth two excepted), and `billing.financial_records_are_append_only()` on the four new tables, all raising `restrict_violation` in the shape of the price-list triggers; and `billing.invoice_cancellations_release_jobs()`, which on a cancellation moves the lines' denormalised status to 3 so the jobs leave the one-live-invoice-per-job index. Numbers are drawn from `platform.sequences` through the persistence layer's transactional allocator, on this module's transaction and under the invoice row's lock, in a scope qualified by the organisation, so a post that rolls back returns its number |
| `20260912092348_InvoiceDrafts` | `billing` | Expand | #153 | Applied to local PostgreSQL 16.13 by the integration fixture and by `BillingMigrationTests`, which migrates a scratch database up through every Billing migration, down to nothing and up again, so `Down` has been executed at least once. `dotnet ef migrations has-pending-model-changes` reports none. Creates `order_facts` and `order_fact_jobs` (what Billing knows about an order and its garments, cancellations included, written by the outbox consumers only; keyed by the order's and the job's own identifiers and carrying no `created_by`, `updated_by` or `xmin`, because nobody but the consumers writes them and the consumers are idempotent — a recorded deviation from the module guide's column set, as `calculation_snapshots` is), `invoices`, `invoice_lines`, `invoice_line_surcharges` and `invoice_tax_components` (`docs/architecture/module-ownership.md` section 5.8). A line is keyed by `(invoice_id, garment_job_id)`, the job it charges for, with `line_number` a plain column: a re-price that drops, adds or reorders lines updates a kept job's row where it is, so neither the live-job index below nor any constraint on the number sees a job move between rows. Money is mapped as Orders maps it: an amount at `numeric(18,4)` beside its three-letter currency. What the model cannot express is explicit SQL with its reason: an `invoice_status` column on `invoice_lines` that the model does not map, denormalised from the parent by the parent's `AFTER UPDATE OF status` trigger (`billing.invoice_lines_follow_status()`) and taken from the parent on insert (`billing.invoice_lines_take_parent_status()`), because a partial index cannot reach across the join. Over it, `ux_invoice_lines_live_garment_job` is a partial unique index on `(garment_job_id) WHERE invoice_status IN (0, 1)` — a garment job is charged on at most one draft or posted invoice — created after the column and declared on the model as well, so that the change tracker orders the commands of one save around it (a re-price that moves a job to another line number deletes the old holder first). Personal data: `invoices.customer_*` is the customer as the document names them, classed and handled under `docs/nfr/data-classification.md` section 5.10 — kept as issued, never edited, excluded from deletion. Posting, numbering and the freeze arrive with E09-F02-2 |

## Checklist

- [ ] The migration touches only its own module's schema.
- [ ] It is expand-only, or the thing it contracts was expanded at least two releases ago.
- [ ] `Down` exists and was executed at least once.
- [ ] Explicit SQL carries a comment explaining why the model could not express it.
- [ ] It applies to an empty database and to the N-1 snapshot.
- [ ] Any new column holding personal data is classified in `docs/nfr/data-classification.md`.
