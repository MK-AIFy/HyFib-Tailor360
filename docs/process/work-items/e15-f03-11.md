Sub-issue of #61. The last part of the plan's **#61c** and the closing pull request of epic **E15**: the go/no-go
record, hypercare and the support rota, the post-launch review schedule, and the release evidence index that every
other document has been pointing at. Three places already name the missing file:
`docs/process/release-gates.md` line 275 ("The release record itself, indexed in
`docs/launch/release-evidence-index.md` (#61c)"), `docs/process/release-evidence.md` line 25 and its line 179. Two
rows of the release evidence checklist are attributed to this work and cannot be filled today — `RE-15` (the
user-acceptance sign-off link and the accountant's approval of the GST output) and `RE-16` (the go or no-go record
naming the decision owner, the rollback decision owner and the criteria used). `ls docs/launch` and `ls docs/evidence`
both fail.

## What is already built (do not re-build)

- `docs/process/release-evidence.md` — the sixteen-row form with `RE-01` to `RE-16`, its rules (an item that does not
  apply is marked `n/a` with a reason and never deleted; an unfinished row blocks the release; `RG-14` carries no
  waiver) and its section 8, the go-or-no-go section `RE-16` points at. **The form exists. This sub-issue writes the
  index that finds a completed copy later, and the go/no-go document the form's own section 8 expects.**
- `docs/process/release-gates.md` — `RG-01` to `RG-14`, their severities and owners, section 6's sixteen evidence
  items with who produces and who confirms each, section 7's failure procedure and section 9's open decisions
  `RG-OD-01` to `RG-OD-04`.
- `docs/nfr/security-operations-targets.md` — the incident path, the human-report requirement (line 276, under
  `OD-15`) and line 366's commitment that the arrangement is reviewed "at the end of hypercare (#61c) against the
  incidents actually seen".
- `docs/nfr/slo.md` — the service window, the service-level indicator catalogue, the objectives, the recovery
  objectives of section 6, section 10's error-budget policy and section 13's compatibility statement for the Owner to
  sign. **Hypercare exit criteria are expressed against these rows and never restate a figure.**
- `docs/nfr/traceability.md` — the `NFR-…` rows, their gates, owners and statuses, section 21's coverage summary and
  section 22's list of targets no automated proof will ever cover. The evidence index is the outward-facing view of
  this table for one release.
- `docs/IMPLEMENTATION_PLAN.md` Section 2.3 — the release-level acceptance criteria of roadmap issue #1, and Section
  6.3, which already maps each of them to the issues that verify it. Section 6.4 lists the E15 exit criteria ("All
  acceptance gates and UAT pass", "Ops ownership, monitoring, training, hypercare active") and states the closing
  rule for every epic: "The last PR of each epic adds `docs/evidence/eXX-closure.md` collecting the links, and the
  roadmap checklist item in #1 is ticked in the same PR."
- `docs/qa/regression-report.md` ([E15-F03-6b]), `docs/qa/records/dress-rehearsal-001.md` ([E15-F03-7]),
  `docs/uat/sign-off.md` ([E15-F03-8], extended by [E15-F03-8b]), `docs/uat/accountant-approval.md` ([E15-F03-8b]),
  `docs/launch/pilot-report.md` ([E15-F03-10]). **These are the artefacts this sub-issue links and files; it does not
  produce their content.**
- `docs/process/waivers.md` — the dated, owned waiver register the go/no-go reads.
- `docs/prd/00-overview.md` and `docs/prd/traceability.md` — the product outcomes and their mapping, for the index's
  "what this release delivers" column.

## What is missing

- No go/no-go document, so the decision that matters most has no named owner, no criteria and no record — and `RE-16`
  cannot be filled.
- No hypercare definition: no duration, no staffing, no exit criteria, nothing that says when the launch is over.
- No support rota, although `docs/nfr/security-operations-targets.md` already requires a reachable human path.
- No post-launch review schedule, so the parent's third required verification ("post-launch review schedule for
  incidents, adoption, turnaround, rework, stock variance and financial reconciliation") has no artefact.
- No release evidence index, so three documents point at a file that does not exist and a completed release record
  would be unfindable a year later — which is precisely the failure `docs/process/release-evidence.md` says it exists
  to prevent.
- No `docs/evidence/e15-closure.md`, which the plan's epic-closure rule requires from this pull request.

## Scope

- [ ] Add `docs/launch/go-no-go.md`: the decision record — the build and image digest; the gate status table, one row
      per `RG-nn` with its outcome, its confirmer and its evidence link; the active waivers with expiry; the
      criteria the decision is made against; **a named holder for each of the four owner roles the plan's #61c
      blueprint requires — business, technical, security and operations** (the parent's fifth acceptance criterion
      says "go-live has named owners", plural); the **named rollback decision owner** with the rollback criteria and
      the window in which it may be taken; the attendees by role; the decision; and the conditions attached to a
      "go". Unfilled, with a worked example row so the form cannot be misread. Two of the four holders are undecided —
      `RG-OD-02` leaves the security owner and deputy open and `docs/dev/staging.md` records the operations owner as
      unfilled — so the form carries the role, the gap and the decision that owes it, never a guess.
- [ ] Add `docs/launch/hypercare.md`: what hypercare is, who is on it, the daily rhythm, what is watched (the
      service-level indicators of `docs/nfr/slo.md` section 4, by identifier), the **support rota** with its
      escalation ladder and out-of-hours arrangement, the defect route during hypercare (reusing
      `docs/launch/defect-triage.md`'s severities, not a new scale), and the **exit criteria**: duration,
      service-level attainment against the cited `docs/nfr/slo.md` rows, open-defect thresholds by severity, and the
      review of the arrangement that `docs/nfr/security-operations-targets.md` line 366 already commits to. Every
      name and channel is a placeholder naming **OD-15**; every duration and threshold is either cited or named as
      open.
- [ ] Add `docs/launch/post-launch-review.md`: the schedule — weekly for four weeks, then monthly, as the plan's
      #61c blueprint states, marked *proposed, to be confirmed* — with the agenda and the key indicators the plan
      names: incidents, adoption by role, turnaround, rework rate, stock variance, financial reconciliation. For each
      indicator: where the number comes from (the #44 to #46 reports, by name), who brings it, and what a bad number
      triggers. No target value is set here; a target is a product decision.
- [ ] Add `docs/launch/release-evidence-index.md`: the index the other three documents already point at. One section
      per release, and within it two tables — (a) every release-level acceptance criterion of roadmap #1 (plan Section
      2.3) with the evidence that satisfies it and the gate that enforced it, using plan Section 6.3's existing
      mapping; and (b) every priority-zero and priority-one journey with its evidence: the regression report row, the
      user-acceptance script, the pilot record row, and the physical evidence where `RG-05`'s note requires it. Plus
      a short "how to find a release's evidence a year from now" preamble.
- [ ] File the completed human artefacts this sub-issue is the home for: the `docs/uat/sign-off.md` links and
      `docs/uat/accountant-approval.md` for `RE-15`, and `docs/launch/pilot-report.md` for the pilot's exit-criteria
      judgement. **Filed, not authored**: the rows come from the people who ran them.
- [ ] Add `docs/evidence/e15-closure.md` per plan Section 6.4: the E15 exit criteria with their evidence links, the
      architecture and security review findings and their resolution, and the place the product owner's acceptance
      comment lives. The pull request also ticks the matching roadmap checklist item in #1, as that rule requires.
- [ ] Update the status cells in `docs/nfr/traceability.md` that this release's evidence now satisfies — at minimum
      `NFR-CU-06` (real labels and devices, once the pilot record carries the physical evidence) — and add the
      `docs/launch/` rows to `docs/launch/README.md`.

## Out of scope

- **Making the go/no-go decision, running hypercare, or holding a post-launch review.** Those are the Owner's, with
  the named owners the documents identify. This sub-issue builds the records and files what has already been
  produced; it does not decide.
- Deploying the release. #59's workflow does that, and `RG-14` refuses to run without a completed evidence record —
  which is the point of the index.
- Writing or changing a gate, a severity, a service-level objective, a recovery objective or a waiver rule. All
  cited.
- The pilot plan, its criteria and the defect-triage rules — [E15-F03-10]. The user-acceptance scripts and the
  accountant document — [E15-F03-8]. The training and support-contact material — [E15-F03-9].
- Monitoring, dashboards and alert configuration — #58. Hypercare *watches* them by identifier.
- Closing any open decision. The go/no-go **records** which decisions are still open at launch and what default is in
  force, which is itself one of its rows.

## Acceptance criteria

- [ ] `docs/launch/go-no-go.md` has a row for every `RG-nn` of `docs/process/release-gates.md` section 4 (`RG-01` to
      `RG-14`), a field for each of the four owner roles — business, technical, security, operations — and a rollback
      decision owner distinct from the decision owner. Where a holder is undecided the field names the open decision
      (`RG-OD-02` for the security owner, `OD-15` for operations) instead of a name.
- [ ] `docs/launch/release-evidence-index.md` covers **every** release-level acceptance criterion of plan Section 2.3
      and every priority-zero journey of `RG-05`, with no row lacking either an evidence link or an explicit `n/a`
      with a reason. A reviewer can follow each link and land on a real artefact.
- [ ] `docs/launch/hypercare.md` states its exit criteria in terms that can be judged: a duration, a service-level
      attainment citing the `docs/nfr/slo.md` row, and an open-defect threshold by severity using the existing `S1`
      to `S3` vocabulary.
- [ ] `docs/launch/post-launch-review.md` names, for each of the six indicators, the report it is read from and who
      brings it.
- [ ] Negative case: the documents state what a **no-go** looks like — who declares it, what happens to the release
      candidate, what is communicated to the shop, and what must be true before the decision is retaken — and that a
      no-go is a normal outcome rather than a failure of the process.
- [ ] Negative case: the go/no-go states that an unfinished `RE-nn` row blocks the release with no waiver (`RG-14`),
      and that a gate with an expired waiver is a blocked gate.
- [ ] Exception case: the rollback decision is recorded as its own row — who may take it, on what signal, within what
      window, and what is lost if it is taken after the shop has taken orders on the new release. It cites the
      rehearsal record of [E15-F03-7] as the evidence that the rollback works.
- [ ] Exception case: hypercare states what happens when its exit criteria are not met at the end of the planned
      duration — extend, reduce scope, or accept with a dated waiver owned by the Owner — and that a data-integrity
      threshold has no waiver.
- [ ] `docs/evidence/e15-closure.md` exists with a row per E15 exit criterion from plan Section 6.4, each with an
      evidence link.
- [ ] `./scripts/dev docs` passes. Every link in all five new documents resolves — this is the criterion that proves the
      index is real rather than aspirational.

## Data and classification

Per `docs/nfr/data-classification.md` section 2. This sub-issue's risk is not new data: it is an index that could
accidentally publish what the rest of the system is careful about.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| The release evidence index | Internal | Find a release's evidence later | As the repository; one section per release, never pruned | Anyone with repository access | **Links only.** No evidence is inlined, because an inlined screenshot or report would carry live data into the repository |
| The go/no-go record | Internal | Prove who decided what, against which criteria | With the release record; see the audit retention row of `docs/nfr/data-classification.md` section 5.11 | Owner, technical reviewer, security owner, operations owner | Gate outcomes, waiver identifiers, names and roles of attendees. No customer data, no amounts |
| The support rota in `hypercare.md` | **Personal** (contact details) | Reach a human | With the release record | All staff during hypercare | Placeholders in the repository naming **OD-15**; real contacts live in the printed and release copies only |
| The post-launch indicator values | Internal, and **Financial** for the reconciliation indicator | Judge adoption and stability | With the review records | Owner, Branch Manager, technical reviewer | Aggregates read from the #44 to #46 reports. Never a per-customer figure, never a named staff member's productivity without the Owner's decision recorded — the schedule notes that adoption by role is a role aggregate, not a person |
| `docs/evidence/e15-closure.md` | Internal | Close the epic | As the repository | Anyone with repository access | Links and outcomes |

## Permissions and roles

No permission key is added. What this sub-issue does name is **who is accountable at launch**, and two of those
holders are undecided.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| The go or no-go decision (a governance right, not an application permission) | organisation | — | — | yes | The Owner | **Exists as a role, unnamed in any document.** This sub-issue records the name in the form; the right is the Owner's per `docs/prd/raci.md` |
| The rollback decision | environment | — | — | yes | The release manager, with the Owner | **Open — `RG-OD-02`** leaves the security owner and deputy undecided and `docs/dev/staging.md` section 1 records the operations owner role as unfilled. The document names the holder and marks the gap |
| Hypercare paging and escalation | organisation | — | — | — | **Open — OD-15** | **Open.** Placeholders only, with the human-report path of `docs/nfr/security-operations-targets.md` line 276 as the default in force |
| Application permissions during hypercare | branch / organisation | per the matrix | per the matrix | per the matrix | As published | **Exists**; **OD-13** open, so the grants in force at launch are proposed and the go/no-go has a row recording that |

## Modules, contracts and events

No module, no schema, no contract, no event, no source file. Documentation only, so no `ARCH-…` rule and no `MO-…`
boundary is engaged.

DOR-05: the go/no-go carries one security row of its own — "no unresolved critical or high security finding", the
parent's second acceptance criterion — whose evidence is #56b's penetration-test report and remediation list and the
`RG-08` to `RG-11` scan summaries, with the threat-model coverage table of `docs/security/README.md` section 3.2 as
the completeness check. It introduces no flow.

## Screens and states

Not applicable: no client change. Stated rather than deleted (`DOR-14`). The index does record, per release, the
browser matrix the release was accepted on, because plan Section 2.3 makes that matrix a release criterion.

## Migration and rollback

No schema change and no migration: five new documents (`go-no-go.md`, `hypercare.md`, `post-launch-review.md`,
`release-evidence-index.md`, `docs/evidence/e15-closure.md`), appended rows in `docs/launch/README.md`, and a small
number of `docs/nfr/traceability.md` status cells. `migrate --dry-run` reports no
outstanding migration. Rollback of this pull request is `git revert`. The rollback of the **release** is #59's
mechanism, rehearsed by [E15-F03-7], and the go/no-go records who may take it and when — which is the one place in
the repository where that is written down.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-15** — operations ownership and the alert channel | **Open** | The rota names placeholders and records the human-report path as the minimum; the go/no-go has a row stating that launching with it open is a decision the Owner takes explicitly | The go/no-go meeting itself |
| **RG-OD-02** — who holds the security owner role and who deputises | **Open** | The technical reviewer confirms the security rows with the gap recorded, under the single-maintainer exception of `docs/process/branch-protection.md` section 5.1 | The go/no-go meeting itself |
| **RG-OD-01**, **RG-OD-03**, **RG-OD-04** — the proposed severities and waiver durations, the `RG-13` restore freshness, the release cadence | Open | As published and marked proposed; the go/no-go quotes them as the criteria it used | The go/no-go meeting itself |
| **OD-02** — hosting model and indicative budget | Open | Whatever #59 provisioned; the index records the model the release was accepted on | The go/no-go meeting itself |
| **OD-04**, **OD-05**, **OD-09**, **OD-13**, **OD-16** | Open | Each has a row in the go/no-go's "decisions open at launch" table, with the default in force and the issue that would close it. **Launching with an open decision is a recorded choice, not an oversight** | As each row states |
| **OD-26** — the pilot's volume, window and branch ([E15-F03-10]) | Open | The exit-criteria judgement in the pilot report, whatever the owner set | The go/no-go meeting itself |
| The weekly-for-four-weeks-then-monthly review cadence, and the hypercare duration | Proposed, to be confirmed | The plan's own proposal, quoted and marked proposed. **No duration or threshold is invented** | The first post-launch review |
| Every service-level and recovery figure | As published in `docs/nfr/slo.md` | Cited by row, never restated | — |

## Required verification

- [ ] Documentation: `./scripts/dev docs` clean, and the `documentation` job of `.github/workflows/ci.yml` green. For
      this sub-issue the link check **is** the main verification: an evidence index whose links do not resolve is
      worse than none.
- [ ] Completeness check: every release-level acceptance criterion of plan Section 2.3 and every priority-zero
      journey of `RG-05` has a row in the index. Pasted as a checklist in the pull request.
- [ ] Completeness check: every `RG-nn` has a row in the go/no-go, and every `RE-nn` of
      `docs/process/release-evidence.md` is reachable from the index. Pasted.
- [ ] Cross-check: every `docs/nfr/slo.md` and `docs/architecture/invariants.md` identifier cited exists — `grep`
      output pasted.
- [ ] Review walkthrough: a reviewer picks two criteria at random from the index and follows the links to the
      artefacts, confirming they answer the criterion.
- [ ] `DOR-16`: the environment needed is the repository and the completed records of children [E15-F03-6b],
      [E15-F03-7], [E15-F03-8], [E15-F03-8b], [E15-F03-9] and [E15-F03-10]; the physical hardware requirement `docs/process/definition-of-ready.md` line 86 attaches to #61c
      was met by the pilot, and the index cites that evidence rather than reproducing it.
- [ ] `DOR-13`: this sub-issue edits the status cells of the `docs/nfr/traceability.md` rows whose evidence the
      release now carries, starting with `NFR-CU-06`, and records in the index which rows remain "evidence only" per
      section 22.

## Evidence the pull request will carry

- [ ] The rendered diff of the five new documents, the appended `docs/launch/README.md` rows and the traceability
      status cells.
- [ ] `./scripts/dev docs` output, clean.
- [ ] The three completeness checklists (release criteria, priority-zero journeys, gates and evidence rows).
- [ ] The identifier `grep` cross-checks.
- [ ] The reviewer's two-criterion link walkthrough.
- [ ] The filed links to `docs/uat/sign-off.md`, `docs/uat/accountant-approval.md`,
      `docs/launch/pilot-report.md`, `docs/qa/regression-report.md` and
      `docs/qa/records/dress-rehearsal-001.md`, each named in the index.
- [ ] The note that the roadmap checklist item in #1 is ticked in this pull request, as plan Section 6.4 requires of
      an epic's last pull request.
- [ ] The pull-request template with items 1, 3, 6, 8 and 9 ticked; items 2, 4, 5 and 7 deleted with the one-line
      reason "documentation only: no endpoint, no migration, no contract, no screen".

## Depends on

#[E15-F03-10] — the completed `docs/launch/pilot-report.md` with its exit-criteria judgement, and
`docs/launch/README.md`, whose rows this sub-issue extends.

#[E15-F03-8] and #[E15-F03-8b] — the nine-row `docs/uat/sign-off.md` and `docs/uat/accountant-approval.md`, which
`RE-15` requires, signed during the pilot window and filed here.

#[E15-F03-7] — the dress-rehearsal record, which the rollback row cites as its evidence.

#[E15-F03-6b] — `docs/qa/regression-report.md`, which `RE-06` and the index's journey table cite, and the four
`NFR-CU-…` status cells it has already updated. This sub-issue edits the **remaining** rows only, so the two never
touch the same cell.

#[E15-F03-9] — the training and support material the hypercare rota and the support path point at.

#56b — the penetration-test report and remediation, without which the "no unresolved critical or high finding" row
cannot be answered. #58 — the dashboards, alerts and runbooks hypercare watches, and the service-level measurements
its exit criteria are judged against. #59 — the release workflow and its evidence enforcement (`RG-14`). #60 — the
restore verification record `RE-12` needs.

Every other child of #61, and every other feature issue of the roadmap: this is the closing pull request of E15 and
of the programme, and the index it adds is the statement that all of it has evidence. **Not** parallel-safe with any
child that touches `docs/launch/` or `docs/nfr/traceability.md`; it is the last to merge by design.

Refs #61
