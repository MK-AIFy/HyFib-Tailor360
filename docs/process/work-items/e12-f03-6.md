Sub-issue of #52. The parent's sixth implementation step — "Set JS/CSS/image budgets plus LCP, INP, CLS, API latency
and memory targets for representative mobile network/device" — and the measurement half of its fourth acceptance
criterion. The targets are all written and **nothing measures them**: `docs/nfr/capacity-and-performance.md`
section 3.3 fixes thirty budgets with a "Gate" column naming `size-limit` for **four** of them (initial JavaScript
bundle, per-route chunk, barcode decoder chunk, CSS) and Lighthouse CI for **six** (Largest Contentful Paint,
Interaction to Next Paint, Cumulative Layout Shift, Time to First Byte, first-load transfer, font subsets), and
neither tool is in the repository — `grep -n "size-limit\|lhci\|lighthouse" clients/pwa/package.json`
returns nothing, there is no budget file anywhere, and `.github/workflows/ci.yml` has ten jobs and no performance
job. `docs/process/release-gates.md` `RG-07` names "the before-and-after profile in
`docs/nfr/performance-report.md`" as its evidence and that file does not exist. The plan is explicit that this
comes before the optimisations — "**Optimisations (measured first, then applied)**" — which is why this sub-issue
measures and **[E12-F03-7]** changes.

## What is already built (do not re-build)

- `docs/nfr/capacity-and-performance.md` section 3 — the whole budget set, already decided and already marked
  "proposed, to be confirmed". Section 3.1 is the measurement contract: every client budget is a target **on the
  reference device over the emulated 4G profile** of `support-matrix.md` section 2, tracked in the lab by
  Lighthouse CI and in the field by the telemetry module, "and where the two disagree the field wins"; budgets
  apply **per route, not as a site average**. Section 3.3's rows this sub-issue turns into gates: initial
  JavaScript ≤ 180 KB gzip and ≤ 550 KB uncompressed, per-route chunk ≤ 60 KB gzip, the barcode decoder chunk
  ≤ 120 KB gzip as a declared exception, CSS ≤ 40 KB gzip, first-load transfer ≤ 350 KB over the wire, Tamil and
  Latin font subsets ≤ 120 KB with `font-display: swap`, Largest Contentful Paint p75 ≤ 2.5 s, Interaction to Next
  Paint p75 ≤ 200 ms, Cumulative Layout Shift p75 ≤ 0.10, Time to First Byte p75 ≤ 800 ms, and the two memory
  ceilings. **Quote the row; never restate a figure as this sub-issue's own, and never change one.**
- `docs/nfr/support-matrix.md` section 2 — the reference device and the throttling profile in exact numbers
  (Android 10, 4 GB, 360 CSS px, device pixel ratio 2, emulated 4G at 9 Mbit/s down, 1.5 Mbit/s up, 170 ms
  round-trip, "applied by Lighthouse CI and the Playwright network profile"). The Lighthouse configuration is
  derived from this row and from nowhere else.
- `docs/nfr/capacity-and-performance.md` section 8 — the traceability table naming, per budget family, the monitor
  and the **evidence artefact**: "Lighthouse CI report stored with the release", "`size-limit` output in the
  pull-request check", "Trend chart plus the on-device measurement in the release evidence". That is the artefact
  list this sub-issue produces.
- `docs/process/release-gates.md` `RG-07` — what the gate checks, that its blocking severity is **S2 with a waiver
  possible**, that a breach on the scan or capture screen is **not** waivable because "it is felt at the counter and
  in the workshop on every order", that a regression pushing a priority-zero journey past its target is treated as
  S1 in practice, and that the technical reviewer owns the waiver.
- `clients/pwa/vite.config.ts` — `build.sourcemap: true` and the `manualChunks` function that already emits
  `vendor-intl`, `vendor-router` and `vendor`. This sub-issue **measures** those chunks and changes neither the
  function nor the router; changing them is **[E12-F03-7]**.
- `tests/e2e/` and its nine projects, from **[E12-F03-1b]** — the memory probe is a Playwright test in that project,
  on the Chromium project only, because `Performance.getMetrics` is a Chrome DevTools Protocol call. The seeded
  accounts it signs in as are **[E12-F03-1a]**'s.
- `clients/pwa/vite.config.ts` lines 29 to 46 — `VitePWA` is **already configured**, with
  `globPatterns: ['**/*.{js,css,html,svg,png,webmanifest}']` and `injectRegister: null`. So the build **emits**
  `sw.js` and a `workbox-*.js` runtime and a precache manifest that list every chunk, even though nothing registers
  a worker yet (#51). Two consequences this sub-issue must handle rather than trip over: a `size-limit` entry must
  not count the unregistered worker files against the initial-bundle row, and the Lighthouse "first-load transfer"
  figure must exclude them too, because no browser fetches them today. Say which files were excluded and why.
- `.github/workflows/ci.yml` — the job shape: pinned action SHAs, `if: ${{ !cancelled() && … }}` step gating, and
  `actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1` at lines 263, 272 and 420. The
  `build-test-pwa` job (line 337) already builds the client; copy how it installs Node and pnpm.
- `scripts/dev` lines 381 to 393 — `./scripts/dev build` copies `clients/pwa/dist` into the web host's `wwwroot`,
  which is the artefact Lighthouse must measure. Measuring the Vite dev server would measure unminified modules and
  prove nothing.

## What is missing

- No `size-limit` configuration, so four budget rows have a named gate and no gate; with the six Lighthouse rows
  that is **ten** of the thirty that this sub-issue can close. The other twenty are server-side, scanner or capacity
  rows and belong to #58, #36 and the quarterly capacity review.
- No Lighthouse CI configuration and no budget file, so the three Core Web Vitals, Time to First Byte, the
  first-load transfer and the font-subset budgets are unmeasured in the lab.
- No memory probe, so the two heap ceilings are unmeasured.
- No `docs/nfr/performance-report.md`, so `RG-07` has no evidence artefact to point at and **[E12-F03-7]** has no
  "before" to compare against.
- No performance job in CI, so a regression lands silently.

## Scope

- [ ] Add `size-limit` and `@size-limit/preset-app` to `clients/pwa/package.json` as devDependencies, with a
      `size-limit` section declaring one entry per budget row of section 3.3 that names `size-limit` as its gate:
      the entry chunk, each route chunk, the CSS total and the vendor chunks. Each entry carries the row it
      implements as a comment, and each limit is **the figure from that row, quoted, not chosen**. The barcode
      decoder chunk's entry is written now with a comment that it is the declared exception of section 3.3 and that
      the chunk itself arrives with **#36**; it does not fail while the chunk does not exist.
- [ ] Add `"size": "size-limit"` to `clients/pwa/package.json` scripts, and name it in
      `clients/pwa/CLAUDE.md` section 8's pre-pull-request list and in `docs/dev/commands.md` beside the client
      commands, so the check is one command a contributor can run.
- [ ] Add `clients/pwa/lighthouserc.json` (or `.cjs` if the assertion functions need it) with: the throttling
      profile read from `support-matrix.md` section 2 in exact numbers, the mobile form factor at 360 CSS px and
      device pixel ratio 2, three runs per URL with the median taken, and assertions for Largest Contentful Paint,
      Cumulative Layout Shift, Time to First Byte, total byte weight and the font-subset weight, each quoting its
      row.
- [ ] **Settle the URL list and the session before writing the configuration, because most routes need one.**
      Five routes are reachable without a session — `/sign-in`, `/install`, `/about`, `/settings/display` and an
      unmatched path — and every other route is behind `RequireSession` or `RequirePermission`, which Lighthouse
      cannot pass by fetching a URL. The list is therefore: those five, plus **one representative authenticated
      route per feature area** — `/` (home), `/admin/users`, `/measurements`, `/billing/outstanding` — collected
      with a Puppeteer script that signs in once through the real screens using **[E12-F03-1a]**'s seeded account
      and then hands the authenticated page to Lighthouse. Nine URLs × three runs is a gate that finishes; one
      entry per route in **[E12-F03-2]**'s coverage list is twenty-five URLs × three runs on a throttled profile,
      which is forty minutes or more and is the nightly sweep that `support-matrix.md` section 9 asks for and that
      `ci.yml` has no `schedule:` to run (#59). Record both facts in the report. Section 3.1's "budgets apply per
      route, not as a site average" is honoured by the gate being **per URL**, not by measuring every URL on every
      pull request — and the report says which routes are unmeasured and why.
- [ ] If the Puppeteer sign-in proves unworkable on the runner, fall back to measuring the five unauthenticated
      routes with Lighthouse and the four authenticated ones with a Playwright trace on the same throttling
      profile, and **say so in the report** — do not quietly drop the authenticated routes and report a pass over
      the public ones.
- [ ] Add the Lighthouse **budget file** (`clients/pwa/lighthouse-budgets.json`) with the resource-size and
      resource-count budgets of section 3.3: script, stylesheet, font, image and total. The two files are changed
      together with the document row they implement, or not at all.
- [ ] Record how **Interaction to Next Paint** is obtained, because it is not a cold-load metric: section 3.1 says
      it is measured "in the field per route and device class; lab through the Lighthouse **user flow** for the scan
      and measurement journeys". Implement the measurement journey's user flow — the only one of the two whose
      screens exist at HEAD — and record in the report that the scan journey's flow arrives with **#36**. Do not
      substitute Total Blocking Time for Interaction to Next Paint and call the row proved.
- [ ] Add `tests/e2e/specs/memory.spec.ts`, tagged `@memory`, Chromium only: drive the heaviest screens that exist
      at HEAD — `clients/pwa/src/routes/catalog/CatalogDesignRoute.tsx` (1,269 lines) and
      `clients/pwa/src/routes/catalog/CatalogVersionEditorRoute.tsx` (994 lines), both reachable on the
      `init-reference-data` catalogue; `MeasurementDraftRoute.tsx` (1,020 lines) needs a draft and therefore a
      customer, which `seed-synthetic` does not write, so it is named as unmeasurable today rather than attempted —
      through a repeated interaction,
      sampling the JavaScript heap through `Performance.getMetrics`, and assert **no upward trend** across the
      repetitions. The two absolute ceilings of section 3.3 are for the **scan** and **capture** screens, which do
      not exist at HEAD; the spec asserts the trend it can assert and the report records that the two ceilings stay
      scheduled against **#36** and **#189**. This is the honest reading of a budget whose subject is unbuilt.
- [ ] Add `docs/nfr/performance-report.md`: the measurement contract in one paragraph (citing section 3.1, not
      restating it), the reference device and profile by citation, then one table per budget family with columns
      budget, row in `capacity-and-performance.md` section 3.3, **baseline measured now**, target, verdict, and the
      date and commit measured. Add the "after" column header with every cell `—`, for **[E12-F03-7]** to fill.
      Record every budget the repository cannot yet measure and why, naming the issue that will make it
      measurable — the scan and capture memory ceilings, the decoder chunk, the scan round trip, and every
      server-side row, which belongs to **#58** and `tests/load`. Record one budget as **configured but vacuous**:
      the "Tamil and Latin font subsets ≤ 120 KB with `font-display: swap`" row measures **zero bytes**, because
      `grep -rn "@font-face" clients/pwa/src` finds nothing and `clients/pwa/public` holds only `icons`,
      `manifest.webmanifest` and `screenshots` — no web font is loaded at all. A green cell there means nothing and
      the report must say so, naming **#50**.
- [ ] Link `docs/nfr/performance-report.md` from the Related-documents table of
      `docs/nfr/capacity-and-performance.md` section 11 and from `docs/nfr/traceability.md`, so
      `./scripts/dev docs` sees it and the `documentation` job stays green.
- [ ] Add one job, `performance-budget`, to `.github/workflows/ci.yml`: install Node and pnpm the way
      `build-test-pwa` does, `pnpm --dir clients/pwa build`, run `size-limit`, then bring up the stack the way the
      `end-to-end` job of **[E12-F03-1b]** does — including `seed-synthetic` with `TAILOR360_SEED_PASSWORD`, because
      the authenticated Lighthouse runs need an account — run Lighthouse CI against the served `wwwroot`, and upload
      the
      `size-limit` output, the Lighthouse reports and the memory samples with the pinned
      `actions/upload-artifact` SHA. **The workflow's existing triggers are not changed**: `ci.yml` runs on
      `pull_request` to `main` and `push` to `main` (lines 27 to 33) and carries no `schedule:`; a nightly cadence
      is a separate workflow file and a separate issue.
- [ ] Record the cadence decision in the pull request rather than inventing one: **CP-11**'s default in force is
      "every pull request, as a fast subset of the performance gate that plan Section 5.3 places at release", so
      both checks run per pull request and both block at `RG-07`'s **S2**. If a vital proves unstable across runs
      on a hosted runner, that is a finding raised as a new issue with the measured variance, **not** a threshold a
      session relaxes.

## Out of scope

- Every optimisation. Route-level code splitting, lazy chunks, list virtualisation and the font strategy are
  **[E12-F03-7]**, which is why this sub-issue ends with a baseline and a verdict column full of whatever the truth
  currently is — including `FAIL`. A red budget on merge is the expected outcome of a measurement sub-issue and is
  recorded in the report and as a new issue, not fixed here.
- The k6 load scenarios, the API latency percentiles, the outbox and projection lag rows, and `tests/load` —
  **#58**. `RG-07` also consumes those; this sub-issue produces the client half and the report says which half.
- The field half of every Core Web Vital — **[E12-F03-5]** and **#58**.
- The scan and capture screens and their two absolute memory ceilings — **#36** and **#189**.
- Image optimisation and responsive derivatives. The `srcset` and derivative work of section 3.3's image rows
  depends on **#31**'s derivative pipeline, which is open; the budget file carries the image budget and the report
  records that nothing in the client emits a derivative yet.
- Any change to `clients/pwa/vite.config.ts` or `clients/pwa/src/app/router.tsx`.

## Acceptance criteria

- [ ] `pnpm --dir clients/pwa size` runs against a real build and prints one line per configured entry with its
      measured size and its limit, and every limit equals the figure in its row of
      `docs/nfr/capacity-and-performance.md` section 3.3 — verified by reading the two side by side in the pull
      request.
- [ ] Lighthouse CI runs against the built client served from `src/Hosts/Tailor360.Web/wwwroot`, on the throttling
      profile of `support-matrix.md` section 2, over the nine URLs named in scope — five unauthenticated and four
      authenticated through the signed-in page — three runs each with the median taken, and produces a report per
      URL.
- [ ] The report names every route that is **not** measured and why, and names the nightly sweep and the workflow
      that does not exist to run it (#59), so nobody reads nine URLs as twenty-five.
- [ ] `docs/nfr/performance-report.md` carries a measured baseline for every budget this repository can measure
      today, each with the commit it was measured at, and an explicit "not yet measurable, because" line for every
      one it cannot, naming the issue.
- [ ] **Negative — the gate actually fails:** a deliberate commit importing a large module into the entry chunk
      makes `size-limit` fail and names the entry and the overshoot; the same commit makes the Lighthouse
      total-byte-weight assertion fail. Both demonstrated and reverted.
- [ ] **Negative — per route, not averaged:** a deliberate regression on one route fails that route's assertion
      while the others stay green, which is what section 3.1's "a single slow route fails the budget" requires.
      Demonstrated and reverted.
- [ ] **Negative — the memory probe detects a leak:** a deliberate listener leak on the measurement draft route
      makes the `@memory` spec's trend assertion fail. Demonstrated and reverted.
- [ ] **Exception — a budget that cannot be met now** is recorded in `docs/nfr/performance-report.md` with its
      measured figure, the route it is on, and either the issue that will fix it or a waiver identifier the
      technical reviewer has granted in `docs/process/waivers.md`. A session does not write its own waiver, and a
      waiver is for a gate that cannot be met — not for a measurement nobody has reviewed yet.
- [ ] `./scripts/dev docs` is clean and the `documentation` job of `ci.yml` is green with the new document linked.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| `size-limit` output — entry name and byte size | Public | Prove the bundle rows of section 3.3 | The `actions/upload-artifact` retention the job applies | Anyone with repository access | A build fact. No personal data is reachable |
| Lighthouse reports — metric values, a resource list and a filmstrip of screenshots | **Internal**, and the filmstrip is **Personal by construction** because it renders a seeded screen | Prove the vitals and the weight budgets | As above | Whoever can read the CI run | Safe only because every value on the screen is synthetic; the report file says so, as the end-to-end suite's README does |
| Heap samples — a number per sample | Public | Prove the memory trend | As above | Anyone | Numbers |
| `docs/nfr/performance-report.md` rows | Public | The before-and-after `RG-07` asks for | Life of the release evidence index | Anyone | Measured figures, the commit, and the route **name**. No route parameter value, per `data-classification.md` section 5.18 |

Nothing is persisted in the product and no new field reaches a database, so no retention period is introduced and
none is invented.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| — none added — | — | — | — | — | — | No endpoint is added or changed. Lighthouse and the memory probe sign in through **[E12-F03-1b]**'s fixture as **[E12-F03-1a]**'s seeded roles, and the routes behind `RequirePermission` are measured as a role that holds the permission |

`docs/security/permission-matrix.md` and `tests/Tailor360.IntegrationTests/Authorization/matrix.yaml` are untouched.
**OD-13** is open and no grant is asserted.

## Modules, contracts and events

No .NET module, no schema, no contract, no integration event. The change set is `clients/pwa/package.json` and two
configuration files beside it, one `tests/e2e` spec, one new document and two link additions under `docs/nfr/`, and
one job in `.github/workflows/ci.yml`. ARCH-005 and MO-2 are not engaged.

Threat-model coverage (DOR-05): none required — no trust boundary, no input, no stored data. The one security-
adjacent fact is that Lighthouse artefacts contain screenshots of seeded screens, which the table above classifies
and the report file states.

## Screens and states

Deleted deliberately: no screen is added or changed. The screens this sub-issue measures are the ones that exist at
HEAD, driven through **[E12-F03-1b]**'s page objects.

## Migration and rollback

No table and no column, so no migration. What proves it: the change set contains nothing under `src/`, and
`migrate --dry-run` reports no outstanding migration. Rollback is removing the job and the two configuration files;
nothing in the product changes, which is the point of measuring before changing.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **CP-07** Core Web Vitals targets on the reference device | **Open** — `capacity-and-performance.md` section 10, needed before W1 exit | LCP p75 ≤ 2.5 s, INP p75 ≤ 200 ms, CLS p75 ≤ 0.10 exactly as section 3.3 states them, quoted by row. The assertions change when the document does | W1 exit gate |
| **CP-10** JavaScript bundle budgets | **Open** — needed before W1 | Initial ≤ 180 KB gzip, per route ≤ 60 KB gzip, decoder chunk ≤ 120 KB gzip, as drafted. `size-limit` quotes the rows | W1 |
| **CP-11** whether the bundle check runs per pull request or only at the release gate | **Open** | Its own drafted default: **every pull request**, as the fast subset of the release performance gate. This sub-issue implements the default and records it rather than choosing it | W1 — it changes the CI definition of #22 |
| **CP-12** memory ceilings on the scan and capture screens | **Open** | 180 MB and 250 MB heap, 400 MB private memory, as drafted. **Neither screen exists**, so the probe asserts the trend on the heaviest screens that do, and the report records the two ceilings as scheduled | Before W3 |
| **SM-01** the lowest supported device | **Open** — `support-matrix.md` section 10 | The section 2 reference device and 4G profile, read into the Lighthouse configuration by citation. Changing the device re-derives every budget, which is exactly what that row warns | Before W1 exit |
| **RG-OD-01** confirm every proposed severity and waiver duration | **Open** — `release-gates.md` section 4 | `RG-07`'s S2 and its 30-day maximum as drafted. This sub-issue cites; it proposes nothing | W1 exit gate |
| **CP-05** object-storage growth, and **#31**'s derivatives | **Open**; #31 is open | The image budget is configured and currently measures an application that emits no derivative. The report says so rather than reporting a pass that means nothing | Before **OD-02** is priced |

## Required verification

- [ ] **Bundle** (`pnpm --dir clients/pwa size`): every configured entry measured against its quoted row, on a
      production build.
- [ ] **Lighthouse CI**: one run set per route, median of three, on the section 2 throttling profile, with the
      budget file applied; and the measurement-journey user flow for Interaction to Next Paint.
- [ ] **End-to-end, memory** (`@memory`, Chromium): the heap trend on the two heaviest existing routes, including
      the negative case of a deliberate listener leak.
- [ ] **Negative**: the oversized-import commit failing `size-limit` and the byte-weight assertion; the
      single-route regression failing only its own route. Both demonstrated and reverted.
- [ ] **Documentation**: `./scripts/dev docs` clean, and the `documentation` job green with the new document
      linked from two places.
- [ ] Non-functional rows affected (DOR-13): `docs/nfr/traceability.md` **NFR-CL-01** and **NFR-CL-02** move from
      "proof scheduled (#52)" to naming this sub-issue for the **lab** half, with the field half against
      **[E12-F03-5]** and **#58**. **NFR-LT-01** (line 108), **NFR-LT-02** (line 109) and **NFR-LT-03** (line 110)
      currently read "proof scheduled (#52, #58)" and "(#36, #52, #58)": **#52 is dropped from those three cells**,
      with a one-line note that #52 delivered the client-side lab gates only and that the k6, server-histogram and
      scanner halves remain #58's and #36's. Leaving #52 in a cell this parent never proves is how a traceability
      sheet comes to claim a proof that did not happen.
- [ ] DOR-16: `./scripts/dev doctor` for the Playwright browsers; the Lighthouse run needs the served stack
      **[E12-F03-1b]**'s README describes, and the job brings it up the way `migration-check` brings up PostgreSQL.
- [ ] The checks run twice, in both orders, and the pull request records the run-to-run spread of each vital —
      because a gate whose variance is unknown is not yet a gate (`CLAUDE.md` section 5).

## Evidence the pull request will carry

- [ ] The `size-limit` output, full, beside a quotation of each row of `capacity-and-performance.md` section 3.3 it
      implements, and a line naming the `sw.js` and `workbox-*.js` files that were excluded from the initial-bundle
      and first-load figures, with the reason (`injectRegister: null`: nothing fetches them yet).
- [ ] The Lighthouse reports per route, and the measurement-journey user-flow report, with the throttling settings
      visible in each.
- [ ] `docs/nfr/performance-report.md` as merged, rendered, including every `FAIL` and every "not yet measurable,
      because" line.
- [ ] The `@memory` output: the heap samples and the trend verdict, plus the deliberate-leak run shown failing and
      reverted.
- [ ] The three negative demonstrations, each shown red and then reverted.
- [ ] The run-to-run spread of Largest Contentful Paint, Cumulative Layout Shift and Time to First Byte across two
      full runs, so the reviewer can see whether the gate is stable on the hosted runner.
- [ ] The `performance-budget` job's run with its uploaded artefacts, and `./scripts/dev docs` clean.

## Depends on

#52 child **[E12-F03-1b]** — `tests/e2e`, its projects and its sign-in fixture, and the documented way to bring the
stack up. The `@memory` spec lives in that project and Lighthouse measures the same served artefact. Transitively
**[E12-F03-1a]** (the seeded account the authenticated Lighthouse runs need) and **#214**.
**[E12-F03-2]** is **not** a dependency. An earlier reading had Lighthouse iterate
`clients/pwa/src/app/accessibilityCoverage.ts`; the nine-URL list above replaces that, because twenty-five URLs on a
throttled profile is not a pull-request gate. The two lists are therefore deliberately different and the report says
why — the accessibility sweep covers every route, the lab performance gate covers a representative route per area —
so this sub-issue can run in parallel with **[E12-F03-2]** rather than behind it.

Named as open and **not** depended on: **#58** (load tests, dashboards) owns the server-side half of `RG-07` and is
open; **#36** (scanner) and **#189** (image capture) own the two screens the memory ceilings name and are open;
**#31** (media derivatives) owns the image pipeline the image budget will eventually measure and is open; **#19**
owns every figure quoted here and is open, which is why each is quoted as proposed. **#50** and **#51**, the
parent's own dependencies, are open and are not needed: the measurement is of the client that exists at HEAD, and
the report records that as the baseline's scope.

Contended files, flagged: `.github/workflows/ci.yml` — shared with **[E12-F03-1b]**, **[E12-F03-2]**,
`[E15-F03-3]` and **#59**; this sub-issue adds a second job, `performance-budget`, which is legitimate because it
needs a built client rather than a browser matrix. `clients/pwa/package.json` and `pnpm-lock.yaml` — shared with
**[E12-F03-5]** and with
client sub-issues in other batches; re-resolve the lock file rather than editing it. `docs/nfr/traceability.md` —
its status cells are edited by almost every sub-issue in this parent and by several in other batches; this one
touches the four rows named above and no others.

Refs #52
