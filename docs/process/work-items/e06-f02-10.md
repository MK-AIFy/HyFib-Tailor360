Sub-issue of #33. The Tailor's navigation already points at a screen that does not exist:
`clients/pwa/src/components/layout/roleNavigation.ts:72` and `:137` give the `tailor` role the destinations
`['home', 'scan', 'production', 'orders']` with `/production` among them, and `clients/pwa/src/app/router.tsx`
defines no `/production` route at all — so the Tailor's own tab is a client 404 today. There is also no
`ScannerSource` anywhere: `grep -rn "ScannerSource" clients/pwa/src` returns nothing, although
`docs/IMPLEMENTATION_PLAN.md` Section 4.4 says the interface is defined by **this** issue and implemented by #36.
This slice builds the client production module, the `ScannerSource` abstraction with its manual-entry source only,
and the Tailor's scanner-first work queue at `/production`.

## What is already built (do not re-build)

- `clients/pwa/src/stories/journeys/TailorQueue.tsx` (475 lines) — the **design prototype for this exact screen**,
  with the whole of `A11Y-RJ-03` walkable against fixtures. It already decides the hard parts: the scan result goes
  to `announceScan` in the shell's reserved region and stays there, a rejection is announced assertively and names
  which rule failed, and a toast is forbidden. Read it before writing a line, then build the real screen beside it.
  **Do not delete or rewrite it** — `clients/pwa/src/stories/journeys/journeys.test.tsx` (488 lines) covers it, and
  `JourneyFrame.tsx` wraps its children in its own `MemoryRouter`, so the prototype and the route cannot share a
  harness.
- `clients/pwa/src/stories/fixtures/jobs.ts` (129 lines) — `MY_JOBS`, `JourneyJob`, `SUPERSEDED_BARCODE` and
  `resolveScan`, all synthetic; `clients/pwa/src/stories/fixtures/branch.ts:90` `PHASES` and `STAFF`. Reuse the
  synthetic shapes for the route's own test fixtures rather than inventing names.
- `clients/pwa/src/components/layout/shellStatus.ts` and `useShellStatus.ts` — the three persistent channels
  (`announceScan`, `announceSync`, `announceAutosave`), rendered by the shell in a reserved slot at the top of
  `main` and kept until superseded. The file explains why a toast is not permitted here (WCAG 4.1.3,
  `docs/nfr/accessibility-localisation.md` section 6, checklist items A11Y-40, A11Y-41, A11Y-44).
- `clients/pwa/src/auth/apiClient.ts` (435 lines) — the **only** transport. `apiRequest` and `apiRequestVersioned`
  return `VersionedResponse<T>` carrying the `ETag`; `ApiRequestOptions` already carries `idempotencyKey` and
  `ifMatch`, and the module re-authenticates, renews step-up in place and replays the identical request.
- `clients/pwa/src/admin/useAdminResource.ts` (83 lines) — the one read effect (abort, reload keeps the previous
  value). `clients/pwa/CLAUDE.md` section 6 forbids a second data-fetching path; use this one even outside `/admin`,
  as the billing routes do.
- `clients/pwa/src/components/states/` — `LoadingState`, `EmptyState`, `ErrorState`, `RetryableError`,
  `OfflineBlockedAction`, `NetworkStatusBanner`, `StateRegion`, `useNetworkState`, and `problemDetails.ts` with
  `plainLanguageDetail` and `failureCauseForStatus` for rendering an RFC 9457 problem as words.
- `clients/pwa/src/components/primitives/` — `DataTable`, `Filters`, `StatusBadge`, `Alert`, `Button`,
  `ButtonGroup`, `Card`, `Icon`, `IconButton`, `TextLink`, `Timeline`; `clients/pwa/src/components/dialogs/` —
  `ConfirmDialog` with its three confirmation tiers, `BottomSheet`.
- `clients/pwa/src/design-system/components/forms/` — `Field`, `TextField`, `TextArea`, `Select`, `SegmentedControl`,
  `NumericStepper`; all with a persistent visible label by construction.
- `clients/pwa/src/design-system/testing/renderWithProviders.tsx` — supplies `AppIntlProvider` and the display
  preferences **and nothing else**: no `SessionProvider` and no router, so a route rendered in it throws. The harness
  a route needs is `clients/pwa/src/admin/testing/storyTransport.tsx` (`withAdminApi`, `storyJson`, `storyProblem`,
  `storyPending`, `STORY_USER`, `StoryRoutes`), which installs the stub transport and wraps the element in
  `SessionProvider` + `MemoryRouter` + `RequireSession`.
- `clients/pwa/src/design-system/testing/expectNoHorizontalOverflow.ts` — already defaults to 320, 360, 768, 1024
  and 1280 CSS px at 100% and 200% zoom. Call it with its defaults; do not narrow the list.
- `clients/pwa/src/i18n/messages/index.ts` (64 lines) — one file per family, two spreads, and
  `clients/pwa/src/i18n/en-IN.ts` / `ta-IN.ts`. `clients/pwa/src/i18n/formatters.ts` and `units.ts` own every number,
  date and measurement rendering; an inline `toLocaleString` is a review finding.
- `clients/pwa/src/api/contract.ts` (491 lines) — every hand-written payload type is pinned with
  `Conforms<Local, Immutable<Response200<'OperationId'>>>` against `clients/pwa/src/api/schema.d.ts`, which
  `pnpm --dir clients/pwa generate:api` regenerates from `docs/api/openapi.v1.json`.

## Scope

- [ ] `clients/pwa/src/scanning/scannerSource.ts` — the interface `docs/IMPLEMENTATION_PLAN.md` Section 4.4 specifies
      and nothing more: a `ScannerSource` with `start`, `stop` and a decode callback, and a `ScanResult` of
      `{ raw, normalised, namespace, id, checksumValid, source, timestamp }`. One implementation only:
      `ManualEntrySource` (a reason is required and the call is audited server-side). `CameraSource` and
      `KeyboardWedgeSource` are named in the file's doc comment as #36's, and are **not** stubbed — an empty class
      that silently never decodes is worse than an absent one.
- [ ] `clients/pwa/src/production/productionApi.ts` — one documented API module over `apiClient` for
      `GET /api/v1/orders/work-queue` and
      `POST /api/v1/orders/garment-jobs/{jobId}/phases/{phaseId}/transitions`, with `Idempotency-Key` minted by
      `crypto.randomUUID()` once when the person commits to the action and **reused** on retry (the pattern
      `clients/pwa/src/routes/catalog/CatalogVersionListRoute.tsx:164` and
      `clients/pwa/src/routes/admin/RoleDetailRoute.tsx:65` already use), and `If-Match` from the read's `ETag`.
- [ ] `clients/pwa/src/production/types.ts`, pinned in `clients/pwa/src/api/contract.ts` with `Conforms<…>`, and
      `clients/pwa/src/production/productionPermissions.ts` with the two keys the screen asks for: `orders.read` and
      `orders.phase_transition`. The screen decides nothing from them — the server re-checks every request.
- [ ] `clients/pwa/src/i18n/messages/production.ts`, spread into `index.ts`, with the English and Tamil strings. Every
      refusal message is a sentence naming the rule that failed, never a code and never the word "invalid" (WCAG
      3.3.1, 3.3.3).
- [ ] `clients/pwa/src/routes/production/WorkQueueRoute.tsx` and `production.css` — one route, mounted at
      `/production` in `clients/pwa/src/app/router.tsx` inside `RequireSession`, wrapped in
      `RequirePermission permission={orders.read}`. Manual entry resolves a job into the queue's selection; the
      selected job shows its phase, its due cue in **text** and not only a colour, and the phase's primary action.
      Filters: mine, unassigned, overdue, due within N days (the caller supplies N — the server has no default),
      blocked and held.
- [ ] The five phase actions from one control group: start, pause (with a reason), resume, complete and, where the
      phase allows it, skip (with a reason). Each is a 56 × 56 CSS px primary shop-floor target with 12 px spacing
      (`clients/pwa/CLAUDE.md` section 4, AL-03). Each publishes into `announceScan` — accepted politely with the
      next expected action, rejected assertively naming which rule failed.
- [ ] Offline behaviour, stated rather than implied: a phase transition attempted with no connection renders
      `OfflineBlockedAction` and is **not** queued, because the offline queue is #51's and a transition replayed an
      hour later would claim work that did not happen then. The message says the action needs a connection.
- [ ] `clients/pwa/src/routes/production/ProductionScreens.test.tsx` — Vitest, through `withAdminApi`, covering every
      state in the table below plus the refusal rendering and the overflow check.

## Out of scope

The camera and keyboard-wedge sources and the `/scan` route (#36). The offline queue and its replay (#51). The Tailor
Master's workboard (E06-F02-11), the desktop planning board (E06-F02-11b) and the Team screen (E06-F02-13). The
job card itself, which is
#204's (`[E06-F01-3] Order intake, estimate and job-card screens`). Recording material against a phase (#39). The
measurement sheet, which is reached from the job card and is #28's. No change to
`clients/pwa/src/components/layout/roleNavigation.ts`: `/production` is already a destination there, and this slice
only makes it resolve.

## Acceptance criteria

- [ ] A Tailor signs in, opens `/production`, sees their branch's open phases one row each with the job number, the
      category, the due cue in text and the phase, filters to "mine", starts a phase, completes it, and hears the next
      expected phase — `A11Y-RJ-03` steps 1, 3 and 6, exercised here in Vitest at phone width through
      `withAdminApi`. The screen-reader pass on a physical phone is **E06-F02-14**'s, because this session has no
      device; nothing in this child is blocked on it.
- [ ] Manual entry of a job number resolves the job into the selection and announces it; manual entry of an unknown
      or superseded value is **refused** with a sentence naming which rule failed, announced assertively, and the
      message stays on screen until it is superseded — `A11Y-RJ-03` steps 2 and 8. **On step 2:** the step's words are
      "Scan in on a job with the wedge scanner", and a keyboard wedge emits exactly the keystroke sequence a person
      types, so typing or pasting into the manual-entry field is the same DOM event stream and is what this child
      covers. The dedicated `KeyboardWedgeSource`, its focus management and the physical device are **#36's**, and
      E06-F02-14 records step 2 against what this child shipped with that reason written down — so a reviewer does not
      read "wedge" as a claim this child makes about hardware.
- [ ] Every one of the five states in the table below renders for the real route, proven by a test that drives the
      route rather than the state component.
- [ ] **Negative:** a caller without `orders.read` sees the `RequirePermission` `EmptyState` with the message ids
      `admin.forbidden.title` and `admin.forbidden.body` and is **not** redirected; a caller with `orders.read` but
      without `orders.phase_transition` sees the queue and no phase-action control.
- [ ] **Negative:** a `409` from a concurrent completion renders the server's current state as a sentence through
      `plainLanguageDetail` and the row reloads — the person is told somebody else finished it, not shown a generic
      error.
- [ ] **Exception:** with no connection, the phase action renders `OfflineBlockedAction` and nothing is queued;
      reconnecting re-enables the action without a reload.
- [ ] `expectNoHorizontalOverflow` passes at its default widths (320, 360, 768, 1024, 1280) at 100% and 200% zoom,
      and the axe run (`clients/pwa/src/design-system/testing/axe.ts`) reports no violation.
- [ ] `pnpm --dir clients/pwa lint`, `typecheck`, `test`, `format:check` and `generate:api:check` all clean.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Queue row rendered on screen: job number, category, due date, priority, phase, hold state | Internal (`docs/nfr/data-classification.md` 5.7) | Pick up the next piece of work | Not stored on the device | The signed-in Tailor, by branch | Never in client telemetry beyond a route name; 5.18 keeps client telemetry Internal by construction |
| Queue row: customer name | Personal — the one identifying field a job card carries (`docs/prd/glossary.md`) | Tell one garment from another | Not stored on the device | As above. Contact details are withheld by `OrdersResponseViews.WorkQueue` and never reach the client | **Never** in a log, a metric, a trace attribute or client telemetry |
| Queue row: assignee display name | Personal (5.15) | Distinguish somebody else's work | Not stored | As above | Never in client telemetry |
| `ScanResult.raw` / `.normalised` | Internal (5.8 — a barcode payload carries no personal data by construction) | Resolve a job from a label or a typed number | Held in memory for the life of the interaction only | The signed-in caller | Never logged by the client, because the server audits the resolve and a second copy on a shared device is a liability |
| Manual-entry reason | Internal | Why the person typed instead of scanning | Sent to the server, not stored on the device | Audit trail | Never in client telemetry |
| `localStorage` | — | **Nothing is stored.** No queue row, no scan and no draft is written to browser storage in this slice; encrypted local drafts are #51's | — | — | — |

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `orders.read` | branch | no | no | no | every role, including `tailor` | **Exists** — `docs/security/permission-matrix.md` line 231 |
| `orders.phase_transition` | branch | no | no | no | `branch_manager`, `tailor_master`, `tailor` | **Exists** — line 230: "The everyday shop-floor action" |

Both keys are named once in `clients/pwa/src/production/productionPermissions.ts`, the way
`clients/pwa/src/admin/adminPermissions.ts` and `clients/pwa/src/billing/billingPermissions.ts` already do. No new
permission, no change to `docs/security/permission-matrix.md` and no authorisation fixture: this slice publishes no
route. **OD-13** is open, so the grants are as proposed in the matrix.

## Modules, contracts and events

Client only. One client feature area, `clients/pwa/src/production/` plus `clients/pwa/src/scanning/`, consuming the
Orders API over HTTP — no .NET change at all, so no module boundary is crossed and ARCH-003/004/005 are not engaged.
DOR-05: the threat is a shared shop-floor device leaving one person's queue, or a customer's name, readable to the
next person; answered by the session, by storing nothing locally, and by the server's branch scope. Recorded against
the **Order and garment workflow** row of `docs/security/README.md` section 3.2, whose model
(`threat-models/order-workflow.md`) is the E14-F01a-4 child of #56 in wave W2 and where the workflow invariants of
`docs/architecture/invariants.md` stand in until it exists.

## Screens and states

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| Tailor work queue | `/production` | `LoadingState` while the first read is in flight; a reload keeps the previous rows (`useAdminResource`) | `EmptyState` — "Nothing is waiting for you in this branch", with the filter set named so a person knows an active filter is the reason | `RetryableError` with `plainLanguageDetail` of the problem document, and a retry that reuses the same `Idempotency-Key` for a command | `NetworkStatusBanner` for the read; `OfflineBlockedAction` on each phase action, which is **not** queued | The `RequirePermission` `EmptyState` with `admin.forbidden.title` / `admin.forbidden.body`, never a redirect |

Message ids: the new `production.*` family in `clients/pwa/src/i18n/messages/production.ts` for the screen's own
words; `navigation.destination.production` already exists for the tab; `admin.forbidden.*` for the forbidden state;
`states.*` for the shared state components. Dates go through `clients/pwa/src/i18n/formatters.ts` and never through
an inline `toLocaleDateString`; there is no money and no measurement on this screen.

**No offline Storybook story and no offline screenshot is required**, for the reason
`clients/pwa/src/routes/catalog/catalogScreens.stories.tsx` records for the administration section: the application
states its offline behaviour through `NetworkStatusBanner` and `OfflineBlockedAction`, which carry their own stories,
and an offline story here would imply a queue this screen must not have. Stories for loading, empty, error and
forbidden **are** required, each rendering the real route through `withAdminApi`.

## Migration and rollback

Deleted: no schema change. This is a client-only slice; rollback is redeploying the previous client bundle, after
which `/production` is a client 404 again.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| #50 `[E12-F01]` responsive design system | **Open issue** — but the components and the journey prototypes this slice uses are **merged and present** in the working copy today at the line counts quoted above | Build on what is in the tree. If a primitive is missing, raise it on #50 rather than adding a one-off component under `routes/` | — |
| #51 `[E12-F02]` installable PWA and bounded network resilience | **Open** | Nothing is queued offline and nothing is written to browser storage. The action is blocked and says so | Before a phase transition may be taken offline |
| The due-soon window | **Branch configuration with no source** | The filter sends `filter[dueWithinDays]` only when the person picks a value; the screen has no default and shows no "due soon" chip until one is chosen | Before a due-soon default is shown anywhere |
| `OD-13` — approved role-to-permission grants | **Open** | The grants at `permission-matrix.md` lines 230 to 231 | W1 exit gate |

## Required verification

- [ ] Vitest (`clients/pwa/src/routes/production/ProductionScreens.test.tsx`) through `withAdminApi`: the five states;
      the filter set; start → complete; the manual-entry resolve and the manual-entry refusal; the `409` rendering;
      the missing-`orders.phase_transition` case.
- [ ] Vitest (`clients/pwa/src/scanning/scannerSource.test.ts`): `ManualEntrySource` normalisation and the
      `checksumValid` flag for a well-formed and a malformed value, using the synthetic payloads in
      `clients/pwa/src/stories/fixtures/jobs.ts`.
- [ ] Accessibility: the axe run with no violation; `expectNoHorizontalOverflow` at its default widths and zooms; a
      keyboard-only pass of every action; the touch-target sizes asserted for the five phase actions.
- [ ] Accessibility, automated only in this child: the axe run, the overflow run, the keyboard-only pass and the
      touch-target assertions above. The screen-reader walk of `A11Y-RJ-03` on a physical phone is **E06-F02-14**'s, and
      this child is not blocked on it; the pull request says which items were automated and which were handed over.
- [ ] Localisation: both catalogues complete, and the screen re-checked at 40% text growth with Tamil selected
      (`clients/pwa/CLAUDE.md` section 4).
- [ ] `pnpm --dir clients/pwa generate:api:check` clean against the committed `docs/api/openapi.v1.json`.
- [ ] Non-functional rows touched: the accessibility rows of `docs/nfr/traceability.md` reached through
      `docs/nfr/a11y-checklist.md` (DOR-13). The Largest Contentful Paint budget of
      `docs/nfr/capacity-and-performance.md` line 211 (p75 ≤ 2.5 s, cold cache) is **E06-F02-14**'s to measure: a
      Lighthouse run on the reference device profile is not something this session can produce, and #52 owns the
      harness. What this child asserts instead is that route-level code splitting is in place and the barcode decoder
      is **not** in the initial bundle — there is no decoder here to split out, which is itself the assertion.
- [ ] Environment: `./scripts/dev doctor` and `pnpm --dir clients/pwa test`; no database is needed (DOR-16).

## Evidence the pull request will carry

- [ ] Screenshots at 360, 768 and 1280 CSS px, light and dark, plus one at 200% zoom.
- [ ] The Storybook stories for loading, empty, error and forbidden, each rendering the real route.
- [ ] The axe output and the `expectNoHorizontalOverflow` run.
- [ ] The Vitest output for the manual-entry resolve, the manual-entry refusal staying on screen, and the
      start-to-complete sequence.
- [ ] One line naming E06-F02-14 as the owner of the `A11Y-RJ-03` device run sheet and the Lighthouse figure, so a
      reviewer does not read their absence as an omission.
- [ ] `pnpm --dir clients/pwa lint`, `typecheck`, `test`, `format:check`, `generate:api:check` output.

## Depends on

E06-F02-8 — `GET /api/v1/orders/work-queue`, its filters and its due indicators. Without it this screen has nothing
to read.

E06-F02-5 — the phase transition route the five actions call.

#201 — confirmed garment jobs, so a queue has rows at all.

**Single-writer files this slice needs:** `clients/pwa/src/app/router.tsx`, `clients/pwa/src/i18n/messages/index.ts`,
`clients/pwa/src/i18n/en-IN.ts`, `clients/pwa/src/i18n/ta-IN.ts`, `clients/pwa/src/api/contract.ts` and
`clients/pwa/src/api/schema.d.ts`. Every other client sub-issue in flight touches the same five — the planned #35
children (`e07-f01-6`, `e07-f01-7`), the #38 client children (`E08-F01-5` to `E08-F01-8`), the #41 and #42 client
children, and E06-F02-11, E06-F02-12 and E06-F02-13 here. One at a time, and rebase rather than merge the router.

Refs #33
