Sub-issue of #42. Two of the parent's **Required verification** items remain after **E09-F02-9** has proved the
renderer against the accountant's golden master: the parent's acceptance criterion "**PDF totals and tax components
match the persisted calculation snapshot**" — which needs a real posting, not a rendered model — and "**accountant
review of sample invoice/credit-note outputs**", which needs a person outside engineering. Plan Section 6.3 lists
"GST configuration and sample output approved by accountant" against #41, **#42** and #44 as a roadmap acceptance
criterion, and `docs/billing/go-live-plan.md` section 2.7's gate table has no row for it. No document in `docs/`
records an accountant's review of a rendered document.

`tests/Tailor360.IntegrationTests/Billing/DocumentArtifactTests.cs` proves the worker renders, stores, streams and
prints, that the stored checksum equals the bytes, and that a cancelled invoice's original PDF still matches its
checksum. **It never compares a printed figure with the stored snapshot.** That comparison is this slice.

## What is already built (do not re-build)

- **E09-F02-9** — the `UglyToad.PdfPig` entry in `Directory.Packages.props` and
  `tests/Tailor360.ContractTests/Adapters/PdfText.cs`, the extraction helper. This slice adds
  `<PackageReference Include="UglyToad.PdfPig" />` to
  `tests/Tailor360.IntegrationTests/Tailor360.IntegrationTests.csproj` (the central version entry already exists) and
  reaches `PdfText` by copying it to `tests/Tailor360.IntegrationTests/Billing/PdfText.cs` with a one-line comment
  saying why there is no shared test project to put it in. **Do not write a second extraction helper from scratch,
  and do not add a shared test project — that is a repository-layout change, not this slice's.**
- `tests/Tailor360.IntegrationTests/Billing/InvoiceScenes.cs` (308 lines) — the scene builder:
  `BuildAsync`, `ConfirmOrderAsync(fixture, branch, customerId, orderNumber, jobCount)`, `CashierAsync`,
  `PublishAsync(fixture, publish)`, `DraftAsync(cashier, orderId, reference)`, `CreateListAsync`, `AddItemAsync`,
  `PublishAsync(client, version)`, `RegisterAsync`, `EnsurePublishedTaxCodeAsync`, `Key()`, `Tagged(tag)`.
  **Read `PriceAsync` before planning anything:** its signature is
  `PriceAsync(fixture, branch, reference, string[] lineKeys, string itemCode, string? surcharge = null)` and it
  hard-codes the date `2026-09-12`, the place of supply `"33"`, quantity `1m`, no discount and no override. It prices
  **one item code with at most one surcharge**. That is why this slice covers two named cases and not seventeen —
  see Scope.
- `src/Modules/Billing/Tailor360.Modules.Billing.Application/Pricing/PricingService.cs` and
  `src/Modules/Billing/Tailor360.Modules.Billing.Contracts/Pricing/IPricingService.cs` —
  `FindSnapshotAsync(organisationId, reference, cancellationToken)` returning the persisted `PricingResult`. This is
  the "persisted calculation snapshot" half of the comparison, and it is read through the published contract, not
  through a table.
- `src/Modules/Integration/Tailor360.Modules.Integration.Infrastructure/Documents/BillingDocumentTemplate.cs` — and
  the suppression rule **E09-F02-9** records: a zero `Discount`, `CGST`, `SGST`, `IGST`, `Cess` or `Round-off` is
  **absent** from the page. `Taxable value`, `Grand total` and `Balance due` are always printed.
- `src/Modules/Billing/Tailor360.Modules.Billing.Application/Invoicing/DocumentArtifactHandler.cs` —
  `RenderPendingAsync(budget)`, the worker pass the test drives to get an artefact, and `OpenInvoiceAsync` /
  `OpenNoteAsync`, which is how the stored bytes are read back.
- `tests/fixtures/billing/calculation-snapshot-v1.json` — the snapshot schema the stored row is written against.
- `docs/nfr/reviews/stakeholder-review.md` — **the house shape for a review record**: written as a template, with the
  agenda, the questions each stakeholder must answer and the signature blocks, carrying the banner "**Status:
  awaiting the workshop. The review has not been held.** Nothing in this document is a record of anything anyone has
  said. Every field marked _to be completed at the review_ is filled in the pull request that follows the meeting,
  together with the date." Copy this shape exactly; it is also the document that singles out the accountant's
  confirmation as the one no engineer can answer.
- `docs/process/definition-of-ready.md` section 5 — deferring a criterion, and the **six fields** a deferral record
  carries: criterion, reason, default in force, owner, date recorded, condition for closure. This is the instrument
  this slice uses. Read the three rules that bound a deferral as well.
- `docs/process/release-gates.md` section 4 and `docs/process/waivers.md` section 3 — **and the reason this slice
  writes no waiver row.** RG-14 (release evidence checklist) is marked "No waiver" in the gate table and is listed in
  `waivers.md` section 3 among what may never be waived, and the accountant is named there as "a required approver
  for the financial evidence in RG-14". A waiver row for this review would be invalid on its face.
- `docs/billing/go-live-plan.md` section 2.7 — the go-live gate table this slice's review record feeds. Its rows
  today are: golden master signed off, tax configuration published, price list published per branch, registrations in
  force, migrations, UAT, release gates.
- `docs/nfr/a11y-checklist.md` section 5.6 (`A11Y-DP-01` to `A11Y-DP-09`) — the records written up here for the
  **rendered** artefact, including the ones **E09-F02-9** proved the renderer cannot satisfy.
- `artifacts/` — already the directory `./scripts/dev` logs to, and already outside version control.

## What is missing

- No assertion anywhere that a figure printed on a **stored, posted** invoice's PDF equals the figure in the
  **persisted** calculation snapshot. The checksum proves the bytes have not changed; it says nothing about whether
  they were ever right, and E09-F02-9's renderer-level proof says nothing about the pipeline from pricing to page.
- No sample pack an accountant could be given, and no review record.
- No go-live gate row for the rendered documents.
- No write-up of the accessibility records for the rendered artefact.

## Scope

- [ ] **The extraction capability in the integration tier**: the package reference and `PdfText.cs` as described
      above. No change to `Directory.Packages.props` — E09-F02-9 owns that file, and this slice must not touch it.
- [ ] **`PdfTotalsMatchSnapshot`** at `tests/Tailor360.IntegrationTests/Billing/PdfSnapshotAgreementTests.cs`
      (Integration tier, needs a database), over **exactly two named scenes**:
      - **intra-state, exclusive** — the `walkthrough-blouse` shape: one garment job, one item with its two
        surcharges, place of supply `33`, so CGST and SGST are printed and IGST, cess and discount are not;
      - **inter-state** — the `inter-state` shape: the same item supplied to place of supply `29`, so IGST alone is
        printed and CGST and SGST are not.
      For each: build the scene with `InvoiceScenes`, price through the contract, draft, post, run
      `DocumentArtifactHandler.RenderPendingAsync`, read the stored bytes back through `OpenInvoiceAsync`, extract
      the text, and assert that **every figure printed on the page equals the figure in the snapshot read through
      `IPricingService.FindSnapshotAsync`, to the paisa** — the nine totals and every line's tax components — and
      that a component the snapshot holds as zero is **absent from the page**.
      The inter-state case needs one change to the scene builder: **add a `placeOfSupplyStateCode` parameter to
      `InvoiceScenes.PriceAsync`, defaulted to `"33"` so no existing caller moves.** That is the only change to
      `InvoiceScenes.cs` this slice makes.
- [ ] **A third assertion, on a note**: post a credit note against the intra-state invoice, render it, and assert its
      printed totals equal the note's own `AdjustmentNotePayload.totals` to the paisa, and that it prints **no**
      balance line (`isNote` suppresses it). A note's figures come from the invoice's line rates, not from a
      snapshot, so this is agreement with the posted note rather than with a snapshot, and the test says so.
- [ ] **The sample pack**: a test, or a documented `dotnet test` filter named in the review record, that renders one
      invoice, one credit note and one debit note from the two scenes above, with synthetic customer details
      including one Tamil name, and writes them to `artifacts/billing-document-samples/`. **The PDFs are attached to
      the pull request, not committed** — say so in the review record, so nobody looks for them in `docs/`.
- [ ] **The review record** at `docs/billing/accountant-document-review.md`, in the shape of
      `docs/nfr/reviews/stakeholder-review.md`:
      - the banner saying the review has not been held and that nothing in the document records anything anyone has
        said;
      - what is being reviewed — the three rendered documents, where they came from, and the golden master they were
        priced from;
      - the questions the accountant must answer, each traceable: every statutory field present on a tax invoice; the
        CGST/SGST/IGST/cess presentation, **including whether a zero component should be printed as zero or omitted,
        which is what the template does today and is a presentation question only the accountant can settle**; the
        HSN/SAC column; the place-of-supply statement; the round-off line; the credit and debit note presentation and
        their link to the invoice they correct; the human-readable number series **and the financial-year token
        (COD-03)**; whether the supplier block as printed is sufficient;
      - the signature blocks;
      - **the accessibility records for the rendered artefact**: `docs/nfr/a11y-checklist.md` section 5.6
        `A11Y-DP-01` to `A11Y-DP-09`, each marked Pass or **Fail with the gap named** — never Not applicable without
        a reason — carrying forward what E09-F02-9's test comment recorded about tagged-PDF structure (A11Y-DP-02,
        and the unassertable half of A11Y-DP-05) and naming the issue that would close each;
      - one line saying why there is **no waiver row**, citing RG-14's "No waiver" and `waivers.md` section 3.
      **No statutory requirement is invented here.** The questions are the accountant's to answer, which is the whole
      reason this document is a template and not a conclusion.
- [ ] **A Definition-of-Ready deferral record on this issue**, with all six fields of
      `docs/process/definition-of-ready.md` section 5 — not a waiver:
      criterion, the accountant's review of the three rendered documents; reason, it needs a person outside
      engineering and cannot be completed inside a pull request; default in force, the review record exists, states
      that the review has not been held, and lists every question; owner, the business owner, who books the
      accountant (`docs/nfr/reviews/stakeholder-review.md` establishes the Owner as approver for the accountant's
      confirmation); date recorded, the date the record is written; condition for closure, the completed record with
      the signature block filled in, before the **W4 gate** and before the first production posting — which is also
      the go-live gate below. **#42 stays open until that happens**, and this slice's pull request says so in one
      line.
- [ ] **A new row in `docs/billing/go-live-plan.md` section 2.7**: "Rendered documents reviewed by the accountant —
      `docs/billing/accountant-document-review.md` completed and signed, covering one invoice, one credit note and
      one debit note rendered from synthetic data (COD-03, and the zero-component presentation question)". Added
      beside the existing "Golden master signed off" row rather than folded into it, because one is about the
      arithmetic and the other about the document.

## Out of scope

- **The remaining fifteen golden-master cases against a live posting.** Driving them all would mean publishing a
  price-list version and a tax configuration per case that reproduce the fixture's items, discount rules, tax codes,
  `taxInclusive` flag, round-off rule and override threshold — a fixture-to-API loader and a generalised
  `PriceAsync`, which is its own slice. **All 17 are already proved at the renderer level by E09-F02-9**; what this
  slice adds is the pipeline proof, and two cases prove the pipeline. A follow-up under #42 carries the loader if a
  reviewer wants the full set through the database.
- The extraction helper's design, the Tamil assertion and the machine-decidable accessibility assertions —
  **E09-F02-9**.
- Any change to `Directory.Packages.props`, the PDF template, the renderer, the font set, the barcode renderer, the
  stored artefact or its checksum. **If a test written here fails because the document is wrong, that is a defect
  issue** raised against #42 with the failing assertion quoted — not a quiet edit inside this slice, which would make
  the test prove nothing.
- Adding tagged-PDF structure to QuestPDF — a renderer change with an ADR-0014 amendment behind it. Recorded as a
  Fail with a named issue, not built.
- The accountant's sign-off of the **pricing** golden master, which is **OD-05** and belongs to #41's closure. This
  slice reviews the **rendered document**, and the record says which of the two questions each answer belongs to.
- The receipt's own review. A receipt is #43's document (#171); the record's scope names the three invoice-family
  documents only.
- Any waiver row. See above: RG-14 admits none.

## Acceptance criteria

- [ ] For both named scenes, every figure printed on the **stored** PDF of a posted invoice equals the figure in the
      persisted calculation snapshot read through `IPricingService.FindSnapshotAsync`, to the paisa — the nine totals
      and every line's tax components — and a component the snapshot holds as zero is absent from the page.
- [ ] The inter-state scene prints IGST and prints no CGST or SGST; the intra-state scene prints CGST and SGST and no
      IGST. This is the assertion that would catch a place-of-supply regression, and it is why two scenes rather than
      one.
- [ ] A credit note posted against the intra-state invoice renders, its printed totals equal its own payload's
      totals to the paisa, and it prints no balance line.
- [ ] `docs/billing/accountant-document-review.md` exists, states that the review has not been held, lists every
      question the accountant must answer with nothing invented, names the three sample documents and the golden
      master they were priced from, and carries the `A11Y-DP-01` to `A11Y-DP-09` records with every Fail's gap named.
- [ ] The deferral record is on the issue with all six fields before the pull request leaves draft, and the pull
      request states in one line that **#42 stays open until the accountant signs**.
- [ ] `docs/billing/go-live-plan.md` section 2.7 carries the new gate row, and `./scripts/dev docs` passes so every
      relative link in the new record resolves.
- [ ] **Negative:** a deliberately wrong figure fails. Prove it: alter one total in the model handed to the renderer
      in a scratch run, show `PdfTotalsMatchSnapshot` failing with **both** figures named, and revert. The transcript
      of that failure is part of the evidence.
- [ ] **Negative:** the test fails rather than passes when the worker has not rendered — an unrendered artefact
      answers `billing.document-not-available`, and a test that treated a missing document as "nothing to compare"
      would be green on a broken pipeline. Assert the artefact exists before extracting.
- [ ] **Exception:** the accessibility records the renderer cannot satisfy are marked **Fail** with the gap and the
      closing issue named, never Pass and never Not applicable. A gap recorded is not a gap waived.
- [ ] **Exception:** no waiver row is written, and the record says in one line why (RG-14 admits none) — so the next
      reader does not add one.
- [ ] Every fixture, name, address and amount used is synthetic (`CLAUDE.md` section 5).

## Data and classification

This slice stores nothing in the product and adds no field. What it creates is one test file, one copied helper,
three rendered sample documents and one documentation page, plus one row in another. Classes from
`docs/nfr/data-classification.md` section 5.10 and section 9 (exports, documents and printed artefacts).

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| The three sample PDFs rendered to `artifacts/billing-document-samples/` | **Synthetic** — classed as the real thing would be (Financial and Personal) and handled that way, because a sample handled loosely teaches a loose habit | Give the accountant something to review, and the pull request something to show | Not committed; `artifacts/` is outside version control, and the copies attached to the pull request live as long as the pull request | Anyone who may read the pull request | Never in logs or telemetry. **No real customer name, telephone number or amount appears in them** |
| The posted invoices, notes and snapshots the scenes create in the test database | **Synthetic** | Produce a real posting to compare against | Dropped with the scratch database | The test run | Never logged. No figure is printed to a test log beyond an assertion message |
| `docs/billing/accountant-document-review.md` | Internal | Record the review, its questions and its outcome | With the repository; the completed record is release evidence | Anyone who may read the repository | A documentation page. **It contains no customer data and no statutory figure** — the answers are the accountant's |
| The deferral record on the issue | Internal | Record that a criterion is deliberately deferred, with its owner and its closure condition | Until closure, when it becomes a blocking item for the W4 gate | Anyone who may read the issue | Not applicable |
| No new table, column, integration event, export column, media class, credential or secret | — | — | — | — | — |

## Permissions and roles

**No permission is added, no grant changes, and no endpoint is added or altered**, so ARCH-007, ARCH-008, ARCH-017,
ARCH-018, ARCH-022 and ARCH-023 have nothing new to judge. The tests call the existing routes exactly as the existing
integration tests do. **OD-13** is open, so the grants below are the shipped defaults rather than approved ones.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `billing.post_invoice` | branch | no | no | no | `branch_manager`, `cashier` | **Exists** — `docs/security/permission-matrix.md` line 146. The scenes post through it, as `InvoiceScenes.CashierAsync` already arranges |
| `billing.create_invoice` | branch | no | no | no | `branch_manager`, `cashier` | **Exists** — line 142; the key the document download is read behind (section 5 row 376) |
| `billing.post_credit_note` | branch | **yes** | no | yes | `branch_manager`, `cashier` | **Exists** — line 145; the key the sample credit and debit notes are posted through. It carries multi-factor authentication, which the scene's client arrangement must satisfy — `CashierAsync(..., alsoGrant)` is where the extra key goes |

No authorisation-matrix fixture changes: the new tests add figure assertions to scenes the matrix already covers.

## Modules, contracts and events

Tests and documentation only. No project under `src/` changes, so no module boundary moves: ARCH-005 and
`src/Modules/CLAUDE.md` MO-2 are untouched, and **ARCH-009** stays green because the new package reference is on a
test project. The persisted snapshot is read through `Billing.Contracts.Pricing.IPricingService`, the published
contract, rather than through `billing.calculation_snapshots` — a test that read the table would be asserting
against something no consumer can see. No integration event is consumed or published. DOR-05: the risk addressed is a
**correctness** one — a legally reviewable document whose printed figures were never compared with the figures the
system actually stored — which is why the assertion reads the PDF's own text layer on one side and the published
snapshot contract on the other, and neither the model the renderer was handed nor a table.

## Screens and states

**Not applicable — this slice adds no screen.** The on-screen document preview and its on-screen `A11Y-DP` records
belong to **E09-F02-5**; the rendered artefact is what this slice reviews, which `docs/nfr/a11y-checklist.md`
section 1 places outside the on-screen checklist's scope.

## Migration and rollback

**Not applicable — no table, no column, and no project under `src/` changes.**
`dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` reporting no outstanding migration is the proof,
and that output goes in the pull request. Rollback is reverting the commit: one test file, one copied helper, one
package reference, one new documentation page and one row in another. No deployed artefact and no stored data is
affected.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-05** the accountant's sign-off of the golden master, and the statutory retention period for GST records | Open — `docs/prd/assumptions-and-open-decisions.md` line 58 | The fixture as merged with #147 is the arithmetic the snapshot is priced by. **This slice asserts the document agrees with what was stored, not that what was stored is correct**, and the review record says which question is which | W3 for rounding, W4 for valuation |
| **COD-03** the invoice number series format, the financial-year token, and whether the series must be strictly gapless for GST purposes | Open — `docs/architecture/conventions.md` line 471, co-signed by the accountant | `INV-<branch>-<FY>-000001` with `2627` for 2026-27, as posted by #154. **It is one of the questions the review record puts to the accountant**, because the go-live plan names it as needing confirmation before the first production posting | The first production posting |
| **OD-07** the device, browser and printer matrix | Open | A4 for the invoice family; no printer is exercised, and the samples are reviewed as files | W0 exit (overdue) |
| **A11Y-OD** items of `docs/nfr/a11y-checklist.md` section 9 | Proposed, to be confirmed — the checklist's own numbers are marked so | The records are written as the checklist asks; where the renderer cannot satisfy one it is marked **Fail** with the gap recorded and an issue named, never Not applicable | #50 closes |
| **OD-13** approved role-to-permission grants | Open — line 66 | The matrix as merged | W1 exit |

**One criterion is deferred, and all six fields of `docs/process/definition-of-ready.md` section 5 are recorded on
the issue**, as Scope requires: criterion — an accountant has reviewed one invoice, one credit note and one debit
note rendered from synthetic data; reason — the review needs a person outside the engineering team and cannot be
completed inside a pull request; default in force — the review record exists, states that the review has not been
held, and lists every question; owner — the business owner, who books the accountant; date recorded — the date the
record is written; condition for closure — the completed record with the signature block filled in, before the W4
gate and before the first production posting. **This is a deferral, not a waiver:** RG-14 admits no waiver
(`docs/process/release-gates.md` section 4, `docs/process/waivers.md` section 3).

## Required verification

Run `./scripts/dev doctor` first. **The integration tier needs a database** through `TAILOR360_TEST_DATABASE_URL` or
a reachable Docker daemon, and `CI=true` turns a skip into a failure (DOR-16, `CLAUDE.md` section 5) — so this slice
cannot be verified in an environment without one, and that is the first thing to check before starting.
DOR-13: the rows affected are the accessibility rows of `docs/nfr/traceability.md` and the document-rendering row of
`docs/nfr/capacity-and-performance.md`; no SLO target changes, because no request path changes.

- [ ] **Integration** (`dotnet test --project tests/Tailor360.IntegrationTests/Tailor360.IntegrationTests.csproj --
      --filter-trait Category=Integration`) — `PdfTotalsMatchSnapshot` over the intra-state and inter-state scenes,
      and the credit-note assertion.
- [ ] **Integration, negative** — the deliberately wrong figure fails, and the failure message names both figures.
      The transcript of that run is evidence, not a step to skip.
- [ ] **Integration, negative** — the test fails when no artefact has been rendered, rather than passing with nothing
      to compare.
- [ ] **Integration** — the existing `DocumentArtifactTests` and `InvoicePostingTests` still green, and
      `InvoiceScenes.PriceAsync`'s new defaulted parameter breaks no existing caller.
- [ ] **Architecture** (`--filter-trait Category=Architecture`) — ARCH-009 still green, and `grep -rn "PdfPig" src/`
      empty.
- [ ] **Manual, accessibility** — the `docs/nfr/a11y-checklist.md` section 5.6 records over the **rendered**
      documents, each marked Pass or Fail with the gap named; no record marked Not applicable without a reason.
- [ ] **Documentation** — `./scripts/dev docs` passes, so every relative link in the new review record and the
      amended go-live plan resolves.
- [ ] `dotnet format --verify-no-changes`, a warning-free Release build, and the whole suite run twice.

## Evidence the pull request will carry

`docs/process/definition-of-done.md` section 4 asks a report or export for a golden-master comparison, a rendered
artefact for a document, and a rendered diff for documentation.

- [ ] The integration tier output from two consecutive runs, with the tier counts and the new test names visible.
- [ ] **The snapshot comparison table**: per scene, the snapshot's nine totals beside the figures extracted from the
      stored PDF, and the per-line tax components beside theirs, including which components were expected to be
      absent and were.
- [ ] **The transcripts of the two deliberately-failing runs** (wrong figure, missing artefact), so a reviewer can
      see the assertions are reached.
- [ ] **The three sample PDFs** attached to the pull request — one invoice, one credit note, one debit note, one of
      them carrying a Tamil name — each with its SHA-256, and a statement that every figure and name in them is
      synthetic.
- [ ] The rendered diff of `docs/billing/accountant-document-review.md` and of the
      `docs/billing/go-live-plan.md` section 2.7 amendment.
- [ ] The completed `docs/nfr/a11y-checklist.md` section 5.6 records for the rendered documents.
- [ ] The deferral record as it appears on the issue, with its six fields, and the one-line statement that **#42
      stays open until the accountant signs**.
- [ ] `migrate --dry-run` showing no outstanding migration; `dotnet format --verify-no-changes` clean; a
      warning-free Release build; `./scripts/dev docs` passing; a one-line statement that no OpenAPI difference exists
      because no endpoint changed.

## Depends on

**E09-F02-9** — must be merged first. It delivers the `UglyToad.PdfPig` entry in `Directory.Packages.props` and
`PdfText.cs`. Starting before it lands means writing both twice, and the two slices would conflict in
`Directory.Packages.props`.

#155 — merged; it delivers the renderer, the templates, the artefact store, the worker render pass and the streaming
download.

#147 — merged; it delivers `IPricingService.FindSnapshotAsync` and the golden master.

#154 — merged; it delivers posting, the numbers and the credit and debit notes the samples are rendered from.

**No dependency on E09-F02-4 to E09-F02-8**, and no file is shared with them. Safe to run concurrently with the four
client slices and with **E09-F02-8**, but **not** with E09-F02-9.

The accountant's availability is the one thing outside engineering's control. The deferral record is what makes the
slice mergeable without it, and the review record is what makes the gap visible rather than forgotten.

Refs #42
