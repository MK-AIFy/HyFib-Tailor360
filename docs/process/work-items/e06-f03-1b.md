Sub-issue of #34, and the second of three catalog slices. **E06-F03-1a creates the four tables and the template and
version lifecycle; this slice fills a draft version with content.** The two child tables `catalog.qc_criteria` and
`catalog.defect_codes` exist after 1a and no route writes them: the criteria typed pass/fail, measurement tolerance
and fit check that plan Section 8 (#34) names, and the defect vocabulary a failed criterion chooses from, are what a
QC result in E06-F03-2a copies and renders against forever
([`docs/prd/category-hierarchy.md`](../../prd/category-hierarchy.md) line 228: "The QC checklist version plus a JSON
copy of the criteria evaluated — historic QC results render identically after the checklist changes").

**This slice adds no migration.** 1a created both tables for exactly this reason, so the `catalog` migration chain
takes one migration for the whole QC area rather than three.

## What is already built (do not re-build)

- **From E06-F03-1a** (its pull request must have merged): `Domain/Quality/{QcChecklistTemplate,QcChecklistVersion,
  QcChecklistStatus,QcCriterion,DefectCode,QcCriterionKind}.cs`, `IQcChecklistStore`, `QcChecklistStore`,
  `QcChecklistHandler` with its audit constants, `Api/Payloads/QcChecklist{Payloads,Requests}.cs`, the four tables,
  the two immutability triggers and the four routes. Extend those files; create no parallel handler or store.
- `src/Modules/Customers/Tailor360.Modules.Customers.Domain/Measurements/TemplateField.cs` (366 lines) and
  `TemplateVersion.cs` (526) — **the shape to copy** for a typed child row owned by a version: the field key, the
  kind, the ordering, the uniqueness rule within the version, and the refusal of every mutator once the version
  leaves draft. Mirror it; do not reference it (ARCH-004).
- `…Catalog.Domain/Catalogue/CatalogVersion.Design.cs` (568 lines) — how Catalog itself owns ordered child rows on a
  version (`design_option_groups`, `design_options`, `design_rules`), including that they are cloned into a new
  draft with fresh rows and the same stable key. The same reading applies to criteria and defect codes:
  `module-ownership.md`'s catalog table list says `defect_codes` belong to the **version**, as `design_options` do.
- `…Catalog.Application/Catalogue/CatalogHandler.Design.cs` (666 lines) — the add / change / remove command set for
  version-owned child rows, and the audit action naming `catalog.<entity>.<verb>` at lines 25-49.
- `…Catalog.Api/Catalogue/CatalogDesignEndpoints.cs` (498 lines) and `Api/Payloads/DesignPayloads.cs` (261) — the
  route and payload shape for editing a draft version's children.
- `…Catalog.Domain/Catalogue/CatalogCode.cs` — `MaximumLength` 40. A criterion code and a defect code are
  `CatalogCode`s; do not invent a second code type or a second length.

## How an endpoint is declared in this repository

There is one accepted shape and it is not optional. Copy it from
`src/Modules/Billing/Tailor360.Modules.Billing.Api/Invoicing/InvoiceEndpoints.cs` (a write at lines 190-196, a read
at 139-141), which is the closest merged example:

```
    .RequirePermission(<PermissionKey>, BranchScope.CurrentBranch)   // ARCH-007
    .ScopedToResource(<ResourceKinds.Thing>, "<routeValueName>")     // ARCH-023, when the route names a row
    .RequireRateLimiting(RateLimitPolicyNames.Write)                 // ARCH-017, exactly one
    .Audited(<Handler>.<Something>Action, reasonRequired: true)      // ARCH-008, every write
    .RequireIdempotency()                                            // where a client may retry
    .RequireIfMatch()                                                // where the row carries an ETag
    .WithRequestTimeout(RequestTimeoutPolicies.Command)               // every endpoint in the repository has one
```

- Verbs: `Platform.Security/Endpoints/EndpointAuthorisationExtensions.cs` (`RequirePermission(key, scope)`,
  `Audited(action, reasonRequired)`, `AllowAnonymousWithJustification`);
  `ResourceScopeEndpointExtensions.cs` (`ScopedToResource(resourceKind, routeValueName, scope)`, and the
  deliberately noisy exemption verb for a route whose identifier names nothing branch-owned);
  `RequestSafetyEndpointExtensions.cs` (`RequireIdempotency`, `RequireIfMatch`);
  `StepUpEndpointExtensions.cs` (`RequireStepUp`).
- **Rate-limit policies**, all of them, from `Platform.Security/Endpoints/RateLimitPolicyNames.cs`:
  `DefaultUser` = `default-user` (ordinary authenticated read), `Write` = `write` (authenticated state change),
  `ScanBurst` = `scan-burst`, `ExportHeavy` = `export-heavy`, plus `AuthenticationAnonymous`,
  `MultiFactorChallenge`, `RecoveryAnonymous`, `DefaultIp`. There is no other policy, and inventing one is a
  change to the limiter configuration in the web host.
- **Request timeouts**: `Platform.Security/Endpoints/RequestTimeoutPolicies.cs` — `Read` = `read`,
  `Command` = `command`, `Report` = `report`, `Export` = `export`. **Every** endpoint in `src/Modules/` carries one
  today (grep `WithRequestTimeout` — there is no exception), so an endpoint without one is the odd one out.
- **Branch scopes** from `Platform.Abstractions/Multitenancy/BranchScope.cs`: `CurrentBranch` (the default),
  `AssignedBranches`, `Organisation`, and the member that demands no branch reach at all.
- **Resource kinds**: each module keeps its own constants class — `BillingResourceKinds`,
  `MeasurementResourceKinds`, `DesignSelectionResourceKinds` — in its `Application` project, with an
  `IResourceScopeResolver` per kind in its `Infrastructure` project, registered with `TryAddScoped`.
- **The matrix row** goes in `docs/security/permission-matrix.md` section 5, *inside* the
  `<!-- matrix:endpoints -->` … `<!-- /matrix:endpoints -->` markers (lines 311-512 today), with the columns
  `| Method | Route | Declaration | Permission | Branch scope | Resource | Audited as | What it is for |`.
  `AuthorisationMatrixTests` fails and names any published route with no row, and any row with no route.

## Scope

- [ ] Fill out `Domain/Quality/QcCriterion.cs`: `code` (a `CatalogCode`), `name`, `name_tamil`, `kind`,
      `display_order`, `is_mandatory`, `requires_evidence`, and — for `MeasurementTolerance` only — the measurement
      **field key** the criterion is about and the tolerance the administrator entered. A code is unique within its
      version. **No tolerance figure, no criterion count, no severity vocabulary and no default criterion set is
      written into code** — they are catalogue data under **OD-10**.
- [ ] Fill out `Domain/Quality/DefectCode.cs`: `code`, `name`, `name_tamil`, `active`, `display_order`, owned by the
      **version**, immutable once it is published — because a QC result copies the defect it recorded and must render
      identically afterwards (`category-hierarchy.md` line 228).
- [ ] Version-content commands on `QcChecklistHandler`: add, change and remove a criterion; add, change and remove a
      defect code; each refusing unless the version's status is `Draft`, each taking the version's `If-Match`, each
      with its own audit action in the house form — `catalog.qc_criterion.added`, `catalog.qc_criterion.changed`,
      `catalog.qc_criterion.removed`, `catalog.defect_code.added`, `catalog.defect_code.changed`,
      `catalog.defect_code.removed`.
- [ ] Two routes under `/api/v1/catalog`, both `catalog.checklists.edit` with `BranchScope.Organisation`, one
      rate-limit policy each, the audit filter, `.RequireIdempotency()`, `.RequireIfMatch()` and a request timeout:
      `PUT /qc-checklists/{templateId}/versions/{versionId}` — **replaces the whole content set** of a draft version
      (criteria and defect codes together) the way `PUT /api/v1/catalog/design-drafts/{draftId}` replaces a whole
      selection set, so a caller cannot half-apply an edit; and
      `GET /qc-checklists/{templateId}/versions/{versionId}` — reads a version with its criteria in `display_order`
      and its defect codes, and returns the tag the next `PUT` sends back as `If-Match`.
      Neither declares `.ScopedToResource(...)`: these name organisation-scoped configuration rows, so ARCH-023 is
      satisfied by the recorded exemption verb with the reason stated in the code.
- [ ] Extend `Api/Payloads/QcChecklist{Payloads,Requests}.cs` with the criterion and defect-code records. Public
      payload types stay in the `Api` project and no `Domain` type is returned (ARCH-013).
- [ ] Rows for both routes in the section 5 endpoints block of `docs/security/permission-matrix.md`, and the
      `identifier-editing` rows for `{templateId}` and `{versionId}` in
      `tests/Tailor360.IntegrationTests/Authorization/matrix.yaml`.

## Out of scope

The tables, the migration, the template and the draft-version lifecycle — **E06-F03-1a**. Publish and retire,
`IQcChecklistQuery`, the `catalog.qc-checklist-not-published` publication finding and the seeded drafts —
**E06-F03-1c**. Recording a QC result — E06-F03-2a. Any screen, for the reason E06-F03-1a's **Out of scope** gives.
**No migration**: if a column turns out to be missing, it is added by a follow-up migration named in the same pull
request and the register row says why, rather than by editing 1a's migration after it has merged
(`src/Modules/CLAUDE.md` section 6).

## Acceptance criteria

- [ ] An Owner adds criteria of all three kinds and a defect-code vocabulary to a draft version in one `PUT`, and
      reads the version back with its criteria in `display_order`, each criterion's kind and mandatory flag, and its
      defect codes.
- [ ] A `MeasurementTolerance` criterion round-trips its measurement field key and its tolerance as the
      administrator entered them, and a `PassFail` or `FitCheck` criterion carrying either is refused by validation
      with the field named — not silently accepted and not silently dropped.
- [ ] Both routes declare a permission with `BranchScope.Organisation` (ARCH-007), exactly one rate-limit policy
      (ARCH-017), the audit filter on the write (ARCH-008) and the ARCH-023 exemption with its reason; the
      endpoint-inventory contract test shows it.
- [ ] **Negative**: editing the content of a **published** version is refused with a named Catalog problem code and
      no row changes; two criteria with the same code in one version are refused; a stale `If-Match` is refused
      `412` before any write; a caller holding `catalog.read` but not `catalog.checklists.edit` receives `403`
      `security.forbidden`; a replayed `Idempotency-Key` returns the first outcome.
- [ ] **Exception**: a `PUT` that removes a criterion still leaves the version readable and consistent — the
      remaining `display_order` values have no gap the read cannot render — and a `PUT` with an empty criteria set is
      refused, because a version with no criteria is not a checklist and E06-F03-1c would let it be published.
- [ ] **Exception**: `published_qc_criteria_are_immutable` refuses an `UPDATE` of a criterion belonging to a
      published version issued directly from the application role, which is the database half of the same rule.

## Data and classification

As E06-F03-1a: configuration data has **no section 5 category** in
[`docs/nfr/data-classification.md`](../../nfr/data-classification.md), whose 5.1 index runs 5.2 to 5.20. The class is
the **Internal** row of the section 4 class table ("Catalogue and workflow definitions …") with retention "Life of
the configuration version". Do **not** cite section 5.6 — that is QC, custody and delivery *evidence*, which is
Sensitive Personal and belongs to Media.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| `catalog.qc_criteria` (code, name, name_tamil, kind, measurement field key, tolerance, display_order, is_mandatory, requires_evidence) | Internal — section 4 class table | What is actually inspected | Life of the configuration version | `catalog.read` holders; edited by `catalog.checklists.edit` | Safe. A tolerance is a **shop standard**, not a customer measurement: no value from a measurement version is stored here, which is why section 5.4 does not reach these rows |
| `catalog.defect_codes` (code, name, name_tamil, active, display_order) | Internal | The defect vocabulary a failed criterion names | As above | As above | Safe; a defect **name** is what a screen shows (`docs/nfr/a11y-checklist.md` A11Y-PH-06 at line 725) |
| Audit actions `catalog.qc_criterion.{added,changed,removed}`, `catalog.defect_code.{added,changed,removed}` | Internal — evidentiary | Prove who changed the standard and when | Per section 5.16 | `auditor`, `owner` | Audit store only |

No new media class, no credential, no secret, no export column and no telemetry attribute.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `catalog.checklists.edit` | organisation | no | no | no | `owner` (Owner) | **Exists** — matrix line 158. Granted `SystemRoles.cs:106`. Used by both routes |

No permission key and no grant is added. **OD-13** is open, so the grant is the seeded default.

## Modules, contracts and events

Catalog only. No `Contracts` project is consumed or published, no integration event is added, no table outside
`catalog` is touched (ARCH-005). Threat-model coverage is **DOR-05's pre-#56a alternative**, not a written model:
[`docs/security/README.md`](../../security/README.md) section 3 records that only the authentication model exists (#23)
and that `abuse-cases.md` is **not written**, with the rest scheduled under #56a — the order-workflow model
specifically in the batch-1 child `E14-F01a-4`. So the abuse case is **named here and carried into that backlog
entry**: as E06-F03-1a, the catalogue administration flow carries the configuration-tampering abuse case, and this
slice adds no new trust boundary.

## Screens and states

Deleted: backend slice, no screen.

## Migration and rollback

**No migration.** Both tables and both triggers were created by E06-F03-1a's `QcChecklistTemplates`. The pull
request proves the absence with `migrate --dry-run` reporting nothing outstanding and
`dotnet ef migrations has-pending-model-changes` clean. Rollback is redeploying the previous tag: the rows stay and
nothing reads them until E06-F03-1c publishes a version. Per [`docs/dev/migrations.md`](../../dev/migrations.md).

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-10** initial catalogue, templates, design options and **QC checklists**, reviewed from the seeded drafts | **Open** — `docs/prd/assumptions-and-open-decisions.md` line 63 | The criterion kinds are code; every criterion, tolerance and defect code is **data**. Nothing is seeded here and nothing is published | W2 per the decision's own row |
| **XQ-01** the configurable reason-code lists for hold, cancellation, rework, delivery failure and custody correction | **Open** — `docs/prd/exceptions.md` line 197 | A defect code is any `CatalogCode` an administrator enters; no severity ranking and no ordering beyond `display_order` is introduced, and no vocabulary is written into code | The exception review #17 records |
| **OD-13** approved role-to-permission grants | **Open** — line 66 | `catalog.checklists.edit` keeps its seeded grant | W1 exit |

## Required verification

- [ ] unit: a criterion code unique within a version; every content mutator refused on a published or retired
      version; a `MeasurementTolerance` criterion requiring its field key and tolerance and the other two kinds
      refusing them; `display_order` normalisation after a removal; `QcCriterionKind` parsing rejecting a number that
      names no member.
- [ ] integration: both routes against real PostgreSQL, including every **negative** case above — published-version
      edit refused, duplicate criterion code, `412` on stale `If-Match`, `403` without the permission, empty
      criteria set refused, idempotency replay — and `published_qc_criteria_are_immutable` refusing a direct
      `UPDATE`.
- [ ] contract: OpenAPI lint and diff over the two routes; the endpoint inventory shows a permission, one rate
      limit, a request timeout and an audit action for the write; `AuthorisationMatrixTests` green with the new rows.
- [ ] migration: `migrate --dry-run` reports nothing outstanding and `has-pending-model-changes` is clean — the
      evidence that this slice adds no schema.
- [ ] NFR rows touched: **NFR-MQ-05** in [`docs/nfr/traceability.md`](../../nfr/traceability.md) line 276; the **S2**
      read row and **S3** command row of [`docs/nfr/slo.md`](../../nfr/slo.md) lines 130-131, with measured figures.
- [ ] environment: `./scripts/dev doctor`; `CI=true` makes a skipped integration tier a failure (DOR-16).

## Evidence the pull request will carry

- [ ] `./scripts/dev test unit|architecture|contract|integration` output, run twice.
- [ ] `migrate --dry-run` showing nothing outstanding and `has-pending-model-changes` clean.
- [ ] The OpenAPI difference report for the two operations and `pnpm --dir clients/pwa generate:api:check`.
- [ ] The `permission-matrix.md` section 5 rows, the `matrix.yaml` diff and the `AuthorisationMatrixTests` run.
- [ ] An HTTP transcript filling a draft version with criteria of all three kinds and a defect vocabulary, then
      reading it back in `display_order` — synthetic data only — beside the audit rows it wrote.
- [ ] `dotnet format --verify-no-changes` clean.

## Depends on

**E06-F03-1a** — for the four tables, both triggers, the Domain types, the store, the handler and the payload files
this slice extends. It must have merged; there is no half-way state in which this slice is buildable.

Nothing else unmerged.

Same-file sequencing: `CatalogDbContext.cs` (mapping only, no migration), `QcChecklistHandler.cs`,
`QcChecklistStore.cs`, the `QcChecklist*` payload files, `CatalogEndpoints.cs`, `docs/api/openapi.v1.json`,
`clients/pwa/src/api/schema.d.ts`, `docs/security/permission-matrix.md` and `matrix.yaml` are shared with
E06-F03-1a and E06-F03-1c and with #91's reconciliation work. One at a time; the second to merge rebases and
re-runs the Catalog tiers.

Refs #34
