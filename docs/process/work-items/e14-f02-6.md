Sub-issue of #57. The parent's fifth acceptance criterion — "secret/key rotation succeeds without source changes and
with documented service impact" — is the only one of the five with no artefact at all today.
`docs/nfr/security-operations-targets.md` section 4 states the obligation in terms of this issue: "**Rotation is
rehearsed.** #57 delivers a rotation runbook and an emergency revocation runbook … A rotation that has never been
performed is an assumption, not a control." `ls docs/runbooks` fails — the directory does not exist. Worse, two
statements in `docs/platform/secrets.md` are written as fact and are not true of this working copy: "A test asserts
that sentinel values written into each secret file never appear in logs, exception output, problem details, health
responses or exporter output" (no such test exists — `grep -rn "sentinel" tests/` finds only
`FaultEndpoints.Sentinel` in `tests/Tailor360.IntegrationTests/Security/ErrorLeakTests.cs`, which is one fabricated
exception path, not the secret files), and "The ring itself is encrypted with a certificate or a key-management
service" (`src/Platform/Tailor360.Platform.Persistence/DataProtection/DataProtectionServiceCollectionExtensions.cs`
lines 47–49 call `SetApplicationName`, `SetDefaultKeyLifetime` and `PersistKeysToDbContext<PlatformDbContext>` and
**nothing else** — no `ProtectKeysWith…`). This issue closes the gap and corrects the document.

## What is already built (do not re-build)

- **Secrets arrive as files and the mechanism works.** `src/Hosts/Tailor360.Web/Program.cs` lines 50–53,
  `src/Hosts/Tailor360.Worker/Program.cs` line 34 and `src/Tools/Tailor360.Cli/CliHost.cs` line 42 all read
  `Secrets:Directory` and call `AddKeyPerFile`. The two long-running hosts pass `reloadOnChange: true`; the CLI
  passes `false`. That asymmetry is real and is the single most important thing the runbook must say.
- `docs/platform/secrets.md` (59 lines) — the file layout, the `__` → `:` mapping, the `LogRedaction` description and
  a four-row rotation table. Correct it; do not rewrite it from scratch.
- `docs/nfr/data-classification.md` section 6 — the class as a whole and **section 6.2, the full secret register**:
  fourteen rows, each with where the secret lives, what protects it, its rotation cadence and what to do on
  compromise. **This is the register. Do not write a second one**; the runbook references it row by row.
- `docs/nfr/security-operations-targets.md` section 4 — the rotation cadence table, including which cadences are
  marked "**Proposed, to be confirmed**". No cadence is invented by this issue.
- `src/Platform/Tailor360.Platform.Observability/Logging/LogRedaction.cs` (87 lines) — the property-name list
  (`password`, `secret`, `apikey`, `token`, `authorization`, `cookie`, `mfasecret`, `recoverycode`,
  `connectionstring`, `privatekey`, `certificate`, `backupkey`, `encryptionkey`, `webhooksecret`, `cardnumber`,
  `cvv` and the rest), the masking of phone and email, and the `Bearer` regex. Extend the list rather than trusting
  call sites (CLAUDE.md security rule 7).
- `tests/Tailor360.IntegrationTests/Security/ErrorLeakTests.cs` — the existing leak harness, including a fault
  endpoint whose exception carries a sentinel and a fabricated absolute path. Extend this file's approach; it already
  knows how to assert absence across problem details and logs.
- **Revocation is built AND already proved. Do not write a second test for it.**
  `POST /api/v1/auth/logout-all` is mapped at
  `src/Modules/Identity/Tailor360.Modules.Identity.Api/Authentication/AuthenticationEndpoints.cs` line 231, backed by
  `SessionService.RevokeAllForUserAsync`
  (`src/Modules/Identity/Tailor360.Modules.Identity.Infrastructure/Sessions/SessionService.cs` line 247), and the
  coverage exists:
  `tests/Tailor360.IntegrationTests/Identity/SessionLifecycleTests.cs` line 84,
  `SigningOutEverywhereEndsTheOtherDevicesAndSparesThisOne`, asserts the revoked tickets resolve `Revoked` and the
  spared one stays `Active`, and `tests/Tailor360.IntegrationTests/Identity/AuthenticationEndpointTests.cs` drives the
  route itself at line 273. Cross-host propagation — the **S11** row of `docs/nfr/slo.md` line 139, ≤ 60 s at p99 on
  every host — needs two hosts and is **#58**'s synthetic probe; the propagation *target* `NFR-SE-03`
  (`docs/nfr/traceability.md` line 143) is scheduled against **#23**, not #57, and #56's `[E14-F01b-4c]` owns the
  abuse-case side of it. **This issue cites all of that in the revocation runbook and writes no new test.**
- **Export expiry is built AND already proved. Do not write a second test for it.**
  `src/Hosts/Tailor360.Worker/Jobs/CustomerExportPurgeService.cs` expires the copy, `CustomerEndpoints.cs` line 212
  documents the `404 customers.export-expired` answer, and
  `tests/Tailor360.IntegrationTests/Customers/CustomerExportEndpointTests.cs` already asserts that code at **lines 202
  and 243** — the stale copy and the refused download. The runbook cites those two assertions by name.
- `.github/workflows/ci.yml` lines 539–560 — `gitleaks` already scans pull-request history with the default rule set
  extended by `.gitleaks.toml`. **This issue changes no workflow file**; #56's children own CI gate changes.
- `docs/process/waivers.md` section 4.2 — the Data Protection ring's unencrypted state is **already** recorded as a
  known gap, as **RR-04** in `docs/security/threat-models/authentication.md`, owned by the technical reviewer,
  corrective issue **#59**, gate RG-10. There is nothing to add there and no waiver to write.

## What is missing

- No `docs/runbooks/` directory, so neither the rotation procedure nor the emergency revocation procedure exists in
  any form.
- No test proves a secret never leaves the process. The claim in `docs/platform/secrets.md` is aspirational and reads
  as settled.
- No test proves a rotated secret file is picked up without a source change, nor that the CLI needs a restart where
  the hosts do not.
- No rehearsal record. Per `docs/nfr/security-operations-targets.md` section 4, that makes every cadence in the
  register an assumption.

## Size, stated up front

Fourteen rotation procedures and a fourteen-row revocation decision table are the bulk of this issue, and they are
**documentation**, not production code: the only `src/` change is `LogRedaction.cs`, and only if a leak is found.
Budget roughly 400 lines of `docs/` and 300 lines of `tests/`, which is one session with headroom **once the two
already-proved tests below are struck from it**. If the rehearsal overruns, the cut is not a procedure — every one of
the fourteen is needed for the register to be complete — it is the Data Protection ring rotation test, which would
then need its own issue rather than a mid-flight scope change.

## Scope

- [ ] **`docs/runbooks/README.md`** — an index, because `docs/nfr/security-operations-targets.md` and #58's alert
      `runbook_url` convention both expect `docs/runbooks/` to be a place. One paragraph plus a table; #58 and #60 add
      their own pages to it.
- [ ] **Key ownership, which nothing else in #57 carries.** The parent's sixth implementation step ends "and document
      key ownership", and `docs/IMPLEMENTATION_PLAN.md` Section 8 puts it in a
      `docs/privacy/data-inventory.md` that #57's children deliberately do **not** create, because
      `docs/nfr/data-classification.md` sections 5 to 7 already are that inventory and a second one can disagree with
      the first. So key ownership lands here instead, as a **column in the section 6.2-shaped table of
      `docs/runbooks/secret-rotation.md`**: for each of the fourteen secrets, who holds it, who may read it, where the
      escrow copy lives and who may rotate it — a **role**, never a person, because **OD-15** is open. Record the
      deviation from the plan's file name, and why, in the pull request; it is the one place #57 departs from the
      blueprint's document layout.
- [ ] **`docs/runbooks/secret-rotation.md`** — one numbered procedure **per row of
      `docs/nfr/data-classification.md` section 6.2**, in that document's order, each stating: the file name under
      `/run/secrets` (so `Database__ConnectionString`, not "the database password"), who may perform it, whether the
      host picks it up by reload or needs a restart, the **service impact in words a shop owner can act on** ("the
      till will refuse payments for about N seconds while the web host restarts" — N measured during the rehearsal,
      not guessed), the verification step, and the rollback if the new secret is wrong. Cadences are **cited** from
      `docs/nfr/security-operations-targets.md` section 4, never restated as numbers of this document's own.
- [ ] **`docs/runbooks/emergency-revocation.md`** — the "rotate first, investigate second" order that
      `docs/nfr/data-classification.md` section 6.1 mandates, with a decision table: which secret, what it can reach,
      the single command or action that revokes it, what breaks immediately, and the audit trail to read afterwards.
      It covers the break-glass read of a secret — which section 6.1 requires to be audited **and followed by
      rotation** — and points at the incident process in `docs/nfr/security-operations-targets.md` section 6 rather
      than inventing a second one.
- [ ] **Correct `docs/platform/secrets.md`.** Replace the two untrue sentences with what is true today: the sentinel
      test now exists and what it covers; and the ring is persisted to `platform.data_protection_keys` **unencrypted**,
      recorded as RR-04 and as a known gap in `docs/process/waivers.md`, closed by #59. Add the reload-versus-restart
      asymmetry between the hosts and the CLI. Point the rotation table at section 6.2 rather than holding a fourth,
      shorter copy of it.
- [ ] **Correct one more untrue sentence, in a second document.**
      `docs/architecture/module-ownership.md` **line 704** — the `data_protection_keys` row — says the gap is
      "recorded as **W-002** in `docs/process/waivers.md`". **There is no W-002.** `waivers.md` section 4.2 says in so
      many words "No waiver has been granted", its register table holds a single `_(none)_` row, and
      `grep -rn "W-002" docs/` finds that one reference and nothing it points at. Replace it with the known-gaps row
      that does exist, keeping the **RR-04** reference, which is correct. This is a one-cell edit and it belongs here
      because this is the child that owns the key-ring documentation; the other children of #57 are told not to touch
      that line.
- [ ] **The secret-file sentinel test** in `tests/Tailor360.IntegrationTests/Security/`: write a distinct sentinel
      value into a temporary secrets directory for **every** configuration key the hosts bind from
      `/run/secrets` — at minimum `Database__ConnectionString`, `ObjectStorage__AccessKey`,
      `ObjectStorage__SecretKey` and the provider keys the options classes declare — point `Secrets:Directory` at it,
      and assert no sentinel appears in: captured log output, any problem-detail body, the three health responses
      (`/health/live`, `/health/ready`, `/health/startup`), the OpenAPI document, or the exception text when start-up
      validation is deliberately made to fail. The last case is the point: `docs/platform/secrets.md` already
      identifies start-up validation failure as "the moment a configuration value is most likely to be echoed".
- [ ] **The rotation test**: with `reloadOnChange: true`, rewrite a bound non-secret-critical file in the secrets
      directory and assert the `IOptions` monitor observes the new value with no redeploy and no source change; assert
      the CLI host, configured `reloadOnChange: false`, does **not** — and that the runbook says so.
- [ ] **Cite, do not re-test, revocation and export expiry.** The parent's last implementation step asks for
      "logout/session revocation, export expiry, media deletion, customer-link revocation and key/secret rotation"
      to be tested. Two of the five are already tested in this working copy — named above with their file and line —
      so this issue's contribution is to **cite them from the runbooks' verification steps** and to record in the
      pull request that it found them rather than writing a second copy. Media deletion is **#192**; customer-link
      revocation is **#211**. Only key and secret rotation is unproved, and that is what the tests below cover.
- [ ] **The Data Protection ring test**: rotate the ring (a new key while the old one is retained) and assert an
      anti-forgery token minted before the rotation is still accepted during the overlap, and that the ring's key
      lifetime is the 90 days `docs/nfr/security-operations-targets.md` section 4 and plan Section 4.4 both state —
      read from configuration, not asserted as a literal in two places.
- [ ] **The rehearsal, performed and recorded.** Rotate the database password and an object-storage key in the
      `infra/` compose stack, end to end, timing the interruption, and paste the transcript into the pull request and
      summarise it in the runbook's service-impact column. This is the evidence that converts the register's cadences
      from assumptions into controls.
- [ ] **Extend `LogRedaction` only if the rehearsal finds a gap**, and say so either way. A sentinel that leaks is a
      defect fixed in this pull request, with the redaction entry added by property name and a test that would have
      caught it. The rule it serves is CLAUDE.md security rule 7 and
      `docs/nfr/data-classification.md` section 8, "Logging, telemetry and the redaction policy" — **not** section
      5.17, which is the application-logs inventory row and says nothing about deletion or anonymisation.

## Out of scope

**Encrypting the Data Protection ring** (`ProtectKeysWith…` and provisioning the key-encryption certificate) is
**#59** — it is already an accepted, owned, dated residual risk as RR-04 with #59 as the corrective issue, and
re-litigating it here would duplicate that work. **No waiver is written by this issue**: `docs/process/waivers.md`
section 2 is explicit that a waiver is granted against a release and that none has been made, and the gap is already
in that document's known-gaps table. **Media deletion** verification, named in the parent's last implementation step,
needs the Media module, which is scaffolding only (`src/Modules/Media/…/MediaModuleServiceCollectionExtensions.cs`
line 14 declares the schema constant and `AddMediaModule` returns `services` unchanged) — it belongs to #187 and
#192. **Customer-link revocation** verification needs the `customer_links` mechanism, which `grep` confirms does not
exist and which **#211** builds. TLS termination, encrypted volumes and server-side encryption on buckets are
infrastructure owned by #59 and #60. Alert routing and the `runbook_url` wiring are **#58**. `.github/workflows/`
changes belong to #56's children.

## Acceptance criteria

- [ ] A sentinel written into every secret file appears in **none** of: the captured logs, any problem detail, the
      three health responses, the OpenAPI document, or the start-up validation failure message. The test names each
      key it covered, so a new secret key added later without a sentinel is visible in the output.
- [ ] Rewriting a bound file in the secrets directory changes the value the web host reads, with no code change, no
      image rebuild and no configuration edit — the parent's fifth criterion, observed rather than asserted.
- [ ] `docs/runbooks/secret-rotation.md` carries one procedure per row of `docs/nfr/data-classification.md` section
      6.2, each with a measured service impact, and a reader can follow the database-password procedure against the
      compose stack and reach a healthy system.
- [ ] **Negative**: a secrets directory containing a file whose name maps to an unknown configuration key does not
      fail start-up silently nor leak the file's contents in the message — the behaviour is stated in the runbook
      whichever way it falls, and the test records it.
- [ ] **Negative**: the emergency-revocation runbook's "read the audit trail afterwards" step is followed once, for
      one credential, and the entries it names are actually present — so the runbook's last step is proved to be
      followable rather than plausible.
- [ ] `docs/runbooks/emergency-revocation.md` and `docs/runbooks/secret-rotation.md` cite the **existing** revocation
      and export-expiry assertions by file and line (`SessionLifecycleTests.cs` line 84,
      `AuthenticationEndpointTests.cs` line 273, `CustomerExportEndpointTests.cs` lines 202 and 243) rather than
      asking for new ones, and the pull request says so explicitly. A reviewer looking for "the logout test #57
      promised" must find a citation, not a second test.
- [ ] **Exception**: rotating a secret to a **wrong** value fails start-up with a message that names the
      configuration key and not the value, and the runbook's rollback step restores service. The test drives the
      wrong value deliberately.
- [ ] **Exception**: the CLI, configured `reloadOnChange: false`, keeps the old value until it is restarted, and the
      runbook says which procedures therefore need a CLI restart as well as a host reload.
- [ ] `docs/platform/secrets.md` contains no statement that is untrue of the code in the same commit, and a reviewer
      can check each of its claims against a named file or test.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Sentinel values used by the test | **Credentials and secrets** by treatment, though they protect nothing | Prove absence | Per test run; never committed as a real value. `.gitleaks.toml` already allowlists documented literals, and the sentinels are generated per run rather than hard-coded where that is possible | The test only | **Asserted absent** from logs, problem details, health responses, the OpenAPI document and exception text — that is the test |
| Rotation runbook content | Internal | Let an operator rotate a secret correctly under pressure | As the repository | Anyone with repository access | The runbook carries **no secret value, no host name that is not already public, and no example credential** (`docs/nfr/data-classification.md` section 6.2: never in an export, a screenshot or a bug report) |
| Measured service-impact timings | Internal | Tell the shop what a rotation costs | As the repository | As above | Safe |
| Rehearsal transcript in the pull request | Internal | Evidence the control was exercised | As the pull request | Reviewers | Scrubbed before pasting: the transcript shows the key names and the outcome, never a value. Reviewed against section 6.2's "never in a support bundle or a bug report" |
| `platform.data_protection_keys` | **Credentials and secrets** | The key ring | Unchanged by this issue | No human routinely | Unchanged, and **unencrypted today** — RR-04, closed by #59. This issue documents that fact rather than changing it |
| Audit event for a break-glass secret read | Personal metadata, evidentiary | Prove a human read a secret, which obliges rotation | Per `docs/nfr/data-classification.md` section 5.16 | `admin.audit.read` holders | The entry records that a read happened and by whom, never what was read |

## Permissions and roles

No new permission key, and no change to any grant. The rotation and revocation procedures are **operator** actions
performed against the deployment — a file on the host, a credential at a provider — not HTTP endpoints, so there is no
route to authorise. The two endpoints the verification tests drive already exist with their own grants:

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| — (`POST /api/v1/auth/logout-all`) | session-bound | — | — | — | any signed-in principal, for their own sessions | **Exists** — the endpoint is already mapped in `AuthenticationEndpoints.cs` line 231; this issue adds no authorisation and changes none |
| `customers.export` | organisation | yes | no | yes | per the existing matrix row (line 497) | **Exists**, unchanged and **not exercised by a new test here** — the export-expiry assertions already live in `tests/Tailor360.IntegrationTests/Customers/CustomerExportEndpointTests.cs` lines 202 and 243, and the runbook cites them |
| `admin.audit.read` | organisation | yes | no | no | `owner`, `auditor` | **Exists** — named because the runbook's "read the audit trail afterwards" step depends on it, and a reader should know who can take that step |

**OD-15** (who operates the system, and the named deputy) is open. The runbooks therefore name a **role** — "the
operator named under OD-15" — and never a person, exactly as `docs/nfr/data-classification.md` section 5.17 already
does for log access. This is the one place in #57 where naming a person would be an invented fact.

## Modules, contracts and events

No module. No schema, no migration, no contract and no integration event. The production changes are confined to
`src/Platform/Tailor360.Platform.Observability/Logging/LogRedaction.cs` (only if the rehearsal finds a leak) and,
where the sentinel test needs a seam, the host configuration wiring in `src/Hosts/Tailor360.Web/Program.cs` — which
is already the file that reads `Secrets:Directory`. Everything else is `docs/` and `tests/`. ARCH-005, ARCH-003 and
ARCH-004 are untouched because no project reference changes.

Threat-model coverage: the abuse cases are "a secret is read from a log, a crash dump or a support bundle" and "a
compromised secret stays valid because nobody knows how to revoke it". The first is contained by `LogRedaction` plus
the sentinel test this issue adds; the second by the emergency revocation runbook and the rehearsal. The relevant
backlog entry is the deployment-and-runtime threat model scheduled by #56's children
(`docs/security/README.md` section 3.2); this issue adds no trust boundary and no new secret.

## Screens and states

Deleted: this issue adds no screen and changes none. The only client-visible behaviour it touches is the refusal a
revoked session already receives, which the existing authentication screens already render.

## Migration and rollback

No schema change. This will be proved, not asserted: the pull request carries
`dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` reporting no outstanding change, and
`dotnet ef migrations has-pending-model-changes` reporting none. Rollback of a documentation and test change is
reverting the commit; rollback of a **rotation** is the runbook's own rollback step, which is the thing the rehearsal
is there to validate.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-15** — who operates the system, and the deputy | Open | The runbooks name the role ("the operator named under OD-15"), never a person | The first production release |
| **OD-03** — the messaging and payment providers | Open | The provider-credential procedure is written once, per credential **class**, so it applies whichever provider is chosen. No provider-specific console steps are written | #55 |
| **OD-14** — the telemetry backend | Open | The sentinel test asserts absence from the application's own log output and exporter configuration; absence from a specific hosted backend's UI is #58's to verify | #58 |
| Cadences marked "**Proposed, to be confirmed**" in `docs/nfr/security-operations-targets.md` section 4 (webhook secrets, provider credentials, database roles, object-storage keys at 180 days; backup passphrase at 12 months) | Open | The runbook **cites** those rows and marks each as proposed. No cadence is restated as settled and none is invented | The stakeholder review signs that document |
| **RR-04** — the unencrypted Data Protection ring | Accepted residual risk, dated 2026-09-05, corrective issue #59, gate RG-10 | Documented as it is. This issue corrects `docs/platform/secrets.md` to match reality and writes **no** waiver, because `docs/process/waivers.md` section 2 reserves a waiver for a release gate and the gap is already in that document's known-gaps table | #59 |

## Required verification

- [ ] **Integration**: the sentinel absence test across logs, problem details, the three health responses, the
      OpenAPI document and a deliberate start-up validation failure; the reload test and its CLI counterpart; the
      wrong-value start-up failure naming the key and not the value; the unknown-key file behaviour; the Data
      Protection ring rotation with an overlapping anti-forgery token. **Not** `logout-all` and **not** export
      expiry — both are already covered, and adding a second assertion of either is the duplication this review
      removed.
- [ ] **Unit**: `LogRedaction` over every property name the new secret keys introduce, including case and separator
      variants (`api_key`, `ApiKey`, `APIKEY`), which the existing normalised matching already handles and which the
      test should pin.
- [ ] **Documentation**: `./scripts/dev docs` to prove every relative link in the new runbooks resolves, and the
      markdown line-length and British-spelling conventions of `CLAUDE.md` section 8.
- [ ] **Manual rehearsal**, recorded: rotate the database password and an object-storage key in the `infra/` compose
      stack, with timings.
- [ ] Non-functional rows affected in `docs/nfr/traceability.md`. **There is no "Revocation and flag propagation" row
      naming #57** — an earlier draft of this issue said there was, and `grep -n "#57" docs/nfr/traceability.md`
      returns exactly five lines: 160, 161, 164, 171 and 173, none of them about revocation. The rows this issue
      actually touches are **NFR-SE-09** (line 149, "No secret in the repository, in a log, in a problem-details
      response, in a health payload or in telemetry", scheduled against #21 and #22 — this issue supplies the
      sentinel half and the row is amended to name #57 as well), and the secret-and-key-rotation cadence rows of
      `docs/nfr/security-operations-targets.md` section 4. **NFR-SE-03** (line 143, session revocation propagation)
      is scheduled against **#23** and is **not** amended here.
- [ ] Environment: `./scripts/dev doctor`, plus `./scripts/dev up` for the compose stack the rehearsal needs. The
      integration tier needs PostgreSQL and `CI=true` turns a skip into a failure.
      **This is the one hard environmental precondition of this issue**: the rehearsal rotates a real database password
      and a real object-storage key in the `infra/` compose stack and times the interruption, so it needs a reachable
      Docker daemon with PostgreSQL and MinIO up. It needs **no** hardware, no printer, no scanner, no provider account
      and no human signature. If `doctor` reports no Docker daemon, the rehearsal cannot be performed and the issue is
      blocked rather than partially done — say so and stop, rather than writing a measured impact that was estimated.

## Evidence the pull request will carry

- [ ] `./scripts/dev test unit` and `integration` output, with the sentinel test's list of covered keys.
- [ ] The **rotation exercise log**: the database-password rotation and the object-storage-key rotation end to end,
      with the measured interruption, the health probes before and after, and the scrubbed transcript.
- [ ] The emergency-revocation rehearsal for one credential, with the audit entries read afterwards.
- [ ] The start-up failure transcript for a wrong secret, showing the key named and the value absent.
- [ ] `migrate --dry-run` showing no outstanding change and `has-pending-model-changes` reporting none.
- [ ] `./scripts/dev docs` output, and the rendered diff of `docs/platform/secrets.md`, the two new runbooks and
      `docs/runbooks/README.md`.
- [ ] `dotnet format --verify-no-changes` clean.

## Depends on

Nothing that is not already merged. `AddKeyPerFile` at all three hosts, `LogRedaction`, `ErrorLeakTests`,
`logout-all`, `RevokeAllForUserAsync`, `CustomerExportPurgeService`, the export endpoints and the `gitleaks` job were
each verified present in this working copy.

#23 — sessions, MFA and recovery, which `logout-all` and `RevokeAllForUserAsync` belong to. Merged.
#26 — the customer export and its purge job. The parent issue is **open**; the code is present here and was verified,
so the dependency is on the merged code.
#59 — **not** a dependency and must not be waited for. It encrypts the Data Protection ring; this issue documents the
ring as it is and says which issue changes it.
#187, #192, #211 — **not** dependencies. They bring media deletion and customer-link revocation, the two items of the
parent's verification list this issue explicitly leaves to them.

This child touches **no** single-writer code file: no migration chain, no `SystemRoles.cs`, no generated block of the
permission matrix, no `matrix.yaml`, no `openapi.v1.json`, no `schema.d.ts`, no `router.tsx`, no `ci.yml`. With
`[E14-F02-5b]` it is the most parallel-safe child of #57 and can run alongside all the others.

Four overlaps to name rather than discover:

- `docs/nfr/security-operations-targets.md` — section 4 here, section 8 in `[E14-F02-5]`, and several sections in
  #56's children. Cite its rows; do not restate a cadence as a number of your own, and the sections do not collide.
- `docs/runbooks/` — #58 and #60 will add pages. This issue creates the directory and its index, so whichever lands
  first owns the index and the other appends a row.
- **#202** (`[E13-F02-1]`, open) owns webhook-subscription **secret rotation** as a mechanism. The section 6.2 row
  "Provider API keys and webhook signing secrets" therefore gets a procedure here that describes the **file and the
  restart**, and marks the subscription-level rotation endpoint as #202's — it is not written or assumed here, and a
  reader is told which issue makes it possible.
- **#56's `[E14-F01a-6]`** writes `docs/security/threat-models/deployment.md`, which "models the secret mechanism as
  the control for every Credentials-and-secrets asset … and records what happens on a rotation and on a leak". That is
  a threat model — the argument that the control is sufficient. This is the runbook — the procedure somebody follows at
  two in the morning. They cite each other and neither restates the other; say so in the pull request so a reviewer
  does not read one as a duplicate of the other.
- `src/Platform/Tailor360.Platform.Observability/Logging/LogRedaction.cs`, **only if the rehearsal finds a leak**,
  which `[E14-F02-5b]` may also touch for the same reason. Coordinate in the pull request if both land in one week.

Refs #57
