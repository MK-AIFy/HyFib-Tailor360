Sub-issue of #38. Follows `E08-F01-5` and `E08-F01-6`. #196 delivers reorder rules as endpoints; the blueprint's
**Screens** line for #38 (`docs/IMPLEMENTATION_PLAN.md` Section 9) asks for a "reorder rule editor", and #38
acceptance criterion 5 — "Reorder rules are branch/location specific and permission controlled" — is a statement
about who may change what, which only a screen makes observable to the person it constrains. Checked today:
`clients/pwa/src` has no reorder-rule screen, no `reorderRulesApi.ts` and no `inventory.manage_reorder_rules` use
outside the constant `E08-F01-5` declares, and `docs/api/openapi.v1.json` contains no `api/v1/inventory` string at
all.
This is the last of the **six** client slices of #38 — `E08-F01-5` (foundation and the location screens), `-5b`
(suppliers), `-6` (the item register), `-6b` (units and conversions), `-7` (the CSV import) and this one.

## What is already built (do not re-build)

Read the "What is already built" sections of `E08-F01-5` and `E08-F01-6` first; the transport, the five state
components, the primitives, the form family and the test and story harness are listed there.

- `clients/pwa/src/inventory/inventoryApi.ts` (from `E08-F01-5`) — the **location list** this child's location
  picker calls. `clients/pwa/src/inventory/itemsApi.ts` (from `E08-F01-6`) — the **item list** the item picker
  calls. Neither is re-implemented here.
- `clients/pwa/src/inventory/inventoryPermissions.ts` — `manageReorderRules: 'inventory.manage_reorder_rules'` is
  **already declared**; do not edit this file.
- `clients/pwa/src/i18n/messages/inventory.ts` — add `inventory.reorder.*` and `inventory.nav.reorderRules` here.
- `clients/pwa/src/admin/adminDestinations.ts` — insert `inventory/reorder-rules` **immediately after
  `inventory/locations`**, keeping `features`, `audit` and `outbox` last, per the order `E08-F01-5` set.
- `clients/pwa/src/admin/templateFieldForm.ts` (529 lines) with `templateFieldForm.test.ts` (490 lines) — the worked
  example of form validation as a plain module beside the feature, unit-tested on its own rather than asserted
  through a component. This child's ordering invariant follows it.
- `clients/pwa/src/routes/admin/RoleDetailRoute.tsx` (261 lines) — how an administrative screen holds one
  `Idempotency-Key` in state and mints a fresh one only after a successful command
  (`RoleDetailRoute.tsx:65` and `:123`).
- `clients/pwa/src/i18n/formatters.ts` — `formatQuantity(value, unitSymbol)` for every level and quantity on these
  screens; `formatNumber` for a lead time in days.
- The field list is **already fixed by documentation and is not to be re-chosen**:
  `docs/prd/configurable-vs-fixed.md` line 70 — "Reorder rules per item and location — minimum, reorder point,
  target quantity, lead time, responsible role", configurable by "Inventory Clerk or Branch Manager with
  `inventory.manage_reorder_rules`", with an "audited change history", applied "at the next evaluation".
- Server side: `reorder_rules` is Inventory's own table (`docs/architecture/module-ownership.md` section 5.7) and
  #196 builds it, including the minimum ≤ reorder point ≤ target invariant and the refusal of a rule against a
  retired item or location.

## What is missing

- No screen on which a rule can be created, changed or retired, so #38 acceptance criterion 5 has nothing that
  demonstrates it and the Inventory Clerk named in `configurable-vs-fixed.md` line 70 cannot exercise the permission
  the matrix grants them.
- No client-side ordering check, so the editor would let a person save minimum > reorder point and learn about it
  from a `422`.
- No way to see what a rule said before a change, which matters because line 70 requires an audited change history
  and #196 audits the previous and the new value — a person changing a reorder point should be able to see what they
  are changing from.

## Scope

- [ ] **`clients/pwa/src/inventory/reorderRulesApi.ts`** — list (filterable by item and by location), read through
      `apiRequestVersioned`, create, update, and **whichever of delete or retire #196 actually published** (its
      scope says "delete-or-retire" and does not settle which; read the generated schema and say in the pull request
      which it turned out to be — the wording on the button follows it, so a person is not told "retire" when the
      record is being deleted). `Idempotency-Key` on create — the one command #196's scope declares it for — minted
      with `crypto.randomUUID()` when the person commits and held across a refusal and a retry, and sent on any
      other command for which the generated schema declares the header. `If-Match` from the `ETag` of the read that
      painted the screen **whenever that read carried one**, per `docs/architecture/conventions.md` section 4.2
      (missing is `428`, stale is the `409` of section 4.3) — never produce a `428`. **#196's scope does not spell
      `If-Match` out**, even though #38's cross-cutting requirements name concurrency protection; if #196's rule
      read emits no `ETag`, record it as a defect on #196 in this pull request rather than working around it. The
      client-side behaviour is verified against the stubbed transport either way.
- [ ] **Types appended to `clients/pwa/src/inventory/types.ts`** — `ReorderRule`, `ReorderRulePage`, the request
      types, and the responsible-role representation. Run `pnpm --dir clients/pwa generate:api` **first** and read
      the shapes out of `clients/pwa/src/api/schema.d.ts`; pin each in `clients/pwa/src/api/contract.ts` with
      `Conforms<…>` against #196's operation ids. Nothing about the shape is invented here.
- [ ] **`clients/pwa/src/inventory/reorderRuleForm.ts`** (+ `reorderRuleForm.test.ts`) — a plain module: the
      ordering invariant (minimum ≤ reorder point ≤ target), non-negative quantities and lead time, and which field
      an ordering failure belongs to, so the message lands on the field the person must change rather than on the
      form. A courtesy, not an authority: #196 validates and its refusal is what is shown when the two disagree.
- [ ] **Rule list** at `/admin/inventory/reorder-rules` — `DataTable` with item, location, minimum, reorder point,
      target, lead time and responsible role; `Filters` for item and location; cursor paging. Every level and
      quantity carries the item's unit through `formatQuantity`; a lead time carries its unit in words ("4 days").
      An item with no rule at a location is not an error — the list is simply the rules that exist, and the empty
      state says what that means: no low-stock evaluation happens there (#40).
- [ ] **Rule editor** at `/admin/inventory/reorder-rules/new` and `/…/:reorderRuleId` — the item picker and the
      location picker (over the active items and the caller's branch's active locations), the four numbers, and the
      responsible role. On an edit, the current stored values are shown as the starting point and the save summary
      names what changed, which is the client side of line 70's audited change history.
- [ ] **The responsible role** is offered from what the generated schema declares for the field, or from whatever
      reference list #196 publishes on its own surface. It is **not** read from `/api/v1/admin/roles`: that route
      needs `admin.roles`, which an Inventory Clerk does not hold
      (`docs/security/permission-matrix.md` — the Inventory Clerk row lists eleven permissions and none of them is
      an administration key), so a screen that fetched it would break for exactly the person the line names. If
      #196 publishes no list and the schema declares a free string, state that in the pull request and render a
      labelled text field rather than inventing an enumeration.
- [ ] A rule against a retired item or location is refused; the pickers offer active records only, and the server's
      refusal is rendered in words if a record was retired while the form was open.
- [ ] Guard both routes with `RequirePermission` on `inventory.manage_reorder_rules`, register them as children of
      the existing `admin` route in `clients/pwa/src/app/router.tsx`, and add the destination entry.
- [ ] Five states for both screens, each with a Storybook story through `storyTransport`, plus the pseudo-locale
      story. Writes are online-only: `OfflineBlockedAction`, nothing queued.
- [ ] DOR-11: no configuration and no seed data in the client. Every number on the screen is the server's; no
      default minimum, reorder point, target or lead time is written into the client, because a default reorder
      point is a product decision and none is recorded (line 70 fixes the fields, not their values).

## Out of scope

Evaluating a rule against a live balance, raising, de-duplicating, snoozing, escalating or clearing a low-stock
alert, the branch alert policy of `docs/prd/configurable-vs-fixed.md` line 69, and the low-stock queue screen — all
of which are #40, which consumes these rules without redefining them (#196 records the same boundary). Also out of
scope: the audit trail viewer — `/admin/audit` already exists
(`clients/pwa/src/routes/admin/AuditTrailRoute.tsx`, 161 lines, gated on `admin.audit.read`) and showing a rule's
history inside this screen would duplicate it for a key an Inventory Clerk does not hold; bulk editing rules from a
file (nothing in #38 asks for it); and the item and location screens themselves (`E08-F01-5`, `E08-F01-6`).

## Acceptance criteria

- [ ] **"Reorder rules are branch/location specific and permission controlled"** (#38 acceptance criterion 5,
      verbatim): an Inventory Clerk holding `inventory.manage_reorder_rules` creates a rule for one item at one
      location, edits its reorder point, and removes it by whichever of delete or retire #196 published — through
      the screen, with the location picker offering only the caller's branch's locations, proved by a Vitest journey
      against the stubbed transport.
- [ ] A rule with minimum > reorder point, or reorder point > target, is refused **in the form**, with the message on
      the field that has to change, before any request is sent; and the server's own field-level problem detail is
      what is shown when it refuses something the form accepted.
- [ ] A negative quantity or a negative lead time is refused with the field named, and a lead time of zero is
      accepted or refused exactly as #196 answers — the client does not invent a minimum.
- [ ] A rule cannot be created against a retired item or location: the pickers do not offer them, and a record
      retired while the form was open produces the server's refusal in words, with the typed values intact.
- [ ] **Concurrency, verified against the stub rather than against a sibling.** When a stubbed read carries an
      `ETag`, the save sends the same value as `If-Match` (asserted from `RecordedCall` in
      `clients/pwa/src/auth/testing/fixtures.ts`); a stubbed `409` carrying `currentVersion` shows what the rule
      says now beside what was typed, offers the choice, and a retry reuses the same `Idempotency-Key`
      (`docs/architecture/conventions.md` section 4.3). When a stubbed read carries no `ETag`, the save sends no
      `If-Match` and never produces a `428`.
- [ ] A caller without `inventory.manage_reorder_rules` sees the sentence and who to ask, never a redirect — and the
      destination is absent from the administration sub-navigation for them, which `AdminShell` already does from
      the entry added here.
- [ ] Every level, quantity and lead time on both screens carries its unit in text, formatted through the shared
      formatters; the empty list says what "no rule here" means rather than only "nothing found".
- [ ] Offline, a write is refused in place with "this will not be queued".

## Data and classification

DOR-06. No new stored field — the client renders what #196 owns. **Two sources disagree and the stricter applies.**
`docs/nfr/data-classification.md` section 2 names **reorder rules** explicitly in its **Internal** row; section 5.9
("Inventory, suppliers and the stock ledger") gives the whole area **Class: Confidential** and also lists "reorder
rules" among its examples. 5.9 wins because it is the per-area row, so every field below is handled as
**Confidential**: identifiers and codes only in a client error report, and nothing at all in client telemetry beyond
route name, component name, stack hash and timing. 5.9's "In logs" rule is the operative one: "Item and ledger
identifiers, movement types and quantities may appear. **Unit costs, valuations and supplier terms may not**." 

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Minimum level, reorder point, target quantity, lead time | **Confidential** (5.9; section 2 would read them as Internal — the stricter applies) | Decide when a location is low and how much to buy | Client-side none: no `localStorage` and no service-worker cache for these routes | `inventory.manage_reorder_rules` holders: Branch Manager, Inventory Clerk | Rule **identifier** only in a client error report. The levels themselves are not sent to telemetry: they are not personal data, but 5.9 reads them as Confidential and nothing is gained by shipping them |
| The item and location a rule is keyed on | **Confidential** (5.9) | Say which shelf in which branch the rule governs | As above | As above | Identifiers and codes only |
| Responsible role (a role key such as `inventory_clerk`) | **Internal** (a role key is configuration vocabulary, not inventory data) | Say who answers a low-stock alert when #40 raises one | As above | As above | A role key is not personal data; **a named person is not stored here** and must not be offered as a value |
| `inventory.reorder.*` message catalogue strings | **Public** (section 2) | The words on the screen | Versioned with the source | Anyone | Safe everywhere |
| Story and test fixtures for rules | **Internal**, synthetic only | Drive the state stories and the journey tests | With the source | Anyone | Synthetic items, locations and figures only (CLAUDE.md section 5) |
| Client telemetry for the two new routes | **Internal** by construction | Diagnose a failing screen | Per the telemetry row of `docs/nfr/data-classification.md` | Operations | Route name, component name, stack hash and timing only |

## Permissions and roles

DOR-10. No new key.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `inventory.manage_reorder_rules` | branch | no | no | no | `branch_manager`, `inventory_clerk` (Branch Manager, Inventory Clerk) | **Exists** — `docs/security/permission-matrix.md` row for #38: "Reorder points are what the low-stock alert fires on". Matches `docs/prd/configurable-vs-fixed.md` line 70's "Who may change it" |
| `inventory.manage_items` | branch | no | no | no | `branch_manager`, `inventory_clerk` | **Exists** — read-only here, for the item picker. A caller holding the reorder key without it must still be able to read and edit a rule: the rule read carries the item it names, so the screen renders without the picker rather than refusing |
| `inventory.manage_locations` | branch | no | no | no | `branch_manager`, `inventory_clerk` | **Exists** — same shape for the location picker |
| `admin.roles` | organisation | yes | yes | yes | `owner`, `admin` | **Exists, and deliberately not used.** The responsible-role control must not call the administration role route: an Inventory Clerk does not hold this key, and a screen that needed it would fail for the person line 70 names |

**OD-13** is open, so every grant above is the documented default rather than an approved one. Client fixtures
extended: `aCurrentUser({ permissions })` gains "holds the reorder key only", which is what proves the two pickers
degrade instead of breaking. The server-side authorisation matrix is #196's.

## Modules, contracts and events

Only `clients/pwa/` changes; no `.NET` project is touched, so ARCH-003, ARCH-004, ARCH-005 and ARCH-011 are not
engaged. The only coupling is the generated `clients/pwa/src/api/schema.d.ts`, pinned in
`clients/pwa/src/api/contract.ts` and checked by `pnpm --dir clients/pwa generate:api:check`. No integration
event is consumed or published: the rules this screen writes are read later by #40's evaluation, server-side, and
nothing about that reaches the client here.

DOR-05: the Inventory flow's threat model is covered by **#56a** in wave W2 (`docs/security/README.md` section 3.2);
the stand-in named there is the inventory invariants of `docs/architecture/invariants.md`. The control that matters
most on this screen is branch scope: the picker offers the caller's branch, and the server evaluates reach rather
than trusting the payload (CLAUDE.md security rule 2).

## Screens and states

DOR-14. Two screens.

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| Reorder-rule list | `/admin/inventory/reorder-rules` | `LoadingState` naming the read; result count announced politely (A11Y-LF-01) | "No reorder rule is set. Nothing here is checked for low stock until one is" — the fact and the act beside it | `RetryableError` with `plainLanguageDetail`; retry keeps the item and location filters (A11Y-LF-02) | `NetworkStatusBanner` over the last value; the create action blocked in place | `RequirePermission` sentence, no redirect; the destination also absent from the sub-navigation |
| Reorder-rule editor | `/admin/inventory/reorder-rules/new`, `/…/:reorderRuleId` | Skeleton form, controls disabled | n/a — a new rule is the empty case | `FormErrorSummary` at the top; the ordering failure on the field that must change; `409` shows both values | `OfflineBlockedAction`: "Needs connection — this will not be queued" | As above |

Each of the four numeric fields has a persistent visible label carrying its unit, and the three levels are read
together by a screen reader as a triple (minimum, reorder point, target) so their ordering is audible and not only
visual. The item and location pickers are labelled comboboxes or selects with a name, never a bare list. No drag, no
colour-only status (NFR-AC-05).

Accessibility: the core checklist of `docs/nfr/a11y-checklist.md` section 4 on both screens in all five states,
family **5.4 `A11Y-LF-nn`** for the list, and sections **4.3** and **4.4** for the editor.

## Migration and rollback

No schema change: no table, column, index or migration, and nothing under `src/` is touched. Proof is
`git diff --stat` showing every changed file under `clients/pwa/` and none under `src/Modules/**/Migrations/`, plus
`dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` reporting nothing outstanding on a database at
#196. Rollback is redeploying the previous client build; two routes and one destination entry disappear and nothing
else changes.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| Default minimum, reorder point, target and lead time | **No document fixes any of them.** `docs/prd/configurable-vs-fixed.md` line 70 fixes the fields, not their values | The client ships **no** default: every field starts empty on a new rule and the person enters it. A pre-filled threshold would be an invented product decision | n/a — do not invent one; raise it in `docs/prd/assumptions-and-open-decisions.md` if a default is wanted |
| How the responsible role is chosen | Depends on #196's published field | Offer exactly what the generated schema declares; never call `/api/v1/admin/roles`; a free string renders as a labelled text field | This pull request, with the decision stated in the description |
| **OD-13** approved role-to-permission grants | **Open**; #24 shipped the matrix under a documented default | Branch Manager and Inventory Clerk hold `inventory.manage_reorder_rules`. The values offered for "responsible role" are **whatever the generated schema declares and nothing else** — no list of role keys is written into the client, because that would be configuration (DOR-11) | W1 exit (overdue; default implemented) |
| **OD-10** initial catalogue and master data | **Open** — `docs/prd/traceability.md` TR-24 | No item, location or rule is hard-coded; every list is whatever the server returns, including empty | W2 |
| **AL-03** touch-target sizes | **Proposed, to be confirmed** (`docs/nfr/accessibility-localisation.md` section 5) | Build to 56/44/32 px; the settled WCAG floor is 24 px | Staff trial before #50 closes |
| Tamil operational vocabulary | Recorded practice at the top of `messages/catalog.ts`; `ta-IN` behind a flag | `inventoryTa` carries every new key with the English sentence | The localisation review gate |

## Required verification

`tests/e2e` does not exist (`scripts/dev` warns that the Playwright suite arrives with #52), so no end-to-end tier
is claimed.

- [ ] **Vitest (client)** on `reorderRuleForm.ts`: the ordering invariant across the boundary cases (equal values
      accepted, minimum > reorder point refused, reorder point > target refused), negative quantity and negative
      lead time refused, and the field each failure is attributed to. Include the boundary where all three levels
      are equal, which the invariant accepts.
- [ ] **Vitest (client), journeys** by role and label: create, edit and retire a rule; the ordering refusal in the
      form; a retired item refused by the server with the typed values kept; `409` (both values, same
      `Idempotency-Key`); missing permission (sentence, no redirect); the reorder key without `manage_items` (the
      picker degrades, the screen renders); the filtered list and the cleared filter; the empty list; a failed read
      retried; an offline write blocked.
- [ ] **Accessibility (Vitest + axe)**: `expectNoAccessibilityViolations` on both screens in all five states, with
      `RULES_JSDOM_CANNOT_JUDGE` in mind; the three levels asserted through their accessible names as a triple.
- [ ] **Reflow and obscured focus (NFR-AC-04) — taken in a real browser, not in jsdom.**
      `clients/pwa/src/design-system/testing/expectNoHorizontalOverflow.ts` (394 lines) is a **Playwright** gate:
      its entry point `expectNoHorizontalOverflow(page)` (`:348`) takes a `PageLike` that "Playwright's `Page`
      satisfies", and the file's own header records that **#52** — which owns `tests/e2e/playwright.config.ts` — is
      what passes it a real page. `measureHorizontalOverflow()` (`:83`) and `measureObscuredFocus()` (`:159`) are
      the functions it evaluates **inside** that page; jsdom performs no layout, so calling them from Vitest would
      assert nothing, and no screen test in the tree calls them today. `tests/e2e` does not exist
      (`scripts/dev:434`), so the evidence for this row is the dev server at 320, 360, 768, 1024 and 1280 CSS px
      and at 200% zoom: no page-level horizontal scroll at any of them, a wide table scrolling inside its own
      container rather than moving the body, and no focused control hidden behind a sticky bar.
      `docs/nfr/traceability.md:208` names the Playwright helper as NFR-AC-04's test and marks the proof
      "scheduled (#50)" — record that, rather than claiming a run that cannot happen yet.
- [ ] **Localisation**: pseudo-locale story at growth factor 1.4; a test asserting no user-facing literal in the new
      components.
- [ ] **Contract**: `pnpm --dir clients/pwa generate:api:check` clean; the rule payload types pinned with
      `Conforms<…>`.
- [ ] **The five commands** of `clients/pwa/CLAUDE.md` section 8, twice (CLAUDE.md section 5).
- [ ] **DOR-13 rows**: NFR-AC-01, NFR-AC-02, NFR-AC-04, NFR-AC-05, NFR-CL-02 (this route imports the inventory
      slice only).
- [ ] **DOR-16**: `./scripts/dev doctor`; no database needed, the suite stubs `fetch`.

## Evidence the pull request will carry

Per `docs/process/definition-of-done.md` section 4, row "Screen".

- [ ] Screenshots of both screens at phone, tablet and desktop widths, including the ordering refusal on the field.
- [ ] The axe run output and the completed accessibility record (core checklist plus family 5.4).
- [ ] Storybook state stories for both screens, and the pseudo-locale story.
- [ ] A screen recording of the create-edit-retire journey as the Inventory Clerk, with the location picker showing
      one branch only — the evidence #38 acceptance criterion 5 is judged on, and the "worked example against
      `inventory.manage_reorder_rules`" #196 asks its own pull request to carry, seen from the client side.
- [ ] The `reorderRuleForm.test.ts` output naming the boundary cases.
- [ ] `lint`, `typecheck`, `format:check`, `test`, `build` and `generate:api:check` output; and the reflow
      screenshots at 320, 360, 768, 1024 and 1280 CSS px and at 200% zoom, with the note that NFR-AC-04's automated
      proof is the Playwright helper and arrives with #52.
- [ ] Pull-request template items 1, 3, 6 and 9 ticked; item 2 deleted with the one-line reason above.

## Depends on

#196 — the reorder-rule routes, their payloads, their operation ids, the responsible-role representation and the
`docs/api/openapi.v1.json` entries this child generates types from. Merged, not nearly merged.

`E08-F01-6` — `itemsApi.ts` for the item picker, and the item types in `inventory/types.ts`.

`E08-F01-5` — `inventoryApi.ts` for the location picker, `inventoryPermissions.ts` with `manageReorderRules` already
declared, `messages/inventory.ts`, `inventory.css` and the destination order.

#193 and #195, transitively.

**Collision note.** This child edits `clients/pwa/src/inventory/types.ts`,
`clients/pwa/src/i18n/messages/inventory.ts`, `clients/pwa/src/admin/adminDestinations.ts`,
`clients/pwa/src/app/router.tsx`, `clients/pwa/src/api/contract.ts` and
`clients/pwa/src/routes/inventory/inventory.css`. **None of the six client children of #38 is parallel-safe with
another**; they merge in the order `-5`, `-5b`, `-6`, `-6b`, `-7`, `-8`, and no two of these sessions may run
concurrently. It is the only one of the six that depends on #196, so in principle it can start as soon as #196,
`-5` and `-6` are all merged, regardless of where `-5b`, `-6b` and `-7` are — but it must still be the last of the
six to merge, because the destination entry it inserts is positioned relative to entries those children add.

Refs #38
