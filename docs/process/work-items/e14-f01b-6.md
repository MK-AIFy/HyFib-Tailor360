Sub-issue of #56. Plan Section 8 closes the W5 half of this parent with "ASVS traceability audit, **independent
penetration test before production and after material auth/payment/upload changes, verified remediation**". The audit
is the preceding sibling. **This sub-issue is the penetration test: the engagement pack, the environment and accounts,
the rules of engagement, the re-test trigger, the remediation register, and the release wiring.**

**Read this before picking it up, because it is the one child of this parent that a coding session cannot finish
alone.** Three documents already require the engagement — `docs/nfr/traceability.md` `NFR-SE-12`, the same document's
line 330 ("a penetration test is by definition adversarial and manual … before go-live and after any material change to
the authentication or media surface") and `docs/nfr/security-operations-targets.md` line 96 ("The running system |
Independent penetration test | Before production, and after any material change to authentication, payments or uploads
(#56b)") — and plan Section 6.2 note 11 makes **#59 depend on the `#56b` half**, so this sits on the critical path to
go-live. But **engaging an assessor, paying for it and providing a seeded instance are owner actions.** The split of
work is therefore explicit:

- **A session can finish the pack.** `docs/security/penetration-test.md`, the account and environment specification,
  the rules of engagement, the re-test trigger, the empty remediation register and the release wiring are ordinary
  documentation work with a green pull request at the end. **That pull request is the deliverable that closes the
  session.**
- **The issue then stays open** across the engagement and the remediation, held by the Owner and the security owner.
  Its later evidence — the report received, the register filled, the fixes verified in production — arrives as comments
  and as separate, scoped pull requests, never as a second session on the same branch.
- **The decision this raises should be raised the day the pack merges**, not at the exit gate: who performs the test,
  under what budget, on what timetable. It is on the critical path to #59 and therefore to go-live.

## What is already built (do not re-build)

- `docs/security/asvs-traceability.md`, **audited** by the preceding sibling: every row carrying a control, a test,
  evidence and a residual, with an audit outcome section giving counts per status per chapter and every downgrade with
  its reason. **This is what the assessor is testing** — the project's actual claims rather than its intentions — and
  the pack points at it rather than restating it.
- The ten flow threat models in `docs/security/threat-models/` — `authentication.md` (#23, reviewed 2026-09-05),
  `authorisation.md` (#32a) and the eight from the W2 half of this parent — each with a data-flow diagram, trust
  boundaries, a STRIDE table, abuse cases, controls mapped to tests and owned residual risk.
- `docs/security/abuse-cases.md` — the eleven families `ABF-01` to `ABF-11`, each with the attacker, the motive, the
  attack pattern in this system's vocabulary and the test that defeats it. **This is the penetration tester's brief**:
  it is already written in attacker terms, which is exactly what an engagement scope needs, so the pack cites it as
  the brief and adds only what a catalogue cannot carry — scope boundaries, environment, accounts and rules.
- `docs/security/vulnerability-management.md` and `docs/security/exceptions.md`, with the `SEC-EX-nnnn` discipline and
  the expiry rule in `scripts/sarif-gate.py` — from the W2 half. **The remediation process already exists; this
  sub-issue feeds it and does not invent a second one.** Severity and the clocks come from
  `docs/nfr/security-operations-targets.md` sections 2.1 and 2.2: critical **7** calendar days, high **30**, medium
  **90**, low next scheduled release, measured from when the finding first appears to when the fix is **deployed to
  production and verified** — never to when the pull request merges.
- `docs/process/release-gates.md` — `RG-08` to `RG-11` and `RG-14`, and section 6's evidence checklist whose item 9 is
  "the security scan summary … with every finding's severity and status", owned by the security owner.
- `docs/process/release-evidence.md` — the release record form: section 4 gate results, section 5 the evidence
  checklist, section 6 waivers carried, section 8 go or no-go with sign-off. **The report and its remediation status
  are evidence in this form, not in a new one.** The preceding sibling has already added the ASVS line to `RG-14` and
  to section 5 item 9; **this sub-issue adds the penetration-test line to the same two places**, which is a one-line
  split stated in both bodies.
- `docs/process/waivers.md` — the waiver register, and section 9's distinction: a **waiver** governs "this release
  shipping with it", a **security exception** governs "the finding's remediation deadline", and a single finding may
  hold both, each within its own dates.
- `infra/compose/docker-compose.staging.yml` and `docs/dev/staging.md` — the interim staging environment (#22), its
  network guard-rails and its **synthetic data only** rule. CLAUDE.md security rule 10 forbids production data in any
  non-production environment. Note that #59 hardens and then **destroys** this environment, which the pack must
  account for.
- `./scripts/dev reset` and `dotnet run --project src/Tools/Tailor360.Cli -- migrate | init-reference-data |
  seed-synthetic` — how a test environment gets data, and the only sanctioned way to populate the one the assessor is
  given. **`reset` refuses to run in Production**, and the pack cites that refusal as a control.
- `docs/security/permission-matrix.md` — the twelve roles the account set must cover, and `docs/security/role-walkthrough.md`.
- `.github/ISSUE_TEMPLATE/security.yml` — where a finding lives. `docs/security/README.md` section 2 already forbids
  the detail of an unpatched exploitable finding from appearing in any document under `docs/security/`, which is the
  rule that decides how the report itself is handled.

## What is missing

- No engagement scope, no rules of engagement, no environment specification, no account set, and no data-handling terms
  for a report that will, by its nature, describe how to break the system.
- **No re-test trigger.** "After any material change to authentication, payments or uploads" has no definition and
  nothing fires it, so `docs/nfr/security-operations-targets.md` line 96 is unenforceable as written.
- No remediation register tying a report finding to a severity **as this project rates it**, a deadline, a corrective
  issue and a fix **verified in production** — which is what `NFR-SE-12` and `RG-14` need as evidence.
- No decision on who performs the test, under what budget, on what timetable. It is on the critical path to #59.

## Scope

**What the session delivers, and what closes its pull request.**

- [ ] Add `docs/security/penetration-test.md`. Section 1: why an engagement exists, what it is **not** (it is not a
      substitute for the regression suites, and a clean report is not a proof of security), and who owns it.
- [ ] Section 2, **scope**: in and out, by flow, naming the ten threat models and the eleven abuse-case families as the
      brief and the audited ASVS sheet as the claim under test. State explicitly what is out: the providers' own
      systems, the mail relay, denial of service against shared infrastructure, and anything belonging to #59's
      hardened environments or #60's backups.
- [ ] Section 3, **environment**: a dedicated instance seeded with `migrate`, `init-reference-data` and
      `seed-synthetic`; **never production data, never a production secret, never a production provider credential**;
      its own Data Protection key ring; reachable by the assessor through the network gate **for the engagement window
      only**, and closed afterwards. Record which topology it runs on and that a hosting change (**OD-02**) or #59's
      replacement of the interim staging environment obliges a re-read of this section.
- [ ] Section 4, **accounts**: one per role in `docs/security/permission-matrix.md` — twelve — in **two branches**, so
      horizontal and vertical escalation are both testable, created through the administration surface #25 shipped. The
      **account list** is in the document; the **credentials are not**, they are handed over out of band, and the
      document says so and says who hands them over and who revokes them.
- [ ] Section 5, **rules of engagement**: what may be attacked; what must not be; and the **stop condition** — if the
      assessor reaches anything that looks like real customer data, they stop, report, and the incident process of
      `docs/nfr/security-operations-targets.md` applies. An engagement with no stop condition is not scoped.
- [ ] Section 6, **evidence the assessor must return**: the finding set with reproduction steps, the severity **as they
      rate it**, the affected flow, and enough detail to re-test. State the format and that the project re-rates
      severity itself.
- [ ] Section 7, **data-handling terms for the report**: the report is **Confidential**, it is **not committed to this
      repository**, and the repository carries the remediation register plus a pointer to where the report is held —
      because `docs/security/README.md` section 2 forbids the detail of an unpatched finding in `docs/security/`.
      Record retention and destruction, and who may read it.
- [ ] Section 8, the **re-test trigger**, as a short checkable rule: what counts as a material change to
      authentication, payments or uploads — name the **paths** (`src/Modules/Identity/**`, `src/Modules/Billing/**`,
      `src/Modules/Media/**`, `src/Platform/Tailor360.Platform.Security/**`) and the **epics** (E03, E09, E13 and the
      media half of E05) that signal one — who decides, and what happens when it fires. A reader must be able to decide
      from the rule alone whether a given merged change requires a re-test.
- [ ] Section 9, the **remediation register**, empty at drafting exactly as `docs/process/waivers.md` is: finding
      identifier, the finding as the report states it, **severity as this project rates it** (not as the report rates
      it, and the document says why), corrective issue, deadline from the service levels, fix merged, **fix verified in
      production**, re-test outcome, status.
- [ ] Wire it into the release record: `docs/process/release-gates.md` `RG-14` and `docs/process/release-evidence.md`
      section 5 item 9 name **the penetration-test report with its remediation status** as a required evidence item,
      with the security owner as its owner. **Do not touch the ASVS line in either** — the preceding sibling owns it,
      and each sub-issue editing only its own line is what keeps the two from conflicting.
- [ ] Update `docs/nfr/traceability.md` `NFR-SE-12` so the penetration-test half names this document and its register,
      and `docs/security/README.md` section 2 with the new document.
- [ ] **Raise the open decision in `docs/prd/assumptions-and-open-decisions.md`**: who performs the independent
      penetration test, under what budget, on what timetable — owned by the Business owner, to be decided alongside
      **RG-OD-02**, needed before go-live (#61c). The pull request carries that diff, and the issue comment that flags
      it to the Owner on the day the pack merges.
- [ ] Versioned configuration (DOR-11): none. The environment is provisioned by the existing compose stack and the
      existing command-line seeding; no configuration key and no seed data is added.

**What the issue then waits for, as comments and separate pull requests, not as session work.**

- [ ] The assessor engaged, the window agreed, the environment stood up and the accounts handed over.
- [ ] The report received, every finding re-rated by this project, and the remediation register filled.
- [ ] **Every fix as its own scoped issue and pull request.** A remediation pull request spanning six modules is
      unreviewable and breaks CLAUDE.md section 6; the register links the issues rather than containing the fixes.
- [ ] Each fix **verified in production** and re-tested — by the assessor where the report warrants it, otherwise by
      the security owner against the reproduction steps — and the register updated.
- [ ] Anything not fixed before the release carried as a `SEC-EX-nnnn` exception with an approver, an expiry and a
      compensating control, **and** a `docs/process/waivers.md` row if it blocks a gate.
- [ ] The environment destroyed and the accounts revoked, with the revocation recorded.

## Out of scope

The ASVS audit (the preceding sibling, which **must merge first** — the pack points at an audited sheet, and pointing
at an unaudited one would put the assessor to work on claims nobody has checked). The models, the catalogue, the
skeleton, the vulnerability process and the exception register — the W2 half. The regression suites and the policy
hardening — eight siblings of the W5 half. **Fixing what the engagement finds**: each finding is its own issue. Choosing
or contracting the assessor — a business decision, raised here and decided by the Owner. #59's hardened environments,
image signing and provenance; #60's backups and disaster recovery; #58's observability and game days. No source file, no
test and no configuration change in this pull request.

## Acceptance criteria

**These close the session's pull request.**

- [ ] `docs/security/penetration-test.md` is complete enough for an assessor to start from it **without asking a
      question**: scope, environment, accounts, rules of engagement, the brief, the evidence expected, the
      data-handling terms, the remediation register and the re-test trigger. The test of this criterion is that a
      reviewer who has not read this issue can read the document and say what the assessor will do on day one.
- [ ] **The environment specification is provably synthetic**: it names the exact commands (`migrate`,
      `init-reference-data`, `seed-synthetic`), states that no production data, production secret or provider
      credential is present, and cites CLAUDE.md security rule 10 and `./scripts/dev reset`'s refusal to run in
      Production as the controls.
- [ ] The account set covers **every one of the twelve roles** in `docs/security/permission-matrix.md`, in **two
      branches**. The account **names** are in the document and the **credentials are not**, and the document says who
      hands them over out of band and who revokes them at the end.
- [ ] **Negative case, the stop condition**: the rules of engagement name what must not be attacked and what happens if
      the assessor reaches real customer data anyway — stop, report, and the incident process applies. An engagement
      with no stop condition is not scoped, and this criterion is the one a reviewer should check first.
- [ ] **Negative case, the report is not committed**: the data-handling terms state that the report does not enter this
      repository, that the register carries the finding's identity and status but never its reproduction steps, and
      they cite `docs/security/README.md` section 2 as the rule.
- [ ] **The re-test trigger is checkable**: a reader can decide, from the rule alone and without judgement, whether a
      given merged change requires a re-test. It names paths and epics, not adjectives.
- [ ] **Exception case**: the document states that a finding not fixed before the release is carried as a
      `SEC-EX-nnnn` exception with an approver, an expiry and a compensating control, **and** as a
      `docs/process/waivers.md` row if it blocks a gate — citing section 9 for why one finding may need both, each
      within its own dates. It also states that an exception cannot be renewed by editing its expiry.
- [ ] No number is invented: every deadline is cited from `docs/nfr/security-operations-targets.md` section 2.1, every
      severity from section 2.2 and `docs/process/release-gates.md` section 4. **No acknowledgement time and no
      engagement duration is promised**, because neither is decided.
- [ ] `RG-14` and `release-evidence.md` section 5 item 9 name the penetration-test report, **and the ASVS line in each
      is untouched**. The pull request states which lines it edited.
- [ ] The open decision is raised as a diff of `docs/prd/assumptions-and-open-decisions.md`, and the issue carries a
      comment flagging it to the Owner.
- [ ] `./scripts/dev docs` passes, and the pack, `release-gates.md`, `release-evidence.md`, `waivers.md`,
      `vulnerability-management.md`, `exceptions.md` and `NFR-SE-12` agree with one another with no contradiction.

**These close the issue, after the engagement. They are not session work and a reviewer must not wait for them.**

- [ ] The report received and every finding re-rated by this project, with the register filled.
- [ ] **No critical or high finding open** without either a fix verified in production or a dated, approved, visible
      exception — the parent's second and fifth acceptance criteria.
- [ ] Every fix carried by its own issue and pull request; the register links them.
- [ ] Each fix verified **in production** and re-tested, with the outcome in the register.
- [ ] The environment destroyed and the accounts revoked, recorded.

## Data and classification

This sub-issue adds one document. The data it introduces is the most sensitive this parent handles: a penetration-test
report and a set of working credentials — which is exactly why it is separated from the audit, whose pull request
handles neither.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| `docs/security/penetration-test.md` — the engagement pack | Internal | Let an assessor start, and let a reader check the engagement was scoped | Lives with the repository | Every contributor | Never logged. It names roles, flows and account **names**; it contains **no credential** and no unpatched finding |
| Test-account credentials for the engagement | Credentials and secrets | Let the assessor authenticate as each role | The engagement window; revoked at the end, with the revocation recorded | The assessor and the security owner | **Not in the repository, not in the document, not in an issue comment, not in a pull-request description.** Handed over out of band; generated by `seed-synthetic` or through administration; every account suspended when the window closes |
| The penetration-test report | **Confidential** — it describes how to break the system | Evidence for `NFR-SE-12` and `RG-14` | Per the report's own retention, agreed in the engagement terms; **it is not committed to this repository** | The security owner, the Owner, the technical reviewer | The repository carries the **remediation register** — identifier, severity, deadline, status — and a pointer to where the report is held. `docs/security/README.md` section 2 is the rule that decides this |
| Remediation register rows | Internal | Show every finding's severity, deadline and status | Kept as history after closure | Every contributor | Identifier, severity, dates, issue number and status only. **No reproduction steps, no payload, no affected-version detail** |
| The engagement environment and any data in it | Synthetic, standing in for Personal, Sensitive Personal and Financial | Make the attack realistic | Destroyed with the environment | The assessor | Synthetic only, from `seed-synthetic`. The pull request records how that was verified, and the environment's destruction is recorded when it happens |
| The network gate configuration for the window | Internal | Let the assessor in, and keep everyone else out | The window | The operations owner | No external hostname, no deployment address and no staging URL appears in the document; it names the mechanism, not the endpoint |

## Permissions and roles

No permission key is added, removed or changed. What this sub-issue needs is **accounts**, not new permissions: one per
role in `docs/security/permission-matrix.md` (Owner `owner`, Admin `admin`, Branch Manager `branch_manager`, Reception
`reception`, Measurement Staff `measurement_staff`, Tailor Master `tailor_master`, Tailor `tailor`, Inventory Clerk
`inventory_clerk`, Cashier `cashier`, Delivery Staff `delivery_staff`, Auditor `auditor`, HyFib Super User
`hyfib_super_user`), in two branches, created through the administration surface #25 shipped.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| — | — | — | — | — | — | **None added.** The engagement exercises the existing catalogue; inventing a permission for a tester would invalidate the test |

**OD-13** is open, so the accounts hold the documented 2026-09-06 default grants, and the pack **says so** — the
assessor is testing the defaults, and the report must be read in that light. **RG-OD-02** decides who approves the
accounts and receives the report; until it is decided the Owner does, with the technical reviewer
(`docs/nfr/traceability.md` line 67).

## Modules, contracts and events

No module, schema, contract, integration event or endpoint; **no `src/` file and no test file is touched**. Files
changed, and this is the complete list: `docs/security/penetration-test.md` (new), `docs/security/README.md`
(section 2), `docs/process/release-gates.md` (`RG-14`, the penetration-test line only),
`docs/process/release-evidence.md` (section 5 item 9, the penetration-test line only), `docs/nfr/traceability.md`
(`NFR-SE-12`, the penetration-test half) and `docs/prd/assumptions-and-open-decisions.md` (the new open decision).
`docs/security/exceptions.md` is appended to **later**, during remediation, not in the session's pull request.

**DOR-05**: the engagement's own "flow" is the running system, covered by `threat-models/deployment.md` from the W2 half
of this parent; the flows it attacks are covered by the other nine models. The pull request names them per DoD 6.

## Screens and states

Deleted: a Lane C governance sub-issue publishes no screen. The engagement will reach every screen, which is why the
pack names the **roles** rather than the screens — a screen list would be stale before the assessor read it.

## Migration and rollback

No table, no column, no migration; `dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` reports no
outstanding change and that output is the evidence. The engagement **environment** is created and destroyed by the
existing compose stack and `./scripts/dev reset`, which **refuses to run in Production** — the pack cites that refusal
as the control that keeps the engagement off the real system.

Rollback of the session's pull request removes the pack and the release wiring. The parts that must **not** be lost are
the remediation register and any `SEC-EX-nnnn` exception, which is why both live in the repository rather than in the
report, and why the register is a section of a versioned document rather than a spreadsheet someone holds.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **Who performs the independent penetration test, under what budget and on what timetable** | **Open — raised by this sub-issue**, and it is on the critical path: plan Section 6.2 note 11 makes #59 depend on the `#56b` half, and #59 gates go-live. Record it in `docs/prd/assumptions-and-open-decisions.md`, owned by the Business owner, to be decided alongside **RG-OD-02**, and **flag it to the Owner the day the pack merges** | The pack is written so that **any** competent assessor can start from it; no vendor is named and no duration is promised. Until an assessor is engaged the pack is complete and the engagement is blocked, and the issue says which | Before go-live (#61c), per `docs/nfr/security-operations-targets.md` line 96 |
| **Who receives a coordinated disclosure report, and the acknowledgement timeframe** (raised by the vulnerability-management sub-issue of the W2 half) | **Open** | `SECURITY.md` points at the repository's private reporting route and at the Owner; **no acknowledgement time is promised in hours or days**, and this pack promises none either | Before the first external release |
| **RG-OD-02** — who holds the security owner role and who deputises | **Open** — needed before W2 | The Owner, with the technical reviewer: approves the accounts, receives the report, owns every remediation row and approves any exception | W2 exit gate |
| **RG-OD-01** — the proposed severities and waiver durations | **Open** — needed before the W1 exit gate | `docs/process/release-gates.md` section 4 as drafted, and the service levels of `docs/nfr/security-operations-targets.md` section 2.1. These decide whether a report finding blocks the release | W2 exit gate |
| **OD-02** — hosting model and indicative monthly budget | **Open** — needed before W0 exit | The engagement runs against the interim staging topology of `infra/compose/docker-compose.staging.yml`. The pack records that a hosting change, **or #59 replacing that environment**, obliges a re-read of section 3 and a re-test of the deployment flow | W0 exit gate (overdue) |
| **OD-13** — approved role-to-permission grants | **Open** | The twelve seeded roles and their 2026-09-06 default grants; the pack states that the assessor tests the defaults | W1 exit gate (overdue) |
| **OD-08** — retention periods | **Open** | The report's retention is set by the engagement terms, not by OD-08, and the pack says so rather than citing a period the project has not decided | W1 exit gate (overdue) |

## Required verification

**For the session's pull request.**

- [ ] **Documentation**: `./scripts/dev docs` and `python3 scripts/check-docs-links.py --self-test`.
- [ ] **Consistency check pasted into the pull request**: every `RG-nn`, `NFR-` row, role key, command and document
      path the pack cites exists — `grep`/`ls` output, not prose. In particular every one of the twelve role keys
      matches `docs/security/permission-matrix.md` exactly, and `./scripts/dev reset`'s Production refusal is shown.
- [ ] **The credential rule proved on the pull request itself**: the `security` job's gitleaks run passes with **no new
      `.gitleaks.toml` allowlist entry**. A pack that needs one has written down a credential.
- [ ] **The line split shown**: the diffs of `RG-14` and `release-evidence.md` section 5 item 9, demonstrating that the
      ASVS line the preceding sibling added is untouched.
- [ ] **DOR-13**: `docs/nfr/traceability.md` rows affected — `NFR-SE-12` (the penetration-test half named);
      `NFR-SE-07` referenced and not edited.
- [ ] **DOR-16**: Python 3 for the link checker and the .NET command line for the dry-run; `./scripts/dev doctor`
      records both. **No database, no container runtime and no environment is needed for the pack itself** — which is
      the point of separating it from the engagement.

**For the engagement, recorded later on the issue.**

- [ ] **Environment check before the window opens**: the seeded instance contains no production data and no production
      secret, recorded as the output of the seeding commands and a statement of what was checked against.
- [ ] **Account check**: twelve roles × two branches created, the list matching section 4, and credentials confirmed
      delivered out of band.
- [ ] **Re-test verification after remediation**: each fixed finding re-tested — by the assessor where the report
      warrants it, otherwise by the security owner against the reproduction steps — and the outcome in the register.
- [ ] **Exception-register consistency**: `python3 scripts/sarif-gate.py --self-test` passes and the gate passes with
      every new `exceptions.md` row, none past its expiry.
- [ ] **Environment teardown and account revocation**, recorded.

## Evidence the pull request will carry

- [ ] The rendered diff of `docs/security/penetration-test.md`, with sections 2 (scope), 3 (environment), 4 (accounts),
      5 (rules of engagement and the stop condition), 8 (the re-test trigger) and 9 (the empty remediation register)
      visible.
- [ ] The rendered diff of `RG-14`, `release-evidence.md` section 5 item 9, `docs/security/README.md` section 2 and the
      `NFR-SE-12` row — with a statement of exactly which lines were edited.
- [ ] The new open-decision row as a diff of `docs/prd/assumptions-and-open-decisions.md`, and a link to the issue
      comment flagging it to the Owner.
- [ ] The consistency `grep`/`ls` transcript, including the twelve role keys and the `reset` Production refusal.
- [ ] `./scripts/dev docs` and `check-docs-links.py --self-test` output.
- [ ] The green `security` job, as evidence that no credential was written down.
- [ ] `migrate --dry-run` showing no outstanding migration.

**Later, on the issue rather than in this pull request**: the remediation register filled, with each finding's
corrective issue number and its verified-in-production date; the re-test outcomes; the `docs/security/exceptions.md`
diff for anything carried; and the teardown record.

## Depends on

- **The ASVS audit sub-issue of this parent — must be merged first.** The pack points at an **audited** sheet; pointing
  at an unaudited one would set an assessor to work on claims nobody has checked, which is the most expensive way to
  discover a documentation problem.
- The **abuse-case catalogue** sub-issue of the W2 half — the brief. The **four threat-model** sub-issues — the ten
  models the scope is drawn against. The **vulnerability-management and exception-register** sub-issue — the process the
  remediation register feeds and the `SEC-EX-nnnn` discipline it uses. All from the W2 half, all merged before this.
- The **seven regression-suite** and the **policy-hardening** sub-issues of the W5 half — not because the pack needs
  them, but because an engagement run before them would spend its budget finding what a test would have found for
  nothing. The pack may merge before they do; the **engagement** should not start before they have.
- Merged work it reads: #19 (`release-gates.md`, `release-evidence.md`, `waivers.md`,
  `security-operations-targets.md`, `traceability.md`), #22 (the interim staging environment, `docs/dev/staging.md`,
  `.gitleaks.toml`), #24 (`permission-matrix.md`, `role-walkthrough.md`), #25 (the administration surface the accounts
  are created through).
- **It blocks #59** (plan Section 6.2 note 11: "#59 needs #56b") and therefore **#61b and go-live**. That is the reason
  the budget and assessor decision must be raised the day the pack merges rather than at the exit gate.

**Parallel safety**: its only file overlaps are `docs/process/release-gates.md` `RG-14` and
`docs/process/release-evidence.md` section 5 item 9, shared with the ASVS-audit sibling — **that sibling edits the ASVS
line in each and this one edits the penetration-test line**, a one-line-each split stated in both bodies, so whichever
merges second rebases cleanly. It writes to `docs/security/exceptions.md` only during remediation; say which
`SEC-EX-nnnn` identifiers are taken, because the `ABF-09` sibling and #59 append to the same register.

Refs #56
