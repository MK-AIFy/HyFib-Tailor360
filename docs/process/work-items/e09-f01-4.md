Sub-issue of #41. All three existing children of #41 — #145, #146 and #147 — are closed and all three were backend;
#145's own body said the screens were "an issue under #41 once the API is stable". The blueprint's fourth bullet,
"**Admin**: price/tax version editor with preview and test cases before publish"
(`docs/IMPLEMENTATION_PLAN.md` line 1850, the `#41 [E09-F01]` blueprint in Section 8), has no child and no
implementation. Checked in this working copy: `grep -rn "tax-configuration\|gst-registration\|billing/price-lists"
clients/pwa/src --include=*.ts --include=*.tsx` matches **nothing** outside the generated
`clients/pwa/src/api/schema.d.ts`, and `clients/pwa/src/app/router.tsx` has no pricing route at all — so an
administrator cannot record a branch's GST registration or see a tax configuration version without a database
client. This is the first of eight client slices; it delivers the read-and-record surface the other seven build on.

**Not on the critical path.** Plan Section 6.2 row 815 puts #41 in Lane A with areas "Billing (engine), PWA admin",
and the W3 wave row (line 602) reads `#41 → #32a → …`. What #32a needs from #41 is the merged
`Billing.Contracts.IPricingService` — delivered and closed by #147 — **not** this administration surface. These
client children are W3 Lane B work that runs alongside #32a/#32b and must not be treated as gating them. Say that
in one line in the pull-request description. The **OD-11** supersession that Section 6.2 note 1 asks an
implementing session to record was already recorded by #147's pull request; do not repeat it here.

## What is already built (do not re-build)

Server side, all of it, closed by #145 and covered by integration tests — do not add, rename or re-shape an
endpoint or a payload in this issue.

- `src/Modules/Billing/Tailor360.Modules.Billing.Api/Registrations/GstRegistrationEndpoints.cs` (137 lines) —
  `ListGstRegistrations`, `AddGstRegistration`, `GetGstRegistration`, `AmendGstRegistration` under
  `/api/v1/billing/gst-registrations`, every write `.RequireIdempotency()`, the amendment `.RequireIfMatch()`,
  all four on `BillingPermissions.ManagePriceLists` with `BranchScope.Organisation`. Note what the shipped code
  does that #145's issue text did not promise: **all four are on the drafting permission**, not "the same two
  permissions" — there is no publish key anywhere on a registration.
- `src/Modules/Billing/Tailor360.Modules.Billing.Api/Tax/TaxConfigurationEndpoints.cs` line 39 —
  `ListTaxConfigurationVersions`, answering `TaxConfigurationSummaryPayload[]`, newest first. The other eight
  routes in that file belong to E09-F01-5 and E09-F01-5b; this issue calls only the list.
- Payloads to mirror, not invent, in
  `src/Modules/Billing/Tailor360.Modules.Billing.Api/Payloads/TaxPayloads.cs`:
  `GstRegistrationPayload(Guid GstRegistrationId, Guid BranchId, string Gstin, string StateCode, string LegalName,
  string? TradeName, DateOnly EffectiveFrom, DateOnly? EffectiveTo, DateTimeOffset CreatedAt,
  DateTimeOffset UpdatedAt)`; `TaxConfigurationSummaryPayload(Guid TaxConfigurationVersionId, int VersionNumber,
  string Name, string? Notes, string Status, DateOnly EffectiveFrom, Guid? ClonedFromVersionId,
  DateTimeOffset CreatedAt, DateTimeOffset? PublishedAt, DateTimeOffset? RetiredAt)`;
  `BillingFindingPayload(string Severity, string Code, string Message, string? Target)`;
  `BillingValidationReportPayload(Guid VersionId, bool CanPublish, IReadOnlyList<BillingFindingPayload> Findings)`.
- The one request body this issue sends, in `.../Payloads/TaxRequests.cs`:
  `GstRegistrationRequest(Guid BranchId, string? Gstin, string? StateCode, string? LegalName, string? TradeName,
  DateOnly? EffectiveFrom, DateOnly? EffectiveTo, string? Reason)`. It is the body of **both** the `POST` and the
  `PUT`, and the `PUT` is **whole-value**: every field must be re-sent, and a field left out is refused, not kept.
  The branch never changes on an amendment (`AmendGstRegistration`'s own summary), so the form shows the branch as
  a read-only fact and still sends `branchId`. `reason` is optional — `.Audited(GstRegistrationHandler.AddedAction)`
  carries no `reasonRequired: true` — so the form offers it as an optional note and never blocks on it.
- `src/Modules/Billing/Tailor360.Modules.Billing.Domain/Registrations/Gstin.cs` — the fifteen-character shape and
  the base-36 check character. **The server is the authority; the client never re-implements it.** The server also
  trims and upper-cases the GSTIN and trims the state code, so the client sends what was typed.
- `tests/Tailor360.IntegrationTests/Billing/TaxConfigurationEndpointTests.cs` —
  `RecordsARegistrationAndRefusesAnOverlappingOneAndAMalformedGstin`,
  `TheDatabaseRefusesOverlappingRegistrationsThatBypassTheHandler`,
  `RefusesEveryRouteToAHolderOfNoBillingPermission`, `RefusesADraftAndARegistrationWithoutAFirstDay`.
- `docs/security/permission-matrix.md` line 143 carries the permission row and lines 363–366 all four endpoint
  rows. **No matrix change belongs in this issue.**
- `docs/api/openapi.v1.json` already publishes the operations and `clients/pwa/src/api/schema.d.ts` already carries
  the paths (`/api/v1/billing/gst-registrations` at line 888, `/api/v1/billing/tax-configuration/versions` at line
  1662), so `pnpm --dir clients/pwa generate:api` is a no-op here and `generate:api:check` must stay green.

Client conventions to reuse rather than re-create:

- `clients/pwa/src/auth/apiClient.ts` (435 lines) — `apiRequest<T>(path, options)` and
  `apiRequestVersioned<T>(path, options): Promise<VersionedResponse<T>>` where
  `VersionedResponse<T> = { value: T; version: string | undefined }` (line 333) and `version` is the `ETag`.
  `ApiRequestOptions` (line 137) carries `method`, `body`, `signal`, `idempotencyKey`, `ifMatch`,
  `challengeOnUnauthenticated` and `challengeOnStepUp`. The transport adds the correlation, client-version and
  anti-forgery headers and renews a stale proof of identity in place on `403 security.step-up-required`
  (`STEP_UP_REQUIRED_CODE`, line 113), replaying the identical body with the same retry key. Do not write a second
  transport and do not answer a step-up refusal in a screen. `ApiError` (line 64) carries `status`, `code` and the
  parsed `problem`; its `message` is **never** rendered.
- `clients/pwa/src/admin/useAdminResource.ts` (83 lines) — `useAdminResource<T>(key: string, read: (signal) =>
  Promise<T>): { value, failure, loading, reload }`. The first argument is a **string** key, not an array, and the
  read must pass the signal to the request.
- `clients/pwa/src/routes/catalog/CatalogVersionListRoute.tsx` (211 lines) — the register pattern to copy: a
  `DataTable`, a `ConfirmDialog` with `tier="reason"`, an `Idempotency-Key` minted with `crypto.randomUUID()` and
  held across a refusal, `AuthProblemAlert` for the failure.
- `clients/pwa/src/admin/adminApi.ts` `listBranches()` (line 180) reading `GET /api/v1/admin/branches/`, and
  `clients/pwa/src/admin/testing/fixtures.ts` `aBranch` — the branch-name source the catalogue editor already uses.
- `clients/pwa/src/billing/billingPermissions.ts` (40 lines), `billingProblems.ts` (46 lines),
  `BillingProblemAlert.tsx` — the billing key list and the code-to-sentence map, both of which this issue extends.
  `billingProblemMessage(failure)` maps an `ApiError`'s `code` to a `MessageKey`; an unmapped code falls through to
  the generic sentence for its status.
- `clients/pwa/src/components/states/index.ts` — `LoadingState`, `EmptyState`, `ErrorState`, `Forbidden`,
  `RetryableError`, `OfflineBlockedAction`; `clients/pwa/src/components/dialogs/ConfirmDialog.tsx` and
  `.../dialogs/confirmTiers.ts`; `clients/pwa/src/i18n/formatters.ts` (`getFormatters`, `formatMoney`,
  `formatPercent`) and `clients/pwa/src/i18n/parseNumber.ts`.
- **`RequirePermission` does not render `Forbidden`.** `clients/pwa/src/admin/RequirePermission.tsx` renders an
  `EmptyState` with `admin.forbidden.title` / `admin.forbidden.body` and, by its own doc comment, **never
  redirects**. `Forbidden` (`clients/pwa/src/components/states/Forbidden.tsx`) is for an in-screen refusal and
  takes `action` in words (never a permission key), a required `onBack`, and `allowedRoles` so that A11Y-89's
  "name who can" is satisfied. Use the right one in the right place and assert the right text.

## Testing and story harness (exact — a wrong wrapper fails on the first render)

- **Route tests** use `render()` from `@testing-library/react` wrapped
  `AppIntlProvider locale="en-IN"` → `SessionProvider` → `MemoryRouter initialEntries={[…]}` → `Routes`/`Route`,
  exactly as `clients/pwa/src/routes/catalog/CatalogScreens.test.tsx` lines 60–80 does. `beforeEach` calls
  `forgetAntiforgeryToken()` (`clients/pwa/src/auth/antiforgery`) and `setSessionChallengeHandler(null)` and routes
  `GET /api/v1/me` through `stubFetch`; `afterEach` repeats both and calls `vi.unstubAllGlobals()` and
  `vi.restoreAllMocks()`.
  **`renderWithProviders` supplies neither a session nor a router** — it is for design-system components and for
  the pseudo-locale growth check of a presentational one, and a route rendered in it throws on `useCurrentUser`.
- **Stories** use `clients/pwa/src/admin/testing/storyTransport.tsx` — note the `.tsx` — which exports
  `storyJson(body, version?)`, `storyProblem(status, code)`, `storyPending()`, `withAdminApi(...)`, `STORY_USER`
  and the `StoryRoutes` type. Every story renders the **real route** under `withAdminApi`, following
  `clients/pwa/src/routes/catalog/catalogScreens.stories.tsx`.
- **Four state stories, not five.** Loading, empty, error and forbidden, plus the pseudo-locale story. There is
  deliberately **no offline story**, for the reason `catalogScreens.stories.tsx` lines 26–36 already records in
  prose: an offline story would imply these screens queue what a person did, and a billing write is never queued.
  `docs/process/definition-of-done.md` line 128 asks for five; the fifth is deleted with that one-line reason in
  the pull request, and the offline behaviour is proved instead by the named Vitest case and by
  `OfflineBlockedAction`'s own story. Copy the same paragraph into the new stories file so the reason travels.
- **Overflow** is checked with `expectNoHorizontalOverflow` at its **defaults** — 320, 360, 768, 1024 and 1280 CSS
  px at 100% and 200% zoom (`LayoutCheckOptions.widths`, `MINIMUM_REFLOW_WIDTH = 320` in
  `clients/pwa/src/design-system/testing/expectNoHorizontalOverflow.ts`). Do not narrow it to three widths:
  **NFR-AC-04** names all five and the 200% zoom.
- **Accessibility** is `expectNoAccessibilityViolations` from `clients/pwa/src/design-system/testing/axe.ts`.
  `RULES_JSDOM_CANNOT_JUDGE = ['color-contrast', 'target-size']` are excluded there; contrast and target size are
  the token tests' and the reviewer's, not this issue's.

## What is missing

- No client module names any pricing route: `clients/pwa/src/billing/billingApi.ts` (325 lines) holds payments,
  invoices, cashier sessions, reconciliation and receipts — nothing for price lists, tax configuration or GST
  registrations.
- `clients/pwa/src/i18n/messages/` holds **fifteen** families — `admin`, `auth`, `billing`, `catalog`, `dialogs`,
  `forms`, `install`, `layout`, `measurements`, `navigation`, `primitives`, `shell`, `states`, `units` and the
  `index` that composes them — and **none of them is pricing**. `billing.ts` (666 lines) is payments and invoices.
- `clients/pwa/src/admin/adminDestinations.ts` offers eight destinations — users, branches, roles, templates,
  catalog, features, audit, outbox — and none is pricing configuration.

## Scope

- [ ] **A new message family** `clients/pwa/src/i18n/messages/pricing.ts`, exporting `pricingEn` and
      `pricingTa: Record<keyof typeof pricingEn, string>` and registered in both spreads of
      `clients/pwa/src/i18n/messages/index.ts`. Every Tamil entry carries the
      `// not translated — awaiting native-speaker review` comment the other families use (see
      `clients/pwa/src/i18n/messages/billing.ts` line 245 and `catalog.ts` line 204), because `ta-IN` stays behind
      its flag until the review gate passes (`clients/pwa/CLAUDE.md` section 5). **The duplication is the
      record** — do not derive one catalogue from the other, for the reason `catalog.ts` lines 196–201 gives.
      This file is the one later siblings append to; append at the end of your own block, do not re-order it.
- [ ] **Hand-written payload types** in `clients/pwa/src/billing/pricingAdminTypes.ts`: `GstRegistration`,
      `GstRegistrationRequest`, `TaxConfigurationSummary`, `BillingFinding`, `BillingValidationReport`. Every
      decimal member is typed `number | string`, because the published schema types a decimal as
      `{"type": ["number", "string"]}`; the precedents are `CatalogVersionSummary.versionNumber` (line 23) and
      `CatalogValidationReport.errorCount` / `warningCount` (lines 188–189) in `clients/pwa/src/catalog/types.ts`.
      **No arithmetic is ever done on one.** Amounts, rates and dates are rendered through `getFormatters()`,
      never with `toLocaleString` at a call site.
- [ ] **The transport** `clients/pwa/src/billing/pricingConfigApi.ts`: `listGstRegistrations`,
      `readGstRegistration` (versioned — the `ETag` is what an amendment is made against),
      `recordGstRegistration` (`POST`, `Idempotency-Key`), `amendGstRegistration` (`PUT`, `Idempotency-Key` and
      `If-Match`, whole-value body), `listTaxConfigurationVersions`.
- [ ] **Conformance assertions** in `clients/pwa/src/api/contract.ts` for `GstRegistration`,
      `TaxConfigurationSummary`, `BillingValidationReport` and the `AddGstRegistration` request body, following the
      `Conforms<…, Immutable<…>>` pattern already in that file (see the `TemplateFieldRequest` pair at lines
      269–277 and the reasoning at line 111).
- [ ] **Both billing configuration keys named once** — add `managePriceLists: 'billing.manage_price_lists'` **and**
      `publishPriceList: 'billing.publish_price_list'` to `clients/pwa/src/billing/billingPermissions.ts`.
      Both are added here, by the first mover on that file, so that no later sibling has to touch it and the
      publish screens of E09-F01-5b and E09-F01-7b conflict with nothing. Add a one-line comment on
      `publishPriceList` saying which issues first use it. `billing.override_price` is deliberately **not** added:
      no screen ever asks for it — the engine consults it server-side (`PricingService.cs` line 194) — and a key in
      this list that nothing checks would read as a client-side decision, which it is not (DOR-11).
- [ ] **The GST registration register** `clients/pwa/src/routes/pricing/GstRegistrationsRoute.tsx` at
      `/admin/gst-registrations`: the organisation's registrations by branch and first day, a record form and an
      amend form. Branch names come from `listBranches()`; when the caller does not hold `admin.branches` the
      branch column falls back to the identifier and the form says which permission is missing — stated here so it
      is built deliberately rather than discovered.
- [ ] **The tax configuration version register** `clients/pwa/src/routes/pricing/TaxConfigurationListRoute.tsx` at
      `/admin/tax-configuration`: version number, name, status, first day and published-at, newest first, read
      only. Rows do not link anywhere yet and there is no "start a draft" control; the editor and the draft control
      are E09-F01-5, and a link to a route that does not exist is a dead end.
- [ ] **Wiring**: both routes into `clients/pwa/src/app/router.tsx` inside the `admin` children, each wrapped in
      `RequirePermission permission={BILLING_PERMISSIONS.managePriceLists}`; two destinations in
      `clients/pwa/src/admin/adminDestinations.ts`. `AdminDestination` is
      `{ readonly path: string; readonly messageId: MessageKey; readonly permission: string }` — the message id
      must be a key of the composed English catalogue, so put the two nav labels in the new `pricing` family, and
      the permission may be `BILLING_PERMISSIONS.managePriceLists` directly because the field is a `string` and not
      an `ADMIN_PERMISSIONS` value. **Insert after `catalog`**, which puts configuration together — and in the same
      edit **update that file's doc comment**, because the comment fixes the order ("a person who finds Roles in the
      fourth place today must find it in the fourth place tomorrow", 3.2.3 Consistent Navigation) and an insertion
      moves `features`, `audit` and `outbox` down two places. Say in the pull request that the three moved and why.
      #182 and #204 also add destinations: whoever merges second re-reads that comment rather than appending blindly.
- [ ] **Four states with a story each plus the pseudo-locale story** in
      `clients/pwa/src/routes/pricing/pricingScreens.stories.tsx`, built on `storyTransport.tsx`, following
      `clients/pwa/src/routes/catalog/catalogScreens.stories.tsx` — including its paragraph on why there is no
      offline story.
- [ ] **Refusals in the shop's words**: extend the `CODE_MESSAGES` map in
      `clients/pwa/src/billing/billingProblems.ts` with `billing.gstin-not-well-formed`,
      `billing.gstin-state-mismatch`, `billing.registration-overlaps`, `billing.registration-changed`,
      `billing.dates-not-ordered`, `billing.state-code-not-well-formed`, `billing.registration-not-found`. All
      seven exist in `src/Modules/Billing/Tailor360.Modules.Billing.Domain/BillingErrors.cs`. A code is never shown.
- [ ] **Fixtures** `clients/pwa/src/billing/testing/pricingConfigFixtures.ts`: `aGstRegistration`,
      `aTaxConfigurationSummary`, `aBillingFinding`. Reuse the existing `BRANCH_ID` constant from
      `clients/pwa/src/billing/testing/fixtures.ts` rather than minting a second branch identifier. Every GSTIN,
      name and number is synthetic (CLAUDE.md section 5).

## Out of scope

Drafting, editing, validating or publishing a tax configuration version (E09-F01-5, E09-F01-5b). Price lists,
versions, items and discount rules (E09-F01-6 through E09-F01-8). The pricing preview and the accountant's case
shapes (E09-F01-8b). Any server change at all: if an endpoint looks wrong, raise it as a new issue against #41
rather than changing it here. Re-implementing the GSTIN check character on the client — the server owns it, and a
second implementation that disagrees is worse than none.

## Acceptance criteria

- [ ] An Owner opens `/admin/gst-registrations`, records a branch's registration with a synthetic GSTIN of the
      documented shape, a state code, a legal name and a first day, and sees it in the register — with no
      deployment and no database access.
- [ ] An amendment changes the dates, the legal name and the trade name of an existing registration against the
      `ETag` the screen read, re-sending every field because the `PUT` is whole-value; the branch is shown as a
      read-only fact and still sent; the register shows the new values after the write.
- [ ] **Negative, refusal shape:** a second registration in force on the same day for one branch is refused with
      `billing.registration-overlaps` rendered as a sentence, the typed values stay on screen, and a retry reuses
      the **same** `Idempotency-Key`; a malformed GSTIN is refused with `billing.gstin-not-well-formed` and a
      GSTIN whose state prefix disagrees with the state code with `billing.gstin-state-mismatch`, both as field
      errors associated with their field, never as a code and never as a stack trace.
- [ ] **Negative, concurrency:** an amendment sent with a stale `If-Match` answers `billing.registration-changed`,
      and the screen offers a re-read and a retry rather than overwriting silently or showing an unhandled error.
- [ ] **Negative, authorisation:** somebody without `billing.manage_price_lists` meets
      `RequirePermission`'s `EmptyState` — `admin.forbidden.title` and `admin.forbidden.body`, never a redirect —
      and neither destination appears in the administration navigation.
- [ ] **Exception path:** a registration is never deleted. The screen offers a last day (`effectiveTo`) and says,
      in words, that documents already posted keep the registration they were posted with
      (`docs/prd/configurable-vs-fixed.md` line 66, the GST-registrations row).
- [ ] **Offline:** the record and amend controls render `OfflineBlockedAction` — whose title is fixed word for word
      as "Needs connection — this will not be queued" — because a billing write is never queued
      (`clients/pwa/CLAUDE.md` section 6, plan Section 4.6).
- [ ] The tax configuration register lists versions newest first with status and first day, and an organisation
      with no version reads as a fact with the act that ends it named, not as an error.

## Data and classification

Nothing new is stored anywhere by this issue: every value lives in component state for the life of the screen. No
`localStorage`, no service-worker cache, no client telemetry attribute.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| `gstin` on screen and in the record form | **Public** | The number printed on every invoice and the basis of place of supply | None client-side: component state only | Holders of `billing.manage_price_lists`; `owner` today | `docs/nfr/data-classification.md` line 50 names "the branch name, address and GSTIN printed on an invoice" as Public. Even so it is **never** put in client telemetry, and line 365 forbids the GSTIN in a server log line |
| `legalName`, `tradeName` | **Public** | The business identity printed on the invoice | None client-side | As above | Treated exactly as the GSTIN. Where the organisation is a proprietorship the legal name is also a person's name; no document settles that reading, so the handling chosen here is the one that is safe under either — permission-gated, never logged, never in telemetry. **Flagged for the reviewer** |
| `stateCode`, `effectiveFrom`, `effectiveTo`, `branchId` | **Internal** | Which registration is in force for a branch on a day | None client-side | As above | Identifiers and dates may appear in a correlated server log line; they are not telemetry attributes here |
| `TaxConfigurationSummary` — version number, name, notes, status, first day | **Internal** | To see what tax configuration exists and where it stands | None client-side | As above | Safe in a log line as identifiers; the register sends nothing to telemetry |
| `BillingFinding` — severity, code, message, target | **Internal** | To say why a publication would be refused | None client-side | As above | Declared here because two later siblings consume it; **nothing in this issue renders one**. The server's sentence is what is rendered; the code is never shown and never logged by the client |
| The optional `reason` on a record or amend | **Internal** — evidentiary | The audit trail of a configuration change | Server-side, per `docs/nfr/data-classification.md` section 5.16 | `auditor`, `owner` | Sent in the body and stored on the audit event; the client keeps it only until the request succeeds |

No rate, amount or discount is read by either screen in this issue, so nothing here is Confidential; the
Confidential rows arrive with E09-F01-6, E09-F01-7 and E09-F01-8. No retention period is stated because nothing is
retained, which is also why **OD-08** does not apply to this slice.

## Permissions and roles

`docs/security/permission-matrix.md` is authoritative and this issue changes no row in it.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `billing.manage_price_lists` | organisation | no | no | no | `owner` (Owner) | **Exists** — line 143, and the endpoint rows at lines 363–366. Both screens and all five transport functions are behind it |
| `billing.publish_price_list` | organisation | yes | yes | yes | `owner` (Owner) | **Exists** — line 148. **Named in `billingPermissions.ts` by this issue and used by none of its screens**: E09-F01-5b and E09-F01-7b are its first consumers, and naming it once here keeps them off this shared file |
| `admin.branches` | organisation | yes | yes | yes | `owner`, `admin` (Owner, Admin) | **Exists** — line 198. Read only, and only for branch **names**: `GET /api/v1/admin/branches/` is what `listBranches()` calls, and because the permission is marked step-up the transport may raise the in-place re-authentication the client already implements. The catalogue editor already depends on this read, so the behaviour is not new |

**OD-13** (the approved role-to-permission grants) is open; #24 shipped the matrix under a documented default, so
the grants above are proposed rather than approved. No authorisation-matrix fixture in
`tests/Tailor360.IntegrationTests` changes: no endpoint is added. The client-side fixtures this issue extends are
`clients/pwa/src/auth/testing/fixtures.ts` (`aCurrentUser({ permissions: … })`) and the new
`clients/pwa/src/billing/testing/pricingConfigFixtures.ts`.

## Modules, contracts and events

Client only. No .NET project is touched, no schema is read or written, no contract or integration event is consumed
or published, so ARCH-003, ARCH-004, ARCH-005, ARCH-007, ARCH-008, ARCH-010 and ARCH-011 are unaffected and stay
green — and because no `src/` file changes, ARCH-014 (`DateTime.UtcNow`) and ARCH-015 (`Guid.NewGuid()`) cannot be
broken by it either. The one generated identifier in this issue is the client's `Idempotency-Key` from
`crypto.randomUUID()`, which is a retry key in a browser and not a resource identifier. No media is read, so the
"media is never given a URL" rule is not engaged. The client crosses no boundary of its own: it speaks HTTP to two
endpoint groups of the Billing module and one read of Identity's branch list, each through
`clients/pwa/src/auth/apiClient.ts`.

DOR-05: the threat model that covers this surface is the administrative-change model behind #25 and the billing
models behind #42/#43; `docs/security/README.md` section 3.2 is the backlog until #56a merges. This issue adds no
new trust boundary, no new input that is not already validated server-side, and no new stored data. The one control
it must not weaken is deny-by-default — hence the authorisation criterion above.

## Screens and states

Administration is a desktop and tablet task, as `clients/pwa/src/routes/catalog/` and #141 both treat it; neither
screen gets a phone-first layout, and the typed confirmation tier is therefore available
(`clients/pwa/src/components/dialogs/confirmTiers.ts`).

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| GST registration register | `/admin/gst-registrations` | `LoadingState`, naming what is being read | `EmptyState`: no registration recorded, with the record control beside it | `RetryableError` for the read; `BillingProblemAlert` for a refused write, with typed values kept | `OfflineBlockedAction` on record and amend; the list still renders what was read | `RequirePermission`'s `EmptyState` for the route; `Forbidden` with `action`, `allowedRoles` and `onBack` for the in-screen branch-name refusal |
| Tax configuration register | `/admin/tax-configuration` | `LoadingState` | `EmptyState`: no version yet, said as a fact | `RetryableError` | No write on this screen, so no blocked action | `RequirePermission`'s `EmptyState` |

Message identifiers live under `pricing.*` in the new family. Dates go through `getFormatters()` (`dd-MM-yyyy`);
there is no money and no measurement on either screen. Every field carries a persistent visible label (A11Y-28), a
required field announces itself as required (A11Y-33), every server field error is announced as part of its field
and can be heard again (A11Y-34, A11Y-35), and the confirmation says what will happen before the control that does
it (A11Y-39). The forbidden state names who can and leaves a keyboard-reachable way back (A11Y-89).

## Migration and rollback

No schema change: this issue adds no table and no column, and touches no `DbContext` or migration. Proof is
`dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` reporting no outstanding migration, pasted in
the pull request. Rollback is redeploying the previous client bundle; the endpoints it calls were already live
before it.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-06** Branches at launch, with their GST registrations | Open (`docs/prd/assumptions-and-open-decisions.md` line 59) | The screen **invents nothing**: no seeded GSTIN, no default state code, no default branch. It is the surface on which the decision is recorded once the owner makes it, and every example in tests and stories is synthetic | W1 exit, per the decision's own row |
| **OD-13** Approved role-to-permission grants | Open (line 66); #24 shipped the matrix under a documented default | The screens ask for the keys the matrix already lists and decide nothing themselves | W1 exit |
| **AL-03** Touch-target sizes | **Proposed, to be confirmed** (`docs/nfr/accessibility-localisation.md` lines 147–149 and 477) | Build to the proposed numbers — 44 × 44 CSS px for a standard control, 32 × 32 for a dense desktop control — and expect the numbers, not the rule, to move | #50 closes |

**OD-11** is named only to say it does not apply: the supersession Section 6.2 note 1 asks for was recorded by
#147's pull request and is not repeated here. Nothing else in
`docs/prd/assumptions-and-open-decisions.md` blocks this slice, and no criterion above is deferred.

## Required verification

Every tier named here is one `./scripts/dev test` can run today. `tests/e2e` and its Playwright axe run arrive with
**#52** — `scripts/dev` line 434 says so at the `e2e)` branch — so the accessibility evidence for this issue comes
from the in-repo helpers, not from Playwright. Say that in the pull request so a reviewer expecting a Playwright
report is not left thinking something was omitted.

- [ ] **Client (Vitest, `pnpm --dir clients/pwa test`)**: a new
      `clients/pwa/src/routes/pricing/PricingRegisters.test.tsx` on the harness described above, querying by role
      and label only — recording a registration, amending one, the register's empty state, and the tax version list.
- [ ] **Client, the negative and exception cases, each named**: `RefusesAnOverlappingRegistrationInWords`,
      `ShowsAMalformedGstinAsAFieldError`, `ShowsAStateMismatchAsAFieldError`, `OffersARereadAfterAStaleTag`,
      `ReusesTheSameIdempotencyKeyOnRetry`, `SendsEveryFieldOnAnAmendment`,
      `ShowsASentenceWithoutTheBillingPermission`, `ShowsBranchIdentifiersWithoutTheBranchKey`,
      `BlocksTheRecordControlWhileOffline` — asserted with `problemResponse` from
      `clients/pwa/src/auth/testing/fixtures.ts`.
- [ ] **Accessibility**: `expectNoAccessibilityViolations` on both screens and on the record dialog, and
      `expectNoHorizontalOverflow` at its default widths and zoom levels.
- [ ] **Localisation**: the pseudo-locale story renders both screens with about 40% text growth and no overflow;
      `pnpm --dir clients/pwa typecheck` is what proves the Tamil catalogue has every key the English one has.
- [ ] **Contract**: `pnpm --dir clients/pwa generate:api:check` stays green (no API change), and
      `pnpm --dir clients/pwa typecheck` fails if a hand-written payload drifts from the published schema, which is
      what the new assertions in `src/api/contract.ts` are for.
- [ ] **Whole client gate**: `lint`, `typecheck`, `format:check`, `test`, `build`, run twice, per
      `clients/pwa/CLAUDE.md` section 8 and CLAUDE.md section 5.
- [ ] DOR-13: the non-functional rows this touches are **NFR-AC-02** (no serious or critical automated violation on
      a changed screen) and **NFR-AC-04** (no horizontal overflow and no obscured focus at 320, 360, 768, 1024 and
      1280 px and at 200% zoom) in `docs/nfr/traceability.md` lines 206 and 208. DOR-16: `./scripts/dev doctor`
      records which tiers ran in the environment used.

## Evidence the pull request will carry

Matched to `docs/process/definition-of-done.md` line 162 — a screen needs screenshots at three widths, the
accessibility run, the state stories and the pseudo-locale story.

- [ ] `pnpm --dir clients/pwa lint | typecheck | format:check | test | build` output, pasted, from two consecutive
      runs.
- [ ] Screenshots of both screens at phone, tablet and desktop widths — the phone width included to show the
      layout does not break, even though administration is not a phone task.
- [ ] The `expectNoAccessibilityViolations` and `expectNoHorizontalOverflow` output for the new screens.
- [ ] Storybook screenshots of the loading, empty, error and forbidden stories and the pseudo-locale story, with
      the one-line reason the offline story is deliberately absent.
- [ ] The refusal screenshots: overlapping registration, malformed GSTIN, state mismatch, stale `If-Match`, and the
      forbidden state for a caller without `billing.manage_price_lists`.
- [ ] A screenshot of `adminDestinations.ts`'s navigation before and after, showing the three destinations that
      moved, with the updated doc comment in the diff.
- [ ] `migrate --dry-run` output showing no outstanding migration, which is how "no schema change" is evidenced.
- [ ] `./scripts/dev doctor` output.
- [ ] A one-line note that these client children do not gate #32a, per the wave note above.

## Depends on

Nothing unmerged. The five endpoints it calls are live on `main`:

#145 — the GST registration and tax configuration endpoints, payloads and permission-matrix rows (closed
2026-09-12).
#24 — the permission catalogue and `RequirePermission` (merged).
#25 — `GET /api/v1/admin/branches/` and `listBranches()` on the client (merged).
#50 — the design system, the state components, `ConfirmDialog`, the formatters and the testing helpers (merged).

**Merge this one first.** Every sibling appends to files this issue creates or first touches:
`clients/pwa/src/i18n/messages/pricing.ts`, `clients/pwa/src/billing/pricingAdminTypes.ts`,
`clients/pwa/src/billing/pricingConfigApi.ts`, `clients/pwa/src/billing/billingProblems.ts`,
`clients/pwa/src/app/router.tsx`, `clients/pwa/src/api/contract.ts`,
`clients/pwa/src/routes/pricing/pricingScreens.stories.tsx` and
`clients/pwa/src/billing/testing/pricingConfigFixtures.ts`. It is also the only one of the eight that is
parallel-safe against the rest of the family.

Refs #41
