Sub-issue of #36. Follows E07-F02-1, E07-F02-2 and E07-F02-2b. **This is the `/scan` screen in single mode up to the
point of commit**: the scanner-first one-handed layout, the resolve call, the visual-first feedback, and the five
rejection remedies. The confirmation card, the undo window and the command port are **E07-F02-3b**, because this
route alone is already most of a session — `clients/pwa/src/routes/catalog/CatalogVersionEditorRoute.tsx` is 994
production lines for one route, before its entry components, its API module, its messages and its stories.

It is the screen four roles' navigation already points at and which does not exist:
`clients/pwa/src/components/layout/roleNavigation.ts` line 64 declares
`scan: { href: '/scan', icon: 'scan', messageId: 'navigation.destination.scan' }`, line 94 the primary action, and
the `tailor`, `tailor-master`, `inventory` and `delivery` rows all take `primaryAction: 'scan'` — asserted by
`roleNavigation.test.ts` line 69 — while `clients/pwa/src/app/router.tsx` (425 lines) has no `scan` path at all.

One ordering fact, so it is not discovered as a defect: until the planned **E12-F01-3** (a child of #50) passes the
journey role into the shell, `AppShell.tsx` line 26 falls back to `DEFAULT_JOURNEY_ROLE = 'reception'`, whose primary
action is `newOrder`. So **no acceptance criterion here may be phrased as "the primary action takes me there"**; this
issue is verified by navigating to the route and by the permission gate.

## What is already built (do not re-build)

- **E07-F02-1** — `clients/pwa/src/scanning/{types,scannerSource,barcodePayload,scanDebounce,KeyboardWedgeSource,
  scannerTelemetry}.ts` and `testing/fixtures.ts` with `FakeScannerSource`. **E07-F02-2** —
  `clients/pwa/src/scanning/camera/{cameraCapability,cameraPermission,decoders,CameraSource}.ts` and
  `camera/testing/fakeCamera.ts`. **E07-F02-2b** — `clients/pwa/src/i18n/messages/scanning.ts`,
  `clients/pwa/src/routes/scanning/ScannerCheckRoute.tsx` at `settings/device` and
  `clients/pwa/src/routes/scanning/scanningScreens.stories.tsx`. This screen composes all of it and redefines none
  of it.
- `clients/pwa/src/components/layout/shellStatus.ts` — **`ScanOutcome` is exactly `'accepted' | 'rejected'`**, and
  `ShellStatusProvider` holds **one** message per channel, so a second scan supersedes the first by construction
  (**A11Y-SC-08**). `ShellStatusRegions.tsx` lines 51-72 render a rejection in a `role="alert"` region and an
  acceptance in a `role="status"` region, each dismissible through `announceScan(null)`. **Do not widen
  `ScanOutcome`** to add an amber tone: that type, its stories and its tests belong to #50's design system, and the
  "needs confirmation" state is the confirmation card of **E07-F02-3b**, not a third shell channel.
  `clients/pwa/src/components/layout/useShellStatus.ts` **throws** outside a `ShellStatusProvider`, deliberately —
  so every test of this screen wraps it in one. `AppShell.tsx` line 118 provides it in the running application.
- `clients/pwa/src/components/layout/roleNavigation.ts` and `PrimaryActionFab.tsx` — already point here; changing
  anything in `components/layout/` would fail `roleNavigation.test.ts` and `PrimaryActionFab.test.tsx`.
- `clients/pwa/src/auth/apiClient.ts` — `apiRequest<T>(path, options)` with `ApiError`/`ApiProblem` (RFC 9457),
  `IDEMPOTENCY_HEADER`, `IF_MATCH_HEADER`, `ETAG_HEADER` and the single-flight session challenge. **Never call
  `fetch` from a screen.** `ApiRequestOptions` (lines 137-177) takes `method`, `body`, `signal`,
  `challengeOnUnauthenticated`, `challengeOnStepUp`, `idempotencyKey` and `ifMatch` and **nothing else** — there is
  no header bag, which is why **E07-F02-5** is the issue that adds the one narrow option it needs.
- `clients/pwa/src/admin/RequirePermission.tsx` — renders an **`EmptyState` carrying the message ids
  `admin.forbidden.title` / `admin.forbidden.body`** (lines 35 and 39). It is **not** the `Forbidden` component and
  it never redirects. Client-side decoration only; the server re-authorises every call.
- `clients/pwa/src/measurements/measurementsPermissions.ts` — the shape for naming a feature's permission keys once,
  to be copied rather than improvised.
- `clients/pwa/src/admin/useAdminResource.ts` — the read hook every merged screen uses. It **does not fit** this
  screen's main read, deliberately: a resolve is triggered by a decode, not by mounting, and `useAdminResource`'s
  contract is a string key plus a read executed on key change. The resolve therefore goes through `apiRequest`
  directly with an `AbortSignal`, and that reason belongs in the file's doc comment so the next reviewer does not
  "fix" it.
- `clients/pwa/src/components/states/` and `problemDetails.ts` — the five states and the problem renderer. A raw
  `detail` is never rendered directly.
- `clients/pwa/src/api/contract.ts` — `Conforms<TMine, TContract>` (line 147) with the helpers `Response200<…>` and
  `Immutable<…>`; the established call is
  `export type XConforms = Conforms<MyType, Immutable<Response200<'OperationId'>>>` (lines 149-190). Per-feature DTOs
  live in the feature's own `types.ts` and are imported here.
- `docs/nfr/a11y-checklist.md` section 5.3 — **A11Y-SC-04** (the result, job number and next expected action
  announced without going to look), **A11Y-SC-05** (a rejection announced assertively, naming *which* rule failed
  out of wrong namespace, bad check character, unknown identity, wrong branch, wrong custodian, and what to do
  next), **A11Y-SC-08**, **A11Y-SC-09** (visual as well as sound or vibration), **A11Y-SC-11** (the last result
  re-readable without rescanning). Section 4 adds **A11Y-40** to **A11Y-44** and **A11Y-47**.
- `docs/architecture/sequences/barcode-handoff.md` section 3 — the sequence this screen drives, down to
  "Confirmation card, or a running list in continuous mode" and "Green flash, large icon and result text in the live
  region". Section 2's last paragraph is the rule it obeys: the client classifies "for immediate feedback only" and
  a client-side checksum pass is never trusted (**INV-BID-07**, `invariants.md` line 331).

## What is missing

- No `/scan` route, no scan API module and no feedback surface beyond the shell's two regions.
  `clients/pwa/src/routes/scanning/` holds only E07-F02-2b's `ScannerCheckRoute.tsx`.
- `docs/api/openapi.v1.json` has **no** `/api/v1/custody/...` path today — only `/api/v1/billing/barcodes/{payload}`
  at line 14125. Until #194 merges there is no operation to pin a DTO against.

## Scope

Target: about 900 production lines plus tests, including the viewfinder overlay and the messages. One route.

- [ ] **`clients/pwa/src/scanning/scanningApi.ts`** — `resolveBarcode(payload, { signal })` over
      `GET /api/v1/custody/barcodes/{payload}` (#194), with the payload percent-encoded into the path. One function
      per act, the established per-feature shape. It does **not** retry: the endpoint carries the `scan-burst`
      rate-limit policy (`support-matrix.md` section 7.1, `RateLimitPolicyNames.ScanBurst`), and a client that
      retried a 429 on its own would spend the very budget the policy protects. A 429 is rendered as its problem.
- [ ] **`clients/pwa/src/scanning/types.ts` additions plus a `Conforms<>` entry in `clients/pwa/src/api/contract.ts`**
      pinning the resolve DTO — `{ status: 'active' | 'superseded' | 'invalidated' | 'unknown', jobId, jobNumber,
      currentPhase, custodian, permittedActions[] }`, the shape #194's scope fixes — against the operation #194
      publishes. Use the `Immutable<Response200<'…'>>` form and the operation id the generated `schema.d.ts`
      actually carries. **If that operation is not in `schema.d.ts`, stop and rebase on #194**: do not hand-write
      the shape and do not run `pnpm generate:api` in this branch.
- [ ] **`clients/pwa/src/scanning/scanningPermissions.ts`** — `SCANNING_PERMISSIONS = { scan: 'custody.scan' }`,
      named once, in the shape of `clients/pwa/src/measurements/measurementsPermissions.ts`.
- [ ] **`clients/pwa/src/routes/scanning/ScanRoute.tsx`** at **`scan`**, inside `RequireSession` and wrapped in
      `<RequirePermission permission={SCANNING_PERMISSIONS.scan}>` in `clients/pwa/src/app/router.tsx`.
- [ ] **The one-handed layout**, per plan Section 8 for #36: viewfinder in the upper 60% of the screen; source
      switch, torch, zoom and the primary action in a bottom bar within thumb reach; **every control ≥ 48 × 48 px**;
      no swipe-only and no long-press-only action. The overlay is a dialog over `useDialogBehaviour`
      (**A11Y-SC-06**) and says what to do while open (**A11Y-SC-07**).
- [ ] **Feedback, visual first**: a full-viewfinder colour flash with a large icon and result text — green accepted,
      red rejected with the reason and the next action. Both are published through `announceScan` into the shell's
      persistent regions, which is what makes them survive until dismissed rather than vanishing (**A11Y-42**), and
      the last result is re-readable without rescanning (**A11Y-SC-11**). Tone and vibration are **secondary and
      switchable**, stored with the existing client display preferences, and absent on iOS, which has no vibration
      API (`support-matrix.md` section 7.1 and plan Section 8). The amber "needs confirmation" tone is **E07-F02-3b's
      confirmation card**, not a shell channel.
- [ ] **Five rejection remedies, and an honest account of which five.** This screen answers the **resolve-stage**
      rejections: `barcode.wrong-namespace` (an `S-`, `I-` or `R-` label is in the operator's hand — say which kind
      and where it belongs), `barcode.malformed` or a failed local check character, `unknown`, `superseded` and
      `invalidated` — the last two naming the job number and offering **Reprint label** only when
      `permittedActions` carries `reprint-label`, which #194 sets per caller. **A11Y-SC-05**'s own five rules are a
      different list: wrong namespace, bad check character, unknown identity, **wrong branch** and **wrong
      custodian**. Wrong branch arrives as #194's branch-scoped refusal and is rendered through
      `problemDetails.ts`; **wrong custodian is a refusal of the scan command and therefore #37's**, so
      A11Y-SC-05 cannot be fully answered until #37 merges. Say that in the pull request rather than claiming the
      item. What this issue owes is that **every** problem code renders as a named rule and a next action, never as
      "scan failed".
- [ ] **A wrong-namespace payload is refused locally before any request** — and refused again by the server, so the
      local check is a convenience and never the control (INV-BID-07). A `gs1` digit string is **not** refused
      locally: #194 resolves EAN-8, EAN-13 and UPC-A against a stock item's `supplier_ean`, so the client sends it
      and lets the server answer.
- [ ] **Messages** added to `clients/pwa/src/i18n/messages/scanning.ts` under `scanning.scan.*`, in **both**
      `scanningEn` and `scanningTa`.
- [ ] **Stories** added to `clients/pwa/src/routes/scanning/scanningScreens.stories.tsx`, driving the **real** route
      through `withAdminApi` / `StoryRoutes` / `storyJson` / `storyProblem` / `storyPending` from
      `clients/pwa/src/admin/testing/storyTransport.tsx` — correct here, unlike on the Scanner check screen, because
      `/scan` **is** inside `RequireSession` and does call an API. Stories: loading, empty, error and forbidden.
      **No offline story** (`catalogScreens.stories.tsx` lines 27-33 record why), and `JourneyFrame` from
      `clients/pwa/src/stories/journeys/JourneyFrame.tsx` line 58 **cannot** be combined with `withAdminApi`,
      because it brings its own `MemoryRouter`. Pick `withAdminApi`.

Versioned configuration (DOR-11): nothing here is a product number. The viewfinder proportion and the 48 px control
floor are plan Section 8 for #36; the scan round-trip objective is **S4** in `docs/nfr/slo.md` and is not restated.

## Out of scope

The confirmation card, the `CONFIRM_BEFORE_COMMIT` set, the undo window and the `ScanCommandSubmitter` port
(**E07-F02-3b**); continuous mode and the `Commit N` driver (**E07-F02-4**); manual entry, the mandatory reason, the
job-number rung and the camera-denied recovery instructions (**E07-F02-5**); the real-device and screen-reader
readings (**E07-F02-6**); the scan command endpoint, the `scan_events` table and the custody state machine (**#37**);
the persisted offline queue (**#51**, plan Section 6.2 dependency note 8); the work queue this screen is entered
from (**#33**).

## Acceptance criteria

- [ ] Navigating to `/scan` as a signed-in member of staff holding `custody.scan`, a fake camera source decodes a
      synthetic `G-` payload, the resolve is called once, and the screen shows the job number, the current phase and
      the custodian from the response — no hand-built fixture inside the screen and no direct `fetch`.
- [ ] **Negative:** a caller without `custody.scan` sees the `RequirePermission` `EmptyState` carrying
      `admin.forbidden.title` / `admin.forbidden.body` — not a redirect, not the `Forbidden` component.
- [ ] **Negative:** a decoded `S-` payload is refused **before** any request; the refusal names the stock namespace
      and what to do with a stock label, and it is published into the shell's `role="alert"` region (**A11Y-SC-05**).
      A `barcode.wrong-namespace` problem from the server renders identically.
- [ ] **Negative:** a `gs1` digit string **is** sent to the server rather than refused locally, and whatever the
      server answers is what the screen shows.
- [ ] **Negative:** a 429 from the `scan-burst` policy renders as its own problem with a "slow down" remedy and the
      client does **not** retry it automatically.
- [ ] **Exception:** `superseded` and `invalidated` each show the job number and offer **Reprint label** only when
      `permittedActions` includes `reprint-label`; neither answers the same as `unknown`.
- [ ] **Exception:** the resolve failing with a network error leaves the screen usable, announces the failure
      assertively, and offers the scan again — no spinner is left running (**A11Y-44**).
- [ ] A second decode **replaces** the first result rather than adding a second persistent region, and the
      replacement is announced (**A11Y-SC-08**); the last result is re-readable without rescanning (**A11Y-SC-11**).
- [ ] No response field rendered anywhere is a customer contact field — #194's contract test guarantees the response
      carries none, and this screen's type has no such member.
- [ ] Accepted results land in `role="status"`, rejections in `role="alert"`, and the result is conveyed visually as
      well as by any sound or vibration (**A11Y-SC-09**).
- [ ] No axe violation in any rendered state; no horizontal overflow at `expectNoHorizontalOverflow`'s **default**
      widths and zoom levels; every interactive control ≥ 48 × 48 px, measured rather than assumed.

## Data and classification

Per `docs/nfr/data-classification.md` section 5.8 (barcode identities, scans and custody events — **Internal**) and
section 5.18 (telemetry — **Internal by construction**). Nothing is persisted client-side; the offline queue is #51.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Resolve response `jobId`, `jobNumber`, `currentPhase`, `permittedActions` | Internal | Show the result and decide which action is offered | On screen only; cleared on navigation | Staff holding `custody.scan`, within branch scope, enforced server-side | `jobId` may appear in a client log line (section 5.8). **Never** a telemetry attribute: a job number plus a device is a location trace of a named customer's garment |
| `custodian` display name | **Personal** — staff identity, `data-classification.md` section 5.15 | Say who holds the garment, which is the whole point of a custody check | Not persisted by the client | As above | Never in a log, never in telemetry. Rendered only |
| Feedback preference (tone on/off, vibration on/off) | Internal | Let a person in a loud workshop or a quiet fitting room choose | Stored with the existing client display preferences | The person at the device | Never transmitted |
| `decode.latency`, `decode.failure` | Internal by construction | Measure the decode term of the S4 objective | Client telemetry **30 days**, the figure already in section 5.18; no period in code | Operations, through the **OD-14** backend | Allowed, closed enumeration only |

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `custody.scan` | branch | no | no | no | `branch_manager`, `reception`, `tailor_master`, `tailor`, `delivery_staff` | **Exists** — `src/Platform/Tailor360.Platform.Security/Permissions/CustodyPermissions.cs` line 21 (`Scan = "custody.scan"`) and `docs/security/permission-matrix.md` line 180, "The universal shop-floor action: every handover of a garment is a scan, not a memory". The matrix attributes the row to #37; #36 consumes it |
| `custody.reprint_label` | branch | **yes** | **yes** | **yes** | `admin`, `branch_manager` — `permission-matrix.md` line 179 | **Exists** — `CustodyPermissions.cs`. This screen only **offers** the action when #194's `permittedActions` names it; the dialog that performs it is **E07-F01-7's** |

No permission row is added and no endpoint is introduced, so `docs/security/permission-matrix.md` and
`tests/Tailor360.IntegrationTests/Authorization/matrix.yaml` are **not** touched — both are single-session files,
and `matrix.yaml` has no custody entry at all today. The authorisation-matrix tests for the resolve endpoint are
**#194's**. **OD-13** is open, so the grants above are the documented default rather than an approved one.

Branch scope is **evaluated server-side** (#194), never inferred here: the screen sends a payload and renders an
answer. A 404 for another branch's identity is the documented answer (`permission-matrix.md` line 379 records the
pattern for the billing equivalent: another branch's, another organisation's, a bad check character and nothing at
all all read alike), and this screen must not try to tell those apart.

## Modules, contracts and events

Client-only. The one server contract consumed is #194's resolve endpoint; #37's scan command is **not** reached from
this issue at all — the port that reaches it is E07-F02-3b's. No .NET project, schema, migration, integration event
or outbox row. `docs/api/openapi.v1.json` and `clients/pwa/src/api/schema.d.ts` are **read, never regenerated** in
this branch. Threat-model coverage (DOR-05): `docs/security/README.md` section 3.2, flow **"Barcode identity and
custody"**, model `docs/security/threat-models/barcode-custody.md`, covered by **#56a** in **wave W2**, with the
custody invariants of `docs/architecture/invariants.md` standing in — **INV-BID-07** above all, which this screen
honours by treating its own classification as feedback. The planned abuse-case identifiers `ABF-03` and `ABF-04` are
frozen by `E14-F01a-1` and do not exist yet.

## Screens and states

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| Scan (single mode) | `scan` | `LoadingState` while the camera starts; a pending state while the resolve is in flight, announced at start and at outcome (**A11Y-44**) | `EmptyState` — nothing scanned yet, naming the ways to scan available on this device | `ErrorState` per reason code, assertive, naming the failed rule and the next action (**A11Y-SC-05**); an `ApiError` renders through `problemDetails.ts`, never as a raw message | The shell's `NetworkStatusBanner` reports the state; a resolve is a read, so `OfflineBlockedAction` is not used here — it belongs to the commit, which is E07-F02-3b's | `RequirePermission`'s `EmptyState` with `admin.forbidden.title` / `admin.forbidden.body` |

Localisation: all copy is `scanning.*` in both languages, gated by `clients/pwa/src/i18n/en-IN.ts`. Any time, date
or number goes through `getFormatters()` from `clients/pwa/src/i18n/formatters.ts` — never `Intl.*` or
`toLocaleString` at a call site. Accessibility items this screen owns: **A11Y-SC-04**, **-08**, **-09**, **-11**,
**A11Y-40** to **A11Y-44** and **A11Y-47**; **A11Y-SC-05** is owned jointly with #37, as the scope says.

## Migration and rollback

No table and no column: TypeScript plus one router entry. `migrate --dry-run` reports no outstanding migration;
rollback is redeploying the previous tag, after which `/scan` 404s to `NotFoundRoute` as it does today.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-07** device, browser and printer matrix | **Open** — needed before W0 exit | `support-matrix.md` sections 4, 6 and 7.1 as written | Before E07-F02-6 |
| **OD-13** role-to-permission grants | **Open** — #24 shipped the matrix under a documented default on 2026-09-06 | The `custody.scan` grants of `permission-matrix.md` line 180 | Before W1 exit (already overdue; recorded, not re-decided here) |
| **SM-09** whether personal phones may be used | **Proposed, to be confirmed** | Shop-issued devices for Tier 1 journeys. It changes nothing in this code; it is named because the feedback preference is stored per device | Before W1 exit |

## Required verification

`./scripts/dev doctor` first (DOR-16). No database needed; `pnpm --dir clients/pwa test`. NFR traceability
(DOR-13): **NFR-LT-03** and the S4 objective in `slo.md` (this issue touches the client terms and sets no target),
and the accessibility rows for **A11Y-SC-04**, **-05**, **-08**, **-09**, **-11** and **A11Y-40** to **A11Y-44**.

- [ ] Vitest, route — **use the real test harness**: `stubFetch()` from `clients/pwa/src/auth/testing/fixtures.ts`
      with a local `renderAt` helper composing `AppIntlProvider` → `SessionProvider` → `ShellStatusProvider` →
      `MemoryRouter` → `Routes`, exactly as `clients/pwa/src/routes/measurements/MeasurementScreens.test.tsx` does.
      **Do not use `withAdminApi` in a Vitest test**: its own doc comment says it is not the test stub — it swaps
      `globalThis.fetch` directly instead of `vi.stubGlobal`, so `vi.unstubAllGlobals()` will not restore it and the
      stub leaks into the next test file. `withAdminApi` is for the stories.
- [ ] Vitest, happy path: decode → resolve called once → job number, phase and custodian rendered and announced.
- [ ] Vitest, negative: no `custody.scan` → the forbidden `EmptyState` with its two ids; an `S-` payload refused
      with **no** request made; a server `barcode.wrong-namespace` rendered identically; a `gs1` digit string sent
      rather than refused; a 429 rendered without an automatic retry.
- [ ] Vitest, exception: `superseded` with and without `reprint-label`; `invalidated`; `unknown`; a network failure
      leaving the screen usable.
- [ ] Vitest, announcement: one persistent result at a time, the second superseding the first (**A11Y-SC-08**);
      accepted in `role="status"`, rejected in `role="alert"`; the result re-readable without rescanning.
- [ ] Accessibility: `expectNoAccessibilityViolations` on each state; `expectNoHorizontalOverflow` at its **default**
      widths and zoom levels; a keyboard-only pass over the overlay (`Escape`, focus restoration) and the bottom
      bar; a measured touch-target check for every bottom-bar control.
- [ ] **Deferred to #52, recorded here:** the Playwright scenarios plan Section 8 asks of #36 — injected scan events,
      and "camera permission denied completes the action via manual entry by job number" — cannot be written yet:
      `tests/e2e` and `playwright.config.ts` do not exist in the repository and `./scripts/dev` says the Playwright
      tier arrives with **#52**. Name them in the pull request so #52 inherits a list rather than a memory.
- [ ] `pnpm --dir clients/pwa lint`, `typecheck`, `format:check`, and the suite **twice**.

## Evidence the pull request will carry

- [ ] `pnpm --dir clients/pwa test` output, twice.
- [ ] Screenshots at 320, 768 and 1280 px of: the empty scan screen, an accepted result, a rejected wrong-namespace
      result, a `superseded` result with and without **Reprint label**, and the forbidden state. Synthetic only.
- [ ] The axe run and the `expectNoHorizontalOverflow` output; the touch-target measurements for the bottom bar.
- [ ] The Storybook story names, with the one-line reason there is no offline story.
- [ ] The `Conforms<>` entry quoted beside the #194 operation id it pins, so a reviewer can see the shape came from
      `docs/api/openapi.v1.json` and not from this issue.
- [ ] `git diff --stat` showing `docs/api/openapi.v1.json`, `clients/pwa/src/api/schema.d.ts`,
      `docs/security/permission-matrix.md` and `tests/Tailor360.IntegrationTests/Authorization/matrix.yaml`
      untouched.
- [ ] The named list of Playwright scenarios handed to #52, and the one-line note that **A11Y-SC-05** is answered
      jointly with #37.

## Depends on

**E07-F02-1** — the abstraction, the parser, the debounce and the telemetry sink.

**E07-F02-2** — the camera engine.

**E07-F02-2b** — the `scanning.*` message family, the `clients/pwa/src/routes/scanning/` directory and the stories
file this issue extends.

#194 — **[E07-F01-3]**, **open**. Provides `GET /api/v1/custody/barcodes/{payload}` under `custody.scan` and branch
scope, the `scan-burst` rate-limit policy, the `barcode.wrong-namespace` and `barcode.malformed` problem codes, the
`permittedActions` flag `reprint-label`, and the contract test that no customer contact field appears in the
response. **This issue cannot be finished before #194 merges**, because until then `clients/pwa/src/api/schema.d.ts`
has no operation to pin with `Conforms<>`.

#190 — **[E07-F01-1]**, open, transitively: the payload format and the check-character vectors.

Contended files, **one session at a time**: `clients/pwa/src/app/router.tsx` (E07-F01-6, E07-F01-7, E07-F02-2b,
#141, #142, #189 and #204 also add routes) and `clients/pwa/src/api/contract.ts`.
`clients/pwa/src/i18n/messages/scanning.ts` and `clients/pwa/src/routes/scanning/scanningScreens.stories.tsx` are
shared with the other children of #36, which merge in order.

Refs #36
