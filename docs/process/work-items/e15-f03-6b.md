Sub-issue of #61. The closing third of the plan's **#61b** regression work and the last of the three exception-path
children: the **delivery, feedback and notification** exceptions, and the full regression report with its stored
artefacts that the parent's first required verification demands ("full release regression with stored
reports/artifacts"). It is the child that completes the pass matrix, so it is also the child that may finally edit
the `docs/nfr/traceability.md` status cells — no sibling touches them.

It was split out of a single "money, delivery and notification" child because seven end-to-end specs plus a new
document plus four traceability cells is more than one session can carry. [E15-F03-6] takes `EX-08`, `EX-09`,
`EX-15` and the dispatch policy matrix; this one takes `EX-11`, `EX-12` and `EX-14` and the report.

## Before a session starts: the same two blocking pre-flight checks as [E15-F03-3]

Recorded in the issue as a DOR-03 re-check, never decided mid-flight. (1) `tests/e2e` holds the #52 page objects and
the scenario area, journey fixture, dataset helper and tag-driven reporter of [E15-F03-3]; confirm it, and confirm
that all fifteen earlier scenarios pass before adding any. (2) Every route these three scenarios drive exists in
`clients/pwa/src/app/router.tsx`. At HEAD there is **no delivery, feedback or notification-operator route at all**;
they arrive with the client children of #47, #48 and #49. A step whose screen is not built is not stubbed and not
driven through the API as a substitute: the scenario is not automatable yet and the sub-issue is re-scoped before a
branch is cut.

## What is already built (do not re-build)

- `tests/e2e/` — #52's configuration and page objects; `tests/e2e/scenarios/`, `support/journey.ts`,
  `support/dataset.ts`, `reporters/pass-matrix.ts` from [E15-F03-3]; the six walkthrough scenarios from
  [E15-F03-3] and [E15-F03-4]; the five intake, production and custody exception scenarios from [E15-F03-5]; the
  four money and dispatch scenarios from [E15-F03-6]. **Fifteen scenarios exist before this sub-issue starts.**
- `docs/prd/exceptions.md` — the catalogue. `EX-11` negative feedback (line 48), `EX-12` failed or returned
  delivery (49) and `EX-14` notification not delivered (51) are the three rows this sub-issue answers, each with its
  required handling and its `XQ-nn` open decision.
- `docs/prd/exceptions.md` section **7.1 Automated coverage** — the append-only `EX-nn` | spec | tag table created
  by [E15-F03-5] and extended by [E15-F03-6]. Append the last three rows; do not re-shape it, and do not edit the
  catalogue rows at lines 38 to 52.
- `docs/process/release-gates.md` `RG-05` — the journey list, the five priority-zero journeys, and the evidence it
  demands: "Playwright reports and traces, screenshots and videos of failures, the per-journey pass matrix by
  browser and device profile". Item **6** of the release evidence checklist (section 6) is that matrix; `RE-06` of
  `docs/process/release-evidence.md` line 101 is the row it fills.
- `docs/qa/test-strategy.md` — child [E15-F03-1]: the regression scope rule and, critically for this sub-issue, the
  **stored-artefact convention** — where a run's report, trace, screenshot and pass matrix are written, what CI
  uploads, what a release attaches. Implement that convention in the report; do not invent a second one.
- `docs/nfr/traceability.md` — `NFR-CU-02` (line 195), `NFR-CU-03` (196), `NFR-CU-04` (197) and `NFR-CU-06` (199),
  whose status cells read "Proof scheduled (#35, #36)", "(#36, #37)", "(#36)" and "Evidence only; scheduled (#35,
  #37, #61b)". These four are the cells this sub-issue edits, and **only** these four.
- The delivery and notification machinery as built: the compensating custody transfer of #48, the delivery engine,
  provider ports, retry, de-duplication and dead-letter queue of #47, and the idempotent service-recovery case of
  #49. **The mechanisms exist; this sub-issue asserts them from the screen inwards.**

## What is missing

- Three catalogued exceptions have no automated scenario: a returned delivery, a complaint, and a provider that
  never delivered the message. Each ends a customer relationship badly if it is mishandled, and none is covered by
  a walkthrough — `docs/prd/walkthroughs.md` section 8 says so explicitly.
- No regression report exists, so `RE-06` has nothing to point at and no document states which journeys ran, on
  which browsers and device profiles, against which build.
- The four `NFR-CU-…` rows still read "scheduled", because no child before this one has a complete matrix to claim
  them with.

## Scope

- [ ] Add `tests/e2e/scenarios/ex11-negative-feedback.spec.ts` (`@ex-11`): feedback at or below the configured
      rating threshold, and an explicit alteration request. Assert that the service-recovery case opens **exactly
      once** per low rating or alteration request (the idempotency the `EX-11` row requires), is owned, carries a
      due time, and escalates when it is not closed — per `XQ-04`, whose threshold and ladder are *proposed, to be
      confirmed*, so the assertion is made at the **configured boundary** and not at a number.
- [ ] Add `tests/e2e/scenarios/ex12-failed-delivery.spec.ts` (`@ex-12`): nobody at the address, a refusal at the
      door, and the parcel returning to the branch. Assert the compensating custody events, the garment's location
      after the return, that the **spent dispatch authorisation cannot be reused**, and that a new attempt requires
      a fresh authorisation re-evaluated against the payment rule from scratch.
- [ ] Add `tests/e2e/scenarios/ex14-notification-undelivered.spec.ts` (`@ex-14`): a provider rejection, a bounce and
      a dead-letter after the configured retries. Assert the visible delivery state, that no uncontrolled duplicate
      is sent, that the operator-visible failure names the **intent and the identifier and not the recipient
      address or the rendered body**, and the audited replay path. Provider faults are injected through the existing
      adapter fakes of #47 and #55 — never by calling a real provider, which is also what keeps `ARCH-009` intact.
- [ ] Append the three rows to the journey table of `tests/e2e/scenarios/README.md` and the three `EX-nn` rows to
      `docs/prd/exceptions.md` section 7.1, append-only, in the convention [E15-F03-5] established. After this
      sub-issue every one of `EX-01` to `EX-15` names either a spec or the walkthrough that covers it.
- [ ] Add `docs/qa/regression-report.md`: the form a release fills in — build identifier and image digest, the
      scenario inventory with its `@tag`, the browser and device profile matrix, the outcome, the artefact links at
      the paths the strategy fixed, the open waivers — **plus the first completed run** against the current build. It
      cites `RG-05` and `RE-06` and restates neither.
- [ ] Update the status cells of `NFR-CU-02`, `NFR-CU-03`, `NFR-CU-04` and `NFR-CU-06` in `docs/nfr/traceability.md`
      to name the end-to-end suite as a proof now that the matrix is complete — **the automated half only**. The
      physical-device half of `NFR-CU-06` stays scheduled against the pilot of [E15-F03-10], and the cell says so.
- [ ] Add the `docs/qa/regression-report.md` row to `docs/qa/README.md`.

## Out of scope

- Adding or changing a retry count, an escalation ladder, a rating threshold or a delivery-failure reason code.
  Those belong to #47, #48 and #49; this sub-issue asserts what they built at the configured boundary.
- `EX-01` to `EX-10`, `EX-13` and `EX-15` — the walkthroughs, [E15-F03-5] and [E15-F03-6].
- The dress rehearsal, the restore and the rollback — [E15-F03-7]. The report this sub-issue writes is the one the
  rehearsal runs **twice**, but the rehearsal itself is not here.
- Mapping the report to the #1 release criteria and the P0/P1 journey list: that is the release evidence index of
  [E15-F03-11]. **This report states what ran; that index states what it proves.**
- Physical devices, printers and scanners — the pilot of [E15-F03-10].
- Editing any `NFR-…` row other than the four named above, or any `RG-nn` severity.

## Acceptance criteria

- [ ] `./scripts/dev test e2e` runs the full suite — six walkthroughs plus twelve exception and policy scenarios,
      eighteen in all — and passes, and the pass matrix lists every one against every browser and device profile
      with **no edit to the reporter**.
- [ ] `docs/qa/regression-report.md` carries a completed run: every scenario, every profile, the build it ran
      against, and a link to each stored artefact at the path the strategy fixed. A reviewer can follow a link and
      land on a real artefact.
- [ ] Every `EX-nn` from `EX-01` to `EX-15` appears in section 7.1 with either a spec file or the walkthrough that
      covers it. Pasted as a fifteen-row checklist in the pull request, because this is the criterion that proves
      the catalogue is no longer a promise.
- [ ] Negative case: `@ex-14` asserts that a retry does not produce a second customer message, and that no recipient
      address, rendered body or token appears in any operator-visible diagnostic (`CLAUDE.md` section 4 rule 7) —
      the same property #47's redaction tests assert server-side, here proved from the screen.
- [ ] Negative case: `@ex-12` asserts the spent dispatch authorisation is refused on re-use, as a problem-details
      body with a reason.
- [ ] Negative case: one forbidden actor per scenario — a Tailor cannot close a service-recovery case, a Delivery
      Staff principal cannot replay a notification — asserted as a refusal, not as a missing button.
- [ ] Exception case: `@ex-12` asserts the garment is back in branch custody with an unbroken chain and can be
      delivered again without a new order.
- [ ] Exception case: `@ex-11` asserts that a second identical low rating does **not** open a second case, and that
      the free text is never quoted into a log or a notification body.
- [ ] Run twice and in reverse order, with no inter-scenario state (`CLAUDE.md` section 5).

## Data and classification

Per `docs/nfr/data-classification.md` section 2. This is the most sensitive of the three exception suites: it
renders customer messages and free text.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| The feedback free text `@ex-11` submits | **Personal** (synthetic), free text | Exercise the rating threshold and the recovery case | As the repository | Anyone with repository access | Synthetic sentences only. Never machine-translated and never quoted in a log (`docs/nfr/accessibility-localisation.md` section 11.4) |
| The notification intents `@ex-14` drives | **Personal** — a recipient and a rendered body | Exercise rejection, bounce, dead-letter and replay | As the repository | Anyone with repository access | The scenario asserts that the operator view shows the intent, the state and the identifier and **not** the recipient address or the rendered body |
| The compensating custody events `@ex-12` produces | Internal, **evidentiary** | Prove the chain is unbroken after a return | As the repository | Anyone with repository access | Garment identifier and action only; never the delivery address |
| Playwright traces, screenshots and videos | **Personal by construction** | Diagnose a failure | The CI artefact retention the `ci.yml` upload step applies | Whoever can read the CI run | Safe only because every value is synthetic |
| `docs/qa/regression-report.md` | Internal | `RG-05` and `RE-06` evidence | With the release record | Technical reviewer, Owner | Scenario, tag, browser, profile, outcome, duration, artefact link. **No payload, no person, no amount** — the report links to artefacts, it does not inline them |

## Permissions and roles

No permission key is added.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| The service-recovery ownership `@ex-11` exercises | branch | per the matrix | per the matrix | per the matrix | `branch_manager` (Branch Manager) | **Exists** — `XQ-04` proposes the owner and the due time |
| The delivery-outcome grant `@ex-12` exercises | branch | per the matrix | per the matrix | per the matrix | `delivery_staff` (Delivery Staff) | **Exists** — as published in `docs/security/permission-matrix.md` |
| The notification replay `@ex-14` exercises | organisation | per the matrix | per the matrix | yes | per the matrix (`admin`, `branch_manager`) | **Exists** — `notifications.replay` with a mandatory reason and step-up; the mandatory reason is part of the assertion |
| — | — | — | — | — | — | Nothing new proposed. **OD-13** open, so every grant is proposed rather than approved |

The authoritative authorisation proof stays in `tests/Tailor360.IntegrationTests/Authorization/`.

## Modules, contracts and events

No module project is touched; no schema is read or written; no contract or integration event is added. The suite
drives the composed application through `Tailor360.Web`, so no `ARCH-…` rule and no `MO-…` boundary is engaged.
Provider faults are injected through the existing adapter fakes of #47 and #55, which is the only sanctioned way to
make a provider fail in a test — `ARCH-009` keeps the real SDKs in `Integration.Infrastructure`.

DOR-05: the flows exercised here — customer messaging, delivery and feedback — have their coverage recorded in the
threat-model coverage table of `docs/security/README.md` section 3.2, and the abuse cases regressed are
message-content leakage and workflow bypass. The suite adds no flow of its own. **Checked fact**: `docs/security/threat-models/` holds exactly one file at HEAD, `authentication.md`. The rest of the threat-model set arrives with #56a, so until it merges DOR-05 is satisfied the way the criterion itself prescribes — the flow appears in the backlog table of `docs/security/README.md` section 3.2 with its covering issue and wave. Cite that table, not a file that does not exist.

## Screens and states

No screen is added. A missing accessible name is fixed on the component, not worked around with a test attribute
(`clients/pwa/CLAUDE.md` section 4), and each such change is listed in the pull request.

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| None added | — | — | — | — | — | — |

## Migration and rollback

No schema change and no migration: three spec files, one document, README rows and four status cells.
`migrate --dry-run` reports no outstanding migration. Rollback is `git revert`, which returns the four status cells
to "scheduled" — and that is the right outcome, because without the suite the proof is gone.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **XQ-04** — the service-recovery rating threshold, case due time and escalation ladder per branch | Proposed, to be confirmed (at most 3 of 5, owned by the Branch Manager, due within one business day) | `@ex-11` asserts at the configured boundary and cites `XQ-04` in a comment | The go/no-go of [E15-F03-11] |
| **XQ-05** — whether a customer is told when a garment fails quality control | Proposed, to be confirmed (only when the date moves) | Asserted as the catalogue states it; `@ex-14` does not assume a message exists where the policy sends none | The go/no-go of [E15-F03-11] |
| **OD-03** — providers (SMS, WhatsApp, email, payment gateway, accounting target) | Open | Adapter fakes only. `@ex-14` never calls a real provider, and the report says so | The pilot of [E15-F03-10] |
| **OD-08** — retention periods, including notification bodies | Open | The report carries no message body at all, so no retention decision is implied | The privacy work of #57 |
| **OD-04** — the payment rule for dispatch and the partial-delivery policy | Open | `@ex-12`'s re-attempt is re-evaluated against whatever policy is configured; the spec asserts that it *is* re-evaluated, never the threshold | The go/no-go of [E15-F03-11] |
| **RG-OD-01** — the proposed gate severities and waiver durations | Open | `RG-05` as published: S1 for a priority-zero journey, S2 otherwise. The report's outcome column quotes them as proposed | The go/no-go of [E15-F03-11] |

No threshold, retry count, duration or escalation step is invented. Where one is needed the spec asserts at the
configured boundary and the report cites the decision.

## Required verification

- [ ] End-to-end: the full eighteen-scenario suite green locally and in the `e2e-regression` CI job, with the
      scenario count in the output.
- [ ] End-to-end negative: the no-duplicate-message assertion, the spent-authorisation refusal, the
      single-case-per-low-rating assertion and each forbidden actor, all asserted by name.
- [ ] Redaction: the `@ex-14` assertion that no recipient address, rendered body or token appears in an
      operator-visible diagnostic.
- [ ] Run twice and in reverse order.
- [ ] Completeness: the fifteen-row `EX-nn` coverage checklist, pasted.
- [ ] `DOR-16`: `./scripts/dev doctor` with Playwright browsers FOUND plus a running stack, and the adapter fakes
      configured. No physical device and no real provider account.
- [ ] `DOR-13`: the status cells this sub-issue edits are `NFR-CU-02`, `NFR-CU-03`, `NFR-CU-04` and `NFR-CU-06` in
      `docs/nfr/traceability.md` section 12, each named as **partly** proved by the suite with the physical half
      still scheduled. No other row's status is touched, and no sibling touches these four.

## Evidence the pull request will carry

- [ ] `docs/qa/regression-report.md` completed for the run, pasted in the pull request as well as committed.
- [ ] The Playwright report and the full pass matrix markdown table for all eighteen scenarios.
- [ ] The `@ex-14` operator-view screenshot, showing the intent and state without the recipient.
- [ ] The trace of the refused re-use of a spent dispatch authorisation.
- [ ] The fifteen-row `EX-nn` coverage checklist.
- [ ] The two-run output including the reversed order.
- [ ] The `docs/nfr/traceability.md` and `docs/prd/exceptions.md` rendered diffs.
- [ ] `migrate --dry-run` output showing no outstanding migration.
- [ ] The pull-request template with items 1, 3, 6, 8 and 9 ticked; items 2, 4 and 5 deleted with the one-line
      reason "no endpoint, no migration, no contract change".

## Depends on

#[E15-F03-3] — the scenario area, the journey fixture, the dataset helper, the tag-driven reporter and the CI job.
#[E15-F03-4] — the walkthroughs these diverge from. #[E15-F03-5] and #[E15-F03-6] — the earlier exception sets and
the `docs/prd/exceptions.md` section 7.1 table this completes. It must merge **last** of the four, because the
report and the status cells claim the whole matrix.

#[E15-F03-1] — the stored-artefact convention the report's artefact-link column implements.

#47 — the delivery engine, the provider fakes, the dead-letter path and the audited replay (**open**). #48 — the
partial-delivery policy, the two-stage dispatch, the delivery queue and the doorstep confirmation (**open**). #49 —
feedback, alteration requests and service recovery (**open**). #55 — the provider adapter fakes `@ex-14` injects
through (**open**; its children #200, #203 and #205 are open). Each must have merged, or the scenario has nothing
to assert.

#59 — the environment and release workflow the suite runs against in CI (plan Section 6.2 note 11).

Parallel-safe with [E15-F03-4], [E15-F03-5] and [E15-F03-6] only for the two append-only files — the journey table
in `tests/e2e/scenarios/README.md` and section 7.1 of `docs/prd/exceptions.md`. **Not** parallel-safe with
[E15-F03-11], which also edits `docs/nfr/traceability.md` status cells; this one edits `NFR-CU-02` to `NFR-CU-04`
and the automated half of `NFR-CU-06`, and [E15-F03-11] edits the rest after the pilot, so the two are sequenced
rather than concurrent.

Refs #61
