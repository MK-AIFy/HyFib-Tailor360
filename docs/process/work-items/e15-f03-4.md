Sub-issue of #61. The second third of the plan's **#61b**. Child [E15-F03-3] lands the business-scenario harness,
the tag-driven pass matrix and the two blouse walkthroughs; this one completes the happy-path regression with the
four walkthroughs that carry the structural cases — multiple garments on one order, job dependencies, a revision
before production, a design revision after confirmation, a hold and resume, multiple advances and a dispatch
blocked on payment. The gap is the same checkable one: `ls tests/e2e` fails today, and
`docs/prd/walkthroughs.md` lines 772 to 773 say these walkthroughs "are the source for the business-scenario
regression suite of issue #61b", which has never existed as an issue.

## What is already built (do not re-build)

- `tests/e2e/` — the Playwright configuration, browser and device projects, reporter list and page objects of #52.
- `tests/e2e/scenarios/`, `tests/e2e/support/dataset.ts`, `tests/e2e/support/journey.ts` and
  `tests/e2e/reporters/pass-matrix.ts` — child [E15-F03-3]. **The reporter derives the matrix from spec tags, so
  this sub-issue adds spec files and edits no shared file.** Follow the conventions in the scenarios README; do not
  restate or re-shape them.
- `tests/e2e/scenarios/w1-blouse-pattern.spec.ts` and `w2-blouse-aari.spec.ts` — the two worked examples. Copy their
  structure: one spec per walkthrough, tagged, asserting the walkthrough's own "Artefacts produced" list.
- `docs/prd/walkthroughs.md` — the source narratives, each with its money section and its artefact list: section 4
  (lines 328 to 423) walkthrough 3 **Salwar: two sets on one order, and a garment that may not leave**; section 5
  (424 to 531) walkthrough 4 **Lehenga: three jobs that must travel together**; section 6 (532 to 619) walkthrough 5
  **Gown: measurements change before production starts**; section 7 (620 to 716) walkthrough 6 **Kids: two children,
  short material and a design revision**. Section 8's grid (lines 717 to 741) is the authoritative statement of what
  each one must exercise.
- `docs/prd/state-transitions.md` — the permitted transitions each revision, hold and resume must follow. A scenario
  asserts the documented transition, it does not define one.
- `tests/fixtures/manifest.json` — child [E15-F03-2]: the fixed identifiers, branch codes and catalogue codes. No
  identifier is hard-coded in a spec.
- `docs/process/release-gates.md` `RG-05` — the journey list and the five priority-zero journeys. Walkthrough 3's
  blocked dispatch is one of them, so its failure is **S1**.

## Before a session starts: the same two blocking pre-flight checks as [E15-F03-3]

Recorded in the issue as a DOR-03 re-check, never decided mid-flight. (1) `tests/e2e` holds the #52 page objects and
this sub-issue's own additions from [E15-F03-3]; confirm it. (2) Every route each of these four walkthroughs drives
exists in `clients/pwa/src/app/router.tsx`. At HEAD there is no intake, order, job-card, label, scan, custody,
phase, quality-control, inventory, delivery or feedback route at all — they arrive with #204, #141, #142 and the
client children of #33 to #49. A step whose screen is not built is not stubbed and not driven through the API as a
substitute: the walkthrough is not automatable yet and the sub-issue is re-scoped before a branch is cut.

## What is missing

- Four of the six documented walkthroughs have no automated scenario, and with them every structural case the
  single-garment blouse journeys cannot reach: two and three garments on one order, the `finish_before` and
  `deliver_together` dependencies, an order revision while every garment is still confirmed, a design revision after
  confirmation, a hold and its resume, two advances against one order, a doorstep delivery, and a dispatch the
  payment rule refuses.
- The pass matrix therefore covers two journeys out of six, which is not the "business-scenario regression per
  garment category" the parent's third implementation step asks for.

## Scope

- [ ] Add `tests/e2e/scenarios/w3-salwar-two-sets.spec.ts`, tagged `@w3`: one order with two salwar sets, customer
      material taken into custody, the production run for both garments, the **dispatch refused while the balance
      stands**, the payment that clears it, the doorstep delivery of the released garment, and feedback. Assert the
      artefacts of section 4.3.
- [ ] Add `tests/e2e/scenarios/w4-lehenga-three-jobs.spec.ts`, tagged `@w4`: three garment jobs on one order with
      the `finish_before` and `deliver_together` dependencies declared at intake, consent recorded during the visit,
      six pieces of customer material in custody, shop stock consumed, two advances recorded at different times, the
      alteration decision, the doorstep delivery of the set together, and feedback. Assert that the set cannot be
      dispatched while one job is unready — the dependency, not the payment — and assert section 5.3's artefacts.
- [ ] Add `tests/e2e/scenarios/w5-gown-revision.spec.ts`, tagged `@w5`: the order revised before production starts
      because the measurements changed, with the new measurement version snapshotted onto the job and the previous
      snapshot still readable, then production, counter collection and feedback. Assert section 6.3's artefacts.
- [ ] Add `tests/e2e/scenarios/w6-kids-design-revision.spec.ts`, tagged `@w6`: two children's garments on one
      order, a design revision after confirmation, the hold when the material runs short and the resume when it
      arrives, counter collection and feedback. Assert section 7.3's artefacts.
- [ ] Add page objects only where a screen these four journeys reach has none, under the directory and in the style
      #52 established. List each addition in the pull request.
- [ ] Extend `tests/e2e/scenarios/README.md` (created by [E15-F03-3]) with the four new rows of its journey table —
      the one shared file this sub-issue touches, appended to, never re-ordered, so a sibling rebases cleanly.

## Out of scope

- Every exception path that is not in these four walkthroughs: `docs/prd/walkthroughs.md` section 8 states plainly
  that "no walkthrough ends in a cancellation, a refund, a credit note, a failed delivery, a custody mismatch or a
  service-recovery case". Those are `EX-13`, which belongs to [E15-F03-5]; `EX-08`, `EX-09` and `EX-15`, which belong to
  [E15-F03-6]; and `EX-11`, `EX-12` and `EX-14`, which belong to [E15-F03-6b].
- The per-policy dispatch variants under **OD-04**. Walkthrough 3 exercises the policy *as configured in the
  fixture*; proving every policy is [E15-F03-6]'s.
- The pass-matrix reporter, the CI job, the scenario fixture and the dataset helper — all [E15-F03-3]'s, consumed
  here unchanged.
- The published regression report — [E15-F03-6b]; its mapping to the #1 release criteria — [E15-F03-11].
- A seventh walkthrough for a cancelled and refunded order. `docs/prd/reviews/exception-review.md` line 172 records
  it as proposed to the workshop and not written; a scenario cannot precede the walkthrough it automates.

## Acceptance criteria

- [ ] `./scripts/dev test e2e` runs all six scenarios and passes, and the pass matrix lists six journeys against
      every browser and device profile the #52 configuration declares — with no edit to the reporter.
- [ ] Walkthrough 4 asserts the dependency semantics observably: with one of the three jobs unready, the set cannot
      be dispatched, and the refusal names the unready garment rather than failing generically.
- [ ] Negative case: walkthrough 3 asserts the **dispatch refused while the balance stands**, through the delivery
      team's receive scan, with the refusal readable by the person holding the scanner — a problem-details body with
      a reason, never a silent no-op. This is an `RG-05` priority-zero journey.
- [ ] Negative case: walkthrough 5 asserts that the superseded measurement snapshot is still readable after the
      revision, and that the revision is refused once the first garment has entered production.
- [ ] Exception case: walkthrough 6's hold and resume is asserted to lose nothing — the job returns to the same
      phase with its history, its design revision and its stock reservation intact, and the hold reason remains on
      the record.
- [ ] Every scenario drives the application through the client and the public API only. No test writes to the
      database or calls a module internal.
- [ ] Run twice: all six scenarios pass in two consecutive runs, in both orders, with no shared state carried
      between them (`CLAUDE.md` section 5).
- [ ] The four new specs are parallel-safe with [E15-F03-5]: `git diff --stat` shows no file in common beyond the
      appended rows of the scenarios README.

## Data and classification

Per `docs/nfr/data-classification.md` section 2. The classes are the same as [E15-F03-3]'s because the mechanism is
the same; what changes is that these journeys render **money** and **customer material** as well.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Scenario input values | Public (synthetic) | Drive the journey | As the repository | Anyone with repository access | From `tests/fixtures/` only; the codes and numbers follow `docs/prd/walkthroughs.md` section 1.2 |
| Playwright traces, screenshots and videos | **Personal by construction** | Diagnose a failure | The CI artefact retention already applied by the `ci.yml` upload step | Whoever can read the CI run | Safe only because the data is synthetic; never run against a real installation |
| The amounts these scenarios assert (two advances, a balance, a refusal) | Public (synthetic) but **financially shaped** | Prove the dispatch gate and the allocation behave | As the repository | Anyone with repository access | Quoted from the walkthrough's own money section. A figure is never invented in a spec: if the walkthrough does not state it, the spec asserts the *relation* (balance greater than zero) rather than a number |
| The pass matrix rows these four add | Internal | `RG-05` evidence and `RE-06` | With the release record | Technical reviewer, Owner | Journey, tag, browser, profile, outcome, duration only |

## Permissions and roles

No permission key is added. The scenarios act as principals holding the published grants of
`docs/security/permission-matrix.md`.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| The grants exercised by walkthroughs 3 to 6 | branch | per the matrix | per the matrix | per the matrix | `reception`, `measurement_staff`, `tailor_master`, `tailor`, `inventory_clerk`, `cashier`, `delivery_staff`, `branch_manager` (Reception, Measurement Staff, Tailor Master, Tailor, Inventory Clerk, Cashier, Delivery Staff, Branch Manager) | **Exists** — as published |
| The dispatch-exception approval walkthrough 3 does **not** use | branch | per the matrix | per the matrix | yes | `owner` (Owner), and `admin` only if **OD-04** says so | **Exists, deliberately unexercised here.** The scenario asserts the refusal, not the override; the override path is [E15-F03-6]'s |

**OD-13** is open, so every grant above is proposed rather than approved; a matrix change changes which principal a
scenario signs in as and nothing else.

## Modules, contracts and events

No module project is touched, no schema is read or written, no contract or event is added. The scenarios drive the
composed application through `Tailor360.Web`, so no `ARCH-…` rule and no `MO-…` boundary is engaged.

DOR-05: every flow these journeys cross already has a threat model or a covering issue recorded in
`docs/security/README.md` section 3.2. The journeys add no trust boundary. The only security-relevant property is
the one in [E15-F03-3]'s table and unchanged here: sign-in credentials come from a secret, never from a committed
value (`CLAUDE.md` section 4 rule 6).

## Screens and states

No screen is added. A page object added for an existing screen must address it by accessible role and name, per
`clients/pwa/CLAUDE.md` section 4; a test-only attribute is a last resort and is listed in the pull request.

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| None added | — | — | — | — | — | — |

## Migration and rollback

No schema change and no migration: four spec files and, at most, page objects. `migrate --dry-run` reports no
outstanding migration. Rollback is `git revert`, which removes four specs and four pass-matrix rows.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-04** — payment rule for dispatch and the partial-delivery policy | Open | Whatever the fixture configures; walkthrough 3 asserts the refusal and the release after payment, never a threshold. Proving each policy is [E15-F03-6]'s | [E15-F03-6] and the go/no-go of [E15-F03-11] |
| **OD-05** — valuation, rounding and the accountant's examples | Open | The money assertions follow each walkthrough's own money section, which is itself marked illustrative pending OD-05 | The accountant approval of [E15-F03-8] |
| **OD-06** — branches, timezones and calendars | Open | The two seeded branches; a due date is asserted as a relation (after confirmation, before the promise) rather than a date | The pilot of [E15-F03-10] |
| **OD-07** — device and browser matrix | Open | The #52 projects | The pilot of [E15-F03-10] |
| **OD-10** — catalogue, templates, design options, QC checklists | Open | The seeded catalogue the manifest names; a category is referenced by its seeded code | The pilot of [E15-F03-10] |
| Walkthrough 9's own open-decision list (`docs/prd/walkthroughs.md` section 9) | Open | As recorded there; nothing in these specs may be read as settling one | As recorded there |

No amount, duration, dependency count or threshold is invented: each comes from the walkthrough being automated or
is asserted as a relation.

## Required verification

- [ ] End-to-end: `./scripts/dev test e2e` green with all six scenarios, locally and in the `e2e-regression` CI job.
- [ ] End-to-end negative: the three refusals above (unpaid dispatch, unready dependency, revision after production
      starts) each asserted by a named test, with the problem-details body checked, not just the status.
- [ ] End-to-end exception: the hold-and-resume and the design-revision assertions named.
- [ ] Run twice, and once with the specs in reverse order, proving no inter-scenario state.
- [ ] `DOR-16`: `./scripts/dev doctor` with Playwright browsers FOUND plus a running stack; no physical device.
- [ ] `DOR-13`: contributes proof to `NFR-CU-06` and to the `RG-05`-gated rows of `docs/nfr/traceability.md`
      sections 12 and 15. No status cell is edited here — [E15-F03-6b] updates them once the matrix is complete.

## Evidence the pull request will carry

- [ ] The Playwright report for all six scenarios and the full pass matrix markdown table.
- [ ] The trace from the unpaid-dispatch refusal, showing the refusal as the person holding the scanner sees it.
- [ ] Screenshots at three widths for the multi-garment confirmation and the doorstep delivery confirmation.
- [ ] The two-run output, including the reversed order.
- [ ] Any page-object-driven change to `clients/pwa/src/**`, line by line with the element that needed it.
- [ ] `git diff --stat` against [E15-F03-5]'s branch if both are open, proving the no-shared-file claim.
- [ ] The pull-request template with items 1, 3, 6 and 9 ticked; item 7 answered with the screenshots; items 2, 4
      and 5 deleted with the one-line reason "no endpoint, no migration, no contract change".

## Depends on

#[E15-F03-3] — `tests/e2e/scenarios/`, the journey fixture, the dataset helper, the tag-driven reporter and the CI
job. Without it there is nothing for these four specs to plug into.

#52 — the Playwright configuration and page objects (through [E15-F03-3]).

#[E15-F03-2] — the fixture manifest the specs read their identifiers from.

#32 to #37 — multi-garment orders with dependencies, revisions, the workflow, labels, scanning and custody.
#38 to #40 — stock consumption and material shortage. #42, #43 — invoices, advances, payments and the dispatch
gate. #48 — the delivery queue, the partial-delivery policy and the doorstep confirmation. #49 — feedback. All
merged, or the scenario asserting their artefact cannot pass.

Parallel-safe with [E15-F03-5] (exception paths) and with [E15-F03-9] (the first-run checklist): no shared file
except the appended journey table in `tests/e2e/scenarios/README.md`, which [E15-F03-5] also appends to — whichever
merges second rebases that one table.

Refs #61
