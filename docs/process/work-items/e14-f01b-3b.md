Sub-issue of #56. The W5 half of this parent owes an "automated security regression suite" over eleven abuse-case
families (plan Section 8), split across seven sibling sub-issues. **This sub-issue takes the two families whose subject
is a record that must not be rewritten and a record that must not be over-read: `ABF-06` stock manipulation and
`ABF-08` export leakage.** They are paired because they share the one property that sets them apart from `ABF-05`:
**neither surface exists yet.** The immutable stock ledger is **#39** `[E08-F02]` and the stocktake, variance-approval
and valuation surface is **#40** `[E08-F03]`; the governed reporting exports are **#186**, on the Reporting
foundations of **#183**. So this sub-issue is written to be read again at W5 entry and to derive its coverage from what
has actually been published, rather than from a route list that would be guesswork today.

**Correcting a dependency error it would be easy to repeat**: the open Inventory sub-issues **#193** (schema, supplier
and location masters), **#195** (item, unit-of-measure and conversion), **#196** (reorder rules) and **#198**
(customer-owned material custody) are all children of **#38** `[E08-F01]`, which builds the **masters**. None of them
builds a movement, a balance, a reservation or a stocktake. Naming #193 and #195 as this suite's dependencies — which
is the obvious mistake, because they are the Inventory issues that happen to be open — would put a session in front of
a schema with no ledger in it. The ledger is #39 and the stocktake is #40, and those are the dependencies that matter.

The gap is checkable today: `ls tests/Tailor360.IntegrationTests/Security` returns five files, none about stock or
exports; `find src/Modules/Inventory -name "*.cs" | wc -l` returns **7**, all scaffolding, with no `DbContext` and no
migration, so the `inventory` schema does not exist; and `find src/Modules/Reporting -name "*.cs" | wc -l` returns
**7** on the same terms.

## What is already built (do not re-build)

- **`tests/Tailor360.IntegrationTests/Security/AbuseCases/` and its three shared files**, from the first suite
  sub-issue of this parent: `AbuseCaseTraits.cs` (all eleven family trait constants), `AbuseCaseProbes.cs` (the five
  probe principals `OwnBranchHolder`, `OtherBranchHolder`, `NoPermissionHolder`, `StaleStrongAuthHolder`,
  `UnenrolledFactorHolder`) and `AbuseCaseCoverage.cs` (the enumeration harness, with the `ABF-06` and `ABF-08`
  predicate-file contract documented in its docstring). **Add two test classes and two predicate files; edit nothing
  shared. Do not fork
  the machinery, do not rename a probe, and do not touch
  `tests/Tailor360.IntegrationTests/Authorization/MatrixFixtures.cs`** — leaving it alone is what makes the four
  suites parallel-safe.
- **The one export surface that is live today**: the customer personal-data export,
  `POST /api/v1/customers/{customerId}/export` and `GET /api/v1/customers/{customerId}/exports/{exportId}`
  (`src/Modules/Customers/Tailor360.Modules.Customers.Api/Customers/CustomerEndpoints.cs:124` and `:177`), backed by
  `customers.customer_exports` and `Customers.Application/Customers/CustomerExportHandler.cs`. Its existing tests are
  `tests/Tailor360.IntegrationTests/Customers/CustomerExportEndpointTests.cs` and `CustomerFieldMaskTests.cs`, with
  `CustomerHarness.cs` as the fixture. **`ABF-08` extends these into leakage sequences; it does not re-test the
  mask.** Whatever else has shipped by W5, this surface is testable from day one, so the session always has a
  non-empty `ABF-08` suite to write even if #186 has slipped.
- `docs/security/field-visibility.md` (#24) and `src/Platform/Tailor360.Platform.Security/FieldVisibility/` — response
  view × field × class × permission, with two of five views served today (`customers.record`,
  `customers.search_card`). This is the mechanism `ABF-08` must prove an export cannot widen. Compare an export's
  field set **against this mechanism's own answer for the same caller**, in the same test, rather than against a
  hard-coded list.
- `src/Platform/Tailor360.Platform.Security/Permissions/` — the keys are seeded even though the endpoints are not:
  `InventoryPermissions.cs` declares `inventory.manage_items`, `inventory.manage_locations`,
  `inventory.manage_suppliers`, `inventory.manage_reorder_rules`, `inventory.record_movement`, `inventory.stocktake`,
  `inventory.approve_variance`, `inventory.approve_negative_stock`, `inventory.view_reports`,
  `inventory.view_valuation`; `ReportingPermissions.cs` declares `reports.read` and `reports.export`;
  `PlatformPermissions.cs` declares `audit.export`. **No endpoint declares any of them today** — that asymmetry is
  the first thing to re-check at W5 entry.
- `docs/architecture/invariants.md` section **4.9** (StockLedger — items, entries, balances and reservations) and
  section 6 — the invariants every `ABF-06` attempt is judged against, above all that **a correction is a new
  compensating entry, never an update and never a delete**. Cite the invariant identifier; do not restate the rule in
  the test's own words.
- `docs/adr/0011-reporting-read-models.md` and `docs/architecture/module-ownership.md` section 5.9 — the projection
  design, the rule that Reporting reads no other module's tables and no module reads `reporting.*`, the
  `t360_reporting` database role, and open decision **AOD-03** (no contract view exists yet, recorded at
  `module-ownership.md:789`). ARCH-011 is the architecture guard.
- `tests/Tailor360.IntegrationTests/Authorization/DenialAuditTests.cs` — the denial-audit assertions (`NFR-AU-04`).
  Reuse for every refusal here.
- `tests/Tailor360.IntegrationTests/Platform/` — `IdempotentCommandTests.cs`, `ConcurrencyContractTests.cs`,
  `AuditTrailTests.cs`. Replay, concurrency and the audit chain are proved there; this suite composes them.
- `tests/Tailor360.IntegrationTests/WebApplicationFixture.cs` and `DatabaseAvailability.cs` — the host fixture, and the
  rule that the integration tier uses `TAILOR360_TEST_DATABASE_URL` when set, otherwise a container, otherwise skips
  with a visible reason — and `CI=true` turns that skip into a **failure** (CLAUDE.md section 5).
- `tests/Tailor360.ContractTests/PublishedEndpointInventory.cs` and `EndpointInventoryTests.cs` — the source of truth
  for what must be covered, read at implementation time.
- `docs/security/abuse-cases.md` (from the W2 half of this parent) — `ABF-06` and `ABF-08` with their control
  patterns. **The specification**, and this sub-issue edits exactly two of its rows.
- `docs/security/threat-models/inventory.md` (from the operations model sub-issue of the W2 half) and
  `reports-exports.md` (from the financial one) — their section 7 abuse cases and section 8 controls, each naming the
  test that must exist. Those `AB-nn` and `CTL-nn` identifiers are what each test cites.
- `docs/nfr/data-classification.md` sections 5.9 (stock), 5.19 (reporting projections and exports) and section 10 (the
  read that must itself be audited), and `docs/nfr/capacity-and-performance.md` for any published page-size or export
  row. **Cite a row; never invent a limit.**

## What is missing

- Nothing asserts ledger immutability **as an attack**: that a correction must be a new compensating entry rather than
  an update or a delete, and that nothing fails today if that changes.
- Nothing asserts that an export cannot return more than the caller may see on screen, that it cannot cross a branch,
  or that a scheduled export cannot be pointed at a recipient the caller chose.
- Nothing asserts that a free-text field cannot become a spreadsheet formula on the way out.
- The **act** of exporting is a sensitive read, and nothing proves it is audited with actor, scope and reason
  (CLAUDE.md security rule 5; `docs/nfr/data-classification.md` section 10).

## Scope

**Read this first, at W5 entry, before writing a line.** Run
`grep -rc "Map\(Get\|Post\|Put\|Patch\|Delete\)" src/Modules/Inventory/ src/Modules/Reporting/` and list the published
routes under the `Inventory` and `Reporting` tags of `docs/api/openapi.v1.json`. Three outcomes, and each has a
defined response:

1. **Both surfaces published.** Cover them, as the bullets below describe.
2. **One published.** Cover that one fully, register only its predicate, and record the other family's row in
   `docs/security/abuse-cases.md` as "scheduled — #NN", naming the unbuilt issue (#39 and #40 for stock, #183 and #186
   for reporting). Do not write a test against a route that was never built, and do not write a test that passes
   vacuously.
3. **Neither published.** `ABF-08` still has the **live customer export** to cover, which is enough for a session and
   is the half that matters most for disclosure. `ABF-06` is then deferred: raise an issue blocking on #39 and #40,
   record `ABF-06` as "scheduled" in the catalogue, and say so in the pull request. **This sub-issue may close with
   `ABF-06` deferred and `ABF-08` complete**, and that is an honest outcome rather than a failure — the alternative is
   a suite of skipped tests nobody reads.

- [ ] Add up to two test classes to `tests/Tailor360.IntegrationTests/Security/AbuseCases/`: `StockManipulationTests`
      (`ABF-06`) and `ExportLeakageTests` (`ABF-08`). Every test carries `[Trait("Category", "Integration")]` —
      without it the test does not run in that tier at all — plus the family trait from `AbuseCaseTraits`.
- [ ] Add a predicate file per family actually covered — `Abf06Coverage.cs`, `Abf08Coverage.cs` — following the contract
      in `AbuseCaseCoverage.cs`'s docstring; **no existing file is edited**. The selections are: `ABF-06` selects every published route that records a movement, adjusts a balance, holds or releases a
      reservation, or submits or amends a stocktake; `ABF-08` selects every published route that produces or retrieves
      an export artefact.
- [ ] `ABF-06`, against the Inventory surface **as published at implementation time**: a movement cannot be backdated
      past a closed period; a balance cannot be driven negative without the approval `inventory.approve_negative_stock`
      demands; a stocktake cannot be amended after submission except through the sanctioned variance path; a variance
      cannot be approved by the person who recorded it; a **ledger entry cannot be updated or deleted through any
      route**, and a correction is a new compensating entry (`docs/architecture/invariants.md` section 4.9); a
      reservation cannot be released twice; and customer-owned material cannot be consumed into another customer's job
      (#198's surface).
- [ ] `ABF-06`, the immutability proof specifically: attempt the mutation through **every** published route that could
      plausibly reach a ledger entry, not only the obvious one, and list the routes tried per target in the pull
      request. "The obvious route refuses it" is not a defence.
- [ ] `ABF-08`, against every export surface published at implementation time — the customer personal-data export
      (always), the Reporting exports of #186, and the audit export: an export never returns a field the caller may
      not read on screen, proved by **comparing the export's field set against `field-visibility.md`'s own answer for
      the same caller in the same test**; an export is scoped to the caller's branch reach; an export artefact cannot
      be fetched by another caller who guesses its identifier, and a **foreign artefact and a non-existent one are
      answered identically**; and the **act** of exporting is audited with actor, scope and reason (`NFR-AU-01`,
      `docs/nfr/data-classification.md` section 10).
- [ ] `ABF-08`, the recipient rule: a scheduled or delivered export cannot be pointed at a recipient the caller chose.
      If no scheduled export is published yet, record the requirement against #186 and say so.
- [ ] `ABF-08`, formula injection: a free-text field that begins `=`, `+`, `-`, `@`, a tab or a carriage return cannot
      become an executable formula in a spreadsheet export. The corpus is a small readable set of payload strings in
      the test file. If the escaping belongs in a pure helper, that helper's property cases go in the **unit** tier
      and the integration test asserts the end-to-end result.
- [ ] Every refusal assertion checks the **audit row** as well as the response, reusing `DenialAuditTests.cs`
      (`NFR-AU-04`: denials audited, coalesced per actor, endpoint and minute, never sampled away).
- [ ] Any production defect found is fixed **in this pull request only if it is small and confined to one module**;
      anything larger becomes a new issue linked to the epic, with the failing test added as **skipped** and the issue
      number in the skip reason (CLAUDE.md section 6 scope discipline).
- [ ] Update `docs/security/abuse-cases.md`: the "defeated today by" column for `ABF-06` and `ABF-08`, **those two
      rows only**, telling the truth about anything deferred. Update the section 8 rows of `inventory.md` and
      `reports-exports.md` whose control said "no test yet" and now has one.
- [ ] Update `docs/nfr/traceability.md` rows `NFR-PR-01` and the reporting freshness and export rows so their
      verification columns name this suite. **Do not edit `NFR-SE-02`** (the first suite sibling's), **`NFR-AU-04`**
      (the second's) or **`NFR-SE-07` and `NFR-AU-01`** (the `ABF-05` sibling's) — one row, one owner.
- [ ] Versioned configuration (DOR-11): none added. Where a control **is** configuration — a stocktake variance
      threshold, an export row cap, a reorder point — the test **pins it explicitly in the fixture** and says so,
      citing **OD-24** for the variance analogue and **OD-05** for valuation, so no assertion depends on an undecided
      default it has not set.

## Out of scope

`ABF-01` to `ABF-04` and the no-client-driven-state rule, and `ABF-05` invoice and payment tampering — three sibling
sub-issues, the first of which **must merge before this one** because it owns the shared machinery. `ABF-07`, `ABF-09`,
`ABF-10` and `ABF-11` — two further siblings. The enforcing-policy hardening, the ASVS audit and the penetration test —
three more. **Building** the stock ledger (#39), the stocktake and valuation surface (#40), the Inventory masters (#38
with #193, #195, #196, #198) or Reporting (#44 with #183 to #186). The valuation **method**, which is **OD-05** and not
a security question. The media streaming half of authorised delivery, which belongs to the `ABF-07` sibling. No new
test tier and no new test project. No change to `matrix.yaml`'s expectations or to `MatrixFixtures.cs`.

## Acceptance criteria

- [ ] `./scripts/dev test integration` runs the test classes this sub-issue actually wrote, and each can be run alone
      by its family trait. Every test carries `[Trait("Category", "Integration")]`.
- [ ] **The published surface is established before anything else, and written down.** The pull request opens with the
      route list for `Inventory` and `Reporting` taken from `docs/api/openapi.v1.json`, and states which of the three
      outcomes in **Scope** applies. A reviewer can see what existed when the suite was written.
- [ ] For every published route the registered predicates select, a case exists, and `AbuseCaseCoverage` **fails when a
      new such route has none**, listing them by name.
- [ ] **`ABF-08` is non-empty in every outcome.** The customer personal-data export is live today, so a pull request
      that covers no export surface at all has not done the work.
- [ ] **Negative case shape**: every refusal is asserted on the exact response — status, RFC 9457 problem type, and
      the absence of any detail that distinguishes a forbidden artefact from a missing one — and on the audit row it
      produced. A test that asserts only "not 200" does not count; one that accepts a 500 does not count at all.
- [ ] **Negative case, ledger immutability**: the update and the delete are attempted through every plausible published
      route, and the pull request lists them. Where #39 has not shipped, this criterion is recorded as deferred with
      the blocking issue, not quietly dropped.
- [ ] **Negative case, disclosure**: an export's field set is compared against `field-visibility.md`'s answer for the
      same caller **in the same test**, not against a hard-coded list — so the test fails if either side changes
      without the other.
- [ ] **Exception case**: each sanctioned way out is tested as *working*, not only as refused — a **compensating**
      ledger entry corrects a balance; a variance approved by a **second** person succeeds; a negative balance approved
      under `inventory.approve_negative_stock` with a reason succeeds and is audited; a caller **with** the branch
      reach and the field permission exports successfully and the export is audited; a projection rebuild does not
      present partial data as current. A suite that only proves refusals would pass against a system that refuses
      everything.
- [ ] Anything deferred is visible: a `docs/security/abuse-cases.md` row reading "scheduled — #NN", an open issue
      number, or a skip reason carrying one. **Nothing is silently removed.**
- [ ] The suite is **stable**: it passes twice in a row in different orders (CLAUDE.md section 5), and the pull request
      reports the integration tier's duration before and after against the 15-minute pull-request pipeline budget
      (plan Section 8).
- [ ] `dotnet format --verify-no-changes` is clean and the other tiers still pass.

## Data and classification

The suite adds no field, table, integration event, log field or export column. It adds **test data and export
artefacts**, and that is where DOR-06 applies.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Fixture customers, stock items, movements and balances | Synthetic — treated as the class of the field they stand in for (Personal, **Financial** for valuation) | Give the manipulation attempts something to manipulate | Per test run; reset between classes by the existing fixture | Whoever can read the test run | **Synthetic only. Never a real customer name, telephone number, measurement or quantity from a real record** (CLAUDE.md section 5). Reuse `CustomerHarness` and whatever Inventory harness #39 ships |
| Export artefacts the tests produce (`customers.customer_exports`, Reporting exports, audit exports) | **Personal** and **Financial** together — the highest-leverage artefact in the system | Prove an export cannot widen or cross a branch | Per run; deleted with the database reset | As above | The test asserts on the artefact's **field set, scope and audit row**, never by printing its contents into the test output or the step summary. This rule is the reason the comparison is made programmatically rather than by eye |
| Fixture credentials and session material | Credentials and secrets — synthetic | Authenticate the probe callers | Per run | As above | The integration-test account password is already allowlisted in `.gitleaks.toml`; **add no new allowlist entry** |
| The formula-injection corpus | Internal — synthetic hostile input | Prove a free-text field cannot become a formula | Per run | As above | A small readable set of payload strings in the test file: no secret, nothing that identifies a person, and nothing that executes outside a spreadsheet |
| The backdated and negative values `ABF-06` sends | Synthetic | Prove the ledger refuses them | Per run | As above | Obviously hostile sentinels — a date before the epoch of the fixture, a quantity larger than any balance — never a plausible real figure |

## Permissions and roles

No permission key is added, removed or changed, and **no authorisation fixture file is edited** — the probe principals
come from `AbuseCaseProbes.cs`. Where this suite needs a surface-specific principal (a clerk without
`inventory.approve_variance`, the clerk who recorded the count attempting to approve it, a caller holding
`reports.export` but reaching for another branch, a caller holding `customers.read` but not `customers.read_contact`)
it composes one from the existing probes **inside its own test class**, so `MatrixFixtures.cs` stays untouched.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `inventory.record_movement`, `inventory.stocktake`, `inventory.approve_variance`, `inventory.approve_negative_stock`, `inventory.view_valuation` | branch | per the matrix | per the matrix | `approve_variance` and `approve_negative_stock` carry a reason | per the matrix | **Exist as catalogue entries** in `InventoryPermissions.cs`. Whether an endpoint declares them is the first thing to re-check at W5 entry |
| `reports.read`, `reports.export` | branch | per the matrix | per the matrix | `export` carries a reason | per the matrix | **Exist** in `ReportingPermissions.cs`; no endpoint declared either as of the date this issue was written |
| `customers.export`, `customers.read`, `customers.read_contact` | branch | per the matrix | per the matrix | `export` carries a reason | per the matrix | **Exist and are declared by live endpoints** — the `ABF-08` half that is testable from day one |
| `audit.export` | organisation | per the matrix | per the matrix | yes | per the matrix | **Exists** in `PlatformPermissions.cs`; the most sensitive export of all |

`matrix.yaml`'s expectations are **not** edited: a change there would mean an endpoint's own behaviour changed, which
is out of scope. **OD-13** is open, so the suite must not depend on a grant that is still only proposed — where it
needs one it assigns it **explicitly** in the fixture.

## Modules, contracts and events

The suite lives in `tests/Tailor360.IntegrationTests` and reaches the application only over HTTP through
`WebApplicationFixture`, so it crosses no module boundary in code. Any production fix is confined to **one** module,
through that module's own projects (ARCH-003, ARCH-004, and **ARCH-011** for anything touching Reporting, which may
reference only other modules' `Contracts` projects); a fix needing a second module becomes a new issue. ARCH-012
holds: only a test project references `Tailor360.Web`. The suite must **not** read `inventory.*` or `reporting.*`
directly to set up a scene — that would be the very boundary violation `ABF-06` exists to protect, and it would make
the test pass against a broken system. Scenes are built through published routes only, and where no route exists yet
the family is deferred rather than set up behind the API.

**DOR-05**: the flows exercised are covered by `threat-models/inventory.md` and `reports-exports.md` from the W2 half
of this parent, plus `customer-and-media.md` for the customer export. The pull request names them and the controls it
closes, per DoD 6.

## Screens and states

Deleted: this sub-issue adds no screen and changes no client code. Where a refusal must be rendered to a user — a
variance that cannot be self-approved, an export that is refused, a report that must show its freshness — the owning
screen issue holds that (#40, #184 to #186); this suite asserts the HTTP behaviour only.

## Migration and rollback

No table, no column, no migration; `dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` reports no
outstanding change and that output is the evidence. If a production fix needs a schema change, that is the signal it
belongs in its own issue — a regression suite does not migrate the database. Rollback is a revert, which removes tests,
so the pull request records the tier's test count before and after, making a silent loss of coverage visible.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-05** — valuation method, rounding and the statutory retention period for GST records | **Open**; #147 shipped the rounding half under a documented default | Weighted average by default with FIFO optional. This suite treats the **valuation read** as an access-control surface and asserts no method of its own | W4 for valuation |
| **OD-24** — the cashier variance a close may carry without a reason | **Open** — raised 2026-09-12 by #161 | Zero: every variance is explained. Used here only as the analogue for the **stocktake** variance; the test pins whatever key #40 introduces and asserts no threshold | W4 |
| **OD-08** — retention periods, including export artefacts | **Open** | No period asserted; the tests prove an export is **authorised and audited**, not how long it is kept. #57 owns retention and #192 owns media holds | W1 exit gate (overdue) |
| **AOD-03** — the catalogue of published contract views and which roles may select from them (`docs/architecture/module-ownership.md:789`) | **Open** | No contract view exists; every read contract is in-process code and the `t360_reporting` grant covers `reporting.*` only. `ABF-08` asserts **that** state, and says in a comment what changes if a view is ever published | #21 and #45 |
| **OD-13** — approved role-to-permission grants | **Open**; #24 shipped the matrix under a documented default | The twelve seeded roles and their 2026-09-06 default grants; probe principals get their permissions **explicitly** | W1 exit gate (overdue) |
| **RG-OD-02** — who holds the security owner role | **Open** — needed before W2 | The Owner, with the technical reviewer, reviews this suite (`docs/nfr/traceability.md` line 67) | W2 exit gate |

## Required verification

- [ ] **The surface check, first**: the `Inventory` and `Reporting` route lists from `docs/api/openapi.v1.json` and
      the `grep -rc` counts over `src/Modules/Inventory/` and `src/Modules/Reporting/`, pasted, with the outcome
      declared. Everything else in this list depends on which outcome it is.
- [ ] **Integration**: `./scripts/dev test integration` — the new classes, run **twice in different orders**. The
      primary tier.
- [ ] **Unit**: the formula-escaping helper's property cases, if the escaping lives in a helper.
- [ ] **Contract**: `./scripts/dev test contract` — the inventory the enumeration reads is unchanged, and
      `EndpointInventoryTests` and `EndpointPolicyTests` still pass.
- [ ] **Architecture**: `./scripts/dev test architecture` — still green, in particular the module-boundary tests and
      **ARCH-011**, if a production fix touched `src/`.
- [ ] **Coverage enforcement demonstrated**: the enumeration failing on a deliberately uncovered route, then passing.
- [ ] **Pipeline budget**: the integration tier's duration before and after, against the 15-minute budget (plan
      Section 8). If the suite pushes the pipeline over, split by family across jobs rather than deleting cases.
- [ ] **DOR-13**: `docs/nfr/traceability.md` rows affected — `NFR-PR-01` and the reporting freshness and export rows
      (verification columns extended). `NFR-SE-02`, `NFR-AU-04`, `NFR-SE-07` and `NFR-AU-01` are referenced and
      **not** edited, because each belongs to a sibling suite.
- [ ] **DOR-16**: a PostgreSQL instance through `TAILOR360_TEST_DATABASE_URL`, or a reachable Docker daemon;
      `./scripts/dev doctor` reports which tiers can run, and `CI=true` makes a skip a failure, so the pull request
      must show the tier actually ran.

## Evidence the pull request will carry

- [ ] The surface check above, as the opening section of the pull-request description.
- [ ] `./scripts/dev test integration` output, **twice**, with the test count and duration both times.
- [ ] The list of new test names grouped by `ABF-nn` family, and the family-to-class mapping as it now reads in
      `docs/security/abuse-cases.md` — including any "scheduled — #NN" row.
- [ ] The enumeration failing on a deliberately uncovered route, then passing.
- [ ] The field-set comparison output for at least one export: the caller's on-screen field set, the export's field
      set, and the assertion that the second is not wider. Field **names** only, no values.
- [ ] The list of routes tried per immutable target, for the ledger immutability criterion.
- [ ] The audit rows asserted for a denied movement and for a **successful** export.
- [ ] For each production defect found: the failing output before the fix, the passing output after, and the issue
      number for anything deferred with its skip reason.
- [ ] The rendered diff of the two `docs/security/abuse-cases.md` rows, the two models' section 8 rows and the
      `docs/nfr/traceability.md` rows.
- [ ] `dotnet format --verify-no-changes` output and `migrate --dry-run` showing no outstanding migration.
- [ ] The threat-model controls closed, named per DoD 6.

## Depends on

- **The first suite sub-issue of this parent (`ABF-01`, `ABF-02` and the shared machinery) — must be merged first.**
  It supplies the `AbuseCases/` folder, `AbuseCaseTraits`, `AbuseCaseProbes`, `AbuseCaseCoverage` and the predicate-file contract.
- The abuse-case catalogue sub-issue of the W2 half — `ABF-06` and `ABF-08` and their control patterns. **The
  specification.**
- The operations threat-model sub-issue of the W2 half (`inventory.md`) and the financial one (`reports-exports.md`) —
  the `AB-nn` and `CTL-nn` identifiers each test cites. **Must be merged first** for the same reason.
- **#39 `[E08-F02]` — the immutable stock ledger — and #40 `[E08-F03]` — stocktake, variance approval and valuation
  reports.** These are the `ABF-06` dependencies. **Not #193 or #195**, which are #38's masters and contain no
  movement, balance, reservation or stocktake; see the correction in the opening paragraphs. Neither #39 nor #40 is
  decomposed into sub-issues as of the date this issue was written.
- **#183 and #186** (on parent #44) — the Reporting foundations and the governed exports. These are the `ABF-08`
  dependencies for the reporting half. The **customer export half depends on merged work only** (#26), which is why
  `ABF-08` is non-empty in every outcome.
- #24 and #26 — merged: `field-visibility.md` and the `FieldVisibility` mechanism, `CustomerHarness`,
  `CustomerFieldMaskTests`, `CustomerExportEndpointTests`, `DenialAuditTests` and the authorisation machinery this
  suite reuses read-only.
- #198 — customer-owned material custody, for the "consumed into another customer's job" case. If it has not shipped,
  record that case against it.

**Parallel safety**: after the first suite sibling has merged, this sub-issue shares **no file** with the other six
suites — its own two test classes, its own two predicate files, its own two `abuse-cases.md` rows, its
own `traceability.md` rows, and the section 8 rows of two models no sibling touches. It edits neither
`MatrixFixtures.cs` nor `matrix.yaml`. Within the wider W5 group, #58 measures the same export and report surfaces for
latency while this suite proves their refusals: agree before either changes a limit.

Refs #56
