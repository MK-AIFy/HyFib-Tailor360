Sub-issue of #34, and the second of two alteration slices. **E06-F03-4a records the request; this slice decides it,
reopens the garment and closes the alteration.** It is the slice that makes
`docs/prd/state-transitions.md` line 230 true — "Delivered | Alteration requested and accepted — **REASON** | In
production | … Request recorded from staff or from feedback through `IAlterationRequests.OpenAsync(...)` …; price and
due-date decisions taken and communicated" — and line 115 of the same document's state diagram, "Delivered -->
InProduction : post-delivery alteration accepted".

`GarmentJob` has **no `Reopen` method** today: its transitions are `Create`, `Revise`, `CheckRevision`,
`StartProduction`, `Hold`, `Resume`, `Reschedule`, `ApplyReadyGate`, `CheckReadyGate`, `CheckDelivery`,
`ConfirmDelivery` and `Cancel`, all `internal`. `orders.job-reopened.v1` is **not** in the published-events list of
[`docs/architecture/module-ownership.md`](../../architecture/module-ownership.md) section 5.5, which does list
`orders.alteration-requested.v1`, `orders.alteration-decided.v1` and `orders.alteration-completed.v1` (lines 448-449).
Both gaps are this slice's to close.

**This slice adds no migration.** E06-F03-4a created the decision columns and the
`alterations_decision_is_append_only` trigger with the table for exactly this reason.

## What is already built (do not re-build)

- **From E06-F03-4a** (its pull request must have merged): `Domain/Alterations/{Alteration,AlterationStatus}.cs`,
  `Application/Alterations/AlterationHandler.cs`, `Application/Abstractions/IAlterationStore.cs`,
  `Infrastructure/Alterations/{AlterationStore,AlterationRequests}.cs`, `Api/Payloads/Alteration{Payloads,Requests}.cs`,
  the `alterations` table with its decision columns and its append-only trigger, the `orders.alteration_request`
  permission, `orders.alteration-requested.v1`, and the two request routes. Extend those files.
- `…Orders.Domain/Jobs/GarmentJob.cs` (997 lines) — `Status`, `DeliveredAt`/`DeliveredBy`, `IsDeliverable` (line 353),
  the `internal bool HasGoneToTheCustomer` (364, read at 727 and 793), and the `internal`-transition discipline the
  file's header documents. `GarmentJobStatus` is `Confirmed = 0`, `InProduction = 1`, `OnHold = 2`, `Ready = 3`,
  `Delivered = 4`, `Closed = 5`, `Cancelled = 6`. A reopen is added here as **one more `internal` method**, never as
  a public setter.
- `…Orders.Domain/Orders/Order.cs` (2,010 lines) — the aggregate root and the only surface `Application` can reach.
  Add the public `Order.ReopenJob(...)` beside `Order.Resume` (857) and `Order.CancelJob` (1258), following their
  shape exactly: find the job, call its `internal` transition, return its `Result`.
- `…Orders.Infrastructure/Persistence/OrdersDbContext.cs:584` — the check constraint
  `ck_garment_jobs_delivered_is_consistent`, which [`docs/dev/migrations.md`](../../dev/migrations.md) line 112 records
  as having been **deliberately weakened** to `status <> 'Delivered' OR delivered_at IS NOT NULL` "because the
  converse … would have to be contracted out at N+2 when issue #34's post-delivery alteration lands". A reopened
  garment therefore keeps its `delivered_at` and returns to `InProduction` with **no migration needed**. Do not
  re-tighten it, and do not add a column.
- `src/Platform/Tailor360.Platform.Abstractions/Money/Money.cs` — the amount-and-currency value object
  `PriceSnapshot` already uses. The charge intent uses it; **no bare `decimal` is introduced**, and Orders performs no
  money arithmetic at all (`PriceSnapshot.cs:19`: the authoritative position is Billing's `IFinancialTotalsQuery`).
- `docs/prd/raci.md:153` already names the route and the permission: "15 | Alteration decision |
  `POST /orders/alterations/{id}/decision` | `orders.alteration_decide` | No | Yes". **Use that path.**
- `docs/prd/state-transitions.md:230` is the binding row, and line 391 its act-to-permission entry ("Decide an
  alteration | Yes | No | `orders.alteration_decide`"). A **rejected** alteration is recorded with its reason and
  communicated, and the original job's history is never rewritten.

## How an endpoint is declared in this repository

There is one accepted shape and it is not optional. Copy it from
`src/Modules/Billing/Tailor360.Modules.Billing.Api/Invoicing/InvoiceEndpoints.cs` (a write at lines 190-196, a read
at 139-141), which is the closest merged example:

```
    .RequirePermission(<PermissionKey>, BranchScope.CurrentBranch)   // ARCH-007
    .ScopedToResource(<ResourceKinds.Thing>, "<routeValueName>")     // ARCH-023, when the route names a row
    .RequireRateLimiting(RateLimitPolicyNames.Write)                 // ARCH-017, exactly one
    .Audited(<Handler>.<Something>Action, reasonRequired: true)      // ARCH-008, every write
    .RequireIdempotency()                                            // where a client may retry
    .RequireIfMatch()                                                // where the row carries an ETag
    .WithRequestTimeout(RequestTimeoutPolicies.Command)               // every endpoint in the repository has one
```

- Verbs: `Platform.Security/Endpoints/EndpointAuthorisationExtensions.cs` (`RequirePermission(key, scope)`,
  `Audited(action, reasonRequired)`, `AllowAnonymousWithJustification`);
  `ResourceScopeEndpointExtensions.cs` (`ScopedToResource(resourceKind, routeValueName, scope)`, and the
  deliberately noisy exemption verb for a route whose identifier names nothing branch-owned);
  `RequestSafetyEndpointExtensions.cs` (`RequireIdempotency`, `RequireIfMatch`);
  `StepUpEndpointExtensions.cs` (`RequireStepUp`).
- **Rate-limit policies**, all of them, from `Platform.Security/Endpoints/RateLimitPolicyNames.cs`:
  `DefaultUser` = `default-user` (ordinary authenticated read), `Write` = `write` (authenticated state change),
  `ScanBurst` = `scan-burst`, `ExportHeavy` = `export-heavy`, plus `AuthenticationAnonymous`,
  `MultiFactorChallenge`, `RecoveryAnonymous`, `DefaultIp`. There is no other policy, and inventing one is a
  change to the limiter configuration in the web host.
- **Request timeouts**: `Platform.Security/Endpoints/RequestTimeoutPolicies.cs` — `Read` = `read`,
  `Command` = `command`, `Report` = `report`, `Export` = `export`. **Every** endpoint in `src/Modules/` carries one
  today (grep `WithRequestTimeout` — there is no exception), so an endpoint without one is the odd one out.
- **Branch scopes** from `Platform.Abstractions/Multitenancy/BranchScope.cs`: `CurrentBranch` (the default),
  `AssignedBranches`, `Organisation`, and the member that demands no branch reach at all.
- **Resource kinds**: each module keeps its own constants class — `BillingResourceKinds`,
  `MeasurementResourceKinds`, `DesignSelectionResourceKinds` — in its `Application` project, with an
  `IResourceScopeResolver` per kind in its `Infrastructure` project, registered with `TryAddScoped`.
- **The matrix row** goes in `docs/security/permission-matrix.md` section 5, *inside* the
  `<!-- matrix:endpoints -->` … `<!-- /matrix:endpoints -->` markers (lines 311-512 today), with the columns
  `| Method | Route | Declaration | Permission | Branch scope | Resource | Audited as | What it is for |`.
  `AuthorisationMatrixTests` fails and names any published route with no row, and any row with no route.
## Adding an integration event, and the six tests that will fail you

`tests/Tailor360.ContractTests/IntegrationEventTests.cs` enforces the whole chain. Read it before writing the
first record.

- `EveryEventIsDocumentedByASchemaAndAnExample` — both `docs/integration/events/<wire-name>.schema.json` and
  `<wire-name>.example.json` must exist. For size: `orders.job-held.v1.schema.json` is 72 lines and its example
  12, so budget about 84 lines per event for the JSON alone.
- `EverySchemaDescribesExactlyWhatTheEventPutsOnTheWire` — the schema's `properties` must be **exactly** the set
  the record serialises, `required` must be non-empty and must name nothing the record lacks, and
  `additionalProperties` must be present and `true` (`docs/integration/events/README.md` section 2: a closed
  schema would make every additive change breaking).
- `EveryExampleMatchesItsSchemaAndNamesItsOwnEvent` — the example carries the same property set and its
  `eventType` is the wire name.
- `EveryEventVersionsItsNameAndItsPayloadTheSameWay` — `SchemaVersion` agrees with the `.vN` in the wire name.
- `EveryEventIsDeclaredInItsOwnModulesPublishedSurface` — the record lives in `Orders.Contracts` and the wire name
  appears in the **Publishes — integration events** list of `docs/architecture/module-ownership.md` section 5.5.
  If it does not, amend that list in the same pull request; section 9 of that document requires it.
- `NoPayloadCarriesAFieldNamedLikePersonalData` — `IntegrationEventInventory.PersonalFieldWords` rejects a
  serialised property whose whole name, **or any camel-cased word inside it**, is one of `name`, `firstname`,
  `lastname`, `fullname`, `displayname`, `nativename`, `phone`, `telephone`, `mobile`, `msisdn`, `email`,
  `address`, `postcode`, `pincode` and the rest of that list. So no `checklistName`, no `defectName`, no
  `criterionName`, no `inspectorName`, no `roleName` — codes, keys, identifiers, statuses, amounts and timestamps
  only. The register `IntegrationEventInventory.PersonalFieldExemptions` exists but using it needs the approval
  `docs/nfr/data-classification.md` section 5.3 describes, which this issue does not have.

Two shapes to copy rather than invent: the payloads are **flat** — no `payload` wrapper — as
`orders.job-held.v1.schema.json` shows (`eventId`, `occurredAt`, `aggregateId`, `organisationId`, `branchId`,
`orderId`, `garmentJobNumber`, `reasonCode`, `eventType`, `schemaVersion`); and the `$comment` those schemas carry
states that a property added within `v1` is optional forever. Add the event's row to the table in
`docs/integration/events/README.md` as well — one row per event, with links to both files.

## A documented disagreement this slice must not resolve silently

`docs/prd/state-transitions.md` line 230 and line 134 both name the actor as "**Branch Manager or Tailor Master**
(`orders.alteration_decide`)". `docs/security/permission-matrix.md` line 220 grants `orders.alteration_decide` to
`branch_manager` **alone**, `docs/prd/raci.md` row 15 makes the Branch Manager accountable, and
`SystemRoles.cs:198` seeds it to `branch_manager` only — `tailor_master`'s block (lines 305-309) does not hold it.

The permission matrix is authoritative for grants (`CLAUDE.md` section 4 and the matrix's own section 9), so the
matrix wins: the decision route is the Branch Manager's, the `matrix.yaml` fixture asserts `tailor_master` is
**refused**, and no grant is added. **Record the disagreement in the pull request** and register it under **OD-13**
in [`docs/prd/assumptions-and-open-decisions.md`](../../prd/assumptions-and-open-decisions.md) — the decision that
approves the grants — rather than widening the grant to make two documents agree. Do not edit
`state-transitions.md` to match without the owner's answer; it is a product decision, not a typo.

## Scope

- [ ] `Domain/Alterations/AlterationCharge.cs` — the decision's intent: chargeable with a `Money` amount, or
      complimentary. It is an **intent**, never a posted document: "Orders never posts a financial document"
      (`docs/prd/exceptions.md` section 4.3 and EX-08), and the charge reaches Billing as event data for #42 and #43.
- [ ] `DecideAlterationCommand` and `CompleteAlterationCommand` on `AlterationHandler`, writing the decision columns
      E06-F03-4a created: the charge intent, the promised due date, the decision reason, the communication status, the
      outcome (`Accepted` or `Rejected`) and the actor and timestamp. Audit actions `orders.alteration_decide` and
      `orders.alteration_complete`.
- [ ] `GarmentJob.Reopen(...)` as an `internal` transition plus the public `Order.ReopenJob(...)`: a `Delivered` or
      `Closed` garment returns to `InProduction` at a named phase of its **pinned** workflow version; it keeps
      `delivered_at`, keeps every earlier QC result, rework, assignment and phase timing; and it does **not** touch
      `is_ready_for_delivery` by hand — the gate is the only writer (INV-JOB-07), and E06-F03-5 recomputes it. Audit
      action `orders.job_reopened`.
- [ ] Three event contracts: `Orders.Contracts/Events/AlterationEvents.cs` with `orders.alteration-decided.v1` and
      `orders.alteration-completed.v1` (both already in `module-ownership.md` section 5.5 lines 448-449), and
      `GarmentJobReopened.cs` with `orders.job-reopened.v1` (**not** in that list). Each with its JSON Schema and
      example. The decided event carries the charge intent as `chargeAmount` and `chargeCurrency` plus the promised
      due date; **none of the three carries a property whose name contains `name`, a decision reason, or anything
      the customer wrote** — see the events block above.
- [ ] Add `orders.job-reopened.v1` to the **Publishes — integration events** list of
      `docs/architecture/module-ownership.md` section 5.5; section 9 of that document requires the amendment in the
      same pull request, and the contract test `EveryEventIsDeclaredInItsOwnModulesPublishedSurface` fails without it.
- [ ] Two routes, declared exactly as the block above sets out, both `orders.alteration_decide` with
      `BranchScope.CurrentBranch`, `.ScopedToResource(<alteration kind>, "alterationId")`,
      `RateLimitPolicyNames.Write`, `.Audited(..., reasonRequired: true)` — matrix line 220 has Reason = **yes** —
      `.RequireIdempotency()`, `.RequireIfMatch()` and `.WithRequestTimeout(Command)`:
      `POST /api/v1/orders/alterations/{alterationId}/decision` (the path `raci.md:153` names) ·
      `POST /api/v1/orders/alterations/{alterationId}/completion`.
      **An alteration is a branch-owned row that these routes name, so ARCH-023 needs a resource kind and an
      `IResourceScopeResolver` for it.** Neither #201 nor E06-F03-4a creates one: add
      `Application/Alterations/` kind constant and `Infrastructure/Alterations/AlterationScopeResolver.cs` here,
      modelled on `Billing.Infrastructure/Invoicing/InvoiceScopeResolver.cs`, and register it with
      `services.AddScoped<IResourceScopeResolver, AlterationScopeResolver>()` beside
      `DesignSelectionDraftScopeResolver`'s registration shape. Add the kind to whichever `OrdersResourceKinds` class
      #201 created rather than starting a second one.
- [ ] Extend `Api/Payloads/Alteration{Payloads,Requests}.cs` with the decision and completion records; no `Domain`
      type is returned (ARCH-013).
- [ ] Rows for both routes in the section 5 endpoints block of `docs/security/permission-matrix.md` and the
      `identifier-editing` rows for `{alterationId}` in
      `tests/Tailor360.IntegrationTests/Authorization/matrix.yaml`.

## Out of scope

**No `orders.reopen` permission and no reopen route.** Reopening is reachable only as an effect of an accepted
alteration, under `orders.alteration_decide`, which is **SQ-03**'s proposed default; reversing a *cancellation*
requires a new order, as SQ-03 states. Creating a **separate** alteration garment job with its own `J-…` number:
`new_garment_job_id` stays null and the reading stays additive. The service-recovery case and anything the customer
wrote — #49, #213. The customer communication itself — #47; this slice records `communication_status` only, and the
pull request says so as a known limitation. The ready-gate recomputation a reopen causes — E06-F03-5. The
pending-alterations dashboard read — E06-F03-6. Screens — E06-F03-8b. **No migration**: if a decision column turns out
to be missing from E06-F03-4a's table, add it as a follow-up expand-only migration named in this pull request with its
own register row, rather than editing a merged migration (`src/Modules/CLAUDE.md` section 6).

## Acceptance criteria

- [ ] A Branch Manager accepts a `Requested` alteration on a **delivered** garment with a charge decision, a new
      promised date and a reason; the original garment returns to `InProduction` at a named phase of its pinned
      workflow version; its `delivered_at`, its earlier QC results, its reworks and its phase timings are all still
      readable; `orders.alteration-decided.v1` carries the charge intent and the promised date; and
      `orders.job-reopened.v1` is published — both in the same transaction as the state change.
- [ ] Completing the alteration moves it to `Completed`, records the actor and timestamp, and publishes
      `orders.alteration-completed.v1`; the garment's own status is left to the gate and to #33's phase commands.
- [ ] Both routes declare `orders.alteration_decide`, a resource scope on `{alterationId}`, exactly one rate-limit
      policy, a request timeout and the audit filter **with a reason** (ARCH-007, ARCH-008, ARCH-017, ARCH-023); the
      endpoint-inventory contract test shows it.
- [ ] **Negative**: a caller without `orders.alteration_decide` receives `403` `security.forbidden` on the decision
      route even when they may open a request under `orders.alteration_request`; **`tailor_master` is refused**, per
      the disagreement section above; a caller whose branch does not reach the order receives the same answer as for
      an order that does not exist; a **second** decision on a decided alteration is refused by the handler **and**
      `alterations_decision_is_append_only` refuses an `UPDATE` of the decision columns from the application role, so
      the answer is a problem detail and not a database error; a decision with no reason is refused by validation; a
      replayed `Idempotency-Key` returns the first outcome; a stale `If-Match` is refused `412`.
- [ ] **Negative, reopen**: `Reopen` refuses a `Confirmed`, `InProduction`, `OnHold`, `Ready` or **`Cancelled`**
      garment with `orders.job-status-transition-not-allowed`, and accepts only `Delivered` or `Closed` — SQ-03's
      proposed default, with the refusal on a cancelled garment naming why.
- [ ] **Exception**: a **rejected** alteration is recorded with its reason and its communication status and is never
      left silent (`state-transitions.md:230`); the garment is not reopened and no `orders.job-reopened.v1` is
      published; `orders.alteration-decided.v1` is, with the rejected outcome.
- [ ] **Exception**: completing an alteration that has not been decided is refused; completing a **rejected** one is
      refused; and a reopen that would happen twice for one alteration is refused, so a retried decision cannot
      reopen a garment that has since been delivered again.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| `orders.alterations` decision columns (outcome, promised due date, decided_at/by, completed_at/by) | Internal — `docs/nfr/data-classification.md` section 5.7 names "alterations" | What was decided, by whom and on what terms | Business record, no routine deletion (section 5.7) | `orders.read` holders within branch scope | Codes, identifiers, statuses, dates and timestamps may appear; actors are staff personal data under section 5.15 |
| `orders.alterations.charge_intent` and its `Money` amount | **Confidential** — section 5.7 classes the price snapshot Confidential and its log row says the price snapshot may **not** appear in logs | Let Billing raise the charge from its own records | As the row | `orders.read` holders whose response view includes money (`Platform.Security/FieldVisibility/OrdersResponseViews.cs`) | **Never in a log line.** It travels on the decided event to Billing only |
| `orders.alterations.decision_reason` (free text) | Internal, free text | Why it was accepted or rejected | As the row | As above | Never in a log, an event payload or an export. The words are recorded in the audit row, which is where a reason belongs |
| `orders.alterations.communication_status` | Internal | Prove the customer was told, per `state-transitions.md:230` | As the row | As above | A low-cardinality status is safe as a telemetry attribute; the message itself is #47's and is not stored here |
| `orders.garment_jobs.delivered_at` on a reopened garment | Internal, with Personal through the job's customer | Keep the delivery fact after a reopen | With the job | As above | Timestamps only |
| Events `orders.alteration-decided.v1`, `orders.alteration-completed.v1`, `orders.job-reopened.v1` | Internal, with a **Confidential** amount on the decided event | Tell Billing (#42, #43), Notifications (#47) and Reporting (#45) | Outbox retention | Approved subscribers | Identifiers, codes, statuses, the promised date, timestamps and the intent amount only — never a reason, never a name, never the feedback text |
| Audit actions `orders.alteration_decide`, `orders.alteration_complete`, `orders.job_reopened` | Internal — evidentiary | Prove who decided, on what terms, and who reopened the garment | Per section 5.16 | `auditor`, `owner` | Audit store only, with the reason |

No new media class, no credential, no secret and no export column.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `orders.alteration_decide` | branch | no | no | **yes** | `branch_manager` **only** | **Exists** — `OrdersPermissions.cs:72`, matrix line 220, "Price, due date and who bears the cost — the Branch Manager is accountable (`raci.md` row 15)". Granted `SystemRoles.cs:198`. It authorises the decision, the reopen it causes and the completion, and — per **SQ-03** — nothing else. See the disagreement section: `state-transitions.md` says "or Tailor Master" and the matrix does not; the matrix wins and the fixture asserts the refusal |
| `orders.read` | branch | no | no | no | eleven roles | **Exists** — matrix line 231; unchanged, used by E06-F03-4a's list route |

**No permission key is added and no grant is changed by this slice** — `orders.alteration_request` was added by
E06-F03-4a. **OD-13** is open, so the grant above is the seeded default.

## Modules, contracts and events

Orders only. No other module's `Contracts` is consumed, no other module's tables are read (ARCH-005, MO-2), no other
module's `Infrastructure` is referenced (ARCH-003). Billing receives the charge as **event data** only, which is what
keeps ARCH-010 intact — no Billing project is referenced and no financial document is posted. Publishes three
versioned outbox events, one of which needs the `module-ownership.md` amendment above. Threat-model coverage is
**DOR-05's pre-#56a alternative**, not a written model: [`docs/security/README.md`](../../security/README.md) section 3
records that only the authentication model exists (#23) and that `abuse-cases.md` is **not written**, with the rest
scheduled under #56a — the order-workflow model specifically in the batch-1 child `E14-F01a-4`. So the abuse case is
**named here and carried into that backlog entry**: the abuse case this slice answers is a
free alteration granted to mask a refund, which the Branch-Manager-only decision permission, the mandatory reason and
the audit action together record, and which the charge travelling as an intent — decided in Billing from Billing's own
records — keeps out of Orders entirely.

## Screens and states

Deleted: no screen. The decision dialogue is E06-F03-8b.

## Migration and rollback

**No migration.** The `alterations` table, its decision columns and `alterations_decision_is_append_only` were created
by E06-F03-4a's `Alterations`; `garment_jobs` needs no change because
`ck_garment_jobs_delivered_is_consistent` was already weakened for exactly this case
(`docs/dev/migrations.md` line 112). The pull request proves the absence with `migrate --dry-run` reporting nothing
outstanding and `has-pending-model-changes` clean. A garment reopened under the new release and read by the previous
one shows `in_production` with a `delivered_at`, which the weakened constraint already permits — which is what makes
rollback a plain redeploy of the previous tag.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **SQ-03** what `job-reopened` may reopen — a closed job only, or also a cancelled one | **Open**, proposed — `docs/prd/state-transitions.md` line 524 | Reopen applies to a **delivered or closed** garment only, and only through an accepted alteration; reversing a cancellation requires a new order. A cancelled garment cannot be reopened and the refusal names why | With #34, per the question's own row |
| **SQ-01** what moves a delivered garment to `closed` | **Open**, proposed — line 522 | Nothing in this slice closes a garment. An alteration completion returns it to the gate and `delivered` stays the last automatic state | With #34 |
| **OD-13** permission matrix | **Open** — `assumptions-and-open-decisions.md` line 66 | `orders.alteration_decide` keeps its seeded `branch_manager`-only grant; the `state-transitions.md` "or Tailor Master" disagreement is registered under this decision rather than resolved in code | W1 exit |
| **OD-04 / XQ-03** whether an advance is refundable, and who bears an alteration's cost | **Open** — `docs/prd/exceptions.md` line 199 | The decision records an **intent** and an amount; no financial document is posted and no refund is initiated from Orders | #42 and #43 |
| **XQ-05** whether a customer must be told when a garment fails QC, or only when the promised date moves | **Open** — `docs/prd/exceptions.md` line 201 | `communication_status` is recorded and nothing is sent: the message is #47's. The default in force is that the decision is *marked* as needing communication and the status stays at its initial value until #47 lands | With #47 |

## Required verification

- [ ] unit: `GarmentJob.Reopen` refuses `Confirmed`, `InProduction`, `OnHold`, `Ready` and `Cancelled` and accepts
      `Delivered` and `Closed`; it keeps `delivered_at`; it does **not** touch `is_ready_for_delivery` (INV-JOB-07),
      asserted on the property rather than by inspection; `Alteration` refuses a second decision, refuses a completion
      before a decision and refuses a completion of a rejected alteration; `AlterationCharge` refuses a negative
      amount and a chargeable intent with no amount.
- [ ] integration: both routes against real PostgreSQL; the authorisation matrix for each including the
      `tailor_master` refusal; the idempotency replay; the stale `If-Match`; the decision trigger refusing an
      `UPDATE` of the decision columns from the application role.
- [ ] integration: the full **EX-03 journey** — a delivered garment, an alteration requested through E06-F03-4a's
      route, accepted here, the garment reopened, and the alteration completed — with the original QC results,
      reworks and phase timings still readable afterwards, and `delivered_at` unchanged.
- [ ] integration: a **rejected** alteration records its reason and communication status, publishes
      `orders.alteration-decided.v1` and publishes **no** `orders.job-reopened.v1`.
- [ ] contract: OpenAPI lint and diff; the endpoint inventory; `IntegrationEventTests` green for the three new
      schema/example pairs, for `orders.job-reopened.v1` now appearing in the module's published surface, and for
      `NoPayloadCarriesAFieldNamedLikePersonalData`.
- [ ] migration: `migrate --dry-run` nothing outstanding and `has-pending-model-changes` clean — the evidence that
      this slice adds no schema.
- [ ] documentation: `./scripts/dev docs` green after the `module-ownership.md` amendment.
- [ ] NFR rows touched: the **S3** command-latency row of [`docs/nfr/slo.md`](../../nfr/slo.md) line 131 for both
      writes, with measured figures.
- [ ] environment: `./scripts/dev doctor`; `CI=true` makes a skipped integration tier a failure (DOR-16).

## Evidence the pull request will carry

- [ ] `./scripts/dev test unit|architecture|contract|integration` output, run twice.
- [ ] `migrate --dry-run` showing nothing outstanding and `has-pending-model-changes` clean.
- [ ] The OpenAPI difference report for the two operations and `pnpm --dir clients/pwa generate:api:check`.
- [ ] The `permission-matrix.md` section 5 rows, the `matrix.yaml` diff including the `tailor_master` refusal, and the
      `AuthorisationMatrixTests` run.
- [ ] An HTTP transcript of the request → decision → reopen → completion journey and of a rejection, synthetic data
      only, beside the audit rows each wrote with its reason.
- [ ] A read of the reopened garment showing `delivered_at` intact, `is_ready_for_delivery` false and its earlier QC
      results and reworks still listed.
- [ ] The three new event schemas and examples, the `module-ownership.md` diff adding `orders.job-reopened.v1`, the
      note in the pull request recording the `state-transitions.md` actor disagreement under OD-13, and
      `dotnet format --verify-no-changes` clean.

## Depends on

**E06-F03-4a** — the `alterations` table with its decision columns and trigger, the `Alteration` aggregate,
`AlterationHandler`, `IAlterationStore`, the payload files and the request route whose output this slice decides. It
must have merged.

#`201` — **[E06-F01-2]**, which is **open**: the Orders `Application` and `Api` layers and the `OrdersResourceKinds`
class this slice adds an alteration kind to.

#`33` — **[E06-F02] the production workflow**, which is **open** and is being decomposed in a parallel batch, so the
child that delivers `workflow_version_phases` has no number yet. The reopen names a phase of the pinned workflow
version, so this slice needs that table to validate the phase code. **Until it merges, do not guess a phase
vocabulary**: wait, or land the reopen validating the phase code for length and shape only, with a follow-up issue
opened for the referential check and the limitation stated in the pull request. Take the first unless a reviewer says
otherwise. This is the same constraint E06-F03-2b carries, for the same reason.

**Not** depended on: #49, #213, #47, #42, #43 — all consume what this slice publishes and none is needed by it.

Same-file sequencing: `OrdersDbContext.cs` (mapping only), `OrdersEndpoints.cs`,
`OrdersModuleServiceCollectionExtensions.cs`, `AlterationHandler.cs`, the `Alteration*` payload files,
`docs/api/openapi.v1.json`, `clients/pwa/src/api/schema.d.ts`, `docs/security/permission-matrix.md`, `matrix.yaml`,
`docs/architecture/module-ownership.md` and `docs/integration/events/README.md` — shared with E06-F03-4a, -2a, -2b,
-3, -6 and #33's children. One at a time.

Refs #34
