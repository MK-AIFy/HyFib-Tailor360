Sub-issue of #58. #58's fifth implementation step is "perform capacity/load/stress/soak tests" and its fourth acceptance
criterion is "capacity targets and error budgets pass the approved load profile". `docs/nfr/capacity-and-performance.md`
section 6 already specifies the suite in full — seven scenarios **S1** to **S7**, each with its shape and its pass
criteria — and states where it lives: "Implemented with k6 under `tests/load/`, run in full for every release and as a
smoke subset per the release gate in plan Section 5.3." **`tests/load/` does not exist**: `ls tests/` returns only the
four .NET test projects and `fixtures`. `docs/nfr/slo.md` section 11 fixes the cadence — "k6 smoke per pull request,
full load and mixed-load test per release (#58)" — and release gate **RG-07** (`docs/process/release-gates.md` lines 194
and 198) and evidence item **RE-08** (`docs/process/release-evidence.md` line 103) both expect a k6 summary that nothing
currently produces.

## What is already built (do not re-build)

- `docs/nfr/capacity-and-performance.md` section 6 — the seven scenarios with their shapes and pass criteria. **This is
  the specification; implement it, do not redesign it.** Section 2.4 line 104 fixes the design point: "20 requests per
  second sustained, 50 requests per second burst for 60 seconds". Section 3.3 is the budget table every threshold reads
  from, and section 4 is the resource envelope.
- `docs/nfr/slo.md` section 5 — the latency and error objectives (read p95 < 400 ms, command p95 < 800 ms, MFA challenge
  p95 < 2 s, error rate ≤ 0.5% and commands ≤ 0.2%), and section 5.1's rule that report, media, export and
  sequence-allocation routes are excluded from S2 and S3 and carry their own budgets. A threshold in k6 must match the
  row it claims.
- `docs/nfr/traceability.md` rows **NFR-LT-01** to **NFR-LT-04** and **NFR-CP-01**, each of which names a k6 scenario as
  its test and is currently "proof scheduled".
- `scripts/dev` — the `test` verb's tier dispatch. The `e2e)` arm at line 429 is the pattern to copy: it checks for the
  directory, warns by name with the owning issue when it is absent and **returns 0**, and the unknown-tier failure at
  line 442 lists the accepted tiers. `doctor` at lines 805-820 shows how a tier reports RUNS or SKIPPED with a reason;
  add a k6 row the same way.
- **`scripts/dev.ps1` is the matched twin and must change identically.** `CLAUDE.md` section 2: the two "are equivalent:
  same verbs, same output, so a transcript from either can be pasted into a pull request." The PowerShell counterparts
  are the `'e2e'` arm at `scripts/dev.ps1:444-455`, the unknown-tier failure at line 457, the help text at line 146 and
  the doctor rows at lines 854-859. Every line added to one is added to the other, and the pull request shows **both**
  transcripts. Forgetting the twin is the most common way this kind of change fails review.
- `docs/dev/commands.md` lines 53-59 — the tier table that must gain the new verb, and line 65's note about the
  integration tier's database requirement, which the load tier needs an equivalent of.
- `.github/workflows/ci.yml` — the existing jobs `build-test-dotnet`, `build-test-pwa`, `api-contract`,
  `migration-check`, `security`, `infrastructure-scan`, `sbom`, `documentation`, `codeql`, `format-dotnet`. The smoke
  job goes beside them; the full run does not.
- The endpoints a k6 script can actually drive today. **Establish this list with `grep` before writing a scenario; the
  obvious assumption is wrong and it is the single most expensive mistake available in this issue.** Drivable over HTTP:
  Identity sign-in and the MFA challenge; Customers under `/api/v1/customers` (`CustomersEndpoints.GroupPrefix`);
  Catalog reads and authoring; and the administrative and read surface of Billing under `/api/v1/billing`
  (`BillingEndpoints.GroupPrefix`) — GST registrations, price lists and their versions, tax configuration, payment
  modes, pricing preview, cashier sessions open and close, and every `GET`.
- **What is NOT drivable over HTTP, and why it kills scenario S5 as written.** An invoice needs a **confirmed order**,
  and there is no way to confirm one over HTTP: `src/Modules/Orders/Tailor360.Modules.Orders.Api/OrdersEndpoints.cs`
  maps **no endpoint at all** — it is a `MapGroup(...).WithTags(...)` and nothing else (30 lines; read it). The
  integration tests get an order only by publishing `GarmentJobCreated` and `OrderConfirmed` **in process** through
  `IOrdersEventPublisher` (`tests/Tailor360.IntegrationTests/Billing/InvoiceScenes.cs:94-122`), which a k6 script cannot
  do. With no order there is no invoice draft, therefore no invoice to post and no payment to record against one. **So
  S5 ("20 concurrent invoice posts plus 20 payment recordings") cannot be implemented in this issue**, and neither can
  the payment leg of any other scenario. It becomes a registry row blocked on **#199**/**#201** (order intake and
  confirmation) or on a .NET billing fixture — which is **#61**'s `E15-F03-2` ("Consolidate the deterministic fixture
  library"), the issue `SeedSyntheticCommand.cs` itself names at line 76 as the owner of "customers, catalogue, orders
  and billing fixtures". Do not try to route around this with a test-only endpoint: a load test that drives a route
  nobody ships measures nothing, and a privileged bootstrap route would be a new, unreviewed authorisation surface.
- `src/Platform/Tailor360.Platform.Security/Endpoints/RateLimitPolicyNames.cs` and
  `src/Hosts/Tailor360.Web/Configuration/RateLimitPolicies.cs` — the policies and their numbers. S2's pass criterion is
  "no rate-limit rejections for authenticated staff traffic", so the load generator must key its clients the way
  `TestClientAddressStartupFilter` does in the integration fixture, or every virtual user collapses into one partition
  and the run measures the rate limiter instead of the application.
- `src/Platform/Tailor360.Platform.Abstractions/Diagnostics/SignalNames.cs` and the dashboards of #58 child 4 — the
  server-side view the run is compared against, so a k6 summary and a dashboard cannot disagree.

## What is missing

- `tests/load/` entirely: no harness, no scenario, no threshold module, no authentication helper, no seeded fixture.
- Any way to run it: `./scripts/dev test load` is not a tier, and `doctor` does not report whether k6 is installed.
- Any data to run it against. **`seed-synthetic` seeds Identity only** —
  `src/Tools/Tailor360.Cli/Commands/SeedSyntheticCommand.cs` resolves `IdentityDbContext` and nothing else, and says so
  at line 75: "Customers, catalogue, orders and billing fixtures are added by their own issues". A load run therefore
  has to create its own customers, price list and invoices through the API, which is part of this issue's work.

## Scope

- [ ] **`tests/load/` founded**: a k6 project with `lib/` holding the shared pieces — a sign-in and session helper that
      obtains a real session and anti-forgery token, a per-virtual-user client-address header so rate-limit partitions
      do not collapse, a `thresholds.js` module whose every entry cites the `docs/nfr/` row it comes from, and a
      `setup()` bootstrap that creates the synthetic branch, staff accounts, customers, price list and invoices the
      scenarios need through the API.
- [ ] **`tests/load/scenarios/auth-challenge.js` — the one scenario in this issue.** It is the authentication scenario
      `docs/nfr/traceability.md` **NFR-LT-04** names, against the **p95 < 2 s** MFA-challenge objective (**S5** of
      `docs/nfr/slo.md` section 4, target in section 5). It is chosen deliberately: sign-in and the MFA challenge are
      fully drivable over HTTP with no fixture beyond a staff account, so it is the scenario that proves the harness,
      the thresholds module, the per-virtual-user client keying and the CI job all work — before any scenario that needs
      business data. Mind the rate-limit policies it will meet: `auth-anon` and `mfa-challenge`
      (`RateLimitPolicyNames.cs:23,29`) are *deliberately* tight, so the scenario must stay inside them and report
      rejections as a **failure of the scenario's shape**, not of the application.
- [ ] **S1-partial and S7 are child 8b's**, and **S5 is neither's** — see the note above on why an invoice cannot be
      created over HTTP. The registry row for each is written here with its blocking issue; the scenarios are not.
- [ ] **`tests/load/registry.js` and `tests/load/README.md`** — one row per scenario **S1 to S7** plus the
      authentication scenario: its identifier, its file or `unimplemented`, the issue that unblocks it, and the
      `docs/nfr/` rows it proves. A test asserts that every scenario identifier in
      `docs/nfr/capacity-and-performance.md` section 6 appears in the registry, so the gap cannot be forgotten. The rows
      this issue writes: **auth-challenge** implemented; **S1** and **S7** owned by **#58 child 8b**; **S5** blocked on
      **#199**/**#201** or **#61**'s `E15-F03-2` fixture library; **S2** blocked on S1; **S3** on **#51**, **#190**,
      **#194**, **#197** and **#37**; **S4** on **#183**, **#184**, **#44** and **#46**; **S6** on **#187**, **#188**
      and **#189**.
- [ ] **`./scripts/dev test load`** added to the tier dispatch, following the `e2e)` arm's shape: warn by name when k6
      is not installed or `tests/load` is absent rather than failing obscurely; and a **k6 row in `doctor`**. Update the
      tier table in `docs/dev/commands.md` and the tier list in the unknown-tier failure message.
- [ ] **A smoke job in `.github/workflows/ci.yml`** running the smoke subset at the stated load against the compose
      stack, with the k6 summary published as a workflow artefact — this is the "k6 smoke per pull request" of
      `docs/nfr/slo.md` section 11. The full load, burst and soak runs stay manual or release-triggered, per the same
      row.
- [ ] **Mark section 6 of `docs/nfr/capacity-and-performance.md`** with which scenarios are implemented and which are
      blocked, naming the blocking issue per row. **No number in that document changes** — this is an annotation column,
      not a re-specification, and a reviewer must be able to see that from the diff.
- [ ] **Create `docs/nfr/performance-report.md`** with the first measured run beside the proposed budgets, and a
      standing table for later runs. The file is named by `docs/process/release-gates.md` line 198 ("the
      before-and-after profile in `docs/nfr/performance-report.md`") and by **RE-08** of
      `docs/process/release-evidence.md` line 103, and it **does not exist**, so RG-07 has nowhere to write its evidence
      until this issue adds it. Every figure in it carries the machine it was measured on; a percentile with no machine
      beside it is not a measurement.

## Out of scope

**S2** (seasonal burst), **S3** (offline queue replay: 20 devices × 200 queued scans), **S4** (month-end reporting) and
**S6** (media storm) cannot be written: scans need **#190**, **#194**, **#197** and **#37**; reporting needs **#183**,
**#184** and **#44**; media needs **#187**, **#188** and **#189**; the offline queue needs **#51**. Each is a registry
row naming its issue. Also out of scope: Lighthouse CI budgets and client-side web vitals — **#52**; the mixed-load test
against reporting isolation — **#46**, which section 6's S4 row serves; the dashboards the run is compared against —
#58 child 4; the fault-injection retry-safety suite — #58's previous sibling, which is correctness under failure rather
than behaviour under load.

## Acceptance criteria

- [ ] `./scripts/dev test load` runs the smoke subset from a clean checkout against the compose stack and exits non-zero
      when a threshold is breached; `./scripts/dev doctor` reports the load tier as RUNS or SKIPPED with a reason, the
      way it already does for the Playwright tier.
- [ ] **The authentication scenario passes** and holds the **p95 < 2 s** MFA-challenge objective of `docs/nfr/slo.md`
      section 5, at a concurrency inside the `auth-anon` and `mfa-challenge` policies, with a zero 5xx rate. **Every
      threshold in `thresholds.js` cites the `docs/nfr/` row it came from**, so a reviewer can check the number rather
      than trust it.
- [ ] The registry and its test list all seven of section 6's scenarios plus the authentication scenario, each with a
      file or a blocking issue; **S5's row names why it is blocked and does not pretend to be merely unwritten**.
- [ ] **Negative:** an authenticated read run produces **no** rate-limit rejections (the S2 pass criterion), proving the
      per-virtual-user client keying works; a run with the keying removed **does** produce rejections, and that contrast
      is recorded. Without it a green run may mean nothing more than that the rate limiter answered quickly. This is
      also the criterion that forces the session to read how
      `tests/Tailor360.IntegrationTests/WebApplicationFixture.cs`'s `TestClientAddressStartupFilter` keys its clients,
      rather than guessing at a header name.
- [ ] **Negative:** a deliberately tightened threshold fails the run and the CI job, so the gate is proved to be able to
      fail. A performance gate that has never failed is not a gate.
- [ ] **Exception:** the registry test fails when a scenario identifier in `docs/nfr/capacity-and-performance.md`
      section 6 has neither a file nor an owning issue, so an unimplemented scenario is visible rather than absent — the
      same distinction the signal catalogue draws for a signal with no producer.
- [ ] **Exception:** the suite refuses to run against a non-development environment. The load scripts honour the same
      rule `src/Tools/Tailor360.Cli/Commands/EnvironmentGuard.cs` applies to `reset`: a destructive or load-generating
      run against Production is refused by the tool, not by a convention.

## Environment this can be verified in

**Two tools this issue depends on are absent from the planning container, and both must be checked before any scenario
is written.** `command -v k6` finds nothing, and there is no Docker daemon, so `docker compose` cannot bring the
application up either. `./scripts/dev doctor` does not report k6 at all today — **adding that row is the first task in
this issue, not the last**, because it is what tells the next session whether the tier can run before it spends the
session finding out. Sequence the work accordingly:

1. Add the `load` tier arm and the k6 `doctor` row to **both** `scripts/dev` and `scripts/dev.ps1`, following the `e2e`
   pattern: absent k6 or absent `tests/load` **warns by name with this issue's number and exits 0**, exactly as `e2e`
   does for #52. Run `./scripts/dev doctor` and paste the output.
2. Only then write the harness, the thresholds module and the one scenario.
3. The measured run needs k6 **and** a running application **and** PostgreSQL. If the session has none of them, it ships
   the harness, the scripts, the CI job and the registry with the `doctor` output proving the tier reports SKIPPED for a
   named reason, and the **measured numbers are produced on a machine that has them** — recorded, with the machine
   named, and never invented or described.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| The branch, staff accounts, customers, price list, invoices and payments the bootstrap creates | **Synthetic — no class, because none of it is real.** `CLAUDE.md` section 4 rule 10 and section 5: synthetic data only, and no production data is ever copied into a fixture | Give the scenarios something to act on | Dropped with the environment; the run is against development or a dedicated load environment, never Production | Whoever runs the suite | Synthetic names, telephone numbers and amounts only. No real customer name, telephone number or measurement appears in any script or fixture |
| Load credentials (the synthetic staff password and TOTP seed) | Credentials and secrets | Sign the virtual users in | Not committed: supplied by environment variable at run time, or created by the bootstrap and discarded | The runner | **Never committed, never printed in a k6 summary, never in a CI log.** The summary reports counts and percentiles only |
| The k6 summary (percentiles, counts, threshold outcomes) | Internal | RG-07 and RE-08 evidence | Stored with the release record, per `docs/process/release-evidence.md` line 103 | Technical reviewer, Owner | Aggregate only. **No request body, no document number, no amount** — a posted invoice's number and value are Financial (`docs/nfr/data-classification.md` sections 5.10 and 8), so the summary reports "n invoices posted", never which ones |
| The performance report, **created by this issue** at `docs/nfr/performance-report.md` — named by `docs/process/release-gates.md` line 198 and `ls docs/nfr/performance-report.md` fails today | Internal | Before-and-after comparison against the budgets | In the repository | Anyone with repository access | Measured percentiles against targets; no customer data and no environment hostname |

Nothing new is stored in the application. DOR-06 is answered in full; no class is deferred.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| Whatever the driven endpoints already declare — `billing.post_invoice` (`BillingPermissions.cs:27`), `payments.record` (`:51`), `payments.session` (`:69`), `customers.read` and the catalogue reads | As declared | As declared | As declared | As declared | The synthetic staff accounts are created with the seeded roles of `src/Modules/Identity/Tailor360.Modules.Identity.Application/Access/SystemRoles.cs` | **Exists and is used unchanged.** Note the vocabulary: payment and cashier-session keys are `payments.*`, not `billing.*`, although Billing declares them. Read each key off its endpoint rather than inferring it |

No permission is added and no grant changes, so `docs/security/permission-matrix.md` and
`tests/Tailor360.IntegrationTests/Authorization/matrix.yaml` are untouched. The load suite must **not** be given a
bypass or a privileged test-only route: it signs in as the roles that hold the permissions, because a load test that
skips authorisation measures a system nobody runs. **OD-13** is open and unaffected.

## Modules, contracts and events

No `src/` project changes. Files: `tests/load/**`, `scripts/dev`, `docs/dev/commands.md`, `.github/workflows/ci.yml`,
`docs/nfr/capacity-and-performance.md` section 6, and `docs/nfr/performance-report.md`. The suite is a black-box HTTP
client: it reads no table, crosses no module boundary and publishes no event, which is also why its sequence-gap
assertion is a read-back rather than a query. Threat-model coverage: denial of service and resource exhaustion —
`docs/nfr/capacity-and-performance.md` section 4's envelope is the control, and the credential-abuse and
denial-of-service suite planned under **#56** (E14-F01b-4c) covers the adversarial half; this suite covers the
legitimate-peak half, and the two must cite each other.

## Screens and states

Deleted with a reason: no client surface. Client-side performance — Lighthouse budgets, web vitals, bundle weight and
the practices listed in `docs/nfr/capacity-and-performance.md` section 7 — belongs to **#52**.

## Migration and rollback

No database change; the `migrate --dry-run` output proves it. The bootstrap creates rows through the API in a
development or load environment and is refused against Production by the environment guard. Rollback is reverting the
commit and removing the CI job; no production behaviour changes. The one caution for the release: a failing smoke job
blocks a pull request, so the job is introduced with its thresholds at the published targets and **not** tightened in
the same change.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-02** Hosting model and budget | **Open**, needed before W0 exit | `docs/nfr/slo.md` section 5 states the latency objectives are identical for both models, so one scenario set serves both. The **environment** the full run happens on is the open part: the pull request records which machine the numbers came from, because a percentile with no machine beside it is not a measurement | W0 exit; the full run is re-baselined once the model is chosen |
| **CP-01** and assumptions **A3**/**A5** of `docs/nfr/capacity-and-performance.md` sections 2 and 9 — the volumes are proposed, not measured | Open; section 5 states the assumptions are re-derived from production telemetry after three months | Run at the section 2.4 design point (20 rps sustained, 50 rps burst), which is deliberately above the derived peak so the budgets have headroom. Invent no other figure | Re-baselined after launch, per section 5 |
| Every budget in `docs/nfr/capacity-and-performance.md` section 3.3 and every objective in `docs/nfr/slo.md` section 5 is **proposed, to be confirmed** | Open — `slo.md` section 13 is unsigned | Assert the proposed numbers as thresholds, with each threshold carrying the row it came from in a comment, so a confirmed change is a one-line edit | W5 exit |
| **OD-07** Device, browser and printer matrix | Open | Not reached: this suite is server-side. The client and device side is #52 and #36 | W0 exit |

## Required verification

- [ ] The load tier itself: the authentication scenario runs green against a running application, with the k6 summary
      attached and the machine named.
- [ ] The two negative runs: the rate-limit-keying contrast, and the deliberately tightened threshold failing the job.
- [ ] The registry test — every section 6 scenario identifier present with a file or an owning issue — run in CI.
- [ ] `./scripts/dev doctor` output showing the new load row, and `./scripts/dev test load` from a clean checkout.
- [ ] `./scripts/dev docs` passes; every link in `tests/load/README.md` resolves.
- [ ] NFR rows advanced (DOR-13): **NFR-LT-04** moves from "proof scheduled" to measured. **NFR-LT-01**, **NFR-LT-02**
      and **NFR-CP-01** are **child 8b's** and their status cells are **not** changed here; **NFR-LT-03** (scan round
      trip) and **NFR-CP-04** (reporting isolation) stay scheduled against #36/#52 and #44/#46.

## Evidence the pull request will carry

- [ ] The k6 summary for the authentication scenario, with the percentiles beside the `docs/nfr/` rows they are measured
      against.
- [ ] The machine the numbers came from: CPU, memory and whether PostgreSQL was on the same host — without it the
      percentiles are not evidence.
- [ ] The two negative runs, shown as what failed and why.
- [ ] The CI smoke job's run, with its published artefact.
- [ ] `./scripts/dev doctor` and `./scripts/dev test load` transcripts **from both scripts** — bash and PowerShell — as
      `CLAUDE.md` section 2 requires, including the SKIPPED-with-a-reason path when k6 is absent.
- [ ] A rendered diff of `docs/nfr/capacity-and-performance.md` section 6, `docs/dev/commands.md` and
      `docs/nfr/performance-report.md`.

## Depends on

Nothing unmerged. The capacity and SLO documents, every endpoint the scenarios drive, `scripts/dev`, the CI workflow and
the rate-limit policies are all merged. #58's other children are **not** dependencies — if child 4's dashboards have
merged, compare the run against them and say so; if not, the k6 summary stands alone. **Single-session files this issue
holds:** `.github/workflows/ci.yml`, `scripts/dev` **and `scripts/dev.ps1`** (both also extended by **#58 child 3**, and
`scripts` is also edited by batch 1's **E14-F01a-7** and **E14-F01a-8** — only one of those four may be in flight),
`docs/dev/commands.md` and `docs/nfr/capacity-and-performance.md`. `.github/workflows/ci.yml` is additionally edited by
batch 1's **E14-F01a-8** and **E15-F03-3**. **#58 child 8b** depends on everything this issue founds, so it must merge
second. **#183**, **#184**, **#187**, **#188**,
**#189**, **#190**, **#194**, **#197**, **#199**, **#201**, **#37**, **#44**, **#46**, **#51** and **#52** are open and
are each a registry row, not a dependency.

Refs #58
