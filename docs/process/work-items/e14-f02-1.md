Sub-issue of #57. The parent's first acceptance criterion — "data inventory maps every class to owner, access policy,
retention and backup behaviour" — is already answered *as prose*: `docs/nfr/data-classification.md` section 5 carries
a twenty-row inventory index and one ten-attribute table per category, and section 8 of
`docs/nfr/security-operations-targets.md` carries the proposed retention period per class. What does not exist is the
**executable** half. `grep -rn "retention_policies" src/` returns nothing; `docs/architecture/module-ownership.md`
line 702 lists `platform.retention_policies` as an owned table and the platform schema gap recorded in this working
copy confirms it is absent (built platform tables are `idempotency_keys`, `sequences`, `audit_events`,
`audit_chain_head`, `feature_flags`, `job_leases`, `worker_heartbeats`, `data_protection_keys`, `outbox_messages`,
`inbox_messages`). This sub-issue builds the register and the resolver that every later child of #57 reads, and
nothing else.

## What is already built (do not re-build)

Reuse all of this rather than re-creating it; each line was verified in this working copy.

- `docs/nfr/data-classification.md` section 5.1 — the twenty-row inventory index (rows 5.2…5.20 plus the section 6
  credentials row). **This is the authoritative list of data classes.** Do not invent a class and do not rename one.
- `docs/nfr/security-operations-targets.md` section 8 — the proposed retention period per class, each already
  labelled with the owner decision that settles it (**OD-08**, **OD-05**, **OD-14**). This is the only legitimate
  source for a period in the seed data.
- `src/Platform/Tailor360.Platform.Persistence/Contexts/PlatformDbContext.cs` — `SchemaName = "platform"`, derives
  from `Conventions/ModuleDbContext.cs`. Add the `DbSet` here; the migration chain is
  `src/Platform/Tailor360.Platform.Persistence/Migrations/`.
- `src/Platform/Tailor360.Platform.Persistence/Entities/FeatureFlag.cs` — the closest existing shape for an
  organisation- and branch-scoped configuration row. Follow it for naming and for `snake_case` by convention.
- The Customers migration `20260908003058_AddConsentAndCommunicationPreferences.cs`, under
  `src/Modules/Customers/Tailor360.Modules.Customers.Infrastructure/Migrations/`
  — **a Customers migration, not a platform one** — and the `consent_records_no_update` trigger it creates: the
  repository's established way to make a table append-only in the database and not only in the domain type. Copy that
  shape; do not invent a new one. `docs/dev/migrations.md` line 109 is its register row and shows exactly the evidence
  a trigger migration is expected to carry ("**All four were proved to bite against the live database**").
- `src/Platform/Tailor360.Platform.Abstractions/Time/IClock.cs` and `Identifiers/IIdGenerator.cs` — ARCH-014 and
  ARCH-015 make `DateTimeOffset.UtcNow` and `Guid.NewGuid()` illegal anywhere else in `src/`.
- `src/Tools/Tailor360.Cli/Commands/InitReferenceDataCommand.cs` — the seeder-per-module pattern
  (`IIdentityReferenceDataSeeder`, `IConsentReferenceDataSeeder`, `ICatalogReferenceDataSeeder`,
  `IMeasurementTemplateReferenceDataSeeder`, `IBillingReferenceDataSeeder`, resolved at lines 47–52). Add one more
  seeder to that list; do not add a new CLI verb.
- `src/Modules/Identity/Tailor360.Modules.Identity.Api/Administration/FeatureFlagAdminEndpoints.cs` and
  `AuditAdminEndpoints.cs` — the precedent for a **platform** administration surface living in `Identity.Api`.
  Platform has no `Api` project, so this is where `/api/v1/admin/...` platform endpoints are declared and where
  `IdentityEndpoints.cs` composes them from `IdentityRoutes` constants (`AdminAudit = "/api/v1/admin/audit"`,
  `AdminFeatureFlags = "/api/v1/admin/feature-flags"`, `AdminTag = "Administration"`).
- **`Identity.Api` cannot see `PlatformDbContext`, and must not be made to.** Its project file references exactly
  `Tailor360.Modules.Identity.Application`, `Tailor360.Platform.Observability` and `Tailor360.Platform.Security` —
  **not** `Tailor360.Platform.Persistence`. The established answer is
  `src/Platform/Tailor360.Platform.Abstractions/FeatureFlags/IFeatureFlagAdministration.cs`, implemented by
  `src/Platform/Tailor360.Platform.Persistence/FeatureFlags/FeatureFlagAdministration.cs` and injected into
  `FeatureFlagAdminEndpoints` as a handler parameter. **Follow that exactly**: an administration port in
  `Abstractions`, its implementation in `Persistence`, registered in `PlatformServiceCollectionExtensions.cs`. An
  endpoint that injects a `DbContext` will not compile, and adding the project reference would be a change to the
  host's dependency graph that this issue has no mandate for.
- `src/Modules/Identity/Tailor360.Modules.Identity.Api/Payloads/AdminPayloads.cs` — where every existing
  administration payload lives (`FeatureFlagPayload`, `AuditPagePayload`, `AuditEntryPayload`). New payloads go here
  or beside it, with a `From(...)` factory, exactly as those do.
- `src/Platform/Tailor360.Platform.Security/Endpoints/EndpointAuthorisationExtensions.cs`,
  `ResourceScopeEndpointExtensions.cs`, `StepUpEndpointExtensions.cs`, `RequestSafetyEndpointExtensions.cs`
  (`RequireIdempotency`, `RequireIfMatch`), `RateLimitPolicyNames.cs` (`DefaultUser = "default-user"`,
  `Write = "write"`, `ExportHeavy = "export-heavy"`), `RequestTimeoutPolicies.cs` (`Read`, `Command`, `Report`,
  `Export`) and `ProblemResults.cs` (`From`, `RetryAfter`) — the endpoint declaration vocabulary.
- `tests/Tailor360.ContractTests/EndpointReviewCitationTests.cs` — the precedent for a contract test that reads a
  repository document and fails when code and document disagree. The consistency test below is the same shape.

## What is missing

- No `platform.retention_policies` table, no entity, no `DbSet`, no migration.
- No data-class vocabulary anywhere in `src/`. Nothing in code names the twenty categories of
  `docs/nfr/data-classification.md` section 5.1, so nothing can be checked against them.
- No way for a worker job or an endpoint to ask "what is the retention policy for this class, in force now". Every
  later child of #57 — the erasure executor, the sweep, the exception report — needs exactly that one question
  answered.

## Scope

- [ ] **`DataClass` catalogue** in a new `src/Platform/Tailor360.Platform.Abstractions/Privacy/DataClass.cs`: one
      `const string` key per row of the `docs/nfr/data-classification.md` section 5.1 index table, plus the
      section 6 credentials row — twenty keys, each with the document section it mirrors in its doc comment, and an
      `All` collection in declaration order. `Abstractions` only, because `Domain` projects may reference nothing
      else (ARCH-001). **The twenty keys are fixed here so that every sibling child of #57 names the same strings;
      do not rename one, and do not add a twenty-first without adding a section 5 row in the same pull request:**

      | Section | `DataClass` key | Section | `DataClass` key |
      | --- | --- | --- | --- |
      | 5.2 | `customer_identity` | 5.12 | `receipts_and_cash` |
      | 5.3 | `consent_records` | 5.13 | `notification_bodies` |
      | 5.4 | `measurements` | 5.14 | `customer_links_and_feedback` |
      | 5.5 | `customer_material_images` | 5.15 | `staff_identity` |
      | 5.6 | `custody_evidence` | 5.16 | `audit_events` |
      | 5.7 | `order_data` | 5.17 | `application_logs` |
      | 5.8 | `barcode_and_scans` | 5.18 | `telemetry` |
      | 5.9 | `inventory_and_suppliers` | 5.19 | `reporting_projections` |
      | 5.10 | `invoices_and_notes` | 5.20 | `backups` |
      | 5.11 | `payments_and_refunds` | 6 | `credentials_and_secrets` |
- [ ] **`RetentionPolicy` entity** in `src/Platform/Tailor360.Platform.Persistence/Entities/RetentionPolicy.cs`:
      `id` (UUIDv7 from `IIdGenerator`), `organisation_id`, `data_class`, `branch_id` (nullable — null means the
      whole organisation), `retention_period` (`interval`, **nullable**), `disposition`, `legal_basis`,
      `effective_from`, `reason`, `pending_decision` (nullable, holds `OD-05` / `OD-08` / `OD-14`), `recorded_at`,
      `recorded_by`. `disposition` is one of `Delete`, `Pseudonymise`, `PartitionDetach`, `BucketLifecycle`,
      `NotApplicable`, `Excluded`, stored as the member **name** so reordering the enumeration cannot re-point a
      stored row (the `allowed_channels` precedent recorded in `docs/dev/migrations.md` for
      `communication_preferences`).
- [ ] **One expand-only migration**, EF name `AddRetentionPolicies` (the repository names migrations in PascalCase
      and EF prefixes the timestamp — see `docs/dev/migrations.md`). Creates the table, a unique index on
      `(organisation_id, data_class, branch_id, effective_from)`, and a `retention_policies_no_rewrite` trigger
      refusing `UPDATE` and `DELETE` — a policy is superseded by a later `effective_from`, never edited, because it
      is the record of what the rule was when a deletion ran. Add the register row in `docs/dev/migrations.md` with
      the applied-migration evidence.
- [ ] **`IErasureParticipant` port** in
      `src/Platform/Tailor360.Platform.Abstractions/Privacy/IErasureParticipant.cs`, modelled on
      `src/Platform/Tailor360.Platform.Abstractions/Ports/ITimelineSource.cs` — the repository's one existing
      **inverted port**: declared in `Abstractions`, implemented by each module's `Infrastructure`, composed by a
      host. `ROD-02` in `docs/architecture/architecture-rules.md` line 406 settles that this shape satisfies
      ARCH-004. The port carries a `ParticipantName`, the `DataClass` keys it can act on, and one method that, given
      a subject (`subjectType`, `subjectId`, `organisationId`), a resolved `RetentionDecision` and a `dryRun` flag,
      answers an `ErasureOutcome` of erased classes with counts, retained classes with a reason, and skipped classes
      with a reason code from the fixed set `hold`, `statutory_retention`, `pending_decision`, `participant_error`.
      **It is declared here and implemented by nobody in this issue** — the Customers implementation is the third
      child, Orders' is the ninth, Media's arrives with #192, and the executor that calls them is the fourth and the
      fourth-b. It is declared here rather than with the request register so that the Customers and Orders
      participants depend on this one child and not on two, which is what lets them be written in parallel with the
      request register. A port with no implementation registers as an empty `IEnumerable<IErasureParticipant>`; a
      unit test asserts that resolving it yields an empty sequence rather than throwing.
- [ ] **`IRetentionPolicies` port** in `src/Platform/Tailor360.Platform.Abstractions/Privacy/IRetentionPolicies.cs`
      with one question — the policy in force for `(organisation, data class, branch, instant)`, resolved as the row
      with the greatest `effective_from` at or before the instant, preferring a branch row over the organisation
      row — and implemented as `Tailor360.Platform.Persistence/Privacy/RetentionPolicyStore.cs`, registered in
      `PlatformServiceCollectionExtensions.cs` beside `IAuditReader` (line 181).
- [ ] **A policy with no period is a refusal to act, not a default.** The resolver answers
      `RetentionDecision.Pending(decisionId)` when `retention_period` is null, and every caller must treat that as
      "do nothing and report it". No fallback period is written anywhere in code. This is the mechanism that keeps
      **OD-05**, **OD-08** and **DC-09** out of the source.
- [ ] **`IRetentionPolicyAdministration` port** in
      `src/Platform/Tailor360.Platform.Abstractions/Privacy/IRetentionPolicyAdministration.cs`, modelled line for
      line on `FeatureFlags/IFeatureFlagAdministration.cs`: `ListAsync` (the register, one row per class, newest
      effective row first), `HistoryAsync(dataClass)` and `RecordAsync(command)`. Implemented as
      `src/Platform/Tailor360.Platform.Persistence/Privacy/RetentionPolicyAdministration.cs` and registered beside
      `IRetentionPolicies`. **This is the type the endpoints inject** — see the note above about `Identity.Api` not
      referencing `Tailor360.Platform.Persistence`. `IRetentionPolicies` stays the narrow resolver the worker and
      the participants use; splitting them is the same reason `IFeatureFlags` and `IFeatureFlagAdministration` are
      two interfaces, and that file's own remarks say so.
- [ ] **Three endpoints** in a new
      `src/Modules/Identity/Tailor360.Modules.Identity.Api/Administration/RetentionPolicyAdminEndpoints.cs`, mapped
      from `IdentityEndpoints.cs` on a new `IdentityRoutes.AdminRetentionPolicies = "/api/v1/admin/retention-policies"`
      group with `IdentityRoutes.AdminTag`:
      `GET /api/v1/admin/retention-policies` (the register, newest effective row per class first),
      `GET /api/v1/admin/retention-policies/{dataClass}` (that class's full effective-dated history), and
      `POST /api/v1/admin/retention-policies` (record a new effective-dated policy).
- [ ] **Payload types** in `Identity.Api/Payloads/` (ARCH-013: no `Domain` or `Persistence` type is ever returned),
      following `AdminPayloads.cs`. **No `ResponseView` is registered, and none is required.**
      `src/Platform/Tailor360.Platform.Security/FieldVisibility/` holds views for Customers and Orders only, there is
      no Identity or Platform view file, and `tests/Tailor360.ContractTests/ResponseViewPayloadTests.cs` drives a
      hand-maintained `ServedViews` list of exactly three entries (`customers.record`, `customers.search_card`,
      `customers.timeline`) — it does **not** fail for an unregistered payload. Registering a view here would also
      oblige rows in the `<!-- matrix:fields -->` and `<!-- matrix:role-fields -->` blocks of
      `docs/security/field-visibility.md`, which is scope this issue does not have. The register carries no customer
      field, so field minimisation has nothing to minimise. `tests/Tailor360.ContractTests/EndpointPayloadTests.cs`
      still applies and is what ARCH-013 is checked by.
- [ ] **Two new permission keys** in `src/Platform/Tailor360.Platform.Security/Permissions/PlatformPermissions.cs`,
      granted in `src/Modules/Identity/Tailor360.Modules.Identity.Application/Access/SystemRoles.cs`, with rows
      added inside the `<!-- matrix:permissions -->` block of `docs/security/permission-matrix.md` and the route rows
      inside `<!-- matrix:endpoints -->`. See **Permissions and roles**.
- [ ] **Versioned configuration, not code (DOR-11).** `IRetentionPolicyReferenceDataSeeder` seeds exactly one row
      per `DataClass` key on `init-reference-data`, idempotent and re-runnable like the existing seeders, resolved in
      `InitReferenceDataCommand.cs` beside `IBillingReferenceDataSeeder` (line 52). `EnvironmentGuard` rules already
      in that file apply unchanged.

      **The mapping is fixed here, because it cannot be derived.** `docs/nfr/security-operations-targets.md`
      section 8 is a table of *log, telemetry and audit* classes, not of the section 5.1 inventory categories: it
      names periods for logs, traces, metrics, client telemetry, media access, authentication events, audit events,
      anchors, notification bodies, feedback text, backups and exports, and it names **no period at all** for
      customer identity, consent, measurements, images, custody evidence, order data, barcodes, inventory, invoices,
      payments, receipts, staff identity or reporting projections. A session left to "take the period from the row
      where that row states one" would have to invent the join. It does not: it seeds this table verbatim.

      | `DataClass` key | `retention_period` | `disposition` | `pending_decision` |
      | --- | --- | --- | --- |
      | `customer_identity` | `NULL` | `Pseudonymise` | `DC-09` |
      | `consent_records` | `NULL` | `Excluded` | `OD-08` |
      | `measurements` | `NULL` | `Delete` | `OD-08` |
      | `customer_material_images` | `NULL` | `Delete` | `OD-08` |
      | `custody_evidence` | `NULL` | `Delete` | `OD-08` |
      | `order_data` | `NULL` | `Pseudonymise` | `OD-08` |
      | `barcode_and_scans` | `NULL` | `Excluded` | `OD-05` |
      | `inventory_and_suppliers` | `NULL` | `NotApplicable` | `OD-08` |
      | `invoices_and_notes` | `NULL` | `Excluded` | `OD-05` |
      | `payments_and_refunds` | `NULL` | `Excluded` | `OD-05` |
      | `receipts_and_cash` | `NULL` | `Excluded` | `OD-05` |
      | `notification_bodies` | `NULL` | `Delete` | `OD-08` |
      | `customer_links_and_feedback` | `NULL` | `Delete` | `OD-08` |
      | `staff_identity` | `NULL` | `Pseudonymise` | `OD-08` |
      | `audit_events` | `NULL` | `PartitionDetach` | `OD-05` |
      | `application_logs` | `30 days` | `BucketLifecycle` | `OD-14` |
      | `telemetry` | `14 days` | `BucketLifecycle` | `OD-14` |
      | `reporting_projections` | `NULL` | `Delete` | `OD-08` |
      | `backups` | `NULL` | `BucketLifecycle` | `OD-08` |
      | `credentials_and_secrets` | `NULL` | `NotApplicable` | — |

      Two periods and only two are written, and both are cited: **30 days** for `application_logs` and **14 days**
      for `telemetry` are the searchable and trace periods that section 8 states outright, each carrying
      `pending_decision = 'OD-14'` because that table marks them as the telemetry backend's to confirm. Their
      disposition is `BucketLifecycle`, because the application deletes neither. `credentials_and_secrets` is
      `NotApplicable` with no decision id: a secret is rotated and revoked, never retained on a schedule, and
      section 6.2 is its register. **Every other row is `NULL`, and every `NULL` carries its decision id**, which is
      what makes the owner's eventual answer a configuration change rather than a code change. The `legal_basis` of
      every seeded row is the literal string `seeded pending owner decision`, and the `reason` names this issue.
- [ ] **Consistency test** in `tests/Tailor360.ContractTests/`: every row of the section 5.1 index table of
      `docs/nfr/data-classification.md` has exactly one `DataClass` key and vice versa, read from the file through
      `RepositoryFiles.cs`. No new `ARCH-…` identifier is proposed, because this adds no source-text rule — it
      supports DOR-06 the way `EndpointReviewCitationTests` supports ARCH-007. If the reviewer judges it a new
      mechanical rule it takes an `ARCH-…` row in `docs/architecture/architecture-rules.md` in this same pull
      request (CLAUDE.md section 3).
- [ ] **Documentation**: confirm the `docs/architecture/module-ownership.md` line 702 `retention_policies` row, which
      already exists and now has an implementation; add `IRetentionPolicies`, `IRetentionPolicyAdministration` and
      `IErasureParticipant` to that document's "Publishes — read contracts and ports" list for Platform; add the two
      permissions and three routes to `docs/security/permission-matrix.md` and the routes to
      `tests/Tailor360.IntegrationTests/Authorization/matrix.yaml`. **Do not touch line 704** (the
      `data_protection_keys` row and its stale `W-002` reference) — correcting that is the sixth child's, which owns
      the key-ring documentation.

## Out of scope

The data-subject request register and the approval workflow are `[E14-F02-2]`; the **implementations** of
`IErasureParticipant` are the third child (Customers), the ninth (Orders) and #192 (Media), and the executor that
calls them is the fourth-b. The
retention-hold and retention-run tables with their four administration routes are `[E14-F02-4]`, and the worker that
*executes* a policy is `[E14-F02-4b]`. Audit chain verification and anchoring are `[E14-F02-5]`, the run-time
audit-completeness harness
`[E14-F02-5b]`, secret rotation `[E14-F02-6]`, the Customers pseudonymisation `[E14-F02-3]`, the Orders snapshot
clearing `[E14-F02-9]`, and the three client children `[E14-F02-7]`, `[E14-F02-8]` and `[E14-F02-8b]`. **No period
that the documentation marks open is written into code or seed data by
this issue** — that is the point of `pending_decision`, not a gap in it. Writing the audit anchor to the
object-locked backup bucket is #60.

## Acceptance criteria

- [ ] An Owner calls `POST /api/v1/admin/retention-policies` over HTTP with a data class, a period, a disposition, a
      legal basis, an `effective_from` and a reason, and `GET /api/v1/admin/retention-policies/{dataClass}` then
      shows two rows for that class — the seeded one and the new one — with the later `effective_from` marked as the
      one in force. No direct database access in the test.
- [ ] Every route this issue adds declares a permission (ARCH-007), exactly one rate-limit policy (ARCH-017), and
      the `POST` carries `.Audited("platform.retention_policy.recorded")` with a mandatory reason (ARCH-008) and
      `.RequireStepUp()`, which ARCH-018 requires because `admin.retention_policies` is catalogued
      `RequiresStepUp: true`. Each route also declares `.TouchesNoBranchOwnedResource(NoBranchResource, Review)` and
      `.WithRequestTimeout(RequestTimeoutPolicies.Read)` or `Command`. **ARCH-023 does not compel that declaration
      here and must not be cited as though it did**: its allowed-exceptions row
      (`docs/architecture/architecture-rules.md` line 346) puts organisation-scoped routes outside the rule
      entirely, and `EndpointAuthorisationInspector.UnscopedIdentifierComplaints` yields nothing for
      `BranchScope.Organisation`. It is declared because `MapExport` in `CustomerEndpoints.cs` and every route in
      `FeatureFlagAdminEndpoints.cs` declare it, and because the matrix's `Resource` cell then reads `—` exactly as
      their rows do (`docs/security/permission-matrix.md` lines 323–326 and 497).
- [ ] `IRetentionPolicies` resolved for a class whose seeded row has `retention_period = NULL` answers `Pending`
      naming the decision id (`OD-08`), and a unit test asserts the resolver never substitutes a period of its own.
- [ ] Resolution is effective-dated: asked for an instant before the new row's `effective_from`, the resolver
      answers the older row, not the newer one.
- [ ] A branch row wins over the organisation row for that branch and leaves every other branch on the organisation
      row.
- [ ] **Negative**: a caller holding `admin.audit.read` but not `admin.retention_policies` is refused the `POST`
      with `403` and the problem-detail code the `answers` block of
      `tests/Tailor360.IntegrationTests/Authorization/matrix.yaml` already defines for a missing permission — not a
      bespoke message. A `POST` naming a data class that is not a `DataClass` key is refused `422` with a field
      error on `dataClass`.
- [ ] **Negative**: a replayed `Idempotency-Key` on the `POST` returns the first outcome and records no second row;
      the same key with a different body is refused `422 idempotency.key-reused` (plan Section 4.6).
- [ ] **Exception**: `UPDATE` and `DELETE` against `platform.retention_policies` are refused by the
      `retention_policies_no_rewrite` trigger, proved against the live database the way
      `docs/dev/migrations.md` records it for `consent_records_no_update`. Correcting a policy is a new
      effective-dated row, and the superseded row is still readable.
- [ ] `init-reference-data` on a clean database writes **exactly twenty** rows, one per `DataClass` key, and the
      command's output names the two classes that carry a period and the eighteen left `Pending` with their decision
      ids. Run twice it adds no second set of rows and changes nothing on the second run.
- [ ] **Exception**: `IEnumerable<IErasureParticipant>` resolves to an empty sequence in a host composed by this
      pull request, and a unit test asserts that rather than the registration throwing — the port is declared here
      and implemented by nobody until the third child.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| `platform.retention_policies.data_class` | Internal | Name the category a policy governs | As the table: a policy is the evidence of what the rule was, so rows are never deleted | Owner, Admin, Auditor through the register endpoints | Safe as a low-cardinality telemetry attribute and in the register payload |
| `platform.retention_policies.retention_period` | Internal | The configured lifetime executed by the sweep | As the table | As above | Safe |
| `platform.retention_policies.legal_basis`, `.reason` | Internal | Why the period is what it is; free text a member of staff typed | As the table | As above | Shown in the register; never in a log or a bulk export |
| `platform.retention_policies.pending_decision` | Internal | Name the open owner decision that blocks the class (`OD-05`, `OD-08`, `OD-14`) | As the table | As above | Safe |
| `platform.retention_policies.recorded_by` | Personal — identifier only | Who recorded the policy | As the table | As above | Identifier only, never a name |
| Audit event `platform.retention_policy.recorded` | Personal metadata, evidentiary | Prove who set a retention rule, when and why | Per `docs/nfr/data-classification.md` section 5.16 | `admin.audit.read` holders (Owner, Auditor) | Audit store only |
| `DataClass` keys | Public | The vocabulary the register and the document share | Not applicable — source code | Anyone reading the repository | Safe |

Nothing here is Personal beyond the recording actor's identifier, and nothing here is Sensitive Personal: the
register describes *rules about* data, never data.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `admin.retention_policies` | organisation | yes | yes | yes | `owner` (Owner) | **Proposed here for reviewer sign-off.** No existing key covers configuring a retention period. Declared in `PlatformPermissions.cs` beside `FeatureFlags = "admin.feature_flags"`, the closest analogue: versioned configuration with a mandatory reason and step-up. **It differs from that analogue in one way on purpose**: `FeatureFlags` is `PermissionScope.NotBranchOwned`, and this key is `PermissionScope.Organisation`, because a retention policy row carries a `branch_id` and therefore does reach a branch's data, which is exactly the distinction the `admin.feature_flags` matrix row (line 240) spends a paragraph on. Because the catalogue entry is `RequiresStepUp: true`, ARCH-018 **obliges** `.RequireStepUp()` on all three routes — omitting it on the two reads fails the matrix, so declare it on all three as `FeatureFlagAdminEndpoints` does. Adds a row to the `<!-- matrix:permissions -->` block in the same pull request |
| `admin.privacy.read` | organisation | yes | no | no | `owner`, `auditor` (Owner, Auditor) | **Proposed here for reviewer sign-off.** Reads the register and, in the later children, the request register and the exception report. Owner and Auditor only, matching the **DC-10** position that `docs/nfr/data-classification.md` section 13 records for the audit viewer |
| `admin.audit.read` | organisation | yes | no | no | `owner`, `auditor` | **Exists** — `PlatformPermissions.AuditRead`, matrix row at `docs/security/permission-matrix.md` line 238. Quoted here because a reader will ask why it is not reused: it governs the audit trail, and the retention register is configuration, not evidence |

There is no `platform.` prefix in this repository's permission vocabulary — the platform keys are `admin.*` and
`audit.export`. Both new keys follow that.

The issue extends `tests/Tailor360.IntegrationTests/Authorization/matrix.yaml` and the fixtures in
`tests/Tailor360.IntegrationTests/Authorization/MatrixFixtures.cs`, and adds the grants in
`src/Modules/Identity/Tailor360.Modules.Identity.Application/Access/SystemRoles.cs`. **OD-13** (the approved
role-to-permission grants) is open, so both grants above are proposed rather than approved.

## Modules, contracts and events

Platform only, plus the endpoint surface in `Identity.Api` that the audit and feature-flag admin endpoints already
established for platform-owned administration. The table lives in the `platform` schema and is mapped by
`PlatformDbContext` alone, so ARCH-005 holds with no exception. `IRetentionPolicies` is a **platform port** — one of
the five sanctioned crossings in CLAUDE.md section 3 — declared in `Tailor360.Platform.Abstractions` and consumed by
later children and by the worker. No integration event is published: a retention policy is configuration read on
demand, and an event would be a second copy of it that can disagree. `docs/architecture/module-ownership.md`
**AOD-02** already places this table in the platform schema as a governance record; this issue implements that
interim position and says so in the pull request.

Threat-model coverage: the register is configuration whose abuse is "a period shortened so evidence disappears",
which is why the table is append-only and the `POST` demands step-up and a reason. The deployment and runtime threat
model that owns this area is scheduled by #56's children (`docs/security/README.md` section 3.2); this issue adds no
new trust boundary.

## Screens and states

Deleted: this is a backend and configuration slice. The retention register **screen** is `[E14-F02-7]` and depends on
these three endpoints.

## Migration and rollback

One expand-only migration, EF name `AddRetentionPolicies`, in
`src/Platform/Tailor360.Platform.Persistence/Migrations/`. It only creates a table, an index and a trigger, so the
previous release tolerates it completely — nothing reads or writes the table before this pull request. Rollback is
redeploying the previous tag with the new table unused; `Down` drops the trigger, the function and the table, and
re-applying must be clean with `dotnet ef migrations has-pending-model-changes` reporting none, exactly as the
register rows in `docs/dev/migrations.md` require. The seeded rows are reference data, re-creatable by
`init-reference-data`, so losing them is not a data loss. Per `docs/dev/migrations.md`.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-05** — statutory retention of GST records | Open (`docs/nfr/data-classification.md` section 13; `docs/nfr/security-operations-targets.md` section 8.1 records it as needed "before the retention policies of #57 are configured") | The financial classes are seeded with `retention_period = NULL` and `pending_decision = 'OD-05'`, and the resolver answers `Pending`. **No number is written.** | The first retention sweep is allowed to act on a financial class |
| **OD-08** — retention periods for measurements, images, feedback text, notification bodies, logs and backups | Open | Seeded from the proposed periods of `docs/nfr/security-operations-targets.md` section 8 **only where that table states one**, each row carrying `pending_decision = 'OD-08'` so the owner's confirmation is a configuration change and not a code change | The W5 exit gate |
| **OD-13** — approved role-to-permission grants | Open | Both new keys are granted to `owner` (and `auditor` for the read) and marked proposed in the matrix | Release |
| **DC-09** — how long after the last order a customer is pseudonymised | Open, "not proposed" (`docs/nfr/data-classification.md` section 13) | The `customer_identity` class is seeded with a null period and `pending_decision = 'DC-09'` | The erasure sweep acts on `customer_identity` |
| **OD-14** — telemetry backend and its retention | Open | `application_logs` is seeded **30 days** and `telemetry` **14 days**, the only two periods section 8 states that map onto a section 5.1 category, each with `pending_decision = 'OD-14'`; their disposition is `BucketLifecycle`, because the application deletes neither. No metric or client-telemetry row exists, because section 5.18 is one category over both | #58 |
| **AOD-02** — which module owns the governance records | Interim position documented (`docs/architecture/module-ownership.md` section 8) | Platform, as that document states | This issue; the pull request records that it implements the interim position |

## Required verification

- [ ] **Unit**: effective-dating (three rows, three instants, three answers); branch row beats organisation row;
      a null period resolves to `Pending` with the decision id and never to a substituted period; an unknown data
      class is rejected; `DataClass.All` has twenty distinct keys in declaration order; an empty
      `IEnumerable<IErasureParticipant>` resolves rather than throwing.
- [ ] **Contract**: the section 5.1 index table and the `DataClass` keys agree in both directions; the three routes
      appear in the endpoint inventory with their permission, rate-limit policy and audit action; the OpenAPI
      document lints and its difference report is attached; `EndpointPayloadTests` passes for the new payloads, and
      `ResponseViewPayloadTests` is **unchanged and still green** — its three-entry list is not touched.
- [ ] **Integration**: the register journey over HTTP; the authorisation matrix rows for the three routes including
      the refusal for a caller with `admin.audit.read` only; the append-only trigger refusing `UPDATE` and `DELETE`;
      the replayed and the reused `Idempotency-Key`; `init-reference-data` idempotent on a second run.
- [ ] **Migration**: `dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` then `migrate`, re-run
      showing "already up to date", and `Down` then re-apply.
- [ ] Non-functional rows affected in `docs/nfr/traceability.md`: the privacy and retention rows that name #57; no
      SLO row changes, because this adds no hot-path read.
- [ ] Environment: `./scripts/dev doctor` output, because the integration tier needs PostgreSQL and `CI=true` turns a
      skip into a failure.

## Evidence the pull request will carry

- [ ] `./scripts/dev test unit`, `contract` and `integration` output.
- [ ] `migrate --dry-run`, `migrate`, the re-run, and the `Down`/re-apply transcript, plus the new
      `docs/dev/migrations.md` register row.
- [ ] The authorisation-matrix rows for the three routes and the OpenAPI difference report.
- [ ] `init-reference-data` output on a clean database and on a second run, with the seeded row count per class and
      the classes left `Pending` listed by decision id.
- [ ] The rendered diff of the `docs/nfr/data-classification.md`, `docs/architecture/module-ownership.md`,
      `docs/security/permission-matrix.md` and `docs/dev/migrations.md` changes.
- [ ] `dotnet format --verify-no-changes` clean.

## Depends on

#21 — the persistence conventions, `ModuleDbContext`, `UseRowVersion` (`xmin`), the migration runner and the
registry this migration joins.
#24 — `RequirePermission`, `RequireStepUp`, the resource-scope declarations and the authorisation matrix fixtures.
#25 — the `/api/v1/admin` surface in `Identity.Api` and the precedent this follows.

All three are **open as issues** while the code they describe is merged and was verified in this working copy, so
the dependency is on the merged code rather than on the issue closing — the same reading that
`docs/process/definition-of-ready.md` section 5 asks for. The parent's own declared dependencies, in
`docs/IMPLEMENTATION_PLAN.md` line 832, are **#19, #24, #26 and #31**; #19 (the non-functional documents) is open but
the documents this issue reads —
`data-classification.md`, `security-operations-targets.md`, `traceability.md` — are all present, and **#31 (Media) is
not a blocker for this child**, because the Media participant is #192's and this child declares only the port.

Files only one session at a time may touch, all needed here:
`src/Platform/Tailor360.Platform.Persistence/Contexts/PlatformDbContext.cs` and its migration chain;
`src/Modules/Identity/Tailor360.Modules.Identity.Application/Access/SystemRoles.cs`;
`docs/security/permission-matrix.md` and `tests/Tailor360.IntegrationTests/Authorization/matrix.yaml`;
`docs/api/openapi.v1.json` and `clients/pwa/src/api/schema.d.ts`;
`src/Modules/Identity/Tailor360.Modules.Identity.Api/IdentityEndpoints.cs` and `IdentityRoutes.cs`;
`src/Tools/Tailor360.Cli/Commands/InitReferenceDataCommand.cs`. Every other child of #57 that adds an endpoint needs
the same matrix and OpenAPI files, so this issue is not safe to run in parallel with them; it is first in the
sequence.

Refs #57
