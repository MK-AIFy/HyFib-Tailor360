Sub-issue of #34. Quality control has a permission, a role grant, an audit action, a matrix row, a state-transition
row, two exception-register entries (EX-04, EX-05), an accessibility checklist and a domain predicate — and no code.
`grep -rn "qc_result" src/` returns nothing. `OrdersPermissions.RecordQc` is `orders.record_qc`
(`src/Platform/Tailor360.Platform.Security/Permissions/OrdersPermissions.cs:63`), granted to `branch_manager`
(`SystemRoles.cs:195`) and `tailor_master` (`:305`), and no route demands it.
[`docs/architecture/module-ownership.md`](../../architecture/module-ownership.md) assigns `qc_results`,
`qc_result_criteria` and `rework_tasks` to the `orders` schema, and the only Orders migration,
`20260911141153_InitialOrdersSchema`, creates none of them — it creates twelve tables and those three are not among
them. `ReadyGatePredicate.QcPassed` (`…Orders.Domain/Jobs/ReadyGatePredicate.cs`, the member at line 21) is already
defined as "the latest QC result is a pass and no rework task is open (INV-JOB-06)" and has nothing to read.

**This slice records the QC result. The rework task is E06-F03-2b.** They are split because the calibration does not
permit one session: three tables, two aggregates, five routes and three events together come to roughly two thousand
production lines against the 1,500-line target of `CLAUDE.md` section 6, measured against the merged analogues
(`MeasurementCaptureHandler.cs` is 745 lines, `MeasurementCapturePayloads.cs` 493,
`MeasurementCaptureEndpoints.cs` 469, and an event schema plus its example is about 84 lines).

## What is already built (do not re-build)

- `…Orders.Domain/Jobs/GarmentJob.cs` (997 lines) — status, hold fields, `ReadyStateBlocks` and the `internal`
  transitions. **Do not add a QC field to it**: a QC result is its own aggregate, and
  `Jobs/ReadyGateInputs.cs` already expects `QcPassed`, `ReworkOpen` and `FailedQcReference` to be *supplied* rather
  than stored on the job (its `Create(...)` takes all eleven facts as parameters).
- `…Orders.Domain/Orders/Order.cs` (2,010 lines) — **the aggregate root every command goes through.** The
  `GarmentJob` transitions are `internal`, so the `Application` project cannot call them: the public entry points are
  `Order.StartProduction` (line 689), `Order.Hold` (792), `Order.Resume` (857), `Order.Reschedule` (915),
  `Order.ApplyReadyGate` (996 and 1066), `Order.ConfirmDelivery` (1182), `Order.CancelJob` (1258), `Order.Cancel`
  (1357), `Order.FindJob` (1408) and `Order.RecomputeStatus` (1548). If recording a QC result needs the job's status
  checked, use `Order.FindJob(garmentJobId)` and the job's public `Status`/`HasEnteredProduction`.
- `…Orders.Domain/OrdersErrors.cs` — the module's error factory, including
  `JobStatusTransitionNotAllowed(from, to)` → `orders.job-status-transition-not-allowed` (line 625) and
  `GarmentJobNotFound`. New refusals go here, as `Error.Conflict`/`Error.Validation` factories with a stable code.
- `…Orders.Infrastructure/Persistence/OrdersDbContext.cs` — `SchemaName = "orders"` (line 99), the `UseRowVersion`
  helper that maps `xmin`, and `ConfigureReadyState` (line 853). New tables join this context; no other module's
  schema is mapped (ARCH-005, MO-2).
- `…Orders.Infrastructure/Persistence/{OrdersJson,OrdersWriteFailures}.cs` — the module's JSON conventions (use them
  for the criteria snapshot) and the write-failure mapping.
- `…Orders.Application/Abstractions/IOrdersEventPublisher.cs` with
  `…Orders.Infrastructure/Persistence/OrdersEventPublisher.cs` — publishes through the module outbox in the same
  transaction. The event below goes through it.
- `…Orders.Application/Abstractions/IOrderStore.cs` with `…Orders.Infrastructure/Orders/OrderStore.cs` — the
  load-and-save under the order's row lock. The `Application` project holds **six files and no handler** today
  (`IEstimateStore`, `IOrderDraftStore`, `IOrderStore`, `IOrdersEventPublisher`, `OrdersSequences`,
  `AssemblyReference`), so this slice writes the module's first `Application/Quality/` folder.
- `…Orders.Api/OrdersEndpoints.cs` is **31 lines and maps an empty group**: `GroupPrefix` is `/api/v1/orders` (line
  15), `OpenApiTag` is `"Orders"`, and `MapOrdersEndpoints` maps the group and adds nothing. There are **zero**
  `/api/v1/orders` rows in the section 5 endpoints block of `docs/security/permission-matrix.md` today.
- `20260911141153_InitialOrdersSchema.cs` — the trigger patterns: `order_revisions_append_only` (the function at
  line 827, the trigger at 846), `job_dependencies_append_only` (882 / 903), `job_snapshots_are_immutable` (678) and
  `garment_jobs_ready_is_gate_only` (944). Each append-only function carries a **parent-still-exists arm**
  (`NOT EXISTS (SELECT 1 FROM …)`) so an `ON DELETE CASCADE` stays executable; `docs/dev/migrations.md` line 112
  explains why at length. Copy that arm.
- `tests/Tailor360.IntegrationTests/Orders/OrdersHarness.cs` (767 lines) — synthetic order, job, the
  `WorkflowVersion` identifier (line 96) and `Order.Build(...)` (line 307). Extend it; do not write a second harness.
- `tests/Tailor360.IntegrationTests/Orders/OrdersMigrationTests.cs` — the up/down/up walk to extend.
- `src/Platform/Tailor360.Platform.Security/FieldVisibility/OrdersResponseViews.cs` — the field-minimisation table,
  already defined and unit-tested. The read route applies it.
- `clients/pwa/src/stories/fixtures/production.ts:44,81` — `QC_CHECKLIST` and `DEFECT_CODES`, design fixtures only.

## How an endpoint is declared in this repository

There is one accepted shape and it is not optional. Copy it from
`src/Modules/Billing/Tailor360.Modules.Billing.Api/Invoicing/InvoiceEndpoints.cs` (a write at lines 190-196, a read
at 139-141), which is the closest merged example:

```
    .RequirePermission(<PermissionKey>, BranchScope.CurrentBranch)   // ARCH-007
    .ScopedToResource(<ResourceKinds.Thing>, "<routeValueName>")     // ARCH-023, when the route names a row
    .RequireRateLimiting(RateLimitPolicyNames.Write)                 // ARCH-017, exactly one
    .Audited(<Handler>.<Something>Action, reasonRequired: true)      // ARCH-008, every write
    .RequireIdempotency()                                            // where a client may retry
    .RequireIfMatch()                                                // where the row carries an ETag
    .WithRequestTimeout(RequestTimeoutPolicies.Command)               // every endpoint in the repository has one
```

- Verbs: `Platform.Security/Endpoints/EndpointAuthorisationExtensions.cs` (`RequirePermission(key, scope)`,
  `Audited(action, reasonRequired)`, `AllowAnonymousWithJustification`);
  `ResourceScopeEndpointExtensions.cs` (`ScopedToResource(resourceKind, routeValueName, scope)`, and the
  deliberately noisy exemption verb for a route whose identifier names nothing branch-owned);
  `RequestSafetyEndpointExtensions.cs` (`RequireIdempotency`, `RequireIfMatch`);
  `StepUpEndpointExtensions.cs` (`RequireStepUp`).
- **Rate-limit policies**, all of them, from `Platform.Security/Endpoints/RateLimitPolicyNames.cs`:
  `DefaultUser` = `default-user` (ordinary authenticated read), `Write` = `write` (authenticated state change),
  `ScanBurst` = `scan-burst`, `ExportHeavy` = `export-heavy`, plus `AuthenticationAnonymous`,
  `MultiFactorChallenge`, `RecoveryAnonymous`, `DefaultIp`. There is no other policy, and inventing one is a
  change to the limiter configuration in the web host.
- **Request timeouts**: `Platform.Security/Endpoints/RequestTimeoutPolicies.cs` — `Read` = `read`,
  `Command` = `command`, `Report` = `report`, `Export` = `export`. **Every** endpoint in `src/Modules/` carries one
  today (grep `WithRequestTimeout` — there is no exception), so an endpoint without one is the odd one out.
- **Branch scopes** from `Platform.Abstractions/Multitenancy/BranchScope.cs`: `CurrentBranch` (the default),
  `AssignedBranches`, `Organisation`, and the member that demands no branch reach at all.
- **Resource kinds**: each module keeps its own constants class — `BillingResourceKinds`,
  `MeasurementResourceKinds`, `DesignSelectionResourceKinds` — in its `Application` project, with an
  `IResourceScopeResolver` per kind in its `Infrastructure` project, registered with `TryAddScoped`.
- **The matrix row** goes in `docs/security/permission-matrix.md` section 5, *inside* the
  `<!-- matrix:endpoints -->` … `<!-- /matrix:endpoints -->` markers (lines 311-512 today), with the columns
  `| Method | Route | Declaration | Permission | Branch scope | Resource | Audited as | What it is for |`.
  `AuthorisationMatrixTests` fails and names any published route with no row, and any row with no route.
## Adding an integration event, and the six tests that will fail you

`tests/Tailor360.ContractTests/IntegrationEventTests.cs` enforces the whole chain. Read it before writing the
first record.

- `EveryEventIsDocumentedByASchemaAndAnExample` — both `docs/integration/events/<wire-name>.schema.json` and
  `<wire-name>.example.json` must exist. For size: `orders.job-held.v1.schema.json` is 72 lines and its example
  12, so budget about 84 lines per event for the JSON alone.
- `EverySchemaDescribesExactlyWhatTheEventPutsOnTheWire` — the schema's `properties` must be **exactly** the set
  the record serialises, `required` must be non-empty and must name nothing the record lacks, and
  `additionalProperties` must be present and `true` (`docs/integration/events/README.md` section 2: a closed
  schema would make every additive change breaking).
- `EveryExampleMatchesItsSchemaAndNamesItsOwnEvent` — the example carries the same property set and its
  `eventType` is the wire name.
- `EveryEventVersionsItsNameAndItsPayloadTheSameWay` — `SchemaVersion` agrees with the `.vN` in the wire name.
- `EveryEventIsDeclaredInItsOwnModulesPublishedSurface` — the record lives in `Orders.Contracts` and the wire name
  appears in the **Publishes — integration events** list of `docs/architecture/module-ownership.md` section 5.5.
  If it does not, amend that list in the same pull request; section 9 of that document requires it.
- `NoPayloadCarriesAFieldNamedLikePersonalData` — `IntegrationEventInventory.PersonalFieldWords` rejects a
  serialised property whose whole name, **or any camel-cased word inside it**, is one of `name`, `firstname`,
  `lastname`, `fullname`, `displayname`, `nativename`, `phone`, `telephone`, `mobile`, `msisdn`, `email`,
  `address`, `postcode`, `pincode` and the rest of that list. So no `checklistName`, no `defectName`, no
  `criterionName`, no `inspectorName`, no `roleName` — codes, keys, identifiers, statuses, amounts and timestamps
  only. The register `IntegrationEventInventory.PersonalFieldExemptions` exists but using it needs the approval
  `docs/nfr/data-classification.md` section 5.3 describes, which this issue does not have.

Two shapes to copy rather than invent: the payloads are **flat** — no `payload` wrapper — as
`orders.job-held.v1.schema.json` shows (`eventId`, `occurredAt`, `aggregateId`, `organisationId`, `branchId`,
`orderId`, `garmentJobNumber`, `reasonCode`, `eventType`, `schemaVersion`); and the `$comment` those schemas carry
states that a property added within `v1` is optional forever. Add the event's row to the table in
`docs/integration/events/README.md` as well — one row per event, with links to both files.

## The resource scope this slice cannot invent

ARCH-023 requires a resource scope on a branch-scoped permissioned route with a route parameter, and
`OrdersModuleServiceCollectionExtensions.cs` says in its own remarks (the paragraph beginning at line 28) that Orders
registers **no `IResourceScopeResolver` and no `IPermissionSource`**, deliberately, because both belong with the
endpoints. There is no `OrdersResourceKinds` class anywhere in `src/`.

**#201 delivers them.** Before starting, check that
`src/Modules/Orders/Tailor360.Modules.Orders.Application/Orders/OrdersResourceKinds.cs` (or wherever #201 put it)
exists with a garment-job kind, and that an `IResourceScopeResolver` for it is registered in
`OrdersModuleServiceCollectionExtensions`. If they are not there, **this issue is blocked** — it cannot satisfy
ARCH-023 and must not proceed by inventing a second resource-kind vocabulary that #201 will then collide with. Say so
on the issue rather than working around it.

## Scope

- [ ] `Domain/Quality/QcResult.cs`, `QcResultCriterion.cs` and `QcOutcome.cs` (`Pass`, `Fail`). **Immutable by
      construction**: `QcResult` exposes no mutator at all, and a mistaken result is superseded by recording a new
      one ([`docs/prd/state-transitions.md`](../../prd/state-transitions.md) section 3.2, and
      `docs/prd/workflows/blouse.md` line 305: "Re-QC recorded as a new result — the failed result is never
      overwritten").
- [ ] A `QcResult` carries the pinned `qc_checklist_version_id`, the checklist **code** and version **number**, the
      `responsible_role_key`, the outcome, `recorded_at`/`recorded_by`, and a JSON `criteria_snapshot` copying the
      criteria and defect vocabulary as published, so a historic result renders identically after the checklist
      changes (`docs/prd/category-hierarchy.md` line 228). `qc_result_criteria` rows carry the criterion code, the
      per-criterion outcome, the observed value for a `MeasurementTolerance` criterion, the defect codes chosen and an
      optional note.
- [ ] `Application/Quality/QualityControlHandler.cs` with `RecordQcResultCommand`,
      `Application/Abstractions/IQcResultStore.cs` and `Infrastructure/Quality/QcResultStore.cs`, in the style of
      `IOrderStore`/`OrderStore`. Registered with `TryAddScoped` in `OrdersModuleServiceCollectionExtensions`.
      Audit action constant on the handler: `orders.record_qc` — the name
      `docs/security/permission-matrix.md` line 232 and `raci.md` already use.
- [ ] Resolve the checklist through `Catalog.Contracts.Catalogue.IQcChecklistQuery.GetPublishedAsync(...)`
      (E06-F03-1c) — the sanctioned read contract of `CLAUDE.md` section 3. Orders never reads `catalog.*`
      (ARCH-004, ARCH-005). A service type whose template has no published version refuses the QC with a named
      problem code carrying the service-type code, not a null-reference.
- [ ] One event contract, `Orders.Contracts/Events/QcRecorded.cs` → `orders.qc-recorded.v1`, already named in the
      **Publishes** list of `module-ownership.md` section 5.5 so no amendment is needed, with its JSON Schema and
      example. Properties: the event envelope fields the other `orders.*` schemas carry, plus `orderId`,
      `garmentJobNumber`, `qcResultId`, `qcChecklistVersionId`, `checklistCode`, `checklistVersionNumber`,
      `outcome`, `failedCriterionCodes`, `defectCodes`, `recordedAt`. **No property whose name contains `name`** —
      see the events block above; no free-text note, no observed measurement value, no actor name
      (`docs/architecture/conventions.md` section 5.5, security rule 7).
- [ ] One expand-only migration, scaffolded as `QcResults`, creating `qc_results` and `qc_result_criteria` with
      `xmin`, `created_at/by`, the trigger pair `qc_results_append_only` and `qc_result_criteria_append_only`
      modelled on `order_revisions_append_only` **including its parent-still-exists arm**, a foreign key to
      `garment_jobs` with `ON DELETE CASCADE`, and an index on `(garment_job_id, recorded_at DESC)` — the index the
      "latest result" query of E06-F03-5 and the failed-QC view of E06-F03-6 both read. A register row in
      [`docs/dev/migrations.md`](../../dev/migrations.md).
- [ ] Three routes on `OrdersEndpoints.GroupPrefix`, declared exactly as the block above sets out:
      `POST /{orderId}/garment-jobs/{jobId}/qc-results` — `orders.record_qc`, `BranchScope.CurrentBranch`,
      `.ScopedToResource(<garment job kind>, "jobId")`, `RateLimitPolicyNames.Write`,
      `.Audited(QualityControlHandler.RecordedAction)` (matrix line 232 has Reason = **no**, so
      `reasonRequired: false`), `.RequireIdempotency()`, `.WithRequestTimeout(Command)`;
      `GET /{orderId}/garment-jobs/{jobId}/qc-results` — `orders.read`, the same resource scope,
      `RateLimitPolicyNames.DefaultUser`, no audit filter, `.WithRequestTimeout(Read)`, `OrdersResponseViews`
      applied;
      `GET /{orderId}/garment-jobs/{jobId}/qc-checklist` — `orders.read`, the same resource scope, `DefaultUser`,
      answering from `IQcChecklistQuery` so the capture screen (E06-F03-7) reads the pinned checklist without
      calling Catalog directly.
- [ ] `Api/Payloads/QcPayloads.cs` and `QcRequests.cs` — public payload types in the `Api` project, no `Domain` type
      returned (ARCH-013), following `MeasurementCapturePayloads.cs` in shape.
- [ ] Rows for all three routes in the section 5 endpoints block of `docs/security/permission-matrix.md` and the
      `identifier-editing` rows for `{orderId}` and `{jobId}` in
      `tests/Tailor360.IntegrationTests/Authorization/matrix.yaml`.

## Out of scope

**The rework task** — `rework_tasks`, `ReworkTask`, the open and complete commands, their two routes and their two
events are **E06-F03-2b**. This slice records a failing result and stops there; the screen for it is E06-F03-7 and
the gate that reads it is E06-F03-5. The ready-gate evaluation and any write to `garment_jobs.is_ready_for_delivery`
— E06-F03-5, which is the only writer (INV-JOB-07). Holds, reschedule, cancellation — E06-F03-3. Alterations —
E06-F03-4a and 4b. The five exception-dashboard reads — E06-F03-6. Any screen — E06-F03-7.

**Evidence media.** `requires_evidence` is enforced as "at least one media reference recorded against the result",
because the Media module is seven files of scaffolding with no `DbContext` and the upload and streaming paths are
#187 and #188. The reference is stored and shape-validated, **never resolved and never turned into a URL** (security
rule 9), and that limitation is stated in the pull request as a known limitation.

## Acceptance criteria

- [ ] A Tailor Master records a passing QC result over HTTP against a job in production, and reads it back with its
      criteria, their outcomes, and the checklist **code and version number** it was judged against.
- [ ] A failing result with at least one defect code per failed criterion is recorded, and recording a **second**
      result afterwards leaves both rows readable with their own actors and timestamps — nothing is overwritten. This
      is half of #34's "Rework cycles preserve each QC result, actor, defect and resolution"; E06-F03-2b carries the
      rework half.
- [ ] `GET .../qc-checklist` returns the **pinned** published checklist version for the job's service type, with its
      criteria in `display_order` and its defect codes, and a named problem code when the template has no published
      version.
- [ ] Every route declares a permission and a resource scope (ARCH-007, ARCH-023), exactly one rate-limit policy
      (ARCH-017), a request timeout, and — on the write — the audit filter (ARCH-008); the endpoint-inventory
      contract test shows each one.
- [ ] **Negative**: no `PUT` and no `PATCH` of a recorded result exists, and a direct `UPDATE` or `DELETE` from the
      application role is refused by `qc_results_append_only`; a caller without `orders.record_qc` receives `403`
      `security.forbidden`; a caller whose branch does not reach the job receives the **same answer as for a job that
      does not exist**; a failing criterion with no defect code is refused by validation with the criterion named; a
      replayed `Idempotency-Key` returns the first result rather than recording a second.
- [ ] **Exception**: recording a result for a service type whose checklist template has no published version is
      refused with a named problem code; recording one against a job that **never entered production** is refused
      with `orders.job-status-transition-not-allowed`; recording one against a **cancelled** job is refused the same
      way. None of the three is silently accepted.
- [ ] **Exception**: deleting the parent order cascades through `qc_results` and `qc_result_criteria` without a
      `restrict_violation` — the defect `docs/dev/migrations.md` line 112 records and the reason the append-only
      functions need their parent-still-exists arm. Asserted beside `OrdersCascadeTests`.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| `orders.qc_results` (outcome, checklist version, recorded_at/by, responsible_role_key) | Internal — `docs/nfr/data-classification.md` section 5.7 names "QC results, rework" in its Examples row | The immutable record of whether the garment met the standard | Business record, **no routine deletion** (section 5.7) | Branch-scoped by role: `orders.read` holders, Tailor Master and Tailor on jobs in their scope, contact details projected away (section 5.7 access row) | Job and result identifiers, the outcome and the checklist version may appear in logs (section 5.7 allows identifiers, phase codes and reason codes); `recorded_by` is staff personal data under section 5.15 |
| `orders.qc_results.criteria_snapshot` (JSON copy of the published criteria and defect vocabulary) | Internal | Render a historic result identically after the checklist changes | As the result | As above | Never logged whole; it is configuration text, not telemetry |
| `orders.qc_result_criteria.observed_value` | **Sensitive Personal by inheritance** — it is a garment measurement taken at QC, and section 5.4 classes measurements Sensitive Personal while section 5.7's log row says "the measurement snapshot … may not" appear in logs | The value a tolerance criterion was judged on | As the result | As above | **Never** in a log, a trace, a metric, an event payload or an export. State this explicitly in the pull request; it is the field most likely to be logged by accident |
| `orders.qc_result_criteria.note` (free text) | Internal, free text | What the inspector saw | As the result | As above | Never in logs, telemetry or any event payload. Only the criterion code and the defect codes travel |
| `orders.qc_results.evidence_media_ids` | Internal as an identifier. **The media itself is Sensitive Personal** under section 5.6 ("QC, custody and delivery evidence — Sensitive Personal for evidence images"), owned by Media, retention **proposed 24 months under OD-08** | Point at the defect photographs | The identifier lives as long as the result; the objects follow Media's retention | As above, and the image only through Media's authorised streaming endpoint, which re-authorises and logs the access (security rule 9) | Identifiers only — **never a storage key and never a URL**. Section 5.6's log row is "Never" |
| Event `orders.qc-recorded.v1` | Internal | Tell Reporting (#45), Notifications (#47) and the gate (E06-F03-5) that the facts changed | Outbox retention | Approved subscribers | Identifiers, codes, the outcome and timestamps only |
| Audit action `orders.record_qc` | Internal — evidentiary | Prove who judged and when | Per section 5.16 | `auditor`, `owner` | Audit store only |

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `orders.record_qc` | branch | no | no | **no** | `branch_manager`, `tailor_master` | **Exists** — `OrdersPermissions.cs:63`, matrix line 232, "Recorded against the pinned checklist version, with a reason on failure". Granted `SystemRoles.cs:195` and `:305`. The matrix's Reason column is **no**, so `Audited(..., reasonRequired: false)`; the "reason on failure" the matrix wording means is the **defect code** on the failed criterion, not an audit reason header |
| `orders.read` | branch | no | no | no | `owner`, `admin`, `branch_manager`, `reception`, `measurement_staff`, `tailor_master`, `tailor`, `inventory_clerk`, `cashier`, `delivery_staff`, `auditor` — eleven roles | **Exists** — matrix line 231, attributed to #32a. The two reads use it with `OrdersResponseViews` field minimisation |

No permission key is added and no grant is changed. **OD-13** is open, so these are the seeded defaults. The fixture
extended is `tests/Tailor360.IntegrationTests/Authorization/matrix.yaml`.

## Modules, contracts and events

Orders owns the schema and is the primary target. **One** other module is reached and only through its `Contracts`
project: `Catalog.Contracts.Catalogue.IQcChecklistQuery` (ARCH-003, ARCH-004). Nothing reads `catalog.*` (ARCH-005).
Publishes one new versioned integration event through the existing module outbox; `orders.qc-recorded.v1` is already
in `module-ownership.md` section 5.5's published list, so that list needs no amendment — **confirm that before
assuming it**, because the contract test `EveryEventIsDeclaredInItsOwnModulesPublishedSurface` reads it. Threat-model
coverage is **DOR-05's pre-#56a alternative**, not a written model:
[`docs/security/README.md`](../../security/README.md) section 3 records that only the authentication model exists (#23)
and that `abuse-cases.md` is **not written**, with the rest scheduled under #56a — the order-workflow model
specifically in the batch-1 child `E14-F01a-4`. So the abuse case is **named here and carried into that backlog
entry**: the one new consideration — an inspector recording a result for a garment
in another branch — is answered by branch scope plus the resource scope on `{jobId}` and is asserted as a negative
test.

## Screens and states

Deleted: no screen. The QC capture screen is E06-F03-7 and consumes these three routes.

## Migration and rollback

One expand-only migration, `QcResults`: two new tables, two new triggers, one index, no column added to an existing
table and no data movement. The previous release tolerates it because nothing reads the tables. Rollback is
redeploying the previous tag with the two tables unused; `Down` drops them and must have been executed at least once
before merge (`src/Modules/CLAUDE.md` section 6), proved by extending
`tests/Tailor360.IntegrationTests/Orders/OrdersMigrationTests.cs` with up/down/up against a scratch database, the way
`CatalogMigrationTests` does. Per [`docs/dev/migrations.md`](../../dev/migrations.md), with a register row in it.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-10** initial catalogue and **QC checklists** reviewed from the seeded drafts | **Open** — `docs/prd/assumptions-and-open-decisions.md` line 63 | Tests publish their own checklist fixture through E06-F03-1c's routes; no seeded checklist is published by this slice | W2 for the content; the mechanism ships now |
| **XQ-01** configurable reason-code lists for hold, cancellation, **rework**, delivery failure and custody correction | **Open** — `docs/prd/exceptions.md` line 197 | A defect code is whatever E06-F03-1b's catalogue holds, validated as one of the pinned version's codes and nothing else. No vocabulary is written into code | The exception review #17 records |
| **OD-13** approved role-to-permission grants | **Open** — line 66 | `orders.record_qc` and `orders.read` keep their seeded grants | W1 exit |
| **SQ-01** what moves a delivered job to `closed` | **Open**, proposed — `docs/prd/state-transitions.md` line 522 | Nothing here closes a job. Recording a QC result does not change the job's status at all | With #34 per the question's row |
| **OD-08** retention periods, including evidence images | **Open** | The result keeps the media **identifier** indefinitely as part of a business record; the object's 24-month retention is Media's and is proposed, not settled. No deletion path is built here | #57 |

## Required verification

- [ ] unit: `QcResult` has no mutator (asserted by reflection over its public surface, not by inspection); a failing
      criterion with no defect code is refused; a defect code outside the pinned version's vocabulary is refused; the
      `criteria_snapshot` round-trips through `OrdersJson`; `QcOutcome` parsing rejects a number naming no member.
- [ ] integration: the three routes against real PostgreSQL; the authorisation matrix for each (allowed, not
      permitted, other-branch, anonymous); the idempotency replay; both append-only triggers refusing `UPDATE` and
      `DELETE` from the application role; the order cascade.
- [ ] integration: two results recorded in sequence leave two rows with their own actors and timestamps, and the
      later one does not alter the earlier — the direct evidence for the "preserve each QC result, actor, defect"
      half of #34's second acceptance criterion.
- [ ] contract: OpenAPI lint and diff; the endpoint inventory; `IntegrationEventTests` green for the new
      schema/example pair, **including `NoPayloadCarriesAFieldNamedLikePersonalData`**.
- [ ] migration: `OrdersMigrationTests` up/down/up; `has-pending-model-changes` clean.
- [ ] NFR rows touched: the **S3** command-latency row of [`docs/nfr/slo.md`](../../nfr/slo.md) line 131 for the QC
      write and the **S2** read row (line 130) for the two reads, with measured figures rather than restated targets;
      **NFR-MQ-05** in [`docs/nfr/traceability.md`](../../nfr/traceability.md) line 276 through the checklist being
      configuration this slice consumes rather than code it contains.
- [ ] environment: `./scripts/dev doctor` before starting; `CI=true` makes a skipped integration tier a failure
      (DOR-16).

## Evidence the pull request will carry

- [ ] `./scripts/dev test unit|architecture|contract|integration` output, run twice.
- [ ] `migrate --dry-run`, then `migrate`, then the up/down/up log.
- [ ] The OpenAPI difference report for the three operations and `pnpm --dir clients/pwa generate:api:check`.
- [ ] The `permission-matrix.md` section 5 rows — the **first** `/api/v1/orders` rows after #201's — the
      `matrix.yaml` diff, and the `AuthorisationMatrixTests` run.
- [ ] An HTTP transcript of a pass, then a fail with defect codes, then a second result, with synthetic data only,
      beside the audit rows it wrote.
- [ ] The new event schema and example files, and the `IntegrationEventTests` run that accepts them.
- [ ] A grep over the new code showing no log or telemetry call takes `observed_value`, `note` or a media key, and
      `dotnet format --verify-no-changes` clean.

## Depends on

**E06-F03-1c** — for `Catalog.Contracts.Catalogue.IQcChecklistQuery` and a published checklist version to pin. 1c
depends in turn on 1a and 1b.

#`201` — **[E06-F01-2] Issue estimates and confirm orders into immutable garment job cards**, which is **open**. It
creates the Orders `Application` and `Api` layers this slice extends, the `OrdersResourceKinds` vocabulary and the
`IResourceScopeResolver` ARCH-023 needs for `{jobId}`, and the confirmed job a QC result is about. #201 names every
hold/resume/reschedule/QC/rework/alteration/cancellation endpoint as out of its own scope, so there is no overlap.
See **The resource scope this slice cannot invent** — without #201 this issue is blocked, not merely inconvenienced.

#`33` — **[E06-F02] the production workflow**, which is **open** and being decomposed in a parallel batch, so its
child issue numbers do not exist yet. This slice needs only the *meaning* of "in production": `Order.StartProduction`
and `GarmentJobStatus.InProduction` are merged today, so the status check works without `job_phases`. It does **not**
need a phase vocabulary — that is E06-F03-2b's dependency, for the rework return phase.

Same-file sequencing: `OrdersDbContext.cs`, the Orders migration chain, `OrdersEndpoints.cs`,
`OrdersModuleServiceCollectionExtensions.cs`, `docs/api/openapi.v1.json`, `clients/pwa/src/api/schema.d.ts`,
`docs/security/permission-matrix.md`, `matrix.yaml`, `docs/dev/migrations.md` and
`docs/integration/events/README.md` are each touched by #201, by #33's children and by E06-F03-2b, -3, -4a and -4b.
One of them at a time; the second to merge rebases and re-runs the Orders tiers.

Refs #34
