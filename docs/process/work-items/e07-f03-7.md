Sub-issue of #37. The whole write side of the custody chain is reachable only by `curl`. There is **no custody client
at all**: `ls clients/pwa/src/` shows `admin`, `api`, `app`, `auth`, `billing`, `catalog`, `components`, `customers`,
`design-system`, `i18n`, `measurements`, `routes`, `stories` — and no `custody`; `clients/pwa/src/routes/` holds
`admin`, `auth`, `billing`, `catalog`, `measurements` and no `custody`; and `grep -n "scan\|custody"
clients/pwa/src/app/router.tsx` returns **nothing**. This slice is the screen a person holding a garment uses:
`docs/nfr/a11y-checklist.md` section 6.4 makes it **`A11Y-PZ-02`, priority zero**, and its steps **4, 5 and 7** — read
the custodian from the timeline, transfer out, receive — are exactly this screen. **Step 8 is deliberately not claimed
here.** Its wording is "Reject a transfer with a reason, **and hear the reconciliation case open**", and the case a
rejection opens is E07-F03-3's; this slice's reject dialogue announces only that the garment stays with the sender.
Step 8 is recorded by E07-F03-8b, which has the case. Steps 1, 2, 3 and 9 need a scanner and are #36's and
E07-F03-9's; step 6 needs the queue and is E07-F03-8's; step 10 needs the persisted offline queue and is #51's.

## What is already built (do not re-build)

- **E07-F03-2** and **E07-F03-5** (merge first) — `POST /custody/transfers`, `POST
  /custody/transfers/{transferId}/receive`, `POST /custody/transfers/{transferId}/reject`, `GET
  /custody/transfers` and `GET /custody/jobs/{garmentJobId}/timeline`. Their request and response types arrive in
  `clients/pwa/src/api/schema.d.ts` through `pnpm --dir clients/pwa generate:api`; **derive the client types from
  there rather than hand-writing them**, the way `clients/pwa/src/billing/types.ts` does.
- `clients/pwa/src/components/primitives/Timeline.tsx` (106 lines) with `TimelineEntry` — `id`, `title`,
  `status?`, `icon?`, `absoluteTime`, `dateTime?`, `relativeTime?`, **`actor?`** and **`detail?`**; its own
  documentation example is "Scanned in at Finishing", and its remarks carry the rule that the absolute time is
  always rendered (`docs/nfr/accessibility-localisation.md` section 12). It renders an `<ol>` and is explicitly
  **not a live region** ("Nothing here is a live region and nothing here animates"), which is why every
  announcement goes through `StateRegion` instead. **This is the timeline. Do not build a second one**, and
  **`detail` is how a correction is shown beside the event it corrects**: the correcting entry's `detail` carries
  the corrected event's action, custodian and time from E07-F03-5's payload, and the corrected entry's `detail`
  names the correction. That is the whole of EX-13's "side by side" — no new layout, no second component.
- `clients/pwa/src/components/primitives/` — `Card`, `DataTable`, `StatusBadge`, `Filters`, `Button`,
  `ButtonGroup`, `Alert`, `TextLink`, `Icon`, exported from `index.ts`; `clients/pwa/src/components/dialogs/` —
  `Dialog`, `ConfirmDialog`, `Drawer`; `clients/pwa/src/components/states/` — `LoadingState`, `EmptyState`,
  `ErrorState`, `RetryableError`, `StateRegion`, `NetworkStatusBanner`, `OfflineBlockedAction` and
  `problemDetails.ts`.
- `clients/pwa/src/components/primitives/statuses.ts` — `STATUS_KINDS` is a **closed union of nineteen members**
  (fifteen job states plus `invited`, `active`, `suspended`, `deactivated`), with a documented rule that
  "**neighbouring statuses do not share a glyph**" and no custody member. **Do not add one** (that is #50's) and
  **do not borrow one**: `held` means an Orders hold and an open custody case is not one; `qc-failed` means a QC
  fail. So this screen uses **no `StatusBadge` at all**. `Timeline.tsx`'s `TimelineEntry` takes either a `status`
  **or** an `icon` ("An explicit glyph, when the entry is not a status change"), and a custody scan is not a status
  change — so every entry carries an `icon` from the existing `IconName` union (`scan` for a scan, `truck` for a
  transfer leg, `refresh` for a correction, `alert-triangle` for a refused event, `dot` otherwise) and the state
  itself is rendered as a **word** from `messages/custody.ts`. Record in the pull request which custody states
  would want badges, as the request to #50.
- `clients/pwa/src/routes/billing/TakePaymentRoute.tsx` (390 lines) and `TakePaymentRoute.test.tsx` (243) — the
  **exact harness a non-admin route is tested with**: `MemoryRouter` + `AppIntlProvider` + `SessionProvider` +
  `RequireSession` + `RequirePermission` + `ShellStatusProvider`, with `stubFetch`, `jsonResponse`,
  `problemResponse` and `aCurrentUser` from `clients/pwa/src/auth/testing/fixtures.ts` (230 lines), plus
  `forgetAntiforgeryToken` from `clients/pwa/src/auth/antiforgery.ts` — **not** from `apiClient.ts`, which only
  imports it — and `setSessionChallengeHandler` from `clients/pwa/src/auth/apiClient.ts:204`.
  **`clients/pwa/src/design-system/testing/renderWithProviders.tsx` supplies `AppIntlProvider` and display
  preferences only — no session and no router — so a route rendered in it throws. Use the Billing harness shape.**
- `clients/pwa/src/admin/useAdminResource.ts` — the loader every route of this repository uses, admin or not (see
  `clients/pwa/src/routes/billing/OutstandingBalancesRoute.tsx:24`), with `value`, `loading` and `failure`.
- `clients/pwa/src/auth/apiClient.ts` — `apiRequest`, `IDEMPOTENCY_HEADER`, `IF_MATCH_HEADER`, `ApiError`,
  `STEP_UP_REQUIRED_CODE`; `clients/pwa/src/auth/AuthProblemAlert.tsx` for a refusal;
  `clients/pwa/src/billing/billingApi.ts` as the model api module, including its documented rule that a retry key
  is "minted when the person commits to the act and held until it succeeds".
- `clients/pwa/src/admin/RequirePermission.tsx` — wraps a route and renders an **`EmptyState` with
  `admin.forbidden.title` / `admin.forbidden.body`**. It is not the `Forbidden` component and it never redirects;
  the forbidden state of every screen below is that.
- `clients/pwa/src/design-system/testing/expectNoHorizontalOverflow.ts` — already defaults to 320, 360, 768, 1024
  and 1280 px at 100% and 200% zoom. Call it with its defaults; do not narrow the list.
  `clients/pwa/src/design-system/testing/axe.ts` exports `expectNoAccessibilityViolations`.
- `clients/pwa/src/i18n/messages/index.ts` — a family owns one file exporting `<family>En` and `<family>Ta` and
  adds one line to each of the two spreads; the type gate in `en-IN.ts` fails `pnpm typecheck` when a key exists
  in one language and not the other. `messages/billing.ts`'s header is the precedent for holding a shop-floor
  vocabulary back for native-speaker review.

## Scope

- [ ] **`clients/pwa/src/custody/custodyApi.ts`** — `readJobCustody(garmentJobId, signal)`,
      `transferOut(request)`, `receiveTransfer(transferId, request)`, `rejectTransfer(transferId, request)`. Every
      write carries an `Idempotency-Key` minted at the moment the person commits and **held across retries**, plus
      the `clientEventUuid` the server de-duplicates on, and the same key is reused by the in-memory retry — which
      is what makes a retry a replay rather than a second hand-off (E07-F03-1's rule).
- [ ] **`clients/pwa/src/custody/types.ts`** and **`custodyStatuses.ts`** — the types derived from `schema.d.ts`
      and the custody-state-to-`StatusKind` map described above.
- [ ] **`clients/pwa/src/routes/custody/JobCustodyRoute.tsx`** at `/custody/jobs/:garmentJobId` — one screen, four
      things: the current custodian and location in a `Card`; the chain in `Timeline`, oldest first, **with a
      correction rendered beside the event it corrects** (EX-13's evidence row); a **Transfer out** action in a
      `Dialog` (choose a user, a team or a location, optional note, confirm); and **Receive** and **Reject**
      actions on a pending transfer for this job, the rejection demanding a reason before its `ConfirmDialog` will
      submit.
- [ ] **The confirmation step, per FBH-02's interim position — and do not write the list twice.** A cross-branch
      transfer out asks for confirmation before committing; a receive and an ordinary transfer out do not. That is
      FBH-02's proposed `Scanning:ConfirmBeforeCommit` list ("dispatch, delivery confirmed, QC result, cross-branch
      transfer out and correction", with "receive … transfer accept … in continuous mode"). **#36's E07-F02-3b owns
      that list** as `CONFIRM_BEFORE_COMMIT` in `clients/pwa/src/scanning/scanModes.ts`, so: if that file exists,
      **import the constant** rather than restating the actions; if #36 has not merged, apply the same FBH-02 list
      locally in one named constant and say in the pull request that whichever merges second collapses the two. No
      new setting is introduced, no default is invented, and FBH-02 is named as still open.
- [ ] **Fill #36's `ScanCommandSubmitter`, if #36 has merged.** E07-F02-3b defines
      `clients/pwa/src/scanning/scanCommand.ts` with
      `submit({ action, jobId, clientEventId, idempotencyKey, source, manualReason? })` and a **fail-visible
      default**, and its own body says in terms "#37 registers the real submitter" through "a registration seam (a
      module-level setter, the shape `setScannerTelemetryTransport` already uses)". **No other child of #37 covers
      this**, so it belongs here: implement the port over `custodyApi.ts` and register it through that setter, reusing
      the same key-minting rule, and add the one test that a scan committed from #36's screen reaches
      `POST /custody/scans` with an `Idempotency-Key` and a `clientEventUuid`. If #36 has **not** merged, do not
      create the port: record it in the pull request as still owed by the later of the two issues, naming
      E07-F02-3b.
- [ ] **In-memory retry with a visible pending state**, and nothing persistent: a failed submission stays on the
      screen as "not yet sent" with a **Retry** action reusing the same key, and a `409`/`422` stops retrying and
      shows the server's problem details through `problemDetails.ts` (plan Section 6.2 note 8: "#37 retries scan
      submissions in memory; #51 is the only implementation of … the persisted offline scan queue"). Use
      `OfflineBlockedAction` for the offline case rather than inventing a banner.
- [ ] **Announcements, in a persistent region.** `StateRegion` carries the outcome of every action: an accepted
      receive politely (A11Y-40), a refusal assertively (A11Y-41), and **never a toast** —
      `docs/nfr/accessibility-localisation.md` section 6 forbids one outright and A11Y-42 is the item a reviewer
      walks.
- [ ] **`clients/pwa/src/i18n/messages/custody.ts`** exporting `custodyEn` and `custodyTa`, added to both spreads
      of `messages/index.ts`. Every string the screen renders, including each refusal code this slice can receive
      (`custody.custodian-mismatch`, `custody.stale-event`, `custody.transfer-not-pending`,
      `custody.not-the-destination`, `custody.event-conflict`) mapped to a sentence that says what to do next.
      Tamil is authored and held for native-speaker review, with the reason stated in the file header as
      `messages/billing.ts` does.
- [ ] **`clients/pwa/src/custody/custodyPermissions.ts`** exporting `CUSTODY_PERMISSIONS` — the four keys named
      once, exactly as `clients/pwa/src/billing/billingPermissions.ts` (40 lines) does for Billing, with the same
      header note that the server re-checks every request.
- [ ] **One route registration** in `clients/pwa/src/app/router.tsx`, inside the existing `RequireSession` element
      at line 170, as a child object `{ path: 'custody/jobs/:garmentJobId', element: <RequirePermission
      permission={CUSTODY_PERMISSIONS.scan}><JobCustodyRoute /></RequirePermission> }`. **The path is relative and
      carries no leading slash** — every sibling is `'billing/payments/new'`, `'billing/cashier'` and so on — and
      the import is static at the top of the file, not lazy, like every other route there. **No navigation destination
      is
      added here** — the screen is reached from a job card, from the transfers queue (E07-F03-8) and from #36's
      scan confirmation card; A11Y-11 ("reachable by at least two routes") is answered by the queue and the job
      card together, and the pull request says which two.
- [ ] **A permission-aware surface.** A caller without `custody.transfer_out` sees the timeline and **no**
      transfer action; without `custody.receive` no receive action; without `custody.reject_transfer` no reject
      action — read from `aCurrentUser`'s permission set the way `clients/pwa/src/billing/billingPermissions.ts`
      does, and never as the only guard, the server being authoritative.

## Out of scope

The camera, the keyboard-wedge source and manual entry — **#36** owns `ScannerSource` and every scan-capture concern,
and this screen deliberately has no scanner: it is reached *after* a scan, or from a queue. The pending-transfer
queue, the overdue list, the exception queues and the correction and approval forms — E07-F03-8. The persisted offline
queue —
#51. A Storybook story: `clients/pwa/src/routes/catalog/catalogScreens.stories.tsx` records why there are deliberately
**no offline stories** for screens of this kind, so state stories are Vitest tests here, not stories, and no offline
screenshot is asked for. Any new `StatusKind` or navigation destination (#50, #33's shell work).

## Acceptance criteria

- [ ] A **Branch Manager** signed in at a branch opens `/custody/jobs/:garmentJobId`, reads the current custodian,
      transfers the garment to a named **Tailor Master**, and the screen shows the transfer as pending with the new
      expected custodian — entirely through the screen, with no direct database access (A11Y-PZ-02 steps 4 and 5).
      The destination is a Tailor Master and not a Tailor because of the seeded grants; see the criterion below.
- [ ] The destination **Tailor Master** — not a Tailor: `custody.receive` is granted to `branch_manager`,
      `tailor_master` and `delivery_staff` only (matrix line 175, `SystemRoles.cs:314`), and `SystemRoles.cs:330-331`
      gives the Tailor role `custody.scan` and `custody.manual_lookup` and nothing else — receives the transfer on
      the same screen and the announced outcome names the new custodian; rejecting instead demands a reason first
      and, once given, announces that the garment **stays with the sender** (A11Y-PZ-02 step 7). The announcement
      says nothing about a reconciliation case, because this release does not open one — that is E07-F03-3's, and
      E07-F03-8b records step 8.
- [ ] **Negative — refusal rendering.** Each of the five refusal codes above renders its own sentence with a next
      action, announced assertively, with the action still available to retry where retrying can help and disabled
      where it cannot. A `403` renders `RequirePermission`'s `EmptyState` with `admin.forbidden.title` /
      `admin.forbidden.body` — not a redirect, not a blank screen.
- [ ] **Negative — retry is a replay.** Two submissions of the same transfer out, the first failing at the network
      level, produce **one** transfer: the test asserts the second request carried the **same** `Idempotency-Key`
      and the same `clientEventUuid` as the first.
- [ ] **#36's seam, or a recorded gap.** With #36 merged, a scan committed from `/scan` reaches
      `POST /custody/scans` through the registered `ScanCommandSubmitter` with both keys, proved by one test; with
      #36 unmerged, the pull request says so and names E07-F02-3b as the owner of the seam. Either way the answer is
      written down rather than left to the next reader.
- [ ] **Exception — a correction on the chain.** A job whose timeline contains a `CORRECTION` event renders the
      original and the correcting event side by side, with both times, and neither is hidden.
- [ ] Loading, empty, error, offline and forbidden states all exist and are each covered by a test
      (`clients/pwa/CLAUDE.md`'s state rule).
- [ ] `expectNoAccessibilityViolations` passes on every state, and `expectNoHorizontalOverflow` passes **with its
      default widths and both zoom levels**.
- [ ] `pnpm --dir clients/pwa typecheck` passes, which is what proves the Tamil catalogue has every key the
      English one has.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Timeline entries rendered (action, custodian identifier, location, time, source) | **Internal** (`docs/nfr/data-classification.md` section 5.8) | Let a person see where the garment has been | Not stored client-side | The signed-in holder of `custody.scan` at that branch | **Nothing is persisted on the device**: no `localStorage`, no cache entry, no service-worker route for these responses (#51 owns any local storage) |
| The in-memory pending submission (job, action, destination, `Idempotency-Key`, `clientEventUuid`) | Internal | Survive a flaky network for the life of the screen | **Lost on navigation or reload, deliberately** | The person at the device | Never written to storage; never sent to client telemetry |
| Free-text note and rejection reason | Internal, free text | What the sender tells the receiver | As the server row | As above | Never sent to client telemetry, which carries web vitals and error **codes** only |
| Custodian display names | Personal (staff, section 5.15) | Show a person who is holding a garment | Not stored | As above | Never in a log line, a metric or a client telemetry attribute — the identifier is what travels |
| Garment job number | Internal | Name the garment on screen and in a row action (A11Y-60) | Not stored | As above | Safe: plan D9 keeps personal data out of every display number and barcode |

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `custody.scan` | branch | no | no | no | `branch_manager`, `reception`, `tailor_master`, `tailor`, `delivery_staff` | **Exists** — `docs/security/permission-matrix.md` line 180; the route guard and the timeline read |
| `custody.transfer_out` | branch | no | no | no | `branch_manager`, `tailor_master` | **Exists** — matrix line 181; hides the transfer action when absent |
| `custody.receive` | branch | no | no | no | `branch_manager`, `tailor_master`, `delivery_staff` | **Exists** — matrix line 175 |
| `custody.reject_transfer` | branch | no | no | **yes** | `branch_manager`, `tailor_master` | **Exists** — matrix line 178; the reason field is mandatory in the form because the permission requires one |

No permission is added and no grant changes; **OD-13** is open under a documented default. A Tailor holds
`custody.scan` but not `custody.receive`, so the screen must be correct for a Tailor who can read the chain and cannot
accept a transfer — that is a test, not a defect.

## Modules, contracts and events

Client only: `clients/pwa`. No .NET project changes, so no ARCH rule about module boundaries is engaged; the relevant
rules are the client ones in `clients/pwa/CLAUDE.md` — every string through the catalogue, every state present, every
number through the shared formatters (`clients/pwa/src/i18n/formatters.ts`), and no personal data in client telemetry
(`CLAUDE.md` section 4 rule 7). DOR-05: the client-side threat is the pending submission becoming a second hand-off,
closed by holding the key across retries and proved by the test above.

## Screens and states

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| Job custody | `/custody/jobs/:garmentJobId` | `LoadingState` while the timeline loads | `EmptyState` — a job with no scan yet: "no custody recorded", with the intake scan named as the next step | `RetryableError` for the read; `AuthProblemAlert` plus a sentence per refusal code for a write | `NetworkStatusBanner`; writes become `OfflineBlockedAction` with the pending submission visible as not yet sent | `RequirePermission`'s `EmptyState` with `admin.forbidden.title` / `admin.forbidden.body` |
| Transfer-out dialogue | same route, `Dialog` | Destinations loading inline | "no destination available at this branch" | Field errors from the server's problem details | Blocked, with the reason | Not rendered at all without `custody.transfer_out` |
| Reject dialogue | same route, `ConfirmDialog` | — | — | Reason required, rendered as a field error | Blocked | Not rendered without `custody.reject_transfer` |

Every instant is rendered by `formatters` in the branch timezone with the absolute value always present
(`docs/nfr/accessibility-localisation.md` section 12); no duration, no money and no measurement appears on this
screen.

## Migration and rollback

Deleted: no schema change. Rollback is redeploying the previous client bundle; the routes simply stop existing, and
the server endpoints this screen calls are unaffected.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **FBH-02** which transitions need a confirmation card before committing | **Open, proposed** (`docs/architecture/sequences/barcode-handoff.md` section 8) | The proposed list: confirm a cross-branch transfer out; do not confirm a receive or an ordinary transfer out. No new setting is introduced | #36 |
| **A11Y-OD-13** whether the eight per-role records of section 6.8 are all walked at #50 | **Open** (`docs/nfr/a11y-checklist.md` line 1498; interim position "the five, plus the three mappings recorded on the same evidence") | `A11Y-PZ-02` steps 4, 5 and 7 are walked here and recorded; `A11Y-RJ-03` is not walked twice | #50 |
| **OD-07** the device, browser and printer matrix | **Open** (`docs/prd/assumptions-and-open-decisions.md` line 60) | Screenshots are taken at the `expectNoHorizontalOverflow` default widths and on the device pairing `docs/nfr/a11y-checklist.md` section 2.3 already names; no new device is assumed | W0 exit |
| **OD-13** role-to-permission grants | **Open** under a documented default | The seeded grants are used unchanged, including a Tailor who may read but not receive | W1 exit |

## Required verification

- [ ] Vitest (`clients/pwa`): the happy path for transfer out, receive and reject through the screen; each of the
      five refusal codes; the same-key retry assertion; the correction rendered beside its corrected event; every
      state in the table above; the permission-aware surface for a Tailor, a Tailor Master and a caller with
      `custody.scan` alone.
- [ ] accessibility: `expectNoAccessibilityViolations` on each state and `expectNoHorizontalOverflow` with its
      default widths and both zoom levels; a **manual** screen-reader pass recording `A11Y-PZ-02` steps **4, 5 and
      7** plus A11Y-40, A11Y-41, A11Y-42 and A11Y-60, on one of the Tier 1 pairings of
      `docs/nfr/a11y-checklist.md` section 2.3 (TalkBack with Chrome, VoiceOver with Safari, or NVDA with Chrome or
      Edge).
- [ ] `pnpm --dir clients/pwa lint`, `typecheck`, `test`, `format:check` and `generate:api:check`.
- [ ] DOR-13: the `docs/nfr/traceability.md` rows for the client performance budgets are named — LCP and INP for
      this route per `docs/nfr/capacity-and-performance.md` lines 211-212 — and no figure is restated.
- [ ] DOR-16: `./scripts/dev doctor`; the client tier needs no database. No Playwright test is written:
      `tests/e2e` and `playwright.config` **do not exist** and `scripts/dev` says the suite arrives with #52.

## Evidence the pull request will carry

- [ ] Screenshots at phone (360), tablet (768) and desktop (1280) widths for the default, empty, error, offline
      and forbidden states, synthetic data only.
- [ ] The Vitest run, the axe run and the overflow run.
- [ ] The completed `A11Y-PZ-02` rows for steps **4, 5 and 7** with the pairing, the date and the items failed —
      the record `docs/nfr/a11y-checklist.md` section 6.4 is a table for. Leave the other seven rows alone; each
      names its own owner in this decomposition.
- [ ] The network transcript of the retry showing one transfer created from two requests with one key.
- [ ] `lint`, `typecheck`, `format:check` and `generate:api:check` output.

## Depends on

#37 E07-F03-2 — the four transfer routes. #37 E07-F03-5 — the timeline read. Both must merge first; the types
come from the regenerated `schema.d.ts`.

#190 and #194 — `[E07-F01-1]` and `[E07-F01-3]`, the identity and the resolve/by-number lookup a job is reached by.
Open, and not strictly required: this screen takes a job identifier in its route.

**Open, not depended on:** #50 (the design system) — every primitive this screen uses is already merged under
`clients/pwa/src/components/`; #36 (the scanner) — this screen captures nothing itself, though if **E07-F02-3b** has
merged this slice fills its `ScanCommandSubmitter` and imports its `CONFIRM_BEFORE_COMMIT`, as **Scope** says; #51
(the offline queue) — the retry here is
in memory by design. **In flight in the same files:** `clients/pwa/src/app/router.tsx`,
`clients/pwa/src/i18n/messages/index.ts` and `clients/pwa/src/api/schema.d.ts`, all of which #33's, #34's, #35's and
#48's client children also touch — one issue at a time.

Refs #37
