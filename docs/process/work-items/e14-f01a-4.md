Sub-issue of #56. Three rows of the **DOR-05** coverage table in `docs/security/README.md` section 3.2 name flows
this sub-issue covers: "Order and garment workflow → `threat-models/order-workflow.md` → #56a → W2", "Barcode
identity and custody → `threat-models/barcode-custody.md` → #56a → W2" and "Inventory and the stock ledger →
`threat-models/inventory.md` → #56a → W2". All three stand in on "the workflow invariants in
`docs/architecture/invariants.md`" and none of the files exists — `ls docs/security/threat-models` returns
`authentication.md` alone. Plan Section 6.2 note 5 puts them in W2 precisely because they must precede W3 and W4:
#32a, #33 to #37 and #38 to #40 are the issues these three models constrain, and the open sub-issues #190, #191,
#194, #197 (barcodes and custody) and #193, #195, #196, #198 (inventory) are all still unstarted.

## What is already built (do not re-build)

**Orders is a domain with no application or endpoint layer.** Model the invariants that exist and the entry points
that are designed.

- `src/Modules/Orders/Tailor360.Modules.Orders.Domain/` — 35 files of real aggregates: `Drafts/OrderDraft.cs`,
  `OrderDraftGarment.cs`, `OrderDraftGarmentContent.cs`, `OrderDraftGarmentDependency.cs`, `MeasurementIntent.cs`;
  `Estimates/Estimate.cs`, `EstimateStatus.cs`; `Orders/Order.cs`, `OrderRevision.cs`, `OrderStatus.cs`,
  `ReadyAggregation.cs`; `Jobs/GarmentJob.cs`, `GarmentJobRevision.cs`, `GarmentJobSpecification.cs`,
  `GarmentJobStatus.cs`, `JobDependency.cs`, `JobDependencyKind.cs`, `ReadyGate.cs`, `ReadyGateBlock.cs`,
  `ReadyGateInputs.cs`, `ReadyGateOutcome.cs`, `ReadyGatePredicate.cs`, `CustodyReconciliation.cs`;
  `Snapshots/MeasurementSnapshot.cs`, `DesignSnapshot.cs`, `DesignSelection.cs`, `MeasuredValue.cs`,
  `PriceSnapshot.cs`; `DisplayNumbers/OrderNumber.cs`, `EstimateNumber.cs`, `GarmentJobNumber.cs`,
  `FinancialYear.cs`, `DisplayNumberFormat.cs`. **There are no workflow, phase, assignment, QC or alteration
  types** — those arrive with #33 and #34.
- `src/Modules/Orders/Tailor360.Modules.Orders.Application/Abstractions/` holds five files and **no handler**:
  `IOrderDraftStore.cs`, `IEstimateStore.cs`, `IOrderStore.cs`, `IOrdersEventPublisher.cs`, `OrdersSequences.cs`.
  `Orders.Api` is a stub: `grep -c MapPost src/Modules/Orders` returns zero endpoint registrations.
- One migration, `Orders.Infrastructure/Migrations/20260911141153_InitialOrdersSchema.cs`, creating **thirteen**
  tables in schema `orders` (declared at `Orders.Infrastructure/Persistence/OrdersDbContext.cs:99`): `order_drafts`,
  `order_draft_garments`, `order_draft_garment_dependencies`, `estimates`, `orders`, `order_revisions`,
  `garment_jobs`, `job_dependencies`, `job_ready_state`, `measurement_snapshots`, `design_snapshots`,
  `outbox_messages`, `inbox_messages`. Verify with
  `grep -rho 'ToTable("[a-z_]*"' src/Modules/Orders/ | sort -u`, which is the list above and nothing else.
  **`job_ready_state` is the ready-gate's persisted evaluation and is an asset in its own right** — it is what an
  attacker would write to in order to make a job look ready, so `order-workflow.md` must carry it in section 3 and
  give it a STRIDE row of its own.
- `Orders.Contracts/` — the richest part, and **already consumed by Billing**: events `EstimateIssued`,
  `OrderConfirmed`, `OrderRevised`, `OrderCancelled`, `GarmentJobCreated`, `GarmentJobCancelled`,
  `GarmentJobEnteredProduction`, `GarmentJobHoldEvents`, `GarmentJobReadyForDelivery`, `GarmentJobRescheduled`; reads
  `IOrderSnapshotQuery` and `GarmentJobSnapshot`; and `Alterations/IAlterationRequests.cs`, declared with no
  implementation. The model's trust boundaries must treat these as the only sanctioned crossing (ARCH-004,
  ARCH-010).
- The open sub-issues that will build the surface, and that each control must be mapped to: #199 (drafts and intake
  endpoints), #201 (estimates and confirmation into immutable job cards), #204 (the intake screens).

**Custody and Inventory are scaffolding.** Model the design and say so.

- `src/Modules/Custody/` and `src/Modules/Inventory/` each hold seven files — an `AssemblyReference.cs` per project
  plus the endpoint stub and the registration extension. No `DbContext`, no migration, so neither the `custody` nor
  the `inventory` schema exists in the database.
- The permission catalogues, however, are **already seeded**:
  `src/Platform/Tailor360.Platform.Security/Permissions/CustodyPermissions.cs` declares `custody.scan`,
  `custody.generate_identity`, `custody.print_label`, `custody.bulk_print_label`, `custody.reprint_label`,
  `custody.invalidate_label`, `custody.verify_label`, `custody.manual_lookup`, `custody.transfer_out`,
  `custody.receive`, `custody.reject_transfer`, `custody.dispatch`, `custody.confirm_delivery`,
  `custody.record_delivery_outcome`, `custody.reconcile`, `custody.approve_reconciliation`, `custody.open_case`; and
  `InventoryPermissions.cs` declares `inventory.manage_items`, `inventory.manage_locations`,
  `inventory.manage_suppliers`, `inventory.manage_reorder_rules`, `inventory.record_movement`,
  `inventory.stocktake`, `inventory.approve_variance`, `inventory.approve_negative_stock`,
  `inventory.view_reports`, `inventory.view_valuation`. **No endpoint declares any of them yet** — that asymmetry is
  itself worth a row in each model.
- The design to model against: `docs/architecture/invariants.md` sections 4.5 (Order and Estimate), 4.6
  (GarmentJob), 4.7 (BarcodeIdentity), 4.8 (CustodyEvent — scans, transfers and reconciliation cases), 4.9
  (StockLedger — items, entries, balances and reservations) and section 6 (invariants that cross aggregates);
  `docs/prd/state-transitions.md`; `docs/prd/workflows/`; `docs/architecture/module-ownership.md` for the tables
  each module owns; `docs/nfr/data-classification.md` sections 5.7, 5.8 and 5.9.
- The sanctioned cross-module mechanisms these three flows depend on, fixed by plan Section 6.2 notes 2, 3 and 10
  and **not yet in the tree** — `grep -rl IConfirmationParticipant src/` finds nothing: the confirmation-participant
  hook (#32a), `Custody.Contracts.ICustodyStateQuery` with its placeholder returning `Unknown` (#34), and
  `Billing.Contracts.IDispatchEligibilityQuery` whose default **fails closed** with
  `Eligible = false, Reason = NotEvaluated` (#37). A model that assumes any of them exists today is wrong.
- `docs/nfr/traceability.md` rows that belong to these flows: `NFR-SE-13` (no personal data in a barcode payload),
  `NFR-CB-03` (manual entry always available, reason required, audited), `NFR-AU-01` and `NFR-AU-04` (audit of state
  changes and of denials).

**Shared, and not to be re-derived**: `docs/templates/threat-model.md`; `docs/security/abuse-cases.md` with the
`ABF-01` to `ABF-11` families (the first sub-issue of this parent) — `ABF-03` workflow bypass, `ABF-04` barcode
replay and `ABF-06` stock manipulation are this sub-issue's centre of gravity;
`docs/security/permission-matrix.md`; `docs/adr/0007-branch-aware-single-tenancy.md`;
`docs/adr/0008-transactional-outbox-and-workers.md`.

## What is missing

- Three of the ten files of `docs/security/README.md` section 3.2. Until they exist, every W3 and W4 issue in this
  area starts under DOR-05's stand-in clause, and DoD 6 has no model for its pull request to reference.
- Nothing writes down, in one place, why the workflow cannot be advanced by calling the API in a different order —
  the invariants document states the rules, but not the threat, the attacker or the test.

## Scope

- [ ] Add `docs/security/threat-models/order-workflow.md` from `docs/templates/threat-model.md`, covering: draft
      intake, the measurement and design snapshot copy, estimate issue and acceptance, confirmation into immutable
      garment job cards, revision, cancellation, hold and resume, phase transitions and assignment, the ready gate
      and its predicates, rework and alterations.
- [ ] Add `docs/security/threat-models/barcode-custody.md`, covering: identity allocation inside the confirmation
      transaction, the payload format, label render, print, reprint and invalidation, the scan surface (camera,
      keyboard wedge, manual entry), transfer out, receive and reject, the dispatch gate, delivery confirmation and
      the reconciliation case.
- [ ] Add `docs/security/threat-models/inventory.md`, covering: item, unit-of-measure, supplier and location
      masters, the immutable stock ledger and its entries, balances and reservations, stocktake and variance
      approval, negative-stock approval, reorder rules, valuation reads, and customer-owned material custody.
- [ ] All three: every template section filled, every "(example — delete)" row and the "How to use" section removed.
- [ ] Section 2 of each model states the build state honestly — which entry points exist (none, for all three
      today), which are designed and by which issue, and that a control mapped to an unbuilt entry point is a
      **requirement on that issue**, not a claim about today.
- [ ] Section 6 of each model works the diagram element by element and records the negative answers too ("not
      applicable, because …"), as the template requires.
- [ ] `order-workflow.md` and `barcode-custody.md` between them model the two fail-closed defaults by name:
      `IDispatchEligibilityQuery` returning `Eligible = false, Reason = NotEvaluated` until #43 registers the real
      rule, and the `CustodyReconciled` predicate resolving through `ICustodyStateQuery`'s `Unknown` placeholder and
      staying disabled by configuration until #37 supplies the real query. Both are controls with an owning issue,
      not assumptions.
- [ ] `barcode-custody.md` states the `NFR-SE-13` requirement as a control with its test: an opaque namespaced
      payload with a check character and **no** customer, order or measurement information, proved by a test that no
      personal field can reach the payload builder (#190 owns the implementation, #35 the requirement).
- [ ] `inventory.md` models the ledger's immutability as the control that defeats `ABF-06`: a correction is a new
      compensating entry, never an update or a delete, per `docs/architecture/invariants.md` section 4.9.
- [ ] Section 7 of each model cites the `ABF-nn` family for every abuse case; section 8 names the test that fails if
      each control is removed, or says "no test yet — #NN"; section 9 carries a residual-risk table with rating,
      owner and acceptance date, and any **high** risk also gets a `docs/process/waivers.md` entry with an expiry and
      a corrective issue.
- [ ] Section 10 of each model: the ASVS requirements the flow is judged against, from
      `docs/security/asvs-traceability.md`; if that sheet has not merged, record its four-value status vocabulary and
      name the sub-issue that fills the rows. Invent no requirement number.
- [ ] Update `docs/security/README.md` sections 2 and 3.2 for these three flows, with this sub-issue's number and
      the date.
- [ ] Versioned configuration (DOR-11): none added. Where a control is configuration — the ready-gate predicate
      enable flag, the variance threshold, the reorder rule — the model names the key and says the **value** is the
      owning issue's and the Owner's, citing **OD-24** for the cashier variance threshold and **OD-05** for
      valuation.

## Out of scope

The authorisation model (#32a), the customer, media, link, billing, payment, reports, integration and deployment
flows (the other **three** model sub-issues of this parent), and every implementation: #199, #201, #204 (orders), #190, #191,
#194, #197 (barcodes and labels), #193, #195, #196, #198 (inventory), #33 to #37, #40. Quality control is modelled
only as far as the workflow gate it blocks — the QC vocabulary itself does not exist in `catalog` yet (no
`qc_checklist_templates`, `qc_criteria` or `defect_codes` table) and the model records that as a gap against the
issue that builds it. No test, no source file and no configuration change in this pull request.

## Acceptance criteria

- [ ] Three files exist, are complete against the template (no placeholder row, no "How to use" section) and are
      reachable from `docs/security/README.md` sections 2 and 3.2, which name this sub-issue.
- [ ] Each model's section 5 lists the entry points that will exist, with the permission key each must declare —
      drawn from `OrdersPermissions.cs`, `CustodyPermissions.cs` and `InventoryPermissions.cs` as they are seeded
      today — and states that none is published yet, which `docs/api/openapi.v1.json` confirms.
- [ ] Every asset in section 3 of each model carries one of the seven classes of
      `docs/nfr/data-classification.md` section 2 and points at the inventory subsection (5.7, 5.8 or 5.9) that
      governs it. Nothing is reclassified.
- [ ] Every STRIDE row ends in a control or a residual risk; every abuse case cites an `ABF-nn` family; every
      control names a test or says "no test yet — #NN".
- [ ] **Negative case, workflow bypass (`ABF-03`)**: each model carries at least one row for advancing state by
      calling the API directly out of order — confirming an order whose estimate was never accepted, dispatching a
      job that is not ready, receiving a transfer nobody sent, recording a movement against a closed stocktake —
      with the server-side check that refuses it and the refusal's exact shape (an RFC 9457 problem-details
      response, never a stack trace, per CLAUDE.md security rule 3).
- [ ] **Negative case, barcode replay (`ABF-04`)**: `barcode-custody.md` models the same label scanned twice, a
      photographed label scanned by another branch, and an invalidated label re-presented, each with the control
      and the test that must exist.
- [ ] **Negative case, stock manipulation (`ABF-06`)**: `inventory.md` models a backdated entry, a negative balance
      forced through a movement, and a variance approved by the person who recorded it, each with the control.
- [ ] **Exception case**: each model documents the sanctioned way out and what it must not do — a label reprint and
      an invalidation (mandatory reason, audited, old label dead: #197), a manual barcode entry (`NFR-CB-03`:
      always available, reason required, audited), an approved dispatch exception (Billing-owned, never read
      directly by Custody, per plan Section 6.2 note 2), and a compensating ledger entry (never an update or a
      delete).
- [ ] No invented number: no promise-date rule, variance threshold, reorder point, retention period or rate limit is
      written as a figure. Each cites its open decision (**OD-05**, **OD-06**, **OD-08**, **OD-09**, **OD-24**) and
      the default in force.
- [ ] `./scripts/dev docs` passes.

## Data and classification

This sub-issue adds three documents; it adds no field, table, event, log field, telemetry attribute or export
column. Each model's own section 3 is where DOR-06 is answered for its flow, against
`docs/nfr/data-classification.md` sections 5.7 to 5.9.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| `docs/security/threat-models/order-workflow.md` | Internal | The argument that the workflow cannot be advanced out of order, and the tests that keep it true | Lives with the repository; superseded, never deleted | Every contributor | Never logged; summarised into release evidence item 9 |
| `docs/security/threat-models/barcode-custody.md` | Internal | The argument that a garment's identity and its chain of custody cannot be forged, replayed or lost | As above | As above | As above |
| `docs/security/threat-models/inventory.md` | Internal | The argument that the stock ledger records what happened and cannot be rewritten | As above | As above | As above |
| Barcode payload format described in `barcode-custody.md` | Internal by construction | Show that the payload carries no personal data (`NFR-SE-13`) | As above | As above | The model describes the **format**, and uses an obviously synthetic example payload only. No real or plausible customer-derived value appears |
| Asset rows naming job, order or stock fields | Internal — identifiers and field names only | Say what is worth attacking | As above | As above | No customer name, telephone number, measurement or price from any real record; synthetic illustrations only (CLAUDE.md section 5) |

## Permissions and roles

No permission key is added, removed or changed, and the authorisation-matrix fixtures in
`tests/Tailor360.IntegrationTests/Authorization/matrix.yaml` are cited as evidence, not edited. All keys below
already exist in `src/Platform/Tailor360.Platform.Security/Permissions/`; the models cite the matrix row for each
rather than restating the grants.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `orders.intake`, `orders.estimate`, `orders.confirm`, `orders.revise`, `orders.cancel`, `orders.cancel_job`, `orders.hold`, `orders.resume`, `orders.reschedule`, `orders.assign`, `orders.phase_transition`, `orders.start_production`, `orders.record_qc`, `orders.open_rework`, `orders.complete_rework`, `orders.alteration_decide`, `orders.revise_design`, `orders.read` | branch | per the matrix | per the matrix | `cancel`, `reschedule`, `revise` and the rework pair carry a reason | per the matrix | **Exist** — `OrdersPermissions.cs`. `orders.confirm` and `orders.cancel` are the elevation rows of section 6 |
| `custody.scan`, `custody.generate_identity`, `custody.print_label`, `custody.bulk_print_label`, `custody.reprint_label`, `custody.invalidate_label`, `custody.verify_label`, `custody.manual_lookup`, `custody.transfer_out`, `custody.receive`, `custody.reject_transfer`, `custody.dispatch`, `custody.confirm_delivery`, `custody.record_delivery_outcome`, `custody.reconcile`, `custody.approve_reconciliation`, `custody.open_case` | branch, and the destination-branch scope for receive and reject (plan Section 4.6 `TransferScopeRequirement`) | per the matrix | per the matrix | `reprint_label`, `invalidate_label`, `manual_lookup` and `approve_reconciliation` carry a reason | per the matrix | **Exist** — `CustodyPermissions.cs`; **no endpoint declares one yet**, which the model records as a finding against #190 to #197 |
| `inventory.manage_items`, `inventory.manage_locations`, `inventory.manage_suppliers`, `inventory.manage_reorder_rules`, `inventory.record_movement`, `inventory.stocktake`, `inventory.approve_variance`, `inventory.approve_negative_stock`, `inventory.view_reports`, `inventory.view_valuation` | branch | per the matrix | per the matrix | `approve_variance` and `approve_negative_stock` carry a reason | per the matrix | **Exist** — `InventoryPermissions.cs`; same asymmetry, recorded against #193 to #198 |
| `billing.approve_dispatch_exception` | branch | per the matrix | per the matrix | yes | per the matrix | **Exists** — cited by `barcode-custody.md` as the **Billing-owned** exception path; Custody never reads `dispatch_exceptions` directly (plan Section 6.2 note 2) |

**OD-13** is open, so every grant cited is the documented 2026-09-06 default rather than an approved one; each model
says so once in section 5. **OD-04** (the payment rule for dispatch and who may approve an exception) is open and is
the decision `barcode-custody.md`'s dispatch-gate section rests on.

## Modules, contracts and events

Modules read: Orders (schema `orders`, owner of the thirteen tables above), Custody (schema `custody`, not created),
Inventory (schema `inventory`, not created), and Billing only through `Billing.Contracts`. No source file changes,
so no architecture rule can be violated by this pull request; the models **assert** ARCH-004, ARCH-005, ARCH-008,
ARCH-010, ARCH-017, ARCH-021 and ARCH-023 as controls and name the architecture test that enforces each.

Cross-module mechanisms the models must name exactly as CLAUDE.md section 3 sanctions them: the read contracts
`Orders.Contracts.IOrderSnapshotQuery`, `Custody.Contracts.ICustodyStateQuery`,
`Billing.Contracts.IDispatchEligibilityQuery`; the versioned outbox events of `Orders.Contracts`; the
confirmation-participant hook that #32a adds and #35 registers first; and the platform ports `IBarcodeRenderer`,
`IPdfRenderer` and `IPrintQueue` (all three declared today in
`src/Platform/Tailor360.Platform.Abstractions/Ports/`). **DOR-05**: this sub-issue is threat-model work and closes
three rows of `docs/security/README.md` section 3.2.

## Screens and states

Deleted: a documentation-only Lane C sub-issue publishes no screen. Where a control belongs to the client — the scan
screen's camera permission, the workboard's refusal to show another branch's jobs — the model names the owning issue
(#36, #204, #34) and the state it must not reach.

## Migration and rollback

No table, no column, no migration. `dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` reports no
outstanding change and that output is the evidence. The models describe the **designed** `custody` and `inventory`
tables; creating them belongs to #190 and #193 respectively, and each model says which issue owns which table.
Rollback is a revert.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-04** — the payment rule for dispatch, the partial-delivery policy and who may approve an exception | **Open** — needed before W4 | The dispatch gate **fails closed**: `IDispatchEligibilityQuery` returns `Eligible = false, Reason = NotEvaluated` and dispatch is refused unless an approved Billing-owned exception exists (plan Section 6.2 note 2). `barcode-custody.md` models that default, not a rule | W4 |
| **OD-09** — thermal label size and whether a QR code accompanies the Code 128 | **Open** — needed before W3 | The payload format is fixed in code and is **not** part of OD-09; the model states the format requirement (`NFR-SE-13`) and leaves the dimensions to #35 | W3 |
| **OD-05** — valuation method, rounding and the statutory retention period for GST records | **Open**; #147 shipped the rounding half under a documented default | Weighted average by default with FIFO optional; `inventory.md` names the valuation read as a control surface and fixes no method | W3 for rounding, W4 for valuation |
| **OD-24** — the cashier variance a close may carry without a reason | **Open** — raised 2026-09-12 by #161 | Zero: every variance is explained. `inventory.md` cites it for the stocktake-variance analogue and fixes no threshold | W4 |
| **OD-06** — branches at launch, with timezones and working calendars | **Open**; #25 shipped the register, calendar deferred to #33 | No promise-date or cut-off rule is modelled as a number; `order-workflow.md` names #33 as the first consumer | W1 exit gate (overdue) |
| **OD-08** — retention periods | **Open** | No period written; every retention cell reads "Open — OD-08" and names #57 as the enforcing issue | W1 exit gate (overdue) |
| **RG-OD-02** — who holds the security owner role | **Open** — needed before W2 | The Owner, with the technical reviewer, is the residual-risk owner in section 9 of all three models | W2 exit gate |
| **OD-13** — approved role-to-permission grants | **Open** | The twelve seeded roles and their default grants, cited as default | W1 exit gate (overdue) |

## Required verification

- [ ] Documentation link check: `./scripts/dev docs`, plus `python3 scripts/check-docs-links.py --self-test`.
- [ ] Path and symbol verification pasted into the pull request: every type, table, permission key, `ARCH-nnn` rule,
      `NFR-` row, invariant section and issue number cited in the three models exists today — `grep`/`ls` output.
      The asymmetries this sub-issue leans on must be shown, not asserted: `grep -rl IConfirmationParticipant src/`
      returning nothing, and zero endpoint registrations under `src/Modules/Orders`, `src/Modules/Custody` and
      `src/Modules/Inventory`.
- [ ] Template completeness: `grep -c "example — delete"` returns 0 for all three files and the "How to use" section
      is absent.
- [ ] Invariant cross-check: every invariant identifier cited from `docs/architecture/invariants.md` sections 4.5 to
      4.9 and section 6 exists, pasted as `grep` output — a model that invents an invariant identifier is worse than
      one that omits it.
- [ ] **DOR-13**: the `docs/nfr/traceability.md` rows these models become the evidence for are `NFR-SE-13`,
      `NFR-CB-03`, `NFR-AU-01`, `NFR-AU-04` and `NFR-SE-02`; no status changes, because a model is not a proof.
- [ ] **DOR-16**: Python 3 for the link checker and the .NET command line for the dry-run; `./scripts/dev doctor`
      records both.

## Evidence the pull request will carry

- [ ] The rendered diff of all three models, with section 6 (STRIDE), section 8 (controls and tests) and section 9
      (residual risk with owner and date) visible for each.
- [ ] The rendered diff of `docs/security/README.md` sections 2 and 3.2.
- [ ] `./scripts/dev docs` output and the link-checker self-test output.
- [ ] The `grep`/`ls` transcript proving every cited path, type, table, permission key and invariant exists, and the
      two "nothing here yet" greps above.
- [ ] `grep -c "example — delete"` showing zero placeholder rows.
- [ ] `migrate --dry-run` output showing no outstanding migration.
- [ ] Any **high** residual risk carried, as a diff of `docs/process/waivers.md` with an expiry and a corrective
      issue.

## Depends on

- The abuse-case catalogue sub-issue of this parent — provides `abuse-cases.md`, the `ABF-nn` identifiers and the
  template's family column. **Must be merged first.**
- The ASVS skeleton sub-issue of this parent — provides the section 10 vocabulary and requirement numbers; if it has
  not merged, section 10 records the vocabulary and names that sub-issue.
- Merged work it reads: #18 (`docs/architecture/invariants.md`, `docs/prd/state-transitions.md`,
  `docs/prd/workflows/`), #19 (`data-classification.md`, `traceability.md`), #22 (`README.md`, the template), #24
  (the permission catalogue and matrix), #41 and the Billing work already in `main` (the contracts
  `barcode-custody.md` cites), and the Orders domain and schema already in `main`.

**In-flight work in the same area**: #199, #201, #204 (orders), #190, #191, #194, #197 (barcodes), #193, #195, #196,
#198 (inventory). None touches `docs/security/threat-models/`. The only file shared with a sibling model sub-issue of
this parent is `docs/security/README.md`, where this one edits three rows of section 3.2 and three rows of section 2;
whichever merges second rebases, one row per flow.

Refs #56
