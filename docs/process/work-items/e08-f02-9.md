Sub-issue of #39. The plan's Lane B line for #39 asks for a "'Record material' panel on the job phase screen from #33
for issue/consumption/return/wastage against job + phase with reason and scanner-first item selection". **Neither host
exists**: #33 is open and the Orders survey shows no workflow, phase, assignment or QC type in `src/`, so there is no
job phase screen to put a panel on, and #36's scanner is open too. This child therefore builds the same command as
**one standalone screen a person can reach today**, with manual item selection, and leaves the panel placement and the
scanner to the issues that own them. The location-transfer form is `E08-F02-9b`: two write forms in one pull request
is two sessions' work, and the repository's own calibration says so — `CatalogVersionEditorRoute.tsx` is 994 lines for
one editor, and the parallel batch estimates one comparable write screen at 800 to 850 lines.

Checked: there is no `clients/pwa/src/routes/inventory/` directory, and
`clients/pwa/src/components/layout/roleNavigation.ts:78` points the Inventory Clerk at an `/inventory` route
`clients/pwa/src/app/router.tsx` never defines — a client 404 today.

## What is already built (do not re-build)

- `E08-F02-3` — `POST /api/v1/inventory/movements` (types `issue`, `consumption`, `return`, `wastage`, `adjustment`),
  `POST /api/v1/inventory/movements/approved-negative` with its step-up, `GET /api/v1/inventory/movements/{entryId}`,
  the refusal codes `inventory.insufficient-stock`, `inventory.nothing-issued-to-consume`,
  `inventory.negative-stock-blocked` and `inventory.reservation-not-drawable`, and `inventory.record_movement` as the
  one permission each route demands. **`inventory.lot-reference-required` is not one of them** — lot discipline
  belongs to the purchase receipt (`E08-F02-5`) and so does its screen (`E08-F02-10b`).
- `E08-F02-7` — `GET /api/v1/inventory/items/{itemId}/balances`, under `inventory.view_reports`, which is where the
  available figure in a refusal sentence comes from. Read the open decision about who may call it before you design
  the screen around it.
- `E08-F02-8` — the client Inventory read screens, `clients/pwa/src/inventory/stockApi.ts`, the
  `clients/pwa/src/i18n/messages/inventory.ts` family and the `/inventory` router section. **This child extends all
  three**; it creates none of them again.
- **#38's client foundation** — `E08-F01-5` (the feature folder, the section shell, the message family, the
  `/inventory` router section and the location master screens) and `E08-F01-6` (the item register) in the parallel
  batch. If they have not merged, read the items and locations through the endpoints their API module would call (`GET
  /api/v1/inventory/items` from #195, `GET /api/v1/inventory/locations` from #193) and put those two functions in
  `clients/pwa/src/inventory/stockApi.ts`, where that module will find them. Say in the pull request which files you
  had to create.
- `clients/pwa/src/auth/apiClient.ts` — **the only transport** (`clients/pwa/CLAUDE.md` section 6 forbids a second).
  `apiRequest` (`:324`) and `apiRequestVersioned` (`:353`); `ApiRequestOptions.idempotencyKey` is sent as
  `Idempotency-Key` (`IDEMPOTENCY_HEADER` at `:125`, set at `:291`) and `ifMatch` as `If-Match` (`:177`, `:294`). It
  re-authenticates, **renews step-up in place and replays the identical request** — which is what makes the
  `approved-negative` route usable from a screen without a bespoke challenge flow. The body is JSON only (`:369`, one
  `JSON.stringify`).
- `clients/pwa/src/admin/useAdminResource.ts` — the one read effect (abort, reload-keeps-value). Do not write a second
  data-fetching path; there is no TanStack Query client in the application.
- `clients/pwa/src/admin/RequirePermission.tsx` — renders an `EmptyState` with the message ids `admin.forbidden.title`
  and `admin.forbidden.body` for a caller without the key, and **never redirects**. It is not the `Forbidden`
  component in `components/states/`. It takes **exactly one** `permission` string (`:22-27`), which matters for the
  open decision below. Reuse it; write no guard.
- `clients/pwa/src/routes/catalog/CatalogVersionListRoute.tsx:164` and `:184`, and
  `clients/pwa/src/routes/admin/RoleDetailRoute.tsx:65`, `:123` — the established idempotency-key discipline on the
  client: `crypto.randomUUID()` minted **once** when the person commits to the action, held in state, and replaced
  only after a success, so a retry of a failed submit carries the same key.
- `clients/pwa/src/components/states/OfflineBlockedAction.tsx` — the component for a write that must not be queued,
  with its own stories. `clients/pwa/src/app/router.tsx:222-225` already records the rule it implements: "Every write
  here is online-only (`OfflineBlockedAction`) — billing, payment and inventory reconciliation are never queued (plan
  Section 4.6)." The other states are exported from `clients/pwa/src/components/states/index.ts`: `LoadingState`,
  `EmptyState`, `ErrorState`, `RetryableError`, `NetworkStatusBanner`, `StateRegion`, and `plainLanguageDetail` and
  `failureCauseForStatus` from `components/states/problemDetails.ts`.
- `clients/pwa/src/design-system/components/forms/` — `Field`, `TextField`, `Select`, `NumericStepper`, `RadioGroup`,
  `SegmentedControl`, `FormErrorSummary`, `DateField`, `Checkbox`, `Switch`, `TextArea`, each with a persistent
  visible label by construction. `clients/pwa/src/routes/admin/AdminReasonField.tsx` (42 lines) — the written-reason
  control for a transition the server demands a reason on; `inventory.record_movement` is declared `RequiresReason:
  true`, so this is the control to use rather than a plain text field.
- `clients/pwa/src/components/dialogs/ConfirmDialog.tsx`, with `confirmTiers.ts` beside it — the three confirmation
  tiers, for a posting that cannot be undone except by a compensating entry.
- **Formatting and parsing, and the two imports a session gets wrong here.**
  - `clients/pwa/src/i18n/formatters.ts` exports **no** free `formatQuantity`. It is a member of the `Formatters`
    interface (declared `:117`, implemented `:279`), with `formatNumber` (`:109`), `formatMoney` (`:111`),
    `formatDateTime` (`:126`) and `formatLongDate` (`:120`). Obtain the set with
    `formattersForLocale(useIntl().locale)` from `clients/pwa/src/design-system/components/forms/formatting.ts` — the
    one locale-aware accessor, which also maps the pseudo-locale back through `resolveLocale` — and then call the
    member. The built billing routes use `getFormatters()` with no argument (`OutstandingBalancesRoute.tsx:22`) and so
    format in the default locale; do not change them.
  - `clients/pwa/src/i18n/parseNumber.ts` exports **`parseDecimalString`** (`:35`), **`parseDecimal`** (`:94`),
    `parseInchInput` (`:109`) and `parseMeasurementToMillimetres` (`:144`). **There is no `parseNumber` function.** A
    typed quantity goes through `parseDecimal`, which reads the display locale's separators instead of trusting
    `Number(value)`.
- `clients/pwa/src/stories/journeys/InventoryStockEntry.tsx:264-310` — the designed "issue against a job" step, and
  the standing rule at `:33-38`: "every purchase carries its conversion in words: '2 rolls of 25 m — 50 metres into
  stock'. A clerk receiving in rolls and issuing in metres is where a stock ledger goes wrong silently." The
  conversion sentence is required on this form too. `clients/pwa/src/stories/fixtures/inventory.ts:12-13` carries the
  other lesson: a quantity is never a bare number, and the spoken unit rides beside the symbol because "'m' read aloud
  is not 'metres'". Both files are Storybook mocks over fixtures, not screens; leave them where they are.
- Testing and stories: `clients/pwa/src/auth/testing/fixtures.ts` (`stubFetch`, `jsonResponse`, `aCurrentUser`), and
  the route-test harness of `clients/pwa/src/routes/billing/TakePaymentRoute.test.tsx:1-40` — `MemoryRouter` +
  `AppIntlProvider` (from `clients/pwa/src/i18n/IntlProvider`) + `SessionProvider` + `RequireSession` +
  `ShellStatusProvider`, with `transport.route('GET /api/v1/me', …)` answering `aCurrentUser({ permissions: [...] })`.
  **`clients/pwa/src/design-system/testing/renderWithProviders.tsx` supplies only the intl provider and display
  preferences — a route rendered in it throws.** Stories go through `clients/pwa/src/admin/testing/storyTransport.tsx`
  (`storyJson`, `storyProblem`, `storyPending`, `withAdminApi`, `STORY_USER`, the `StoryRoutes` type). `JourneyFrame`
  in `journeys.stories.tsx` brings its own `MemoryRouter` and cannot be combined with `withAdminApi` — pick one.
  `expectNoHorizontalOverflow` (`clients/pwa/src/design-system/testing/`) already defaults to 320, 360, 768, 1024 and
  1280 px at 100% and 200% zoom; do not narrow it. `expectNoAccessibilityViolations` is
  `clients/pwa/src/design-system/testing/axe.ts:61`.

## Scope

- [ ] **`clients/pwa/src/routes/inventory/RecordMovementRoute.tsx`** at `/inventory/movements/new` — a
      `SegmentedControl` for the four shop-floor types (`issue`, `consumption`, `return`, `wastage`); item and
      location pickers; a quantity field with a unit selector that shows **the conversion in words** beneath it
      whenever the chosen unit is not the item's base unit; the garment job field, required for `issue`, `consumption`
      and `return` and optional for `wastage`; an optional phase field, shown as optional and explained in one line
      because phases arrive with #33; the reason control (`AdminReasonField`) for wastage and for any type where the
      server demands one; and a `ConfirmDialog` step that reads back type, item, location, quantity-with-unit and job
      before posting. **`adjustment` is deliberately not offered on this screen** — a correction needs the entry it
      corrects, which this form has no way to choose; it is reached from the ledger browser of `E08-F02-8` in a
      follow-up, and the API keeps it available meanwhile.
- [ ] **The refusals are rendered as sentences, not as codes** — `inventory.insufficient-stock` becomes what is
      available and what was asked for, taking the available figure from the balances read and **not** from the
      refusal, which deliberately carries no figure (`docs/api/conventions.md` section 4.3: a refusal never explains
      how to pass); `inventory.nothing-issued-to-consume` says that nothing has been issued to this job for this item;
      `inventory.reservation-not-drawable` says the reservation has already been resolved or is for something else;
      `inventory.negative-stock-blocked` says the branch does not allow a negative balance and that an authorised
      person can post it. Use `plainLanguageDetail` and `failureCauseForStatus` from
      `clients/pwa/src/components/states/problemDetails.ts`; add no second problem-rendering path. **Where the caller
      cannot read balances** (see the open decisions) the insufficient-stock sentence carries no figure at all rather
      than a guess.
- [ ] **The negative-stock path** — when the server refuses with `inventory.negative-stock-blocked` and
      `useCurrentUser().permissions` contains `inventory.approve_negative_stock`, offer a single explicit action that
      re-posts to `movements/approved-negative` with a mandatory reason and **the same `Idempotency-Key`**; the
      step-up challenge is handled by `apiClient` replaying in place, so the screen shows only its pending state. A
      caller without the key is told that somebody who can approve it must, and is offered nothing else. The client
      never decides the outcome: the server answers 403 `security.step-up-required` or 403 `security.forbidden` on its
      own terms (`tests/Tailor360.IntegrationTests/Authorization/matrix.yaml`, the `answers` block).
- [ ] **Online-only** — wrap the submit in `OfflineBlockedAction` so an offline attempt is refused with words and
      **never queued**, which is `A11Y-RJ-05` step 9 ("Attempt a posting **offline** and hear that it is blocked, not
      queued") and `router.tsx:222-225`'s rule.
- [ ] **One function in `clients/pwa/src/inventory/stockApi.ts`** for the movement post and one for the
      approved-negative post, both taking the idempotency key from the caller, plus the item, location and balance
      reads if `E08-F01-5` and `E08-F01-6` have not landed. No fetch in a component.
- [ ] **Router entry** in `clients/pwa/src/app/router.tsx`, wrapped in `<RequirePermission
      permission={INVENTORY_PERMISSIONS.recordMovement}>`, inside the `/inventory` section, in the fixed order the
      section establishes (3.2.3 Consistent Navigation); reorder nothing.
- [ ] **Messages** added to `clients/pwa/src/i18n/messages/inventory.ts` in both `inventoryEn` and `inventoryTa` —
      every label, every unit label, every refusal sentence, every confirmation line. The conversion sentence is **one
      parameterised message**, never assembled from fragments, so Tamil can order it differently. The type gate in
      `clients/pwa/src/i18n/en-IN.ts` fails `pnpm typecheck` when a key is present in one language and missing in the
      other, which is what proves Tamil was written in the same change.
- [ ] **Stories** in `clients/pwa/src/routes/inventory/inventoryMovementScreens.stories.tsx` via `withAdminApi`:
      loading (`storyPending`), the insufficient-stock refusal, the negative-stock refusal with and without the
      approval key, forbidden (a `STORY_USER` without `inventory.record_movement`), and a pseudo-locale story — six.
      **No offline story**: `OfflineBlockedAction` carries its own, and
      `clients/pwa/src/routes/catalog/catalogScreens.stories.tsx:27-33` records why a screen like this must not imply
      queueing.
- [ ] **Tests** in `clients/pwa/src/routes/inventory/InventoryMovementScreens.test.tsx` with the route harness above,
      `expectNoAccessibilityViolations` and `expectNoHorizontalOverflow` at its default widths.

## Out of scope

**The location-transfer form** — `E08-F02-9b`, which depends on this child for the shared form conventions. **The
"Record material" panel on the job phase screen**, which is #33's workboard surface and belongs to whichever child of
#33 builds it; this child's route is the same command on a screen that exists now, and the panel can call the same
`stockApi.ts` function later. **Scanner-first item selection by `S-` barcode** (`A11Y-RJ-05` step 2) — #36 owns the
scanner and the Custody resolve endpoint does not exist; the item is chosen from a picker here. **The `adjustment` and
correction path**, as above. The purchase order and receipt forms — `E08-F02-10` and `E08-F02-10b`. Reservations: the
reserve and release commands of `E08-F02-2` have no screen in #39 at all, because **FSR-01**'s default in force is
that a reservation is entered deliberately and the plan gives it no screen; if one is wanted it is a follow-up naming
FSR-01. The stocktake count screens are #40. Any offline queue — #51.

## Acceptance criteria

- [ ] An Inventory Clerk holding `inventory.record_movement` issues material against a garment job from
      `/inventory/movements/new` over HTTP, sees the conversion sentence when the entered unit is not the base unit,
      confirms, and lands on a result that names the posted entry; the same screen then records a consumption and a
      return against the same job.
- [ ] **Idempotency:** the key is minted once when the person commits, is unchanged across a retry of a failed submit,
      and is replaced only after a success — asserted by a test that fails the first request and inspects the
      `Idempotency-Key` header on the second.
- [ ] **Offline:** with the network reported offline, the submit is refused through `OfflineBlockedAction` with words,
      **posts nothing** and queues nothing; `A11Y-RJ-05` step 9 is walked and recorded in `docs/nfr/a11y-checklist.md`
      section 6.8.
- [ ] **Negative:** a caller without `inventory.record_movement` sees the `RequirePermission` empty state
      (`admin.forbidden.title` / `admin.forbidden.body`) and is **not** redirected; an `inventory.insufficient-stock`
      refusal renders as a sentence, with the available quantity when the caller may read balances and without any
      figure when they may not; an `inventory.nothing-issued-to-consume` refusal names the job and the item.
- [ ] **Exception:** the `inventory.negative-stock-blocked` refusal offers the approval action **only** to a caller
      holding `inventory.approve_negative_stock`; the re-post carries the same idempotency key and a reason; the
      step-up challenge is satisfied by `apiClient` replaying in place rather than by a second form; and a caller
      without the key is offered no action at all.
- [ ] **Exception:** a caller who holds `inventory.record_movement` but **not** the reads the pickers need sees the
      explanatory empty state named in the open decisions, not a screen of controls that all fail.
- [ ] **Accessibility:** every field has a persistent visible label; the confirmation reads type, item, location,
      quantity-with-unit and job before committing, which is `A11Y-RJ-05` step 3 as far as it can go today — that step
      says "job **and phase**", and the phase half is recorded as not walked because #33 has not published phases;
      `expectNoAccessibilityViolations` passes in each state; `expectNoHorizontalOverflow` passes at all its default
      widths; the error summary moves focus to the first invalid field.
- [ ] **Localisation:** every string is in both `inventoryEn` and `inventoryTa`; quantities render through
      `formatters.formatQuantity` and typed numbers parse through `parseDecimal`, so a Tamil-locale decimal separator
      is read rather than silently truncated — asserted by a test that types a locale-formatted decimal under `ta-IN`.
- [ ] `pnpm --dir clients/pwa lint`, `typecheck`, `test`, `format:check` and `build` pass, and `generate:api:check` is
      clean — the schema this child consumes was regenerated by `E08-F02-3`, not here.

## Data and classification

`docs/nfr/data-classification.md` section 5.9 — **Confidential**, not Internal.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Movement form input (item, location, quantity, unit, job, phase, reason) | Confidential | Post the movement | **Nothing is persisted client-side** — no draft in `localStorage` or `sessionStorage`, no resumable form. A half-typed movement is lost on navigation, deliberately: a stock posting replayed from a stale draft is the defect this avoids | The person entering it | Never in client telemetry. The request body is never logged (security rule 7) |
| The idempotency key | Internal | Make a retry safe | Component state only, for the life of the action | The client and the server | Safe to log as an identifier; it names an attempt, not a person |
| The reason the clerk types | Confidential | Explain a wastage or an approved negative posting | Stored server-side as the ledger entry's reason | As the entry | **Free text a person wrote**: never in telemetry, and never in a pull-request screenshot — the evidence uses synthetic text and says so |
| The garment job identifier | Confidential — identifier | Tie the movement to the work | Not persisted client-side | The person posting | Identifier only; never the customer behind it (security rule 8) |
| Balance figures shown beside the form | Confidential | Let the clerk see what is on the shelf before committing | Not persisted client-side | Holders of `inventory.view_reports` only | No quantity in client telemetry |

No cost figure appears anywhere on this screen: `unit_cost` is not on any payload this child reads.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `inventory.record_movement` | branch | no | no | yes | `branch_manager`, `tailor`, `inventory_clerk` | **Exists** — `InventoryPermissions.cs:28`, `docs/security/permission-matrix.md` line 209. The `RequiresReason` flag is why the reason control is `AdminReasonField` and not an optional note |
| `inventory.approve_negative_stock` | branch | yes | yes | yes | `owner`, `branch_manager` | **Exists** — `InventoryPermissions.cs:37`, matrix line 203. The client offers the approval action only when `useCurrentUser().permissions` contains it, and the **server** is what actually decides; the step-up is renewed in place by `apiClient` |
| `inventory.view_reports` | branch | no | no | no | `owner`, `branch_manager`, `inventory_clerk`, `auditor` | **Exists** — matrix line 211. Consumed here only for the balance panel and the refusal figure. **Note the Tailor does not hold it** |

**OD-13** is open, so every grant above is the documented default rather than an approved set. No matrix row and no
grant changes in this child.

## Modules, contracts and events

Client only: no `.csproj` is touched, and Inventory is reached over HTTP through `clients/pwa/src/api/schema.d.ts` as
`E08-F02-3` generated it. Orders is **not** reached: the garment job is typed or pasted as an identifier, because
Orders publishes no endpoint to look one up (`OrdersEndpoints.cs` maps an empty group) — see the open decisions.
DOR-05: `E14-F01a-4` holds the inventory threat model and `E14-F01b-3b` the stock-manipulation regression suite;
neither blocks this slice, and the client-side concern both cover here is that **no authority is inferred on the
client**.

## Screens and states

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| Record movement | `/inventory/movements/new` | `LoadingState` for the item, location and balance reads; the submit shows its own pending state | `EmptyState` when the branch has no active item or no location, pointing at the item and location admin screens | Refusals as sentences through `plainLanguageDetail`; a 500 renders `RetryableError` and keeps the input | `OfflineBlockedAction` on the submit — blocked, never queued | `RequirePermission` empty state, `admin.forbidden.title` / `admin.forbidden.body`; and the reads-missing empty state of the open decision below |

Quantities through `formatters.formatQuantity`, typed numbers through `parseDecimal`, instants through
`formatters.formatDateTime`. The conversion sentence is rendered text, not a tooltip, so a screen reader reaches it.

## Migration and rollback

No schema change: client-only. Rollback is the previous client bundle, which does not define this route; the command
remains available to the API's other callers.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **Which roles this screen can actually serve** — a real gap, found in the matrix rather than assumed | `inventory.record_movement` is granted to `branch_manager`, `tailor` and `inventory_clerk` (matrix line 209), but the three reads this form needs are not: balances need `inventory.view_reports` (line 211: `owner`, `branch_manager`, `inventory_clerk`, `auditor`), the item list needs `inventory.manage_items` and the location list `inventory.manage_locations` (lines 205 and 206: `branch_manager`, `inventory_clerk`). **The Tailor holds none of the three.** `RequirePermission` takes exactly one permission (`RequirePermission.tsx:22-27`), so the guard cannot express "all four" | Guard the route on `inventory.record_movement`, the write authority. A caller who passes the guard but holds none of the reads — today, exactly the Tailor — gets an `EmptyState` saying the recording screen needs the stock reads their role does not have, and that material for a job is recorded from the job's own screen when #33 ships it. **No new permission key is coined, no read is called that would 403, and the Tailor's route to recording consumption stays #33's panel.** Say this in the pull request: it is the one thing a reviewer will ask about | #24 closes the matrix, or #33 ships the panel — whichever comes first |
| **FSR-02** — consumption automatic at phase completion, or a deliberate movement | Proposed, to be confirmed (`docs/architecture/sequences/stock-reservation.md` section 8) | A deliberate movement on this screen. Nothing here reacts to a phase transition | #39 closes |
| **FSR-05** — negative stock blocked or allowed with approval, per branch | Proposed, to be confirmed (same section 8; **OD-13**), and `docs/prd/configurable-vs-fixed.md` line 71 is the source for it being configuration at all | Blocked. The approval action appears only after the server refuses, and only for a caller holding the key | #39 closes |
| **FSR-01** — what determines a reserved quantity | Proposed, to be confirmed | No reservation screen in #39 at all; the commands exist and nothing in the client calls them | #39 closes |
| **The garment job picker** | Blocked, not undecided | The job is entered as an identifier and validated only by the server. Orders has **zero endpoints** today (`src/Modules/Orders/…/OrdersEndpoints.cs` maps an empty group), so there is nothing to search; when #32's or #33's screens publish a job lookup, replacing the field with a picker is a small follow-up | #33's workboard |
| **The workflow phase on a movement** | Deferred, under `docs/process/definition-of-ready.md` section 5 — *criterion*: a job-linked movement names the phase it happened at; *reason*: `orders.job_phases` does not exist and #33 is open, so there is nothing to offer in a picker; *default in force*: the phase field is optional, free of a picker, and labelled as arriving with #33; *owner*: backend lead with the Tailor Master; *date recorded*: at this issue's creation; *closure*: #33 publishes phase identifiers, before the wave gate that accepts #33 | #40 reports consumption by phase |
| **#36 (scanner)**, **#33 (workboard)**, **#50 (design system)**, **#52 (Playwright)** | All **open** | Manual item selection; a standalone route; the components that exist in `clients/pwa/src/components/` and `src/design-system/` today; Vitest in jsdom plus a recorded manual walkthrough, because `tests/e2e` and `playwright.config` do not exist and `./scripts/dev test e2e` advertises a tier #52 delivers. **No end-to-end test is promised by this child** | Each in its own issue |

## Required verification

- [ ] Vitest: the happy path; the conversion sentence appearing only for a non-base unit; the idempotency-key
      discipline across a failed retry; each refusal rendering as a sentence; the approval action's presence and
      absence by permission; the offline block posting nothing; the forbidden state; the reads-missing empty state.
- [ ] Accessibility: `expectNoAccessibilityViolations` per state; `expectNoHorizontalOverflow` at its default widths;
      focus moving to the first invalid field from the error summary; a manual screen-reader walk of `A11Y-RJ-05` step
      3 (partial, phase excluded) and step 9, recorded in `docs/nfr/a11y-checklist.md` section 6.8.
- [ ] Storybook: the six stories named in Scope, each rendering the real route, with the one-line reason there is no
      offline story.
- [ ] `pnpm --dir clients/pwa lint`, `typecheck`, `test`, `format:check`, `build`, `generate:api:check`.
- [ ] DOR-13: the `docs/nfr/traceability.md` rows touched are **NFR-CU-05** (line 198, "billing, payment and inventory
      reconciliation are online-only with an explicit blocked state" — the `OfflineBlockedAction` test is the unit
      half; its end-to-end half is #51's) and the accessibility and localisation rows for a new screen. No performance
      budget is claimed; #52 owns those.
- [ ] DOR-16: `./scripts/dev doctor`; the client tier needs only `pnpm install`, so it runs anywhere.

## Evidence the pull request will carry

- [ ] Screenshots at 360, 768 and 1280 px in English and Tamil, including the conversion sentence and one rendered
      refusal — with **synthetic** reason text only.
- [ ] A capture of the retried submit showing the same `Idempotency-Key` on both attempts.
- [ ] A capture of the offline attempt showing nothing posted and nothing queued.
- [ ] The accessibility run, the overflow run, and the filled `A11Y-RJ-05` step 3 and step 9 rows, with the phase half
      marked not walked and why.
- [ ] The story list, and `pnpm lint`, `typecheck`, `test`, `format:check`, `build` and `generate:api:check` output.
- [ ] A note naming which foundation or picker files this child had to create because `E08-F01-5` or `E08-F01-6` had
      not merged, if any.

## Depends on

`E08-F02-3` — the movement command, the approved-negative route and the refusal codes. `E08-F02-7` — the balances read
the refusal sentences draw their figures from. `E08-F02-8` — the client Inventory read screens, `stockApi.ts`, the
message family and the `/inventory` router section this child extends. None merged; this child follows all three.

#38's client children `E08-F01-5` and `E08-F01-6` in the parallel batch — the item and location pickers' lists. Not
filed as issues yet; the fallback is in Scope.

#33, #36, #50, #52 — all **open**, all named above as open, none depended on.

Contended files: `clients/pwa/src/app/router.tsx`; `clients/pwa/src/i18n/messages/inventory.ts` and
`clients/pwa/src/i18n/messages/index.ts`; `clients/pwa/src/inventory/stockApi.ts` and
`clients/pwa/src/routes/inventory/` — every one shared with `E08-F02-8`, `E08-F02-9b`, `E08-F02-10`, `E08-F02-10b` and
#38's six client children. `clients/pwa/src/api/schema.d.ts` is generated; do not hand-edit it.

Refs #39
