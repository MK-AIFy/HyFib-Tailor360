Sub-issue of #60. The second half of the parent's sixth implementation step and all of its seventh: the recoveries
where the machine, the key or the trust is gone rather than the data — "site failure, secret compromise,
ransomware-like event" and "PostgreSQL major upgrade" — plus "define primary/alternate owners, communications,
decision points, DNS/certificate/provider dependencies and return-to-primary steps", and the parent's third required
verification, "access, immutability and disaster-dependency review". It is split from child [E15-F02-5] because the
two sets answer different questions with different evidence: that child recovers **data** with a command, this one
recovers a **service** with a decision, and its hardest content is the dependency list — what the system needs that
is not in the system, which nothing in the repository records today. `grep -rn "return-to-primary\|registrar\|ACME
token\|break-glass" docs/` finds the phrases only in `docs/IMPLEMENTATION_PLAN.md` and `docs/nfr/slo.md`'s cost and
governance tables, never as a procedure.

## What is already built (do not re-build)

- Child **[E15-F02-5]**, merged first: `docs/runbooks/README.md` (the index and its rules) and
  `docs/runbooks/_template.md`. **Extend the index; do not restructure it, and do not write a second template.**
  The six data-recovery runbooks it added are the ones this slice's documents hand off to — a site failure ends in
  `restore.md`, a secret compromise may end in `point-in-time-recovery.md`.
- Children **[E15-F02-1]** to **[E15-F02-3]**: the repository, the object-locked bucket with its three identities
  (application, archiver with no delete, restore read-only) and the drill. `infra/backup/policies/` is the artefact
  the access review reads; it is committed precisely so the review can be a diff.
- `docs/nfr/slo.md` — section 5 (failure modes per hosting model, including line 264: "The machine, the building or
  the line is lost — **No automatic failover exists.** Recovery is rebuild-and-restore per section 6.1"), section 6.1
  (the RTO decomposition and the condition attached to the four-hour target: a **pre-agreed replacement host**,
  without which "Model A's realistic RTO is hardware procurement time"), section 8 (the operating model: a named
  operator **and** a deputy who has completed one restore exercise; UPS check, disk health, a spare disk, a router
  and line owner, "someone who can be at the machine within the RTO"; cloud account governance — billing owner, MFA,
  break-glass credentials escrowed, quarterly console access review), and section 9 (what is paid for, which is where
  the off-site bucket and the replacement host appear as money).
- `docs/nfr/security-operations-targets.md` — section 5's rotation table (line 157: the five database role passwords,
  **180 days proposed**; line 159: the backup repository cipher passphrase, **12 months**, "escrowed in the owner's
  password manager or KMS, **never only on the VM**, never inside the backup set it protects"), line 181 (the
  **quarterly** access review of "production and infrastructure access: shell, deploy, backup bucket, cloud console,
  DNS and registrar, password-manager vaults", owned by the operations owner with the Owner, evidenced by "a dated
  list naming every human and machine identity"), section 6 (the incident process, severities and the two overriding
  rules — cite **section 6.2**, at whose end the two rules sit, rather than a line number: they sit beside a fenced
  mermaid block and move whenever it is edited) and section 6.4 (the per-situation immediate actions, including
  ransomware: "the recovery runbook restores onto a machine that never held the key file (#60); the object-locked
  bucket is what makes this survivable").
- `docs/platform/secrets.md` — the secret classes, the file-per-key convention, rotation and **what to do after a
  leak**. The secret-compromise runbook is the operational form of that document and must not contradict it; where
  the document is silent (the order in which to rotate, who revokes sessions first) the runbook fills the gap and
  says it is filling it.
- `infra/caddy/Caddyfile` and `infra/CLAUDE.md` section 4 — the proxy, and the **ACME DNS-01 block that is a
  deliberate placeholder**: enabling it needs the hostname and DNS-provider decision **and** a Caddy image built with
  that provider's module, because "the official image contains none, so `acme_dns` would fail at start-up". The
  certificate dependency of a site failure is therefore real and unresolved, and the runbook says so rather than
  pretending a certificate will simply reissue.
- `docs/dev/staging.md` section 9 (TLS and the hostname), section 10 (**where the virtual machine lives —
  undecided**) and section 12 (still owed: no VM, no `Dockerfile.cli`, no `tailor360-deploy`, no persisted Data
  Protection key ring, no per-role grants, no MinIO digests). Every one of those is a dependency of a real recovery
  and belongs in the dependency list.
- `docs/architecture/deployment.md` section 5 (the volume table: `caddy-data` holds the certificates, `minio-data`
  the objects), section 6 (secrets as files, and the table naming the backup cipher key and the Data Protection key),
  section 10 (release, promotion and rollback) and section 11 (the Kubernetes-ready path, which is where
  CloudNativePG is named as preferred if Kubernetes is adopted — relevant to the major-upgrade runbook).
- `docs/adr/0010-deployment-portability.md` — the egress list an on-premises deployment needs ("the malware-signature
  mirror, the backup bucket endpoint, the certificate-authority domain-validation provider, the observability backend
  and the container registry"). That list **is** the disaster-dependency list's starting point; do not invent a
  different one.

## What is missing

- No procedure for losing the machine, the building or the line, and no record of the pre-agreed replacement host
  that `docs/nfr/slo.md` section 6.1 makes a condition of the four-hour objective.
- No rotation order for a compromised secret, no statement of what a leaked backup passphrase means for backups
  already written under it, and no ransomware procedure — so the one control that makes ransomware survivable
  (restore on a machine that never held the key file) is written nowhere an operator would look.
- No dependency list: what the service needs that the service does not contain — DNS, registrar, ACME, the container
  registry, the password-manager vaults, the off-site bucket's account, the replacement hardware.
- No owners-and-communications matrix, and no return-to-primary procedure, so a recovery has no defined end.
- No access or immutability review procedure, although `docs/nfr/security-operations-targets.md` line 181 schedules
  one quarterly.

## Scope

- [ ] `docs/runbooks/site-failure.md` — the machine, the building or the line is lost. Written against
      `docs/nfr/slo.md` section 6.1's **six stage names verbatim**, because child [E15-F02-7] compares stage by stage
      and a paraphrase breaks the comparison: *Detect and confirm*, *Decide to rebuild rather than repair*,
      *Provision a replacement host*, *Restore the database and verify*, *Media, TLS, DNS and smoke test*,
      *Contingency* — with the proposed allowance of each cited (15, 15, 60, 90, 30, 30 minutes) and asserted as a
      promise by none of them. Within those stages: decide (is this a restart, a rebuild on new hardware, or a rented
      host), provision, deploy the promoted digests, restore, verify with the invariant queries, reopen, tell people.
      Carries the **condition** plainly: without a pre-agreed
      replacement host the first stage is procurement, and the runbook's own preconditions section is where that host
      is recorded once the Owner agrees one (**SLO-03**, open). States what is *not* lost — the repository, the
      images in the registry, the backup bucket — and what is, if nothing was done: the `caddy-data` certificates and
      the Data Protection key ring, both named in `docs/architecture/deployment.md` section 5 and 6.
- [ ] `docs/runbooks/secret-compromise.md` — one secret, or the whole set. A table with one row per secret class from
      `docs/platform/secrets.md` and `docs/nfr/security-operations-targets.md` section 5: what it reaches, what to do
      first, what to rotate next, and what the compromise means for data already written. The two rows people get
      wrong are written out in full: the **backup repository passphrase** — every backup encrypted under it must be
      treated as readable by the holder, the response is to re-encrypt going forward and shorten the retention of the
      exposed set, per the rotation table's own "Treat every backup as readable by the holder" cell — and the **Data
      Protection key-encryption key**, whose loss invalidates every session cookie and protected payload. Session
      revocation, credential reset and audit review come from section 6.4's first row; the runbook orders them.
- [ ] `docs/runbooks/ransomware.md` — the procedure section 6.4 promises. The ordering is the content: isolate before
      investigating; assume every credential the host held is compromised, including the archiver's; **restore onto a
      machine that never held the key file**, fetching the passphrase from escrow; verify the audit chain against the
      anchored head in the locked bucket rather than against the database's own head; and the statement of why it
      works — object lock means the archiver could not delete history even with its credential
      (child [E15-F02-2]'s policy is the citation). Names the one thing that must never be done: paying or
      negotiating is not a technical decision and is not in this document.
- [ ] `docs/runbooks/postgresql-major-upgrade.md` — the maintenance-window logical upgrade of plan #60: rehearse in
      the restore environment first, upgrade into the new-version container, **new pgBackRest stanza and a full backup
      before traffic returns**, and the CloudNativePG preference if Kubernetes is ever adopted
      (`docs/architecture/deployment.md` section 11). Cites the lifecycle dates already in the plan's #59 entry —
      .NET 10 LTS and PostgreSQL 16 support end November 2028 — rather than inventing a schedule, and says the
      upgrade is a planned change under the ordinary release path, not an incident.
- [ ] `docs/runbooks/roles-and-communications.md` — the owners and communications matrix the parent's seventh step
      asks for: primary and alternate owner **by role** for each runbook, who is told at each severity, through which
      channel, what a customer-facing message may and may not say, the decision points that are the Owner's rather
      than the operator's (reopening an environment with known data loss; accepting a partial recovery; declaring a
      disaster), and **return to primary** — how a service running on replacement infrastructure goes home, including
      re-pointing DNS, re-issuing certificates, re-creating the stanza and taking a full backup before the temporary
      host is destroyed. Severity definitions are **linked, never restated**
      (`docs/nfr/security-operations-targets.md` section 5).
- [ ] `docs/runbooks/disaster-dependencies.md` — the list, as a table: every external dependency a recovery needs,
      who holds it, where the credential lives, what breaks without it, and how it is verified (a quarterly check
      that the credential still works is the only verification that means anything). Rows come from
      `docs/adr/0010-deployment-portability.md`'s egress list, `docs/nfr/slo.md` section 8's governance rows and
      `docs/dev/staging.md` sections 9, 10 and 12 — DNS and registrar, the ACME provider and the Caddy image that
      carries its module, the container registry and the image digests, the backup bucket's provider account and its
      break-glass credentials, the password-manager vaults, the off-site copy's destination, the replacement host, the
      physical duties of section 8 (UPS, spare disk, router and line owner). **No credential value, ever** — the row
      records custody, not content.
- [ ] `docs/runbooks/backup-access-review.md` — the quarterly review of
      `docs/nfr/security-operations-targets.md` line 181, made executable: the identities to enumerate (the three
      object-storage identities of child [E15-F02-2]; the database roles, where the documents **disagree** and the
      review must say so rather than pick one — `docs/platform/database.md` line 27 says "Four roles rather than one"
      and tables `t360_migrator`, `t360_app`, `t360_reporting`, `t360_backup` (the last at line 35, "`SELECT` plus
      replication"), while `docs/nfr/data-classification.md` line 555, `docs/nfr/security-operations-targets.md` line
      157 and `infra/compose/.env.example` all list a fifth, `t360_retention`; and **none of the four or five exists
      yet**,
      because every process still connects with the same connection string until #21 lands (`docs/dev/staging.md`
      section 12 item 6), so the first review records two findings here rather than ticking a row;
      shell and deploy access, the cloud console, DNS and registrar, the vaults), the **immutability checks** —
      object lock still set with the expected retention, the lifecycle rule still present and still an expiry, the
      archiver still refused a delete (the three refusals child [E15-F02-2] recorded are re-run here, which is what
      makes this a check and not a reading), and the escrow checks — the passphrase and the key-encryption key are
      still retrievable from escrow by someone other than their author, which is the only way to discover that an
      escrow entry was never made. Output: "a dated list naming every human and machine identity", exactly as line
      181 requires, as a record form.
- [ ] `docs/runbooks/README.md` — the index extended with these seven, each with its severity, its expected duration
      and the alerts that should link to it; and one line added to the index's rules: a recovery is not over until
      `roles-and-communications.md`'s closing steps are done.
- [ ] One cross-link from `docs/platform/secrets.md` to `secret-compromise.md` and one from
      `docs/nfr/security-operations-targets.md` section 6.4 to `ransomware.md`, so the documents that promise these
      procedures point at them. Both are narrow, additive edits — a link, not a rewrite.

Versioned configuration, not code (DOR-11): documentation only. The record forms are markdown templates; the completed
records are child [E15-F02-7]'s.

## Out of scope

- The six data-recovery runbooks, the index and the template: child **[E15-F02-5]**, merged first.
- **Executing** any of these: child **[E15-F02-7]** runs the quarterly disaster exercise and the first access review
  and files the records. Nothing here claims a review happened.
- The mechanisms: children **[E15-F02-1]** to **[E15-F02-4]**. This slice issues no new policy and changes no bucket.
- Incident severity, escalation targets, the post-incident review and the alert channel: already in
  `docs/nfr/security-operations-targets.md` sections 5 and 6 and **#58**. Linked, not restated.
- The infrastructure-as-code that would make "provision a replacement host" a command rather than a procedure:
  **#59**. This runbook describes the manual route and names #59 as what replaces it, the way `infra/CLAUDE.md`
  section 6 already does for `tailor360-deploy`.
- The deployment threat model itself: **#56a**, as child **[E14-F01a-6]** (`docs/security/README.md` section 3.2
  gives it wave W2). This slice's dependency list and residual risks are inputs to it, not a substitute for it.
- Choosing the replacement host, the DNS provider or the telemetry backend: **OD-02**, **OD-14** and **SLO-03** are
  the Owner's. The runbooks carry the slots and say what is missing; they do not decide.
- A waiver for anything. `docs/process/waivers.md` is for a gate that cannot be met, not for a decision the Owner has
  not taken — an unsigned **SLO-03** is recorded as an open decision with a default, not waived.

## Acceptance criteria

- [ ] Each of the seven documents follows `docs/runbooks/_template.md` with every section present, and each names a
      primary and an alternate owner by **role**.
- [ ] `site-failure.md` states, in its preconditions, that the four-hour objective depends on a pre-agreed
      replacement host (`docs/nfr/slo.md` section 6.1) and carries the slot where that host is recorded — empty, with
      **SLO-03** named, rather than filled with an assumption.
- [ ] `secret-compromise.md` has a row for every secret class in `docs/platform/secrets.md` and
      `docs/nfr/security-operations-targets.md` section 5, with no row reading "rotate it" and nothing else: each says
      what the compromise means for data already written.
- [ ] `ransomware.md`'s restore step is on a machine that never held the key file, the passphrase comes from escrow,
      and the audit chain is verified against the **anchored** head in the locked bucket — the three facts
      `docs/nfr/security-operations-targets.md` section 6.4 and `docs/nfr/traceability.md` **NFR-BR-07** require.
- [ ] `disaster-dependencies.md` has a row for every item in `docs/adr/0010-deployment-portability.md`'s egress list
      and every governance row of `docs/nfr/slo.md` section 8, each with a holder, a custody location and a
      verification — and **no credential value anywhere**, which a reviewer can check by reading.
- [ ] `backup-access-review.md` produces "a dated list naming every human and machine identity", as
      `docs/nfr/security-operations-targets.md` line 181 specifies, and includes the three archiver refusals as
      **re-run checks** rather than as references to an earlier run.
- [ ] **Negative — a recovery cannot quietly reopen a damaged environment:** `roles-and-communications.md` makes
      "reopen with known data loss" an Owner decision with a recorded rationale, and `site-failure.md` forbids
      reopening before the invariant queries of child [E15-F02-3] pass. Both are written as refusals, not as
      recommendations.
- [ ] **Negative — the runbooks do not assume the system is available to read them:** every document states the
      offline copy rule (printed or in the operator's password-manager vault, per
      `docs/runbooks/README.md`'s rules), because a site failure takes the repository's own host with it if that is
      where it was being read.
- [ ] **Exception — a dependency that does not exist yet is recorded as absent:** the ACME DNS-01 placeholder, the
      missing `tailor360-deploy`, the missing CLI image, the unpersisted Data Protection key ring and the two
      unpinned MinIO digests each appear in `disaster-dependencies.md` with the issue that closes them
      (`docs/dev/staging.md` section 12 items 2, 3, 4, 8 and 9). A dependency list that omits the broken dependencies
      is worse than none.
- [ ] `python3 scripts/check-docs-links.py` and `./scripts/dev docs` pass, including the two new inbound links.
- [ ] British spelling, prose wrapped at 120 columns.

## Data and classification

No new field, table, payload, log field or export column. The risk this slice carries is the opposite of a schema
change: it is the document most likely to have a credential written into it by a well-meaning operator, so the rules
are the classification (`docs/nfr/data-classification.md` section 6, `docs/platform/secrets.md`).

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| The seven documents | Internal | Recover a service, not only a database | With the repository | Anyone with repository access | Committed. They name variables, file paths under `/run/secrets`, vault entries and roles — never a value |
| The dependency list's rows | Internal | Know what the system needs that the system does not contain | With the repository, reviewed quarterly | Operations owner, Owner | A provider name and an account **reference** are Internal; an account number, a registrar login, an API token or a recovery phrase is **Credentials and secrets** and never appears. The row records who holds it and where |
| The access-review record form | Internal, evidentiary | Satisfy `docs/nfr/security-operations-targets.md` line 181 | The completed records are filed by child [E15-F02-7]; the form lives with the repository | Operations owner, Owner, Auditor (`docs/prd/raci.md` line 57 makes Auditor `I` on backup verification) | Identity **names** and what each may do; never a key, never a password, never a retention bypass token |
| Escrow locations (password manager, KMS) | Internal — the **location**; the contents are Credentials and secrets | Let a deputy retrieve a key the author is unavailable to provide | With the repository | Operations owner, Owner | The vault and entry name may be written down. The entry's contents may not, and the runbook says so where a reader would otherwise paste one |
| Customer-facing message wording | Internal until sent | Tell people the truth without creating a liability | With the repository | Owner, Branch Managers | Templates only, with synthetic examples. No real customer appears. External communication during a suspected exposure waits for the advice `docs/nfr/security-operations-targets.md` section 6.4 requires |
| The restored environment created during a rehearsal or a real recovery | **The union of every class** — section 5.20 | Recover | Destroyed when the recovery ends; the ransomware runbook additionally requires the temporary machine to be destroyed, not reused | The operator performing it | Never reachable from outside its own network; never seeded into another environment |

## Permissions and roles

No application permission key is added, used or changed; no endpoint, no payload, no client surface, so ARCH-007,
ARCH-008, ARCH-017 and ARCH-023 are not engaged, and `docs/security/permission-matrix.md` and
`tests/Tailor360.IntegrationTests/Authorization/matrix.yaml` are untouched. The authorisations recorded are
operational and come from documents that already exist: `docs/prd/raci.md` row 28 with footnote (20) (the restore test
is the operator's, the evidence is the Owner's accountability, the Auditor is `I`), `docs/nfr/slo.md` section 8 (a
named operator **and** a deputy who has completed one restore exercise; cloud-account governance with MFA and
escrowed break-glass credentials) and `docs/process/release-gates.md` section 2.2 (the operations owner is RG-13's
waiver authority for a staging-only release and has none for a production release). Two application permissions are
mentioned and unchanged: `admin.health.read` for backup age and `admin.audit.read` /
`audit.export` for the audit review a compromise triggers
(`src/Platform/Tailor360.Platform.Security/Permissions/PlatformPermissions.cs:41` and `:44`, both declared
`RequiresMfa: true` and `audit.export` additionally `RequiresReason: true`). **OD-15** is open, so every
owner is a role; **OD-13** is open, so the existing grants are as proposed as every other.

## Modules, contracts and events

No module, project, schema, contract or event is touched. The only edits outside `docs/runbooks/` are two inbound
links, in `docs/platform/secrets.md` and `docs/nfr/security-operations-targets.md`. The module-boundary rules are not
engaged; the verification is the link check and a tabletop walkthrough, not a test tier, and the pull request says so.

Threat-model coverage (DOR-05): the flow is "Deployment, secrets and the runtime", whose model
`docs/security/threat-models/deployment.md` is owed by **#56a** in wave W2 per `docs/security/README.md` section 3.2,
with `docs/platform/secrets.md`, `docs/adr/0010-deployment-portability.md` and `docs/process/branch-protection.md`
section 8 standing in — all three are cited here. This slice's `disaster-dependencies.md` is a direct input to that
model's asset list, and its residual risks — a compromised bucket owner can still change a lifecycle rule; one
operator and no out-of-hours cover (**RR-02**); an unsigned replacement-host condition (**SLO-03**) — are named in the
documents so the model inherits them rather than rediscovering them.

## Screens and states

Not applicable — no client surface. These documents must be usable when every screen is down, which is why the
offline-copy rule is one of the acceptance criteria rather than a nicety.

## Migration and rollback

No schema change and no migration; markdown and two links only. `migrate --dry-run` printing "The database is up to
date with this build." is the proof and CI's `migration-check` job confirms no model change.

Rollback is a revert, with the same caveat child [E15-F02-5] recorded: once #58 points a `runbook_url` at one of these
documents, deleting it breaks an alert, so a removal and its replacement travel in one pull request.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **SLO-03** Accept the Model A conditions, including the pre-agreed replacement host of `docs/nfr/slo.md` section 6.1 | **Open** — `docs/nfr/slo.md` line 426; risk **RR-02** at `docs/nfr/risk-review.md` line 81 | `site-failure.md` carries an empty, named slot for the replacement host and states that without it the first stage is procurement time. The document does not assert the four-hour objective, and records that the objective is unsigned | W5 exit gate |
| **OD-02** Hosting model and budget | **Open** — `docs/prd/assumptions-and-open-decisions.md` line 55 | Procedures are written for the Model A / A′ single-VM deployment ADR-0010 makes the baseline, with a "under a managed database and a managed object store this step is the provider's console" note wherever the two differ. The dependency list carries both columns, because the egress list of ADR-0010 applies either way | W0 exit gate |
| **OD-15** Operations ownership, alert channel and out-of-hours arrangement | **Open** — line 77; `docs/prd/raci.md` line 216 | Every owner, approver and escalation contact is a **role**. `roles-and-communications.md` is the document the names are written into when OD-15 closes, and it says so | W5 |
| **OD-14** Telemetry backend | **Open** — line 76 | The dependency list carries the observability backend as a row with its custody unfilled and OD-14 named | W5 |
| **DC-05** What the customer-facing deletion promise may say; section 8.4 external-communication advice | **Open** — `docs/nfr/data-classification.md` section 11 | The communications templates carry no promise about backups or deletion periods, and the suspected-exposure path waits for advice, exactly as `docs/nfr/security-operations-targets.md` section 6.4 requires | Before launch |
| **RG-OD-03** Whether RG-13's 7-day restore freshness is achievable | **Open** — `docs/process/release-gates.md` line 360 | `backup-access-review.md` reviews access and immutability quarterly, which is independent of the restore cadence; nothing here assumes the weekly drill runs | W5 exit gate |

## Required verification

Environment (DOR-16): `./scripts/dev doctor` — the link check and the structural review need only Python and a
reader, and **no criterion of this slice requires a Docker daemon.** Two pieces of *evidence* benefit from one
(re-running the three archiver refusals to prove the access-review procedure as written produces the evidence it
claims); where the session has none, those are recorded as not executed here, carried into child [E15-F02-7], and the
pull request opens for review anyway. A documentation slice is not held open waiting for hardware — and the escrow
check below almost certainly cannot be performed at all while OD-02 is open, which is itself the finding.

- [ ] **Documentation**: `python3 scripts/check-docs-links.py --self-test`, then the full run, and `./scripts/dev
      docs`.
- [ ] **Structural review, which always applies**: each of the seven documents diffed against `_template.md` with no
      missing heading, every owner a role, every severity linked rather than restated, and no credential value
      anywhere. Reviewer named.
- [ ] **Tabletop walkthrough, recorded**: `site-failure.md` and `ransomware.md` read against the current deployment by
      someone other than the author where possible, recording every step that cannot be performed today and why — the
      missing VM (`docs/dev/staging.md` section 12 item 1), the missing deploy script (item 3), the missing CLI image
      (item 2), the unpersisted Data Protection key ring (item 4), the unpinned MinIO digests (item 9). **The record of
      those gaps is the deliverable**, each gap becomes a row in `disaster-dependencies.md`, and this needs no daemon
      and no host: it is a reading exercise whose output is a list of things that do not exist.
- [ ] **Mechanical re-check, recorded where a daemon exists and stated as not run where it does not**: the three
      archiver refusals, the object-lock and lifecycle state, and the read-only restore policy, run as
      `backup-access-review.md` instructs — proving the procedure as written produces the evidence it claims. Without a
      daemon, the procedure is still reviewable by reading it against child [E15-F02-2]'s recorded refusals, and the
      pull request says which of the two happened.
- [ ] **Escrow check, recorded**: that someone other than the author can retrieve the repository passphrase and the
      Data Protection key-encryption key from escrow, **or** — if escrow has not been established, which is likely
      while OD-02 is open — a row in `disaster-dependencies.md` saying so with the issue that closes it. This is the
      check that discovers an escrow nobody ever made.
- [ ] **Review pass, recorded**: no credential value anywhere in the seven documents, every owner a role, every
      severity linked rather than restated. Reviewer named.
- [ ] Non-functional rows this slice moves (DOR-13): `docs/nfr/traceability.md` **NFR-BR-06** (line 264, the
      quarterly disaster-recovery exercise — the procedure half; the run is child [E15-F02-7], and that row's Status
      already reads "Evidence only; scheduled (#60)"), **NFR-BR-07** (line 265, keys escrowed separately, and the
      restore rehearsed on a machine that never held the key file), **NFR-BR-03** (line 261, the bucket policy review)
      and **NFR-OP-02** (line 248, every alert has a runbook and an owner). **Append to the Status cell; never replace
      what is there and never touch the Target column** — NFR-BR-03 reads "Proposed, to be confirmed; depends on
      **OD-05** and **OD-08**" and NFR-OP-02 reads "Target open — depends on **OD-15**", and neither open decision is
      this slice's to close.

## Evidence the pull request will carry

- [ ] A rendered diff of the seven new documents and the extended index.
- [ ] The tabletop transcript for `site-failure.md` and `ransomware.md`, with the list of steps that cannot be
      performed today and where each is recorded.
- [ ] The mechanical re-check output: the three refusals, `mc retention info`, `mc ilm rule ls`.
- [ ] The escrow-check result, or the dependency row that records its absence.
- [ ] The dependency table, rendered, beside `docs/adr/0010-deployment-portability.md`'s egress list, so a reviewer
      can see that every item is covered.
- [ ] `scripts/check-docs-links.py --self-test` and the full link check; `./scripts/dev docs`.
- [ ] `migrate --dry-run` output showing no schema change.

## Depends on

- #`<E15-F02-5>` — `docs/runbooks/README.md` and `_template.md`, and the six data-recovery runbooks these documents
  hand off to. Starting before it means two templates and two indexes.
- #`<E15-F02-2>` — the three object-storage identities and their committed policies, which the access review reads and
  re-checks, and the object lock that makes `ransomware.md` true.
- #`<E15-F02-3>` — the drill and the invariant queries, which `site-failure.md` requires to pass before an environment
  is reopened.

Named for sequencing, all open: **#59** (infrastructure as code, `tailor360-deploy`, the CLI and PostgreSQL image
build jobs — every "provision a replacement host" step is manual until it lands), **#58** (the alerts that link here,
and the observability row of the dependency list), **#56a** / child [E14-F01a-6] (the deployment threat model this
slice feeds), **#57** (the audit anchors `ransomware.md` verifies against, and the secret-compromise audit review),
**#23**-related work for the persisted Data Protection key ring (`docs/dev/staging.md` section 12 item 4), and
**#61**'s child [E15-F03-7], whose dress rehearsal executes these procedures as part of a release candidate. Shared
single-writer files: `docs/runbooks/README.md` (with child [E15-F02-5]), `docs/nfr/traceability.md`,
`docs/nfr/security-operations-targets.md` and `docs/platform/secrets.md` — the last two take a one-line link each and
nothing more.

Refs #60
