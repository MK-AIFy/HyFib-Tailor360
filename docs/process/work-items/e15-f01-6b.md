Sub-issue of #59. **EVIDENCE ONLY — NOT A CODING SESSION.** This child writes no production code and no test. It
carries the one piece of #59's required evidence that a coding agent cannot produce, because it needs a person at a
browser and a host that outlives a job: the rollback rehearsal `docs/nfr/traceability.md` **NFR-BR-08** is waiting for.

That row reads, in full: "**NFR-BR-08** | Rollback works: release N runs against a database at N+1, **with a client at
N+1 still cached in a browser** | Evidence: the rollback rehearsal record | RG-12, RG-14 | Technical reviewer | **Proof
scheduled (#59)**". The plan's #59 blueprint asks for the same thing in the same words — "roll back to N while the
database stays at N+1, verify health and a smoke journey **with PWA N+1 still cached in a browser**, roll forward".
Three facts make the browser half impossible for a coding session, and each was checked rather than assumed:

- there is no automated browser. `tests/e2e` and a Playwright configuration do not exist, and `scripts/dev` line 434
  says so in the repository's own words — "tests/e2e does not exist yet: the Playwright suite arrives with issue #52".
  **#52 is open.**
- nothing registers a service worker yet, so "still cached in a browser" is the browser's own HTTP cache plus the
  installed shell, not a service-worker precache: `clients/pwa/vite.config.ts` lines 38-39 record
  `injectRegister: null` and say "nothing registers a worker yet". **#51**, which owns the installable application and
  its update prompt, is open.
- no host exists. `docs/dev/staging.md` section 12 item 1: "**The virtual machine itself.** No host has been
  provisioned."

So the honest split is: **E15-F01-6** builds `scripts/tailor360-deploy` and proves every branch it can drive itself,
including the HTTP-observable half of NFR-BR-08 (release N answering a request that carries the N+1 client's
`X-Client-Version`); **this child** records what a person did with a browser, and nothing else. Filing it separately is
what stops a coding session either stalling on hardware it cannot have or pasting a screenshot it did not take.

## What is already built (do not re-build)

- **E15-F01-6**'s `scripts/tailor360-deploy`, `docs/runbooks/deploy.md` and `docs/runbooks/rollback.md` — the
  procedure this rehearsal follows. **Follow it exactly as written.** Where the runbook turns out to be wrong or
  incomplete, that is the most valuable thing this rehearsal produces: record it and raise it against
  **E15-F01-6**, rather than improvising a step and writing the improvisation down as the procedure.
- **E15-F01-6**'s measured interruption window, taken on the ephemeral host CI gave it. This child re-measures it on
  whatever host the rehearsal actually ran on and says which, because **NFR-AV-06**'s evidence is "deployment
  rehearsal record with the measured interruption window" and a number from a CI runner is not a number from the
  staging machine.
- **E15-F01-5**'s integration and CI proof that release N serves against a database at N+1 — `CanServe` true,
  `DatabaseIsAhead` true, `MigrationStateHealthCheck` `Degraded` rather than `Unhealthy`. The database half of
  NFR-BR-08 is already evidenced there; **do not re-derive it**, cite it.
- `src/Hosts/Tailor360.Web/Middleware/ClientVersionMiddleware.cs` and `ClientCompatibilityOptions` — the
  `X-Client-Version` handshake and the `426` refusal. `MinimumClientVersion` is `""` in `appsettings.json`, which means
  "answer every build", so the rehearsal's N host will answer an N+1 client by configuration as well as by rule. Say
  that, so the rehearsal is not read as proving more than it does.
- `docs/nfr/security-operations-targets.md` line 134 — the rule the rehearsal is testing: "`minimumClient` raised only
  in the release **after** the change that requires it; an outdated client receives `426`". Cite it; do not restate it.
- `docs/process/release-evidence.md` — the per-release form. This rehearsal's record is what fills its rollback row.
- `docs/dev/staging.md` section 11 — what the interim environment is for, and section 7.3 on reading what happened.

## What is missing

- The record. **NFR-BR-08** has read "Proof scheduled (#59)" since it was written and there is no rehearsal record
  anywhere in the repository — `docs/evidence/` does not exist and `docs/qa/` has no rehearsal record either.
- A number for **NFR-AV-06** measured anywhere but a CI runner.
- Any statement of which host the evidence came from, which is the thing that makes or unmakes all of it.

## Scope

This child's whole deliverable is **one record**, plus the issues it raises. It adds no script, no workflow, no test and
no configuration.

- [ ] Carry out the rehearsal on a real host, following `docs/runbooks/deploy.md` and `docs/runbooks/rollback.md`
      without improvising: deploy release **N+1** including its expand migration; open the application in a **real
      browser** and use it, so the N+1 client is genuinely in that browser's cache and, if it is installed, its
      installed shell; roll back to **N** with `scripts/tailor360-deploy`, leaving the database at N+1; then, **in the
      same browser session, without clearing the cache and without a hard reload**, complete one smoke journey; then
      roll forward to N+1 and confirm `migrate` applies nothing.
- [ ] Record it as a dated rehearsal record: the host and what kind of host it was, the two digest sets, the browser
      name and version, the operating system, what was in the cache and how that was verified (the browser's own
      storage or network panel, shown), the journey that was completed step by step, every response that was not a
      `200`, and the `/api/version` and `/health/startup` bodies at each stage.
- [ ] Measure and record the **interruption window** for the web container swap on that host — the figure
      **NFR-AV-06** names. Record the measurement method and the number. **Do not assert a target**: `docs/nfr/slo.md`
      owns the availability objective and this record reports a measurement beside it.
- [ ] Re-run the **failed-health-check** and **failed-migration** rehearsals from **E15-F01-6** on the same real host,
      including the production branch **refusing** to reset, and record that the behaviour observed on a real host is
      the behaviour the ephemeral run showed. A branch that behaves differently on a real host is a defect against
      **E15-F01-6**.
- [ ] Record **what the rehearsal found**: every surprise, every step that needed a person who was not expected, every
      sentence of either runbook that turned out to be wrong, and every command that had to be typed by hand. A
      rehearsal that reports nothing has not been run.
- [ ] Raise each defect as its own issue against the child that owns the mechanism — **E15-F01-6** for the script and
      the runbooks, **E15-F01-5** for the migration behaviour, **E15-F01-7**/**E15-F01-7b** for the host — and list the
      numbers in the record. **This child fixes nothing inside its own branch.**
- [ ] Update `docs/nfr/traceability.md`: **NFR-BR-08** from "Proof scheduled (#59)" to proven, naming this record; and
      **NFR-AV-06** likewise, with the measured number. If either is **not** proven — because the journey failed, or
      because the only available host was not representative — say so and leave the row scheduled. **A row marked proven
      by a rehearsal that did not really run is the worst outcome available here.**
- [ ] Where the record lives, and **neither candidate directory exists yet, so this is a decision**: `ls docs/` returns
      `adr api architecture billing customers dev integration nfr platform prd process security templates` — there is
      **no `docs/qa/` and no `docs/evidence/`**. #61's children name both (`E15-F03-7` writes `docs/qa/records/`,
      `E15-F03-11` writes `docs/evidence/`) and neither has merged. So: **default to an appendix to
      `docs/process/release-evidence.md`**, which exists, is the form this record feeds, and needs no new tree; and if
      `docs/qa/records/` has landed by then, put it there instead and say so in one line. **Do not create
      `docs/evidence/`**, and do not found a third home.

## Out of scope

Building, changing or fixing anything. The script, the runbooks and the halt-and-rollback behaviour are
**E15-F01-6**'s; the migration behaviour is **E15-F01-5**'s; the host is **E15-F01-7**'s and **E15-F01-7b**'s. The
**full dress rehearsal** including a backup and a restore is #61's child `E15-F03-7`, which covers the same ground
from the release-process end and depends on #59 — this child is the narrower mechanism rehearsal that NFR-BR-08 names,
and the two records cross-reference rather than repeat each other. Automating any of this — **#52**'s Playwright suite,
after which the journey half can be re-run by a machine and this record becomes its baseline. The service worker and
the update prompt — **#51**. Backups, point-in-time recovery and restore — **#60**; a rollback here is "redeploy the
previous digest", never "restore the database". Raising `minimumClient` for a real release is a release decision.

## Acceptance criteria

Every criterion here is satisfied by a **record**, not by a green check. That is what makes this an evidence child.

- [ ] The record exists, is dated, and names the host, the browser and both digest sets. A record that does not say
      where it ran proves nothing, which is the failure mode this criterion exists to prevent.
- [ ] Release N, serving against a database at N+1, completed one smoke journey in a browser that still held the N+1
      client, with the cache state shown rather than claimed.
- [ ] The roll-forward applied nothing, proved by `migrate`'s own output.
- [ ] The measured interruption window is recorded as a number, with the method and the host.
- [ ] The failed-health-check and failed-migration rehearsals behaved on the real host as they did on the ephemeral
      one, or the differences are recorded as defects with issue numbers.
- [ ] **Negative:** if the journey **failed** — the client refused to start, a request came back `426`, a cached asset
      no longer matched the server — that failure is the record's headline and **NFR-BR-08 stays scheduled**. This
      criterion is satisfied by an honest failure and violated by a quiet success.
- [ ] **Negative:** if no representative host could be obtained, the record says so, names what was used instead, and
      the traceability rows stay scheduled. **OD-02** is open and `docs/dev/staging.md` section 12 item 1 says no host
      exists, so this is the likely outcome and it must be an acceptable one.
- [ ] **Exception:** where the smoke journey needs a permission an ordinary member of staff would not hold, it is
      re-run as an ordinary user and the record says which role was used. A journey only a super-user can complete
      proves nothing about the release.
- [ ] **Exception:** the journey runs on **synthetic data only** (CLAUDE.md section 4 rule 10, **NFR-MQ-04**). No real
      customer name, phone number or measurement appears in the record, and screenshots are checked for them before
      they are attached — this is the one way an evidence child can leak personal data.
- [ ] `./scripts/dev docs` passes over the changed documents.

## Data and classification

Classes are from `docs/nfr/data-classification.md` section 2.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| The rehearsal record: host kind, digests, browser, journey, responses, measured window | Internal | The evidence **NFR-BR-08** and **NFR-AV-06** name, and the rollback row of `docs/process/release-evidence.md` | With the release record, permanently | Technical reviewer, operations owner, Owner | Safe. It names digests, versions, a browser and a journey. **It must not carry the host's address or hostname** — a rehearsal record is read by everyone who reads the release |
| Screenshots of the client completing the journey | **Internal, and the one item here that can leak** | Show the cached client working | With the record | As above | The screen shows a customer record. It is **synthetic**, and the record states that and names the fixture — but every screenshot is still read before it is attached, for a name, a phone number, a measurement or a photograph. Redact rather than re-shoot if one appears |
| The browser's cache or storage listing | Internal | Prove what was cached | With the record | As above | Safe. It names asset paths and versions. Strip any cookie or token value the panel displays — `__Host-t360.session` carries an opaque session identifier and there is no reason for it to appear |
| `/api/version` and `/health/startup` bodies | Public / Internal | The stage-by-stage state | With the record | As above | `/api/version` is already anonymous and withholds the commit outside Development. `/health/startup` is internal and may name a schema and a check; that is safe in a release record and is **not** to be quoted into anything customer-facing |
| Anything from production | — | — | — | — | **Nothing.** There is no production. And no production data is ever copied into a rehearsal (CLAUDE.md section 4 rule 10) |

No field, table, column, integration-event payload, media class, credential or secret is added, because no code is
written.

## Permissions and roles

No endpoint, no permission key, no `docs/security/permission-matrix.md` row and no
`tests/Tailor360.IntegrationTests/Authorization/matrix.yaml` row. **OD-13** is open and nothing here depends on it.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| Run `scripts/tailor360-deploy` on the rehearsal host | One host | Per the host's access arrangement | n/a | Yes, if the host is production-like | Operations owner, with the Owner | **Exists as proposed** by **E15-F01-6**, which owns the row |
| Sign in to the rehearsal environment as a synthetic ordinary user | The rehearsal environment | Per the environment's own policy | n/a | No | The person running the rehearsal | **Proposed here for reviewer sign-off.** The journey must not be run as a super-user |

## Modules, contracts and events

Nothing. No module, no `Contracts` project, no schema, no migration, no integration event, no C#, no TypeScript, no
YAML. `src/`, `clients/`, `infra/`, `tests/` and `.github/` are **not touched** — if any of them changes, this child
has gone out of scope and the change belongs to whichever sibling owns the mechanism. ARCH-001 to ARCH-023 are
untouched and no new identifier is created; the pull request says so in one line.

Threat-model coverage (DOR-05): none is added. The deployment path's model is #56a's `E14-F01a-6` and this rehearsal is
an exercise of the mitigations, not a new trust boundary. If the rehearsal reveals one, that is a finding for that
model, raised as an issue.

## Screens and states

No screen is added, changed or styled. One **existing** journey is used from a browser. Name the journey and its route,
and note that its accessibility is **#52**'s and is not assessed here.

## Migration and rollback

No table and no column; no migration is written. The rehearsal **exercises** an expand migration that already exists,
and leaves the rehearsal database at N+1 — which is the point. Rollback of this pull request is `git revert` of a
document.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-02** hosting model, and with it whether a representative host exists at all | **Open**, raised 2026-09-03, needed before W0 exit; `docs/dev/staging.md` section 12 item 1 records that no host has been provisioned | Run the rehearsal on the most representative host available — a local virtual machine or a long-lived container running the staging compose file — and **say exactly which**. The record is valid evidence of the mechanism and is explicitly **not** evidence about the production machine; it is re-run when one exists | W0 exit |
| **A11Y-OD-01** which browser and assistive-technology pairing a pull request runs on | **Proposed, to be confirmed** — `docs/nfr/a11y-checklist.md` sections 2.3 and its open-decision table, which records that NVDA arrives with #52 | Use one browser, name it and its version, and make **no** claim about the matrix or about a screen reader. `docs/nfr/support-matrix.md` owns the supported set and this rehearsal does not sample it | #52 |
| **NFR-AV-06**'s interruption window | **Proposed, to be confirmed**; `docs/nfr/traceability.md` | Measure and record; assert no target. The figure becomes the release-notes template's value, which **E15-F01-1** left as "not yet measured" and which this record fills | The first release train review after this merges |
| **OD-15** operations ownership | **Open**, needed before W5 | The Owner is named as the operations owner in the record, the same substitution `docs/process/release-gates.md` section 2.2 makes for **RG-OD-02** | W5 |
| Where a rehearsal record lives | No convention yet: `docs/evidence/` does not exist, and #61's `E15-F03-11` also names it | Use an existing home — `docs/qa/records/` or an appendix to `docs/process/release-evidence.md` — and say why in one line. **Do not found a third directory**, and do not create `docs/evidence/` ahead of the issue that names it | #61's children |

## Required verification

There is no test tier to run, and saying so plainly is part of this child's point.

- [ ] Resilience rehearsal, recorded: the rollback sequence with the cached client, end to end.
- [ ] Resilience rehearsal, recorded: the failed-health-check halt and the automatic redeploy of the previous digest
      set, on the real host.
- [ ] Resilience rehearsal, recorded: the failed migration resetting on a non-production target and **refusing** to
      reset on a production target.
- [ ] Migration rehearsal, recorded: the roll-forward applying nothing.
- [ ] Unit, architecture, contract, integration and client tiers: **unchanged and untouched**. No code changes, so
      `docs/api/openapi.v1.json` and `clients/pwa/src/api/schema.d.ts` must show **no diff** — if either does, the
      branch has picked up work that is not this child's.
- [ ] Accessibility: not assessed. The journey reuses an existing screen whose accessibility is **#52**'s, and this
      record says so rather than implying a pass.
- [ ] Documentation: `./scripts/dev docs` over the changed `docs/nfr/traceability.md` and the new record.
- [ ] Environment (DOR-16): a plain statement of the host, the browser, the operating system and who ran it. **This
      child cannot be completed by an agent in a sandbox**, and that is not a defect to work around: it is why it is a
      separate issue with "evidence" in its title.

NFR rows affected (DOR-13): **NFR-BR-08** (this record is its evidence), **NFR-AV-06** (the measured window),
**NFR-AV-04** (the restart behaviour observed again on a real host) and **NFR-MQ-04** (synthetic data only).

## Evidence the pull request will carry

The record *is* the evidence, so the pull request carries it and a reading of it.

- [ ] The complete rehearsal record.
- [ ] The browser cache or storage listing, showing the N+1 assets present while N was serving.
- [ ] Screenshots of the journey's steps, checked for personal data before attaching.
- [ ] `/api/version` and `/health/startup` at each stage, and the `426`-or-`200` answer to the N+1 client.
- [ ] The `migrate` output for the N+1 deploy and for the roll-forward.
- [ ] The measured interruption window with its method.
- [ ] The list of defects found, with issue numbers and the sibling each was raised against.
- [ ] A rendered diff of `docs/nfr/traceability.md`'s two rows — or a statement that they stay scheduled, with why.

## Depends on

- #59's child **E15-F01-6** — `scripts/tailor360-deploy` and the two runbooks. There is no procedure to rehearse
  without them, and the point of the rehearsal is to follow the written one.
- #59's child **E15-F01-7** — a provisioned host. In the absence of a real machine (**OD-02**), the playbook is how a
  representative one is stood up reproducibly, and the record names what it stood up.

Named for sequencing, not as blockers: **E15-F01-5** supplies the CI-level database half of NFR-BR-08, cited rather
than repeated. **#52** adds the Playwright suite that would automate the journey half, after which this record becomes
the baseline rather than the only evidence. **#51** adds the service worker and the update prompt, which will change
what "cached" means and will need this rehearsal re-run — say so in the record. **#61**'s child `E15-F03-7` is the full
dress rehearsal including restore and depends on #59; cross-reference it and do not duplicate its stages. **#60** owns
the restore path, which this rehearsal does not touch.

Refs #59
