Sub-issue of #50. Two of #50's own obligations are mechanical and unbuilt, and a third is a ledger that no longer
tells the truth. `docs/nfr/traceability.md:216` and `:219` make **NFR-LO-01** ("all user-facing text goes through
message identifiers") and **NFR-LO-04** ("Indian rupee grouping, `dd-MM-yyyy` dates and 12-hour times come from one
formatters module") provable by a **lint rule**, and `clients/pwa/eslint.config.js` contains neither — its only
project-specific rules are `react-refresh/only-export-components` and `no-unused-vars`. `clients/pwa/package.json`
declares `build-storybook`, and the `build-test-pwa` job in `.github/workflows/ci.yml` (lines 337-423) runs
`format:check`, `lint`, `generate:api:check`, `typecheck`, `test` and `build` — **not** `build-storybook` — so the
Storybook the design system is reviewed in can break without any pipeline noticing. There are also no documentation
pages: `.storybook/main.ts` globs `../src/**/*.mdx`, `find clients/pwa/src -name "*.mdx"` returns nothing, and no
story meta carries `tags: ['autodocs']`, so `@storybook/addon-docs` is installed and produces no component page.

## What is already built (do not re-build)

- `clients/pwa/.storybook/main.ts` and `preview.tsx` — the framework, `@storybook/addon-a11y` (set to
  `test: 'todo'` on purpose, because #52 owns the enforcing accessibility gate), `@storybook/addon-docs`, the
  `../public` static directory, the four toolbar globals (locale including the pseudo-locale, theme including
  high contrast, text size, density) and the five viewport widths. Do not restructure either file: the header of
  `main.ts` explains why there are exactly two.
- 41 `*.stories.tsx` files across `src/components/**`, `src/design-system/components/forms/`,
  `src/design-system/foundations/`, `src/routes/**` and `src/stories/journeys/` — the state stories already exist,
  including the eight reference journeys and the 320 px reflow-floor variants.
- `clients/pwa/src/i18n/formatters.ts` — the one formatting module, with `units.ts`, `parseNumber.ts` and
  `pseudo.ts` beside it, all unit-tested. `clients/pwa/src/i18n/messages/` holds fifteen family files with both
  languages in each, and `MessageCatalogue` in `en-IN.ts` already fails the type check when a key exists in one
  language and not the other.
- `clients/pwa/src/design-system/testing/` — `axe.ts`, `contrast.ts`, `cssTokens.ts`, `tokenContrast.test.ts`,
  `renderWithProviders.tsx` and `expectNoHorizontalOverflow.ts` (a structural `PageLike` helper, deliberately with
  no Playwright import, defaulting to 320, 360, 768, 1024 and 1280 px at 100% and 200% zoom, with the
  obscured-focus assertion included). Leave the widths alone.
- `.github/workflows/ci.yml`'s `build-test-pwa` job, with pnpm pinned through `package_json_file` and the store
  cached from `clients/pwa/pnpm-lock.yaml`. Add a step; do not restructure the job.
- `docs/nfr/traceability.md` sections 2.2 and 2.3 — the proof types and the status vocabulary (**Proven**,
  **Proof scheduled (#NN)**, **Target open**, **Evidence only**), and the maintenance rule in section 1 that a
  pull request changing a target changes its row in the same pull request.

## What is missing

- The NFR-LO-01 gate. Nothing stops a literal English string being typed into JSX in a shipped component; today
  the rule holds only by review and by the pseudo-locale stories being looked at.
- The NFR-LO-04 gate. Searching `clients/pwa/src` outside `src/i18n/**` for `toLocaleString`,
  `toLocaleDateString`, `toLocaleTimeString`, `Intl.NumberFormat` and `Intl.DateTimeFormat` finds **no** real
  violation today — which is exactly when a ratchet is cheap to install.
- `build-storybook` in continuous integration.
- Any component documentation page, and therefore any written answer to "what does a new component owe before it
  is merged" other than the prose in `clients/pwa/CLAUDE.md`.
- An honest `docs/nfr/traceability.md`: five rows say **Proof scheduled (#50)** (NFR-AC-04, NFR-AC-05, NFR-LO-01,
  NFR-LO-03, NFR-LO-04), two say **(#50, #52)** (NFR-AC-01, NFR-AC-02), one says **(#23, #50)** (NFR-AC-06) and one
  **(#50, #51)** (NFR-CB-05) — and several of those proofs either exist now or were never #50's to run.
- One stale cross-reference: the header of `expectNoHorizontalOverflow.ts` says
  "docs/nfr/support-matrix.md section 10 lists it as a per-pull-request gate". Section 10 of that document is
  **Open decisions**; the enforcement table is **section 9**.

## Scope

- [ ] **NFR-LO-01 as a lint rule.** A `no-restricted-syntax` entry (or an equivalent rule needing no new runtime
      dependency) that refuses literal user-facing text in JSX — `JSXText` with two or more letters, and the
      text-bearing attributes `title`, `placeholder`, `aria-label`, `alt` — in the shipped client trees
      (`src/components`, `src/design-system`, `src/routes`, `src/app`, `src/auth`, `src/admin`, `src/billing`,
      `src/catalog`, `src/customers`, `src/measurements`). The message names `FormattedMessage` or
      `intl.formatMessage` as the remedy, so the failure teaches the rule.
- [ ] The carve-out, written down with its reason rather than left implicit: `src/stories/**` (the eight reference
      journeys are Storybook evidence, not shipped screens, and
      `clients/pwa/src/stories/journeys/ReceptionIntake.tsx:65` deliberately carries literal text), `*.test.tsx`
      and `*.stories.tsx`. State in the rule's comment that the 40% text-growth proof for the journeys therefore
      comes from the components they are built out of, which do use message ids.
- [ ] **NFR-LO-04 as a lint rule.** Refuse `toLocaleString`, `toLocaleDateString`, `toLocaleTimeString`,
      `new Intl.NumberFormat` and `new Intl.DateTimeFormat` anywhere in `clients/pwa/src` outside `src/i18n/`,
      naming `getFormatters()` as the remedy. One existing use has to be resolved rather than exempted:
      `clients/pwa/src/components/states/Forbidden.tsx:69` builds a conjunction with `new Intl.ListFormat`.
      Preferred resolution — move it into `src/i18n/formatters.ts` as a `formatList` member with its own unit test,
      so the rule needs no exception; if it stays where it is, `Intl.ListFormat` is named in the rule as permitted
      with the reason, never silenced with an inline disable.
- [ ] **The expected violation count is known, so the session does not have to decide whether to stop.** A
      ripgrep pass over `clients/pwa/src` excluding `**/*.test.tsx`, `**/*.stories.tsx` and `src/stories/**` finds
      **no** JSX text literal of two or more letters and **no** literal `title`, `placeholder`, `aria-label` or
      `alt` in the shipped trees, and exactly **one** direct-formatting violation:
      `clients/pwa/src/components/states/Forbidden.tsx:69`. Every other hit is inside a carve-out. Verify the two
      scans first and paste the counts; if a rule fires somewhere this inspection did not predict, **fix only the
      files this slice already touches, and raise a follow-up issue linked to #50 for anything in another lane's
      file** — do not reformat another lane's work and do not ask for a decision mid-flight.
- [ ] **`build-storybook` in continuous integration**, as a step in the existing `build-test-pwa` job after
      `pnpm build`, with the built `storybook-static/` **not** uploaded as an artefact unless a later issue needs
      it. One step, no new job, no new permission on the workflow.
- [ ] **Component documentation.** `tags: ['autodocs']` on the story metas of the component families
      (`src/components/primitives`, `navigation`, `states`, `dialogs`, `layout`, and
      `src/design-system/components/forms`) — not on the journey or route-screen stories, which are evidence
      rather than API documentation.
- [ ] One `.mdx` overview page under `clients/pwa/src/design-system/` — the design system's front door, and the
      only new prose: the token families and where they are defined, the one `FieldProps` contract, the five screen
      states and their components, the three confirmation tiers and when each applies, and a short checklist of
      what a new component owes (a story per state, a pseudo-locale story, an axe assertion, message ids in both
      catalogues, no inline style or script). Every claim in it links to the file that implements it.
- [ ] **Re-verdict #50's rows in `docs/nfr/traceability.md`**, one row at a time, each with the evidence named in
      the row itself and using only the section 2.3 vocabulary: NFR-LO-01 and NFR-LO-04 become **Proven** by the
      two rules added here; NFR-AC-05 (status never by colour alone) is **Proven** by
      `src/components/primitives/StatusBadge.*` and its tests; NFR-AC-04 (no overflow, no obscured focus) keeps its
      helper but moves its run to **#52**, which owns `tests/e2e` and `playwright.config` — neither exists today;
      NFR-LO-03 (40% growth) stays scheduled against #52 for the same reason, with the pseudo-locale stories noted
      as the half that exists; NFR-AC-01, NFR-AC-02 and NFR-AC-06 keep #52 and #23 as stated and are not claimed
      here. No number, threshold or target is changed — only the proof and the status.
- [ ] Fix the stale section reference in `expectNoHorizontalOverflow.ts` (section 10 → section 9), and add a row
      to `docs/nfr/support-matrix.md` section 9 for the Storybook build if and only if it is genuinely an
      enforcement of the matrix; if it is not, leave that document alone and say why in the pull request.
- [ ] **Correct the one sentence in `clients/pwa/CLAUDE.md` section 6 that is now false and is actively
      misleading every client session**: "**Server state is server state.** From #50 it is TanStack Query with
      `Idempotency-Key`, `X-Correlation-Id`, `X-Client-Version` and the anti-forgery header injected centrally".
      There is **no `@tanstack` dependency in `clients/pwa/package.json`**, and no child of #50 adds one —
      introducing a query cache now would touch every client feature area and collide with roughly twenty-five
      in-flight client sub-issues. What is true is `apiRequest` in `clients/pwa/src/auth/apiClient.ts` (which
      already injects all four of those headers) plus the per-feature `useAdminResource` hook in
      `clients/pwa/src/admin/useAdminResource.ts`. Rewrite the bullet to say that, keep "Do not build a second
      data-fetching path" exactly as it stands, and **record the shared query cache as an open item in
      `docs/prd/assumptions-and-open-decisions.md`** — which is the route CLAUDE.md section 8 prescribes for
      something undecided — naming that plan Section 4.6 assigns it to #50 and that #50's children deliberately do
      not deliver it. Do **not** add the dependency here, and do not leave the sentence standing: a session that
      reads it either invents a cache or builds the second path the same bullet forbids. The section 6 bullet about
      the locale in `localStorage` is **e12-f01-2b's** to correct; leave it alone.
- [ ] Bring `clients/pwa/README.md` up to date where it is now wrong: the design system is no longer something
      that "lands on top" of the scaffold, `app/router.tsx` is no longer "home and 404", the icons are no longer
      "placeholder … final artwork with #50" if that is still the case say so plainly, and the command table gains
      `pnpm storybook` and `pnpm build-storybook`. Add the two new lint gates to `clients/pwa/CLAUDE.md` section 5
      beside the rules they enforce, as one line each, naming the rule rather than describing it.

## Out of scope

Visual-regression baselines, the Playwright suite, Lighthouse CI and the enforcing axe gate are **#52** — it owns
`tests/e2e`, `playwright.config` and the cross-browser reports, and none of them exists in the repository today
(`./scripts/dev` says the Playwright suite arrives with #52). The Tamil enablement gate
(`docs/nfr/accessibility-localisation.md` section 10.3) is not touched. No message is re-worded and no component is
restyled here.

## Acceptance criteria

- [ ] `pnpm --dir clients/pwa lint` fails on a newly added JSX literal in a shipped component, and names
      `FormattedMessage` in the failure — demonstrated by a deliberately added line in the pull request
      description, not by a committed fixture.
- [ ] `pnpm --dir clients/pwa lint` fails on a newly added `amount.toLocaleString('en-IN')` in a component and
      names the formatters module.
- [ ] `pnpm --dir clients/pwa lint` passes on the repository as it stands after the fixes, and
      `pnpm --dir clients/pwa test`, `typecheck`, `format:check`, `build` and `build-storybook` all pass.
- [ ] Negative: the rule does **not** fire on `src/stories/journeys/*.tsx`, on a `*.test.tsx`, or on a
      `*.stories.tsx`, and the reason for each carve-out is readable in the configuration rather than only in this
      issue.
- [ ] Negative: the rule does **not** fire on `src/i18n/formatters.ts` itself, which is the one place the banned
      calls belong.
- [ ] Negative: an inline `eslint-disable` is not used anywhere in this change. If a rule cannot be satisfied, the
      code changes or the rule's own comment records the permitted case.
- [ ] A continuous-integration run shows the `build-storybook` step passing, and a deliberately broken story
      (shown in the pull request, not committed) fails that step — so the gate is demonstrated to gate something.
- [ ] The Storybook documentation pages render for every family tagged: one docs page per component, with its
      props table, reachable from the sidebar, plus the overview page.
- [ ] Every `docs/nfr/traceability.md` row touched uses a status from section 2.3 verbatim and names the file or
      issue that is the proof; `./scripts/dev docs` resolves every link added.
- [ ] `clients/pwa/CLAUDE.md` section 6 no longer claims TanStack Query, `grep -n tanstack clients/pwa/package.json`
      still returns nothing, and the open item is recorded in `docs/prd/assumptions-and-open-decisions.md` with the
      plan section that assigned it. Negative: no `@tanstack/*` package appears in the lockfile diff — if one does,
      this slice has grown a dependency it is not allowed to add.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| The lint configuration, the documentation page and the workflow step | **Public** — the application shell assets and the repository's own documentation, `docs/nfr/data-classification.md` section 2 | Make two written rules mechanical, and give the design system a front door | Source control | Anyone with the repository | None of it runs at runtime; nothing is emitted |
| The built `storybook-static/` in continuous integration | Public — it contains only synthetic fixture data (`clients/pwa/src/stories/fixtures/`) | Prove the review environment still builds | Discarded with the runner; **not** published as an artefact by this slice | The pipeline | Not deployed anywhere |

Nothing new is stored, transmitted or logged. No personal data is involved: every fixture the stories use is
synthetic, which `clients/pwa/src/stories/fixtures/` already guarantees and CLAUDE.md section 4 rule 10 requires.

## Permissions and roles

Deleted with its reason: this slice adds no endpoint, touches no authorisation code, and changes no grant, so there
is no permission, scope, step-up or matrix row to state. **OD-13** is not engaged.

## Modules, contracts and events

No module in `src/` is touched, so no boundary is crossed and ARCH-003, ARCH-004, ARCH-005 and ARCH-011 are not
engaged. Two points worth stating because they are easy to get wrong: the lint rules are **client** rules and carry
no `ARCH-…` identifier — the `ARCH` catalogue in `docs/architecture/architecture-rules.md` is about the .NET project
graph and the source text under `src/`, and inventing an `ARCH` row for an ESLint rule would put it in a catalogue
whose tests cannot see it (CLAUDE.md section 3). The rules are documented in `clients/pwa/CLAUDE.md` instead, which
is the guide that owns the client tree. No integration event, no contract, no schema.

## Screens and states

Deleted with its reason: no screen changes. The documentation page is Storybook prose, not an application route, and
the overview page inherits the preview decorators like every other story.

## Migration and rollback

No schema change and no migration: this slice touches configuration, documentation and a workflow step only.
`migrate --dry-run` reporting no change is the evidence. Rollback is reverting the commit; a failing lint rule can
also be narrowed in a follow-up without reverting the documentation.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **DOD-OD-02** — the coverage floors in `.github/coverage-floors.json` | Proposed, to be confirmed (`docs/process/definition-of-done.md`) | Not touched: no floor is changed and no new coverage claim is made | #22's own closure |
| Tamil enablement gate, `docs/nfr/accessibility-localisation.md` section 10.3 | Open | The pseudo-locale and `ta-IN` toolbar entries stay exactly as they are; no language becomes selectable | The gate's eight criteria |
| Whether Storybook's static build becomes a published artefact | Not a recorded decision | It is built and discarded. Publishing it would be a hosting decision, and none has been taken | Whoever needs a hosted Storybook raises it |

## Required verification

- [ ] `pnpm --dir clients/pwa lint` on the whole tree, before and after, with the before-count of violations stated
      (the expectation, from this session's inspection, is zero for the formatting rule).
- [ ] `pnpm --dir clients/pwa test` — the existing 1,200-odd tests still pass; this slice adds none beyond a unit
      test for `formatList` if that member is added.
- [ ] `pnpm --dir clients/pwa typecheck`, `format:check`, `build` and `build-storybook`.
- [ ] `./scripts/dev docs` for the links added to the documentation and the overview page.
- [ ] A continuous-integration run on the branch, showing the new step in `build-test-pwa` and the job still
      inside its usual time (DOR-16: nothing here needs a database, so `./scripts/dev doctor` is a formality, but
      run it and paste it).
- [ ] Non-functional rows touched (DOR-13), named individually: NFR-AC-01, NFR-AC-02, NFR-AC-04, NFR-AC-05,
      NFR-AC-06, NFR-LO-01, NFR-LO-03, NFR-LO-04, NFR-CB-05. Each row's new status and its proof are the diff a
      reviewer reads, so the pull request shows that table as a rendered before-and-after.

## Evidence the pull request will carry

- [ ] The two failures, pasted: the rejected JSX literal and the rejected `toLocaleString`, with the rule's own
      message visible.
- [ ] `pnpm lint`, `test`, `typecheck`, `format:check`, `build` and `build-storybook` output, clean.
- [ ] A screenshot of one generated Storybook documentation page and of the overview page.
- [ ] The continuous-integration run link showing the `build-storybook` step, and the deliberate-break run showing
      it failing.
- [ ] The rendered diff of `docs/nfr/traceability.md`, `clients/pwa/README.md` and `clients/pwa/CLAUDE.md`.

## Depends on

Nothing unmerged, and deliberately **last** of #50's coding children: the literal-string rule would otherwise force
rework in **e12-f01-2**, **e12-f01-2b**, **e12-f01-3** and **e12-f01-3b**, all of which add message ids while they
are in flight. Start it after **e12-f01-3b** merges. It does not depend on any of them technically — it depends on
them *chronologically*, and its acceptance criteria stand on their own, so if one of them slips this child can still
be verified and merged as long as its lint pass is re-run on the tree as it then stands.

Shared files other lanes are editing now, with the specific collisions named because each is a real queued child:
`clients/pwa/eslint.config.js` — **E14-F01b-1** adds a Trusted Types / `innerHTML` lint rule to the same file, so
these two must not run together; `.github/workflows/ci.yml` — **E15-F03-3** adds the business-scenario suite to it;
`docs/nfr/traceability.md` — **E14-F01a-2** and **E14-F01b-5** build and audit the ASVS sheet that cross-references
it; `clients/pwa/CLAUDE.md` — **e12-f01-2b** edits one bullet of the same section 6. Rebase rather than reformat, and
keep every edit in this slice additive.

Refs #50
