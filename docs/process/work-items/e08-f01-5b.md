Sub-issue of #38. Follows `E08-F01-5`. #193 delivers the supplier master as endpoints under
`/api/v1/inventory/suppliers`; nothing renders them, so the "supplier … admin" half of the blueprint's **Screens**
line for #38 (`docs/IMPLEMENTATION_PLAN.md` Section 9, `### #38`) has no surface, and the supplier half of #38
acceptance criterion 1 — "Authorized users can add/retire inventory without code deployment" — cannot be
demonstrated. Checked in the working copy today: there is no supplier screen anywhere under `clients/pwa/src`, and
`docs/api/openapi.v1.json` contains no `api/v1/inventory` string at all.

This child exists because the foundation and both masters do not fit one pull request. The measured precedent in
this tree is `clients/pwa/src/routes/catalog/`: 1,624 production lines of routes for **one** list and **one**
editor, over a feature module of 915 lines. The supplier editor is the larger and more sensitive of #193's two
masters — it is the only screen in the whole of #38 that renders **Personal** data (supplier contact people) and
**Confidential** commercial terms — so it gets a pull request of its own and a review of its own.

## What is already built (do not re-build)

Read `E08-F01-5`'s "What is already built" first — the transport, the five state components, the primitives, the
form family, the test and story harness and the administration-section pattern are all listed there at their
verified line counts, and all still apply. What this child additionally builds on, and must not duplicate:

- `clients/pwa/src/inventory/` as created by `E08-F01-5`: `types.ts` (already carrying `SupplierCard` and
  `SupplierPage` for the list), `inventoryApi.ts` (already carrying `listSuppliers` and `listLocations`, and the
  location read/create/update/transition calls), `inventoryPermissions.ts` — which **already declares
  `manageSuppliers: 'inventory.manage_suppliers'`**, so do not edit that file — and `testing/fixtures.ts`.
- `clients/pwa/src/i18n/messages/inventory.ts` (created by `E08-F01-5`, which owns `inventory.common.*`) — add
  `inventory.nav.suppliers` and `inventory.suppliers.*` here; do not create a second family and do not touch the
  1,521-line `messages/admin.ts`.
- `clients/pwa/src/routes/inventory/inventory.css` (created by `E08-F01-5`) — add to it; create no second
  stylesheet.
- `clients/pwa/src/routes/inventory/LocationListRoute.tsx` and `LocationEditorRoute.tsx` (`E08-F01-5`) — the shape
  this child copies for the second master: the same `useAdminResource` read, the same `RequirePermission` guard, the
  same five states, the same `Idempotency-Key` and `If-Match` handling.
- `clients/pwa/src/routes/admin/AdminReasonField.tsx` (42 lines) — the written-reason control, for a status
  transition #193 demands a reason on.
- `clients/pwa/src/routes/admin/StaffDetailRoute.tsx` and `clients/pwa/src/routes/admin/RoleDetailRoute.tsx`
  (261 lines) — the two existing editors that hold one `Idempotency-Key` in state and mint a fresh one only after a
  success (`RoleDetailRoute.tsx:65`, `:123`).
- Server side: `suppliers` is Inventory's own table (`docs/architecture/module-ownership.md` section 5.7: "Vendor
  and storage master data with branch scope and allowed transfer destinations") and #193 builds it.

## What is missing

- No supplier list and no supplier editor, so #193's supplier routes are unreachable from the application.
- No rendering anywhere in the client of a supplier's contact person, GSTIN, lead time or payment terms — which is
  why this child, and not its siblings, carries the **Personal** and **Confidential** handling rules of
  `docs/nfr/data-classification.md` section 5.9 and has to prove them.

## Scope

- [ ] **Supplier calls appended to `clients/pwa/src/inventory/inventoryApi.ts`** — read through
      `apiRequestVersioned`, create, update, and the deactivate/reactivate transitions as #193 published them.
      `listSuppliers` is already there from `E08-F01-5`; extend it with the `filter[...]`, `sort` and cursor
      parameters this child's list screen needs rather than adding a second list function.
- [ ] **Supplier types appended to `clients/pwa/src/inventory/types.ts`** — the full `Supplier` detail type, its
      request types, and the approval-status union. **Run `pnpm --dir clients/pwa generate:api` first and read the
      shapes out of `clients/pwa/src/api/schema.d.ts`**; pin each in `clients/pwa/src/api/contract.ts` with
      `Conforms<…>` against #193's operation ids. Nothing about the shape is invented here.
- [ ] **The status set is #193's four, and the transition is whatever #193 published.** #193's scope names the
      supplier approval status as **draft, active, suspended, retired**, and its endpoints as
      "deactivate/reactivate". `docs/IMPLEMENTATION_PLAN.md` Section 9 writes the same set as
      "draft | approved | suspended | retired"; **#193 is the implementing issue and wins** (CLAUDE.md section 8:
      when two documents disagree, the one that owns the detail wins). Render the four statuses the generated schema
      actually declares, drive each transition through the endpoint the schema actually declares, and if the schema
      declares a status value this body does not name, render it rather than guessing — and say so in the pull
      request.
- [ ] **Supplier list** at `/admin/inventory/suppliers` — `DataTable` with code, name, status, lead time and the
      primary contact; `Filters` for status and a text term; cursor paging through `nextCursor`; status as a
      `StatusBadge` carrying an icon **and** a word (NFR-AC-05).
- [ ] **Supplier editor** at `/admin/inventory/suppliers/new` and `/admin/inventory/suppliers/:supplierId` — the
      fields #193 publishes: supplier code, name, contact details, GSTIN/tax reference, lead time, payment terms,
      commercial notes and branch scope. `FormErrorSummary` at the top of the form, field errors bound to their
      field, and the status transitions through `ConfirmDialog`; where the server demands a written reason, collect
      it with `AdminReasonField` rather than discovering the refusal afterwards. The branch shown is the caller's
      own (`useCurrentUser().branchId`); the screen never offers a branch the caller is not in, and the server
      re-checks regardless (CLAUDE.md security rule 2).
- [ ] **Idempotency and concurrency exactly as `E08-F01-5` established them**: `Idempotency-Key` on create (the one
      command #193's scope declares it for), minted with `crypto.randomUUID()` when the person commits and held
      across a refusal and a retry; `If-Match` from the `ETag` of the read that painted the screen whenever that
      read carried one, per `docs/architecture/conventions.md` section 4.2 — never produce a `428`. If #193's
      supplier read emits no `ETag`, that is a defect raised on #193, not something this child works around;
      `E08-F01-5`'s pull request records which way it went.
- [ ] Insert `inventory/suppliers` into `ADMIN_DESTINATIONS` **immediately before `inventory/locations`**, keeping
      `features`, `audit` and `outbox` last, per the order `E08-F01-5` set.
- [ ] Guard both routes with `RequirePermission` on `inventory.manage_suppliers`, and register them as children of
      the existing `admin` route in `clients/pwa/src/app/router.tsx`.
- [ ] Five states for both screens, each with a Storybook story driven through `storyTransport`, plus one
      pseudo-locale story. Writes are **online-only**: an offline attempt renders `OfflineBlockedAction` ("this will
      not be queued"), because master edits are not in the approved offline set (`clients/pwa/CLAUDE.md` section 6).
- [ ] **No contact field is ever sent to telemetry, and no contact field is ever echoed into a problem-detail log.**
      This is the one rule in this child that is not about layout: `docs/nfr/data-classification.md` section 5.9
      makes supplier contact people **Personal** and supplier terms **Confidential**, and forbids supplier terms in
      logs. A test asserts that the client's error reporting carries the route name, the component name, a stack
      hash and timing, and no field value.
- [ ] DOR-11: no seed data and no versioned configuration in the client. The opening supplier list is OD-10 through
      TR-24 and comes from the server; nothing is hard-coded.

## Out of scope

The location screens and the module foundation (`E08-F01-5`); the item register (`E08-F01-6`); the item units and
conversions panel (`E08-F01-6b`); the bulk CSV import, which imports items and not suppliers (`E08-F01-7`); the
reorder-rule editor (`E08-F01-8`). Also out of scope: purchase orders, receipts and anything that buys from a
supplier (#39); supplier performance or spend reporting (#40 and #44); a supplier-facing portal or any outbound
notification to a supplier (nothing in #38 asks for one, and Notifications' consent and quiet-hours gate is #47's);
importing suppliers from a file (nothing in #38 asks for it); and pseudonymising a retired supplier's contact
person, which `docs/nfr/data-classification.md` 5.9 routes through section 5.2's mechanism and #57's erasure
workflow, not through this screen.

## Acceptance criteria

- [ ] An Inventory Clerk holding `inventory.manage_suppliers` creates a supplier with a contact, a GSTIN, a lead
      time and payment terms, edits it, suspends it and reactivates it from the screen — proved by a Vitest journey
      against the stubbed transport, with no direct database access and no deployment in between. This is the
      supplier half of #38 acceptance criterion 1.
- [ ] Both routes this child adds are guarded by `RequirePermission` on exactly one key, and a caller without the
      key sees the sentence and who to ask — **never a redirect** and never a list of controls that all refuse.
- [ ] A duplicate supplier code is shown as a field-level error on the code field, taken from the server's RFC 9457
      problem through `plainLanguageDetail`, with the typed input intact — never a raw code and never a toast.
- [ ] **Concurrency, verified against the stub.** When a stubbed read carries an `ETag`, the save sends the same
      value as `If-Match` (asserted from `RecordedCall`); a stubbed `409` carrying `currentVersion` shows what the
      record says now beside what the person typed, offers the choice, and a retry reuses the **same**
      `Idempotency-Key` (`docs/architecture/conventions.md` section 4.3). When a stubbed read carries no `ETag`,
      the save sends no `If-Match` and never produces a `428`.
- [ ] A retired or suspended supplier is still listed and still readable, and the screen offers reactivation instead
      of edit controls that would be refused — the client half of `docs/prd/configurable-vs-fixed.md` line 82
      ("Retired items stay visible in history but …"), and of #193's own acceptance criterion that deactivation
      "refuses only new selection".
- [ ] Offline, a read shows the last value with the network banner and a write is refused in place with
      `OfflineBlockedAction`; nothing is queued and nothing is silently lost.
- [ ] **No supplier contact value, GSTIN, payment term or commercial note reaches client telemetry or a client log
      line** — asserted by a test over the telemetry call, not by inspection.
- [ ] No user-facing literal anywhere in the new components: every string is an `inventory.*` message id, and
      `pnpm --dir clients/pwa typecheck` proves `ta-IN` carries every key.

## Data and classification

DOR-06. The client stores nothing new; what it does is render classified fields. Classes from
`docs/nfr/data-classification.md` section 5.9, whose row reads **Class: Confidential; supplier contact people are
Personal**, and whose "In logs" rule reads "Item and ledger identifiers, movement types and quantities may appear.
**Unit costs, valuations and supplier terms may not**." Where section 2's Internal row would read a field more
loosely, 5.9 wins because it is the per-area row.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Supplier contact name, telephone, e-mail rendered on the supplier screens | **Personal** (5.9: "supplier contact people are **Personal**") | Place and chase an order | Client-side none: no `localStorage`, no `sessionStorage`, no service-worker cache (the worker arrives with #51 and these routes are not in its approved set). Server-side retention is **open — OD-08**, as 5.9's retention row records | `inventory.manage_suppliers` holders: Branch Manager, Inventory Clerk | **Never** in client telemetry, never in a client log line, never echoed into a problem-detail report. Shown on screen only |
| Supplier GSTIN / tax reference, payment terms, lead time, commercial notes | **Confidential** (5.9) | Decide who to buy from and on what terms | As above | As above | **Never** in logs or telemetry: 5.9 says supplier terms may not appear. Route name and timing only |
| Supplier code, name and status | **Confidential** (5.9) | Identify the supplier in a list and a picker | As above | As above | Supplier **identifier and code** only in a client error report |
| `inventory.suppliers.*` and `inventory.nav.suppliers` message catalogue strings | **Public** (section 2 lists the `en-IN` and `ta-IN` message catalogues as Public) | The words on the screen | Versioned with the source | Anyone | Safe everywhere |
| Story and test fixtures for suppliers and their contacts | **Internal**, synthetic only | Drive the state stories and the journey tests | With the source | Anyone | CLAUDE.md section 5: never a real supplier, contact name, telephone number or GSTIN, in any fixture |
| Client telemetry for the two new routes | **Internal** by construction | Diagnose a failing screen | Per section 5.18 of `docs/nfr/data-classification.md` | Operations | Route name, component name, stack hash and timing only (`clients/pwa/CLAUDE.md` section 6) |

## Permissions and roles

DOR-10. This child adds **no** permission key: `inventory.manage_suppliers` already exists in
`docs/security/permission-matrix.md:208` and is already declared in
`clients/pwa/src/inventory/inventoryPermissions.ts` by `E08-F01-5`.

| Permission | Module | Scope | MFA | Step-up | Reason | Granted to | Issue | Matrix reason, verbatim |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `inventory.manage_suppliers` | Inventory | branch | no | no | no | `branch_manager`, `inventory_clerk` | #38 | "Suppliers and their terms" |

`inventory_clerk` is the Inventory Clerk role of `docs/security/permission-matrix.md:109`: branch scope, eleven
permissions, none of them an administration key. **OD-13** is open, so the grant is the documented default rather
than an approved one — #24 shipped the matrix on 2026-09-06 under it. Client fixtures extended:
`aCurrentUser({ permissions })` without the key, for the forbidden state and for the destination's absence from the
sub-navigation. The server-side authorisation matrix belongs to #193.

## Modules, contracts and events

Only `clients/pwa/` changes; no `.NET` project is touched, so ARCH-003, ARCH-004, ARCH-005 and ARCH-011 are not
engaged — the client crosses no module boundary because it speaks HTTP to Inventory's published surface. No
integration event is consumed or published. The only coupling is the generated
`clients/pwa/src/api/schema.d.ts`, pinned in `clients/pwa/src/api/contract.ts` and checked by
`pnpm --dir clients/pwa generate:api:check`.

One boundary shapes this screen and the body says so on purpose: the supplier's GSTIN and tax reference are
**Inventory's own copied values**, held as plain strings the way Identity's `branches` table holds a
GST-registration reference (`docs/architecture/module-ownership.md` section 3). The editor must **not** call
`/api/v1/billing/*` to resolve or validate a tax code — that is the coupling ARCH-005 forbids on the server, and
re-creating it in the client would put the same join one layer out.

DOR-05: the Inventory flow has no threat model yet — `docs/security/README.md` section 3.2 lists
`threat-models/inventory.md` as **#56a**'s in wave W2, with the inventory invariants of
`docs/architecture/invariants.md` as the named stand-in. The control that matters most on this screen is the
personal-data rule: contact values are rendered and never logged, never cached and never sent to telemetry.

## Screens and states

DOR-14. Two screens. Administration is a tablet and desktop task in practice, but layout is chosen by container
query and nothing may break at 320 px (`clients/pwa/CLAUDE.md` sections 3 and 4). Dates and numbers go through
`getFormatters()`; no `toLocaleString` at a call site.

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| Supplier list | `/admin/inventory/suppliers` | `LoadingState` naming what is being read; the count announced politely when it arrives (A11Y-LF-01) | "No supplier is recorded yet", with the act that ends it beside it | `RetryableError` with `plainLanguageDetail`, retry keeps the filter set (A11Y-LF-02) | `NetworkStatusBanner` over the last value; the create action blocked in place | `RequirePermission` sentence, no redirect |
| Supplier editor | `/admin/inventory/suppliers/new`, `/…/:supplierId` | Skeleton form, controls disabled | n/a — a new record is the empty case | `FormErrorSummary` at the top plus per-field errors; `409` shows both values | `OfflineBlockedAction`: "Needs connection — this will not be queued" | As above |

The contact block has a persistent visible label on every field and a group name, so a screen-reader user knows
which of several contacts they are in. A lead time carries its unit in words ("4 days"), never a bare number.

Accessibility: the core checklist of `docs/nfr/a11y-checklist.md` section 4 on both screens in each of the five
states, family **5.4 `A11Y-LF-nn`** for the list, and sections **4.3** and **4.4** for the editor (labels,
required, invalid, error summary). No drag (section 4.10); no colour-only status (NFR-AC-05).

## Migration and rollback

No schema change: this child adds no table, column, index or migration and touches nothing under `src/`. The proof
is `git diff --stat` showing every changed file under `clients/pwa/`, with none under `src/Modules/**/Migrations/`,
and `dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` reporting nothing outstanding on a database
already at #193. Rollback is redeploying the previous client build; two routes and one destination entry disappear
and nothing else changes.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| The supplier status vocabulary | **Two documents disagree**: #193's scope says draft, active, suspended, retired; plan Section 9 says draft, approved, suspended, retired | #193 wins as the implementing issue; render exactly what its generated schema declares, and note the wording difference in the pull request | This pull request, as a note on #193 |
| **OD-08** retention periods | **Open**, recorded 2026-09-04; `docs/nfr/data-classification.md` 5.9 names it for supplier contact details after retirement | The client retains nothing at all — no cache, no storage — so no client behaviour depends on the answer | W1 exit (overdue) |
| **OD-13** approved role-to-permission grants | **Open**; #24 shipped the matrix under a documented default on 2026-09-06 | Branch Manager and Inventory Clerk hold `inventory.manage_suppliers`; the screens read the caller's keys and assume no role | W1 exit (overdue; default implemented) |
| **OD-10** initial catalogue and master data | **Open** — `docs/prd/traceability.md:94` (TR-24) | No supplier is hard-coded; the list is whatever the server returns, including empty | W2 |
| **AL-03** touch-target sizes | **Proposed, to be confirmed** — `docs/nfr/accessibility-localisation.md` section 5, lines 147-149 | Build to 56/44/32 px; the settled WCAG 2.2 floor is 24 px and is never the phone target | Staff trial before #50 closes |
| Tamil operational vocabulary | Practice recorded at the top of `clients/pwa/src/i18n/messages/catalog.ts`; `ta-IN` behind a flag | `inventoryTa` carries every new key with the English sentence | The localisation review gate |

No decision in this table blocks the work: each has a default in force and the default is what gets built.

## Required verification

`tests/e2e` does not exist — `scripts/dev:434` warns that the Playwright suite arrives with #52 — so **no
end-to-end tier is claimed**.

- [ ] **Vitest (client)**: both screens queried by role, label and text — never a test id. The happy journey
      (create, edit, suspend, reactivate) and, as named cases: missing permission (forbidden sentence, no
      redirect), duplicate code (field error from a `problemResponse`), `409` (both values shown, same
      `Idempotency-Key` on retry, asserted from `RecordedCall`), read-with-no-`ETag` sends no `If-Match`, offline
      write (blocked in place), retired and suspended records (read-only with reactivation), empty list, failed read
      then retry.
- [ ] **Vitest (client), the personal-data rule**: a test that renders the editor with a contact, GSTIN and payment
      terms, triggers a client error, and asserts the telemetry payload carries no field value.
- [ ] **Accessibility (Vitest + axe)**: `expectNoAccessibilityViolations` on each screen in each of the five
      states, remembering `RULES_JSDOM_CANNOT_JUDGE`.
- [ ] **Reflow and obscured focus (NFR-AC-04) — taken in a real browser, not in jsdom.**
      `clients/pwa/src/design-system/testing/expectNoHorizontalOverflow.ts` (394 lines) is a **Playwright** gate:
      its entry point `expectNoHorizontalOverflow(page)` (`:348`) takes a `PageLike` that "Playwright's `Page`
      satisfies", and the file's own header records that **#52** — which owns `tests/e2e/playwright.config.ts` — is
      what passes it a real page. `measureHorizontalOverflow()` (`:83`) and `measureObscuredFocus()` (`:159`) are
      the functions it evaluates **inside** that page; jsdom performs no layout, so calling them from Vitest would
      assert nothing, and no screen test in the tree calls them today. `tests/e2e` does not exist
      (`scripts/dev:434`), so the evidence for this row is the dev server at 320, 360, 768, 1024 and 1280 CSS px
      and at 200% zoom: no page-level horizontal scroll at any of them, a wide table scrolling inside its own
      container rather than moving the body, and no focused control hidden behind a sticky bar.
      `docs/nfr/traceability.md:208` names the Playwright helper as NFR-AC-04's test and marks the proof
      "scheduled (#50)" — record that, rather than claiming a run that cannot happen yet.
- [ ] **Localisation**: the pseudo-locale story at `PSEUDO_GROWTH_FACTOR` 1.4, and a test asserting no literal
      string in the new components.
- [ ] **Contract**: `pnpm --dir clients/pwa generate:api:check` clean, and the `Conforms<…>` pins in
      `clients/pwa/src/api/contract.ts` compiling.
- [ ] **The five commands** of `clients/pwa/CLAUDE.md` section 8: `lint`, `typecheck`, `format:check`, `test`,
      `build`. Run the suite twice (CLAUDE.md section 5).
- [ ] **DOR-13 non-functional rows**: NFR-AC-01, NFR-AC-02, NFR-AC-04, NFR-AC-05, and NFR-CL-02 (the route imports
      the inventory slice only). Server latency rows NFR-LT-01 and NFR-LT-02 are #193's.
- [ ] **DOR-16 environment**: `./scripts/dev doctor`. No database is needed — the suite stubs `fetch`.

## Evidence the pull request will carry

Per `docs/process/definition-of-done.md` section 4, row "Screen", and `docs/nfr/a11y-checklist.md` section 2.5.

- [ ] Screenshots of both screens at phone, tablet and desktop widths, with **synthetic** supplier data only.
- [ ] The axe run output, and the completed accessibility record for the core checklist plus family 5.4 on the list.
- [ ] The Storybook state stories for both screens, and the pseudo-locale story.
- [ ] A screen recording of the create-edit-suspend-reactivate journey — the evidence #38 acceptance criterion 1 is
      judged on for the supplier master.
- [ ] The telemetry test output, which is the evidence for Definition of Done item 6 (secrets and personal data) on
      the one screen in #38 that renders Personal data.
- [ ] The output of `lint`, `typecheck`, `format:check`, `test` and `build`, and of `generate:api:check`.
- [ ] Screenshots at 320, 360, 768, 1024 and 1280 CSS px and at 200% zoom showing no page-level horizontal scroll
      and no obscured focus, with the one-line note that NFR-AC-04's automated proof is the Playwright helper and
      arrives with #52.
- [ ] Pull-request template items 1, 3, 6 and 9 ticked; item 2 (migration) deleted with the one-line reason above.

## Depends on

#193 — the supplier routes (`/api/v1/inventory/suppliers`: list, get by id, create, update,
deactivate/reactivate), their payloads, their status vocabulary, their operation ids and the
`docs/api/openapi.v1.json` entries this child generates types from. Merged, not nearly merged.

`E08-F01-5` — the `clients/pwa/src/inventory/` module, `inventoryApi.ts` with `listSuppliers` already in it,
`inventoryPermissions.ts` with `manageSuppliers` already declared, `messages/inventory.ts` with
`inventory.common.*`, `routes/inventory/inventory.css`, and the six administration-section decisions recorded there.

**Collision note.** This child edits `clients/pwa/src/inventory/types.ts`,
`clients/pwa/src/inventory/inventoryApi.ts`, `clients/pwa/src/i18n/messages/inventory.ts`,
`clients/pwa/src/admin/adminDestinations.ts`, `clients/pwa/src/app/router.tsx`,
`clients/pwa/src/api/contract.ts` and `clients/pwa/src/routes/inventory/inventory.css`, every one of which `-6`,
`-6b`, `-7` and `-8` also edit. **It is not parallel-safe with any of them**; the six client children merge in the
order `-5`, `-5b`, `-6`, `-6b`, `-7`, `-8`.

Refs #38
