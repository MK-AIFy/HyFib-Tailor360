Sub-issue of #50. The three role-optimised shells are built and tested, and none of them is connected to the person
using them. `clients/pwa/src/components/layout/AppShell.tsx:26` declares
`export const DEFAULT_JOURNEY_ROLE: JourneyRole = 'reception'` with the comment "Authentication and the permission
model arrive with #23 and #24; there is no identity yet and this issue must not invent one", and
`clients/pwa/src/App.tsx` renders `<AppShell>` with no `role` prop — so every signed-in person, whatever they hold,
gets Reception's navigation. Both dependencies have since landed: `GET /api/v1/me` returns the caller's permission
list (`clients/pwa/src/auth/types.ts:120`) and `clients/pwa/src/admin/RequirePermission.tsx` already uses it.

The result today is a bar of dead ends. Of the twelve destinations in
`clients/pwa/src/components/layout/roleNavigation.ts:62-83`, **only `/` and `/measurements` resolve to a registered
route**; `/customers`, `/orders`, `/scan`, `/workboard`, `/production`, `/inventory`, `/delivery`, `/reports`,
`/billing` and `/settings` all fall through to `NotFoundRoute` at `clients/pwa/src/app/router.tsx:422`. Two of those
are wrong hrefs rather than missing screens — `/settings/display` and `/billing/outstanding` **are** registered
(router.tsx lines 168 and 227) — and there is no destination for `/admin` at all, so an Owner cannot reach the
administration section from the shell even though `AdminShell` is registered at router.tsx:289 and renders its own
filtered sub-navigation. The floating action is dead too: `PRIMARY_ACTIONS.takePayment.href` is `/billing/new`
(roleNavigation.ts:107-112), a route that has never existed — the real one is `/billing/payments/new`.

**The home screen is not in this slice.** `HomeRoute` stays the placeholder it is; replacing it with a destination
launcher is **e12-f01-3b**, which depends on this one. A session that both rebuilds the navigation for twelve
permission sets *and* builds a new screen is two sessions.

## What is already built (do not re-build)

- `clients/pwa/src/components/layout/roleNavigation.ts` — `DESTINATION_IDS` (line 36, twelve ids) and `DESTINATIONS`
  (line 62: href, icon, message id, `end`), `PRIMARY_ACTIONS` (line 94: `scan`, `newOrder`, `capture`,
  `takePayment`), `ROLE_NAVIGATION` per journey role (line 126) with its `destinations`, `bottomBar` and
  `primaryAction`, `SECTION_ORDER` (line 185: `work`, `customers`, `money`, `manage`) for the desktop rail,
  `NavigationBadges`, and the `useRoleNavigation(role, …)` hook (line 221). Unit-tested in `roleNavigation.test.ts`,
  which asserts among other things that "the four shop-floor roles" have Scan as their primary action (line 69) —
  **that test is about `ROLE_NAVIGATION` and must keep passing untouched.**
- `clients/pwa/src/components/navigation/navigationItems.ts:56` — `MAX_BOTTOM_NAV_ITEMS = 5`.
- `clients/pwa/src/components/layout/AppShell.tsx` with `PhoneShell`, `TabletShell`, `DesktopShell`, `ShellParts`,
  `useShellKind`, `useVirtualKeyboard`, `RouteAnnouncer`, `ShellStatusProvider` and `PrimaryActionFab`. Covered by
  `AppShell.test.tsx`, `AppShell.stories.tsx` and axe assertions. The shell measures its own width and must keep
  doing so — never a user-agent string.
- `clients/pwa/src/components/navigation/` — `BottomNav`, `SideNav`, `Tabs`, `HelpEntry`, `NavBadge`, each with a
  test, a story and an axe assertion. They render a list they are given; they do not decide what is in it.
- `clients/pwa/src/admin/RequirePermission.tsx` — the established client-side pattern: it is **not** the
  authorisation, it never redirects, and it renders an `EmptyState` with the message ids `admin.forbidden.title` and
  `admin.forbidden.body` (it is not the `Forbidden` component). Reuse its reasoning and its wording choices.
- `clients/pwa/src/routes/admin/AdminShell.tsx` — registered at `/admin`, it reads `useCurrentUser().permissions`,
  filters `ADMIN_DESTINATIONS` (`clients/pwa/src/admin/adminDestinations.ts`) to what the caller holds, renders its
  own named `nav` landmark, and renders its own heading and lede. **`/admin` is therefore a valid landing on its
  own**, with or without a child route matched — which is what makes it the right href for a shell destination.
- `clients/pwa/src/admin/adminPermissions.ts` — `ADMIN_PERMISSIONS`: `users`, `branches`, `roles`, `featureFlags`,
  `auditRead`, `outboxReplay`, `templatesEdit`, `templatesPublish`, `catalogEdit`, `catalogPublish`.
  `clients/pwa/src/billing/billingPermissions.ts` (`BILLING_PERMISSIONS`, including `createInvoice:
  'billing.create_invoice'` and `recordPayment: 'payments.record'`) and
  `clients/pwa/src/measurements/measurementsPermissions.ts` (`MEASUREMENT_PERMISSIONS.capture:
  'measurements.capture'`) are the same per-feature pattern, and each file's header already records that the keys
  are hand-mirrored from the server catalogue.
- `clients/pwa/src/auth/useSession.ts` — `useSession()` (`status`: `loading | anonymous | active | unavailable`, and
  `user`) and `useCurrentUser()`, which **throws** outside a session. That matters because the shell also renders
  for anonymous visitors at `/install`, `/about` and `/settings/display`: this slice must use `useSession()`, never
  `useCurrentUser()`.
- `clients/pwa/src/stories/journeys/` — the eight reference journeys and `JourneyFrame`, which supplies a role to
  the shell for Storybook and wraps its children in its own `MemoryRouter` (so it cannot be combined with any other
  router harness). They are the evidence `A11Y-RJ-01`..`A11Y-RJ-08` is walked against, so `ROLE_NAVIGATION` must
  keep answering exactly what it answers today for each of the eight journey roles.

## What is missing

- Any use of the session in the shell. `rg -n "useSession|useCurrentUser" clients/pwa/src/components/` returns
  nothing.
- A permission against each destination. `DESTINATIONS` carries an href, an icon and a message id, and no
  authorisation fact at all.
- Any notion of "this destination has a route yet". Ten of the twelve hrefs reach the 404 screen.
- An entry point to `/admin` from the shell, and a real landing destination:
  `AccountPreferences.landingRoute` is returned by `GET /me`, typed at `clients/pwa/src/auth/types.ts:107`, and
  read by nothing — `rg -n "landingRoute" clients/pwa/src` finds only the type and two fixtures.

## Scope

- [ ] `clients/pwa/src/app/shellPermissions.ts`, in the style of the three existing per-feature permission modules,
      naming the twelve keys the shell needs. **Every one is quoted from
      `src/Platform/Tailor360.Platform.Security/Permissions/`; invent none.** The exhaustive list, verified against
      those files:

      | Key | Server constant |
      | --- | --- |
      | `customers.read` | `CustomersPermissions.Read` |
      | `measurements.capture` | `CustomersPermissions.CaptureMeasurements` |
      | `orders.read` | `OrdersPermissions.Read` |
      | `orders.intake` | `OrdersPermissions.Intake` |
      | `orders.assign` | `OrdersPermissions.Assign` |
      | `orders.phase_transition` | `OrdersPermissions.PhaseTransition` |
      | `custody.scan` | `CustodyPermissions.Scan` |
      | `custody.dispatch` | `CustodyPermissions.Dispatch` |
      | `inventory.record_movement` | `InventoryPermissions.RecordMovement` |
      | `billing.create_invoice` | `BillingPermissions.CreateInvoice` |
      | `payments.record` | `BillingPermissions.RecordPayment` |
      | `reports.read` | `ReportingPermissions.Read` |

      Re-export, do not re-declare, the three that an existing client module already names
      (`MEASUREMENT_PERMISSIONS.capture`, `BILLING_PERMISSIONS.createInvoice`, `BILLING_PERMISSIONS.recordPayment`),
      so one rename stays one edit.
- [ ] Add two facts to each entry of `DESTINATIONS`, in the same table so they cannot drift apart: the permission
      that reveals it, and whether its route is registered today. **The governing rule, so nothing is chosen by
      feel: a destination's permission is the permission its target route already declares in
      `clients/pwa/src/app/router.tsx`.** For a route that does not exist yet, it is the key the landing issue's
      route will declare, and the entry says which issue that is. The table as it must land:

      | Id | href | Permission | Route registered today | Owner of the route |
      | --- | --- | --- | --- | --- |
      | `home` | `/` | none — always offered | yes (index under `RequireSession`) | — |
      | `scan` | `/scan` | `custody.scan` | **no** | #36 |
      | `customers` | `/customers` | `customers.read` | **no** | #182 |
      | `measurements` | `/measurements` | `measurements.capture` (router.tsx:181-188) | yes | #28, merged |
      | `orders` | `/orders` | `orders.read` | **no** | #204 |
      | `workboard` | `/workboard` | `orders.assign` | **no** | #33 |
      | `production` | `/production` | `orders.phase_transition` | **no** | #33 |
      | `inventory` | `/inventory` | `inventory.record_movement` | **no** | #38, #39 |
      | `billing` | **`/billing/outstanding`** (corrected from `/billing`) | `billing.create_invoice` (router.tsx:227-233) | yes | #43, merged |
      | `delivery` | `/delivery` | `custody.dispatch` | **no** | #48 |
      | `reports` | `/reports` | `reports.read` | **no** | #184-#186 |
      | `settings` | **`/settings/display`** (corrected from `/settings`) | none — the screen is reachable anonymously by design (router.tsx:161-168) | yes | #50, merged |
      | `admin` | `/admin` | **any** value of `ADMIN_PERMISSIONS` | yes | #25, partly merged |

- [ ] The `admin` destination is new: add `'admin'` to `DESTINATION_IDS` and to the `manage` section's `members` in
      `SECTION_ORDER` beside `settings`. It points at `/admin`, **not** at `/admin/users`: `AdminShell` filters its
      own sub-navigation, so a person holding only `admin.feature_flags` lands on a page that works, whereas
      `/admin/users` would hand them `RequirePermission`'s forbidden `EmptyState`. `ROLE_NAVIGATION` is keyed by
      journey role and is **not** changed, so the eight journey stories are untouched.
- [ ] Correct `PRIMARY_ACTIONS.takePayment.href` from `/billing/new` to `/billing/payments/new`
      (router.tsx:235-241, which declares `BILLING_PERMISSIONS.recordPayment`). Add the permission and the
      route-registered flag to each of the four actions: `scan` → `custody.scan`, `/scan`, **not registered**;
      `newOrder` → `orders.intake`, `/orders/new`, **not registered**; `capture` → `measurements.capture`,
      `/measurements/new`, registered; `takePayment` → `payments.record`, `/billing/payments/new`, registered.
- [ ] A `useSessionNavigation()` hook — new file beside `roleNavigation.ts` — returning the destinations and the
      primary action for the person in front of the device. A destination is offered when **the session holds its
      permission (or it needs none) and its route is registered**; both conditions, because a link that 404s is as
      much a dead end as one that refuses. Order is the fixed `SECTION_ORDER`, never personalised — the rule
      `roleNavigation.ts` already records for 3.2.3 Consistent Navigation — and the bottom bar is the first
      `MAX_BOTTOM_NAV_ITEMS` of them. The primary action is the first **available** candidate in the fixed
      precedence `scan → newOrder → capture → takePayment`, where available means permission held *and* route
      registered; when none is available there is no floating action button, not a disabled one.
- [ ] **The route-registered set is a parameter, not a hard-coded constant in the hook.** Pass it in (defaulting to
      the table's flags) so a test can prove the behaviour of a route that has not landed yet — for example that a
      `custody.scan` holder gets the Scan action the moment `/scan` is registered. This is what makes the slice
      verifiable on its own rather than resting on ten unmerged siblings.
- [ ] `AppShell` takes its navigation from that hook when `useSession().status === 'active'`, and falls back to
      today's `useRoleNavigation(role ?? DEFAULT_JOURNEY_ROLE)` otherwise. `DEFAULT_JOURNEY_ROLE` **stays**,
      because `JourneyFrame` and the eight journey stories pass a role explicitly and must keep working; what goes
      is the live shell's reliance on it. Say in the pull request which of the two paths each test exercises.
- [ ] Honour `landingRoute`: a signed-in person arriving at `/` whose account names a landing route is taken
      there **once per sign-in**, and only when that destination is one the hook offers them. Register a small
      `LandingRedirect` element around the index route in `router.tsx` rather than putting the logic in
      `HomeRoute`, which **e12-f01-3b** replaces. "Once per sign-in" is a module-level flag holding the `userId`
      it has already redirected for, cleared when `useSession().user?.userId` changes or the session becomes
      anonymous — **in memory, never `sessionStorage`**, because the device is shared. The client re-checks that
      the value is a local path (`startsWith('/')` and not `startsWith('//')`) even though
      `UserPreferences.SetPresentation` already refuses anything else on the way in.
- [ ] Message ids for the one new destination in `clients/pwa/src/i18n/messages/navigation.ts`, in both the `en-IN`
      and `ta-IN` objects (`MessageCatalogue` in `en-IN.ts` fails the type check if one is missing). No other
      message is added or re-worded.

## Out of scope

The **home launcher** is e12-f01-3b. The **screens behind the destinations** are each delivered by their own feature
issue, and flipping that destination's route-registered flag to true is a one-line change in the issue that lands
the route — say so in the pull request so the siblings know where the hook is: #182 (customers), #204 (order
intake), #36 (scan), the #33 workboard and production screens, the #38/#39 inventory screens, #48 (delivery),
#184-#186 (reports). Badge counts stay unimplemented: `NavigationBadges` already exists and nothing in this slice
has a number to put in it. The account's display preferences are **e12-f01-2**; its locale is **e12-f01-2b**.
No server-side authorisation changes, so no permission-matrix row and no `matrix.yaml` change.

## Acceptance criteria

- [ ] A session holding Reception's seeded grants sees exactly Home, Measurements, Billing-outstanding where the
      grants allow it, Settings and nothing whose route is unregistered — asserted through the **rendered**
      navigation, not by calling the hook. Reception holds `measurements.capture` and `payments.record` but not
      `billing.create_invoice` (`SystemRoles.cs`, the `new(Reception, …)` block at lines 246-276), so the billing
      destination is **absent** for Reception and present for a Cashier: assert both, because it is the case that
      proves the rule is the route's own declared permission and not a guess.
- [ ] An administrator holding `admin.users` reaches `/admin` from the shell navigation and lands on a page that
      renders; a person holding no `admin.*` key sees no administration destination.
- [ ] Every destination rendered resolves to a route registered in `clients/pwa/src/app/router.tsx` — one test,
      twelve permission sets, no href reaching `NotFoundRoute`. This is the defect the slice exists to remove, so
      it is asserted for every role and not for one, and it includes the floating action's href.
- [ ] The bottom bar never renders more than `MAX_BOTTOM_NAV_ITEMS` items, for any permission set, including an
      Owner holding everything.
- [ ] Primary action, stated against today's registered routes so the criterion is checkable now: a Cashier
      (`payments.record`, no `measurements.capture`) gets **Take payment** at `/billing/payments/new`; a Reception
      holder gets **Capture** at `/measurements/new`; a **Tailor gets no floating action at all today**, because
      the only action their grants reach is Scan and `/scan` is not registered yet. With the route-registered set
      widened to include `/scan`, the same Tailor gets Scan — asserted through the hook's parameter, which is why
      it is a parameter.
- [ ] Negative: a session whose permission list is empty — the fail-closed state
      `clients/pwa/src/auth/types.ts:119` documents — sees Home and Settings and nothing else, and the shell still
      renders its landmarks, its skip link and its Help entry in the same position (3.2.6).
- [ ] Negative: a person whose account names a landing route they no longer hold the permission for, or whose
      route is not registered, is left on Home, and that destination is not shown to them anywhere.
- [ ] Negative: `landingRoute` of `"https://example.test/x"` or `"//example.test"` is ignored and the person stays
      on Home. The server already refuses to store it; the client refuses to follow it.
- [ ] Negative: the landing route fires **once** — a second navigation to `/` in the same session stays on Home,
      so a person cannot be bounced away from the home screen they deliberately opened.
- [ ] Exception: the session is `anonymous` at `/install`, `/about` or `/settings/display` — the shell renders with
      no permissioned destination, nothing calls `useCurrentUser()`, nothing throws, and nobody is sent to a
      sign-in loop.
- [ ] Exception: `useSession().status` is `loading` or `unavailable` — the shell renders the anonymous arrangement
      rather than an empty bar or a spinner in the navigation.
- [ ] The eight reference journeys render unchanged: `clients/pwa/src/stories/journeys/journeys.test.tsx` and
      `clients/pwa/src/components/layout/roleNavigation.test.ts` both pass **untouched**, proving `ROLE_NAVIGATION`
      still answers what the accessibility records are walked against.
- [ ] Axe is green on the shell in all three layouts through `clients/pwa/src/design-system/testing/axe.ts`.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| The caller's permission list, already on the session | **Personal** — a role and permission grant of an identified member of staff (`docs/nfr/data-classification.md` section 5.15) | Decide what to offer, so the interface shows no dead ends | Not persisted by this slice; held for the session | The holder | Never logged from the client. `clients/pwa/CLAUDE.md` section 6 allows route names, component names, stack hashes and timings in telemetry and nothing else |
| `AccountPreferences.landingRoute` | Personal — an attribute of the staff record (section 5.15) | Take a person to the screen they work on | On the account row | The holder; `admin.users` (#25) | Not a telemetry attribute: it says what a named person's job is. The in-memory "already redirected" flag holds a `userId` and is never written anywhere |
| The destination table (permission, href, route-registered flag) | **Internal** — it describes how the shop's software is arranged, not a person | One place where "who may see this" and "does it exist" are stated together | Source code | Anyone with the repository | Nothing is emitted at runtime |

No new storage, no new request, no new field on any payload: every fact used here is already on the session.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to (per `SystemRoles.cs`) | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `customers.read` | branch | no | no | no | Owner, Admin, Branch Manager, Reception, Measurement Staff, Tailor Master, Cashier, Delivery Staff, Auditor | **Exists** — `CustomersPermissions.Read` |
| `measurements.capture` | branch | no | no | no | Reception, Measurement Staff and above | **Exists** — `CustomersPermissions.CaptureMeasurements` |
| `orders.read`, `orders.intake`, `orders.assign`, `orders.phase_transition` | branch | no | no | no | as seeded | **Exists** — `OrdersPermissions.*` |
| `custody.scan`, `custody.dispatch` | branch | no | no | no | as seeded | **Exists** — `CustodyPermissions.Scan`, `CustodyPermissions.Dispatch` |
| `inventory.record_movement` | branch | no | no | no | Inventory Clerk and above | **Exists** — `InventoryPermissions.RecordMovement` |
| `billing.create_invoice`, `payments.record` | branch | no | no | no | Cashier holds both; Reception holds `payments.record` only | **Exists** — `BillingPermissions.CreateInvoice`, `BillingPermissions.RecordPayment` |
| `reports.read` | branch | no | no | no | as seeded | **Exists** — `ReportingPermissions.Read` |
| the `admin.*` set | organisation | yes for the roles `Identity:Mfa:RequiredRoles` names | as each admin route already declares | — | Owner, Admin | **Exists** — `ADMIN_PERMISSIONS`, mirrored from `PlatformPermissions` and `IdentityPermissions` |

No permission key is added and no grant is changed, so `src/Modules/Identity/…/Access/SystemRoles.cs` is **not**
edited and no `matrix.yaml` row moves. This slice decides only what is **offered**; the server refuses anything
reached another way, which is the reasoning `RequirePermission.tsx` already records and which the new module's
header must repeat, because a reader who mistook the filter for the authorisation would later "simplify" a server
check away. **OD-13** (the approved role-to-permission grants) is open, so the per-role expectations in the tests
are written against the seeded register and will move with it.

## Modules, contracts and events

Client only, and one client feature area: the shell. It reads what `GET /api/v1/me` already returns and calls no new
endpoint, so no OpenAPI change and no `clients/pwa/src/api/schema.d.ts` regeneration. Nothing in `src/` is touched,
so ARCH-003, ARCH-004 and ARCH-011 are not engaged.

## Screens and states

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| The shell, all three layouts | every route | The anonymous arrangement while `status` is `loading` | A session with no permitted destination still has Home, Settings and Help | No request is made, so no error state; `unavailable` renders the anonymous arrangement | `NetworkStatusBanner`, unchanged | A destination the person cannot use is **absent**, never shown disabled |

Message ids live in `clients/pwa/src/i18n/messages/navigation.ts`, both languages, through `FormattedMessage` or
`useIntl` — no concatenation, no literal string in JSX. Nothing here formats a number, a date or an amount, so
`clients/pwa/src/i18n/formatters.ts` is untouched.

## Migration and rollback

No schema change and no migration: this slice reads only what the session already carries. `migrate --dry-run`
reporting no change is the evidence. Rollback is redeploying the previous client bundle, which returns the shell to
Reception's fixed navigation; nothing is stored that the previous bundle cannot read.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-13** — the approved role-to-permission grants | Open (`docs/prd/assumptions-and-open-decisions.md`; `SystemRoles.cs` records the same) | The seeded register is the source of the per-role expectations; no grant is added or changed here | #24, #25 |
| **AL-03** — the 56 / 44 / 32 px touch-target minimums | Open, `docs/nfr/accessibility-localisation.md` section 13 | The existing `CONTROL_SIZES` tokens are used unchanged; no control size is chosen here | Before #50 closes |
| Which screen a role lands on by default | Not a recorded decision, and **none is invented**: `landingRoute` is a per-person preference the server already stores, and an account with none lands on Home | No default landing route per role | — |
| The primary-action precedence `scan → newOrder → capture → takePayment` | Not a recorded decision. It is taken from the per-role `primaryAction` values `ROLE_NAVIGATION` already ships, read in shop-floor-first order, and is stated in the hook's header as the reason | As written | A product decision would supersede it; raise it in `docs/prd/assumptions-and-open-decisions.md` if contested |

## Required verification

- [ ] Vitest: the rendered navigation for **twelve permission sets**, one per seeded role, rendered through
      `AppShell` with `AppIntlProvider` + `MemoryRouter` + `SessionProvider` over a stubbed `fetch` — the pattern in
      `clients/pwa/src/auth/RequireSession.test.tsx`, with `aCurrentUser` from
      `clients/pwa/src/auth/testing/fixtures.ts`. `renderWithProviders` cannot be used: it supplies no session and
      no router. **The twelve sets are an explicit fixture in the test file**, each holding only the subset of the
      twelve shell keys that role is seeded with, copied once from
      `src/Modules/Identity/Tailor360.Modules.Identity.Application/Access/SystemRoles.cs` with the file and the
      role's line range cited in a comment above it. A Vitest test cannot read a C# file, and pretending the
      fixture is derived from the register would be a claim no test makes; the existing per-feature permission
      modules are hand-mirrored for the same reason and say so in their headers.
- [ ] Vitest: a guard test that every permission string in that fixture appears in `shellPermissions.ts` or
      `ADMIN_PERMISSIONS`, so a typo in the fixture fails rather than silently hiding a destination.
- [ ] Vitest: every rendered href, including the floating action's, resolves to a path registered in
      `clients/pwa/src/app/router.tsx`. Derive the registered set from the router's own route objects rather than
      re-listing them, so a route added later cannot drift from the assertion.
- [ ] Vitest: the bottom-bar cap; the absent primary action; the widened route-registered set giving a Tailor
      Scan; the empty permission list; the two rejected landing routes; the lost-permission landing route; the
      once-per-sign-in flag; the anonymous shell at `/install`; `loading` and `unavailable`.
- [ ] Vitest: `journeys.test.tsx` and `roleNavigation.test.ts` pass with no edit.
- [ ] Accessibility: `expectNoAccessibilityViolations` on the three shells in the phone, tablet and desktop
      arrangements.
- [ ] `pnpm --dir clients/pwa lint`, `typecheck`, `format:check`, `test`, `build`, `build-storybook` clean.
- [ ] Reflow and obscured focus are **not** claimed from Vitest: jsdom has no layout engine, and
      `expectNoHorizontalOverflow` is the browser helper #52's Playwright suite drives — `tests/e2e` and
      `playwright.config` do not exist in this repository today. The pull request says which of the two it
      produced.
- [ ] Non-functional rows touched (DOR-13): `docs/nfr/traceability.md` **NFR-AC-01** and **NFR-AC-02** remain
      scheduled against #52; nothing in the traceability table is edited here.
- [ ] `./scripts/dev doctor` first; no database is needed (DOR-16).

## Evidence the pull request will carry

- [ ] Screenshots of the phone, tablet and desktop shells for three different permission sets — a Tailor, a
      Cashier and an Owner — at 360, 768 and 1280 px, showing three different navigations from one build.
- [ ] A screenshot of the empty-permission case and of the Owner's `/admin` destination.
- [ ] The axe run output.
- [ ] `pnpm` output for lint, typecheck, format, test, build and build-storybook, and the list of test names
      covering each negative and exception case above.

## Depends on

Nothing unmerged. `GET /api/v1/me` with its permission list is #23 (closed), and the permission catalogue and the
seeded role register are #24's work already in `main` (`SystemRoles.cs`, `PermissionCatalogue`). **#24 itself is
still open** for its authorisation regression suite, which does not block reading the list the endpoint already
returns — do not describe #24 as merged in the pull request. #50 (this parent) and #19 are also open.

Shared files other lanes are editing now: `clients/pwa/src/components/layout/roleNavigation.ts` (cited by several
in-flight client issues, including the inventory, production and delivery screens, which expect their destination to
start resolving), `clients/pwa/src/app/router.tsx` (every client feature child in the parallel batches adds routes
to it — roughly twenty of them, so rebase rather than re-order) and `clients/pwa/src/i18n/messages/navigation.ts`.

Refs #50
