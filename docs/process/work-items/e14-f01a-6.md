Sub-issue of #56. The last two rows of the **DOR-05** coverage table in `docs/security/README.md` section 3.2 that
this parent owns are "Integration adapters → `threat-models/integrations.md` → #56a → W2" and "Deployment, secrets
and the runtime → `threat-models/deployment.md` → #56a → W2". Neither file exists — `ls docs/security/threat-models`
returns `authentication.md` alone — and both are already being leant on: `authentication.md` declares "TLS
termination, the reverse proxy and the container runtime" and "the mail relay's own security" out of its scope and
points at `threat-models/deployment.md` (#56a) for both. Those two forward references are promises this sub-issue
keeps. W4's integration work (#202, #206, #207, #200, #203, #205) and W5's hardening (#59, #60) are the issues the
two models constrain.

## What is already built (do not re-build)

**Integration is a partly-populated module with no schema and no endpoints.**

- `src/Modules/Integration/` — 18 `.cs` files. `Integration.Api/IntegrationEndpoints.cs` is a stub (no endpoint
  registration). `Integration.Application`, `.Contracts` and `.Domain` hold nothing but `AssemblyReference.cs`.
  There is **no `DbContext` and no migration**, so the `integration` schema does not exist in the database.
- What `Integration.Infrastructure` does hold, and what the model must treat as the live edge:
  `Storage/MinioObjectStorage.cs` (the only real `IObjectStorage` implementation), beside
  `Storage/InMemoryObjectStorage.cs`, `Storage/ObjectStorageOptions.cs` and
  `Storage/ObjectStorageOptionsValidator.cs` — the in-memory double and the options validation are part of the edge
  and the model must say which of the two is registered where, because a production host that silently fell back to
  the in-memory store would lose every object;
  `Documents/QuestPdfRenderer.cs`, `Code128BarcodeRenderer.cs`, `BillingDocumentTemplate.cs`,
  `ReceiptTemplateLayout.cs`, `DocumentModel.cs`, `DocumentFonts.cs` and `Fonts/`;
  `Printing/LoggingPrintQueue.cs` (the interim print queue #205 replaces); and
  `IntegrationModuleServiceCollectionExtensions.cs`.
- `docs/architecture/module-ownership.md` section 5.11 fixes the design: schema `integration` with
  `integration_events`, `webhook_subscriptions`, `webhook_deliveries`, `provider_configurations`,
  `payment_intents`, `payment_callbacks`, `accounting_export_batches`, `print_bridge_adapters`; the published events
  `integration.webhook-delivered.v1`, `integration.webhook-dead-lettered.v1`,
  `integration.payment-callback-reconciled.v1`; no owned object-storage prefix; **no module reads `integration.*`**;
  and the rule the payment model depends on — "a payment callback never posts financial state itself: it records a
  verified outcome and Billing applies it".
- `src/Platform/Tailor360.Platform.Abstractions/Ports/IOutboundHttp.cs` — the port every outbound call must go
  through (ARCH-016). **`grep -rl IOutboundHttp src/` finds the port and `src/Modules/CLAUDE.md` and nothing else:
  there is no implementation.** `docs/nfr/traceability.md` `NFR-SE-10` states the behaviour it must have — refuse
  private, loopback, link-local and metadata addresses, disable redirects, cap response size — with "Proof scheduled
  (#54, #55)". That is the server-side-request-forgery control, and it does not exist yet.
- `integration.manage_webhooks` and `integration.replay_delivery` are seeded in
  `src/Platform/Tailor360.Platform.Security/Permissions/IntegrationPermissions.cs`; no endpoint declares either.
- ARCH-009 — provider SDK packages are referenced only by `Integration.Infrastructure` and by test projects — is
  enforced today, and is a control in its own right.
- `docs/adr/0012-integration-ports-and-adapters.md` and `docs/adr/0014-document-rendering-and-object-storage.md`.
- The open sub-issues whose controls this model must be mapped to: #202 (webhook subscription administration,
  verification, allowlist, branch scope, secret rotation), #206 (signed delivery with HMAC, retry and dead letter),
  #207 (delivery diagnosis and operator-authorised replay), #200 (the payment-gateway port and adapter with the
  no-half-posting sequence), #203 (the accounting export and Tally XML adapter), #205 (the real print bridge).

**The deployment is real and running.** Model what is in `infra/`, not an idealised topology.

- `infra/compose/docker-compose.yml` (backing services), `docker-compose.app.yml`, `docker-compose.staging.yml` (the
  interim staging environment of #22, which #59 hardens and then destroys); `infra/docker/Dockerfile.web`,
  `Dockerfile.worker`, `Dockerfile.pwa`; `infra/caddy/Caddyfile` (TLS termination, forwarded headers, rate limits);
  `infra/observability/otel-collector-config.yaml`; `infra/CLAUDE.md` for the rules that tree is held to.
- `docs/platform/secrets.md` — secrets arrive as **files** under `/run/secrets`, the file name is the configuration
  key and `__` is the section separator; never an environment variable, never a build argument, never a committed
  value (CLAUDE.md security rule 6). This is the deployment model's central control.
- `docs/architecture/deployment.md`, `docs/adr/0010-deployment-portability.md`, `docs/dev/staging.md`,
  `docs/process/branch-protection.md` section 8 — the topology, the portability decision, the staging guard-rails and
  the repository-side protections.
- The supply-chain gates that already run on every pull request, in `.github/workflows/ci.yml`: gitleaks over the
  whole history (`security` job); CodeQL with `scripts/sarif-gate.py` and the accepted-findings register
  `.github/sarif-accepted.json`; Trivy `scan-type: config` over `infra` gated at severity 9.0 and Trivy
  `scan-type: fs` over the dependency tree gated at 7.0; and the Syft CycloneDX bill of materials written to
  `artifacts/sbom/tailor360.cyclonedx.json`. The model cites these as existing controls with their gate severities —
  it does not propose them again.
- The residual risks already recorded elsewhere that this model must pick up rather than rediscover:
  `threat-models/authentication.md` **RR-04** (the Data Protection key ring in `platform.data_protection_keys` is
  stored unencrypted, so whoever can read that table can decrypt every enrolled TOTP secret) and **AS-03** (the
  client address is trusted because the proxy sets it, which both rate-limit partitions depend on).
- `docs/nfr/traceability.md` rows that belong here: `NFR-SE-09` (no secret in the repository, a log, a
  problem-details response, a health payload or telemetry), `NFR-SE-10` (the outbound port), `NFR-PR-06` (event
  payloads carry identifiers only), `NFR-BR-07` (key escrow), and `NFR-SE-07` (no unresolved critical or high
  finding at release).
- `docs/process/release-gates.md` `RG-09`, `RG-10` and `RG-11` — the scan gates, their blocking severities and their
  waiver authority. `RG-11` (image, infrastructure as code, bill of materials, signature and provenance) is
  **#59's**, and the deployment model says so rather than claiming it.

**Shared, and not to be re-derived**: `docs/templates/threat-model.md`; `docs/security/abuse-cases.md` with the
`ABF-01` to `ABF-11` families (the first sub-issue of this parent) — `ABF-09` server-side request forgery and
`ABF-11` denial of service are this sub-issue's centre of gravity, with `ABF-10` credential abuse in its
provider-credential sense; `docs/nfr/data-classification.md` sections 5.13, 5.17, 5.18, 5.20 and section 6
(credentials and secrets, including 6.2's register and 6.3's "what a credential must never be").

## What is missing

- The last two files of `docs/security/README.md` section 3.2, and with them the two forward references
  `authentication.md` already makes. Until they exist, the deployment, the proxy, the secret mechanism and the mail
  relay are modelled nowhere at all.
- Nothing states, in one place, that the outbound-request control `NFR-SE-10` describes has no implementation — the
  port exists, the rule is written, and nothing enforces it. `ABF-09` is therefore uncovered, and the model is where
  that becomes visible with an owner.

## Scope

- [ ] Add `docs/security/threat-models/integrations.md` from `docs/templates/threat-model.md`, covering: the relay
      copy of integration events, outbound webhook subscription and signed delivery, retry, dead letter and
      operator-authorised replay, provider configuration and its feature flag and support owner, the payment intent
      and callback sequence up to the point Billing applies the outcome, the accounting export batch and its
      acknowledgement, the print bridge, and every outbound call's passage through `IOutboundHttp`.
- [ ] Add `docs/security/threat-models/deployment.md`, covering: the reverse proxy and TLS termination, forwarded
      headers and the trusted client address, published versus internal ports, the container images and the runtime
      they run as, the secret mechanism under `/run/secrets` and its rotation, the Data Protection key ring, the
      database and object-storage credentials, the worker host and its leases, the observability collector and what
      may leave in telemetry, the staging environment's guard-rails, the repository and branch protections, and the
      supply chain from dependency to built image.
- [ ] Both: every template section filled; every "(example — delete)" row and the "How to use" section removed.
- [ ] `integrations.md` models `ABF-09` explicitly and honestly: the control is `IOutboundHttp` with the address,
      redirect and size rules of `NFR-SE-10`; **no implementation exists**; the control is therefore a requirement on
      #54 and #55, and until then `ABF-09` is a residual risk in section 9 with an owner and an acceptance date.
- [ ] `integrations.md` models the callback surface: a forged callback with a valid signature but no matching
      provider status, a callback arriving before the status poll, a replayed callback, and a provider idempotency key
      reused — each with the control and the test that must exist (the fault-injection suite plan Section 8 requires
      of #55).
- [ ] `integrations.md` models webhook secret handling: stored how, rotated how, never logged, with the rotation
      window and the dead-letter path; and the subscription allowlist and branch scope as the server-side request
      forgery boundary for caller-supplied URLs.
- [ ] `deployment.md` models the secret mechanism as the control for every **Credentials and secrets** asset, citing
      `docs/platform/secrets.md` and section 6 of `docs/nfr/data-classification.md`, and records what happens on a
      rotation and on a leak.
- [ ] `deployment.md` picks up `authentication.md` **RR-04** and **AS-03** as its own rows — a residual risk recorded
      in one model and ignored in the model that actually owns the component is the failure this step prevents.
- [ ] `deployment.md` states the supply-chain controls that exist with their real gate severities (Trivy config at
      9.0, Trivy dependencies at 7.0, CodeQL through `scripts/sarif-gate.py`, gitleaks over full history, the Syft
      bill of materials) and names `RG-11`'s image signing and provenance as **#59's**, not as present.
- [ ] Section 7 of each model cites the `ABF-nn` family for every abuse case; section 8 names the test, the
      architecture rule or the continuous-integration job that fails if each control is removed, or says "no test
      yet — #NN"; section 9 carries a residual-risk table with rating, owner and acceptance date, and any **high**
      risk also gets a `docs/process/waivers.md` entry with an expiry and a corrective issue.
- [ ] Section 10 of each model: the ASVS requirements the flow is judged against, from
      `docs/security/asvs-traceability.md`; if that sheet has not merged, record its four-value status vocabulary and
      name the sub-issue that fills the rows. Invent no requirement number.
- [ ] Update `docs/security/README.md` sections 2 and 3.2 for these two flows, with this sub-issue's number and the
      date. If this is the last model sub-issue of this parent to merge, **delete section 3.2's stand-in table** as
      its own preamble instructs ("it is deleted when #56a completes the set") and replace it with the index of the
      ten written models; if it is not the last, say in one line which rows remain.
- [ ] Versioned configuration (DOR-11): none added. Where a control is configuration — the provider feature flag, the
      webhook allowlist, the rate-limit policy, the published port set — the model names the key or the file and says
      the **value** belongs to the owning issue and, for providers, to **OD-03**.

## Out of scope

The authorisation model (#32a); the other seven flows (the other **three** model sub-issues of this parent); backups, restore
and disaster recovery, which are #60's and are modelled there — `deployment.md` ends at "a backup exists and its key
is escrowed" and cites `NFR-BR-07`; the hardened environments, image signing, provenance and the deploy-time
signature check, which are #59's and `RG-11`'s; and implementing anything — #54, #55, #200, #202, #203, #205 to #207,
#59, #60. No test, no source file, no workflow and no configuration change in this pull request.

## Acceptance criteria

- [ ] Both files exist, are complete against the template (no placeholder row, no "How to use" section) and are
      reachable from `docs/security/README.md` sections 2 and 3.2, which name this sub-issue.
- [ ] `authentication.md`'s two forward references resolve: "TLS termination, the reverse proxy and the container
      runtime" and "the mail relay's own security" are both genuinely covered by `deployment.md`, and
      `./scripts/dev docs` proves the links resolve.
- [ ] `integrations.md` section 5 lists every designed entry point — the inbound callback route, the administration
      routes, the worker jobs — with the permission or the justified anonymous exposure each must declare (ARCH-007,
      ARCH-022) and the rate-limit policy from
      `src/Platform/Tailor360.Platform.Security/Endpoints/RateLimitPolicyNames.cs` (ARCH-017), and states that none is
      published today.
- [ ] Every asset in section 3 of both models carries one of the seven classes of
      `docs/nfr/data-classification.md` section 2, and every provider credential, signing secret, database
      credential and key-ring entry is **Credentials and secrets** pointing at section 6 of that document.
- [ ] Every STRIDE row ends in a control or a residual risk; every abuse case cites an `ABF-nn` family; every control
      names a test, an `ARCH-nnn` rule or a continuous-integration job that exists, or says "no test yet — #NN".
- [ ] **Negative case, `ABF-09`**: `integrations.md` records that an outbound call to `127.0.0.1`, to a private
      range, to a link-local metadata address, or through a redirect to one, is **not refused by anything today**,
      names `IOutboundHttp` as the control, #54 and #55 as the owners, and carries the residual risk with an owner
      and an acceptance date. A model that claims this is covered fails review.
- [ ] **Negative case, `ABF-11`**: both models record at least one denial-of-service path with its control — a
      provider quota exhausted by retries, a dead-letter queue that grows without bound, an upload that exhausts
      storage, a scan that exhausts worker leases, a published port that should not be published — citing the
      rate-limit policy catalogue and `docs/nfr/capacity-and-performance.md` rather than inventing a limit.
- [ ] **Negative case, secrets**: `deployment.md` records what must happen when a secret reaches a log, a
      problem-details body, a health payload or telemetry (`NFR-SE-09`), and names the gitleaks job and the
      redaction tests as the controls.
- [ ] **Exception case**: each model documents the sanctioned way out and what it must not do — an operator-authorised
      webhook replay (authorised, audited, never a second side effect), a secret rotation (old secret honoured for a
      stated window, never two live secrets indefinitely), a provider disabled by flag (never a silent fallback to a
      fake in production — the startup guard plan Section 8 requires), and an accepted scan finding (an entry in the
      exception register with an expiry, which the sibling sub-issue of this parent builds).
- [ ] No invented number: no retry count, backoff interval, rate limit, token lifetime, rotation period or port
      number is written as a figure unless it is already in `infra/`, `docs/nfr/capacity-and-performance.md` or
      `docs/nfr/security-operations-targets.md` — in which case the row cites the source.
- [ ] `./scripts/dev docs` passes.

## Data and classification

This sub-issue adds two documents; it adds no field, table, event, log field, telemetry attribute or export column.
Each model's section 3 is where DOR-06 is answered for its flow.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| `docs/security/threat-models/integrations.md` | Internal | The argument that the system's edge cannot be used to forge, replay or amplify, and that no outbound call is unconstrained | Lives with the repository; superseded, never deleted | Every contributor | Never logged; summarised into release evidence item 9 |
| `docs/security/threat-models/deployment.md` | Internal | The argument that the runtime, its secrets and its supply chain are trustworthy | As above | As above | As above |
| Asset rows naming a provider credential, signing secret, database credential or key-ring entry | Internal, describing a **Credentials and secrets** asset | Say what must never be committed, logged or built into an image | As above | As above | **No value, no fragment and no example that could be mistaken for one.** The documents name the configuration key and the file under `/run/secrets`, never a secret. `.gitleaks.toml` allowlists exact literals and these documents add none |
| Asset rows naming a published port or internal hostname | Internal | Show the attack surface | As above | As above | Ports and service names already in `infra/compose/*.yml` may be cited; no external hostname, no IP address of a real deployment, and no staging URL appears |
| Telemetry and log fields described in `deployment.md` | Internal | Show what may leave the system | As above | As above | The model restates the redaction policy of `docs/nfr/data-classification.md` section 8 by reference, and demonstrates it with no real value |

## Permissions and roles

No permission key is added, removed or changed; the authorisation-matrix fixtures in
`tests/Tailor360.IntegrationTests/Authorization/matrix.yaml` are cited as evidence and not edited.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `integration.manage_webhooks` | branch | per the matrix | per the matrix | yes | per the matrix | **Exists** — `IntegrationPermissions.cs`; **no endpoint declares it yet**, recorded as a finding against #202 |
| `integration.replay_delivery` | branch | per the matrix | per the matrix | yes | per the matrix | **Exists**; the operator-authorised replay control of #207 |
| `admin.feature_flags` | organisation | per the matrix | per the matrix | yes | per the matrix | **Exists** — `PlatformPermissions.cs`; the control over enabling a provider adapter, and the subject of the startup guard that forbids a fake in production |
| `audit.export` | organisation | per the matrix | per the matrix | yes | per the matrix | **Exists**; cited by `deployment.md` where evidence leaves the system |
| The inbound provider callback route | — | — | — | — | — | **Anonymous by necessity.** The model records it as an `AllowAnonymousWithJustification` case (ARCH-007, ARCH-022) whose justification, rate-limit policy and signature verification are the controls, and names #200 as the owner. This sub-issue proposes no permission of its own |

**OD-13** is open, so every grant cited is the documented 2026-09-06 default rather than an approved one; each model
says so once in section 5.

## Modules, contracts and events

Modules read: Integration (schema `integration`, not created) and Platform; every other module only through the
outbox relay. No source file changes, so no architecture rule can be violated by this pull request; the models
**assert** ARCH-009 (provider SDKs only in `Integration.Infrastructure` and tests), ARCH-016 (`IOutboundHttp`),
ARCH-017 (one rate-limit policy per endpoint), ARCH-020 (`IWorkerScopeFactory` only in the worker and command-line
hosts), ARCH-021 (`[WorkerJob]` declares permissions and branch scope) and ARCH-022 as controls, naming the
architecture test that enforces each.

Cross-module mechanisms to name exactly as `docs/architecture/module-ownership.md` section 5.11 and CLAUDE.md
section 3 fix them: the Platform outbox relay as the only inbound path for other modules' facts; the published events
`integration.webhook-delivered.v1`, `integration.webhook-dead-lettered.v1`,
`integration.payment-callback-reconciled.v1`; the platform ports `IOutboundHttp`, `IObjectStorage`, `IEmailSender`,
`IPrintQueue`, `IPdfRenderer`, `IBarcodeRenderer`; and the rule that a payment callback records a verified outcome
while **Billing** applies it. **DOR-05**: this sub-issue is threat-model work and closes the last two rows of
`docs/security/README.md` section 3.2 that this parent owns.

## Screens and states

Deleted: a documentation-only Lane C sub-issue publishes no screen. Where a control belongs to an administration
screen — the webhook subscription form's URL allowlist, the provider configuration's flag — the model names the
owning issue (#202, #207) and the state it must not reach.

## Migration and rollback

No table, no column, no migration. `dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` reports no
outstanding change and that output is the evidence. The models describe the **designed** `integration` tables;
creating them belongs to #54 and its sub-issues. Rollback is a revert of the pull request; nothing at run time reads
these files, and no workflow or image changes.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-03** — providers: SMS, WhatsApp and email vendors, the payment gateway, the accounting export target | **Open** — needed before W4 | Fakes only, and no adapter is enabled without a `docs/integrations/<provider>.md` support-ownership document. `integrations.md` models the **port and the sequence**, never a named vendor's behaviour | W4 |
| **OD-02** — hosting model and indicative monthly budget | **Open** — needed before W0 exit | `docs/adr/0010-deployment-portability.md` keeps the topology portable; `deployment.md` models what `infra/` contains today and records which rows change with the hosting answer | W0 exit gate (overdue) |
| **OD-14** — telemetry backend | **Open** — needed before W5 | A collector on the application virtual machine (`infra/observability/otel-collector-config.yaml`); `deployment.md` models what may leave in telemetry regardless of backend | W5 |
| **OD-15** — operations ownership and alert channel | **Open** — needed before W5 | Alerts fire into the channel #58 configures; `deployment.md` records the detective controls that need a receiver and names #58 | W5 |
| **OD-08** — retention periods, including logs and backups | **Open** | No period written; every retention cell reads "Open — OD-08" and names #57 and #60 | W1 exit gate (overdue) |
| **RG-OD-01** — the proposed severities and waiver durations | **Open** — needed before the W1 exit gate | The blocking severities of `docs/process/release-gates.md` section 4, and the Trivy and CodeQL gate thresholds as `.github/workflows/ci.yml` sets them today | W2 exit gate |
| **RG-OD-02** — who holds the security owner role | **Open** — needed before W2 | The Owner, with the technical reviewer, is the residual-risk owner in section 9 of both models | W2 exit gate |
| **OD-13** — approved role-to-permission grants | **Open** | The twelve seeded roles and their default grants, cited as default | W1 exit gate (overdue) |

## Required verification

- [ ] Documentation link check: `./scripts/dev docs`, plus `python3 scripts/check-docs-links.py --self-test`. This
      sub-issue is the one that makes `authentication.md`'s two forward links resolve, so the link check is part of
      its acceptance, not a formality.
- [ ] Path and symbol verification pasted into the pull request: every file under `infra/`, every workflow job and
      step name, every gate severity, every port, every permission key, every `ARCH-nnn` rule and every `NFR-` row
      cited exists today — `grep`/`ls` output. The absence this sub-issue leans on must be shown, not asserted:
      `grep -rl IOutboundHttp src/` listing only the port and `src/Modules/CLAUDE.md`.
- [ ] Template completeness: `grep -c "example — delete"` returns 0 for both files and the "How to use" section is
      absent.
- [ ] Secret-leak check on the pull request itself: the `security` job's gitleaks run passes with **no new
      `.gitleaks.toml` allowlist entry**. A documentation change that needs one has written down a secret.
- [ ] **DOR-13**: the `docs/nfr/traceability.md` rows these models become the evidence for are `NFR-SE-09`,
      `NFR-SE-10`, `NFR-PR-06`, `NFR-BR-07` and `NFR-SE-07`; no status changes, because a model is not a proof.
- [ ] **DOR-16**: Python 3 for the link checker and the .NET command line for the dry-run; `./scripts/dev doctor`
      records both. No container runtime is needed: the deployment model is written from `infra/` and the documents,
      not from a running stack.

## Evidence the pull request will carry

- [ ] The rendered diff of both models, with section 6 (STRIDE), section 8 (controls and the test, rule or job that
      proves each) and section 9 (residual risk with owner and date) visible for each.
- [ ] The rendered diff of `docs/security/README.md` sections 2 and 3.2 — including, if this is the last model
      sub-issue to merge, the removal of the stand-in table and its replacement by the index of ten models.
- [ ] `./scripts/dev docs` output and the link-checker self-test output.
- [ ] The `grep`/`ls` transcript proving every cited path, workflow step, gate severity and permission key exists,
      and the `IOutboundHttp` grep showing no implementation.
- [ ] `grep -c "example — delete"` showing zero placeholder rows.
- [ ] The green `security` job, as evidence that no secret was written down.
- [ ] `migrate --dry-run` output showing no outstanding migration.
- [ ] The `docs/process/waivers.md` diff for any **high** residual risk carried — `ABF-09` having no implemented
      control is the most likely candidate, and it needs an expiry and a corrective issue.

## Depends on

- The abuse-case catalogue sub-issue of this parent — provides `abuse-cases.md`, the `ABF-nn` identifiers and the
  template's family column. **Must be merged first.**
- The ASVS skeleton sub-issue of this parent — provides the section 10 vocabulary and requirement numbers; if it has
  not merged, section 10 records the vocabulary and names that sub-issue.
- The other three model sub-issues of this parent, **only** for the section 3.2 clean-up: whichever merges last
  deletes the stand-in table. If this one merges earlier it leaves the table in place and says which rows remain.
  Nothing else in this sub-issue waits for them.
- Merged work it reads: #19 (`data-classification.md`, `traceability.md`, `security-operations-targets.md`,
  `release-gates.md`), #20 and #21 (the Platform mechanisms, the key ring, the outbox), #22 (`infra/`, the continuous
  integration workflow, the interim staging environment, `branch-protection.md`), #23 (`authentication.md`, RR-04 and
  AS-03), #53 (the security headers and the problem-details envelope), and the Integration infrastructure already in
  `main`.

**In-flight work in the same area**: #200, #202, #203, #205, #206, #207 (the integration surface). None touches
`docs/security/threat-models/`. The only file shared with a sibling model sub-issue of this parent is
`docs/security/README.md`, where this one edits two rows of section 3.2 and two rows of section 2, plus the stand-in
table if it merges last; whichever merges second rebases.

Refs #56
