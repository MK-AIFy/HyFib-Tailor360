Sub-issue of #57. The third and last client child: **the two surfaces that explain why an erasure did not happen** —
the retention-hold register, where a hold is placed and released, and the exception report, where every skip is named
with its reason. `docs/nfr/data-classification.md` section 7 item 4 is the requirement: "Every skipped record appears
in an exception report; a silent skip is a defect." A report nobody can read is a silent skip with extra steps.

It is split from `[E14-F02-8]` because that child is a decision screen with two commands and a confirmation, which the
repository's own history makes a session on its own: `StaffDetailRoute.tsx` is 267 lines and `RoleDetailRoute.tsx` 261
for that shape, `TemplateDetailRoute.tsx` 652 once an irreversible act is added, and
`clients/pwa/src/routes/catalog/CatalogVersionEditorRoute.tsx` is 994 for one route. Two list screens plus two
commands is the other session.

## What is already built (do not re-build)

- `[E14-F02-7]` has already added the `/admin/privacy` register, the `privacyRead` and `retentionPolicies` entries in
  `clients/pwa/src/admin/adminPermissions.ts`, two `ADMIN_DESTINATIONS` entries, and `listPrivacyRequests` in
  `clients/pwa/src/admin/adminApi.ts`. `[E14-F02-8]` has already added `privacyApprove` to `ADMIN_PERMISSIONS`,
  `readPrivacyRequest`, `approvePrivacyRequest`, `refusePrivacyRequest` and **`listRetentionHolds`** to `adminApi.ts`,
  and the decision screen that reads holds read-only. **Extend all of that; do not duplicate it.**
- `clients/pwa/src/routes/admin/BranchListRoute.tsx` (198 lines) and `FeatureFlagRoute.tsx` (166 lines) — the two
  closest existing shapes: a register with a create-or-change form, a reason, a confirmation and the three states.
  **Model the holds register on them.**
- `clients/pwa/src/routes/admin/AuditTrailRoute.tsx` (161 lines) and `OutboxRoute.tsx` (186 lines) — the keyset-paging
  idiom (`useAdminResource` keyed on `${applied}|${cursor ?? ''}`, a paging **button** and not infinite scroll, with
  `seen` concatenated across pages) and the labelled filter input beside it. **Model the exception report on them.**
- `clients/pwa/src/admin/useAdminResource.ts` (83 lines) — the fetch hook every admin screen uses, keyed on a string so
  a changed filter refetches.
- `clients/pwa/src/routes/admin/AdminReasonField.tsx` — the shared reason input with a persistent visible label. Both
  commands here use it; do not write a second reason field.
- **Step-up is handled centrally and must not be re-implemented.** `clients/pwa/src/auth/apiClient.ts` declares
  `STEP_UP_REQUIRED_CODE = 'security.step-up-required'` (line 113), `SessionChallengeReason` (line 187),
  `setSessionChallengeHandler` (line 204) and the retry at line 418 with `challengeOnStepUp` defaulting to on
  (line 419). **Import it; never edit it.** `admin.privacy.hold` is catalogued **without** step-up, so the happy path
  here does not raise a challenge — which is exactly why the test must still prove the screen renders the `403` path
  correctly if a future catalogue change adds one. There is no `AdminStepUp.tsx` component, only
  `AdminStepUp.test.tsx` (210 lines) and `AdminStepUp.stories.tsx`.
- `clients/pwa/src/admin/RequirePermission.tsx` (45 lines) — renders an `EmptyState` with `admin.forbidden.title` /
  `admin.forbidden.body`. **Not the `Forbidden` component, and it never redirects.**
- `clients/pwa/src/components/states/` (`LoadingState`, `EmptyState`), `clients/pwa/src/auth/AuthProblemAlert.tsx`,
  `clients/pwa/src/i18n/formatters.ts` for every instant, and `clients/pwa/src/admin/adminDestinations.ts`, whose order
  is deliberately fixed ("a person who finds Roles in the fourth place today must find it in the fourth place
  tomorrow — 3.2.3 Consistent Navigation") — so say in the pull request where the new entry goes and why.
- **Testing**: `clients/pwa/src/routes/admin/AdminScreens.test.tsx` (415 lines) — `MemoryRouter` + `SessionProvider` +
  `RequireSession` + `AppIntlProvider`, with `stubFetch`, `jsonResponse`, `problemResponse` and `aCurrentUser` from
  `clients/pwa/src/auth/testing/fixtures` and `versionedResponse` from `clients/pwa/src/admin/testing/fixtures.ts`, plus
  `expectNoAccessibilityViolations` from `clients/pwa/src/design-system/testing/axe.ts`.
  **`renderWithProviders` is not usable** — it supplies `AppIntlProvider` and display preferences only, with no router
  and no `SessionProvider`, so a route rendered in it throws.
- **Stories**: `clients/pwa/src/admin/testing/storyTransport.tsx` — `storyJson`, `storyProblem`, `storyPending`,
  `withAdminApi`, `STORY_USER`, `StoryRoutes`. `JourneyFrame` wraps children in its **own** `MemoryRouter` and cannot
  be combined with `withAdminApi` / `StoryRoutes` — pick `withAdminApi`. There are deliberately **no offline stories
  for admin screens**; `adminScreens.stories.tsx` lines 36–40 record why, so **no offline story and no offline
  screenshot is asked for below**.
- `clients/pwa/src/design-system/testing/expectNoHorizontalOverflow.ts` — already defaults to 320, 360, 768, 1024 and
  1280 px at 100% and 200% zoom. **Use the default; do not narrow it.**

## What is missing

- Nothing shows a retention hold, so an operator answering a legal letter cannot stop an erasure from a screen, and
  `admin.privacy.hold` — a permission `[E14-F02-4]` proposes and the server enforces — is unusable from the client.
- Nothing shows the exception report, so a skip is invisible to everyone who is not reading the database.

## Scope

- [ ] **`/admin/privacy/holds` — the hold register.** New `clients/pwa/src/routes/admin/RetentionHoldRoute.tsx`. Lists
      holds with filters for open-or-released, subject and data class, newest first, paged by button. A **Place hold**
      form takes the subject type, the subject identifier, an optional data class (empty meaning every class for that
      subject, stated in words on the form and not left to be inferred), a kind of `Legal` or `Business`, and a reason
      through `AdminReasonField`, with a caller-minted `Idempotency-Key`. A **Release** action per open hold takes a
      reason, a confirmation, the `If-Match` from the read, and an `Idempotency-Key`.
- [ ] **The subject identifier is typed, and that is a designed risk.** The form cannot offer a customer picker,
      because resolving an identifier to a name would put a name on a privacy screen, which
      `docs/nfr/data-classification.md` section 5.2 rules out here. So the form validates the identifier's **shape**
      client-side (a UUID), the server answers `422` with a field error when it names nothing, and the confirmation
      echoes the identifier back verbatim before the hold is placed. Say all three in the pull request: this is the one
      place in #57 where a person types an identifier by hand.
- [ ] **`/admin/privacy/runs` — the exception report.** New `clients/pwa/src/routes/admin/RetentionRunRoute.tsx`. Lists
      runs newest first with their kind (`RequestExecution` or `PolicySweep`), start and finish instants, outcome and
      counts, and each run's exceptions **grouped by reason code** — `hold`, `statutory_retention`, `pending_decision`,
      `participant_error`. A `pending_decision` exception shows the **decision id** — `OD-05`, `OD-08`, `DC-09`,
      `OD-14` — because "we did not delete this because nobody has decided how long to keep it" is the honest message
      and an empty cell is not.
- [ ] **API functions** in `clients/pwa/src/admin/adminApi.ts`: `placeRetentionHold`, `releaseRetentionHold` and
      `listRetentionRuns`. `listRetentionHolds` already exists from `[E14-F02-8]` and is **reused with a wider filter**,
      not re-declared. Use `apiRequestVersioned` for the read that feeds the release `If-Match`.
- [ ] **Types** in `clients/pwa/src/admin/types.ts` after `pnpm --dir clients/pwa generate:api`, with
      `generate:api:check` passing.
- [ ] **Permissions, navigation and routes**: add `privacyHold: 'admin.privacy.hold'` to `ADMIN_PERMISSIONS`; add
      **one** `ADMIN_DESTINATIONS` entry for the exception report on `privacyRead` (the holds register is reached from
      it and from the decision screen, so it gets no second navigation entry — say why in the pull request); add both
      routes to `clients/pwa/src/app/router.tsx`, each wrapped in `RequirePermission` on `privacyRead`, with the
      **Place-hold and Release actions gated separately** on `privacyHold`, the way `TemplateDetailRoute` gates publish
      separately from edit.
- [ ] **Messages** in `clients/pwa/src/i18n/en-IN.ts` and `ta-IN.ts`, whose `MessageKey` is typed from `en-IN`, so a
      missing Tamil key is a type error and both files change together. The reason-code-to-sentence mapper is a
      **pure, unit-tested function** shared with `[E14-F02-8]` — if that child landed first, **import its mapper; do
      not write a second one**, and say in the pull request which way round it happened.
- [ ] **No invented copy about what a hold means to a customer.** **DC-01** is open. The screens name fields and
      explain system behaviour; they make no statement to or about a customer.
- [ ] **State stories** in `clients/pwa/src/routes/admin/adminScreens.stories.tsx` with `withAdminApi`: loading, empty,
      error and forbidden for both screens, plus the two that carry the risk — a run whose `exceptions` cover all four
      reason codes, and a hold that is already released. **No offline story.**

## Out of scope

The two register screens and the retention policy form are `[E14-F02-7]`; the request decision screen is
`[E14-F02-8]`, and the read-only holds panel on it is **its** work, not a second copy here. The correlation-id filter
on the audit screen is `[E14-F02-5]`. **No change to `clients/pwa/src/auth/apiClient.ts`**, to the service worker or to
offline behaviour (#51). Media holds have no client surface here: #192 builds `media_retention_holds`, which is a
different table at a different granularity, and its own parent owns its screens. Nothing here resolves a subject
identifier to a customer name.

## Acceptance criteria

- [ ] An Owner opens `/admin/privacy/holds`, places a legal hold on a typed subject identifier with a reason, confirms,
      and sees it listed as open with its kind, reason, actor and instant; releasing it with a reason shows it as
      released with **both** reasons and **both** actors still readable. Driven **through the screen** with the API
      stubbed at the transport.
- [ ] `/admin/privacy/runs` shows a run with one `hold` skip, one `statutory_retention` skip, one `pending_decision`
      skip **naming its decision id** and one `participant_error`, each grouped under its reason code with a sentence a
      non-engineer can act on.
- [ ] An Auditor holding `admin.privacy.read` but **not** `admin.privacy.hold` sees both screens and sees **no**
      Place-hold or Release action — and the absence is explained in text, not silent.
- [ ] **Negative**: a `422` field error on the place-hold form — an identifier that names nothing, a missing reason, a
      reason over the server's maximum — is shown **against the field it names**, the form keeps what was typed, and the
      reason is not cleared.
- [ ] **Negative**: a client-side malformed identifier (not a UUID) is refused before the request is made, with a
      visible message on the field, and the request is **not** sent — asserted by the transport stub recording no call.
- [ ] **Negative**: a stale `If-Match` on Release producing `409` is explained with a way to reload, and the typed
      reason is **not** lost by the reload prompt.
- [ ] **Negative**: `403 security.step-up-required` on either command is renewed in place and the request retried once
      by the central session-challenge handler, rather than rendering as a generic failure (defect #99, closed).
      Asserted by installing a handler with `setSessionChallengeHandler`, as `AdminStepUp.test.tsx` does — **even
      though `admin.privacy.hold` carries no step-up today**, because a screen that cannot survive one is the same
      defect waiting for a catalogue change.
- [ ] **Negative**: `RequirePermission` on `privacyRead` renders the `EmptyState` with `admin.forbidden.title` /
      `admin.forbidden.body` on both routes — not a redirect, not a blank screen.
- [ ] **Negative**: an empty hold register and an empty **filtered** hold register render **different** empty states —
      "no hold has been placed" is not the same answer as "no hold matches that filter" — and the filter stays visible
      so it can be cleared.
- [ ] **Exception**: pressing Place hold twice sends the **same** `Idempotency-Key` and the register shows one hold.
- [ ] **Exception**: a run with **no** exceptions renders an empty state phrased as **good news** ("nothing was
      skipped"), not as an absence of data — the two are different facts and a reader must be able to tell them apart.
- [ ] **Exception**: an unknown reason code from a newer server renders as the code itself rather than as blank, proved
      by a unit test on the mapper.
- [ ] `expectNoAccessibilityViolations` passes on both screens; the release confirmation is reachable and dismissible
      by keyboard with focus returned to the trigger; `expectNoHorizontalOverflow` passes at its **default** 320, 360,
      768, 1024 and 1280 px at 100% and 200% zoom.
- [ ] Every string is a message id present in **both** `en-IN.ts` and `ta-IN.ts`; the pseudo-locale run shows no
      truncation, including on the grouped exception headings.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Hold fields (kind, data class, instants, actor identifiers) | Internal, with Personal identifiers | Explain why an erasure is blocked | Not stored on the client | Owner, Admin, Auditor per the server's grants | Identifiers only, never a name; nothing is written to `localStorage`, `sessionStorage` or IndexedDB; nothing is logged to the console in production |
| Hold reason and release reason | Personal | Free text a member of staff typed about why a record must be kept | Held in component state until submitted, then discarded | The server records them | Never logged, never in a query string, never in a URL |
| The subject identifier typed into the place-hold form | Personal — identifier only | Name the record to protect | Component state until submitted | As above | **Never put in the query string or the path**, because the address bar is copied into chat messages and tickets; held in component state, matching how `AuditTrailRoute` keeps its filter |
| Run rows, counts and outcome | Internal | Prove a run happened and what it did | Not stored on the client | As above | Safe |
| Exception entries (class, subject identifier, reason code, detail) | Internal, with Personal identifiers | The exception report | Not stored on the client | As above | Counts and reason codes are safe in telemetry; **subject identifiers are not sent as telemetry attributes** |
| Filter values in the URL | Internal | Let a reader share a view | As the browser's history | The reader | A status, kind or reason-code filter is safe in a URL; **a subject identifier is not** |
| `If-Match` value and `Idempotency-Key` | Internal | Make the release safe to retry and safe against a concurrent change | Per submission | — | Never logged |

Nothing Sensitive Personal reaches either screen: no measurement, no image, no rendered message body.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `admin.privacy.read` | organisation | yes | no | no | `owner`, `auditor` (Owner, Auditor) | **Proposed by `[E14-F02-1]`.** Gates both routes. Cited, not re-declared |
| `admin.privacy.hold` | organisation | yes | **no** | yes | `owner`, `admin` (Owner, Admin) | **Proposed by `[E14-F02-4]`**, catalogued `RequiresStepUp: false`, which is why no step-up is expected on the happy path. Gates Place hold and Release. **Note the asymmetry `[E14-F02-4]` records**: `admin` may place a hold and is not granted `admin.privacy.read`, so an Admin reaching these screens sees the forbidden state. That is the backend's matrix row to change if the reviewer judges it unusable, not this screen's |
| `admin.privacy.approve_erasure` | organisation | yes | yes | yes | `owner` (Owner) | **Proposed by `[E14-F02-2]`.** Named because this screen links to the decision screen that uses it; no action here carries it |

The client decides nothing from a key: `clients/pwa/src/admin/adminPermissions.ts` says so in its own comment, and the
server re-checks every request. **OD-13** is open, so every grant above is the proposal the backend children record.

## Modules, contracts and events

Client only: `clients/pwa/`. No module, no schema, no migration, no contract, no integration event. The one generated
artefact is `clients/pwa/src/api/schema.d.ts`, regenerated from `docs/api/openapi.v1.json`.

Threat-model coverage: the abuse cases are "a hold used to keep data for ever" and "a privacy screen left open on a
shared counter browser". The first is made **visible** here — every open hold is listed with its reason and its age,
and every run repeats the skip it caused — and is bounded server-side by the mandatory reason and the access-review
cadence of `docs/nfr/security-operations-targets.md` section 5. The second by the server's `no-store`, by no
client-side persistence, and by keeping the subject identifier out of the URL. The customer-data threat model is
scheduled by #56's children (`docs/security/README.md` section 3.2).

## Screens and states

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| Hold register | `/admin/privacy/holds` | `LoadingState` while the register is in flight | Two **distinct** empty states: no hold has ever been placed, with the form still reachable; and no hold matches the filter, with the filter kept visible so it can be cleared | `AuthProblemAlert` with a retry; a `422` on the form shows against the named field; `409` on Release offers a reload that preserves the typed reason | Not a designed state: the section is behind `RequireSession` and the reads are `no-store`, so a failed fetch is the error state. Client-wide offline behaviour is #51, and admin screens deliberately have no offline story | `RequirePermission` on `privacyRead` renders `EmptyState` with `admin.forbidden.title` / `admin.forbidden.body`; within the screen an action the caller cannot use is absent **with a sentence saying why** |
| Exception report | `/admin/privacy/runs` | `LoadingState` | Two **distinct** empty states: no run has completed yet, and a run with no exceptions — the second is good news and is phrased as such | `AuthProblemAlert` with a retry, the filter preserved so the reader does not retype it | As above | As above |

Message ids are added to both `en-IN.ts` and `ta-IN.ts`. Instants go through `clients/pwa/src/i18n/formatters.ts`. The
release confirmation is a focus-trapped dialog whose focus returns to its trigger on dismissal; labels are persistent
and visible, never placeholder-only; and no state — including a released hold — is conveyed by colour alone
(`docs/nfr/a11y-checklist.md`, `clients/pwa/CLAUDE.md`).

## Migration and rollback

No schema change — a client slice. Proved rather than asserted: the pull request carries
`dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` reporting no outstanding change. Rollback is
reverting the commit; both routes are additive, and the only inbound link is the one `[E14-F02-8]`'s decision screen
adds, so a revert should remove that link too or leave it resolving to the router's existing
`{ path: '*', element: <NotFoundRoute /> }` catch-all rather than to a crash.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-05**, **OD-08**, **DC-09**, **OD-14** — retention periods | Open | A `pending_decision` exception **names its decision id on screen**. The screens never render a period they were not given, and never an empty cell where a decision is missing | The owner confirms them, which is then a configuration change and no client change |
| **DC-01** — lawful basis and the customer-facing wording | Open (`docs/nfr/data-classification.md` section 11) | The screens name fields and explain system behaviour; no customer-facing sentence is written | A customer-facing privacy notice is published |
| **OD-13** — approved role-to-permission grants | Open | Actions are offered on the keys the backend children propose, including the `admin.privacy.hold` / `admin.privacy.read` asymmetry, which is named rather than worked around | Release |
| **OD-15** — who operates the system | Open | The exception report names a **role** where it explains who should act on a `participant_error`, never a person | The first production release |
| **AL-08** / **OD-MEA-10** — the Tamil label review, and the Tamil glossary review owed by `docs/prd/glossary.md` | Open (`docs/nfr/accessibility-localisation.md` rows AL-01 and AL-08) | Both locale files are populated and the pseudo-locale run is the evidence; a native-speaker review of the new strings is a separate recorded step and is **not** claimed here | The first release with Tamil offered |

## Required verification

- [ ] **Client (Vitest, jsdom)**: place then release a hold through the screen; the subject-wide hold with no data
      class; both filters reaching the request; the paging button; the two distinct empty states on each screen; the
      malformed identifier refused before the request is made; the `422` field error keeping the typed reason; the
      stale `If-Match` `409` preserving it; `403 security.step-up-required` renewed in place; the read-only Auditor
      seeing no actions and being told why; the forbidden render through `RequirePermission`; the exception report's
      four reason codes and the unknown-code fallback; `expectNoAccessibilityViolations` including keyboard operation
      of the confirmation; and `expectNoHorizontalOverflow` at its default widths and zooms. Harness: the providers and
      `stubFetch` of `clients/pwa/src/routes/admin/AdminScreens.test.tsx`.
- [ ] **Unit (client)**: the reason-code-to-sentence mapper including an unknown code — **or**, if `[E14-F02-8]` landed
      first, an assertion that this child imports that mapper rather than declaring a second.
- [ ] **Contract**: `pnpm --dir clients/pwa generate:api:check` passes, proving `schema.d.ts` matches
      `docs/api/openapi.v1.json`.
- [ ] **Lint, types, format**: `pnpm --dir clients/pwa lint`, `typecheck` and `format:check`.
- [ ] Non-functional rows affected in `docs/nfr/traceability.md`: the accessibility and localisation rows for a new
      administration screen, and **NFR-PR-02** (line 161, the exception report) — which this child completes on the
      read side. **AL-01** (whether an external accessibility audit is commissioned) is open and is **not** claimed.
- [ ] Environment: `./scripts/dev doctor`; the client tier needs only `pnpm`, so it runs everywhere. **No hardware and
      no human sign-off is needed**; the native-speaker Tamil review is a separate recorded step under AL-08.

## Evidence the pull request will carry

- [ ] `pnpm --dir clients/pwa test`, `lint`, `typecheck`, `format:check` and `generate:api:check` output.
- [ ] Screenshots of both screens at 360 px, 768 px and 1280 px, including the release confirmation open and a run
      showing all four reason codes, plus the pseudo-locale run.
- [ ] The axe run for both screens, a note on the keyboard path through the confirmation, and the
      `expectNoHorizontalOverflow` result at its default widths and zooms.
- [ ] The state stories as rendered captures: loading, empty, error, forbidden, all-four-reason-codes and
      already-released. **No offline story** — `adminScreens.stories.tsx` lines 36–40 record why admin screens
      deliberately have none.
- [ ] A capture of a `pending_decision` exception showing its decision id, and of the "nothing was skipped" empty
      state.
- [ ] `migrate --dry-run` showing no outstanding change.

## Depends on

`[E14-F02-4]` — `POST /api/v1/admin/privacy/holds`, `POST /api/v1/admin/privacy/holds/{holdId}/release`,
`GET /api/v1/admin/privacy/holds`, `GET /api/v1/admin/privacy/runs` and `admin.privacy.hold`. Must be **merged**,
because `generate:api:check` reads the committed API document.
`[E14-F02-7]` — the `privacyRead` key and the `ADMIN_DESTINATIONS` pattern. Must be **merged**.
`[E14-F02-8]` — `listRetentionHolds` in `adminApi.ts` and the reason-code mapper. **Not a hard dependency**: if it has
not merged, this child declares both and `[E14-F02-8]` then imports them, which is why both bodies say to check which
way round it happened rather than assuming.
`[E14-F02-4b]` — **not** a dependency. The exception report renders whatever rows `platform.retention_runs` holds, and
the "no run has completed yet" empty state is the designed answer until the job lands. Say so in the pull request, so a
reviewer seeing an empty report does not read it as a broken screen.
#50 — the responsive design system. **Open**; these screens use only primitives the existing admin routes already
import, so no unmerged design work is depended on.
#99 — **closed**, by pull request #111. Merged work, not a dependency.

Contended files needed here: `clients/pwa/src/app/router.tsx`; `clients/pwa/src/api/schema.d.ts`;
`clients/pwa/src/i18n/en-IN.ts` and `ta-IN.ts`; `clients/pwa/src/admin/adminApi.ts`, `types.ts`,
`adminPermissions.ts` and `adminDestinations.ts`; `clients/pwa/src/routes/admin/adminScreens.stories.tsx` and
`AdminScreens.test.tsx`. All of these are also needed by `[E14-F02-7]`, `[E14-F02-8]`, `[E14-F02-5]` and by #182, #204,
#216 and the batch-1 client children of #38, #41, #42 and #43, so this child is not parallel-safe with any of them. It
**must not** edit `clients/pwa/src/auth/apiClient.ts`.

Refs #57
