Sub-issue of #61. The second part of the plan's **#61c**: the parent's sixth implementation step — "training
material, quick guides, support contacts, runbooks and controlled production-access onboarding". The gap is
checkable: `ls docs/training` fails, and five documents already point at training material that does not exist —
`docs/prd/workflows/{blouse,salwar,lehenga,gown,kids}.md` each say at line 11 that their "What changes for staff"
table "feeds the training material of issue #61c", `docs/prd/raci.md` line 229 says the grid feeds it too, and
`docs/nfr/security-operations-targets.md` line 380 commits that "the out-of-hours arrangement is **stated to the
staff in plain language** during training (#61c)".

## What is already built (do not re-build)

- `docs/prd/workflows/{blouse,salwar,lehenga,gown,kids}.md` section 3, "What changes for staff" — one row per role
  with *today*, *after Tailor360* and a **training note**. `blouse.md` line 436 names itself "the source for the
  blouse modules of the training material". **Every card below is generated from these rows; do not write new
  content where a row already says it.**
- `docs/prd/raci.md` — who is accountable, responsible, consulted and informed for each activity, and section 4,
  where accountability changes hands. A card says what its role owns, taken from here.
- `docs/prd/walkthroughs.md` — the six journeys, their synthetic staff (section 1.3) and their artefacts. A card's
  worked example is one of these, so a trainee, a script and a test all describe the same order.
- `docs/uat/**` — child [E15-F03-8]: nine role scripts with their steps, refusals and questions. A quick card is the
  **one page a person keeps**; the script is the longer thing they were walked through. The card links to its script.
- `docs/nfr/support-matrix.md` — section 3 (device classes per role), 7.1 (the scanning ladder: camera, hardware
  scanner, manual entry), 7.2 (thermal label printing), 7.3 (A4 and A5 document printing), 7.4 (offline behaviour),
  section 8 (assistive-technology pairings). The first-run card's steps come from these ladders.
- `clients/pwa/src/app/router.tsx` — the real routes a card may name: `/install` and `/about` (outside the session,
  line 160, deliberately reachable before sign-in), `/settings/display`, `/account/security`,
  `/account/security/authenticator`, `/account/sessions`. `clients/pwa/src/routes/InstallRoute.tsx` and
  `useInstallPrompt.ts` are the install page the card points at — **it exists; the card describes it rather than
  inventing instructions**.
- `docs/nfr/accessibility-localisation.md` — section 10.1 the language rules at launch, 10.3 the **Tamil enablement
  gate** with its eight criteria (`AL-05`, proposed), 10.4 what is translated where, section 11 the Tamil glossary
  and what must be signed off, 12.1 to 12.5 the `en-IN`/`ta-IN` formats.
- `docs/prd/assumptions-and-open-decisions.md` section 5.1 — "**Tamil user interface as a launch language**" is
  *deferred*: "English at launch; Tamil when the message catalogue is at least 95 per cent translated and the
  glossary review is complete".
- `docs/nfr/security-operations-targets.md` — the incident path, the human-report requirement (line 276, under
  `OD-15`) and the hypercare review commitment (line 366).
- `docs/platform/secrets.md`, `docs/process/branch-protection.md` section 5.1 and #25's administration surface
  (invitation with an expiring single-use token in `identity.recovery_tokens`, suspension, revocation) — the
  mechanisms a production-access procedure uses. **The mechanisms exist; this sub-issue writes the procedure that
  governs who gets one.**
- `docs/runbooks/` — #60's operational runbooks. The support document **links** to them; it does not write one.

## What is missing

- No training material of any kind, so the five workflow documents and the RACI grid point at nothing.
- No quick card, so there is nothing a member of staff can keep beside a sewing machine or a till.
- No support contact sheet, so a Reception who cannot print a label has no documented first move — which
  `docs/nfr/security-operations-targets.md` line 276 explicitly requires to exist.
- No production-access procedure, so "controlled production-access onboarding" is a promise with no steps, no
  approver and no revocation.
- Seven forward references in `docs/prd/` point at "#61c" (and one at "#61b") for material that does not exist, and
  after this decomposition those issue numbers are wrong as well as unresolved.
- Nothing states that the staging environment is retained after go-live as the training environment, although the
  plan's #61c blueprint says it is.

## Scope

- [ ] Add `docs/training/README.md`: the index and the training plan — who is trained, in what order, on which
      environment, by whom, how long each session is planned to take, and what a trainee leaves with. It states the
      language rule plainly: **English at launch** (`docs/prd/assumptions-and-open-decisions.md` section 5.1), with
      the Tamil position as section 10.3's gate describes it.
- [ ] Add `docs/training/quick-cards/<role>.md` — one card per role the parent names: `owner`, `reception`,
      `measurement-staff`, `tailor-master`, `tailor`, `inventory-clerk`, `cashier`, `delivery-staff`, `auditor`.
      **Each card is at most 50 lines of markdown** — that is what fits one printed A4 page at a readable size, and
      it is a hard limit, not a target: a card that needs more is a card that is doing the script's job. Nine cards
      is therefore about 400 lines, and the whole sub-issue about 1,000. Each card is what this role does in the system in five lines; the three or four routes it lives
      on, named as the person sees them; the two or three things that are different from today, from that role's
      "What changes for staff" rows; the refusals it will meet and why they are deliberate; the one exception it is
      most likely to hit and the first move; and where to get help.
- [ ] Add `docs/training/first-run-card.md`: the per-role first-run checklist as a **printed card** — install the
      application from `/install`, sign in, enrol the second factor at `/account/security/authenticator`, grant the
      camera permission, scan one label from the training label sheet, pair the print station and print one test
      label, and check the display settings at `/settings/display`. Each step names the capability ladder it depends
      on from `docs/nfr/support-matrix.md` and what to do when it is unavailable.
- [ ] Add `docs/training/label-sheet.md`: how the training label sheet is produced from synthetic fixture
      identifiers, and the rule that a training label is visibly marked and never resolves to a real garment.
- [ ] Add `docs/training/support-contacts.md`: the first move for each of the six most likely "it is not working"
      situations, the escalation ladder, the out-of-hours arrangement in plain language as
      `docs/nfr/security-operations-targets.md` line 380 requires, and a link to each relevant `docs/runbooks/`
      entry. Every name and channel is a **placeholder naming OD-15**, because the register records the alert
      channel and the escalation contact as open.
- [ ] Add `docs/training/production-access.md`: who may hold a production account at go-live and why, the approval
      step and its approver, the invitation route (#25's expiring single-use invitation, not a shared password), the
      mandatory second factor, the least-privilege rule (no role granted "to get started"), the joiner–mover–leaver
      steps, the revocation expectation with its service-level objective cited from `docs/nfr/slo.md`, and the rule
      that a developer never holds a standing production credential — secrets arrive as files under `/run/secrets`
      (`CLAUDE.md` section 4 rule 6, `docs/platform/secrets.md`).
- [ ] Add `docs/training/training-environment.md`: the arrangement the plan's #61c blueprint states — the
      infrastructure-as-code staging environment of #59 is **retained after go-live as the training environment** —
      with the rules that make it safe: synthetic data only, never a copy of production, reset on a stated cadence,
      and no production secret mounted in it. The environment itself is #59's configuration, not code here (DOR-11).
- [ ] Correct **every** forward reference in `docs/prd/` that points at `#61c` or `#61b` for training material or
      user-acceptance scripts. This sub-issue is the single owner of those edits, so that no two children contend for
      one line. There are seven, all verified at HEAD:
      - the line-11 sentence in each of `docs/prd/workflows/blouse.md`, `salwar.md`, `lehenga.md`, `gown.md` and
        `kids.md` — "Section 3 is the closing 'what changes for staff' table that feeds the training material of
        issue #61c" — to name `docs/training/` and this sub-issue (five edits);
      - `docs/prd/workflows/blouse.md` lines 436 to 437 — "This table is the source for the blouse modules of the
        training material in issue #61c, and for the role-based UAT scripts in #61b" — **one sentence naming both**,
        so it is corrected once, here, to name `docs/training/` and `docs/uat/`. `docs/uat/` exists by now because
        [E15-F03-8] and [E15-F03-8b] have merged, so both halves of the link resolve;
      - `docs/prd/raci.md` line 229 — "updates section 2, the affected workflow map under `workflows/`, and the
        training material of issue #61c" — to name `docs/training/`.
      Nothing else under `docs/prd/` is touched: the catalogue, the walkthroughs' own #61b reference at lines 7 to 8
      and 772 to 773 (which belongs to the regression suite, not to training) and every table's content stay as they
      are.

## Out of scope

- **The in-app first-run checklist and `identity.user_onboarding`.** The plan's #61c blueprint also describes
  tracking the checklist in the application shell against a new `identity.user_onboarding` table. That is a product
  feature with its own migration, endpoints and screen — it belongs to a Lane B/Lane A feature issue raised against
  E12, not to the launch-gate issue, and `docs/architecture/module-ownership.md` section 5.1 does not list the table
  today. **Default in force:** the printed `first-run-card.md` above, ticked on paper and filed with the pilot
  record of [E15-F03-10]. This sub-issue names the gap in its index so the follow-up issue has a home.
- Writing or changing an operational runbook — #60 owns `docs/runbooks/`.
- Running the training, recording attendance, or translating anything into Tamil. Tamil cards wait on the glossary
  sign-off of `docs/nfr/accessibility-localisation.md` section 11.2; see the deferral below.
- The user-acceptance scripts ([E15-F03-8]), the pilot ([E15-F03-10]) and the go/no-go, hypercare and rota
  ([E15-F03-11]). The support document links to the rota; it does not define it.
- Provisioning or changing any environment. #59 owns both staging and production.

## Acceptance criteria

- [ ] Nine quick cards exist and each fits one printed page at A4 — checked by printing one to PDF and attaching it,
      per `docs/nfr/support-matrix.md` section 7.3.
- [ ] Every card's content is traceable: each "different from today" line cites the workflow document row it came
      from, and each route it names exists in `clients/pwa/src/app/router.tsx`.
- [ ] `docs/training/first-run-card.md` covers install, sign-in, second factor, camera permission, one test scan and
      one test print, and for each step names the fallback from the relevant capability ladder.
- [ ] Negative case: `docs/training/production-access.md` states explicitly what is **refused** — a shared account, a
      standing developer credential, a role granted without an approval, a production account without a second
      factor — and what a person does instead in each case.
- [ ] Negative case: `docs/training/support-contacts.md` states what to do when the named channel is unreachable,
      since the channel itself is undecided under OD-15.
- [ ] Exception case: every quick card names one exception from `docs/prd/exceptions.md` and the **first move**, not
      the full procedure, with a link to the script and the runbook that carry the rest.
- [ ] Exception case: `docs/training/training-environment.md` states what happens when a trainee's action on the
      training environment looks like a real order — the reset cadence and the visible environment banner that makes
      the confusion impossible.
- [ ] The Tamil position is recorded as a deferral with all six fields required by `docs/process/definition-of-ready.md`
      section 5: criterion (`DOR-14` localisation of training material), reason (the glossary is not signed off and
      `AL-05` is proposed), default in force (English cards, with the Tamil glossary terms carried in a parallel
      column where a term already exists), owner (the Owner, with a native-speaker reviewer), date recorded, and the
      condition for closure (the section 11.2 sign-off, after which the Tamil cards are a follow-up pull request).
- [ ] `./scripts/dev docs` passes; every link resolves, including into `docs/runbooks/`, `docs/uat/` and
      `docs/nfr/slo.md`.

## Data and classification

Per `docs/nfr/data-classification.md` section 2.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| The cards and the training plan | Internal | Teach a role its part of the system | As the repository; reprinted when a card changes | All staff, and anyone with repository access | Synthetic examples only, following `docs/prd/walkthroughs.md` section 1.2. A card printed for the shop floor is a physical document and the index says where the master copy lives |
| The training label sheet's barcode payloads | Public (synthetic) | Let a person scan something real on their first day | Reprinted per training round | All staff | Payloads carry no personal data by construction (`NFR-CU-01`); training labels are visibly marked and resolve only in the training environment |
| `production-access.md`'s access list | **Personal** — named people with what they may do | Control who holds a production account | As the release record; reviewed at each joiner, mover or leaver | The Owner and the technical reviewer | The document holds the *procedure* and the roles. **The list of named holders is not committed to the repository**: it lives with the release record, because it is a standing description of who can reach live customer data |
| `support-contacts.md` channels and names | **Personal** (contact details) | Let a member of staff reach help | As the release record | All staff | Placeholders only in the repository until OD-15 is decided; real telephone numbers and handles are filled in the printed copy and the release record, never committed |
| The out-of-hours arrangement | Internal | Set expectations honestly | As the release record | All staff | Stated in plain language, per `docs/nfr/security-operations-targets.md` line 380 |

## Permissions and roles

No permission key is added. The cards *describe* grants, and the production-access procedure *governs* them.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| The grants each card describes | branch, except `owner` and `auditor`, which are organisation | per the matrix | per the matrix | per the matrix | The card's own role key | **Exists** — quoted from `docs/security/permission-matrix.md`, including each role's reach and permission count |
| The administration grants the production-access procedure requires (invite, assign, suspend, revoke) | organisation | yes | yes | yes | `admin`, and `owner` (Admin, Owner) | **Exists** — #25 built the surface; the procedure adds the approval step around it, not a permission |
| — | — | — | — | — | — | Nothing new proposed. **OD-13** open, so every grant a card describes is proposed; **OD-16** (dual confirmation for Owner-level changes) is open, and the procedure records the question rather than assuming an answer |

## Modules, contracts and events

No module, no schema, no contract, no event, no source file under `src/` or `clients/pwa/src/`. Documentation only,
so no `ARCH-…` rule and no `MO-…` boundary is engaged. The in-app checklist that *would* have touched the `identity`
schema is explicitly out of scope above, which is why this sub-issue ships no migration.

DOR-05: `production-access.md` is the human half of the credential-abuse and privilege-escalation abuse-case
families whose models and covering issues are recorded in `docs/security/README.md` section 3.2 — the authentication
threat model of #23 and the privacy and secrets work of #57. It introduces no flow and no new trust boundary; it
constrains who may cross one that already exists.

## Screens and states

No screen is added. The cards name existing routes — `/install`, `/about`, `/settings/display`,
`/account/security`, `/account/security/authenticator`, `/account/sessions` and each role's working routes — and
must describe them as the person sees them, in the message wording the client already uses
(`clients/pwa/CLAUDE.md` section 5: text comes from the catalogue, so a card quoting a button must quote the real
label).

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| None added. The first-run card describes the **offline** and **permission-denied** states of the install and scan steps, because those are the two a first day actually meets | `/install`, each role's scan route | — | — | — | The card states the offline behaviour from `docs/nfr/support-matrix.md` section 7.4 | The card states that a missing camera permission falls back to manual entry by job number |

## Migration and rollback

No schema change and no migration: fifteen documents and seven corrected references (five line-11 sentences,
`docs/prd/workflows/blouse.md` lines 436 to 437 and `docs/prd/raci.md` line 229). `migrate --dry-run` reports no
outstanding migration. Rollback is `git revert`. The deliberate absence of a migration here is the consequence of
the in-app checklist being out of scope.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-15** — operations ownership and the alert channel | **Open** | Every name and channel in `support-contacts.md` is a placeholder naming OD-15, with the document stating that a human report is a valid detector (`docs/nfr/security-operations-targets.md` line 276) | The go/no-go of [E15-F03-11] |
| **AL-05** / the Tamil enablement gate | Proposed, to be confirmed | **English cards at launch**, recorded as a six-field deferral; Tamil cards follow the glossary sign-off of `docs/nfr/accessibility-localisation.md` section 11.2 | A follow-up pull request after the glossary review |
| **OD-07** — device, browser, printer and scanner matrix | Open | The published ladders of `docs/nfr/support-matrix.md` sections 7.1 to 7.4 | The pilot of [E15-F03-10] |
| **OD-06** — branches at launch | Open | The training plan is written per branch with the two seeded branches as the example | The pilot of [E15-F03-10] |
| **OD-13** — role-to-permission grants | Open | The matrix as published; a card states what its role can do today and says it is proposed | The go/no-go of [E15-F03-11] |
| **OD-16** — dual confirmation for Owner-level administrative changes | Open | Single confirmation as built; `production-access.md` records the question | The go/no-go of [E15-F03-11] |
| **OD-02** — hosting model | Open | The training environment is whichever environment #59 provisioned as staging | The go/no-go of [E15-F03-11] |
| **OD-09** — label format | Open | The training label sheet uses whatever the label template produces, and says so | The pilot of [E15-F03-10] |
| The revocation objective cited in `production-access.md` | As published in `docs/nfr/slo.md` | Cited, never restated | — |

No duration, session length, contact, threshold or retention period is invented. Where a number is needed and
undecided, the placeholder names the decision.

## Required verification

- [ ] Documentation: `./scripts/dev docs` clean, and the `documentation` job of `.github/workflows/ci.yml` green.
- [ ] Print check: one quick card and the first-run card printed to PDF at A4, attached, to prove the one-page claim
      and that nothing is cut off (`docs/nfr/support-matrix.md` section 7.3).
- [ ] Route check: every route named in every card matched against `clients/pwa/src/app/router.tsx`, pasted as a
      list in the pull request.
- [ ] Traceability check: every "different from today" line matched to its workflow-document row, pasted as a table.
- [ ] Walkthrough: one card read aloud against a running installation from `./scripts/dev reset` by someone other
      than the author, who confirms the steps match the screens. This is also the `DOR-16` answer: a seeded local
      installation and a printer-capable device for the print check; no production access is needed or granted.
- [ ] `DOR-13`: the rows this sub-issue contributes evidence to are `docs/nfr/traceability.md` section 14
      (localisation) and section 22 (targets no automated proof covers). No status cell is edited; the training
      index links the cards.

## Evidence the pull request will carry

- [ ] The rendered diff of the fifteen documents and the seven corrected references.
- [ ] The two printed PDFs (one quick card, the first-run card).
- [ ] `./scripts/dev docs` output, clean.
- [ ] The route list and the traceability table.
- [ ] The reader's walkthrough note, naming who read it and against which build.
- [ ] The six-field Tamil deferral, quoted in the pull-request description so the reviewer accepts it explicitly.
- [ ] The pull-request template with items 1, 3, 6, 8 and 9 ticked; items 2, 4, 5 and 7 deleted with the one-line
      reason "documentation only: no endpoint, no migration, no contract, no screen".

## Depends on

#[E15-F03-8] and #[E15-F03-8b] — the nine user-acceptance scripts each card links to. Writing the card before the
script inverts the dependency: the card is the summary of the journey the script establishes. Both must have merged,
or the card's link to `docs/uat/<role>.md` does not resolve and `./scripts/dev docs` fails. Neither of them edits
`docs/prd/`, so the forward-reference corrections in this sub-issue's scope have no competing author.

#17 — `docs/prd/workflows/**`, its "What changes for staff" tables, `docs/prd/raci.md` and
`docs/prd/walkthroughs.md`. The files named above exist at HEAD; the issue is still **open** in GitHub, so verify the paths, not the issue state — of #17 to #60 only #20, #23, #28, #43 and #53 are closed today.

#23, #25 — the install page, the second-factor enrolment, the invitation mechanism and the administration surface
the production-access procedure governs. The files named above exist at HEAD; the issue is still **open** in GitHub, so verify the paths, not the issue state — of #17 to #60 only #20, #23, #28, #43 and #53 are closed today.

#57 — the privacy, audit and secrets work the production-access rules rest on.

#59 — the environment that is retained as the training environment, and #60 — the `docs/runbooks/` entries
`support-contacts.md` links to. Both must have merged for the links to resolve, which `./scripts/dev docs` enforces.

Parallel-safe with [E15-F03-10] and [E15-F03-11] (`docs/launch/`), with [E15-F03-7] (`docs/qa/`, `infra/`) and —
after the correction above — with [E15-F03-8] and [E15-F03-8b] as well: this sub-issue is now the **only** child that
edits anything under `docs/prd/`, so there is no shared line with any sibling. It still *depends* on [E15-F03-8] and
[E15-F03-8b], because a card links to its script and `./scripts/dev docs` fails on a link that does not resolve.

Refs #61
