Sub-issue of #61. Part of the plan's **#61b** and the second of the three exception-path children: the **money and
dispatch** exceptions, and the dispatch policy variants the parent's third implementation step names explicitly
("partial/full payment, blocked/allowed dispatch"). The delivery, feedback and notification exceptions and the
published regression report are [E15-F03-6b]'s. The gap is the one `docs/prd/exceptions.md` lines 211 to 212 record
— the business-scenario suite "that issue #61b maintains" has never existed — plus `docs/prd/walkthroughs.md`
section 8's statement that no walkthrough ends in a cancellation, a refund or a credit note.

This sub-issue was split out of a single "money, delivery and notification" child because seven end-to-end specs,
a new document and four traceability cells is more than one session can carry: four specs here, three and the
report in [E15-F03-6b].

## Before a session starts: the same two blocking pre-flight checks as [E15-F03-3]

Recorded in the issue as a DOR-03 re-check, never decided mid-flight. (1) `tests/e2e` holds the #52 page objects and
the scenario area, journey fixture, dataset helper and tag-driven reporter of [E15-F03-3]; confirm it. (2) Every
route these four scenarios drive exists in `clients/pwa/src/app/router.tsx`. At HEAD the billing routes exist
(outstanding, payments, allocate, cashier, reconciliation, dispatch exceptions — lines 226 to 277) but **there is no
order, job-card, custody, scan or delivery route at all**; they arrive with #204 and the client children of #33 to
#48. A step whose screen is not built is not stubbed and not driven through the API as a substitute: the scenario is
not automatable yet and the sub-issue is re-scoped before a branch is cut.

## What is already built (do not re-build)

- `tests/e2e/` — #52's configuration and page objects; `tests/e2e/scenarios/`, `support/journey.ts`,
  `support/dataset.ts`, `reporters/pass-matrix.ts` from child [E15-F03-3]; the six walkthrough scenarios from
  [E15-F03-3] and [E15-F03-4]; the five intake, production and custody exception scenarios from [E15-F03-5].
- `docs/prd/exceptions.md` — the catalogue. `EX-08` cancelled order (line 45), `EX-09` refund (46) and `EX-15`
  dispatch exception expired or invalid (52) are the three rows this sub-issue answers, each with its trigger, the
  roles involved, the required handling, what it leaves behind and its `XQ-nn` open decision. `EX-10` unpaid
  dispatch attempt (47) is already asserted by walkthrough 3 in [E15-F03-4]; this sub-issue proves the **policy
  matrix** around it rather than re-asserting it.
- `docs/prd/exceptions.md` section **7.1 Automated coverage** — the append-only `EX-nn` | spec | tag table created
  by [E15-F03-5]. **Append rows to it; do not re-shape it and do not edit the catalogue rows at lines 38 to 52.**
- `docs/process/release-gates.md` `RG-05` — the journey list, the five priority-zero journeys (order confirmation,
  custody transfer, the dispatch gate, invoice posting, payment recording) and the evidence it demands. The
  dispatch gate is one of them, so a failure here is **S1**.
- `docs/qa/test-strategy.md` — child [E15-F03-1]: the regression scope rule and the stored-artefact convention.
- `docs/prd/state-transitions.md` — the cancellation, refund and hold transitions.
- The dispatch gate as built: the fail-closed eligibility query of #37, the balance rule of #43 (closed: `#164`
  shipped `IDispatchEligibilityQuery`, the dispatch policy and the single-use dispatch exception; `#163` shipped
  refunds, reversals and reconciliation batches; `#154` shipped posting, cancellation, credit and debit notes), and
  the partial-delivery policy and two-stage dispatch of #48 (plan Section 6.2 note 2). **The policies exist; this
  sub-issue proves each one, it does not add one.**

## What is missing

- Three catalogued exceptions have no automated scenario, and they are the ones that move money: a cancellation
  after work has been done, a refund, and an approved dispatch exception presented after it expired.
- "Blocked/allowed dispatch under each policy" is asserted for exactly one policy — whatever the fixture happens to
  configure — so the parent's third implementation step is unmet.

## Scope

- [ ] Add `tests/e2e/scenarios/ex08-cancelled-order.spec.ts` (`@ex-08`): an order cancelled after confirmation and
      after some work has been done. Assert what the catalogue row requires — that cancellation is **blocked** in
      the prohibited financial, stock and custody states with the blocking reason named, that the job cards move to
      a cancelled state without deletion, that custody of customer material is resolved, and that the financial
      consequence is a compensating record rather than an edit.
- [ ] Add `tests/e2e/scenarios/ex09-refund.spec.ts` (`@ex-09`): money returned after a cancellation and after a
      duplicate payment. Assert the append-only behaviour — a credit note or refund record, never a modified
      receipt — that the payment mode's `allowed_for_refund` flag gates the tender, and that the customer-facing
      document reflects it.
- [ ] Add `tests/e2e/scenarios/ex15-expired-dispatch-exception.spec.ts` (`@ex-15`): an approved exception presented
      after its expiry, and one presented after the balance grew or the job set changed. Assert the refusal in both
      cases and that a fresh approval is required — the fail-closed behaviour of plan Section 6.2 note 2 and of the
      `EX-15` row, which fixes the expiry at "at most 72 hours" and single use.
- [ ] Add `tests/e2e/scenarios/dispatch-policies.spec.ts` (`@dispatch`): one parameterised case per dispatch policy
      the configuration supports under **OD-04**, each asserting the blocked attempt **and** the allowed attempt.
      **The policy set is read from the running configuration, never listed in the spec**, so a policy the owner
      later removes removes a case rather than breaking the spec.
- [ ] Append the four rows to the journey table of `tests/e2e/scenarios/README.md` and the three `EX-nn` rows to
      `docs/prd/exceptions.md` section 7.1, both append-only, in the convention [E15-F03-5] established.

## Out of scope

- Adding or changing a dispatch policy, a refund rule or a cancellation window. Those belong to #43 and #48 (and to
  **OD-23**, which leaves `Billing:Invoices:CancellationWindow` deliberately unset); this sub-issue asserts what
  they built at the configured boundary.
- `EX-11` negative feedback, `EX-12` failed or returned delivery and `EX-14` notification not delivered, and
  `docs/qa/regression-report.md` with the traceability status cells — all [E15-F03-6b].
- `EX-01` to `EX-07`, `EX-10` and `EX-13` — the walkthroughs and [E15-F03-5].
- The dress rehearsal, the restore and the rollback — [E15-F03-7].
- Mapping the regression evidence to the #1 release criteria and the P0/P1 journey list — the release evidence
  index of [E15-F03-11].
- Physical devices, printers and scanners — the pilot of [E15-F03-10].

## Acceptance criteria

- [ ] `./scripts/dev test e2e` runs the fifteen scenarios that then exist (six walkthroughs, five from
      [E15-F03-5], four here) and passes, and the pass matrix lists every one against every browser and device
      profile with **no edit to the reporter**.
- [ ] Negative case: every dispatch policy has a **blocked** case as well as an allowed one, and the blocked case
      asserts the refusal the delivery team actually sees — `custody.dispatch-blocked` with its reason, as an
      RFC 9457 problem-details body, never a silent no-op (`CLAUDE.md` section 4 rule 3).
- [ ] Negative case: `@ex-15` proves the fail-closed rule twice — expired, and materially changed — and that
      neither can be worked around by retrying.
- [ ] Negative case: one forbidden actor per scenario, asserted as a problem-details refusal — a Cashier cannot
      approve their own refund, a Delivery Staff principal cannot approve a dispatch exception, and the approver of
      an exception is never its requester.
- [ ] Exception case: `@ex-08` and `@ex-09` assert that nothing is deleted or edited — the cancellation and the
      refund are new records that reference what they correct, and the original documents still render as issued.
- [ ] Exception case: `@ex-08` asserts the **blocked** cancellation as well as the permitted one, because the
      catalogue's own handling text makes the block the primary behaviour.
- [ ] Every scenario drives the application through the client and the public API only. No test writes to the
      database or calls a module internal.
- [ ] Run twice and in reverse order, with no inter-scenario state (`CLAUDE.md` section 5).
- [ ] Parallel-safe with [E15-F03-5] and [E15-F03-6b]: no shared file beyond the appended README rows and the
      appended section 7.1 rows.

## Data and classification

Per `docs/nfr/data-classification.md` section 2. This suite renders financial documents, so the classification that
matters is what a trace and a rendered artefact may carry.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| The invoices, credit notes, receipts and refunds these scenarios produce | **Financial** (synthetic) | Prove the append-only and compensating behaviour | As the repository | Anyone with repository access | Synthetic amounts from the fixture. The assertion is the *relation* — a new record referencing the corrected one — never an invented figure |
| The dispatch exceptions `@ex-15` and `@dispatch` create | Internal, **evidentiary** | Prove the fail-closed consumption rule | As the repository | Anyone with repository access | Order, job set, maximum outstanding amount, policy version and expiry. The scenario asserts the refusal, never the approver's reason text |
| Playwright traces, screenshots and videos | **Personal by construction** — they render a customer record | Diagnose a failure | The CI artefact retention the `ci.yml` upload step applies | Whoever can read the CI run | Safe only because every value is synthetic; never run against a real installation |
| The sign-in credentials the suite uses | Credentials and secrets | Sign in as each role | Not stored in the repository | The CI job and a developer running locally | A mounted file or a CI secret per `docs/platform/secrets.md` — never a committed value, never printed |

## Permissions and roles

No permission key is added.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| The cancellation grant `@ex-08` exercises | branch | per the matrix | per the matrix | yes | `reception`, `branch_manager` (Reception, Branch Manager) | **Exists** — as published in `docs/security/permission-matrix.md` |
| The refund grant `@ex-09` exercises | branch | per the matrix | per the matrix | yes | `cashier`, with the approval the matrix requires (Cashier, approved by Owner or Branch Manager) | **Exists** — `payments.refund` / `payments.reverse`; the separation is the assertion |
| The dispatch-exception approval `@ex-15` and `@dispatch` exercise | branch | per the matrix | per the matrix | yes | `owner` (Owner); `admin` and `branch_manager` only if **XQ-02** says so | **Exists** — and the scenario asserts that the approver is not the requester |
| — | — | — | — | — | — | Nothing new proposed. **OD-13** open, so every grant is proposed rather than approved |

The authoritative authorisation proof stays in `tests/Tailor360.IntegrationTests/Authorization/`; this suite does
not re-assert the matrix, it asserts the journey.

## Modules, contracts and events

No module project is touched; no schema is read or written; no contract or integration event is added. The suite
drives the composed application through `Tailor360.Web`, so no `ARCH-001` to `ARCH-023` rule and no `MO-1` to
`MO-4` boundary is engaged, and it reads no database so it cannot violate `ARCH-005`.

DOR-05: the flows exercised here — payment, refund and dispatch authorisation — have their coverage recorded in the
threat-model coverage table of `docs/security/README.md` section 3.2, and the abuse cases this suite regresses are
named there: invoice or payment tampering, and workflow bypass. It adds no flow of its own.
**Checked fact**: `docs/security/threat-models/` holds exactly one file at HEAD, `authentication.md`. The rest of the
set arrives with #56a, so until it merges DOR-05 is satisfied the way the criterion itself prescribes — the flow
appears in the backlog table of `docs/security/README.md` section 3.2 with its covering issue and wave. Cite that
table, not a file that does not exist.

## Screens and states

No screen is added. A missing accessible name is fixed on the component, not worked around with a test attribute
(`clients/pwa/CLAUDE.md` section 4), and each such change is listed in the pull request.

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| None added | — | — | — | — | — | — |

## Migration and rollback

No schema change and no migration: four spec files, README rows and three section 7.1 rows. `migrate --dry-run`
reports no outstanding migration. Rollback is `git revert`.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-04** — payment rule for dispatch, the partial-delivery policy, who may approve an exception, whether an advance unlocks dispatch, whether Delivery Staff may collect the balance | **Open** | `@dispatch` reads the supported policy set from the running configuration and asserts a blocked and an allowed case for each one configured. No threshold, share or approver is written into a spec | The go/no-go of [E15-F03-11] |
| **XQ-02** — who may approve a dispatch exception, and whether Delivery Staff may collect at the door | Proposed, to be confirmed (Owner only, no doorstep collection) | Asserted as the catalogue states it, with `XQ-02` cited in a comment | The go/no-go of [E15-F03-11] |
| **XQ-03** — whether an advance is refundable on cancellation, and with which approval | Already registered as confirmed at the workshop | `@ex-09` asserts the compensating approved record, not a policy | — |
| **OD-05** — valuation, rounding, the accountant's examples, the statutory retention period | Open | Amounts come from the fixture; assertions are relations, not figures | The accountant approval of [E15-F03-8b] |
| **OD-22** — whether one order may be invoiced in parts, and what becomes of an abandoned draft | Open (#153's default: a draft names the jobs it covers, one live invoice per job, a draft may be discarded with a reason) | Asserted as built; the spec never assumes a second invoice per order is offered | W4, per the register |
| **OD-23** — how long after posting an invoice may still be cancelled | Open (`Billing:Invoices:CancellationWindow` deliberately unset by #154, so no window is enforced) | `@ex-08` and `@ex-09` assert the compensating documents, never a window | W4, per the register |
| **RG-OD-01** — the proposed gate severities and waiver durations | Open | `RG-05` as published: S1 for a priority-zero journey, S2 otherwise | The go/no-go of [E15-F03-11] |

No threshold, amount, retry count or window is invented. Where one is needed the spec asserts at the configured
boundary and cites the decision.

## Required verification

- [ ] End-to-end: all fifteen scenarios green locally and in the `e2e-regression` CI job, with the scenario count in
      the output.
- [ ] End-to-end negative: one blocked case per dispatch policy; the two `@ex-15` refusals; the blocked
      cancellation; each forbidden actor. Each asserted by a named test with the problem-details body checked, not
      just the status.
- [ ] End-to-end exception: the append-only assertions of `@ex-08` and `@ex-09`.
- [ ] Run twice and in reverse order.
- [ ] `DOR-16`: `./scripts/dev doctor` with Playwright browsers FOUND plus a running stack from `./scripts/dev up`
      and `./scripts/dev run`. No physical device and no real provider account.
- [ ] `DOR-13`: this sub-issue contributes proof to `NFR-CU-02` and `NFR-CU-06` of `docs/nfr/traceability.md`
      section 12 and to the `RG-05`-gated rows of sections 11 and 15. **No status cell is edited here** —
      [E15-F03-6b] edits them once the matrix is complete, so the two children never touch the same cell.

## Evidence the pull request will carry

- [ ] The Playwright report for all fifteen scenarios and the pass matrix markdown table.
- [ ] The trace of one blocked dispatch per policy, and of the expired-exception refusal.
- [ ] The rendered artefacts the cancellation and refund scenarios produce — the credit note and the refund receipt
      (`docs/process/definition-of-done.md` section 4, "label, receipt or invoice rendering": the rendered artefact
      is attached; no print or scan is claimed, because this is emulation).
- [ ] The problem-details body for each refusal asserted, with its correlation identifier.
- [ ] The two-run output including the reversed order.
- [ ] The `docs/prd/exceptions.md` rendered diff showing the three new section 7.1 rows.
- [ ] `migrate --dry-run` output showing no outstanding migration.
- [ ] The pull-request template with items 1, 3, 6, 8 and 9 ticked; items 2, 4 and 5 deleted with the one-line
      reason "no endpoint, no migration, no contract change".

## Depends on

#[E15-F03-3] — the scenario area, the journey fixture, the dataset helper, the tag-driven reporter and the CI job.
#[E15-F03-4] — the walkthroughs these diverge from, walkthrough 3 in particular, which asserts `EX-10`.
#[E15-F03-5] — the `docs/prd/exceptions.md` section 7.1 table this appends to.

#43 — payments, receipts, refunds, the cashier session and the balance-based dispatch rule (issue closed; its
children #161 to #165 are closed). #42 — invoices, credit notes and the immutability rules `@ex-08` and `@ex-09`
assert (#153, #154, #155 closed; the parent is open). #48 — the partial-delivery policy, the two-stage dispatch and
the doorstep confirmation (**open**). #37 — custody and the fail-closed eligibility query (**open**). Each must have
merged, or the scenario asserting its artefact has nothing to assert.

#59 — the environment and release workflow the suite runs against in CI (plan Section 6.2 note 11).

Parallel-safe with [E15-F03-5] and [E15-F03-6b] except for two append-only files: the journey table in
`tests/e2e/scenarios/README.md` and section 7.1 of `docs/prd/exceptions.md`. Whichever merges last rebases those.

Refs #61
