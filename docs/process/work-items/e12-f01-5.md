Sub-issue of #50. **Evidence only — this issue writes no production code, and it cannot be completed by a coding
session.** It is carved out precisely so that no coding child of #50 is blocked on hardware and a human it cannot
have.

#50's fifth acceptance criterion is "visual regression and representative real-device review pass", and the plan's
"Evidence at W1" for #50 (`docs/IMPLEMENTATION_PLAN.md` section 8, `#50`) is the **eight reference journeys walked
per role** against `docs/nfr/a11y-checklist.md` with **VoiceOver on iOS and TalkBack on Android**, plus the
**device / orientation / zoom matrix**. None of that is a code change. It needs a real iPhone or iPad with VoiceOver,
a real Android phone or tablet with TalkBack, a real desktop, a person to drive them, and a second person to
counter-sign two of the findings. Every other child of #50 produces the software these walkthroughs are run against;
this one produces the record, and **#50 cannot close without it**.

`docs/process/waivers.md` is explicitly **not** the route here: a waiver is for a gate that cannot be met, not for a
human pass that has not happened yet. The outstanding work is stated, owned and scheduled, not waived.

## What is already built (do not re-build)

- `docs/nfr/a11y-checklist.md` — the whole instrument. Line 7 calls it a "dated, signed, attachable record"; line 28
  ties it to **DoD 7** and **DOR-14**; section 4 is the per-screen core checklist; section 6.8 (line 883) is the
  eight per-role reference journeys `A11Y-RJ-01` to `A11Y-RJ-08`; the copyable templates at the end are **Journey
  record (section 6)** (line 1428), **Result** (1445), **Defects raised** (1459) and **Signatures** (1465). The
  grading table (S1/S2/S3 and what is waivable) is reprinted at line 1144. **Nothing in this document is rewritten
  by this issue** — it is filled in.
- `docs/nfr/a11y-checklist.md` section 6.8's own cross-references, which say which journeys must **not** be walked
  twice: `A11Y-RJ-01` (Reception intake) is covered by `A11Y-PZ-01`, `A11Y-RJ-06` (Cashier payment) by
  `A11Y-PZ-05`, and `A11Y-RJ-07` (Delivery queue to dispatch) by `A11Y-PZ-03`. The five with records of their own
  are `A11Y-RJ-02` (measurement wizard), `A11Y-RJ-03` (Tailor scan to queue), `A11Y-RJ-04` (Tailor Master
  workboard), `A11Y-RJ-05` (Inventory stock entry) and `A11Y-RJ-08` (Owner dashboard).
- `clients/pwa/src/stories/journeys/` — the eight journeys as Storybook screens against synthetic fixtures, with
  `JourneyFrame` supplying the role and its own `MemoryRouter`. These are what is walked: they exist, they are the
  W1 evidence surface, and the walkthrough does not need any feature screen to have shipped.
- `docs/nfr/support-matrix.md` section 3 (the Tier 1 device classes: shop-floor phone, counter tablet, back-office
  desktop, print station), section 5 (orientation), section 8 (assistive-technology pairings) and **section 9**
  (how the matrix is enforced — note it is section 9, not section 10; the stale reference in
  `expectNoHorizontalOverflow.ts` is **e12-f01-4's** to fix).
- `docs/nfr/a11y-checklist.md` items that are specifically real-device items and cannot be produced any other way:
  **A11Y-17** (on a real phone with the real virtual keyboard open, is a focused control ever wholly hidden — the
  document itself says the headless helper "never sees a real on-screen keyboard resize a real viewport. This item
  is that gap, not a repeat of the helper"), **A11Y-15** (the product's own theme switch versus the operating
  system's dark mode and `prefers-contrast`) and **A11Y-72** (the product's 150% text size together with the device
  font size at its largest).
- `clients/pwa/src/components/layout/DisplayPreferencesPanel.tsx` and `/settings/display` — where A11Y-15 and
  A11Y-72 are set. After **e12-f01-2** the preference follows the account, which is what makes the 150% pass
  repeatable on a second device.

## What is missing

- Any completed record. `docs/nfr/a11y-records/` does not exist; neither does `docs/evidence/`.
- Any device evidence at all. `docs/nfr/support-matrix.md` section 9 requires "a recorded walkthrough per Tier 1
  device class, in a browser tab **and** in installed mode, with the device, operating system and browser versions
  logged", and none is recorded.
- A visual-regression baseline, which **this issue does not produce either** — see Out of scope.

## Scope

- [ ] Create `docs/nfr/a11y-records/` and commit one completed record per journey, named
      `<yyyy-mm-dd>-<journey>-<pairing>.md`. That path and that name are the **default in force** under open
      decision **A11Y-OD-06** (`docs/nfr/a11y-checklist.md` line 31: "proposed, to be confirmed"); name the
      decision id in the pull request and do not invent a different location. Add a one-paragraph `README.md` in
      that directory saying what the files are and which decision governs the path.
- [ ] Walk and record the **five** journeys that have records of their own: `A11Y-RJ-02`, `A11Y-RJ-03`,
      `A11Y-RJ-04`, `A11Y-RJ-05`, `A11Y-RJ-08`. Do **not** walk `A11Y-RJ-01`, `A11Y-RJ-06` or `A11Y-RJ-07`: section
      6.8 says each is covered by a priority-zero record and must not be walked twice. Say so in the pull request,
      with the line reference, so a reviewer does not read three missing files as three missing passes.
- [ ] Each record is the four templates filled in — Journey record, Result, Defects raised, Signatures — with the
      build identifier (`GET /api/version`, which the About screen already shows), the device, the operating-system
      version, the browser version and the assistive-technology version, and the keyboard-only and
      screen-reader-only outcomes as Yes or No rather than prose.
- [ ] Two pairings per journey at minimum: **VoiceOver with Safari on a real iPhone or iPad** and **TalkBack with
      Chrome on a real Android device**. Section 8 of the checklist is authoritative on the pairings; for TalkBack
      it requires a screen recording **with audio**, transcribed into the "Verbatim announcements captured" row, or
      the developer-settings speech log attached.
- [ ] The real-device items that exist only here, each recorded explicitly as Pass, Fail or N/A with a reason:
      **A11Y-17** with the real virtual keyboard open, **A11Y-15** with the product theme set against the operating
      system's dark mode and `prefers-contrast`, and **A11Y-72** at the product's 150% text size with the device
      font size at its largest.
- [ ] The **device, orientation and zoom matrix** for the Tier 1 classes of support-matrix section 3: phone, counter
      tablet and back-office desktop, in portrait and landscape, in a browser tab **and** in installed mode, with
      versions logged — which is what section 9's "Real-device evidence" row requires and what NFR-CB-05's "install
      records per device class" means.
- [ ] Grade every Fail under the checklist's section 7 table, **raise one issue per S1 and S2 finding** linked to
      #50 with the item id in the title, and list them in the Defects raised table. A finding is not closed by this
      issue; it is raised, graded and owned.
- [ ] Two signatures: the **Runner** and the **Reviewer**, with the Reviewer having independently re-run at least
      two Fail or N/A items, which is what the Signatures template states. The Owner row is signed at release, not
      here.
- [ ] Re-verdict exactly **two** rows in `docs/nfr/traceability.md`, and no others: **NFR-AC-03** (currently
      "Evidence only; scheduled (#52)") gains this record as its W1 evidence while keeping #52 for the release
      audit, and **NFR-CB-05** (currently "Proof scheduled (#50, #51)") records the installed-mode evidence this
      issue produces. Use the section 2.3 vocabulary verbatim and change no target, threshold or number.
      **e12-f01-4** owns the other seven #50 rows — coordinate, because both children edit this file.

## Out of scope

- **Visual-regression baselines, the Playwright suite, Lighthouse CI, the cross-browser reports and the enforcing
  axe gate are #52.** It owns `tests/e2e` and `playwright.config`, **neither of which exists in this repository
  today** (`./scripts/dev` says the Playwright suite arrives with #52), and `tests/load` does not exist either.
  #50's criterion names visual regression and #52 is where the runner lives; this issue does not stand one up, and
  says so rather than silently dropping it.
- **The full accessibility audit, NVDA, and the complete browser matrix** — also #52, per
  `docs/nfr/a11y-checklist.md` and the plan's #52 blueprint.
- **Fixing any finding.** Findings become issues. A record that has been edited to remove a Fail is not evidence.
- Any change to `docs/nfr/a11y-checklist.md`, `docs/nfr/support-matrix.md` or any component. If a walkthrough
  reveals that the checklist itself does not cover something, the Result template's last row says that is "a change
  to a11y-checklist.md, raised as an issue" — raise it, do not make it here.
- **The per-screen manual passes other issues already own.** `#216` (open) covers the screen-reader pass, the
  five-state and pseudo-locale stories and the reflow and theme checks for #165's **billing screens**, and
  `E09-F03-5c`/`E09-F03-5d` in the parallel batch cover their stories. This issue records the **eight per-role
  reference journeys** (`A11Y-RJ-…`), not per-screen core checklists; if a journey's walkthrough crosses a billing
  screen, note the screen and leave its per-screen record to #216 rather than producing a second one.
- The thermal printer and the print-station device class. Printed-label and printed-document evidence is #35's
  (`e07-f01-9` in the parallel batch) and the receipt and invoice work's; this issue covers the three Tier 1
  interactive classes only.

## Acceptance criteria

- [ ] Five committed records exist under `docs/nfr/a11y-records/`, each naming its build identifier, device,
      operating system, browser and assistive-technology versions, and each with both a keyboard-only and a
      screen-reader-only outcome recorded as Yes or No.
- [ ] Each record covers both required pairings, and the TalkBack passes carry a transcript or an attached speech
      log as section 8 requires.
- [ ] A11Y-17, A11Y-15 and A11Y-72 are recorded explicitly in every record, and any N/A names one of the reasons
      the Result template allows (desktop pairing / kit unavailable / fixture unavailable / genuinely not present).
- [ ] The device, orientation and installed-mode matrix is recorded for all three Tier 1 interactive classes, with
      versions.
- [ ] Every S1 and S2 finding has an issue number in the Defects raised table, and every S1 is named in the pull
      request body. **Negative: a record with no findings at all is challenged, not accepted** — a first real-device
      pass over eight journeys that finds nothing is more likely to be an unrun pass than a perfect product, and the
      reviewer's two independent re-runs are what settle it.
- [ ] Negative: a journey that could not be completed with the keyboard alone or with the screen reader alone is
      recorded as **No** with its S1 finding, and this issue still closes. Recording a failure is success for this
      issue; hiding one is not.
- [ ] Exception: a pairing's kit is genuinely unavailable — the item is **N/A with the reason "kit unavailable"**,
      the gap is named in the pull request, and **no waiver is written**: an unavailable device is an unfinished
      record, which stays open, not a gate that cannot be met.
- [ ] Runner and Reviewer signatures are present, and the Reviewer's statement names the two items they re-ran.
- [ ] `./scripts/dev docs` resolves every link in the new files and in the two edited traceability rows.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| The completed records, screenshots and screen recordings | **Internal** — they describe the product and the devices it was tested on (`docs/nfr/data-classification.md` section 2) | Evidence for NFR-AC-03, NFR-CB-05 and release gate RG-06 | Source control, and the release evidence index | Anyone with the repository | Not a runtime artefact |
| The synthetic fixtures visible in every screenshot | Internal, synthetic | Walk a journey without a real customer | With the repository | Anyone | `clients/pwa/src/stories/fixtures/` is synthetic by construction, and CLAUDE.md section 4 rule 10 forbids anything else |
| Staff names in the Signatures table | **Personal** — an identified member of staff | Accountability for the record | With the record | Anyone with the repository | A name in a signed record is its purpose; nothing else personal goes in |
| A screen recording of a real device | Internal — **and it must contain no real customer data**, no real phone number and no real measurement. Re-record rather than redact if one appears | Prove the announcements | With the record | Anyone with the repository | Check every recording before committing: a recording is the easiest way to commit personal data by accident |

No new table, no new endpoint, no new storage key, no secret.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| — | — | no | no | no | — | **None.** No code and no endpoint. The walkthroughs are run against Storybook with synthetic fixtures, so no production access and no real account is used |

**OD-13** is not engaged. No authorisation-matrix row, no `matrix.yaml` change.

## Modules, contracts and events

Nothing in `src/` and nothing in `clients/pwa/src/` is touched, so no architecture rule is engaged. The only files
added are Markdown and media under `docs/nfr/a11y-records/`, plus two row edits in `docs/nfr/traceability.md`.

## Screens and states

Deleted with its reason: no screen is added or changed. The screens walked are the eight existing reference journeys
in `clients/pwa/src/stories/journeys/`.

## Migration and rollback

No schema change and no migration. `migrate --dry-run` reporting no change is the evidence, and there is nothing to
roll back: reverting would delete a record, which is a decision about evidence rather than about code.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **A11Y-OD-06** — where a completed record lives (`docs/nfr/a11y-checklist.md` line 31) | Proposed, to be confirmed | `docs/nfr/a11y-records/<yyyy-mm-dd>-<journey>-<pairing>.md`, exactly as proposed. Named in the pull request rather than chosen silently (DOR-09) | The release evidence index of #61 |
| **AL-02** contrast target and **AL-03** touch-target minimums (`docs/nfr/accessibility-localisation.md` section 13) | Open | Recorded as observations against the existing tokens; no target is asserted as met, and no number is invented | Before #50 closes |
| **NFR-LO-02 / RR-16** — Tamil ships at 95% catalogue coverage | Open | The walkthroughs are run in `en-IN`; the Tamil pass is not claimed and is not attempted, because the catalogue awaits native-speaker review | RR-16's own closure |
| The S2 waiver route | Open per finding | An S2 finding is raised as an issue and may be waived only by the Owner on the technical reviewer's recommendation for 30 days, which is the checklist's own grading table — this issue raises, it does not waive | Per finding |

## Required verification

This issue is verified by a human, on hardware, and that is the point of carving it out.

- [ ] The five records, read by the Reviewer, with two items independently re-run.
- [ ] `./scripts/dev docs` for every link added.
- [ ] No build, test or lint step is required, and none is claimed. Run `./scripts/dev doctor` and paste it anyway,
      because DOR-16 asks every issue to state what environment it needs — and this one needs devices, not a
      database.
- [ ] Non-functional rows touched (DOR-13), named individually: **NFR-AC-03** and **NFR-CB-05**, and no others.

## Evidence the pull request will carry

- [ ] The five completed records, as committed files.
- [ ] One screenshot per Tier 1 device class per orientation, in a browser tab and in installed mode, with the
      device and version caption.
- [ ] The TalkBack recordings or speech logs, and the transcribed announcements.
- [ ] The issue numbers raised for every S1 and S2 finding.
- [ ] The rendered diff of the two `docs/nfr/traceability.md` rows.
- [ ] The signed Signatures table for each record.

## Depends on

The software being walked is already in `main` — the eight reference journeys, the three shells, the component
families and the display-preferences screen all merged with #50's earlier commits. So this issue is **runnable now**,
and it does not depend on any sibling.

It is, however, worth more after **e12-f01-2** (the account's 150% text size following the person to a second
device, which is what A11Y-72 is really about) and **e12-f01-3** (a shell whose navigation is the person's own
rather than Reception's). **If it is run before those merge, say in each record which build it was run against, and
expect the Reception-shaped navigation** — a record against a known build is evidence; a record against an
unspecified one is not. #50, #19, #24, #25 and #52 are all open, and none of them is described as merged here.

Shared file: `docs/nfr/traceability.md` — **e12-f01-4** edits seven other #50 rows in the same table, and
**E14-F01a-2** and **E14-F01b-5** cross-reference it from the ASVS sheet. Two rows only, and rebase rather than
re-order.

Refs #50
