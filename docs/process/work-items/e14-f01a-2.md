Sub-issue of #56. Plan Section 7 row `#56a` lists "ASVS traceability skeleton with owners" and Section 8 fixes the
sheet's columns: "requirement → control → issue/PR → test → evidence → residual risk → owner → review date". The
file does not exist — `ls docs/security` shows no `asvs-traceability.md` — and the one place it is already wired in
is empty on purpose: `docs/security/threat-models/authentication.md` section 10 carries the eight-column table with
one blank row and the note "Filled in from #56a onwards. Left deliberately empty rather than partly filled, so that
a half-populated table is not mistaken for a completed assessment." This sub-issue builds the sheet and fills that
one section, so that every later model and every later feature pull request has a requirement to close against.
It is the **skeleton**, with owners and verdicts; the per-requirement control, test and evidence cells are filled by
the ASVS audit in the W5 half of this parent.

## What is already built (do not re-build)

- `docs/security/threat-models/authentication.md` — 373 lines, reviewed 2026-09-05 (#23). Sections 8 and 9 already
  allocate `CTL-01` to `CTL-30` and `RR-01` to `RR-09` for the authentication, session, multi-factor and recovery
  flow. **These are the control identifiers the sheet's authentication and session chapters point at; do not invent
  new ones and do not renumber.** Section 10 is the empty table this sub-issue fills.
- `docs/nfr/traceability.md` section 8 — `NFR-SE-01` to `NFR-SE-14`, each with its verification method, its release
  gate, its accountable role ("Security owner") and a status such as "Proof scheduled (#24, #53)". This is the
  system's own requirement register and it is **already mapped to tests and gates**; the ASVS sheet cross-references
  it instead of restating the mapping. Row `NFR-SE-12` is the one this sub-issue advances.
- `docs/architecture/architecture-rules.md` — the `ARCH-001` to `ARCH-023` catalogue, each with one assertion and a
  failing test. Several are ASVS controls in everything but name (ARCH-007 deny by default, ARCH-008 audit filter,
  ARCH-014/015 clock and identifier abstraction, ARCH-016 `IOutboundHttp`, ARCH-017 rate-limit policy,
  ARCH-018 step-up, ARCH-019 one authentication scheme, ARCH-022, ARCH-023 resource scope).
- `docs/security/permission-matrix.md` (#24, written 2026-09-06, not approved) and `docs/security/field-visibility.md`
  — the access-control evidence for the V4 chapter; the matrix test in its section 8 fails the build when a
  published route has no row.
- `docs/process/release-gates.md` — `RG-01` to `RG-14` with severities and waiver authority; `RG-08` to `RG-11` are
  the security scans. The sheet's evidence column cites a gate, not a promise.
- `tests/Tailor360.IntegrationTests/Security/` and `tests/Tailor360.IntegrationTests/Authorization/` — the existing
  security and authorisation suites (`ContentSecurityPolicyTests.cs`, `CrossSiteDefenceTests.cs`,
  `ErrorLeakTests.cs`, `ForwardedHeaderTests.cs`, `ResourceScopePipelineTests.cs`, `AuthorisationMatrixTests.cs`,
  `RoleMatrixTests.cs`, `DenialAuditTests.cs`, `IdentifierEditingTests.cs`), plus
  `tests/Tailor360.ContractTests` for the endpoint inventory and the OpenAPI lint. These are the named tests for the
  requirements that are already covered.
- `docs/api/conventions.md` sections 3 and 4 (#53) — request and response standards, the RFC 9457 problem-details
  envelope and the field-error shape: the evidence for the validation and error-handling chapters.
- `src/Hosts/Tailor360.Web/Middleware/SecurityHeadersMiddleware.cs` — the enforcing nonce-based policy and the
  response header set, with `tests/Tailor360.IntegrationTests/Security/ContentSecurityPolicyTests.cs` proving it.

## What is missing

- No document says which ASVS version and level this system is judged against, so "ASVS Level 2" cannot be
  checked by anybody. That is a decision, and this sub-issue records it rather than inventing it silently.
- No sheet exists, so `NFR-SE-12` ("ASVS baseline coverage, with a traceability record and a penetration test whose
  findings are remediated") has no artefact, and `docs/process/release-gates.md` RG-14 evidence item 9 has nothing
  to summarise.
- `authentication.md` section 10 is empty, so even the one reviewed model cannot show its ASVS coverage.

## Scope

- [ ] Add `docs/security/asvs-traceability.md`. Section 1 records: the ASVS version and level in force, how the
      version was chosen, the licence attribution for the standard (ASVS is published under CC BY-SA — cite the
      requirement number with a one-clause paraphrase; **do not paste the standard's text**), the document owner,
      and the review cadence.
- [ ] One section per ASVS chapter. Within a chapter, **one row per Level 2 requirement** that governs a control
      this system builds, with columns: requirement (number plus a one-clause paraphrase) · level · the flow model
      or models that answer it · control owner · status · covering issue · review date.
- [ ] Status is one of exactly four values, defined in section 1: `covered` (a control exists and a named test
      proves it), `scheduled` (with the issue that will supply it), `not applicable` (with the reason), `accepted`
      (with the exception-register entry that carries it). A blank cell is not a status.
- [ ] A chapter that is wholly not applicable or wholly delegated gets **one** chapter-level row with the reason —
      for example the requirements that presume federation or a customer account surface, which assumption **A2**
      of `docs/prd/assumptions-and-open-decisions.md` excludes and **OD-12** keeps open.
- [ ] Section 2: the control owner per chapter. Until **RG-OD-02** is decided the Owner holds every chapter, with
      the technical reviewer, exactly as `docs/nfr/traceability.md` line 67 states.
- [ ] Section 3: the cross-reference table `ASVS chapter → NFR-SE-nn rows → RG-nn gates`, so the sheet and
      `docs/nfr/traceability.md` cannot drift apart. One row per chapter.
- [ ] Section 4: what the W5 audit must add — the per-requirement control, test, evidence and residual columns —
      stated as a contract so the audit is not free to redesign the sheet.
- [ ] Fill `docs/security/threat-models/authentication.md` section 10 from the sheet: every requirement the
      authentication flow answers, pointing at that file's own `CTL-nn` and `RR-nn` identifiers. Replace the
      "filled in from #56a onwards" note with what was done and when.
- [ ] Update `docs/security/README.md` section 2: `asvs-traceability.md` becomes written, with this sub-issue's
      number.
- [ ] Update `docs/nfr/traceability.md` row `NFR-SE-12` so its status names the sheet as delivered and the
      penetration test as still scheduled.
- [ ] Versioned configuration (DOR-11): none. The sheet is a document; no seed data and no configuration key.

## Out of scope

The nine flow threat models and their own section 10 tables — each model sub-issue fills its own, from this sheet.
The audit that fills the control, test, evidence and residual columns per requirement, and the penetration test —
both are the W5 half of this parent. The vulnerability-management process and the exception register the `accepted`
status points at — its own sub-issue; until it merges, `accepted` is defined here and used by nothing.

## Acceptance criteria

- [ ] A reader can answer, for any ASVS Level 2 requirement, one of exactly four things: it is covered and by
      which control owner and model; it is scheduled and by which issue; it does not apply and why; or it is
      accepted and under which register entry. No requirement in a chapter marked applicable is missing from the
      sheet.
- [ ] The ASVS version and level in force are stated in section 1 with how they were chosen and who confirms them,
      and the standard's licence attribution is present.
- [ ] Section 3 maps every chapter to at least one `NFR-SE-nn` row and at least one `RG-nn` gate, and every
      `NFR-SE-01` to `NFR-SE-14` row appears at least once in the sheet.
- [ ] `authentication.md` section 10 is no longer empty, and every control it cites is an identifier that already
      exists in that file's sections 8 and 9 — no new `CTL-nn`, no renumbering.
- [ ] **Negative case**: a requirement whose control exists but has **no test** is recorded as `scheduled`, not
      `covered`. The worked instance is ASVS's outbound-request/SSRF requirement:
      `src/Platform/Tailor360.Platform.Abstractions/Ports/IOutboundHttp.cs` declares the port and `grep -rl
      IOutboundHttp src/` finds no implementation, so the row reads `scheduled` against #54 and #55, matching
      `NFR-SE-10`'s "Proof scheduled".
- [ ] **Exception case**: a requirement that this system answers by **not having the feature** is `not applicable`
      with the assumption that excludes it named (A2 and OD-12 for federation and customer accounts), never silently
      `covered`.
- [ ] No requirement row invents a threshold, period or limit. Where a requirement demands a number the project has
      not decided, the row cites the open decision (**OD-08** retention, **RG-OD-01** severities) and the default in
      force.
- [ ] `./scripts/dev docs` passes.

## Data and classification

No field, table, event, log field, telemetry attribute or export column is added.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| `docs/security/asvs-traceability.md` | Internal | Show which verification requirement is met by which control, owner and test | Lives with the repository; reviewed at each wave exit gate | Every contributor | Summarised into the release evidence checklist (`docs/process/release-gates.md` section 6, item 9). Never logged |
| The `scheduled` and `accepted` statuses | Internal | Name a gap and who carries it | As above | As above | A gap is a gap, not an exploit: the row names the missing control, never how to exploit it — `docs/security/README.md` section 2 |
| The quoted ASVS requirement paraphrases | Public — third-party, CC BY-SA | Identify the requirement | As above | As above | Attribution recorded in section 1; no verbatim bulk copy of the standard |

## Permissions and roles

No permission key is added or changed, and no authorisation-matrix fixture is touched. The access-control chapter of
the sheet **cites** `docs/security/permission-matrix.md` and its section 8 test as the evidence, in the role
vocabulary of that document (Owner `owner`, Admin `admin`, Branch Manager `branch_manager`, Reception `reception`,
Measurement Staff `measurement_staff`, Tailor Master `tailor_master`, Tailor `tailor`, Inventory Clerk
`inventory_clerk`, Cashier `cashier`, Delivery Staff `delivery_staff`, Auditor `auditor`, HyFib Super User
`hyfib_super_user`).

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| — | — | — | — | — | — | **None added.** The sheet cites existing rows only |

**OD-13** is open, so the matrix the access-control chapter cites is a documented default rather than an approved
one; the chapter says so.

## Modules, contracts and events

No module, schema, contract, integration event or endpoint. Files changed: `docs/security/asvs-traceability.md`
(new), `docs/security/threat-models/authentication.md` (section 10 only), `docs/security/README.md` (section 2 row),
`docs/nfr/traceability.md` (row `NFR-SE-12`). ARCH-005 and MO-2 are not engaged. **DOR-05**: the flow this sub-issue
documents is the verification programme itself; the authentication flow it fills a table for is covered by
`threat-models/authentication.md`, which is written and reviewed.

## Screens and states

Deleted: a documentation-only Lane C sub-issue publishes no screen.

## Migration and rollback

No table, no column, no migration; `migrate --dry-run` reports no outstanding change and that output is the
evidence. Rollback is a revert: nothing at run time reads these files.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **Which ASVS version and level** — no document in the repository names one; plan Section 8 says only "ASVS L2 + selected L3" | **Open — raised by this sub-issue.** Record it in `docs/prd/assumptions-and-open-decisions.md` section 3 as a new open decision owned by the Business owner with the technical reviewer, for confirmation with **RG-OD-02** | **Level 2 of ASVS 4.0.3**, with named Level 3 requirements where a flow handles money, custody or credentials. 4.0.3 is the default because its numbering is what every other published mapping this project will be read against uses; ASVS 5.0 renumbers, so moving later is a re-keying exercise the sheet's section 1 must warn about | The W2 exit gate |
| **RG-OD-02** — who holds the security owner role | **Open** — "needed before W2, when #56a lands" | The Owner holds every chapter, with the technical reviewer (`docs/nfr/traceability.md` line 67) | The W2 exit gate |
| **RG-OD-01** — the proposed severities and waiver durations | **Open** — needed before the W1 exit gate | The section 4 severities of `docs/process/release-gates.md` as drafted | The W2 exit gate |
| **OD-12** — authentication strategy | **Open**; #23 shipped under the A2 default | Local staff accounts, password plus TOTP or passkeys, no customer accounts. The sheet marks the federation requirements `not applicable` **on that basis and says so**, so the verdict is revisited if OD-12 closes the other way | Before a federation decision |
| **OD-08** — retention periods | **Open** | No period is written into the sheet; the data-protection chapter rows cite OD-08 and the default in force | W1 exit gate (overdue; tracked on OD-08) |

## Required verification

- [ ] Documentation link check: `./scripts/dev docs`, plus `python3 scripts/check-docs-links.py --self-test`.
- [ ] Cross-reference check pasted into the pull request: every `NFR-SE-nn` cited exists in
      `docs/nfr/traceability.md` section 8; every `RG-nn` exists in `docs/process/release-gates.md` section 4;
      every `ARCH-nnn` exists in `docs/architecture/architecture-rules.md`; every `CTL-nn` and `RR-nn` cited for
      authentication exists in `threat-models/authentication.md`. `grep` output, not prose.
- [ ] Completeness check: the count of `NFR-SE-nn` rows in `docs/nfr/traceability.md` section 8 equals the count of
      distinct `NFR-SE-nn` references in section 3 of the new sheet — pasted as two `grep -c` outputs.
- [ ] **DOR-13**: the `docs/nfr/traceability.md` rows affected are `NFR-SE-12` (changed) and `NFR-SE-01` to
      `NFR-SE-14` (referenced, unchanged).
- [ ] **DOR-16**: no environment beyond Python 3 for the link checker; `./scripts/dev doctor` records it.

## Evidence the pull request will carry

- [ ] The rendered diff of `docs/security/asvs-traceability.md`, with the section 1 version and licence statement
      and at least one worked chapter visible.
- [ ] The rendered diff of `authentication.md` section 10, `docs/security/README.md` section 2 and the
      `NFR-SE-12` row.
- [ ] `./scripts/dev docs` output and the self-test output.
- [ ] The cross-reference and completeness `grep` transcripts.
- [ ] The new open-decision row as a diff of `docs/prd/assumptions-and-open-decisions.md`.

## Depends on

Nothing unmerged. It builds on #19 (`docs/nfr/traceability.md`, `docs/process/release-gates.md`), #22
(`docs/security/README.md`, the template), #23 (`threat-models/authentication.md` and its control identifiers), #24
(the permission matrix and the authorisation suites) and #53 (`docs/api/conventions.md`, the security headers and
the enforcing policy).

Runs **in parallel with** the abuse-case catalogue sub-issue of this parent: they share no file. It is not blocked by
the flow models — each model's section 10 is filled by that model's own sub-issue, from this sheet.

Refs #56
