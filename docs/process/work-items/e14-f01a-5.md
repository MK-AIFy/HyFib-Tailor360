Sub-issue of #56. Two rows of the **DOR-05** coverage table in `docs/security/README.md` section 3.2 name flows this
sub-issue covers: "Billing, invoicing and payments → `threat-models/billing-payment.md` → #56a → W2" and "Reports and
exports → `threat-models/reports-exports.md` → #56a → W2". Neither exists — `ls docs/security/threat-models` returns
`authentication.md` alone — and the billing one is the most overdue of the nine, because **Billing is already built**:
thirteen migrations, thirty-nine tables and sixty-five endpoint registrations are in `main`, so this model documents
live money-handling code rather than a design. The parent's third acceptance criterion ("workflow/payment/stock/
barcode business rules cannot be bypassed through direct API calls") cannot be judged for payments until this model
says what the rules are.

## What is already built (do not re-build)

**Billing is live code.** Read it before writing a word of the model.

- `src/Modules/Billing/` — all five projects populated, 183 `.cs` files. Schema `billing`, declared at
  `Tailor360.Modules.Billing.Infrastructure/Persistence/BillingDbContext.cs:29`.
- **Thirteen** migrations under `Billing.Infrastructure/Migrations/`: `20260912061752_InitialBillingSchema`,
  `PriceLists`, `20260912082804_CalculationSnapshots`, `InvoiceDrafts`, `InvoicePosting`, `DocumentArtifacts`,
  `CashierSessions`, `Payments`, `Receipts`, `Reversals`, `ReconciliationBatches`, `DispatchExceptions`,
  `20260912224416_AuditEventMapping`. Count them with
  `ls src/Modules/Billing/Tailor360.Modules.Billing.Infrastructure/Migrations/*.cs | grep -v Designer | grep -v ModelSnapshot | wc -l`
  — **and do not filter on the word `Snapshot`**, which silently drops `CalculationSnapshots` and is how the count
  twelve got into circulation. Thirty-nine distinct tables: `grep -rho 'ToTable("[a-z_]*"' src/Modules/Billing/ |
  sort -u | wc -l`. The tables they
  create include `invoices`, `invoice_lines`, `invoice_tax_components`, `invoice_line_surcharges`,
  `invoice_cancellations`, `adjustment_notes`, `adjustment_note_lines`, `adjustment_note_taxes`, `payments`,
  `payment_allocations`, `payment_modes`, `payment_mode_branches`, `payment_reversals`, `advances`, `refunds`,
  `receipts`, `cashier_sessions`, `cashier_session_counts`, `cashier_session_mode_totals`,
  `reconciliation_batches`, `reconciliation_batch_mode_lines`, `dispatch_exceptions`, `dispatch_exception_jobs`,
  `price_lists`, `price_list_versions`, `price_list_items`, `price_list_version_branches`, `discount_rules`,
  `tax_configuration_versions`, `tax_codes`, `tax_components`, `gst_registrations`, `calculation_snapshots`,
  `document_artifacts`, `order_facts`, `order_fact_jobs`, `outbox_messages`, `inbox_messages`. **Enumerate them from
  the migrations, not from this list**, and note that `order_facts` and `order_fact_jobs` are Billing's own
  projection of order data received through `Orders.Contracts` — not a read of `orders.*`.
- Sixty-five endpoint registrations across `Billing.Api/`: `Invoicing/InvoiceEndpoints.cs`,
  `Payments/PaymentEndpoints.cs`, `CashierSessionEndpoints.cs`, `ReceiptEndpoints.cs`, `RefundEndpoints.cs`,
  `ReconciliationEndpoints.cs`, `DispatchExceptionEndpoints.cs`, `PaymentModeEndpoints.cs`,
  `Pricing/PricingEndpoints.cs`, `PriceListEndpoints.cs`, `Tax/`, `Registrations/`, with payload types under
  `Billing.Api/Payloads/` and its own `Problems.cs`. Enumerate the live routes from
  `docs/api/openapi.v1.json`, which is the published contract.
- `tests/fixtures/billing/pricing-golden-master.json` and `calculation-snapshot-v1.json` — the accountant's golden
  master. It is the existing integrity control over pricing and tax, and the model cites it as such.
- The permission keys already seeded in
  `src/Platform/Tailor360.Platform.Security/Permissions/BillingPermissions.cs`: `billing.create_invoice`,
  `billing.update_invoice`, `billing.post_invoice`, `billing.cancel_invoice`, `billing.post_credit_note`,
  `billing.print_receipt`, `billing.override_price`, `billing.manage_price_lists`, `billing.publish_price_list`,
  `billing.approve_dispatch_exception`, `payments.record`, `payments.record_intent`, `payments.allocate`,
  `payments.allocate_manual`, `payments.refund`, `payments.reverse`, `payments.session`,
  `payments.approve_reconciliation`.
- `docs/architecture/invariants.md` sections 4.10 (Invoice, credit note and debit note), 4.11 (Payment, allocation,
  advance, refund and receipt), 4.12 (CashierSession) and section 6 — the invariants the model's controls point at,
  including the cashier-close rules (`INV-CSH-03`, `INV-CSH-04`) that #161 shipped.
- `docs/nfr/data-classification.md` sections 5.10 (invoices and notes), 5.11 (payments, advances, refunds and
  allocations), 5.12 (receipts, cashier sessions and reconciliation) and 5.19 (reporting projections and exports) —
  the asset inventory. **Financial** is one of the seven classes; do not reclassify anything.
- `docs/prd/assumptions-and-open-decisions.md` **OD-05** (valuation, rounding and GST retention, with #147's
  documented default), **OD-04** (the dispatch payment rule) and **OD-24** (the cashier variance threshold, default
  zero, raised by #161) — the three decisions this flow runs on.

**Reporting is scaffolding.** Model the design and say so.

- `src/Modules/Reporting/` holds seven files, all scaffolding; no `DbContext`, no migration, so the `reporting`
  schema does not exist. `docs/architecture/module-ownership.md` around line 605 gives its published events
  (`reporting.report-reconciliation-mismatch.v1`, `reporting.report-freshness-breached.v1`) and the rule that
  matters most here: **Reporting reads no other module's tables and no module reads `reporting.*`**; the
  `t360_reporting` database role holds `SELECT` on `reporting.*` and on published contract views only, and open
  decision **AOD-03** records that no contract view exists yet.
- `docs/adr/0011-reporting-read-models.md` — the projection design; ARCH-011 (Reporting references only other
  modules' `Contracts` projects) is its architecture guard.
- `reports.read` and `reports.export` in `ReportingPermissions.cs`, and `audit.export` in `PlatformPermissions.cs`,
  are seeded; no endpoint declares any of them yet.
- The open sub-issues that will build it, and that the model's controls must be mapped to: #183 (foundations,
  projection runner, checkpoints, rebuild, query isolation), #184 (sales, invoice register, GST summary), #185
  (payments, advances, refunds, cashier variance), #186 (receivables ageing, governed exports, scheduled reports).

**The one export surface that does exist** is the customer personal-data export —
`POST /api/v1/customers/{customerId}/export` and `GET /api/v1/customers/{customerId}/exports/{exportId}`
(`src/Modules/Customers/Tailor360.Modules.Customers.Api/Customers/CustomerEndpoints.cs:124` and `:177`), backed by
`customers.customer_exports` and `Customers.Application/Customers/CustomerExportHandler.cs`. It belongs to
`threat-models/customer-and-media.md`; `reports-exports.md` cross-references it rather than re-modelling it, and the
two models must not both claim it.

**Shared, and not to be re-derived**: `docs/templates/threat-model.md`; `docs/security/abuse-cases.md` with the
`ABF-01` to `ABF-11` families (the first sub-issue of this parent) — `ABF-05` invoice or payment tampering and
`ABF-08` export leakage are this sub-issue's centre of gravity; `docs/security/permission-matrix.md`;
`docs/security/field-visibility.md`; `docs/adr/0015-shared-audit-ledger-mapped-into-module-contexts.md`.

## What is missing

- Two of the ten files of `docs/security/README.md` section 3.2. For Billing that means sixty-five live endpoints,
  including every money-moving one, are in production-bound code with no threat model — the single largest DOR-05
  gap in the repository today.
- Nothing writes down how a posted invoice, an allocated payment or a closed cashier session is protected from being
  rewritten, so DoD 6 has nothing for a Billing pull request to reference and `NFR-SE-07` has no flow-level argument.

## Scope

- [ ] Add `docs/security/threat-models/billing-payment.md` from `docs/templates/threat-model.md`, covering: price
      list and tax configuration versions and their publication, the pricing calculation and its snapshot, invoice
      draft, posting, cancellation and adjustment notes, the GST registration and place-of-supply inputs, payment
      intent, record, allocation and manual allocation, advances, refunds, reversals, receipts, the cashier session
      and its close and reconciliation, the dispatch-exception approval, and the document artefacts (invoice and
      receipt renders) the flow stores.
- [ ] Add `docs/security/threat-models/reports-exports.md`, covering: the projection runner and its checkpoints, a
      rebuild, query isolation and the `t360_reporting` role, report reads, the governed export (who may export what,
      at what granularity, with what audit), scheduled reports and their delivery, freshness and reconciliation
      mismatch signalling, and the export formats and their injection surface.
- [ ] Both: every template section filled; every "(example — delete)" row and the "How to use" section removed.
- [ ] `billing-payment.md` section 2 enumerates the **live** routes from `docs/api/openapi.v1.json` with the
      permission each declares today, and section 5 states where a route's permission, rate-limit policy, audit
      action or resource scope is the control (ARCH-007, ARCH-008, ARCH-017, ARCH-018, ARCH-023).
- [ ] `billing-payment.md` models the no-half-posting sequence and the money invariants as controls with their
      tests: a posted invoice is immutable and is corrected only by an adjustment note; an allocation never exceeds
      the payment or the invoice; a reversal is a new row, never an update; a closed cashier session cannot be
      reopened; the golden master in `tests/fixtures/billing/` pins the arithmetic. Name the real test files that
      exist under `tests/Tailor360.IntegrationTests/Billing/` and `tests/Tailor360.UnitTests`.
- [ ] `reports-exports.md` models **query isolation** as a control: Reporting reads no other module's tables
      (ARCH-011), no module reads `reporting.*`, and the `t360_reporting` role's grants are the enforcement — citing
      `docs/architecture/module-ownership.md` and recording **AOD-03** as the open decision over contract views.
- [ ] `reports-exports.md` models export leakage (`ABF-08`) end to end: an export that widens a caller's field
      visibility, an export that crosses a branch, a scheduled export delivered to an address the caller chose, a
      stale projection presented as current, and formula injection in a spreadsheet export.
- [ ] Section 7 of each model cites the `ABF-nn` family for every abuse case; section 8 names the test that fails if
      each control is removed, or says "no test yet — #NN"; section 9 carries a residual-risk table with rating,
      owner and acceptance date, and any **high** risk also gets a `docs/process/waivers.md` entry with an expiry and
      a corrective issue.
- [ ] Section 10 of each model: the ASVS requirements the flow is judged against, from
      `docs/security/asvs-traceability.md`; if that sheet has not merged, record its four-value status vocabulary and
      name the sub-issue that fills the rows. Invent no requirement number.
- [ ] Update `docs/security/README.md` sections 2 and 3.2 for these two flows, with this sub-issue's number and the
      date.
- [ ] Versioned configuration (DOR-11): none added. Where a control is configuration — the variance reason
      threshold `Billing:Cashier:VarianceReasonThreshold`, the payment modes, the tax configuration version — the
      model names the key or the versioned data and says the **value** is the Owner's and the accountant's, citing
      **OD-05** and **OD-24**.

## Out of scope

The authorisation model (#32a); the customer personal-data export, which `customer-and-media.md` owns; the order,
custody, inventory, integration and deployment flows (the other **three** model sub-issues of this parent); the payment
**gateway** adapter and the accounting export adapter, which belong to `threat-models/integrations.md` and to #200
and #203 — `billing-payment.md` ends at the port and says so. Implementing any control, and building Reporting
(#183 to #186). No test, no source file and no configuration change in this pull request.

## Acceptance criteria

- [ ] Both files exist, are complete against the template (no placeholder row, no "How to use" section) and are
      reachable from `docs/security/README.md` sections 2 and 3.2, which name this sub-issue.
- [ ] `billing-payment.md` section 2 lists every live money-moving route from `docs/api/openapi.v1.json` with the
      permission it declares, and section 6 has at least one STRIDE row per live endpoint group — invoice, payment,
      refund, reversal, receipt, cashier session, reconciliation, dispatch exception, price list, tax configuration.
- [ ] Every asset in section 3 of both models carries one of the seven classes of
      `docs/nfr/data-classification.md` section 2 and points at the inventory subsection (5.10, 5.11, 5.12 or 5.19)
      that governs it. Money and tax data are **Financial**; nothing is reclassified.
- [ ] Every STRIDE row ends in a control or a residual risk; every abuse case cites an `ABF-nn` family; every
      control names a test file that exists, or says "no test yet — #NN".
- [ ] **Negative case, payment tampering (`ABF-05`)**: rows for a client-supplied amount or tax figure being trusted,
      an allocation that over-allocates, a reversal that mutates the original payment, a receipt reprinted as a
      second receipt, a posted invoice edited, and a cashier session closed twice — each with the server-side check
      that refuses it and the refusal's exact shape (RFC 9457 problem details, never a stack trace).
- [ ] **Negative case, export leakage (`ABF-08`)**: rows for an export that returns fields the caller may not read
      on screen, an export scoped to another branch, and a scheduled export whose recipient is caller-chosen — each
      with the control and the test that must exist, and with `docs/security/field-visibility.md` named as the
      existing mechanism.
- [ ] **Exception case**: each model documents the sanctioned way out and what it must not do — an adjustment note
      (never an edit of the posted invoice), a reversal (never a deletion), a manual allocation (reason required and
      audited), an approved dispatch exception (Billing-owned, time-bounded, audited), and a projection rebuild
      (never presented as live data while it runs, and never silently changing a figure already exported).
- [ ] No invented number: no rounding rule, variance threshold, retention period, export row cap or page-size limit
      is written as a figure. Each cites its open decision (**OD-04**, **OD-05**, **OD-08**, **OD-24**) or the
      published row in `docs/nfr/capacity-and-performance.md` it comes from.
- [ ] `./scripts/dev docs` passes.

## Data and classification

This sub-issue adds two documents; it adds no field, table, event, log field, telemetry attribute or export column.
Each model's section 3 is where DOR-06 is answered for its flow.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| `docs/security/threat-models/billing-payment.md` | Internal | The argument that money cannot be moved, rewritten or unrecorded, and the tests that keep it true | Lives with the repository; superseded, never deleted | Every contributor | Never logged; summarised into release evidence item 9 of `docs/process/release-gates.md` section 6 |
| `docs/security/threat-models/reports-exports.md` | Internal | The argument that a report cannot show what its reader may not see, and that an export is governed and audited | As above | As above | As above |
| Asset rows naming invoice, payment and tax fields | Internal — field names only, describing **Financial** assets | Say what is worth attacking | As above | As above | No amount, invoice number, GST registration or receipt from any real record appears; synthetic illustrations only (CLAUDE.md section 5) |
| Asset rows naming a provider credential or a gateway key | Internal, describing a **Credentials and secrets** asset | Say what must never be stored in this schema or logged | As above | As above | No value appears; the model points at `docs/platform/secrets.md` and at `threat-models/integrations.md` for handling |
| Asset rows naming an export file | Internal, describing an artefact that may carry **Personal** and **Financial** data together | Show why an export is the highest-leverage disclosure in the system | As above | As above | The model states that an export's **existence** is audited (`audit.export`, `reports.export`) and that its contents are never logged |

## Permissions and roles

No permission key is added, removed or changed; the authorisation-matrix fixtures in
`tests/Tailor360.IntegrationTests/Authorization/matrix.yaml` are cited as evidence and not edited. All keys below
exist in `src/Platform/Tailor360.Platform.Security/Permissions/`, and the models cite the
`docs/security/permission-matrix.md` row for each rather than restating the grants.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `billing.create_invoice`, `billing.update_invoice`, `billing.post_invoice`, `billing.cancel_invoice`, `billing.post_credit_note` | branch | per the matrix | per the matrix | `cancel_invoice` and `post_credit_note` carry a reason | per the matrix | **Exist** — `BillingPermissions.cs`. `post_invoice` is the irreversible step and the centre of section 6 |
| `billing.override_price`, `billing.manage_price_lists`, `billing.publish_price_list` | branch | per the matrix | per the matrix | `override_price` carries a reason | per the matrix | **Exist** — the price-tampering rows |
| `billing.approve_dispatch_exception` | branch | per the matrix | per the matrix | yes | per the matrix | **Exists** — the cash-protection exception; Custody never reads `dispatch_exceptions` directly (plan Section 6.2 note 2) |
| `payments.record`, `payments.record_intent`, `payments.allocate`, `payments.allocate_manual`, `payments.refund`, `payments.reverse`, `payments.session`, `payments.approve_reconciliation` | branch | per the matrix | per the matrix | `allocate_manual`, `refund`, `reverse` and `approve_reconciliation` carry a reason | per the matrix | **Exist** — `approve_reconciliation` is the four-eyes control `INV-CSH-04` asks for |
| `billing.print_receipt` | branch | per the matrix | per the matrix | no | per the matrix | **Exists** — the reprint-versus-second-receipt row |
| `reports.read`, `reports.export` | branch | per the matrix | per the matrix | `export` carries a reason | per the matrix | **Exist** as catalogue entries in `ReportingPermissions.cs`; **no endpoint declares either yet**, recorded as a finding against #183 to #186 |
| `audit.export` | organisation | per the matrix | per the matrix | yes | per the matrix | **Exists** — `PlatformPermissions.cs`; cited by `reports-exports.md` as the most sensitive export of all |

**OD-13** is open, so every grant cited is the documented 2026-09-06 default rather than an approved one; each model
says so once in section 5.

## Modules, contracts and events

Modules read: Billing (schema `billing`, owner of every table above) and Reporting (schema `reporting`, not
created); Orders and Customers only through their `Contracts` projects. No source file changes, so no architecture
rule can be violated by this pull request; the models **assert** ARCH-004, ARCH-005, ARCH-008, ARCH-010 (no Billing
project references any Orders project), ARCH-011 and ARCH-017 as controls and name the architecture test that
enforces each.

Cross-module mechanisms the models must name exactly: the read contracts `Orders.Contracts.IOrderSnapshotQuery`,
`Customers.Contracts.ICustomerSnapshotQuery`, `Billing.Contracts.IPricingService`,
`Billing.Contracts.IDispatchEligibilityQuery`; Billing's own projection tables `order_facts` and `order_fact_jobs`,
fed by the versioned outbox events of `Orders.Contracts` (ADR-0008) and **not** by a read of `orders.*`; the
published Reporting events `reporting.report-reconciliation-mismatch.v1` and
`reporting.report-freshness-breached.v1`; and the platform ports `IPdfRenderer`, `IPrintQueue` and `IObjectStorage`
for document artefacts. **DOR-05**: this sub-issue is threat-model work and closes two rows of
`docs/security/README.md` section 3.2.

## Screens and states

Deleted: a documentation-only Lane C sub-issue publishes no screen. Where a control belongs to the client — a
cashier close that must not let a count be edited after submission, a report that must show its freshness — the
model names the owning issue (#161-era billing screens, #184 to #186) and the state it must not reach.

## Migration and rollback

No table, no column, no migration. `dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` reports no
outstanding change and that output is the evidence. The models describe Billing's **existing** tables and
Reporting's **designed** ones; creating the latter belongs to #183. Rollback is a revert.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-05** — valuation method, rounding and the statutory retention period for GST records | **Open**; #147 shipped the rounding half under a documented default | Half away from zero to paise, once per printed component, sums never re-rounded, the document round-off explicit, and an inclusive rate backing the rounded components out of the quoted amount — as `docs/architecture/conventions.md` section 1.2 fixes and `tests/fixtures/billing/pricing-golden-master.json` pins. The model cites the golden master as the control and writes no rule of its own | W4 |
| **OD-24** — the cashier variance a close may carry without a reason, and who approves beyond it | **Open** — raised 2026-09-12 by #161 | `Billing:Cashier:VarianceReasonThreshold` defaults to **zero**: every variance is explained. The model cites the key, not a figure | W4 |
| **OD-04** — the payment rule for dispatch, the partial-delivery policy and who approves an exception | **Open** — needed before W4 | The dispatch gate fails closed (`Eligible = false, Reason = NotEvaluated`); the exception is the only way through and it is Billing-owned, approved, reasoned and audited | W4 |
| **OD-08** — retention periods, including financial records | **Open** | No period written; every retention cell reads "Open — OD-08" and names #57 as the enforcing issue. GST record retention is also part of **OD-05** and is co-signed by the accountant | W1 exit gate (overdue) |
| **OD-03** — providers, including the payment gateway | **Open** — needed before W4 | Fakes only; the gateway is out of this model's scope and is carried by `threat-models/integrations.md` and #200 | W4 |
| **AOD-03** — the catalogue of published contract views and which roles may select from them (`docs/architecture/module-ownership.md`) | **Open** | No contract view exists; every read contract is in-process code, and the `t360_reporting` grant covers `reporting.*` only. `reports-exports.md` models that state | #21 and #45 |
| **RG-OD-02** — who holds the security owner role | **Open** — needed before W2 | The Owner, with the technical reviewer, is the residual-risk owner in section 9 of both models | W2 exit gate |
| **OD-13** — approved role-to-permission grants | **Open** | The twelve seeded roles and their default grants, cited as default | W1 exit gate (overdue) |

## Required verification

- [ ] Documentation link check: `./scripts/dev docs`, plus `python3 scripts/check-docs-links.py --self-test`.
- [ ] Path, route and symbol verification pasted into the pull request: every route, table, permission key,
      `ARCH-nnn` rule, invariant identifier, `NFR-` row and test file cited in either model exists today —
      `grep`/`ls` output. For Billing the route list must be taken from `docs/api/openapi.v1.json`, the published
      contract, not from the source.
- [ ] Template completeness: `grep -c "example — delete"` returns 0 for both files and the "How to use" section is
      absent.
- [ ] Control-to-test check: every control in section 8 of `billing-payment.md` that claims a test names a file
      under `tests/`, and the `ls` of those files is pasted. A claimed test that does not exist is the failure mode
      this check exists to catch.
- [ ] **DOR-13**: the `docs/nfr/traceability.md` rows these models become the evidence for are `NFR-SE-02`,
      `NFR-SE-07`, `NFR-AU-01`, `NFR-AU-04`, `NFR-PR-01` and the reporting freshness rows; no status changes,
      because a model is not a proof.
- [ ] **DOR-16**: Python 3 for the link checker and the .NET command line for the dry-run; `./scripts/dev doctor`
      records both.

## Evidence the pull request will carry

- [ ] The rendered diff of both models, with section 6 (STRIDE), section 8 (controls and tests) and section 9
      (residual risk with owner and date) visible for each.
- [ ] The rendered diff of `docs/security/README.md` sections 2 and 3.2.
- [ ] `./scripts/dev docs` output and the link-checker self-test output.
- [ ] The `grep`/`ls` transcript proving every cited route, table, permission key, invariant and test exists.
- [ ] `grep -c "example — delete"` showing zero placeholder rows.
- [ ] `migrate --dry-run` output showing no outstanding migration.
- [ ] Any **high** residual risk carried, as a diff of `docs/process/waivers.md` with an expiry and a corrective
      issue. Billing is the flow most likely to produce one: a live money surface with no model until now.

## Depends on

- The abuse-case catalogue sub-issue of this parent — provides `abuse-cases.md`, the `ABF-nn` identifiers and the
  template's family column. **Must be merged first.**
- The ASVS skeleton sub-issue of this parent — provides the section 10 vocabulary and requirement numbers; if it has
  not merged, section 10 records the vocabulary and names that sub-issue.
- Merged work it reads: #18 (`invariants.md`), #19 (`data-classification.md`, `traceability.md`,
  `capacity-and-performance.md`), #22 (`README.md`, the template), #24 (the permission catalogue and matrix,
  `field-visibility.md`), #41 and #147 and #161 and the rest of the Billing work already in `main`, and
  `docs/adr/0011-reporting-read-models.md`.

**In-flight work in the same area**: #183 to #186 (Reporting), #200 (payment gateway port and adapter), #203
(accounting export), #205 (print bridge). None touches `docs/security/threat-models/`. The only file shared with a
sibling model sub-issue of this parent is `docs/security/README.md`, where this one edits two rows of section 3.2 and
two rows of section 2; whichever merges second rebases, one row per flow.

Refs #56
