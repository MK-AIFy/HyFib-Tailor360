Sub-issue of #48. **Evidence only. No production code, no migration, no endpoint, no screen.** It exists so that no
coding session in this parent is blocked on a thermal printer, a hardware scanner, a real phone at a real doorstep, a
person who reads Tamil, or a browser tool a headless agent cannot drive. Its deliverable is recorded evidence in `docs/`
and in the pull request, nothing more.

**The parent's own required verification asks for it by name.** #48 lists "Physical scanning and delivery-team mobile
UAT", plan Section 8's row for #48 ends with "delivery-team mobile UAT in installed mode; **paid-dispatch physical
rehearsal**", and plan Section 6.2 note 2 says the paid dispatch "is rehearsed again in #43 and #48". None of those is a
test run. `docs/process/definition-of-ready.md` line 86 settles where they belong: its optional-criteria table lists
"**DOR-16 physical hardware** — the issue's evidence includes a printed label, a scanned barcode or a real device
journey" as applying to "**#35, #36, #37, #48 and #61c always**". Every coding child of #48 therefore declares a dev
container and a database, and **this child declares the devices and the people**.

**And `A11Y-PZ-03` is priority zero and unwaivable.** `docs/nfr/a11y-checklist.md` section 6.5 is the dispatch gate's
screen-reader record, with its nine steps and its blunt reason: "a blocked dispatch that sounds like a broken screen is
how the control gets worked around". A screen-reader walk is a human at a device with a screen reader, recorded on the
template in that document's section 8.

## What this issue is NOT

- **Not a waiver.** `docs/process/waivers.md` is for a **gate that cannot be met**, not for evidence a human has not yet
  produced. Nothing here is waived; it is scheduled.
- **Not a test-automation issue.** The Playwright suite, its axe integration, `expectNoHorizontalOverflow` in a real
  browser and the Lighthouse budgets are **#52**'s — `tests/e2e` and `playwright.config` do not exist in this repository
  and `scripts/dev` says the suite arrives with #52. If #52 has merged by the time this runs, automate what it can carry
  and record only what remains.
- **Not an excuse to thin the coding siblings' verification.** Every machine-checkable assertion stays where it is: the
  jsdom axe runs, the pseudo-locale stories, the five state stories, the Vitest route tests, the authorisation matrix,
  the six eligibility reasons and the redaction assertions are in [E11-F02-1] through [E11-F02-8b] and are **not**
  deferred here.
- **Not #61's release evidence index, and not its regression suite.** [E15-F03-6] automates blocked and allowed dispatch
  under every policy and [E15-F03-6b] the delivery exceptions; this issue **feeds** the release evidence index and
  replaces neither.

## Scope

- [ ] **The paid-dispatch physical rehearsal** (plan Section 6.2 note 2), at the head branch, with a **printed label**
      and a **keyboard-wedge or camera scan**: a confirmed order, an invoice posted and settled, the delivery-team
      receive scan, the dispatch scan, the doorstep confirmation and the delivery recorded. Log the device, the printer,
      the scanner, the build (commit hash), the participants and the date. `docs/process/definition-of-ready.md` line
      174 already names the kit: "Thermal printer and at least one keyboard-wedge scanner available at the head branch".
- [ ] **The blocked-dispatch rehearsal, which is the one the business is buying**: the same order **unpaid**, scanned at
      the branch, refused; the balance taken at the counter; scanned again and allowed. Then once more through the
      **exception** path, approved by somebody other than the dispatcher (EX-10 requires step-up and a different
      person), and confirm that a second use of the same exception is refused. Record what the delivery staff member saw
      and heard at each step.
- [ ] **The failed-delivery and return rehearsal** (EX-12): record a failed delivery at the door with a configured
      reason, bring the garment back, receive it into branch custody, and confirm from the screens that the queue entry
      reopened and that a **new** authorisation was required — "the spent dispatch authorisation cannot be reused".
- [ ] **The `A11Y-PZ-03` screen-reader walk, all nine steps**, on the Tier 1 kit of `docs/nfr/a11y-checklist.md` section
      2.2, recorded on the template in its section 8. Step 4 — "hear the refusal, the reason and what to do next" — is
      the one the record exists for; do not record it as a pass because the text is present on screen if the screen
      reader did not announce it.
- [ ] **Real-device installed-mode walk** of `/delivery`, `/delivery/orders/:orderId`, the two gate screens and the
      doorstep screen, on one Tier 1 Android phone and one Tier 1 iPhone, in a browser tab **and** in installed mode,
      with device, operating-system and browser versions logged — the shape `docs/nfr/support-matrix.md` line 327
      specifies ("Every release; stored with the release evidence index"). Record the result in that document's
      installed-mode rows.
- [ ] **Real-device browser walk of the customer status page** `/c/status/{token}` on the same two devices, in a browser
      tab only: it is served outside the PWA shell and outside the service-worker scope, so installed mode does not
      apply — state that rather than leaving it as a gap — plus **one Lighthouse mobile-profile run**, which is what
      plan line 2083 asks for, reported against the Largest Contentful Paint row of
      `docs/nfr/capacity-and-performance.md` section 3.3 and **claimed as no gate**, because that row names other route
      classes and every target in that table is proposed.
- [ ] **The one-time code, end to end on a real handset**: a code that actually arrives on a consented channel, typed
      in, accepted; then a wrong code, then the fifth wrong attempt, then an expired one. With **OD-03** open the only
      live channel is email through the existing SMTP adapter, which Mailpit serves in the compose stack (`./scripts/dev
      up`), so record the channel used and say plainly that SMS and WhatsApp were not exercised.
- [ ] **Browser axe run** over the four client surfaces and the server-rendered status page in both locales, with no
      serious or critical violation. A browser extension or a one-off script against the running host is acceptable
      evidence while #52 is open; attach the report.
- [ ] **Keyboard-only walk** of the doorstep screen proving a handover completes with **no dragging interaction at all**
      (`A11Y-75`) and that the typed name plus code is offered even under the `Signature` branch policy (`A11Y-78`).
      Record the tab order actually observed.
- [ ] **200% zoom and 320 px read** of all five surfaces, by eye, in English and Tamil, **outdoors or under a bright
      lamp in the High contrast — sunlight theme**, which is how a delivery phone is actually read. This is the thing
      `expectNoHorizontalOverflow` cannot tell you: whether the page is still *usable*, not merely free of horizontal
      scroll.
- [ ] **The Tamil read**, by somebody who reads Tamil, of every string in `clients/pwa/src/i18n/messages/delivery.ts`,
      of the four `Templates/delivery.*` bodies and the `delivery.confirmation-code` body, and of the status page's
      server-side Tamil set. A pseudo-locale story proves the layout survives growth; it proves nothing about whether
      the words are right.

## Out of scope

- Writing or changing any code under `src/` or `clients/pwa/src/`. **If a walk finds a defect, file it against the
  sibling that owns the surface** and link it here; do not fix it in this issue, because a fix needs the tests and the
  review of the slice it belongs to.
- Building the Playwright suite, the continuous Lighthouse budgets or the client-telemetry redaction test — **#52**.
- The load and penetration testing of `/c/status/{token}`: `tests/load` does not exist and a penetration test is #56b's.
  The abuse bound here is #211's per-token and per-IP counters, asserted in [E11-F02-5]'s integration tests.
- A payment gateway. The balance is taken at the counter in cash or by an existing recorded mode; **OD-03** leaves no
  live gateway, and none is needed for any step above.
- An owner sign-off. **OD-04**, **OD-08**, **OD-10** and **OD-13** stay open and are closed by the owner at the
  workshop, not by this issue. Record the behaviour the interim defaults produced.
- Creating `docs/evidence/`. It does not exist, and plan Section 6.4 assigns the epic-closure document to the last pull
  request of the epic; inventing the directory early would give two pull requests a claim on it. Attach the artefacts to
  the pull request and link them.

## Acceptance criteria

- [ ] Every rehearsal above is walked end to end and recorded, each with the date, the participants, the build (commit
      hash), the devices, the printer and scanner used, and the outcome. A rehearsal that could not be completed is
      recorded as **not completed, with the reason and the blocking issue** — never as a pass.
- [ ] **Negative:** the blocked-dispatch rehearsal is walked, and the refusal is recorded **as the delivery staff member
      experienced it** — what was said, what was offered, what they would have done next without being told. A rehearsal
      that only walks the paid path does not satisfy this issue; positive-path-only coverage is the review finding
      `CLAUDE.md` section 5 names as the most common.
- [ ] **Negative:** the failed delivery, the return to branch, the re-evaluated second attempt, the five-attempt code
      exhaustion and the second use of a spent exception are each observed and recorded with what the screen said.
- [ ] **Exception:** the completed `docs/nfr/a11y-checklist.md` record for `A11Y-PZ-03` is attached with **every one of
      its nine steps answered**, including any answered "not applicable", each with a one-line reason. Section 6.5 is
      priority zero, and a partially completed record is not evidence.
- [ ] **Exception:** the Tamil read is done by somebody who reads Tamil, and every correction is filed against the
      owning sibling with the exact message id or template key. If nobody available reads Tamil, that is recorded as an
      **open** item with an owner and a date — not as a pass, and not as a waiver.
- [ ] **Exception:** where #52 has merged, the axe runs, the overflow widths and the Lighthouse budgets are recorded as
      **automated** and this issue records only the device, rehearsal and screen-reader halves. Say which applied.
- [ ] The installed-mode rows of `docs/nfr/support-matrix.md` carry the two device walks with their versions.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Everything entered during the rehearsals | **Synthetic only** | Exercise the journeys | With the evidence | Whoever reads the pull request | `CLAUDE.md` section 5 and security rule 10: synthetic data only outside production, and **no production data is ever copied into development, staging or a fixture**. No real customer name, telephone number, address or measurement in any screenshot or recording. Note that `seed-synthetic` covers only Identity and Customers today, so the order, billing and delivery fixtures are built by hand in the walk |
| Screenshots and recordings | Internal | Evidence | With the release evidence index | As above | Check every frame. A screenshot is the commonest way a real name or number leaves the building |
| The delivery one-time code used in a walk | **Credentials and secrets** — `docs/nfr/data-classification.md` section 6.2, row "Delivery one-time passwords" | Complete the handover | Spent by the walk; single-use by construction | Nobody | **Never** in a screenshot, a recording frame, a pasted message or a bug report. Photograph the page, not the message |
| A status link used in a walk | **Credentials and secrets** — section 5.14 | Open the page | Revoke it after the walk | Nobody | **Never** in a screenshot, a recording frame, a pasted URL or a bug report. Crop the address bar |
| The recipient name and signature captured in a walk | **Personal** and **Sensitive Personal** — section 5.6 | Complete the handover | With the synthetic record | As above | Use a synthetic name and the walker's own scrawl; never a customer's |
| Device, operating-system, browser, printer and scanner versions | Internal | Say what was actually tested | With `support-matrix.md` | As above | Safe |

## Permissions and roles

No permission is added and `docs/security/permission-matrix.md` is not touched. The walks are performed as the seeded
roles precisely in order to observe the grants the matrix already records — `custody.receive` (line 175),
`custody.dispatch` (169), `custody.confirm_delivery` (168), `custody.record_delivery_outcome` (177), `custody.open_case`
(173), `custody.manual_lookup` (172) and the `custody.read_delivery_queue` that [E11-F02-1] proposes — and to observe
that a Delivery Staff principal **cannot** approve the dispatch exception they are blocked by, which needs
`billing.approve_dispatch_exception` with MFA and step-up and a different person. That separation is a thing to *observe
and confirm*, not a defect to report. **OD-13** is open.

## Modules, contracts and events

None. This issue touches no module, crosses no boundary and publishes no event. The only file it changes is
`docs/nfr/support-matrix.md` (the installed-mode rows), plus the completed checklist and the rehearsal records attached
to the pull request.

## Screens and states

None. It exercises the five surfaces [E11-F02-5], [E11-F02-7], [E11-F02-8a] and [E11-F02-8b] ship.

## Migration and rollback

No migration and nothing to roll back. A `docs/` change is reverted by reverting the commit.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-03** providers — SMS, WhatsApp, email | **Open**, needed before W4 | Walk the code journey on **email through the SMTP adapter**, which Mailpit serves in the compose stack; every other channel suppresses with a recorded reason, and that suppression is itself one of the negative cases to observe | W4 |
| **OD-04** — payment rule, partial-delivery policy, doorstep payment | **Open**, needed before W4 | Walk the interim defaults — `WholeOrder`, Billing's `Full` rule, `Otp` recipient confirmation — and record what they produced. No doorstep take-payment step exists to walk | W4 exit |
| **The Tier 1 device list** | **Open** as part of **#19** and **#52**; `assumptions-and-open-decisions.md` records the device, browser and printer matrix as undecided | Use the Tier 1 classes `docs/nfr/support-matrix.md` already names; where it names none firmly, walk one recent Android phone and one recent iPhone and **record exactly which**, so a later decision can say whether that was enough | #19 / #52 |
| **OD-08**, **OD-10**, **OD-13** | All open | Observe the behaviour the defaults produce and record it. This issue closes none of them, and a walk is not an owner sign-off | Their own dates |

## Required verification

This issue **is** the verification. What it must not do is verify by assertion: every item above resolves to an artefact
— a completed checklist, a report file, a screenshot, a logged version string, a dated note naming the participants —
because `docs/process/definition-of-done.md` section 4 admits nothing else, and `CLAUDE.md` section 7 is explicit: "A
pull request is done when a second person can see that it is done", and "'Tested locally' is not evidence."

## Evidence the pull request will carry

- [ ] The rehearsal records: paid dispatch, blocked dispatch with both remedies, failed delivery and return — each with
      date, participants, commit hash, devices, printer, scanner and outcome.
- [ ] The completed `docs/nfr/a11y-checklist.md` record for `A11Y-PZ-03`, all nine steps answered.
- [ ] The `docs/nfr/support-matrix.md` diff carrying the two installed-mode walks with their versions.
- [ ] The axe reports for the four client surfaces and the status page, in both locales.
- [ ] The keyboard walk's observed tab order, and the statement that a handover completed with no drag.
- [ ] Screenshots at 320 px and 200% zoom in both locales, in the High contrast — sunlight theme — **synthetic data
      only, address bars cropped, no code or link visible**.
- [ ] The Lighthouse mobile run summary for `/c/status/{token}`, with no gate claimed.
- [ ] A list of every defect found, each filed against its owning sibling with the issue number, and a statement that
      none was fixed in this pull request.

## Depends on

[E11-F02-5] — the status page. [E11-F02-7] — the queue and stop-card screens. [E11-F02-8a] — the receive and dispatch
screens. [E11-F02-8b] — the doorstep screen. [E11-F02-6] and [E11-F02-6b] — the messages, without which no code and no
status link ever reaches a handset. **All must merge first.**

#190, #37's **[E07-F03-1]** and **[E07-F03-2]**, #34, #35 and #36 — the schema, the scans, the ready gate, the printed
label and the scanning experience the rehearsal physically uses. All **open**: a rehearsal with no printed label is not
a rehearsal, which is why this child is last in the parent and why it is not a coding session.

#208, #209, #210 and #211 — templates, the consent gate, the delivery engine and the link mechanism. All **open**. Until
#210 merges, nothing leaves the building and the code journey cannot be walked at all; record that as the blocker rather
than walking a fake.

#52 — **open**. If it merges first, automate what it can carry and record only the rest.

## Single-session files this slice touches

`docs/nfr/support-matrix.md` and `docs/nfr/a11y-checklist.md` only. Nothing under `src/`, nothing under
`clients/pwa/src/`, no generated document, no permission matrix, no migration chain.

Refs #48
