Sub-issue of #60. The parent's eighth implementation step — "run scheduled restore and full DR exercises; record
actual RPO/RTO, gaps and corrective issues" — carved out deliberately, because **no coding session can produce this
evidence.** A measured recovery point and recovery time are properties of a machine that does not exist yet
(`docs/dev/staging.md` section 12 item 1: "**The virtual machine itself.** No host has been provisioned … Owed by
OD-02, then #22 or #59"), at a data volume nothing in the repository has, performed by "an operator who did not
author the runbook" (`docs/process/release-gates.md` RG-13, and `docs/nfr/traceability.md` **NFR-BR-06**, whose Status
cell already reads "**Evidence only**; scheduled (#60)"). Everything mechanical is built by children [E15-F02-1] to
[E15-F02-6]; this child schedules the exercises, runs them, records what they measured and opens the corrective
issues. It is the last of #60's children to close and it is what allows the parent to close.

**Read this before picking this issue up — it is not a coding session and must not be assigned as one.** It is an
operations issue with a documentation deliverable: it needs a provisioned environment that is neither production nor a
hosted runner, real-volume data, a working alert receiver, and a human operator who did not author the runbook. None
of those is something a coding agent can provide, and three of them do not exist yet (**OD-02**, then #59 for the
host; #58 for the receiver; **OD-15** for the named operator and deputy). A session that cannot reach a provisioned
environment cannot complete this issue, and must not substitute a development-laptop run for the measured figures — a
four-gigabyte restore on a laptop says nothing about the four-hour objective, and recording it as if it did is worse
than recording nothing. What a session *can* honestly deliver with none of that, and what it should deliver if picked
up today, is the register, the record template, the cadence record and a row per exercise reading "not run — blocked
on OD-02 / #59", after which **the issue stays open** until the exercises are run.

## What is already built (do not re-build)

- Children **[E15-F02-1]** to **[E15-F02-4]** — the chain, the locked bucket, the drill with its
  `artifacts/backup/restore-*.json` record, the invariant queries and the freshness signals. **Run them; change
  nothing.** If an exercise finds a defect in one of them, the fix is a separate pull request against that child's
  code, linked from the exercise record — which is the point of recording gaps rather than repairing them silently.
- Children **[E15-F02-5]** and **[E15-F02-6]** — the ten runbooks, the index, the template,
  `roles-and-communications.md`, `disaster-dependencies.md` and `backup-access-review.md` with its record form. The
  exercises **follow** these documents literally; every place the document had to be corrected is a finding.
- `docs/nfr/slo.md` section 6 (the cadences: **weekly** automated restore, **monthly** point-in-time recovery,
  **monthly** missing-object recovery, **quarterly** full disaster recovery; and the rule that every exercise records
  actual RPO and RTO and "opens a `release-blocker` corrective issue when a target is missed") and section 6.1 (the
  stage decomposition the measurement is compared against, stage by stage).
- `docs/process/release-gates.md` **RG-13** (line 255) — what the gate checks, its 7-day freshness requirement
  (**proposed**, and **RG-OD-03** is open on whether it is achievable), that the **operations owner** may waive it for
  a staging-only release and **nobody** may waive it for a production release, and the evidence it wants: "the restore
  log, the measured recovery point and recovery time, the invariant query results, the backup age and completeness
  metrics, and the exercise record naming the operator".
- `docs/process/release-evidence.md` **RE-12**, at line 107 in that document's **section 5, "The evidence
  checklist"** — "The most recent restore verification record, with its measured recovery point and recovery time and
  its age in days", owned by the operations owner (and repeated in section 7's sign-off table at line 167). The record
  form this child files must be readable straight into that row, **including the age in days**, which means the
  register carries the date of every run and not only the latest.
- `docs/process/waivers.md` — **and its limits.** A waiver is for a gate that cannot be met, dated and owned. It is
  **not** the place to record that an exercise has not happened yet or that an owner has not been named: those are
  open decisions with defaults (**OD-02**, **OD-15**, **SLO-03**), recorded where decisions are recorded.
- `docs/prd/raci.md` row 28 and footnote (20) — backup verification is the Owner's accountability with execution
  delegated to the operator **OD-15** names, "evidenced in the issue #60 runbook"; the Auditor is `I`
  (`docs/prd/raci.md` line 57).
- `docs/nfr/risk-review.md` **RR-12** (line 109) — the weekly cadence needs "a standing staging machine, or a machine
  created and destroyed for each exercise. Either is a recurring cost", and **RR-02** (line 81) — with one operator and
  no out-of-hours cover the realistic recovery time is the following morning. Both are already owned risks; the
  exercise records are what turn them from predictions into measurements.
- `docs/nfr/traceability.md` rows **NFR-BR-01** … **NFR-BR-07** (lines 259-265) — the seven rows this child is the
  only issue that can add a measured result to. **Read all seven before editing one**: four say "Proof scheduled" or
  "Evidence only; scheduled", and three say "Proposed, to be confirmed" with an open-decision dependency. Only the
  first kind is about *evidence*; the second kind is about whether the **target** is agreed, and an exercise does not
  settle that. See the Scope bullet; this is the edit most likely to be made wrongly in this child.

## What is missing

- No exercise has been run, so every recovery figure in the repository is **proposed** and none is measured.
- Nothing schedules the cadence: `.github/workflows/` holds `ci.yml` and `pr-policy.yml` only, neither with a
  `schedule:` trigger for a restore, and there is no host to schedule one on.
- No `docs/runbooks/exercises/` directory and no filed record, so **RE-12** and **RG-13** have nothing to cite and
  RG-13 has never been exercised against a real run.
- No corrective-issue convention: `release-blocker` is named by `docs/nfr/slo.md` section 6 as a label and **does not
  exist in the repository** — the label list confirmed today is `bug`, `enhancement`, `documentation`,
  `architecture`, `ci`, `chore` and `release-ready`. Creating it is a **repository-settings change that no pull request
  can carry**, so it is an action recorded by this issue rather than a file in its diff. See Scope.

## Scope

- [ ] `docs/runbooks/exercises/README.md` — the register: one row per exercise with its date, type, operator role,
      environment, measured recovery point, measured recovery time, verdict, and links to the findings it opened. Plus
      the cadence table, citing `docs/nfr/slo.md` section 6 rather than restating it, and the rule that an exercise
      whose record is missing counts as not having happened.
- [ ] `docs/runbooks/exercises/_record-template.md` — the record form, shaped so it reads straight into **RE-12** and
      satisfies RG-13's evidence list: environment and data volume; the backup label and repository state before the
      run; the target timestamp; stage-by-stage timings **against the stages of `docs/nfr/slo.md` section 6.1**;
      measured recovery point and how it was derived; measured recovery time; invariant query results; the runbook
      followed and its version (a commit hash); the operator's **role** and whether they authored the runbook;
      findings, each with its issue number; and the verdict.
- [ ] **Run and file the four exercise types**, each as its own record:
      (a) the **weekly automated restore** — `infra/backup/restore-drill.sh` on the provisioned environment, restoring
      to one hour before now, with `migrate --dry-run`, the invariant queries and the measured figures;
      (b) a **point-in-time recovery** to a chosen timestamp, following
      `docs/runbooks/point-in-time-recovery.md`;
      (c) a **missing-object recovery** from bucket versioning, following `docs/runbooks/lost-object.md`;
      (d) the **full disaster-recovery exercise**, following `docs/runbooks/site-failure.md` and
      `docs/runbooks/roles-and-communications.md`, **executed by an operator who did not write the runbook** — the
      criterion the parent states and the only one that cannot be satisfied by the author of any earlier child.
- [ ] **The alert test** `docs/nfr/traceability.md` **NFR-OP-04** requires ("Monitor: backup age metric with an alert;
      Evidence: a recorded alert test"): stop the scheduler, confirm the backup-age and WAL-freshness signals degrade
      as child [E15-F02-4] emits them, confirm the alert fires and reaches its receiver, and record it. The alert and
      the receiver are **#58**'s; this is the test that they work, and it is recorded here because the parent's
      fourth implementation step asks for it ("monitor backup age/completeness/failures **and test alerting**").
- [ ] **The first backup access and immutability review**, following
      `docs/runbooks/backup-access-review.md`: the dated list of every human and machine identity that
      `docs/nfr/security-operations-targets.md` line 181 requires, the immutability re-checks, and the escrow
      retrieval check by someone other than the key's author.
- [ ] **The corrective-issue mechanism.** `release-blocker` **does not exist today** — confirmed against the
      repository's label list, which holds `bug`, `enhancement`, `documentation`, `architecture`, `ci`, `chore` and
      `release-ready`. Creating it is a **repository-settings change, and no pull request can carry one**: a label
      lives in GitHub's settings, not in the tree, so this issue's pull request cannot create it and must not pretend
      to. What the pull request carries is the *convention*; the label is created by whoever holds repository
      administration, and `docs/runbooks/exercises/README.md` records that it was created, by which role and on what
      date, so a reader can tell a missing label from an unrecorded one. If the label does not exist when an exercise
      misses a target, the corrective issue is still filed and the register says the label is owed — an unlabelled
      blocker is better than an unfiled one. State the rule in `docs/runbooks/exercises/README.md`: a missed target
      opens a
      `release-blocker` issue linked to the exercise record, with a named owner and a date. `docs/nfr/slo.md`
      section 10 clause 3 already requires that shape ("corrective actions are raised as `release-blocker` issues with
      a named owner and a date, **exactly as a missed recovery target is under #60**") and clause 4 gives the closing
      condition ("merged or explicitly re-scoped by the Owner"). Every finding from every exercise is filed, including
      the ones that are only documentation corrections.
- [ ] **Register the cadence** so it runs without a person remembering: a `schedule:`-triggered workflow is **not**
      the mechanism for the restore itself (RG-13 and `docs/nfr/slo.md` section 6 both forbid a hosted runner), so the
      schedule lives on the provisioned host — a timer unit or cron on the environment #59 provisions — and the
      **reminder** may live in GitHub. Whichever is used, `docs/runbooks/exercises/README.md` records where the
      schedule lives, who is paged when a run is missed, and how a missed run is detected (the `S14` restore-proof
      signal of `docs/nfr/slo.md` line 142 and the freshness signals of child [E15-F02-4]).
- [ ] **Update the measured rows**: `docs/nfr/traceability.md` **NFR-BR-01** … **NFR-BR-07** (lines 259-265),
      **appending** the measured outcome and the record's path to each Status cell. **Change no target, and do not
      replace what the cell already says.** This is narrower than it sounds and an earlier draft of this issue got it
      wrong by saying the cells should stop "saying 'proof scheduled'". Only three of the seven say that: NFR-BR-04
      ("Proof scheduled (#60) — see **RR-12**"), NFR-BR-05 and NFR-BR-07 ("Proof scheduled (#60)"), and NFR-BR-06
      ("Evidence only; scheduled (#60)"). The other three — NFR-BR-01, NFR-BR-02 and NFR-BR-03 — read "Proposed, to be
      confirmed" with an open-decision dependency (`depends on **OD-02** — see **RR-03**`; `see **RR-02**`; `depends on
      **OD-05** and **OD-08**`). Those words are about whether the **target** is agreed, which an exercise record does
      not settle and **SLO-03** still owns. Deleting them would silently close an open decision on the Owner's behalf,
      which is the one edit this child must never make. Append; never replace.
      If a measurement misses a target, the target is still not edited: a `release-blocker` issue is opened and
      `docs/nfr/slo.md` section 13's compatibility statement is what the Owner signs or declines. Lowering a target to
      match a measurement is a product decision, not a housekeeping edit.
- [ ] One line in `docs/process/release-evidence.md` naming `docs/runbooks/exercises/` as where the record is found —
      a narrow, additive edit so the gate can find its evidence. **The RE-12 row is at line 107, in that document's
      section 5, "The evidence checklist"** — not section 3, which is "Release identification" and holds the version,
      tag and date. An earlier draft of this issue named section 3; editing there would put the pointer where nobody
      reading the checklist would see it. RE-12 also appears in section 7's sign-off table at line 167 against the
      operations owner; leave that row alone.

Versioned configuration, not code (DOR-11): the records and the register are documentation; the schedule is
configuration on the provisioned host and is recorded, not committed, because it is environment state.

## Out of scope

- Every mechanism: children **[E15-F02-1]** to **[E15-F02-6]**. This child runs them and records what happened. A
  defect found becomes a separate pull request against the owning child, linked from the record.
- The alert rules, receivers and dashboards: **#58**. This child tests the delivery path and records it.
- Provisioning the environment the exercises run on: **#59** (and **OD-02** before it). This child cannot substitute
  a laptop.
- The **release dress rehearsal** — signature and provenance, clean provisioning, an induced migration failure,
  rollback, restore, the regression suite twice, and the completed release-evidence form: **#61**'s child
  **[E15-F03-7]**, which names #60's runbooks as an input and is a different exercise with a different record. Do not
  write a release procedure here, and do not file its record here.
- The user-acceptance and go-live records: **#61c**.
- Any change to a target, a cadence, a retention period or a threshold: those are **OD-02**, **OD-05**, **OD-08**,
  **SLO-03**, **SLO-04** and **RG-OD-03**, and they are the Owner's.

## Acceptance criteria

- [ ] Four exercise records exist under `docs/runbooks/exercises/`, one per type, each complete against
      `_record-template.md` with no field left blank and no field reading "n/a" without a reason.
- [ ] The disaster-recovery record names an operator **role** who did not author the runbook, and records at least one
      place where the runbook was unclear or wrong — or states explicitly that none was found, which a reviewer may
      disbelieve and ask about. The parent's fourth acceptance criterion is satisfied by this record or not at all.
- [ ] Each record carries a **measured** recovery point and recovery time, derived as child [E15-F02-3]'s record
      defines them, with the stage timings of `docs/nfr/slo.md` section 6.1 beside the published estimates, and the
      data volume the run was performed at — because a figure without a volume is not a measurement.
- [ ] The register lists every exercise, and `docs/nfr/traceability.md` **NFR-BR-01** … **NFR-BR-07** cite the records
      rather than saying "proof scheduled".
- [ ] **Negative — a missed target does not quietly pass:** at least one target is compared and, where it is missed, a
      `release-blocker` issue exists, is linked from the record and from the register, and the record's verdict says
      the gate would have blocked. If every target is met, the record still shows the comparison and the margin.
- [ ] **Negative — an exercise that cannot be run is recorded as not run:** any exercise type the environment cannot
      support has a register row saying so, naming the blocking issue (**#59**) or decision (**OD-02**), with no
      measured figure invented and no waiver written — because `docs/process/waivers.md` is for a gate that cannot be
      met, not for work that has not happened.
- [ ] **Negative — the alert test proves delivery, not configuration:** the record shows the signal degrading, the
      alert firing **and** reaching its receiver, with the time from cause to notification. An alert that fired into
      an empty room is recorded as a failure of the test, not a pass.
- [ ] **Exception — a finding in a mechanism is filed, not fixed in place:** every defect the exercises surface in
      children [E15-F02-1] to [E15-F02-6] has its own issue, linked from the record, and **this pull request changes
      no script and no runbook step** beyond the register, the records and the three narrow edits in Scope. An
      exercise that repairs what it is testing has tested nothing.
- [ ] `python3 scripts/check-docs-links.py` and `./scripts/dev docs` pass; British spelling; prose wrapped at 120
      columns.

## Data and classification

The records are the sensitive part of this child: an exercise touches production-derived data and its record is
committed.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Exercise records and the register | Internal, **evidentiary** | Satisfy RG-13 and **RE-12**, and turn proposed recovery figures into measured ones | With the repository; **RE-12** reads the most recent and its age in days | Operations owner, Owner, technical reviewer, Auditor (`I` per `docs/prd/raci.md` line 57) | Committed. Timings, counts, labels, WAL positions, verdicts and **role** names only |
| What a record must **never** contain | Personal, Sensitive Personal, Financial, Credentials and secrets — `docs/nfr/data-classification.md` sections 2 and 6 | — | — | — | No customer name, telephone number or measurement; no invoice number or amount; no object key; no credential, passphrase, endpoint or bucket name; no person's name where a role will do (**OD-15** is open). A failing invariant is recorded as "n rows failed `invoice-sequence.sql`", and the rows themselves stay in the operator's terminal |
| The restored environment each exercise creates | **The union of every class** — section 5.20 | The exercise itself | Destroyed at the end of every exercise, and the record states that it was and how that was verified | The operator performing it | Never reachable from outside its own network; never seeded into another environment; never left standing "for investigation" without an Owner decision recorded in the record |
| The data volume and row counts recorded | Internal | Make the measurement meaningful | With the record | As above | Aggregate counts per schema are Internal and are what make the figures comparable between exercises. No business identifier accompanies them |
| The alert test's notification | Internal | Prove the delivery path | With the record | As above | The **channel** and the **time to notify** are recorded; the receiver's address, number or token is not (**OD-15** will name the channel; the address stays in the secret store) |
| The access-review list | Internal, evidentiary | `docs/nfr/security-operations-targets.md` line 181's "dated list naming every human and machine identity" | With the record, reviewed quarterly | Operations owner, Owner, Auditor | Identity names and what each may do. Never a key, a password or a retention-bypass capability |

## Permissions and roles

No application permission key is added, used or changed, and nothing under `src/` is touched. The operational
authorisations are cited, not invented: `docs/prd/raci.md` row 28 with footnote (20) (execution by the operator
**OD-15** names, accountability with the Owner, Auditor informed); `docs/nfr/slo.md` section 8 (a named operator and a
deputy who has completed one restore exercise — and this child's exercise **is** how a deputy completes one, which is
worth recording in the register); `docs/process/release-gates.md` section 2.2 and RG-13 (the operations owner may
waive for a staging-only release, nobody may for production). The permissions an exercise uses in passing are
unchanged: `admin.health.read` to read backup age through #58's endpoint, and `admin.audit.read` for the audit-chain
verification an exercise may perform
(`src/Platform/Tailor360.Platform.Security/Permissions/PlatformPermissions.cs:50` and `:41`). **OD-15** is open, so
every record names a role and, where a person must be identified for accountability, the record says which role they
held — not the other way round.

## Modules, contracts and events

No module, project, schema, contract or event is touched, and no script or runbook step is edited. The only files
changed are under `docs/runbooks/exercises/`, plus the Status cells of `docs/nfr/traceability.md` and one additive line
in `docs/process/release-evidence.md`. There is no test tier for this child and the pull request says so plainly: its
verification is the records themselves, reviewed by the operations owner and the technical reviewer.

Threat-model coverage (DOR-05): the deployment flow's model is owed by **#56a** in wave W2
(`docs/security/README.md` section 3.2). This child's findings are empirical input to it — the gap between a control
as designed and a control as exercised is exactly what a threat model cannot predict — and the disaster-recovery
record is where the residual risks **RR-02** (one operator, no out-of-hours cover) and **RR-12** (the restore
environment's cost and reliability) stop being estimates.

## Screens and states

Not applicable — no client surface. An exercise is performed at a terminal and on a telephone; the records are
markdown.

## Migration and rollback

No schema change and no migration: documentation only. `migrate --dry-run` printing "The database is up to date with
this build." is the proof, and CI's `migration-check` job confirms no model change. Note that the **exercises
themselves** apply migrations inside a restored environment — that is the point of the `migrate --dry-run` step — and
nothing they do reaches production; the record states which environment each command ran in, for every command.

Rollback is a revert of the records, which is **not** a normal operation: an exercise record is evidence, and evidence
is corrected by a superseding record that says what it corrects, not by a quiet revert. The register's rules say so.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-02** Hosting model and indicative monthly budget | **Open** — `docs/prd/assumptions-and-open-decisions.md` line 55; needed **before the W0 exit gate** | **This is the blocking decision for this child.** Until it is taken and #59 provisions an environment, the exercises have nowhere to run that is neither production nor a hosted runner. The default in force is to file the register, the template and the cadence, and to record each exercise as "not run — blocked on OD-02 / #59", with no invented figure | W0 exit gate |
| **SLO-03** Accept the Model A conditions and the pre-agreed replacement host | **Open** — `docs/nfr/slo.md` line 426; **RR-02** | The records report the measured figures against the **proposed** targets and say the compatibility statement of section 13 is unsigned. A missed target opens a `release-blocker` issue; it never edits a target | W5 exit gate |
| **RG-OD-03** Whether RG-13's 7-day restore freshness is achievable with the chosen model | **Open** — `docs/process/release-gates.md` line 360 | The register records each run's age in days, which is the data the decision needs. Nothing here relaxes the freshness rule | W5 exit gate |
| **OD-15** Operations ownership, alert channel and out-of-hours arrangement | **Open** — line 77 | Records name roles. The alert test records the channel used and notes that the receiver is provisional | W5 |
| **OD-14** Telemetry backend | **Open** — line 76 | The alert test is performed against whatever receiver #58 has configured, and the record says which | W5 |
| **OD-08** / **OD-05** Retention periods and GST record retention | **Open** — lines 61 and 58 | The retention review that `docs/IMPLEMENTATION_PLAN.md` line 2449 requires in the quarterly record is performed against the **proposed** figures and records that they are proposed | W1 / W3 |

## Required verification

Environment (DOR-16): **this child needs a provisioned environment that is neither production nor a hosted runner** —
`docs/nfr/slo.md` section 6 and RG-13 both forbid the alternatives, and `docs/dev/staging.md` section 12 item 1 says
no host exists. `./scripts/dev doctor` is not sufficient evidence of readiness for this child; the readiness check is
"does an environment exist, provisioned by #59, with a copy of a real-volume backup". If it does not, the only
honest completion is the register, the template, the cadence record and rows saying each exercise is blocked — and the
issue stays open until the exercises are run.

- [ ] **Operational, recorded**: the weekly automated restore, with its `restore-*.json` record and the measured
      figures.
- [ ] **Operational, recorded**: the point-in-time recovery to a chosen timestamp, including what was lost between the
      target and the present and how that was communicated.
- [ ] **Operational, recorded**: the missing-object recovery, with the checksum comparison.
- [ ] **Operational, recorded, by a non-author**: the full disaster-recovery exercise, with stage timings against
      `docs/nfr/slo.md` section 6.1 and the runbook corrections it produced.
- [ ] **Operational, recorded**: the alert test — signal degraded, alert fired, receiver reached, time to notify.
- [ ] **Operational, recorded**: the backup access and immutability review, with the dated identity list, the
      immutability re-checks and the escrow retrieval by a non-author.
- [ ] **Documentation**: `python3 scripts/check-docs-links.py --self-test` then the full run, and `./scripts/dev
      docs`.
- [ ] Non-functional rows this child closes (DOR-13): `docs/nfr/traceability.md` **NFR-BR-01**, **NFR-BR-02**,
      **NFR-BR-03**, **NFR-BR-04**, **NFR-BR-05**, **NFR-BR-06**, **NFR-BR-07** and **NFR-OP-04** — the eight rows
      whose evidence is an exercise record. Status cells only; no target changes.

## Evidence the pull request will carry

- [ ] The four exercise records and the register, rendered.
- [ ] The restore logs and `restore-*.json` records behind them, and the invariant query results.
- [ ] The backup age and completeness metrics at the time of each exercise, as RG-13's evidence list requires.
- [ ] The alert-test transcript including the notification's arrival time.
- [ ] The dated access-review list and the immutability re-check output.
- [ ] Every `release-blocker` issue the exercises opened, linked from the register, with the target each one missed.
- [ ] The runbook corrections the non-author exercise produced, each as a linked issue or a linked pull request
      against the owning child — **not** as edits in this pull request.
- [ ] `migrate --dry-run` output showing no schema change, and the link-check output.

## Depends on

- #`<E15-F02-3>` — the drill, the invariant queries and the record shape the measurements use.
- #`<E15-F02-4>` — the freshness signals the alert test degrades.
- #`<E15-F02-5>` — `restore.md`, `point-in-time-recovery.md` and `lost-object.md`, followed literally by exercises
  (a), (b) and (c).
- #`<E15-F02-6>` — `site-failure.md`, `roles-and-communications.md` and `backup-access-review.md`, followed by
  exercise (d) and the review.
- **#59** (open) — the provisioned environment and the promoted images. Without it there is nowhere an exercise may
  legitimately run, and this child's register records that rather than working around it.
- **#58** (open) — the alert rules and the receiver the alert test exercises.
- **OD-02** (open) — the hosting decision #59 waits on. Named as a dependency because it is one.

Also named: **#61**'s child [E15-F03-7] consumes this child's most recent record for its release-evidence form, and
must not be confused with it. Shared single-writer files: `docs/nfr/traceability.md`,
`docs/process/release-evidence.md` and `docs/runbooks/exercises/` (this child alone).

Refs #60
