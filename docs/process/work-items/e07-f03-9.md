Sub-issue of #37. The parent's third required verification is not a test: "**Physical end-to-end rehearsal** from
measurement intake through dispatch", with real labels, real devices and a real printer, and plan Section 8 (#37) adds
"recorded walkthrough of each exception type through to a compensating event" and a rehearsal "from intake scan to
ready-for-delivery and delivery-team receipt, with dispatch through the exception path". That is evidence produced by
people with garments in their hands, it cannot be produced by any of the eight slices that build the software, and it
is named by **two** rows of the plan, both of which this pull request ticks: section **6.3** line 676 ("End-to-end
garment lifecycle with real labels and devices | #37 (custody rehearsal), #48 (dispatch leg),
#61c (pilot)") and section **6.4** line 743, the E07 epic exit criterion ("Real labels and devices complete the
physical workflow | #37 rehearsal, #48 dispatch leg, #61c pilot"). Cite both; they are different tables. So it is
carved out here, with the two pieces of repeatable scaffolding the rehearsal needs: a seeded custody scenario and a
replay test nobody else owns.

## What is already built (do not re-build)

- **E07-F03-1, -2, -3, -3b, -4, -5, -6, -7, -8 and -8b** — every command, read, worker job and screen the rehearsal
  exercises. This slice adds **no endpoint, no table and no screen**.
- `src/Tools/Tailor360.Cli/Commands/SeedSyntheticCommand.cs` (156 lines) — repeatable, upsert-based, refuses in
  Production through `EnvironmentGuard.cs`, and today seeds **only** two branches and two feature flags. Its own
  closing lines say so: "Customers, catalogue, orders and billing fixtures are added by **their own issues** and
  consolidated into the shared fixture library by issue #61a." That is the invitation this slice accepts for
  custody.
- `docs/nfr/a11y-checklist.md` section 3.7's fixture table — the two rows that name what custody owes a runner:
  "**Five distinct scan rejections**… a printed synthetic label sheet carrying one pre-made payload per rule —
  wrong namespace, bad check character, unknown identity, wrong branch, wrong custodian. The check-character
  payload is **generated by the seed tool, never hand-crafted**", and "**A named synthetic seed set**… a custody
  timeline carrying a **correction event**".
- `docs/prd/exceptions.md` — EX-07 (damaged label), **EX-10** (unpaid dispatch attempt), EX-12 (failed delivery),
  **EX-13** (custody mismatch or lost garment), EX-15 (expired exception), each with its "Evidence" column naming
  what a walkthrough must leave behind.
- `docs/nfr/capacity-and-performance.md` — line 186 "a scan round-trip p95 under 1 second on 4G", line 200-201 the
  separate WebKit target, and **scenario S3** at line 317: "20 devices each replaying 200 queued scans within 60
  seconds, all with idempotency keys and client event UUIDs | No duplicate custody events; the `scan-burst`
  rate-limit policy admits the replay". **`tests/load` does not exist**, so S3 as a load scenario is not runnable
  here; its *shape* is.
- `docs/nfr/a11y-checklist.md` sections 2.3 and 6.4 — the device pairing and the ten steps of `A11Y-PZ-02`, whose
  screen-reader halves E07-F03-7 and E07-F03-8 already record. This slice records the **physical** half.
- `tests/Tailor360.IntegrationTests/WebApplicationFixture.cs` and `DatabaseAvailability.cs` — the fixture every
  integration test already uses, and the skip-unless-`CI=true` rule `CLAUDE.md` section 5 states.
- **What does not exist, and must not be assumed**: `docs/evidence/` (plan line 713 promises it for each epic's
  closing pull request, which is not this one), `tests/e2e`, `playwright.config`, `tests/load`. The rehearsal record
  therefore goes in a module-area document, following the `docs/billing/go-live-plan.md` precedent, and no
  Playwright or load test is written here (#52 brings the first, #51 and #61b the rest).
- **`docs/custody/` may already exist.** #35's **E07-F01-8** creates `docs/custody/labels.md` and
  `docs/custody/label-tests/`; `ls docs/custody` fails today. So create the directory only if it is still missing,
  and add the rehearsal document beside whatever is there rather than declaring the directory new.
- **E07-F01-9** — #35's own hardware-evidence child ("Printed label evidence: printer, device and durability
  records"), which exists for exactly the reason this one does: "an issue that is half code and half hardware
  strands whoever picks it up at seventy per cent". Follow its shape, and reuse its printer and label-stock records
  rather than repeating them.

## Scope

- [ ] **A custody scenario in `seed-synthetic`** — added to `SeedSyntheticCommand` beside the existing branches
      and flags, repeatable and upsert-based, refusing in Production as the command already does: one confirmed
      order with three garment jobs at the main branch and one at the second branch, their barcode identities
      (through #190's allocator), a custody chain for each job, **one job with a `mismatch` case resolved by a
      `CORRECTION` event** (the a11y fixture row's "custody timeline carrying a correction event"), and **one
      pending cross-branch transfer** between the two seeded branches so the E07-F03-4 grant is walkable by hand.
      Plus the two **state-shaped** rejection fixtures a printed sheet cannot carry: an identity allocated at the
      **second** branch, and a job whose recorded custodian is a different seeded user — which are what make "wrong
      branch" and "wrong custodian" refusable at all. Printed output names every identifier a runner needs and the
      refusal each fixture is there to provoke.
- [ ] **The five rejection payloads: reuse #35's command, and seed only the two it cannot produce.** The fixture
      row at `docs/nfr/a11y-checklist.md` line 327 is owned by "Technical reviewer, with **#35 and #36**", not by
      #37, and **#35's E07-F01-8 already owns the command**: `dotnet run --project src/Tools/Tailor360.Cli --
      label-sheet … [--payloads <file>]`, whose scope includes "a sheet of the deliberately invalid payloads of
      `docs/nfr/a11y-checklist.md` section 3.7 — a bad check character, a wrong namespace (`S-`, `I-`, `R-`), a
      malformed string, confusable characters (`I`/`L`/`O`), and a well-formed payload that is allocated to
      nothing". **Do not build a second payload generator here.** Three of the five rejections are shapes of a
      payload and come off that sheet. The other two — **wrong branch** and **wrong custodian** — are not payload
      properties at all: they are *states*, an identity allocated at the second branch and a job whose recorded
      custodian is somebody else, and they are what the seeded custody scenario above must therefore contain. The
      seed command's printed output names the identifier and the expected refusal for each. If E07-F01-8 has not
      merged, record the three payload rejections as **not run, blocked on #35**, rather than hand-crafting a check
      character — the checksum is a Damm-style character over Crockford base32 and "cannot be guessed at a desk".
- [ ] **`tests/Tailor360.IntegrationTests/Custody/ScanReplayConcurrencyTests.cs`** — the replay suite no other
      slice owns, in the **shape** of scenario S3 and with no invented number: N queued submissions per actor
      replayed in order with their original `Idempotency-Key` and `clientEventUuid`; the same batch replayed
      twice; two actors replaying the same UUID; and a batch interleaved with a conflicting live scan. Asserts
      **no duplicate custody event**, the first outcome replayed, `409 custody.event-conflict` for the
      foreign-actor case, and that a conflict **pauses** the run rather than skipping an entry (the client rule of
      `docs/architecture/sequences/barcode-handoff.md` section 6: "replay pauses on the first `409` or `422`").
      The batch size is a constant in the test with a comment that S3's 200 is a **load** figure owned by #51 and
      #61b, not a target asserted here.
- [ ] **`docs/custody/custody-rehearsal.md`** — the rehearsal script **and** its completed record, in one
      document, on the model of `docs/billing/go-live-plan.md`: the kit (devices from the section 2.3 pairing, a
      configured keyboard-wedge scanner, the printed label sheet, a branch print station), the seeded scenario,
      then a numbered script from **measurement intake through to the delivery-team leg**, and a results table
      with one row per step carrying the device, the outcome, the observed round trip and the defect raised. Every
      defect found becomes its own issue linked to the epic (`CLAUDE.md` section 6, scope discipline), and the
      record links them.
- [ ] **The six exception walkthroughs, physically** — one row per EX-13 case type (`mismatch`, `duplicate`,
      `stale`, `unknown location`, `lost`, `disputed`) taken from the refusal on the device through to the
      compensating `CORRECTION` event and its second approver, plus **EX-07** (a deliberately damaged label
      resolved by job number and a reprint). The server-side walkthroughs are E07-F03-3's evidence; these are the
      same paths with a garment, a phone and a printer, and the record says which device each was run on.
- [ ] **The dispatch leg, honestly bounded.** #37 cannot rehearse a paid dispatch: the dispatch authorisation, the
      dispatch scan and the doorstep confirmation are #48's (see E07-F03-1's **Out of scope**). The script
      therefore ends at the **delivery-team leg as far as this release reaches** and records, with the reason and
      the issue number, that the paid and exception dispatch legs are rehearsed in #43 and #48 — which is exactly
      what plan Section 6.2 note 2 says ("The W3 rehearsal records dispatch through the exception path; paid
      dispatch is rehearsed again in #43 and #48"). If #48's receive and dispatch routes have merged by the time
      the rehearsal is run, the script includes them and says so; if not, the two steps are recorded as **not run,
      with the blocking issue named** — never as passed.
- [ ] **A row in `docs/nfr/traceability.md`** for the scan round-trip observation, pointing at the record rather
      than restating a figure, and **both** plan rows ticked for the custody item in the same pull request: section
      6.3 line 676 and section 6.4 line 743.
- [ ] **Three plan corrections this decomposition owes, in this pull request, because no other child edits the
      plan.** Each is a one-line amendment with its reason, and each exists because leaving it would read as a
      dropped acceptance criterion rather than a moved one (`CLAUDE.md` section 8: change the document, do not work
      around it).
      **(a)** Section 8 (#37)'s **Tests** bullet assigns "dispatch scan rejected with the default implementation,
      accepted with a fake returning `Paid`, accepted with a fake returning `ApprovedException`" to #37, and its
      **Dispatch-eligibility contract** bullet says the contract is "introduced here". Both are superseded: #164
      (closed) merged `Billing.Contracts.Payments.IDispatchEligibilityQuery` with `ConsumeExceptionAsync`, and #48's
      own children own `POST /custody/deliveries/receive`, `POST /custody/dispatch` and
      `custody.dispatch_authorisations`. Amend both bullets, and Section 6.2 **note 2**, to name #48 — and note that
      **#37's fifth acceptance criterion ("Dispatch scan requires passed QC, delivery-team custody and satisfied
      payment rule") is therefore closed by #48, not by #37**. #37's contribution is E07-F03-1's guard table
      refusing `DeliveryTeamReceive`, `Dispatch` and `DeliveryConfirmed` with `custody.action-not-available`, the
      `scan_events`/`custody_transfers` spine, and E07-F03-5's real `ICustodyStateQuery`, which is how
      "delivery-team custody" is answerable at all. Section 6.4 line 758 ("E11 | Unpaid/failed-QC cannot dispatch
      without authorized override | #37, #43, #48") gets the same treatment.
      **(b)** Section 8 (#37)'s **Screens** bullet ends "registers a timeline source". E07-F03-5 records why it
      cannot: `ITimelineSource` is asked about a `CustomerId` and `Orders.Contracts` publishes no by-customer
      garment-job read. Amend the bullet to point at the new issue E07-F03-5 raises against epic #8, and say the
      same about `docs/architecture/module-ownership.md` section 5.6's "an `ITimelineSource` implementation".
      **(c)** Section 8 (#37)'s model bullet and `docs/architecture/module-ownership.md` section 5.6 must agree on
      `job_custody_state`, which E07-F03-1 adds to 5.6; check they do and correct whichever lags.

## Out of scope

Any change to a command, an endpoint, a screen or a migration — if the rehearsal finds a defect, it becomes an issue
and a separate pull request, because a rehearsal that edits the thing it is rehearsing proves nothing. The persisted
offline queue and step 10 of `A11Y-PZ-02` ("Repeat step 7 offline") — #51 owns the queue, so that step is recorded as
**not run, blocked on #51**. The Playwright suite (#52), the load suite (#61b), the pilot (#61c). The paid dispatch
and doorstep legs (#43, #48). The label test sheet itself (#35's E07-F01-8) and the scanner's own device matrix
evidence (#36) — both are reused and neither is rebuilt.

## Acceptance criteria

- [ ] `dotnet run --project src/Tools/Tailor360.Cli -- seed-synthetic` run **twice** leaves the same custody
      scenario — the same four jobs, the same resolved case, the same one pending cross-branch transfer — and
      refuses under `ASPNETCORE_ENVIRONMENT=Production`, exactly as the command already does for branches and
      flags.
- [ ] The five rejection payloads the command emits are each **refused by the server for the stated reason** —
      wrong namespace, bad check character, unknown identity, wrong branch, wrong custodian — proved by an
      integration test over the real endpoints, so the printed sheet cannot drift from the rules it is meant to
      demonstrate.
- [ ] **Negative — no duplicate events under replay.** The replay suite shows one custody event per `(actor,
      clientEventUuid)` across a doubled batch, the original outcome replayed for each repeat, and `409
      custody.event-conflict` when a second actor presents a seen UUID.
- [ ] **Negative — a conflict stops the run.** An interleaved conflicting scan makes the replay **pause** at the
      first refusal with the server's problem details, and the entries after it are **not** silently skipped.
- [ ] The completed record in `docs/custody/custody-rehearsal.md` carries, for every step: the device, the person,
      the date, the outcome, the observed round trip, and — for each step not run — the issue blocking it. A step
      with no device named is not evidence.
- [ ] All seven exception walkthroughs (six EX-13 types plus EX-07) appear in the record with the compensating
      event identifier each one produced, and **no history was edited to produce any of them** — proved by the
      append-only triggers still refusing an `UPDATE` after the rehearsal. The server-side versions of the same six
      are E07-F03-3's (opening) and E07-F03-3b's (repair), and the record says which it is repeating on hardware and
      which it is referencing.
- [ ] The pull request states plainly which of #37's five acceptance criteria this evidence closes and which are
      closed elsewhere, so a reviewer does not have to infer it.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Seeded custody scenario rows | **Internal** (`docs/nfr/data-classification.md` section 5.8) | Give a rehearsal something to scan | Development and staging only; destroyed by `./scripts/dev reset` | Whoever holds the environment | **Synthetic only.** No real customer name, phone number or measurement, in the seed or in the record (`CLAUDE.md` section 5 and plan Section 2.2) |
| The five rejection payloads | **Internal**, opaque (plan D9) | Demonstrate each refusal rule on a real device | Printed sheets, kept with the record | The runner | Safe in logs and on paper by construction: a payload carries no personal data and no display number |
| The rehearsal record | **Internal** | Evidence for the W3 exit gate and the Definition of Done | With the repository | Any contributor | Device models, role names and job numbers only. **No photograph that shows a person's face, a customer's garment label with a name on it, or a screen with contact details** |
| Observed round-trip timings | Internal | Compare against the published budget | With the record | Any contributor | Figures belong to the record; the **target** stays in `docs/nfr/capacity-and-performance.md` and is cited, never restated |
| Defects raised from the rehearsal | Internal | Fix what the rehearsal found | Issue tracker | Any contributor | A defect body carries identifiers and reason codes, never a customer's details |

## Permissions and roles

No permission is added, and none is changed. The rehearsal is run **as the seeded representative users of each role**
(`docs/security/role-walkthrough.md`, which `SeedSyntheticCommand` already points at): Reception for intake, Tailor
Master for the transfer out, Tailor for the receive and the phases, Branch Manager for the correction, Owner for the
approval, Delivery Staff for the delivery-team leg. The record names which role ran each step, which is what turns it
into evidence that the **seeded grants** work rather than that an administrator can do everything. **OD-13** is open
under a documented default, and a step a seeded role cannot perform is recorded as a finding for that decision, not
worked around by granting a permission for the afternoon.

## Modules, contracts and events

No module source changes except `src/Tools/Tailor360.Cli` (the seed scenario), which is a host-class project and
references modules through their registration extensions exactly as it does today (ARCH-006, ARCH-020 — the
command-line host is one of the two that may use `IWorkerScopeFactory`). No schema, no contract, no event. DOR-05: the
rehearsal is where the threat model meets reality — it is the one place the "forged or replayed scan" and "accept a
garment I was never sent" abuse cases of the #56a set are attempted by hand, and the record says what happened.

## Screens and states

Deleted: no screen is built. Screens are **used**, and the record names which and on what device.

## Migration and rollback

Deleted: no schema change. `dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` reporting no pending
change is the evidence. Rollback is reverting the seed scenario and the document; neither affects a deployed database
except through `seed-synthetic`, which refuses in Production.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-07** the device, browser and printer matrix | **Open** (`docs/prd/assumptions-and-open-decisions.md` line 60), "needed before W0 exit" | The rehearsal uses the pairing `docs/nfr/a11y-checklist.md` section 2.3 already names and the printer #35's label evidence used; the record lists exactly what was used, so a later matrix decision can be compared against it rather than guessed at | W0 exit |
| **OD-04** the dispatch payment rule | **Open** (line 57), "needed before W4" | The paid dispatch leg is **not rehearsed here**; the record says so and names #43 and #48, per plan Section 6.2 note 2 | W4 |
| **CP-04** ten scans per garment job, and **CP-09** the separate WebKit budget | **Open** (`docs/nfr/capacity-and-performance.md` lines 383, 388) | Observations are recorded against the published p95 targets as they stand; no new target is asserted and no figure is restated | Before W3 |
| **FBH-03** the acceptance SLA | **Open, proposed** | The seeded scenario leaves the SLA unconfigured, so the pending cross-branch transfer does not expire during the rehearsal | OD-06 |
| Step 10 of `A11Y-PZ-02` (offline replay) | Not a decision — a sequencing fact | Recorded as **not run, blocked on #51**, which owns the persisted queue | #51 |

## Required verification

- [ ] integration: the replay and concurrency suite above; the five rejection payloads refused for the five stated
      reasons; `seed-synthetic` run twice being idempotent and refusing in Production.
- [ ] migration: the append-only triggers still refuse an `UPDATE` and a `DELETE` after the rehearsal's traffic,
      re-run against the rehearsal database.
- [ ] physical rehearsal: the script in `docs/custody/custody-rehearsal.md`, run on the section 2.3 device pairing
      with a wedge scanner, a printed label sheet and a branch print station, with the results table completed.
- [ ] accessibility: no new screen, so no new axe or overflow run; the manual `A11Y-PZ-02` records from E07-F03-7
      and E07-F03-8 are **referenced**, not repeated, and the physical steps 1, 2, 3 and 9 are recorded here
      because they need the hardware.
- [ ] DOR-13: the `docs/nfr/traceability.md` rows for the scan round-trip budget and for the custody exit criteria
      of plan section 6.4 are updated to point at the record.
- [ ] DOR-16: `./scripts/dev doctor`, plus the named staging environment and the hardware kit listed in the record
      — this is the one child of #37 that cannot be completed on a laptop alone.

## Evidence the pull request will carry

- [ ] The completed rehearsal record, with one row per step: device, role, outcome, observed round trip, defect.
- [ ] Photographs of the printed label sheet and of a successful scan on each device class, **showing no personal
      data**.
- [ ] The replay-suite output, and the `seed-synthetic` output from two consecutive runs plus the Production
      refusal.
- [ ] The seven exception walkthroughs with their compensating event identifiers, and the post-rehearsal trigger
      refusals proving no history was edited.
- [ ] The list of issues raised by the rehearsal, each linked to epic #8, and a one-line statement of which of
      #37's acceptance criteria this pull request closes.

## Depends on

#37 E07-F03-1, -2, -3, -3b, -4, -5, -6, -7, -8 and **-8b** — all ten. This slice is the last child of #37 and
verifies the others; it cannot start before E07-F03-8b is merged, because the exception walkthroughs are run through
the case screens and the correction is recorded there.

#190, #191, #194, #197 — `[E07-F01-1..4]`: identities, rendered and queued labels, resolve and by-number lookup, and
reprint. Open. **Without printed labels there is no physical rehearsal**, which is why #35 is #37's own declared
dependency in plan Section 8 row #37.

#36 — `[E07-F02]`, the camera, wedge and manual-entry scanner. Open. Steps 1, 2 and 3 of `A11Y-PZ-02` cannot be run
without it; if it has not merged, those steps are recorded as not run with #36 named, and the rehearsal proceeds
through the screens.

**Open, not depended on:** #43 and #48 (the paid dispatch and doorstep legs, rehearsed there), #51 (the offline step),
#52 (Playwright), #61a (which later consolidates this seed scenario into the shared fixture library), #61c (the pilot).
**In flight in the same files:** `src/Tools/Tailor360.Cli/Commands/SeedSyntheticCommand.cs` — #61a and other issues'
fixtures land in the same command — and `docs/nfr/traceability.md`.

Refs #37
