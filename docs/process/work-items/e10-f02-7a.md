Sub-issue of #45. The navigation already points at a reporting section that does not exist:
`clients/pwa/src/components/layout/roleNavigation.ts` line 81 declares `reports: { href: '/reports', icon:
'bar-chart', messageId: 'navigation.destination.reports' }` and `ROLE_NAVIGATION` gives it to **`tailor-master`
(line 143), `cashier` (line 153) and `owner` (line 170)**, while `clients/pwa/src/app/router.tsx` has **no `reports`
path at all** (`grep -c "reports" clients/pwa/src/app/router.tsx` is `0`) and `clients/pwa/src/reporting/` does not
exist. This slice builds the client reporting area and the hub that destination has been leading nowhere towards. The
dashboards are E10-F02-7b, E10-F02-8a and E10-F02-8b; it is split because one session cannot carry a new client area
plus a charted dashboard plus a drill list — the measured precedent is
`clients/pwa/src/routes/catalog/CatalogVersionEditorRoute.tsx` at **978 production lines for one route**.

## What is already built (do not re-build)

- `clients/pwa/src/routes/billing/OutstandingBalancesRoute.tsx` (**106 lines**) — the worked shape of a read-only
  screen: `useAdminResource(key, signal => read(signal))`, `AuthProblemAlert` for the failure, `LoadingState`,
  `EmptyState`, `DataTable` with one `primary` column, `getFormatters()`, and a `page`-classed `<section>` with an
  `h1` from the catalogue. Copy it.
- `clients/pwa/src/billing/` — the worked shape of a **client area**: an API module, view types, a permissions
  constant and a testing fixture module. Follow it rather than inventing a layout.
- `clients/pwa/src/admin/useAdminResource.ts` — the one read effect, with its documented rules: the dependency is a
  **key string**, the read is held in a ref, and a reload keeps the old value on screen. "It is not a cache and not a
  second data-fetching path" — and there is **no TanStack Query in `package.json`**, so do not import one.
- `clients/pwa/src/auth/apiClient.ts` — `apiRequest<T>(path, { signal, … })`, the only transport.
  `clients/pwa/src/auth/AuthProblemAlert.tsx` renders an RFC 9457 problem in plain language.
- `clients/pwa/src/admin/RequirePermission.tsx` — renders an `EmptyState` with `admin.forbidden.title` /
  `admin.forbidden.body` for a caller without the key. **It is not the `Forbidden` component and it never
  redirects.** Route entries wrap the element exactly as `router.tsx` lines 226 to 248 do for Billing.
- `clients/pwa/src/components/primitives/` — `Card`, `DataTable`, `Filters`, `StatusBadge`, `Alert`, `Button`,
  `TextLink`, `IconButton`, `Icon`. `icons.ts` already has `bar-chart` (line 49), `clock` (30), `alert-triangle`
  (28), `pause` (31), `clipboard` (44) and `rupee` (62), so no icon is added.
- `clients/pwa/src/components/states/` — `LoadingState`, `EmptyState`, `ErrorState`, `RetryableError`,
  `NetworkStatusBanner`, `OfflineBlockedAction`, and `problemDetails.ts`.
- `clients/pwa/src/i18n/` — `messages/index.ts` composes one file per family and `en-IN.ts` derives `MessageKey` from
  it, so a key missing from `ta-IN` fails `pnpm typecheck`. `formatters.ts` is the only place a date or a number is
  formatted (`getFormatters()`); never `toLocaleString` at a call site.
- **The test harness these screens actually use** — `clients/pwa/src/auth/testing/fixtures.ts`: `stubFetch()`,
  `transport.route('GET /path', …)`, `jsonResponse`, `problemResponse`, `aCurrentUser({ permissions })`, composed with
  `MemoryRouter`, `SessionProvider`, `RequireSession`, `RequirePermission`, `AppIntlProvider` and
  `ShellStatusProvider` exactly as `clients/pwa/src/routes/billing/TakePaymentRoute.test.tsx` lines 1 to 49 do.
  **`clients/pwa/src/design-system/testing/renderWithProviders.tsx` supplies the intl provider and display
  preferences only — no session and no router — so a route rendered in it throws.** The admin story harness
  (`clients/pwa/src/admin/testing/storyTransport.tsx`, exporting `storyJson`, `storyProblem`, `storyPending`,
  `withAdminApi`, `STORY_USER` and `StoryRoutes`) is for **stories**, not for these tests, and
  `clients/pwa/src/stories/journeys/JourneyFrame.tsx` wraps children in its **own** `MemoryRouter` so it cannot be
  combined with `withAdminApi`/`StoryRoutes` — pick one.
- `clients/pwa/src/design-system/testing/axe.ts` (`expectNoAccessibilityViolations`) and
  `expectNoHorizontalOverflow.ts`, which already defaults to **320, 360, 768, 1024 and 1280 CSS px at 100% and 200%
  zoom** (its own documentation at lines 10 and 247). Do not narrow the list.
- #186's `GET /api/v1/reporting/status?report=…`, and the **status strip component, which is unowned today**: #184's
  scope calls it "the #44-1 status strip", meaning #183's, while #183's scope lists no client component at all. So
  whichever of #183, #184 and this slice lands first owns the file; this slice creates
  `clients/pwa/src/reporting/ReportStatusStrip.tsx` if neither has, and says so in the pull request so they reuse it.

## Scope

- [ ] **`clients/pwa/src/reporting/`** — the client area, following `clients/pwa/src/billing/`:
      `reportingApi.ts` (one function per read, each taking an `AbortSignal`), `types.ts` (hand-written view types over
      the generated schema), `reportingPermissions.ts` (`REPORTING_PERMISSIONS = { read: 'reports.read', export:
      'reports.export' }` — the same strings the server catalogues in `ReportingPermissions.cs`) and
      `testing/fixtures.ts` with **synthetic rows only**.
- [ ] **`ReportStatusStrip.tsx`** in that area, reading #186's status endpoint and rendering data-as-of in branch
      time, projection lag, the last reconciliation run and delta, and an "unreconciled" warning when a threshold is
      exceeded. **If #183 or #184 has already landed a strip, import theirs and delete this item**, saying so in the
      pull request. If #186 has not merged, the strip renders the `asOf` instant the payload it is given already
      carries and says nothing about reconciliation, rather than showing a blank claim.
- [ ] **`clients/pwa/src/routes/reporting/ReportsIndexRoute.tsx`** at `/reports` — the hub, listing only the report
      screens the caller's permissions allow, each as a `TextLink` with a one-line description. Small and link-shaped:
      no read, no chart, no table. The entries for the dashboards that do not exist yet are **absent**, not disabled —
      a link to a route the router does not have is a dead link, and each later slice adds its own entry.
- [ ] **`reporting.css`** beside the routes, following `clients/pwa/src/routes/billing/billing.css`. **No inline style
      anywhere**, including later on the charts: the content security policy's `StaticDirectives` forbid it.
- [ ] **Messages** in `clients/pwa/src/i18n/messages/reporting.ts` exporting `reportingEn` and `reportingTa`, added to
      both spreads in `messages/index.ts`. Every string from the catalogue, ICU for every plural and number, no
      sentence built by concatenation (word order differs in Tamil). This slice creates the file; every later client
      slice extends it.
- [ ] **One router entry** in `clients/pwa/src/app/router.tsx` for `/reports`, wrapped in
      `<RequirePermission permission={REPORTING_PERMISSIONS.read}>`, in the shape of lines 226 to 248.
- [ ] **No navigation change.** `roleNavigation.ts` already has the `reports` destination for three roles; this slice
      adds no destination and no primary action, which keeps it out of a file every other client slice is editing.
- [ ] **Vitest tests** for the hub: the permitted entries render, an entry the caller's permissions do not allow is
      absent, the forbidden state renders for a caller without `reports.read`, and `expectNoAccessibilityViolations`
      and `expectNoHorizontalOverflow` pass at the default widths and zoom levels.

## Out of scope

Every dashboard: the pipeline dashboard and its drill list (E10-F02-7b), workload (E10-F02-8a), quality (E10-F02-8b),
Today (E10-F02-9). The sales, invoice-register, GST, payments and receivables screens (#184, #185, #186). Any export
control: `reports.export` is flagged for multi-factor authentication and the export service is #186's. Offline
behaviour beyond saying so: a report is an online read, the shell already carries `NetworkStatusBanner` and
`OfflineBlockedAction`, and **no offline Storybook story is required** —
`clients/pwa/src/routes/catalog/catalogScreens.stories.tsx` records why the administration screens have none and the
same reasoning holds here. Making anything the Owner's landing screen (recorded by E10-F02-9 as the shell follow-up it
is). Teaching `roleNavigation.ts` about `branch-manager`, `admin` or `auditor` — see the open decision below.

## Acceptance criteria

- [ ] An Owner signs in, follows the existing **Reports** destination, and reaches `/reports`, which lists the report
      screens they may open. The destination no longer leads nowhere, which is the one user-visible change in this
      slice.
- [ ] The hub lists only the entries the caller's permissions allow, and a caller whose permissions allow none sees a
      hub saying so rather than an empty page with a heading.
- [ ] **Negative — forbidden:** a Tailor reaching `/reports` sees the `RequirePermission` empty state with
      `admin.forbidden.title` / `admin.forbidden.body`, is **not** redirected, and the shell's navigation still works.
- [ ] **Negative — a Cashier follows their own navigation:** `cashier` carries the `reports` destination
      (`roleNavigation.ts` line 153) and holds **no Reporting permission at all** — its `SystemRoles.cs` grant list at
      lines 351 to 373 names Customers, Catalog, Orders and Billing keys only — so a Cashier following the destination
      must meet the `RequirePermission` empty state and never a broken screen. **Assert it**, because it is a live
      inconsistency between the navigation and the grants rather than a hypothetical.
- [ ] **Exception — the strip has no endpoint:** with #186 unmerged, the strip renders the as-of instant it is given
      and omits the reconciliation line entirely; a test asserts it shows no reconciliation claim rather than an empty
      one.
- [ ] No horizontal page scroll at any of the five default widths at 100% and 200% zoom, and no axe violation, on the
      hub.
- [ ] `pnpm --dir clients/pwa typecheck` passes with the `ta-IN` keys present, which is what `en-IN.ts`'s derived
      `MessageKey` enforces.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| The hub's link list | **Internal** | Let a reader find the report they need | Not stored | `reports.read` | **No personal data in client telemetry, ever** (`clients/pwa/CLAUDE.md` section 6): route names, component names and timings only |
| Status-strip figures (data-as-of, lag, last reconciliation delta) | Internal (5.19) | Say how old the figures are — **NFR-BG-05** | Not stored | `reports.read` | Safe as a timing metric |
| `localStorage` / `sessionStorage` | — | — | — | — | **Nothing is written.** Only the locale preference lives there today, and a shop device is shared |
| Customer names, measurements, images, amounts, staff figures | **Not carried** | — | — | — | Nothing in this slice reads a payload containing any of them |

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `reports.read` | branch | no | no | no | `owner`, `admin`, `branch_manager`, `tailor_master`, `auditor` | **Exists** — `docs/security/permission-matrix.md` line 246; the route-level gate on `/reports`. Note that the five roles holding it and the three roles carrying the navigation destination are **different sets** |
| `reports.export` | branch | **yes** | no | yes | `owner`, `branch_manager`, `auditor` | **Exists** — line 245. Declared in `reportingPermissions.ts` for completeness and **used by no screen here**; exports are #186's |

No permission is added and no matrix row changes: this slice adds no endpoint. **OD-13** is open, so the grants above
are the documented default.

## Modules, contracts and events

Client-only. No backend project is touched, no schema, no event, no contract. The only generated artefact consumed is
`clients/pwa/src/api/schema.d.ts`; if this branch regenerates it, it must be from an unchanged
`docs/api/openapi.v1.json` and `pnpm --dir clients/pwa generate:api:check` must be clean. Threat-model coverage: the
"Reports and exports" row of `docs/security/README.md` section 3.2 — `threat-models/reports-exports.md`, carried by
#56a in W2, with **ADR-0011** standing in until it lands (DOR-05). No new ingress and no new egress.

## Screens and states

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| Reports hub | `/reports` | The links are static, so no loading state; the strip has its own inside its card | A caller whose permissions allow no entry sees an `EmptyState` saying so, not a bare heading | No read of its own; the strip renders its own failure inside its card and never blanks the hub | Links only; the shell's `NetworkStatusBanner` covers connectivity | `RequirePermission` empty state, `admin.forbidden.title` / `admin.forbidden.body`, **no redirect** |

Message ids live under `reporting.*` in `clients/pwa/src/i18n/messages/reporting.ts`, with `reportingTa` keys present
so `pnpm typecheck` passes; every date goes through `getFormatters()`. Targets are **44 × 44 CSS px** for standard
controls (**AL-03**, `docs/nfr/accessibility-localisation.md` section 5 line 148, marked proposed), and status changes
are announced politely through the state components' `live="polite"`.

## Migration and rollback

No migration: this slice adds no table and no column. Rollback is redeploying the previous client bundle; the route
disappears and the navigation destination returns to leading nowhere, which is where it leads today.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| Which roles carry the `reports` destination | **A live inconsistency, not a decision.** `ROLE_NAVIGATION` has keys for `capture`, `reception`, `measurement-staff`, `tailor`, `tailor-master`, `inventory`, `cashier`, `delivery` and `owner` — there is **no `branch-manager`, `admin` or `auditor` key at all**, so three of the five roles that hold `reports.read` have no navigation entry and reach `/reports` only by URL, while `cashier` has an entry and no permission | This slice changes nothing in `roleNavigation.ts`: it is a file every client slice edits, the role layouts are **#50's**, and the fix is a shell change rather than a report screen's. Recorded here as a named finding with the evidence, and raised as a new issue linked to the epic (CLAUDE.md section 6, scope discipline) | #50's role layouts |
| Who owns the status strip | **Unowned between #183, #184 and this slice.** #184 calls it "the #44-1 status strip"; #183's scope lists no client component | Whichever lands first owns the file; this slice creates it if neither has and says so in the pull request | When #183 and #184 merge |
| **#50** responsive design system and role layouts | **Open issue** | The components this screen composes exist today — `Card`, `DataTable`, `Filters`, the state family, `AppShell` and the three shells. Nothing here waits on #50, and nothing here may assume #50 has merged | #50's own closure |
| **AL-03** touch-target sizes | **Proposed, to be confirmed** — `docs/nfr/accessibility-localisation.md` section 5 | 44 × 44 CSS px for standard controls and 56 × 56 for a primary shop-floor action; expect the numbers, not the rule, to move | #50's staff trial |
| `ta-IN` selectability | Settled rule | Keys are added to both catalogues; Tamil stays behind its flag until 95% translated and reviewed | #52 |

## Required verification

- [ ] **Vitest** (`pnpm --dir clients/pwa test`): the hub's permitted and absent entries, the forbidden state, and the
      Cashier case, built on `clients/pwa/src/auth/testing/fixtures.ts` with `MemoryRouter` + `SessionProvider` +
      `RequireSession` + `RequirePermission` + `AppIntlProvider` + `ShellStatusProvider`, as
      `routes/billing/TakePaymentRoute.test.tsx` does.
- [ ] **Vitest**: the strip with and without #186's endpoint, asserting no empty reconciliation claim.
- [ ] **Accessibility**: `expectNoAccessibilityViolations` on the hub in each state, and `expectNoHorizontalOverflow`
      at its default five widths at 100% and 200% zoom.
- [ ] `pnpm --dir clients/pwa lint`, `typecheck`, `format:check`, `build` and `generate:api:check`.
- [ ] Non-functional rows (DOR-13): the Core Web Vitals and mobile budgets of
      `docs/nfr/capacity-and-performance.md` section 5 are **#52's** to measure; this slice stays inside them by
      adding no dependency and no chart library, and says so.
- [ ] Environment (DOR-16): `./scripts/dev doctor`; the client tier needs only Node 22 and pnpm 10. **No end-to-end
      tier**: `tests/e2e` and `playwright.config` do not exist and arrive with **#52** (`scripts/dev` line 434 says so
      in as many words), so nothing here claims one.

## Evidence the pull request will carry

- [ ] Screenshots of the hub at phone, tablet and desktop widths in the default theme, with synthetic data only, plus
      the forbidden state and the Cashier's forbidden state.
- [ ] The axe run output and the `expectNoHorizontalOverflow` assertion output.
- [ ] `pnpm --dir clients/pwa test`, `lint`, `typecheck`, `format:check` and `build` output, the test run **twice**.
- [ ] `generate:api:check` clean, proving no schema drift.
- [ ] A one-line statement of whether this slice created the status strip or imported #183's or #184's.
- [ ] The new-issue link for the `roleNavigation.ts` inconsistency, rather than a fix smuggled into this branch.
- [ ] **No offline story and no offline screenshot for this screen**, with the one-line reason and the pointer to
      `catalogScreens.stories.tsx` — deleting an inapplicable evidence item with its reason is what the Definition of
      Done asks for.

## Depends on

Nothing in #45. The hub renders links and the strip reads one endpoint, so this slice can merge before any #45 read
exists — which is deliberate, because it owns the `router.tsx` and `messages/index.ts` edits once and every later
client slice then extends files it created.

#186 — `GET /api/v1/reporting/status?report=…` for the strip. **Open today, and not required to merge first**: without
it the strip renders the as-of instant it is given and omits the reconciliation line.

#183, #184 — the status strip, if either lands first. **Open today.** Whichever is first owns the file.

Single-writer files this slice takes, which is why it is not parallel-safe: `clients/pwa/src/app/router.tsx`,
`clients/pwa/src/i18n/messages/index.ts`, `clients/pwa/src/i18n/messages/reporting.ts` (created here),
`clients/pwa/src/reporting/*` (created here) and `clients/pwa/src/api/schema.d.ts` — shared with E10-F02-7b,
E10-F02-8a, E10-F02-8b, E10-F02-9, #184, #185, #186 and the client slices of #33, #34 and #38 now in flight. Rebase;
never hand-merge `schema.d.ts`.

Refs #45
