Sub-issue of #51. The parent's last implementation step — "Document supported offline/resilience boundaries and support
troubleshooting" — plus the two hardening checks that belong to nobody else, and the honest traceability statuses.
Neither document exists today (`ls docs/pwa docs/support` both fail; they are named only at
`docs/IMPLEMENTATION_PLAN.md` lines 2185-2187), and `docs/nfr/traceability.md` still records the four rows this feature
answers as "Proof scheduled (#51)".

**This child needs no hardware and is fully verifiable in this environment.** The real-device install, update and
rollback walkthroughs are **#E12-F02-6b**, deliberately separated so that no coding session is blocked waiting for an
iPhone. `docs/process/definition-of-ready.md` DOR-16 makes the same distinction — it treats "a device or printer for a
physical rehearsal" as an environment requirement in its own right.

## What is already built (do not re-build)

- `docs/nfr/support-matrix.md` — section 7.4 is already the offline-behaviour contract (precached shell; allowlisted
  reference reads "with a visible staleness marker"; queued scans; the doorstep confirmation "only when it references
  a dispatch authorisation obtained **while online**"; blocked billing, payments, receipts, cashier close, stock
  movements, order confirmation, invoice posting and label allocation, each with its one-line reason), section 4.1 the
  iOS storage and eviction caveats, section 3 the device classes and their tiers, section 9 the enforcement table
  including the real-device evidence requirement, section 10 decision **SM-07**, and section 11 Related documents. The
  new documents **summarise and link** it for a shop audience; they do not restate the contract in different words,
  and where the two disagree the matrix wins.
- `docs/architecture/failure-modes.md` section 9 ("A device is offline") — user-visible behaviour, data safety and
  recovery, already written, including the sentence at line 196 that a queued delivery confirmation reads "delivered,
  not yet synced" and never as delivered. Link it; do not fork it.
- `docs/adr/0013-caching.md` sections 4.1 and 4.2 — what may be cached and what never may, including the two client
  rows ("Static assets in the client … never caches protected responses" and "Non-sensitive reference endpoints in the
  client … Allowlist only"). `docs/adr/0016-…` from #E12-F02-1 records the nonced-shell decision.
- `docs/dev/troubleshooting.md` — the existing developer troubleshooting page, and the precedent for the tone of the
  support page.
- `tests/Tailor360.IntegrationTests/Platform/ClientVersionHandshakeTests.cs` — the server half of the compatibility
  handshake is already proven. Read it before writing the **NFR-CB-06** status, so the status names both halves.
- `docs/nfr/traceability.md` section 2.3 — the **only** status vocabulary that may be used: `Proven` ("the proof exists
  today and runs"), `Proof scheduled (#NN)`, `Target open`, `Evidence only`. Do not invent a fifth.
- `docs/process/release-evidence.md` — the per-release form with **RE-06** and the gate rows. It is a form, not an
  index: this issue adds no file under `docs/evidence/`, which does not exist and is not created here.
- Everything from #E12-F02-1 to #E12-F02-5b: the worker and its three caches, the staleness marker, the update path,
  the encrypted store, the measurement drafts, the queue, the mirror, the sync region and the `/sync` screen. This
  issue adds **no mechanism**.

## What is missing

- `docs/pwa/` and `docs/support/` do not exist as directories.
- No test inspects the **whole** of what the application leaves on a device; each earlier child asserts its own store.
- Nothing exercises a genuinely exhausted quota across the draft store, the queue and the caches together.
- The four traceability rows still say "Proof scheduled (#51)".
- **The offline, reconnect, duplicate and conflict end-to-end suite has no owner.** This issue raises that as a new
  issue rather than pretending it is covered — see Scope.

## Scope

- [ ] **`docs/pwa/offline-and-resilience.md`** — the supported boundary, for a reader who runs a shop and for the
      reviewer who has to agree it. Sections: what works with no connection (the shell, navigation, the allowlisted
      catalogue read **and its staleness marker**, typing into an open measurement wizard); what is queued and what
      that means (the two allowlisted operations, the bounds from `OFFLINE_BOUNDS`, "not yet sent" never meaning done);
      what is **blocked** and why (billing, payments, receipts, cashier close, stock movements, order confirmation,
      invoice posting, label allocation — with the one-line reason each, taken from `docs/nfr/support-matrix.md`
      section 7.4); how a conflict is resolved (the drain pauses, a person decides on `/sync`); what is stored on the
      device and for how long; and how to clear local data. Every number is a citation, not a restatement: quote
      **SM-07** and say it is **proposed**.
- [ ] **`docs/support/pwa-troubleshooting.md`** — one symptom per section, each with what the person sees, what to
      check, and what to do: the application will not install (per platform, pointing at `/install`); the update prompt
      keeps coming back, or the application will not update; "waiting to send" is not clearing; "cannot be sent from
      this device"; the catalogue says it was last read at a time (the staleness marker, which is correct behaviour and
      not a fault); storage is full; the application says it must be updated before anything can be saved (the `426`
      path); and how to clear local data safely, naming what is lost. Include the iOS per-browser storage caveat from
      section 4.1: a queue raised in one iOS browser is invisible in another.
- [ ] **Link both documents** from `docs/nfr/support-matrix.md` section 11 (Related documents) and from
      `docs/dev/troubleshooting.md`, so `./scripts/dev docs` proves the links resolve and neither page is an orphan.
- [ ] **`docs/nfr/traceability.md`** — update **three** rows now, using the section 2.3 vocabulary and nothing else:
      - **NFR-CL-04** (line 241, protected responses never cached) → `Proven`, naming `swRouting.test.ts`,
        `referenceReads.test.ts` and `devicePrivacy.test.ts`.
      - **NFR-CU-05** (line 198, the offline queue carries only approved idempotent operations) → one honest status,
        not two: the allowlist unit test exists and runs, and the end-to-end half does not, so write `Proven` for the
        allowlist with the end-to-end half named against the **new follow-up issue** below rather than against #52.
      - **NFR-CB-06** (line 232, an outdated client is refused with a clear upgrade path) → `Proven`, naming both
        `ClientVersionHandshakeTests.cs` and the client tests of #E12-F02-2.
      **Leave NFR-CB-05 (line 231) alone**: its proof is "install records per device class", which is #E12-F02-6b's
      deliverable, and that child edits that one row. One row, one writer.
      **Do not** flip SM-07 in `docs/nfr/support-matrix.md` section 10: it is the technical reviewer's decision, and an
      unconfirmed proposal recorded as confirmed is worse than an open one. `docs/process/waivers.md` is **not** the
      instrument here either — it is for a gate that cannot be met, not for an approval that has not happened yet.
- [ ] **Open a follow-up issue** under epic **E12** (#12) for the Playwright offline, reconnect, duplicate and conflict
      end-to-end suite, and link it from the NFR-CU-05 row. This is not optional tidying: the parent asks for that
      suite, `tests/e2e` and `playwright.config.ts` do not exist (`scripts/dev` line 434 says the suite arrives with
      **#52**), and **#52's own decomposition explicitly does not depend on #51** — its first child states that the
      suite it founds "asserts nothing about a service worker, an offline state or a design token". So the suite has no
      owner today, and CLAUDE.md section 6 is explicit: "Anything discovered out of scope becomes a new issue linked to
      the epic." The new issue depends on #52's end-to-end foundation, on **#37** (the scan submissions) and on **#48**
      (the doorstep confirmation), because there is no Custody endpoint to drive until they land —
      `src/Modules/Custody/Tailor360.Modules.Custody.Api/CustodyEndpoints.cs` maps an empty group and no Custody
      operation appears in `docs/api/openapi.v1.json`. Name it in this pull request.
- [ ] **`clients/pwa/src/offline/devicePrivacy.test.ts`** — the cache-and-storage inspection as an automated test.
      After a scripted session (sign in, read a customer, read a measurement draft, read the catalogue, queue a
      fixture operation), assert that: (a) every Cache Storage key belongs to a name from `swRouting.ts` and no key
      matches a protected path — `/api/v1/customers`, `/api/v1/me`, `/api/version`; (b) every IndexedDB value is
      ciphertext and contains no plaintext fragment of the synthetic sentinels used; (c) `localStorage` holds **only**
      the documented keys — the display preference, the locale, the what-changed version and the queue mirror — and
      the mirror holds no payload, no name and no raw identifier. Enumerate the stores rather than checking the keys
      you expect, so a key nobody thought of fails the test. Use distinctive synthetic sentinels in the style of
      `clients/pwa/src/auth/browserStorage.test.ts`'s `SESSION_MATERIAL`. This is the test that makes the privacy
      claim checkable rather than asserted, and it is the one #56's security-regression children (E14-F01b-2 and
      E14-F01b-2b in the #56 decomposition) can reuse.
- [ ] **`clients/pwa/src/offline/quotaExhaustion.test.ts`** — drive a real `QuotaExceededError` through the three
      writers: a draft autosave, an enqueue, and a cache put. Assert that each fails loudly, that the person is told
      in words, that nothing is silently dropped, that the queue's existing entries are all still readable afterwards,
      and that the application stays usable online.
- [ ] **A rollback rehearsal note** in `docs/pwa/offline-and-resilience.md`, written from the observed behaviour
      #E12-F02-1 and #E12-F02-2 recorded in their own pull requests — republish the previous asset build with the same
      minimum client, the old worker activates, the newer caches are deleted by name, no client is stranded. Cite those
      pull requests. The per-device rehearsal is #E12-F02-6b's.

## Out of scope

Every mechanism: the worker, the caches and the staleness marker (**#E12-F02-1**), the update path (**#E12-F02-2**),
the store (**#E12-F02-3a**), the measurement drafts (**#E12-F02-3b**), the queue (**#E12-F02-4**), the region and the
screen (**#E12-F02-5a**, **#E12-F02-5b**). **All real-device evidence, and the NFR-CB-05 row** (**#E12-F02-6b**) — this
child needs no phone, no tablet and no installed mode, and must not claim any. The Playwright end-to-end suite itself:
this issue only **raises** the issue for it. `tests/load` does not exist and load and soak testing are #58's. Client
telemetry for service-worker failures, queue depth and eviction (#52, #58). The camera permission troubleshooting
beyond the browser-level permission reset: the scan screen is **#36** and does not exist, so the page says what to do
about the permission and links forward rather than documenting a screen nobody can open. `docs/evidence/` — it does
not exist and is not created here.

## Acceptance criteria

- [ ] `./scripts/dev docs` passes with the two new documents in place and every relative link resolving, including the
      new entries in `docs/nfr/support-matrix.md` section 11 and `docs/dev/troubleshooting.md`.
- [ ] A reader who has never seen the code can answer, from `docs/pwa/offline-and-resilience.md` alone: what works
      offline, what is queued, what is refused, how long a queued item lives, and how to clear local data.
- [ ] Every number in both documents is either a citation of `docs/nfr/support-matrix.md`, `docs/nfr/slo.md`,
      `docs/nfr/capacity-and-performance.md` or a named constant in the code. A reviewer can check each one, and no
      number appears that has no source — an unsourced number is a product decision, not a detail.
- [ ] **Negative — the privacy inspection fails when it should:** `devicePrivacy.test.ts` fails if a protected response
      is cached, if a plaintext draft is stored, or if the mirror gains a payload field. Prove it by temporarily
      breaking each of the three and pasting the three failures.
- [ ] **Negative — an unexpected key fails the test:** adding an undocumented `localStorage` key makes
      `devicePrivacy.test.ts` fail, because it enumerates rather than spot-checks. Prove that too.
- [ ] **Negative — quota:** with the quota exhausted, an autosave, an enqueue and a cache write each surface a plain
      sentence; nothing is dropped silently; the queue's existing entries are all still readable afterwards.
- [ ] **Exception — the documents do not overclaim:** neither document states that anything is authorised offline. The
      sentence "the server re-authorises every replayed request" appears in the offline document, and nothing
      contradicts it.
- [ ] The three `docs/nfr/traceability.md` rows read truthfully under the section 2.3 vocabulary, the end-to-end half
      of NFR-CU-05 is attributed to the **new follow-up issue** and not to #52, **NFR-CB-05 is untouched**, and SM-07
      is left open in `docs/nfr/support-matrix.md` section 10.
- [ ] The follow-up issue for the offline end-to-end suite exists, is linked to epic #12, names #52, #37 and #48 as
      its dependencies, and is referenced from the NFR-CU-05 row.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| `docs/pwa/offline-and-resilience.md`, `docs/support/pwa-troubleshooting.md` | **Public** — project documentation containing no data | State the supported boundary and the recovery steps | Version-controlled with the repository | Anyone with the repository | Not applicable. **No real customer name, phone number, measurement or job number** may appear in an example; synthetic data only (CLAUDE.md section 5) |
| `devicePrivacy.test.ts` and `quotaExhaustion.test.ts` fixtures | Internal | Give the assertions a plaintext needle to look for | In the repository | Anyone | Synthetic sentinel values only, in the style of `clients/pwa/src/auth/browserStorage.test.ts`'s `SESSION_MATERIAL`. Never a real name, number or measurement |
| The traceability status rows | Public | Say honestly what is proven | Version-controlled | Anyone | Not applicable |

Nothing new is stored by this issue and no new field, log, metric or export is introduced, so DOR-06 has only the rows
above to classify.

## Permissions and roles

No endpoint, no permission key, no `docs/security/permission-matrix.md` row, no
`tests/Tailor360.IntegrationTests/Authorization/matrix.yaml` entry, no `docs/api/openapi.v1.json` change and no edit to
`Identity.Application` `SystemRoles.cs`. The documents describe the existing authorisation behaviour and **must not
imply that anything offline is authorised locally**: the server re-authorises every replayed request, which is the
sentence the offline document carries, grounded in plan Section 4.4 — authentication and authorisation run before the
idempotency lookup, so "a replay by a revoked or unauthorised principal is refused, never served from the store". The
two permissions the queued operations will need, `custody.scan` and `custody.confirm_delivery`, already exist in
`src/Platform/Tailor360.Platform.Security/Permissions/CustodyPermissions.cs` (lines 21 and 57) and are granted by
#24's matrix, not here. **OD-13** is open and untouched.

## Modules, contracts and events

Documentation and client tests only. No module, schema, migration, `DbContext`, contract or integration event; nothing
crosses a module boundary, so ARCH-001 to ARCH-023 have no project or endpoint to apply to. **DOR-05**: the rows of
`docs/security/README.md` section 3.2 this issue's documents describe are **"Customer data, measurements and media"**
and **"Barcode identity and custody"** (both model files under `docs/security/threat-models/`, both covering issue
**#56a**, both wave **W2**). #56a is not an issue yet — it is the plan Section 6.2 note 11 split of **#56**, which is
open — so cite **#56** and the rows, never "#56b". `devicePrivacy.test.ts` is the artefact those models' regression
suites can reuse; say so in the test's module comment so they find it.

## Screens and states

Deleted with a reason: this sub-issue adds no screen, no route and no component. The screens its documents describe
belong to #E12-F02-3b, #E12-F02-5a, #E12-F02-5b and the existing measurement and install screens, and their own state
stories and accessibility evidence shipped with them.

## Migration and rollback

No database change: no table, no column, no migration.
`dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` reporting nothing outstanding is the proof.
Rolling this issue back removes two documents and two tests and changes no behaviour; the traceability rows go back to
"Proof scheduled (#51)", which is the honest status in that case.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **SM-07** offline queue bounds | Open — technical reviewer, needed "Before #51" (`docs/nfr/support-matrix.md` section 10) | Documented as **proposed**, with the constant cited. **Not** marked confirmed by this issue, and no waiver written for the absent approval | #19 transcribes the confirmation into `docs/prd/assumptions-and-open-decisions.md` |
| **OD-08** retention periods | Open | The offline document states the 24-hour device bound and no other period | #57 |
| **OD-14** telemetry backend | Open; needed before W5 | The documents describe what will be reported once #52 and #58 land, and claim nothing that is reported today | #58 |
| **OD-02** hosting model | Open | Neither document depends on it; `docs/adr/0013-caching.md` says the client caching rules do not change with it | #59 |
| Who owns the offline end-to-end suite | **Undecided, and that is the finding** | Raise a new issue under epic #12 rather than attributing it to #52, which has declined the dependency | This pull request creates it |

## Required verification

- [ ] `./scripts/dev docs` — every relative link in the new documents and the two link additions resolves.
- [ ] Vitest: `devicePrivacy.test.ts` and `quotaExhaustion.test.ts`, plus the **four** deliberately broken runs pasted
      as evidence that the privacy test can fail (a cached protected response, a plaintext draft, a payload in the
      mirror, an undocumented `localStorage` key).
- [ ] `pnpm --dir clients/pwa lint`, `typecheck`, `format:check`, `test`, `build`. **Run the client suite twice**: the
      privacy test inspects module-level stores that other tests also write.
- [ ] `dotnet test --solution HyFib.Tailor360.slnx` — unchanged, quoted to show the documentation change broke nothing;
      `dotnet format --verify-no-changes` clean.
- [ ] Non-functional rows affected (DOR-13): `docs/nfr/traceability.md` **NFR-CL-04**, **NFR-CU-05** and
      **NFR-CB-06** — this is the pull request that changes their status. **NFR-CB-05 is #E12-F02-6b's.**
- [ ] Environment: `./scripts/dev doctor` (DOR-16). **No hardware is required for this child**, and no criterion above
      needs one — which is the point of the split.

## Evidence the pull request will carry

- [ ] The rendered diff of `docs/pwa/offline-and-resilience.md`, `docs/support/pwa-troubleshooting.md`, the two link
      additions and the three traceability rows.
- [ ] `./scripts/dev docs` output.
- [ ] The `pnpm test` output naming the two new test files, run twice, plus the four deliberately broken privacy runs.
- [ ] A screenshot of the Application panel at the end of the scripted session: the cache names, the encrypted
      IndexedDB records and the documented `localStorage` keys.
- [ ] The quota-exhaustion output or screenshots: the sentence shown for a draft, for an enqueue and for a cache write.
- [ ] The number and title of the follow-up issue raised for the offline end-to-end suite.
- [ ] `migrate --dry-run` output showing no schema change.

## Depends on

#E12-F02-1, #E12-F02-2, #E12-F02-3a, #E12-F02-3b, #E12-F02-4, #E12-F02-5a and #E12-F02-5b — all seven must be merged:
the documents describe their behaviour and the privacy test inspects all of their stores at once.
#53 — closed: `ClientVersionHandshakeTests.cs`, which the NFR-CB-06 status names.
#50 — **open**; do not record it as merged. Nothing in this child depends on it.
**Single-writer files this issue touches**, so nothing may run beside it: `docs/nfr/traceability.md` and
`docs/nfr/support-matrix.md`, both of which several other W5 decompositions also edit — rebase, re-read the rows before
changing them, and change only the three named here.

Refs #51
