Sub-issue of #34. Two things #34 asks for have no home in the four slices that write the exception rows. The first
is the **exception dashboards** — plan Section 8 (#34) is explicit that they are "query-time views over the source
tables, not projections", so they belong to Orders and not to Reporting (#45), and `docs/api/openapi.v1.json`
publishes no Orders operation at all today. The second is the **customer timeline**: Orders is listed as publishing
an `ITimelineSource` implementation in [`docs/architecture/module-ownership.md`](../../architecture/module-ownership.md)
section 5.5, and `OrdersModuleServiceCollectionExtensions.cs:40` records the absence as "a **debt** rather than a
deferral", noting that `ix_estimates_customer` and `ix_orders_customer` were built for a reader that does not exist.
This slice serves the five views and pays that debt for the exception facts. It adds no table.

## What is already built (do not re-build)

- `src/Platform/Tailor360.Platform.Abstractions/Ports/ITimelineSource.cs` — the inverted port (ROD-02 in
  `docs/architecture/architecture-rules.md`): `SourceName`, `ReadAsync(TimelineQuery, ct)`, the obligation to filter
  on `TimelineQuery.Permissions` rather than on a role, to return at most `TimelineQuery.Limit` entries newest first
  and strictly older than `TimelineQuery.Before`, and to return an empty list rather than throw.
- `src/Modules/Customers/Tailor360.Modules.Customers.Infrastructure/Persistence/CustomerTimelineSource.cs` — **the
  implementation to copy**, including how it clamps its own limit (line 95).
- `src/Hosts/Tailor360.Web/Timeline/CustomerTimelineComposer.cs` and `Endpoints/CustomerTimelineEndpoints.cs` — the
  host already composes every registered source and already publishes
  `GET /api/v1/customers/{customerId}/timeline` (`CustomerTimelineEndpoints.Path`). Registering a second source is
  the whole integration; **no host endpoint changes**.
- `src/Modules/Customers/Tailor360.Modules.Customers.Application/Abstractions/ICustomerDirectory.cs:87,90` —
  `CustomerSearchQuery.DefaultLimit = 20` and `MaximumLimit = 50` with a cursor. This is the repository's page-size
  convention; the five reads use it rather than inventing one.
- `src/Modules/Orders/…Infrastructure/Orders/OrderSnapshotQuery.cs` — the module's read-query style: projection to a
  payload record, `ReadyStateBlock` mapping (line 324), branch filtering. The five reads follow it.
- `src/Platform/Tailor360.Platform.Security/FieldVisibility/OrdersResponseViews.cs` — the field-minimisation table
  already defined and unit-tested. Every read below applies it, so a Tailor's dashboard carries no price.
- `src/Platform/Tailor360.Platform.Security/Endpoints/RateLimitPolicyNames.cs` — `DefaultUser` for an ordinary
  authenticated read, `ExportHeavy` for an expensive one. The five reads are ordinary reads.
- The source tables are **not** this slice's: `qc_results` and `qc_result_criteria` (E06-F03-2a), `rework_tasks`
  (E06-F03-2b), `holds` and `cancellations` (E06-F03-3), `alterations` (E06-F03-4a, with its decision columns written
  by E06-F03-4b). Each already carries the index its view needs — verify that before relying on it.

## How an endpoint is declared in this repository

There is one accepted shape and it is not optional. Copy it from
`src/Modules/Billing/Tailor360.Modules.Billing.Api/Invoicing/InvoiceEndpoints.cs` (a write at lines 190-196, a read
at 139-141), which is the closest merged example:

```
    .RequirePermission(<PermissionKey>, BranchScope.CurrentBranch)   // ARCH-007
    .ScopedToResource(<ResourceKinds.Thing>, "<routeValueName>")     // ARCH-023, when the route names a row
    .RequireRateLimiting(RateLimitPolicyNames.Write)                 // ARCH-017, exactly one
    .Audited(<Handler>.<Something>Action, reasonRequired: true)      // ARCH-008, every write
    .RequireIdempotency()                                            // where a client may retry
    .RequireIfMatch()                                                // where the row carries an ETag
    .WithRequestTimeout(RequestTimeoutPolicies.Command)               // every endpoint in the repository has one
```

- Verbs: `Platform.Security/Endpoints/EndpointAuthorisationExtensions.cs` (`RequirePermission(key, scope)`,
  `Audited(action, reasonRequired)`, `AllowAnonymousWithJustification`);
  `ResourceScopeEndpointExtensions.cs` (`ScopedToResource(resourceKind, routeValueName, scope)`, and the
  deliberately noisy exemption verb for a route whose identifier names nothing branch-owned);
  `RequestSafetyEndpointExtensions.cs` (`RequireIdempotency`, `RequireIfMatch`);
  `StepUpEndpointExtensions.cs` (`RequireStepUp`).
- **Rate-limit policies**, all of them, from `Platform.Security/Endpoints/RateLimitPolicyNames.cs`:
  `DefaultUser` = `default-user` (ordinary authenticated read), `Write` = `write` (authenticated state change),
  `ScanBurst` = `scan-burst`, `ExportHeavy` = `export-heavy`, plus `AuthenticationAnonymous`,
  `MultiFactorChallenge`, `RecoveryAnonymous`, `DefaultIp`. There is no other policy, and inventing one is a
  change to the limiter configuration in the web host.
- **Request timeouts**: `Platform.Security/Endpoints/RequestTimeoutPolicies.cs` — `Read` = `read`,
  `Command` = `command`, `Report` = `report`, `Export` = `export`. **Every** endpoint in `src/Modules/` carries one
  today (grep `WithRequestTimeout` — there is no exception), so an endpoint without one is the odd one out.
- **Branch scopes** from `Platform.Abstractions/Multitenancy/BranchScope.cs`: `CurrentBranch` (the default),
  `AssignedBranches`, `Organisation`, and the member that demands no branch reach at all.
- **Resource kinds**: each module keeps its own constants class — `BillingResourceKinds`,
  `MeasurementResourceKinds`, `DesignSelectionResourceKinds` — in its `Application` project, with an
  `IResourceScopeResolver` per kind in its `Infrastructure` project, registered with `TryAddScoped`.
- **The matrix row** goes in `docs/security/permission-matrix.md` section 5, *inside* the
  `<!-- matrix:endpoints -->` … `<!-- /matrix:endpoints -->` markers (lines 311-512 today), with the columns
  `| Method | Route | Declaration | Permission | Branch scope | Resource | Audited as | What it is for |`.
  `AuthorisationMatrixTests` fails and names any published route with no row, and any row with no route.

## The hold-age question, registered rather than invented

**No document in this repository fixes the age at which a hold is "overdue".** `docs/prd/exceptions.md` EX-02 and
EX-06 make the due-soon window and the escalation delay **branch configuration** and give no number for a hold;
`docs/nfr/slo.md` has no row for it; the plan has none. An unsourced number is a product decision
(`CLAUDE.md` section 8), so this slice invents none.

Register it as **XQ-06** in `docs/prd/exceptions.md` section 6 — `XQ-01` to `XQ-05` exist at lines 197-201, so XQ-06
is the next free identifier — and transcribe it to
[`docs/prd/assumptions-and-open-decisions.md`](../../prd/assumptions-and-open-decisions.md) with all six deferral
fields of [`docs/process/definition-of-ready.md`](../../process/definition-of-ready.md) section 5:

| Field | Value |
| --- | --- |
| Criterion it defers | "Exception dashboards for … overdue holds" (#34, implementation step 7) |
| Reason | No document fixes a hold age. EX-02 and EX-06 make the comparable windows branch configuration and give no figure for a hold |
| Default in force | The view lists **every open hold, oldest first, with its age in whole days computed from `IClock`**, and labels none of them overdue. The route is named `overdue-holds` because #34 names it so; the response field is `ageInDays`, not `isOverdue` |
| Owner | Business owner with the Tailor Master |
| Date recorded | The day this issue is opened |
| Condition for closure | The exception review that issue #17 records, which is the same forum XQ-01 closes at. The `HoldOverdue` condition and its fifteen-minute evaluator are #33's and consume the figure once it exists |

**DOR-09 is therefore satisfied**: the decision has an identifier, it is open, and the default in force is written
down. This issue is ready and does not wait for an answer.

## Scope

- [ ] Five read endpoints on `OrdersEndpoints.GroupPrefix`, one per dashboard plan Section 8 names, each
      branch-scoped, each under `orders.read` with the `DefaultUser` rate-limit policy and each applying
      `OrdersResponseViews` (ARCH-007, ARCH-017):
      `GET /api/v1/orders/exceptions/failed-qc` · `GET /api/v1/orders/exceptions/repeated-rework` ·
      `GET /api/v1/orders/exceptions/overdue-holds` · `GET /api/v1/orders/exceptions/pending-alterations` ·
      `GET /api/v1/orders/exceptions/cancelled-jobs`.
- [ ] `Application/Exceptions/ExceptionDashboardQueries.cs` and its `Infrastructure/Exceptions/` implementation —
      one query per view, each a join over the owning tables and `garment_jobs` within the caller's branch reach,
      each returning the job number, the category and service-type keys, the due date, the actor-free summary the
      screen needs, and the view's own ordering:
      - *failed QC*: jobs whose **latest** `qc_results` row is a fail, newest first, carrying the failed result's
        identifier and its defect codes.
      - *repeated rework*: jobs with **two or more** `rework_tasks` rows, ordered by that count descending. Two is
        the literal reading of "repeated" and not a tuned threshold; the count is returned so a screen can show it,
        and no other threshold is written into code.
      - *overdue holds*: every **open** `holds` row, oldest first, with the hold's age computed from `IClock`. It
        labels nothing "overdue" — see **Open decisions in force**, because no document fixes a hold-age threshold.
      - *pending alterations*: `alterations` in `Requested` status, oldest first, separated by `source` so a
        customer complaint is not mixed in with a counter correction (the reason `AlterationSource`'s own
        documentation gives).
      - *cancelled jobs*: `cancellations` newest first, with the reason code and the scope.
- [ ] Paging on all five: `limit` defaulting to `20` and clamped to `50`, plus a cursor, following
      `CustomerSearchQuery` (`ICustomerDirectory.cs:87,90`) rather than inventing a page size. Reuse its clamping
      style so a caller cannot ask for an unbounded page.
- [ ] `Infrastructure/Persistence/OrdersTimelineSource.cs` implementing `ITimelineSource` with
      `SourceName = "orders"`, contributing the **exception** entries this parent creates — QC recorded, rework
      opened and completed, held, resumed, rescheduled, alteration requested, decided and completed, job and order
      cancelled, garment reopened — filtered on `TimelineQuery.Permissions` (`orders.read`, with money fields absent
      unless the caller's view includes them) and limited as the port requires. Registered in
      `OrdersModuleServiceCollectionExtensions`, which pays the debt its own line 40 records. If #201 has already
      registered an Orders source, **extend that one** rather than registering a second.
- [ ] Rows for the five routes in [`docs/security/permission-matrix.md`](../../security/permission-matrix.md)
      section 5, and the behavioural rows in `tests/Tailor360.IntegrationTests/Authorization/matrix.yaml`.
- [ ] The dashboard reconciliation test plan Section 8 (#34) asks for by name: replay a scripted history through the
      real commands, then assert each view's counts equal the counts derived from the event stream the same history
      published. It lives in `tests/Tailor360.IntegrationTests/Orders/` beside the harness.

## Out of scope

Every write: the five views read and nothing else. The tables themselves — E06-F03-2a, -2b, -3, -4a and -4b. The
`orders.hold-overdue.v1` condition and the fifteen-minute evaluator that raises it — #33 (plan Section 8, #33). The
estimate, order and job entries of the customer timeline — those are #201's facts, and this slice adds only the
exception entries, extending #201's source if it exists. Any projection, read model or `reporting` schema table —
#44 creates the Reporting module and plan Section 6.2 note 4 is explicit that nothing else may assume it. The
screens — E06-F03-8a and E06-F03-8b. Analytics on repeated rework and turnaround — #45.

## Acceptance criteria

- [ ] Each of the five routes answers for a branch the caller can reach, with the view's documented ordering, the
      paging contract above, and `OrdersResponseViews` applied — a Tailor's response carries no price field.
- [ ] **#34's acceptance criterion "Exception dashboards reconcile with job history"**: the reconciliation test
      replays a scripted history — two failed QCs with one rework each, one job reworked twice, two open holds of
      different ages, one requested and one decided alteration, one cancelled job — and every view's count and
      membership equals what the published event stream implies. A discrepancy fails the test with the view named.
- [ ] The timeline source contributes the exception entries for a customer through the **existing**
      `GET /api/v1/customers/{customerId}/timeline` (`CustomerTimelineEndpoints.Path`), newest first, never more
      than `TimelineQuery.Limit`, and returns an empty list rather than throwing when the customer has no order.
- [ ] Every route declares a permission and exactly one rate-limit policy (ARCH-007, ARCH-017); none is a state
      change, so none carries the audit filter, and the pull request says so in one line rather than leaving
      ARCH-008 unmentioned.
- [ ] **Negative**: a caller without `orders.read` receives `403` `security.forbidden`; a caller whose branches do
      not include the rows receives an empty page and **not** another branch's jobs — asserted with two branches and
      the same query; a `limit` of `0`, `-1` or `5000` is clamped rather than honoured, and a forged cursor is
      refused rather than reinterpreted.
- [ ] **Exception**: a view whose source table is empty returns an empty page with the paging fields present, not a
      `404`; the timeline source failing does not fail the composed timeline, because the host treats a throwing
      source as a partial failure — asserted by the host's existing composer test.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| The five view responses (job number, category and service-type keys, due date, reason codes, defect codes, counts, ages, statuses) | Internal — `docs/nfr/data-classification.md` section 5.7 | Let a Branch Manager and a Tailor Master see what is stuck | Not stored; a query-time view | `orders.read` holders within branch scope, minimised by `OrdersResponseViews` | Identifiers, codes and counts may appear in logs. **No free-text reason and no customer name is selected by any of the five queries** |
| The customer name, where a view shows one | **Personal** — section 5.7 | Only where a screen needs to name the person waiting | Not stored | Callers whose response view includes contact fields | Never in a log line. Section 5.7's export rule applies: a name appears only where the purpose is customer-facing work |
| Money fields (the alteration charge intent) | **Confidential** — section 5.7 | Show a Branch Manager what a decision committed | Not stored | Only callers whose `OrdersResponseViews` entry includes money | Never in a log line |
| Timeline entries contributed by `OrdersTimelineSource` | Internal, with Personal through the customer they are about | One customer's history, composed by the host | Not stored | Whoever the host already authorised for the timeline route, filtered again here on `TimelineQuery.Permissions` | The entry's summary carries codes and identifiers only, never a free-text reason |
| Telemetry: a counter per view with the row count returned | Internal by construction | See which dashboard is hot and how large a page is | Per the telemetry row of section 5.11 | Operations | Allowed. The branch identifier is a low-cardinality attribute; a job number is not an attribute |

No new table, no new column, no audit action, no credential, no media class and no new export.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `orders.read` | branch | no | no | no | `owner`, `admin`, `branch_manager`, `reception`, `measurement_staff`, `tailor_master`, `tailor`, `inventory_clerk`, `cashier`, `delivery_staff`, `auditor` | **Exists** — `docs/security/permission-matrix.md` line 231: "Field-level minimisation, not this permission, is what keeps pricing off a Tailor's job card". That is exactly the discipline the five views apply |

No permission key is added. A separate "dashboard" permission was considered and rejected: the views answer about
jobs the caller may already read, and a second key would let a role see the exceptions of jobs it cannot open.
**OD-13** is open, so the grant above is the seeded default. The fixture extended is
`tests/Tailor360.IntegrationTests/Authorization/matrix.yaml`.

## Modules, contracts and events

Orders only. No other module's `Contracts` is consumed, no event is published or consumed, and no table outside the
`orders` schema is read (ARCH-005, MO-2). The one cross-boundary mechanism used is the **platform port**
`ITimelineSource`, implemented in `Orders.Infrastructure` and composed by `Tailor360.Web` — the sanctioned shape of
`CLAUDE.md` section 3 and ROD-02, and the reason no cross-schema join is needed for the customer timeline.
Threat-model coverage is **DOR-05's pre-#56a alternative**, not a written model:
[`docs/security/README.md`](../../security/README.md) section 3 records that only the authentication model exists (#23)
and that `abuse-cases.md` is **not written**, with the rest scheduled under #56a — the order-workflow model
specifically in the batch-1 child `E14-F01a-4`. So the abuse case is **named here and carried into that backlog
entry**: these are reads of data the caller may already read, so the relevant abuse case
([`docs/security/README.md`](../../security/README.md) section 3) is cross-branch leakage through an
unfiltered list — answered by branch filtering in the query itself and asserted with the two-branch negative test.

## Screens and states

Deleted: this slice publishes the reads and no screen. The dashboard is E06-F03-8a and consumes these five routes;
the message ids and the five screen states belong to that issue.

## Migration and rollback

**No migration.** The five views read tables created by E06-F03-2a, -2b, -3, -4a and -4b, and each of those slices
already adds the index its view needs: `(garment_job_id, recorded_at DESC)` on `qc_results`,
`(garment_job_id, status)` on `rework_tasks`, `(branch_id)` filtered on open `holds`, and `(branch_id, status)` on
`alterations`. Check each one exists before writing a query that assumes it. If a read
turns out to need one more index, it is added by **this** slice as its own expand-only migration, named
`ExceptionDashboardIndexes`, with the query plan that justified it pasted into the pull request; otherwise the pull
request proves the absence with `migrate --dry-run` reporting nothing outstanding and
`has-pending-model-changes` clean. Per [`docs/dev/migrations.md`](../../dev/migrations.md).

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **XQ-06 the hold age at which a hold is "overdue"** — registered by this pull request; see **The hold-age question, registered rather than invented** above for all six deferral fields | **Open**, newly recorded | The view lists **every open hold, oldest first, with its `ageInDays`**, and labels none overdue | The exception review #17 records; #33's `HoldOverdue` consumes the figure |
| What counts as **repeated** rework | Not an owner decision | Two or more rework tasks on one garment, the literal reading of the word. The count is returned so nothing is hidden behind the threshold | — |
| **OD-13** approved role-to-permission grants | **Open** | `orders.read` keeps its seeded grant | W1 exit (shipped under a documented default) |
| **SQ-04** whether cancelling every job cancels the order | **Open**, proposed | The cancelled-jobs view lists job cancellations and order cancellations distinctly and infers nothing from one about the other | With #34 |

## Required verification

- [ ] unit: each query's ordering and its limit clamping, against hand-built rows; the "latest result is a fail"
      selection when a job has a pass **after** a fail — the case a naive join gets wrong.
- [ ] integration: the five routes against real PostgreSQL; the authorisation matrix for each; the **two-branch**
      negative test proving no cross-branch row appears; the forged-cursor refusal; the clamped `limit`.
- [ ] integration: the **dashboard reconciliation test** described in Scope — the direct evidence for #34's
      "Exception dashboards reconcile with job history".
- [ ] integration: the composed customer-timeline route carries the Orders exception entries beside the Customers
      entries, in one merged order, with the Orders source's own permission filtering asserted.
- [ ] contract: OpenAPI lint and diff over the five operations; the endpoint inventory showing a permission and one
      rate-limit policy for each and no audit filter on a read.
- [ ] NFR rows touched: the S2 read-latency row of [`docs/nfr/slo.md`](../../nfr/slo.md) — record the measured p95 for
      the heaviest view at the data volume of
      [`docs/nfr/capacity-and-performance.md`](../../nfr/capacity-and-performance.md) rather than restating the
      target; and `docs/nfr/traceability.md`'s reporting-freshness row is **not** touched, because these are
      query-time views and not projections.
- [ ] environment: `./scripts/dev doctor`; `CI=true` makes a skipped integration tier a failure (DOR-16).

## Evidence the pull request will carry

- [ ] `./scripts/dev test unit|architecture|contract|integration` output, run twice.
- [ ] The reconciliation test's output, showing each view's count beside the event-derived count.
- [ ] The OpenAPI difference report for the five operations and `pnpm --dir clients/pwa generate:api:check`.
- [ ] The `permission-matrix.md` section 5 rows, the `matrix.yaml` diff and the `AuthorisationMatrixTests` run.
- [ ] `EXPLAIN (ANALYZE, BUFFERS)` for the heaviest of the five at the capacity document's volume, with the measured
      p95 beside the S2 row.
- [ ] A two-branch transcript showing the same query answering differently for two callers.
- [ ] `migrate --dry-run` showing nothing outstanding, or the one index migration with the plan that justified it.
- [ ] The diff of `docs/prd/assumptions-and-open-decisions.md` recording the hold-age question with all six
      deferral fields.

## Depends on

**E06-F03-2a** — `qc_results` and `qc_result_criteria`, and its `(garment_job_id, recorded_at DESC)` index, for the
failed-QC view.

**E06-F03-2b** — `rework_tasks` and its `(garment_job_id, status)` index, for the repeated-rework view.

**E06-F03-3** — `holds` and `cancellations` for two more.

**E06-F03-4a** — `alterations` and its `(branch_id, status)` index, for the pending-alterations view and for the
`orders.alteration-requested.v1` timeline entry.

**E06-F03-4b** — the decision columns and `orders.alteration-decided.v1`, `orders.alteration-completed.v1` and
`orders.job-reopened.v1`, which the timeline entries describe. Without 4b the pending-alterations view still works
(every row is `Requested`) but the timeline source has three fewer entry kinds, so **4b must merge first** rather than
leaving the source to be extended later.

#`201` — [E06-F01-2]: the Orders `Api` group and the `garment_jobs` read style every view joins through; and, if it
has registered an Orders `ITimelineSource`, the implementation this slice extends instead of duplicating.

Not depended on: #33 (`HoldOverdue` and the evaluator), #45 (analytics), #44 (the Reporting module). None of the
five views reads the `reporting` schema, which does not exist.

**In flight on the same ground, deliberately not a dependency**: `E09-F02-8` in the batch-1 scratchpad index
(`[E09-F02-8] Contribute invoices, cancellations and notes to the customer timeline`) registers **Billing's** own
`ITimelineSource`. That is a different module's implementation in a different project, and Customers registers its
source with `AddScoped` rather than `TryAdd` precisely so sources join the list instead of replacing one another
(`OrdersModuleServiceCollectionExtensions.cs`'s own remark says so). The only shared ground is the host's composer
test in `src/Hosts/Tailor360.Web/Timeline/`, which both extend; whichever merges second rebases. Do not wait for it
and do not register a second Orders source if #201 has already registered one — extend that one.

Same-file sequencing: `OrdersEndpoints`, `OrdersModuleServiceCollectionExtensions.cs`,
`docs/api/openapi.v1.json`, `clients/pwa/src/api/schema.d.ts`, `docs/security/permission-matrix.md` and
`matrix.yaml` are shared with E06-F03-2a, -2b, -3, -4a, -4b and with #33's children; merge after the slices that create
the tables, and rebase before pushing.

Refs #34
