Sub-issue of #56. Plan Section 6.2 note 5 splits #56 at the wave boundary — #56a in W2 (threat models, abuse
cases, the ASVS traceability skeleton, the CI severity gates) and #56b in W5 — and Section 8 under
"#56 [E14-F01]" names `docs/security/abuse-cases.md` as a #56a deliverable. It does not exist: `ls docs/security`
returns `README.md`, `field-visibility.md`, `permission-matrix.md`, `role-walkthrough.md` and a `threat-models/`
directory holding `authentication.md` alone. This sub-issue writes the catalogue first, before any of the nine
remaining flow models, so that all of them instantiate the same eleven abuse-case families under the same
identifiers instead of each inventing a framing — and so that the #56b regression suite has one test family per
catalogue entry to be measured against.

## What is already built (do not re-build)

- `docs/templates/threat-model.md` — the 248-line template every model is a copy of. Its section 7 already lists
  the eleven families in prose ("insecure direct object reference, privilege escalation, workflow bypass by calling
  the API directly, barcode replay, invoice or payment tampering, stock manipulation, malicious upload, export
  leakage, server-side request forgery, credential abuse, and denial of service") and defines the per-model
  identifier spaces `TM-nnn`, `AB-nn`, `CTL-nn` and `RR-nn`. **Reuse that list verbatim as the family set; do not
  re-derive it.**
- `docs/security/threat-models/authentication.md` — the only written model (373 lines, #23, reviewed 2026-09-05).
  Its section 7 allocates `AB-01` to `AB-09` in its own file-local space and its section 8 allocates `CTL-01` to
  `CTL-30`. It is the worked example of the depth expected, and it is also the **first consumer** of the new
  catalogue: its nine abuse cases map onto families ABF-10 (credential abuse), ABF-02 (privilege escalation) and
  ABF-11 (denial of service).
- `docs/security/README.md` — the directory index (#22). Section 2 already names `abuse-cases.md` with
  "Delivered by #56a · Not written", and section 3.2 is the DOR-05 coverage table that says which flow is covered
  by which issue and what stands in meanwhile. Both tables are edited by this sub-issue, not replaced.
- `docs/process/definition-of-ready.md` **DOR-05** and `docs/process/definition-of-done.md` **DoD 6** — the two
  rules that give the catalogue its force. Do not restate them; cite them.
- `docs/nfr/data-classification.md` section 5.1 to 5.20 — the data inventory, one subsection per flow. Every
  family's "what it is worth" column points at the inventory subsection rather than describing the data again.
- `tests/Tailor360.IntegrationTests/Authorization/` (`matrix.yaml`, `AuthorisationMatrix.cs`, `EndpointInventory.cs`,
  `DenialAuditTests.cs`, `IdentifierEditingTests.cs`) and `tests/Tailor360.IntegrationTests/Security/`
  (`ContentSecurityPolicyTests.cs`, `CrossSiteDefenceTests.cs`, `ErrorLeakTests.cs`, `ForwardedHeaderTests.cs`,
  `ResourceScopePipelineTests.cs`) — the suites that already defeat part of ABF-01, ABF-02 and ABF-10. The
  catalogue's "defeated today by" column names these files; it does not propose new ones.

## What is missing

- No document states which abuse cases the project expects every flow to answer, so a model author has no list to
  answer and a reviewer has no list to check against. The template's prose sentence is not citable: it has no
  identifiers.
- Nothing maps a family to the flow models that must cover it, so "every critical data flow has threats, controls,
  tests and a residual-risk owner" (the parent's first acceptance criterion) cannot be measured.
- `docs/security/README.md` section 3.2 names `#56a` as the covering issue for nine flows. Once #56 is decomposed
  those rows point at an issue that will never exist as a number; they need the real sub-issue identifiers.

## Scope

- [ ] Add `docs/security/abuse-cases.md`: the eleven families `ABF-01` to `ABF-11`, in the order issue #56 lists
      them — insecure direct object reference, privilege escalation, workflow bypass through a direct API call,
      barcode replay, invoice or payment tampering, stock manipulation, malicious upload, export leakage,
      server-side request forgery, credential abuse, denial of service.
- [ ] Each family carries: the identifier; the attacker and the motive; the generic attack pattern in this
      system's vocabulary; the data-classification inventory subsection it targets; the control pattern that
      defeats it (by `ARCH-nnn` rule or by the platform component that owns it); what defeats it **today**, with
      the test file that proves it or "nothing yet, scheduled in #NN"; and the flow models that must answer it.
- [ ] Add a coverage map — one row per family × flow — pre-populated with the model file that will answer it, so a
      later model child adds nothing to this file and no two children edit the same row.
- [ ] Record the identifier convention: families are `ABF-nn` and live only here; a model's own abuse cases stay
      `AB-nn` in its file-local space (as `authentication.md` already does) and cite the family they instantiate.
- [ ] Add an `Abuse-case family` column to the section 7 table of `docs/templates/threat-model.md`, so every model
      written after this one carries the citation by construction.
- [ ] Map `authentication.md`'s existing `AB-01` to `AB-09` onto their families in the coverage map, without
      renumbering anything in that file (the template forbids renumbering: the identifiers are cited from pull
      requests and tests).
- [ ] Update `docs/security/README.md`: section 2 marks `abuse-cases.md` written, and section 3.2 replaces `#56a`
      with the real sub-issue number for each of the nine flows.
- [ ] Record in section 1 of `abuse-cases.md` that the security owner is **RG-OD-02**, still open, and that until
      it is decided the Owner holds every family's residual-risk ownership — the default
      `docs/nfr/traceability.md` line 67 already states.
- [ ] Versioned configuration (DOR-11): none. This sub-issue adds no configuration and no seed data; the catalogue
      is a document, and the only machine-read artefact it touches is the documentation link check.

## Out of scope

The nine flow threat models themselves (the **four** model sub-issues of this parent), the ASVS traceability sheet
(its own sub-issue), the vulnerability-management process with the exception register and its continuous-integration
gate (its own sub-issue), the licence audit (its own sub-issue), and every test that closes a family — the `#56b`
half of this parent owns the regression suite across **seven** sub-issues: `ABF-01` and `ABF-02` with the shared
machinery, then `ABF-03` and `ABF-04` with the no-client-driven-state rule, then `ABF-05`, then `ABF-06` with
`ABF-08`, then `ABF-07`, then `ABF-09`, then `ABF-10` with `ABF-11`. **This is why the catalogue must name all eleven
families with stable identifiers and a control pattern each**: seven different sessions will instantiate them, months
apart, and the identifier is the only thing holding their work together. This sub-issue writes no test and changes no
source file.

## Acceptance criteria

- [ ] A reader of `docs/security/abuse-cases.md` can name, for any one of the eleven families, who attacks, what
      they are after, which flow models must answer it, and which test defeats it today or which issue will add
      one. No family is left with an empty "defeated by" cell: the honest value is "nothing yet, scheduled in #NN".
- [ ] Every family row cites at least one of: an `ARCH-nnn` rule from `docs/architecture/architecture-rules.md`, an
      `NFR-SE-nn` row from `docs/nfr/traceability.md` section 8, or a named test file that exists today.
- [ ] The coverage map lists every one of the ten flows of `docs/security/README.md` section 3.2 — the nine this
      parent covers plus `authorisation.md`, which #32a owns — and no flow appears twice.
- [ ] `docs/templates/threat-model.md` section 7 has the family column, and `docs/security/README.md` section 2
      records `abuse-cases.md` as written with this sub-issue's number.
- [ ] **Negative case**: a family whose control pattern does not exist in the tree is recorded as uncovered with
      its scheduling issue, not as covered — specifically ABF-09, server-side request forgery, where
      `src/Platform/Tailor360.Platform.Abstractions/Ports/IOutboundHttp.cs` is the declared port and `grep -rl
      IOutboundHttp src/` finds **no implementation**, so NFR-SE-10 stays "proof scheduled (#54, #55)".
- [ ] **Exception case**: a family that does not apply to a flow is recorded as "not applicable, because …" in the
      coverage map rather than omitted — the template's own rule for STRIDE rows, applied to the map so that a
      blank cell always means unanswered.
- [ ] `./scripts/dev docs` passes: every relative link in the two new or changed documents resolves.

## Data and classification

The sub-issue adds no field, table, event, log field or export column. What it adds is a document, and the document
itself has a class.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| `docs/security/abuse-cases.md` | Internal | Name the attacks every flow model must answer, and the control pattern that defeats each | Lives with the repository; superseded rather than deleted | Every contributor; it is in the repository | Not logged. Nothing in it is an exploit for an unpatched finding — `docs/security/README.md` section 2 already forbids that, and an open finding stays in its issue under `.github/ISSUE_TEMPLATE/security.yml` |
| The attack-pattern prose of each family | Internal | Describe the pattern, never a working exploit against this deployment | As above | As above | As above |
| Example identifiers used in the prose | Public — synthetic | Illustrate a pattern | — | — | Synthetic only: no real customer name, telephone number, measurement or barcode payload appears, per CLAUDE.md section 5 |

## Permissions and roles

No permission key is added, removed or changed, and no authorisation-matrix fixture is touched. The document names
roles when it describes who an attacker impersonates; it uses the twelve role keys of
`docs/security/permission-matrix.md` verbatim — `owner`, `admin`, `branch_manager`, `reception`,
`measurement_staff`, `tailor_master`, `tailor`, `inventory_clerk`, `cashier`, `delivery_staff`, `auditor`,
`hyfib_super_user` — and their display names as that document gives them.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| — | — | — | — | — | — | **None added.** ABF-02 cites the existing grants of `docs/security/permission-matrix.md` rather than proposing any |

**OD-13** (the approved role-to-permission grants) is open, so every grant the document cites is a documented
default, not an approved one; say so where the document cites one.

## Modules, contracts and events

No module, no schema, no contract, no integration event, no endpoint. Files changed: `docs/security/abuse-cases.md`
(new), `docs/security/README.md`, `docs/templates/threat-model.md`. ARCH-005 and MO-2 are not engaged because no
`DbContext` and no source file is touched. **DOR-05**: this sub-issue is part of the threat-model programme itself
and is therefore covered by `docs/security/README.md` section 3.2 with its own wave, W2.

## Screens and states

Deleted: a documentation-only Lane C sub-issue publishes no screen and changes no client code.

## Migration and rollback

No table, no column, no migration. `dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` reports no
outstanding change, and that output is the evidence. Rollback is a revert of the pull request; nothing in the
running system depends on these files.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **RG-OD-02** — who holds the security owner role and who deputises (`docs/process/release-gates.md` section 9) | **Open** — "needed before W2, when #56a lands" | The Owner holds it, with the technical reviewer, exactly as `docs/nfr/traceability.md` line 67 states. Every family's residual-risk owner column reads "Owner (security owner once RG-OD-02 is decided)" | The W2 exit gate |
| **RG-OD-01** — confirm the proposed severities and waiver durations | **Open** — needed before the W1 exit gate | The severities of `docs/process/release-gates.md` section 4 as drafted; this document cites them and fixes none of its own | The W2 exit gate |
| **OD-13** — approved role-to-permission grants | **Open**; #24 shipped the matrix under a documented default | The twelve seeded roles and their 2026-09-06 default grants; cited as default, never as approved | W1 exit gate (overdue; tracked on OD-13) |
| **OD-08** — retention periods | **Open** | No retention period is written into this document; where one would be needed the row points at `docs/prd/assumptions-and-open-decisions.md` | W1 exit gate (overdue; tracked on OD-08) |

## Required verification

- [ ] Documentation link check: `./scripts/dev docs` — every relative link in `abuse-cases.md`, the changed
      `README.md` and the changed template resolves. This is the only automated gate a documentation change has,
      and `scripts/check-docs-links.py --self-test` proves the checker still detects a broken link.
- [ ] Manual cross-reference check, recorded in the pull request: for each of the eleven families, the cited
      `ARCH-nnn` rule exists in `docs/architecture/architecture-rules.md`, the cited `NFR-SE-nn` row exists in
      `docs/nfr/traceability.md` section 8, and the cited test file exists — pasted as the `grep`/`ls` output.
- [ ] Review against `docs/process/definition-of-ready.md` DOR-05: every flow in `docs/security/README.md`
      section 3.2 appears in the coverage map with a covering sub-issue and a wave.
- [ ] **DOR-13, non-functional impact**: the `docs/nfr/traceability.md` rows this sub-issue touches are
      **NFR-SE-12** (ASVS baseline coverage, evidence only) — it becomes partly evidenced — and **NFR-SE-07** and
      **NFR-SE-10**, whose "proof scheduled" wording this document must not contradict.
- [ ] **DOR-16, environment**: none needed beyond Python 3 for the link checker; `./scripts/dev doctor` records it.

## Evidence the pull request will carry

Per `docs/process/definition-of-done.md` section 4, a documentation change is evidenced by a rendered diff.

- [ ] The rendered diff of `docs/security/abuse-cases.md`, showing the eleven families and the coverage map.
- [ ] The rendered diff of `docs/security/README.md` sections 2 and 3.2, and of `docs/templates/threat-model.md`
      section 7.
- [ ] `./scripts/dev docs` output, and `python3 scripts/check-docs-links.py --self-test` output.
- [ ] The `grep`/`ls` transcript proving every cited rule, NFR row and test file exists.
- [ ] `dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` showing no outstanding migration.

## Depends on

Nothing unmerged. It builds only on work already in `main`: #22 (the `docs/security/README.md` index and the
threat-model template), #23 (`threat-models/authentication.md`), #24 (the permission matrix and the authorisation
suites it cites) and #19 (`docs/nfr/data-classification.md`, `docs/nfr/traceability.md`,
`docs/process/release-gates.md`).

This sub-issue **blocks** the **four** threat-model sub-issues of this parent — they cite `ABF-nn` identifiers and use
the template column this one adds — and it blocks **all five** regression-suite sub-issues of the `#56b` half, whose
`AbuseCaseTraits` constants are this catalogue's eleven identifiers in code and whose coverage rows are its rows. It
does not block the ASVS skeleton sub-issue, which is keyed by ASVS requirement rather than by family, nor the
vulnerability-management or licence-audit sub-issues.

**Because seven later sessions depend on these identifiers, the numbering is frozen when this merges.** `ABF-01` to
`ABF-11` are allocated in the order issue #56 lists them and are never renumbered, never reordered and never reused —
the same discipline `docs/templates/threat-model.md` already imposes on `TM-nnn`, `AB-nn`, `CTL-nn` and `RR-nn`, and
for the same reason. A family that turns out to be two is a new `ABF-12`, not a split of an existing number.

Refs #56
