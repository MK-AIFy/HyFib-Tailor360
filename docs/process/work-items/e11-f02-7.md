Sub-issue of #48. The delivery team's two reading screens, and the first client work in this feature. The shell already
believes they exist: `clients/pwa/src/components/layout/roleNavigation.ts:80` declares `delivery: { href: '/delivery',
icon: 'truck', messageId: 'navigation.destination.delivery' }` and lines 157-159 put `delivery` in the Delivery role's
destinations **and** its bottom bar, while `grep -n "delivery" clients/pwa/src/app/router.tsx` returns **nothing** — so
every Delivery Staff member's main destination is a dead link that falls through to `NotFoundRoute` today. The message
ids are already translated in both languages (`clients/pwa/src/i18n/messages/navigation.ts:36` and `:71`). There is no
`clients/pwa/src/delivery/` directory and no route under `clients/pwa/src/routes/delivery/`.

## What is already built (do not re-build)

- `clients/pwa/src/stories/journeys/DeliveryDispatch.tsx` (440 lines) — the #50 reference journey for this whole flow,
  `useState` over fixtures with **no backend**. Its fixtures are `DELIVERY_QUEUE` (line 81), `DOORSTEP` (line 124) and
  `DELIVERY_FAILURE_REASONS` (line 130) in `clients/pwa/src/stories/fixtures/billing.ts`. Read it first: it is the
  agreed behaviour, including why an unpaid job "is not hidden from the queue and its control is not disabled". **Do not
  replace or delete it** — retiring the mock in favour of the real route is its own issue against #50.
- `clients/pwa/src/billing/` and `clients/pwa/src/routes/billing/` — the feature-area pattern to copy exactly:
  `billingApi.ts` (every call through `apiRequest` from `clients/pwa/src/auth/apiClient.ts`, with `Idempotency-Key` on
  writes and `challengeOnStepUp: true` where the matrix flags step-up), `types.ts`, `billingPermissions.ts`,
  `billingProblems.ts`, `BillingProblemAlert.tsx`, `testing/fixtures.ts`, and seven route components of 41 to 440 lines
  — and for the calibration that matters, `clients/pwa/src/routes/catalog/CatalogVersionEditorRoute.tsx` is **994 lines
  for one route**, before its entry components, its API module, its messages and its stories. Two screens is a session;
  three is not.
- `clients/pwa/src/components/states/` — `LoadingState.tsx`, `EmptyState.tsx`, `ErrorState.tsx`, `Forbidden.tsx`,
  `NetworkStatusBanner.tsx`, `OfflineBlockedAction.tsx`, `useNetworkState.ts` (one module-level snapshot for the whole
  document, read through `useSyncExternalStore`) and `problemDetails.ts`. All five states exist; none is to be rebuilt.
- `clients/pwa/src/components/primitives/` — `Card`, `DataTable` (with the responsive card switch), `Filters`,
  `StatusBadge` (icon **and** text, never colour alone), `Timeline`, `Alert`, `Button`, `ButtonGroup`, `TextLink`,
  `Icon`, and `clients/pwa/src/design-system/components/forms/` for `TextField`, `Select`, `Switch`.
- `clients/pwa/src/admin/RequirePermission.tsx` — the route guard. It renders an `EmptyState` with the message ids
  **`admin.forbidden.title`** and **`admin.forbidden.body`**; it is **not** the `Forbidden` component and it never
  redirects. That is the forbidden state these screens get.
- `clients/pwa/src/routes/billing/TakePaymentRoute.test.tsx` (243 lines) — **the harness a route test must use**:
  `MemoryRouter` + `SessionProvider` + `RequireSession` + `RequirePermission` + `ShellStatusProvider` composed by hand,
  with `stubFetch`, `jsonResponse` and `aCurrentUser` from `clients/pwa/src/auth/testing/fixtures.ts`, and
  `expectNoAccessibilityViolations` from `clients/pwa/src/design-system/testing/axe.ts`. Lines 172-173 show how the
  offline branch is reached. **`clients/pwa/src/design-system/testing/renderWithProviders.tsx` supplies
  `AppIntlProvider` and display preferences only — no session and no router — so a route rendered in it throws.**
- `clients/pwa/src/admin/testing/storyTransport.tsx` — `withAdminApi(element, routes, { path, at })`, `storyJson`,
  `storyProblem`, `storyPending`, `STORY_USER`, `StoryRoutes`. `STORY_USER.session` is a **getter**: spread it inside
  the route thunk, never at module scope. `clients/pwa/src/routes/catalog/catalogScreens.stories.tsx` is the house model
  for route-level stories and records why there are **no offline stories for admin screens**; it also shows the
  forbidden story shape, `'GET /api/v1/me': () => storyJson({ ...STORY_USER, permissions: [] })`.
- `clients/pwa/src/i18n/` — `formatters.ts` (`getFormatters()`; money and dates never through `toLocaleString` at a call
  site), `messages/index.ts`, and the compile-enforced pairing of `*En` and `*Ta` message objects.
- `clients/pwa/src/design-system/testing/expectNoHorizontalOverflow.ts` — already defaults to **320, 360, 768, 1024 and
  1280 px at 100% and 200%** (`MINIMUM_REFLOW_WIDTH = 320`). It needs a real browser, so it runs under #52's Playwright
  harness and not in jsdom; do not narrow its widths and do not claim its run here.
- `docs/nfr/a11y-checklist.md` section 6.5 (`A11Y-PZ-03`, **priority zero**) — steps **1 and 2** are this issue's: "Open
  the branch delivery queue and identify a job by its row" and "Read the job's ready state, and any blocking reason,
  from the timeline". Section 5.4 and 5.7 are the families they cite.

## What is missing

- No `/delivery` route, so the Delivery role's primary destination is dead.
- No `clients/pwa/src/delivery/` feature area: no API module, no types, no permission constants, no fixtures, no message
  catalogue.
- No screen anywhere shows a blocking reason, which is the one thing `A11Y-PZ-03` says the journey exists to make
  understandable.

## Scope

- [ ] **`clients/pwa/src/delivery/`** — the feature area, modelled on `clients/pwa/src/billing/`: `deliveryApi.ts`
      (reads only in this issue, each through `apiRequest`), `types.ts` typed against the generated
      `clients/pwa/src/api/schema.d.ts` the way `clients/pwa/src/api/contract.ts` pins the billing types,
      `deliveryPermissions.ts` holding `custody.read_delivery_queue` as a named constant, `DeliveryProblemAlert.tsx`
      over `problemDetails.ts`, and `testing/fixtures.ts` with synthetic rows — reuse the shapes of
      `clients/pwa/src/stories/fixtures/billing.ts`'s `DELIVERY_QUEUE` rather than inventing new ones, and never a real
      name, telephone number or amount.
- [ ] **`/delivery` — the branch delivery queue** (`clients/pwa/src/routes/delivery/DeliveryQueueRoute.tsx`): orders
      grouped as cards on a phone and rows in a `DataTable` from tablet upwards, ordered by the promise, each showing
      the order number, the promised date, the ready/blocked count, the outstanding balance and a status badge with
      **icon and text**. `Filters` for ready, blocked and overdue. An unpaid or blocked order is **shown, readable and
      reachable**, never hidden and never a disabled row — the rule `DeliveryDispatch.tsx`'s own header gives, and the
      reason `A11Y-PZ-03` exists.
- [ ] **`/delivery/orders/:orderId` — the stop card** (`clients/pwa/src/routes/delivery/DeliveryStopRoute.tsx`): every
      garment of the order with its job number, its live ready state, its **blocking reason in words**, the
      `deliver_together` partners it is waiting for, the policy outcome for the order, the outstanding balance, and a
      `Timeline` of what has happened to it. The primary action leads to the receive scan, which is **#E11-F02-8a's**
      screen; here it is a link that is disabled with an explanation when the order cannot be received.
- [ ] **Router entries** in `clients/pwa/src/app/router.tsx` inside `RequireSession`, each wrapped in
      `<RequirePermission permission={DELIVERY_PERMISSIONS.readQueue}>` in the pattern the measurement and billing
      routes already use — three layers, of which only the server's is the authorisation.
- [ ] **`clients/pwa/src/i18n/messages/delivery.ts`** with `deliveryEn` and `deliveryTa`, registered in
      `messages/index.ts`. Every string in both languages; no hard-coded English anywhere, proved by a pseudo-locale
      story.
- [ ] **Stories** — `clients/pwa/src/routes/delivery/deliveryScreens.stories.tsx` over `withAdminApi`, giving each
      screen its **loaded, loading, empty, error and forbidden** story plus a **pseudo-locale** story, following
      `catalogScreens.stories.tsx`. These two screens are reads, so there is no `OfflineBlockedAction` state to tell a
      story about; say so in the file where the offline story would have been.
- [ ] **Formatting through the shared helpers only**: amounts and dates through `getFormatters()`, the promised date in
      the branch's own day rather than the device's, and ages in words rather than a raw timestamp.

## Out of scope

Every write. The receive scan and the dispatch scan are **#E11-F02-8a** (steps 3 to 6 of `A11Y-PZ-03`); the doorstep
confirmation, the failed-delivery form and the dispute are **#E11-F02-8b** (steps 7 to 9). The camera and
hardware-scanner experience is **#36's** (`clients/pwa/src/` has no scanner component today — `grep -rn "scanner"
clients/pwa/src` finds only comments and journey mocks). The offline queue, the "delivered — not yet synced" state and
`navigator.storage.persist()` are **#51's**. The Playwright overflow and obscured-focus run, the axe-in-a-real-browser
pass and the Lighthouse budgets are **#52's**. The manual screen-reader walk of `A11Y-PZ-03` is a human run on the Tier
1 kit of `docs/nfr/a11y-checklist.md` section 2.2, recorded on the template in its section 8 — not something a code
change discharges, and not this issue's to produce. Retiring `DeliveryDispatch.tsx` is an issue against #50.

## Acceptance criteria

- [ ] A signed-in Delivery Staff principal holding `custody.read_delivery_queue` opens `/delivery` from the bottom bar
      and reaches a real queue — the destination at `roleNavigation.ts:80` is no longer a dead link — and from a row
      reaches `/delivery/orders/:orderId` and back.
- [ ] Every garment on the stop card shows either "ready" or a **blocking reason in words** (the named phase, the QC
      failure, the hold, the sibling job number, the open reconciliation case, or the payment refusal), never a bare
      code and never an empty cell.
- [ ] Both screens have their loading, empty, error and forbidden states as stories over the **real route components**,
      plus a pseudo-locale story with every visible string accented; an unaccented run of English is a hard-coded string
      and is fixed in this pull request (`clients/pwa/CLAUDE.md` section 5, **NFR-LO-01**).
- [ ] **Negative:** the forbidden state renders `RequirePermission`'s `EmptyState` with `admin.forbidden.title` and
      `admin.forbidden.body` and **none** of the screen's controls, driven by `permissions: []` from `GET /api/v1/me`.
- [ ] **Negative:** a `403` or `404` from the server renders through `DeliveryProblemAlert` in plain language with no
      code, no correlation identifier and no raw message (CLAUDE.md section 4 rule 3); the queue does not silently show
      an empty list in place of a refusal.
- [ ] **Negative:** a blocked order is visible, focusable and reorderable in the list, its row announces the blocking
      reason, and its primary action is explained rather than silently disabled — the `A11Y-PZ-03` rule that "a blocked
      dispatch that sounds like a broken screen is how the control gets worked around".
- [ ] **Exception:** an order whose gate closed between the list read and the card read shows the new state on the card
      and says the list was stale, rather than rendering two contradictory answers; the screen never caches the ready
      verdict.
- [ ] Status is never conveyed by colour alone (**A11Y** status-badge rule), every interactive target is reachable by
      keyboard in the documented order, and `expectNoAccessibilityViolations` passes on every state rendered in the
      Vitest suite.
- [ ] `pnpm --dir clients/pwa lint`, `typecheck`, `format:check`, `test`, `build` and `build-storybook` are all clean,
      and `pnpm --dir clients/pwa generate:api:check` reports no drift.

## Data and classification

No new stored field: a client screen holds nothing. What matters is what it is allowed to display and what must never
reach telemetry.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Queue rows on screen — order number, promised date, job numbers, states, reason codes | **Internal** — `docs/nfr/data-classification.md` section 5.8 | Let the delivery team work the rack | Not stored; in memory for the session only | Holders of `custody.read_delivery_queue`, branch-scoped by the server | Never in client telemetry beyond a route name; no identifier in an error report |
| Outstanding balance shown per order | **Financial** — section 5.11 | Show what is owed before a visit | Not stored | As above | **Never** in client telemetry, an error report or a log — the absolute prohibition list is **section 8**, which names "amounts, tax components, balances" and applies to "client telemetry event" in the same breath |
| Customer name, if a future row shows one | **Personal** — section 5.2 | Not displayed by this issue at all: the queue is addressed by order number | — | — | The server's responses carry no customer name on these routes, and the screens add none |
| Fixture data in tests and stories | Synthetic | Drive the states honestly | Committed literals | Anyone with the repository | Never a real name, number or measurement (CLAUDE.md section 5) |

DOR-06 is answered: nothing new is stored, and the two classes on screen are cited rather than assumed.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `custody.read_delivery_queue` | branch | no | no | no | `owner`, `branch_manager`, `delivery_staff`, `reception`, `auditor` | **Proposed by #E11-F02-1** and added there to `CustodyPermissions`, `docs/security/permission-matrix.md`, `SystemRoles.cs` and `matrix.yaml`. This issue consumes the key and proposes nothing; the constant in `deliveryPermissions.ts` is the server's own string |

No route is added to the server, so no authorisation-matrix row changes. **OD-13** is open, so the grant is #24's
documented default. `RequirePermission` is a convenience for the person, never the authorisation: the server refuses
again on every request.

## Modules, contracts and events

Client only — `clients/pwa`. No .NET project is touched, no module boundary is crossed and no schema is read, so
ARCH-003, ARCH-004, ARCH-005 and ARCH-011 are not engaged. The only cross-cutting contracts are the generated
`clients/pwa/src/api/schema.d.ts` — **regenerated, not hand-edited**, with `pnpm --dir clients/pwa generate:api:check`
clean, which is what makes a server payload change fail here too — and `clients/pwa/src/api/contract.ts`, the
hand-written file that pins each feature's types onto the generated `components`/`operations` and which every feature
area extends, so it is a single-writer file as much as the router and the message index are. DOR-05: no new threat
surface; the screens reach only the two read routes #E11-F02-1 added, and the authorisation is the server's.

## Screens and states

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| Branch delivery queue | `/delivery` | `LoadingState` while the queue is in flight | `EmptyState`: nothing is waiting at this branch today, with the filter that produced it named | `DeliveryProblemAlert` over the problem, with a retry | Read-only screen: the `NetworkStatusBanner` shows the connection and no action is queued; no `OfflineBlockedAction` is needed and the story file says why | `RequirePermission` → `EmptyState` with `admin.forbidden.title` / `admin.forbidden.body` |
| Order stop card | `/delivery/orders/:orderId` | `LoadingState` on the order's entries | An order with no garment on the queue — says so and offers the way back | `DeliveryProblemAlert`; an unknown or other-branch order reads as not found | As above | As above |

Message ids: a new `delivery.*` set in `clients/pwa/src/i18n/messages/delivery.ts`, in `deliveryEn` **and**
`deliveryTa`, which is compile-enforced. Reason codes are translated to sentences in the catalogue, never rendered raw.
Amounts and dates through `getFormatters()`. Accessibility items served: `docs/nfr/a11y-checklist.md` section 4.12 (the
five states), the status-badge rule (icon and text), section 5.4 and 5.7 as `A11Y-PZ-03` steps 1 and 2 cite them.

## Migration and rollback

No migration: no .NET project and no schema is touched. Proven by the diff containing no file under
`src/Modules/*/Migrations/` and by `migrate --dry-run` reporting nothing outstanding. Rollback is reverting the commit;
the only visible consequence is the `/delivery` destination going back to being a dead link.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-04** — payment rule and partial-delivery policy | **Open** | The screens **display** the server's policy outcome and blocking reason and decide nothing; a change of policy changes the words, not the code | W4 exit |
| **OD-13** — approved role-to-permission grants | **Open** | #24's seeded defaults; the screens read the key the server publishes | W1 exit (overdue) |
| **AL-03** — the three touch-target sizes | Proposed, to be confirmed (`docs/nfr/accessibility-localisation.md` section 5) | Build to them; jsdom cannot judge target size, so the stories are where a reviewer sees them | Staff trial before #50 closes |
| **SQ-09** — does closing one member's gate close the rest? | Proposed, to be confirmed | Partners stay on the queue, so the card must say "waiting for job X" rather than hiding the row — the interim reading the server implements | With #34 |

## Required verification

- [ ] **Client (Vitest)**: `DeliveryQueueRoute.test.tsx` and `DeliveryStopRoute.test.tsx` in the
      `TakePaymentRoute.test.tsx` harness — `MemoryRouter` + `SessionProvider` + `RequireSession` + `RequirePermission`
      + `ShellStatusProvider` + `stubFetch` — covering loaded, empty, error and forbidden, each with
      `expectNoAccessibilityViolations`, querying by role and accessible name and never by class name or test id
      (`clients/pwa/CLAUDE.md` section 7).
- [ ] **Client (Vitest)**: the blocking-reason mapping as its own unit test over every reason code the server can send,
      including one it has never seen, which must render a safe fallback sentence rather than a blank.
- [ ] **Stories**: `build-storybook` succeeds, which type-checks every story; the pseudo-locale and the sunlight
      high-contrast theme are checked through the existing toolbars in `clients/pwa/.storybook/preview.tsx`.
- [ ] **Generated contract**: `pnpm --dir clients/pwa generate:api:check` clean.
- [ ] Run the suite twice (`CLAUDE.md` section 5); `useNetworkState` keeps one snapshot per document and the stubbed
      `fetch` is global, which is exactly the class of change that passes in one order only.
- [ ] DOR-13: serves the accessibility and localisation rows of `docs/nfr/traceability.md` (the five states, the
      pseudo-locale growth check, status not by colour alone); the 200% zoom and overflow run is **NFR-AC-04's** and is
      #52's instrument, not claimed here. DOR-16: no database needed; Node 22 and pnpm 10 per `clients/pwa/CLAUDE.md`
      section 1, after `./scripts/dev doctor`.

## Evidence the pull request will carry

- [ ] `pnpm --dir clients/pwa lint`, `typecheck`, `format:check`, `test`, `build`, `build-storybook` and
      `generate:api:check` output.
- [ ] Screenshots of both screens at **phone, tablet and desktop** widths in the loaded state, plus one screenshot each
      of the empty, error and forbidden states, taken from the stories.
- [ ] A screenshot at 320 px with no horizontal scroll, and one in the **High contrast — sunlight** theme, which is the
      theme a delivery phone is read in.
- [ ] A screenshot of the pseudo-locale beside `en-IN`, so the 40% text growth is visible.
- [ ] A screenshot of a **blocked** order's row and card, which is the evidence for the priority-zero criterion.
- [ ] The axe assertion output per state from the Vitest run. The manual screen-reader record is not in this pull
      request and is named here as a boundary: it is a human run on the Tier 1 kit, recorded on
      `docs/nfr/a11y-checklist.md` section 8's template.

## Depends on

#E11-F02-1 — the two read routes, their payloads, `custody.read_delivery_queue` and the regenerated
`clients/pwa/src/api/schema.d.ts`. Must be merged first; without it there is nothing to read and the permission key does
not exist.

**#50 is open — do not record it as merged.** Everything this issue reuses from the design system is nevertheless
already on `main`: the five state components, the primitives and form components listed above, `.storybook/preview.tsx`
with its locale, theme, text-size and viewport toolbars, `clients/pwa/src/i18n/pseudo.ts`,
`design-system/testing/axe.ts`, `roleNavigation.ts` and the eight journey mocks. Build on those paths. The same
correction applies to **#19**, also open, for `docs/nfr/a11y-checklist.md` and `docs/nfr/traceability.md`, which are
likewise present.

**In flight in the same files:** `clients/pwa/src/app/router.tsx`, `clients/pwa/src/i18n/messages/index.ts`,
`clients/pwa/src/api/contract.ts` and `clients/pwa/src/api/schema.d.ts` are single-writer files — #141, #142, #182, #204
and #216 all touch at least one of them, and so do #36's and #49's client children in this same batch. One at a time,
and the second to merge rebases and regenerates. **#E11-F02-8a** and **#E11-F02-8b** import this issue's
`deliveryApi.ts`, `types.ts`, `deliveryPermissions.ts`, fixtures and message catalogue and must merge after it, in that
order.

Refs #48
