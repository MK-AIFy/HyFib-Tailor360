Sub-issue of #40. `E08-F03-6` serves the stock-on-hand and low-stock reports with their reconciliation footer,
freshness fields and CSV exports; none of it is reachable from the application, so #40's fourth acceptance criterion
("report balances reconcile to ledger at the same cutoff time") is provable only from a test run and not from the
product. This child is **two read screens and the one transport change a CSV download needs**.

**The valuation screen is `E08-F03-11b`.** An earlier decomposition put three screens and the transport change in one
child at 1,250 production lines. Measured against this tree that is optimistic: `CashierSessionRoute.tsx` is 440
production lines for one comparable operational screen, `CatalogVersionEditorRoute.tsx` is 994 for one admin route,
and the valuation screen is not a table — it has a cut-off control, a command, a run list and a run's lines, with
money formatting and its own permission separation. Two screens plus the transport helper is one session; four
screens is two.

Checked in this working copy today: there is no `clients/pwa/src/routes/inventory/` directory and no
`clients/pwa/src/inventory/`; `clients/pwa/src/i18n/messages/` holds no `inventory.ts`; and
`clients/pwa/src/auth/apiClient.ts` (435 lines) parses **every** response as JSON (`response.json()` at lines 250
and 270) and sends `Accept: application/json` (line 279), so a CSV download has no path through the one transport —
adding one is part of this child's scope.

## What is already built (do not re-build)

- `clients/pwa/src/routes/billing/OutstandingBalancesRoute.tsx` (106 lines) — the smallest operational read screen in
  the tree and the model for these two: `useAdminResource`, `DataTable`, `Filters`, the state components, nothing
  else. Billing's routes are flat children of `RequireSession` in `clients/pwa/src/app/router.tsx`, from `:227`; the
  `admin` route with its own shell starts at `:289`. These are operational routes, **not** children of the admin
  shell — `E08-F01-5` puts #38's master screens under `/admin/inventory/...`, and these belong under `/inventory`.
- `clients/pwa/src/admin/useAdminResource.ts` (83 lines) — the one read effect (abort; a reload keeps the previous
  value; the key is not an array). A second data-fetching path is forbidden (`clients/pwa/CLAUDE.md` section 6).
- `clients/pwa/src/auth/apiClient.ts` — the **only** transport and the only place a request may be made. It exports
  `apiRequest`, `apiRequestVersioned` (returning a `VersionedResponse<T>` that carries the `ETag`),
  `setSessionChallengeHandler`, and an `ApiRequestOptions` already carrying `idempotencyKey` (`:168`) and `ifMatch`
  (`:177`); it re-authenticates, renews step-up in place and replays the identical request. It is also a file the
  parallel planning round is changing: `E07-F01-7` (planned under #35) adds an `apiRequestBlob` helper for a PDF
  download.
  **Whichever lands first owns the helper and the other reuses it** — read the file before writing, and say in the
  pull request what you found.
- `clients/pwa/src/admin/RequirePermission.tsx` (45 lines) — renders an `EmptyState` with the message ids
  `admin.forbidden.title` / `admin.forbidden.body`, and **never redirects**. It is not the `Forbidden` component
  (which is exported from `clients/pwa/src/components/states/index.ts` and is something else).
- `clients/pwa/src/admin/testing/storyTransport.tsx` (153 lines, note the `.tsx`) — the real harness:
  `storyJson`, `storyProblem`, `storyPending`, `withAdminApi(element, routes, { path, at })`, `STORY_USER` and the
  `StoryRoutes` type. It supplies `SessionProvider`, a `MemoryRouter` and `RequireSession`, so it drives the **real
  route at a real address**, and it is transport-level rather than administration-specific, so it works for
  `/inventory/...`. `STORY_USER.permissions` carries **no** inventory key, so every story stubs
  `'GET /api/v1/me': () => storyJson({ ...STORY_USER, permissions: [...] })` — and this child needs **three** users:
  `inventory.view_reports` alone, `view_reports` plus `reports.export`, and one holding neither.
- `clients/pwa/src/design-system/testing/renderWithProviders.tsx` (45 lines) supplies `AppIntlProvider` and display
  preferences **only** — no `SessionProvider` and no router — so a route rendered in it throws. Use the harness above.
- `JourneyFrame` (defined in `clients/pwa/src/stories/journeys/JourneyFrame.tsx`, which wraps its children in its
  **own** `MemoryRouter` at `:58`, and used from `journeys.stories.tsx`) cannot be combined with
  `withAdminApi`/`StoryRoutes`, because both supply a router. Pick one; these screens use `withAdminApi`.
- `clients/pwa/src/design-system/testing/axe.ts` — `expectNoAccessibilityViolations`, with
  `RULES_JSDOM_CANNOT_JUDGE = ['color-contrast', 'target-size']` (`:22`), which is what jsdom cannot judge.
- `clients/pwa/src/i18n/formatters.ts` — **`formatQuantity` is not a module export.** The file exports
  `getFormatters(locale?, timeZone?)`, `createFormatters`, `DEFAULT_TIME_ZONE` and `CURRENCY_CODE`;
  `formatQuantity(value, unitSymbol)`, `formatNumber`, `formatShortDate` and `formatDateTime` are **methods on the
  `Formatters` interface** (`:104`, `formatQuantity` at `:117`). The call shape every route in the tree uses is `const
  formatters = getFormatters()` once per component, then `formatters.formatQuantity(value, unit)` — see
  `clients/pwa/src/routes/billing/CashierSessionRoute.tsx:32,57` and `AllocateAdvanceRoute.tsx:19,39`. Importing the
  bare name `formatQuantity` from that module fails `pnpm typecheck`. The set is cached per locale and timezone on
  purpose, so never build a formatter in a row renderer and never format at a call site (`clients/pwa/CLAUDE.md`
  section 5).
- `clients/pwa/src/components/states/index.ts` — `LoadingState`, `EmptyState`, `ErrorState`, `Forbidden`,
  `RetryableError`, `OfflineBlockedAction`, `NetworkStatusBanner`, `StateRegion`, and `plainLanguageDetail` /
  `failureCauseForStatus` re-exported from `./problemDetails`.
  `clients/pwa/src/components/primitives/index.ts` — `DataTable`, `Filters`, `StatusBadge`, `Alert`, `Button`,
  `ButtonGroup`, `Card`, `Icon`, `IconButton`, `TextLink`, `Timeline`.
- `docs/nfr/data-classification.md` section 5.9 — "**Cost, valuation and margin figures are Confidential** and are
  **not shown on shop-floor screens**". That sentence is why there is no cost column on either of these two screens
  and why valuation is its own screen behind its own key (`E08-F03-11b`).
- The E08-F01 client foundation planned as `E08-F01-5`: `clients/pwa/src/inventory/{types,inventoryApi,
  inventoryPermissions}.ts`, `clients/pwa/src/i18n/messages/inventory.ts`,
  `clients/pwa/src/routes/inventory/inventory.css` — **extend those five if they exist, create them in the same
  shape if they do not**, and say in the pull request which happened.

## What is missing

- No report screen, so a branch cannot see its own stock and the reconciliation footer exists only in a payload.
- No non-JSON response path in the transport, so no CSV can be downloaded at all.

## Scope

- [ ] Run `pnpm --dir clients/pwa generate:api` **first** and read every shape out of
      `clients/pwa/src/api/schema.d.ts`; pin each local type in `clients/pwa/src/api/contract.ts` with
      `Conforms<Local, Immutable<Response200<'OperationId'>>>` against `E08-F03-6`'s operation ids (`Conforms` is
      exported at `contract.ts:147`; `Response200` and `Immutable` are the local helpers at `:99` and `:134`). A shape
      missing from the generated schema is a defect raised on the sibling, not something to guess around.
- [ ] **`/inventory/reports/stock-on-hand`** — item, location, on hand, reserved, available and in transit, each
      quantity through `formatters.formatQuantity` with its unit; `Filters` for location and item; cursor paging
      through `nextCursor`; the **reconciliation footer** rendered as two labelled totals and an explicit statement of
      whether they agree — and, when they do not, by how much, with no suggestion that the screen can fix it
      (`docs/nfr/traceability.md` **NFR-BG-06**: "Reconciliation detects projection drift and financial or stock
      mismatch, and **reports it rather than correcting silently**"); and the **freshness line** showing when the
      figures were read.
- [ ] **`/inventory/reports/low-stock`** — the low-stock and replenishment list: shortfall, reorder point, target,
      open purchase quantity and the server's suggested replenishment quantity, with the same footer and freshness
      line plus the instant of the last completed evaluation pass, because alert state is deliberately eventually
      consistent (`docs/architecture/invariants.md` line 395). The suggestion is the server's arithmetic; the client
      does not recompute it, and the client never decides whether a line is low.
- [ ] **One download action on both screens, and the one transport change it needs.** `Download CSV` calls the
      matching `…/export` route. The transport must accept a non-JSON response: add **one** helper to
      `clients/pwa/src/auth/apiClient.ts` — or reuse `E07-F01-7`'s `apiRequestBlob` if it landed first — keeping every
      other
      behaviour of the module (session renewal, step-up renewal, the identical replay, the `ApiProblem` shape), and
      send `Accept: text/csv` for these calls only. The file is handed to the browser as a download; **no link is
      constructed and nothing is stored**, because the export is streamed by the re-authorising endpoint.
- [ ] **The download is gated on `reports.export`, not on `inventory.view_reports` — a correction a reviewer will
      otherwise raise.** `E08-F03-6` puts both export routes on `ReportingPermissions.Export` because
      `docs/nfr/data-classification.md` section 5.9's "In exports" row names that key for stock exports by name, and
      it is flagged `RequiresMfa` and `RequiresReason` and is **not** granted to `inventory_clerk` (matrix line 245:
      `owner`, `branch_manager`, `auditor`). So: the **Download CSV affordance is absent, not disabled**, for a caller
      without `reports.export`; the download sends the reason the permission's `RequiresReason` flag demands,
      collected through `clients/pwa/src/routes/admin/AdminReasonField.tsx` (42 lines); and a second-factor demand on
      the download renders as itself, the way `ReconciliationApprovalRoute.tsx` and
      `DispatchExceptionApprovalRoute.tsx` render theirs (the defect #99 fixed: "a step-up demand renders as an
      unknown failure on every administration screen but one"). If `E08-F03-6`'s reviewer moves the key, follow the
      generated schema and the matrix, not this paragraph.
- [ ] **`inventoryPermissions.ts` — a contradiction to name, not to discover in review.** `E08-F01-5` states it
      "declares all four keys now — `manageItems`, `manageSuppliers`, `manageLocations`, `manageReorderRules` — **so
      no later child touches this file**". Every #40 client child does touch it; this one adds `viewReports` and the
      Reporting `export` key. Say in the pull request that `E08-F01-5`'s sentence holds for #38's own screens and not
      for #40's. The file is contended across all four #40 client children, so they merge one at a time.
- [ ] Guard both screens with `RequirePermission` on `inventory.view_reports`; register both as children of the
      existing `RequireSession` route in `clients/pwa/src/app/router.tsx`, beside the billing routes. Add the four
      calls to `clients/pwa/src/inventory/inventoryApi.ts`, the types to `types.ts`, and every string to
      `clients/pwa/src/i18n/messages/inventory.ts` under `inventory.reports.*`, English and Tamil (operational
      vocabulary stays English pending native-speaker review — the recorded practice at the top of
      `clients/pwa/src/i18n/messages/catalog.ts`, whose own heading is "Why every string below is English in the Tamil
      catalogue"; `ta-IN` is not selectable, `clients/pwa/CLAUDE.md` section 5).
- [ ] **Five states per screen** as Storybook stories through `withAdminApi` — loading, empty, error, offline,
      forbidden — plus one story of a **disagreeing** reconciliation footer, one of a **refused export** at the row
      cap, and one pseudo-locale story (`clients/pwa/src/i18n/pseudo.ts`). A download attempted offline renders
      `OfflineBlockedAction`; the reports themselves read from their last value. `clients/pwa/src/routes/billing/`
      ships no stories today (#216 is open to add them); this child writes them rather than adding to that debt.

## The offline story, because this is where a reviewer and the tree disagree

`docs/process/definition-of-done.md` **DoD 7** asks, word for word, that "Storybook stories exist for the loading,
empty, error, **offline** and forbidden state of every new screen", and `clients/pwa/CLAUDE.md` section 6 repeats it.
But `clients/pwa/src/routes/catalog/catalogScreens.stories.tsx` ships **no** offline story and records why in its own
doc comment: "editing and publishing a catalogue are online-only acts, and the application says so through
`NetworkStatusBanner` and `OfflineBlockedAction`, which carry their own stories. An offline story here would imply
these screens queue what a person did, and they must not."

Read that exception for what it is: it is about a screen whose offline story would **imply queueing**. These are
**operational** screens whose offline state is a *visible refusal* — `OfflineBlockedAction` saying the action will not
be queued — which is exactly what **NFR-CU-05** requires of inventory actions and what a person on a shop floor needs
to see. So: **write the offline story**, and put one sentence in the story file's doc comment saying that it renders
the refusal rather than a queue, so it reads as following the catalogue's reasoning rather than ignoring it. Do **not**
produce an offline *screenshot* at five widths; the offline state is a story and a Vitest assertion, not a device
photograph.

## Out of scope

The valuation screen and the valuation CSV download (`E08-F03-11b`); the alert screens (`E08-F03-8`); the stocktake
screens (`E08-F03-9`, `E08-F03-10`); the movement, purchase, consumption, wastage and aging reports and every
cross-branch or historical trend, which plan Section 9 `### #46` assigns to #46 ("#40's operational screens are not
duplicated"); the Owner dashboard and its tiles, which are #45's (`A11Y-RJ-08` steps 1–3); #44's status strip, which
does not exist — this child renders the two freshness fields the server answers; charts of any kind; scheduled or
emailed reports (#44); the `/reports` destination, which `roleNavigation.ts` already points at and which belongs to
the Reporting module (#44/#46), **not** to these `/inventory/reports/...` screens; the `/inventory` index route,
which is #39's client work.

## Acceptance criteria

- [ ] An Inventory Clerk holding `inventory.view_reports` reads the stock-on-hand report for their branch, filters it
      by location, pages to a second page, and reads the reconciliation footer with both totals **labelled** and the
      agreement stated in words — `A11Y-RJ-08` step 7, verbatim: "Read a report with its reconciliation footer, and
      hear the totals labelled".
- [ ] A caller holding `inventory.view_reports` **and `reports.export`** downloads both report CSVs, supplying the
      reason the permission demands; the download goes through the one transport with `Accept: text/csv`, and the test
      asserts the request that was sent rather than the file that arrived.
- [ ] **Negative:** a caller holding `inventory.view_reports` but **not** `reports.export` sees both reports and finds
      **no Download CSV affordance anywhere in the DOM** — absent, not disabled. An Inventory Clerk is exactly this
      caller, which is why the test names the role.
- [ ] **Negative:** a caller holding neither key sees the `RequirePermission` empty state with the message ids
      `admin.forbidden.title` / `admin.forbidden.body` and is **not** redirected.
- [ ] **Negative:** an export refused for exceeding the configured row cap renders the server's own words through
      `plainLanguageDetail`, telling the reader to narrow the filter — not an empty file, not a silent failure and not
      a truncated download.
- [ ] **Exception:** when the footer's two totals disagree, the screen says so plainly, shows the difference, and
      offers no corrective action (**NFR-BG-06**). There is a story for this state, because it is the state nobody
      will see in development and everybody will see eventually.
- [ ] **Exception/offline:** with the network down each report renders its last value and the download renders
      `OfflineBlockedAction` saying it will not be queued (**NFR-CU-05**: "billing, payment and inventory
      reconciliation are online-only with an explicit blocked state").
- [ ] **Exception:** a second-factor or step-up demand on the download renders as what it is — "this needs a recent
      re-authentication" — and not as an unknown failure; the retry that follows is the transport's identical replay.
- [ ] Accessibility: `expectNoAccessibilityViolations` on every state of both screens; each total and each freshness
      value is announced with its label; the tables are navigable by keyboard and each has a caption naming what it
      counts and at what cut-off.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Stock-on-hand and low-stock rows | Confidential (`docs/nfr/data-classification.md` section 5.9, which makes the whole inventory and stock-ledger area Confidential) | Let the branch see what it holds and what to reorder | Component state for the life of the view only; **no `localStorage` and no client draft** — encrypted local drafts are #51's alone (plan Section 6.2 note 8) | `inventory.view_reports` holders within their branch | No quantity and no item code in client telemetry; this child adds no telemetry field |
| Reconciliation footer totals and agreement flag | Confidential | Make a ledger/balance mismatch visible rather than silently corrected | Not cached | As above | The boolean is safe as a telemetry attribute; the totals are not |
| The downloaded CSV file | Confidential — marked in its own header block by `E08-F03-6` | A branch's working copy | Not stored by the client: handed to the browser and forgotten. No link is constructed | `reports.export` holders in branch scope — `owner`, `branch_manager`, `auditor` | The download is audited server-side as `inventory.report.exported` with its reason (section 10: an export download is a read that is audited explicitly) |
| The reason typed for a download | Internal at best — staff free text | Satisfy `reports.export`'s `RequiresReason` flag | Sent and discarded client-side | As above | Never in a log, a trace or client telemetry (section 8) |
| Freshness fields (read instant, last evaluation instant) | Internal | Say how old the answer is | Not cached | As above | Safe in logs |

**No cost, unit cost or valuation figure appears on either screen at all** — that is `E08-F03-11b`'s, behind
`inventory.view_valuation`, and section 5.9 is explicit that cost figures are not shown on shop-floor screens. No
personal data appears on either screen; supplier **contact** data (Personal, section 5.9) is not a column on either.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `inventory.view_reports` | branch | no | no | no | `owner`, `branch_manager`, `inventory_clerk`, `auditor` | **Exists** — `docs/security/permission-matrix.md` line 211. Guards both screens |
| `reports.export` | branch | **yes** | no | **yes** | `owner`, `branch_manager`, `auditor` | **Exists** — matrix line 245, `ReportingPermissions.Export`. Gates the **download only**, per `E08-F03-6`. The consequence this screen must render honestly: an **Inventory Clerk can read both reports and cannot download either** |

This child changes no permission, no grant and no matrix row; it consumes them. **OD-13 is open**, so if the reviewer
re-grants `reports.export` the screen follows the matrix and the generated schema rather than this paragraph.

## Modules, contracts and events

Client only: `clients/pwa/`. No .NET project is touched and no module boundary is involved. The one shared file
changed outside this child's own area is `clients/pwa/src/auth/apiClient.ts` (the non-JSON response helper), which
`E07-F01-7` is also changing for a PDF download — named here so the sequencing is visible. DOR-05: no new trust
boundary; every authorisation, every branch filter, every row cap and the reason requirement are the server's, and the
export-leakage abuse case is attacked from the outside by `E14-F01b-3b` (planned under #56).

## Screens and states

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| Stock on hand | `/inventory/reports/stock-on-hand` | `LoadingState`; the table keeps its previous value across a filter change (`useAdminResource`) | "No stock is recorded at this branch yet" | `RetryableError` with the server's words through `plainLanguageDetail`; a refused export renders its own message | Report readable from its last value; `Download CSV` renders `OfflineBlockedAction` | `RequirePermission` → `EmptyState` with `admin.forbidden.title` / `admin.forbidden.body`; the download affordance is **absent** for a caller without `reports.export` |
| Low stock and replenishment | `/inventory/reports/low-stock` | `LoadingState` | Two distinct empties: "nothing is below its reorder point" and "no alert policy set, so nothing is evaluated" — the second keeps the server's deny-by-default posture legible instead of reading as "no problems" | As above | As above | As above |

Message ids under `inventory.reports.*` in `clients/pwa/src/i18n/messages/inventory.ts`, reusing the shared
`inventory.common.*` words `E08-F01-5` declares. Quantities through `formatters.formatQuantity` with their unit,
instants through `formatters.formatDateTime`, totals through the shared formatters and **never computed on the
client** (`docs/architecture/conventions.md` section 1.1 rule 3: "the client never computes an authoritative
amount"). Graded against `docs/nfr/a11y-checklist.md` `A11Y-RJ-08` step 7.

## Migration and rollback

Deleted deliberately: client-only change, no table and no column. The proof is that no `src/Modules/**` migration
file appears in the diff.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| Alert policy numbers (raised by `E08-F03-1`) | Open | The low-stock screen's second empty state says, in words, that no policy is set and nothing is evaluated; it pre-fills and suggests no number | W4 exit |
| Export expiry (section 9 records a **proposed** 7 days) | Open, in the document | Not applicable: the CSV is streamed and handed to the browser, so there is nothing to expire and no link to share. `E08-F03-6` amends section 9's exception list to record that | #44 |
| OD-07 Device, browser and printer matrix | Open | Screenshotted at the default width list of `expectNoHorizontalOverflow` (320, 360, 768, 1024, 1280 px at 100% and 200% zoom), the only width set written down anywhere today; the real device matrix is #52's | W0 gate (overdue) |
| OD-13 Permission matrix | Open | Keys consumed as published; the Inventory Clerk's inability to download is rendered rather than worked around | W1 (overdue) |
| #50 (design system), #19 (NFRs) and #44 (the status strip this child does **not** use) are **open issues** | Open | The design-system components and the NFR rows exist in the tree and are consumed; #44's strip does not exist and is not depended on. Neither #50 nor #19 may be described as merged | W1/W0/W4 |

## Required verification

- [ ] Vitest: the two read journeys (filter, page, read the footer; read the low-stock list), each through
      `withAdminApi`, asserting the requests sent including `Accept: text/csv` on a download and the reason the
      download carries (`RecordedCall` in `clients/pwa/src/auth/testing/fixtures.ts` is how a request is asserted).
- [ ] Vitest: the three-user permission test — `view_reports` alone sees no download affordance;
      `view_reports` + `reports.export` sees it; neither key gets the `RequirePermission` empty state.
- [ ] Vitest: the disagreeing-footer state, the refused-export message, the second-factor demand on the download, and
      the five states of each screen.
- [ ] Vitest: **a regression test for the transport change** proving the JSON path is unaffected — the helper must not
      change `apiRequest`'s behaviour, its `ApiProblem` handling, its session renewal or its step-up replay. This is
      the riskiest line of code in the child, because every screen in the application goes through that file.
- [ ] Accessibility: `expectNoAccessibilityViolations` on every state of both screens, with
      `RULES_JSDOM_CANNOT_JUDGE` documented in the test as what jsdom cannot judge; plus a recorded manual keyboard
      and screen-reader pass against `A11Y-RJ-08` step 7.
- [ ] **Reflow is not asserted in jsdom.** `clients/pwa/src/design-system/testing/expectNoHorizontalOverflow.ts`
      (394 lines) takes a Playwright `PageLike` and measures inside a real page, and **no screen test in the tree
      calls it** — jsdom performs no layout. Its default widths (320, 360, 768, 1024 and 1280 px at 100% and 200%
      zoom) are nonetheless the screenshot list, and **wide numeric tables at 320 px are the specific risk here**; the
      automated check arrives with #52, and `tests/e2e` and `playwright.config` do not exist yet.
- [ ] `pnpm --dir clients/pwa lint`, `typecheck`, `test`, `build`, `format:check`, `generate:api:check`.
- [ ] DOR-13 rows: **NFR-BG-06**, **NFR-CU-05** and the E12 accessibility rows in `docs/nfr/traceability.md`.
- [ ] DOR-16: `./scripts/dev doctor` attached; the client suite needs no database.

## Evidence the pull request will carry

- [ ] Screenshots of both screens at 320, 360, 768, 1024 and 1280 px, at 100% and 200% zoom, including each numeric
      table at 320 px with no horizontal page scrolling. (`docs/process/definition-of-done.md` section 4 asks for
      three widths as the minimum for a screen; five is what `expectNoHorizontalOverflow` itself defaults to and is
      what #52 will check.)
- [ ] The five state stories per screen, the disagreeing-footer story, the refused-export story and the pseudo-locale
      story — with the offline story's doc comment carrying the one sentence described above.
- [ ] The accessibility run output and the manual `A11Y-RJ-08` step 7 walk.
- [ ] One downloaded CSV opened in a spreadsheet, screenshotted with its marked header block visible — the evidence
      that `E08-F03-6`'s marking and injection prefixing survive the round trip.
- [ ] The transport regression test output, proving the JSON path is unchanged, and the diff of
      `clients/pwa/src/auth/apiClient.ts` on its own.
- [ ] `generate:api:check` clean; the client suite run twice.

## Depends on

`E08-F03-6` — the two report reads, their footer and freshness fields, the two CSV export routes, the row cap and the
`reports.export` decision on the exports.
#38, through its client foundation slice `E08-F01-5` (planned in the parallel round, not yet numbered) — the five
client Inventory files. Those, plus `clients/pwa/src/app/router.tsx` and `clients/pwa/src/api/contract.ts`, are
shared with every other #40 client child, so they merge one at a time.
In flight on a shared file: `E07-F01-7` (planned under #35) changes `clients/pwa/src/auth/apiClient.ts` for a PDF
download. Whichever merges first owns the non-JSON helper; the second reuses it and adds no second one.

Refs #40
