Sub-issue of #35. Follows E07-F01-5b and E07-F01-6. Plan Section 8 gives #35 two client deliverables, and this is the
second: the "label print screen (select jobs by order, queue or scan; preview template/size/orientation)" and the
rule that "**every print action on phone layouts offers Send to print station <name> first and Download PDF as
fallback**", with the reprint action and its reason on the job card. None of #190, #191, #194 or #197 carries any of
it — #191 states it out of scope — and nothing in the client does it today: the only print control in
`clients/pwa/` is the single **Print receipt** button in `clients/pwa/src/billing/ReceiptPanel.tsx`, which offers the
queue and no download at all, and `clients/pwa/src/app/router.tsx` has no label route. `docs/nfr/support-matrix.md`
section 7.2 fixes the ladder this issue implements, and `docs/process/definition-of-ready.md` section 7 — the
readiness note written for #35 itself — lists "label reprint dialog" and "damaged-label replacement" among the
screens the issue owes, with all five states each.

## What is already built (do not re-build)

- `clients/pwa/src/printing/` and `clients/pwa/src/routes/printing/PrintStationRoute.tsx` — **E07-F01-6's** feature
  area, message family `printing.*`, permissions module and fixtures. This issue extends them rather than starting a
  second printing area.
- `clients/pwa/src/auth/apiClient.ts` — the one transport, with the caller-held `idempotencyKey` and
  `challengeOnStepUp` that answers a `403 security.step-up-required` by re-authenticating in place and replaying the
  identical request. `readBody` (line 262) returns `undefined` for any response that is not JSON, which is why a
  bytes-returning call needs the small addition named in Scope and **not** a second `fetch` wrapper.
- `clients/pwa/src/billing/ReceiptPanel.tsx` — the existing print call, and the `printKey`/`fingerprint` idempotency
  pattern to copy. It is also the component this issue retrofits.
- `clients/pwa/src/components/dialogs/ConfirmDialog.tsx` with `confirmTiers.ts` — tiers `'confirm' | 'reason' |
  'typed'`, and `resolveConfirmTier(tier, shellKind)` which is what keeps the typed tier off phone layouts. The
  reprint dialog is the `reason` tier.
- `clients/pwa/src/components/states/`, `clients/pwa/src/components/primitives/` — the five states, `DataTable`,
  `StatusBadge`, `Button`, `ButtonGroup`, `Alert`, `Card`; all with stories.
- `clients/pwa/src/api/contract.ts` — `Conforms<…>` pinning of every hand-written payload against the generated
  `schema.d.ts`; `PrintJobConforms` at line 483 is the existing print payload. The queue row's own type is
  `PrintStationJob`, added by E07-F01-6, because `PrintJob` is already Billing's acknowledgement type
  (`clients/pwa/src/billing/types.ts` line 287).
- **`clients/pwa/src/admin/useAdminResource.ts`** is how every merged screen reads — there is no TanStack Query in
  the client yet (`clients/pwa/CLAUDE.md` section 6: it arrives with #50). `value` / `loading` / `failure`, with
  `AuthProblemAlert` or `RetryableError` rendering the failure;
  `clients/pwa/src/routes/billing/OutstandingBalancesRoute.tsx` is the worked example. Do not write a `useEffect`
  fetch and do not add a second data-fetching path.
- `src/Hosts/Tailor360.Web/Middleware/SecurityHeadersMiddleware.cs` — the policy. After E07-F01-5b it is
  `img-src 'self' data: blob:`, `frame-src 'self'`, `object-src 'none'`. **An inline PNG preview from a blob URL is
  already allowed; a blob PDF in a frame is not, and this issue does not widen the policy again.**
- **Server side, already specified elsewhere — read the regenerated `docs/api/openapi.v1.json` before writing a
  type:** `POST /api/v1/custody/labels/print` (single and batch, the cap and `custody.bulk_print_label`) and
  `POST /api/v1/custody/labels/preview` from #191; `GET /api/v1/custody/jobs/by-number/{jobNumber}` from #194 —
  under `custody.scan`, answering the same DTO as a scan of `GET /api/v1/custody/barcodes/{payload}`, with
  `permittedActions[]` including `reprint-label`; `POST /api/v1/custody/identities/{identityId}/reprint` and
  `…/invalidate` with the mandatory reason and the reason-code catalogue from #197; the order and garment-job read
  from #201.
- `src/Platform/Tailor360.Platform.Abstractions/Ports/IBarcodeRenderer.cs` — `RenderPngAsync(payload, symbology,
  width, height)`, already implemented by `Code128BarcodeRenderer`. It is why a PNG preview is available at all.

## What is missing

- No screen anywhere chooses garment jobs and queues their labels, so the labels #191 can render are unreachable
  from the product.
- No shared print action, so the ladder of `docs/nfr/support-matrix.md` section 7.2 — station first, PDF second — is
  stated in three documents and implemented nowhere. `ReceiptPanel.tsx` offers the queue and **no** PDF fallback,
  although `GET /api/v1/billing/receipts/{receiptId}/document` has existed since #169.
- No reprint dialog, so EX-07 in `docs/prd/exceptions.md` — a damaged or unreadable label — has a server path
  (#194 plus #197) and no way for Reception to walk it.

## Scope

- [ ] **`clients/pwa/src/routes/printing/LabelPrintRoute.tsx`** at `/labels/print`, guarded by `RequirePermission`
      on `custody.print_label`, with **two** ways to choose what to print: **by order** (the order's garment jobs,
      through #201's read, each row showing the job number and the due cue) and **by job number** (#194's
      `GET /api/v1/custody/jobs/by-number/{jobNumber}`). Reading a job out of `by-number` additionally needs
      `custody.scan`, which is what #194 declares on both its routes, so the by-number panel is rendered only for a
      caller holding **both** `custody.print_label` and `custody.scan` (matrix lines 174 and 180; Reception and
      Branch Manager hold both) and the by-order panel is what a holder of `custody.print_label` alone sees.
- [ ] **The raw-payload lookup is deliberately not on this screen.** #194 makes `X-Scan-Source: manual` require
      `custody.manual_lookup` **and a written reason** — matrix line 172, "typing an identity instead of scanning it
      needs a reason, because it is the path that bypasses the check character" — and that key's `Built by` column
      names **#36**, which owns the scan-source ladder, the manual-entry reason and the shared scanner. Putting a
      payload field here would mean either inventing a second reason flow or sending a scan-source value no document
      defines. A job is found here by order or by its printed job number, which is exactly what EX-07 prescribes
      ("Resolve the job by its printed job number or from the personal queue"); the payload path arrives with #36's
      scanner and this screen then mounts it rather than growing its own.
      **Check this against the merged contract before writing the lookup call**, because it is the one thing that
      could change the shape: if #194 merged `X-Scan-Source` as a *required* header on `by-number` as well, send the
      value #194 publishes for a non-scanned read and render the reason field gated on `custody.manual_lookup`; if
      it merged as optional — which is how #194's body reads — send no such header at all. Either way the fallback
      that keeps this issue deliverable is the by-order panel, which touches neither.
- [ ] **Preview before queueing.** Template, size and orientation are chosen on the screen and sent to
      `POST /api/v1/custody/labels/preview`; the answer is shown inline as a PNG in an `<img>` from a blob URL,
      which the existing `img-src 'self' data: blob:` already permits. Where the preview answers a PDF only, the
      screen offers it as a download instead of framing it — `frame-src` is not widened past E07-F01-5b's
      `'self'` and `object-src` stays `'none'`.
- [ ] **Queueing, single and batch.** One `Idempotency-Key` per commit, held across a retry and across an in-place
      re-authentication. A selection above the configured batch cap (#191 owns the option; plan Section 8 records
      the default as 50 and this screen never hard-codes a number — it reads the refusal and explains it) is
      refused by the server without `custody.bulk_print_label`, and the screen renders that refusal as the
      plain-language problem detail with the selection intact, never discarded.
- [ ] **`PrintTargetAction`** in `clients/pwa/src/printing/`: one component, used everywhere anything prints.
      **Send to print station** first and **Download PDF** second, in that order, on every layout, with the
      outcome announced in a live region and never in a toast (`clients/pwa/CLAUDE.md` section 3; a11y items
      **A11Y-DP-06** and **A11Y-DP-07**). The station is named only where a name is known; no station registry
      exists on `Identity.Domain.Branches.Branch`, so the default label is "Send to print station" without one.
- [ ] **The bytes-returning client helper is `apiRequestBlob(path, options)` in
      `clients/pwa/src/auth/apiClient.ts`** — one helper, one name, beside `apiRequest` and `apiRequestVersioned`,
      reusing `exchange` so the correlation header, the client-version header, the anti-forgery pair, the 401 replay
      and the step-up retry all still apply. It returns `{ blob, contentType, fileName }`, reading the file name from
      `Content-Disposition` when the server sent one, and it takes an `accept?: string` option on
      `ApiRequestOptions` so a caller can ask for `application/pdf`; the default stays `application/json`, so no
      existing call site changes behaviour.
      **Four planned units need it and they are in different waves, so the instruction is order-independent:**
      `E09-F02-5` (#42), `E07-F01-7` (#35), `E07-F02-5` (#36) and `E08-F03-11` (#40). Whichever branch merges first
      adds it exactly as specified above; every later one **reuses it and adds nothing**, and none of them renames it
      or adds a second wrapper. If you find it already in `apiClient.ts`, that is the expected case — use it.
      Used here by the label preview and by **Download PDF**.
- [ ] **Adopt `PrintTargetAction` in `clients/pwa/src/billing/ReceiptPanel.tsx`**, so the receipt finally offers the
      PDF fallback its document route has had since #169. This is the only file outside `printing/` this issue
      changes in the billing area, and its existing tests must still pass unchanged in intent.
- [ ] **`LabelReprintDialog`** in `clients/pwa/src/printing/`: `ConfirmDialog` at the `reason` tier, the reason code
      chosen from #197's catalogue with required free text for `other`, posting
      `POST /api/v1/custody/identities/{identityId}/reprint` with `challengeOnStepUp: true` so `apiClient` raises
      the re-authentication dialog — `custody.reprint_label` carries MFA, step-up and a reason
      (`docs/security/permission-matrix.md` line 179). On success it queues the new label through the same
      `PrintTargetAction`. Offered only where #194's response says `permittedActions` includes `reprint-label`.
- [ ] **The damaged-label path as one journey**: a by-number read that resolves `superseded` or `invalidated`
      renders an unambiguous "no valid label" state — never a silent fall back to the superseded one — with the job
      number, how it was found, and the reprint action where the caller may take it (EX-07 in
      `docs/prd/exceptions.md` line 44, whose own wording is "resolve the job by its printed job number").
- [ ] **The five states** on the screen and the dialog, from the shared components; `OfflineBlockedAction` on every
      command, because label allocation and printing are online-only (`docs/nfr/support-matrix.md` section 7.4 lists
      "Order confirmation, invoice posting, label allocation" as **Blocked**).
- [ ] **Messages** added to `clients/pwa/src/i18n/messages/printing.ts` (E07-F01-6 creates the family) in both the
      English and Tamil objects, and **Storybook stories** for the selection, the preview, the batch-cap refusal,
      the reprint dialog and the five states, plus the pseudo-locale story.

## Out of scope

The print station screen itself (E07-F01-6). The camera scanner, continuous scanning and the scan-source ladder
(#36 — which also owns the raw-payload lookup, the `X-Scan-Source` vocabulary and the `custody.manual_lookup`
reason flow; when it lands, this screen mounts its scanner beside the two panels built here).
The label templates, the preview renderer and the print API (#191). Reprint and invalidate server behaviour, the
reason-code catalogue and their concurrency (#197). The job-card screen and the order screens (#204) — the reprint
action is delivered here as a component the job card can mount later, not as a change to a screen that does not
exist. `custody.invalidate_label`: invalidation is an administrative act on a garment that no longer exists (EX-08),
it has no label to print, and it belongs with the job-cancellation screen rather than the print screen — named here
so the remainder has a home. The physical scan and durability evidence (E07-F01-9), including the photographed
EX-07 walk on real hardware.

## Acceptance criteria

- [ ] Reception opens `/labels/print`, finds an order's garment jobs, previews the label, queues one label per job
      with a single `Idempotency-Key`, and sees each job acknowledged with the queue it went to — done through the
      screen with the transport stubbed at `fetch`.
- [ ] The same screen queues a label for a job found by **job number** and for a job reached **by its order**, and
      both reach the identical confirmation shape — the by-number answer is the same DTO #194 gives a scan, which is
      what stops the screen growing a second code path when #36's scanner lands.
- [ ] Every print control in the client — the label screen and `ReceiptPanel` — offers **Send to print station**
      before **Download PDF**, in that order, at 320, 360, 768, 1024 and 1280 px, and announces which it did.
- [ ] **Negative:** a selection above the batch cap without `custody.bulk_print_label` is refused by the server and
      the screen shows the refusal in plain language with the selection still made and nothing queued; a retry
      reuses the same `Idempotency-Key` and does not queue twice.
- [ ] **Negative:** a caller without `custody.print_label` sees the forbidden state and no controls; a caller
      without `custody.reprint_label` never sees the reprint action, because #194 does not put it in
      `permittedActions`.
- [ ] **Negative:** the reprint dialog cannot be submitted with no reason, and `other` cannot be submitted with no
      free text; both errors are text tied to their field and summarised at the top of the dialog.
- [ ] **Exception:** a reprint meets `403 security.step-up-required`; `apiClient` raises the re-authentication
      dialog, the person signs back in, the identical request replays with the same reason and the same key, and
      exactly one new label is queued.
- [ ] **Exception (EX-07):** an identity found by job number whose status is `superseded` shows "no valid label"
      with the job number and the reprint action, and never shows the superseded label as usable.
- [ ] axe clean in every state at the phone, tablet and desktop profiles, at 200% zoom and at 320 px; no horizontal
      page scroll; focus never obscured; primary actions at the 56 × 56 CSS px shop-floor target (AL-03).

## Data and classification

Per `docs/nfr/data-classification.md`. This issue stores nothing server-side; the rows are what it renders and sends.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Barcode payload, job number, category cue, due cue, branch code — what a label carries | Internal — section 5.8, and section 9: "A **label** carries an opaque payload, a job number, a category cue, a due cue and a branch code — **no personal data at all**" | Bond a garment to its identity | Not stored by the client | Holders of `custody.print_label` in the branch | A payload may appear in a log by classification; this client logs none, and client telemetry carries no route-parameter values at all |
| The customer's name, if the order read returns one for the selection list | **Personal** — section 5.2 | Let Reception see they are printing for the right order | Held in memory for the view only | As above, subject to the server's field-visibility decision — the client never unmasks | Never in telemetry, never in a log, and **never printed on the label** |
| The rendered preview bytes | Internal — it is a label | Let somebody see what will print before it prints | The blob URL is revoked when the preview changes or the screen unmounts | As above | Never cached, never written to disk, never given a URL that outlives the view |
| The reprint reason code and its free text | Internal, free text, evidentiary | Say why the bond was broken and re-made (INV-BID-06) | Server-side with the `label_prints` row (#197) | As above, plus `auditor` | Sent to the server; never used as a metric label and never echoed into a URL |
| The downloaded PDF | The artefact's class — a label is Internal; a receipt is **Financial** (section 5.12) | Print from a device with the printer attached, when no station will | Lives in the person's download folder, which is why the station is offered first: section 9 — "Where a print is initiated from a phone the print station holds the artefact, so the file does not travel through a personal device" | The person who downloaded it | Not logged; the download is audited server-side by the document route |

## Permissions and roles

**OD-13 is open**, so every grant is proposed rather than approved. Every key below already exists in
`src/Platform/Tailor360.Platform.Security/Permissions/CustodyPermissions.cs` and in the matrix; this issue adds none.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `custody.print_label` | branch | no | no | no | `branch_manager`, `reception` (Branch Manager, Reception) | **Exists** — `docs/security/permission-matrix.md` line 174, "Printing the label that bonds a garment to its identity" |
| `custody.bulk_print_label` | branch | no | no | no | `branch_manager`, `reception` | **Exists** — matrix line 167, "A batch print for a whole order at the counter" |
| `custody.scan` | branch | no | no | no | `branch_manager`, `reception`, `tailor_master`, `tailor`, `delivery_staff` | **Exists** — matrix line 180. What #194's two lookups demand, which is why the by-number panel is rendered only for a caller who holds it as well as `custody.print_label` |
| `custody.manual_lookup` | branch | no | no | **yes** | `branch_manager`, `reception`, `tailor_master`, `tailor`, `delivery_staff` | **Exists** — matrix line 172, `Built by` **#36**. Named to say it is *not* used here: no control on this screen sends `X-Scan-Source: manual`, so nothing here needs a lookup reason |
| `custody.reprint_label` | branch | **yes** | **yes** | **yes** | `admin`, `branch_manager` (Admin, Branch Manager) | **Exists** — matrix line 179. The three flags are why the dialog is the `reason` tier and why the call passes `challengeOnStepUp: true` |
| `billing.print_receipt` | branch | no | no | no | `branch_manager`, `cashier` | **Exists** — matrix line 147. Named because `ReceiptPanel` is retrofitted here and its gate does not change |

No server-side authorisation row changes, so no authorisation-matrix fixture changes: #191 and #197 own those rows.
The client is not the authorisation — `RequirePermission` only avoids showing controls that would all fail.

## Modules, contracts and events

Client only, plus nothing else: no .NET project is touched, so ARCH-003, ARCH-004 and ARCH-011 are not engaged. The
screen consumes Custody's label, resolve and reprint routes and Orders' order read through the generated contract
(`pnpm --dir clients/pwa generate:api`), with every hand-written type pinned in `src/api/contract.ts` so a server
change fails `pnpm typecheck`. It reads no database and holds no state a server does not own.

**DOR-05:** this screen is where the "label forgery" abuse case of the `barcode-custody` threat model is met on the
client side — the #56a backlog row in `docs/security/README.md` line 74, not yet written. The controls it carries are
that the reprint action appears only when the server says it is permitted, that the reason is mandatory and typed by
a person rather than defaulted, and that a superseded label is never presented as usable.

## Screens and states

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| Label print — selection | `/labels/print` | `LoadingState` while the order or lookup resolves; the result count announced (A11Y-LF-01) | `EmptyState` — "Nothing selected yet", naming the three ways to find a job | `RetryableError` from the problem details, retried with the same request; a job number that matches nothing is named as such rather than shown as an empty result | `NetworkStatusBanner` plus `OfflineBlockedAction` on preview and queue | `Forbidden` via `RequirePermission` on `custody.print_label` |
| Label print — preview | same route | Busy state on the preview button | Prompt to choose a template and size | The refusal as text; a PDF-only answer becomes a download instead of an inline preview | `OfflineBlockedAction`: a preview is a server render | As above |
| Reprint dialog | same route and, later, the job card | Busy state on submit | — | Field errors as text, summarised at the top; the step-up refusal is answered by `apiClient` | `OfflineBlockedAction` in place of submit | Not rendered unless `permittedActions` includes `reprint-label` |
| No valid label (EX-07) | same route | — | — | The state itself is the answer: job number, why, and the reprint action where permitted | The state is a read, so it renders from what was last fetched, marked stale | As above |

Message ids in the `printing.*` family. Dates and the due cue go through `getFormatters()` (`dd-MM-yyyy`, 12-hour),
never `toLocaleString` at a call site. Scored against the core checklist of `docs/nfr/a11y-checklist.md` section 4,
the list family **A11Y-LF-01** to **A11Y-LF-10** (including **A11Y-LF-07**, the multi-select count and the scope of
"select all", which that document ties to exactly this case: "Bulk label printing above the configured cap needs its
own permission; selecting the wrong set is expensive"), and the document-preview family **A11Y-DP-01**,
**A11Y-DP-03**, **A11Y-DP-06** and **A11Y-DP-07**.

## Migration and rollback

Deleted with a reason: no schema change. `./scripts/dev migrate --dry-run` on the branch reports no change, and that
output is the proof.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-09** — label stock size, and whether a QR accompanies Code 128 | **Open** (plan Section 11 item 9; **SM-05** in `docs/nfr/support-matrix.md` section 10) | The size and orientation are **chosen on the screen from what the server offers**, never hard-coded, which is the deferral `definition-of-ready.md` section 7 already records for #35: "the template is built parameterised by label size so the owner's answer changes configuration, not code" | The W3 exit gate |
| The batch cap | Configuration, owned by #191; plan Section 8 records the default as 50 | The screen reads the server's refusal and explains it; **no number is written into the client** | With #191 |
| The reason-code catalogue for a reprint | Shape owned by #197, which says no enumerated reason-code type exists in the codebase to copy | The dialog renders whatever codes the server publishes and requires free text for `other`; it invents no code | With #197 |
| **Whether a by-number read from this screen is a "manual" scan source** | **Not decided anywhere.** #194 requires `custody.manual_lookup` and a reason when `X-Scan-Source: manual` is sent, and its `Built by` column for that key names #36 | **This screen sends no `X-Scan-Source` header.** A job number typed into a print screen is a selection, not a scan that bypasses a check character, and the reason flow belongs with #36's manual-entry ladder. If #194 merged the header as mandatory, send the value it publishes for a non-scanned read and gate the panel on `custody.manual_lookup` as well — checked against the merged OpenAPI document, never guessed | With #36 |
| **OD-13** — approved role-to-permission grants | **Open** | The six keys above as the matrix records them; this issue adds none | The W1 exit gate |
| **AL-03** — touch-target sizes | **Proposed, to be confirmed** | 56 × 56 CSS px with 12 px spacing for the primary actions | Before #50 closes |
| A named print station per branch | Not a recorded decision — there is simply no field for it | "Send to print station", unnamed; naming it is a later change to #25's branch master data, noted in E07-F01-5b | — |

## Required verification

- [ ] **Vitest, positive**: select by order and by job number; preview; queue single and batch; the receipt panel
      offering both rungs — all driven through the screen, queried by role and accessible name.
- [ ] **Vitest, negative and exception**: forbidden on `custody.print_label`; the by-number panel absent without
      `custody.scan`; an unknown job number; the batch-cap refusal; offline blocking; an empty reason and an empty
      `other` text; the step-up replay with the same key; `superseded` rendering "no valid label".
- [ ] **Vitest, transport**: `apiRequestBlob` carries the correlation, client-version and anti-forgery headers and
      replays
      once on a 401, asserted against the stub the way `apiClient`'s own tests do.
- [ ] **Accessibility**: `expectNoAccessibilityViolations` in each state; the overflow and obscured-focus helper at
      320, 360, 768, 1024 and 1280 px and 200% zoom; the pseudo-locale story; the keyboard pass with a wedge-style
      input that does not trap focus.
- [ ] **Contract**: `pnpm --dir clients/pwa generate:api:check` clean.
- [ ] The five commands of `clients/pwa/CLAUDE.md` section 8, run twice.
- [ ] **DOR-13**: **NFR-CB-04** (printing from a phone through the station with a PDF fallback,
      `docs/nfr/traceability.md` line 230) and **NFR-LO-05** (Tamil renders in every generated document, line 220) —
      the physical proof of both is E07-F01-9's; what this proves is that both rungs exist and are reachable.
- [ ] **DOR-16**: `./scripts/dev doctor` for the client suite. No printer is needed to merge: the download is
      asserted against a stubbed anchor and the station hand-off against the stubbed transport.

## Evidence the pull request will carry

- [ ] Screenshots at phone, tablet and desktop widths of the selection, the preview, the batch-cap refusal, the
      reprint dialog and the "no valid label" state.
- [ ] The axe run, and the overflow and obscured-focus helper output at the five widths and 200% zoom.
- [ ] The state stories and the pseudo-locale story.
- [ ] The five client commands' output, run twice.
- [ ] A short before-and-after of `ReceiptPanel`, showing the PDF fallback it did not have.
- [ ] One end-to-end walkthrough of EX-07 recorded as screenshots, with the transport stubbed: a superseded
      identity → by-number lookup → "no valid label" → reprint with a reason → the new label queued → the hand-off
      to the station screen (E07-F01-6). No physical label and no printer: the photographed walkthrough on real
      hardware is E07-F01-9's. This is the walkthrough #197's own
      verification list asks for, seen from the client side.
- [ ] `generate:api:check` output.

## Depends on

#35's E07-F01-6 — the `printing/` feature area, the `printing.*` message family and the station screen this one
hands off to. Merging this first would create a second printing area and a conflicting message family.

#191 — `POST /api/v1/custody/labels/print` and `…/preview`, and the batch cap option.

#194 — `GET /api/v1/custody/jobs/by-number/{jobNumber}` under `custody.scan`, and `permittedActions`, which is what
decides whether the reprint action is offered. Read the merged route before writing the call: whether
`X-Scan-Source` is optional there is the one contract question this screen turns on, and the Open decisions table
says what to do in each case.

#197 — `POST /api/v1/custody/identities/{identityId}/reprint` and the reason-code catalogue.

#201 — the order and garment-job read the "by order" selection uses. Plan Section 6.2 note 11 records that "#35 and
#42 need #32a only", which is the sub-issue group #199 and #201 deliver.

In flight and touching the same client files: #204 (order intake screens) and #141/#142 (design administration and
picker) both add routes to `clients/pwa/src/app/router.tsx`; whichever merges second rebases.

Refs #35
