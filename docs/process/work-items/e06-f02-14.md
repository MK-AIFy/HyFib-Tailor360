Sub-issue of #33. **This is an evidence issue, not a coding session.** It produces no production code. It exists
because three of #33's own Required-verification lines cannot be satisfied by a coding agent in a container, and
leaving them inside a coding child would have blocked that child on hardware and on a person:

- "real-device workboard usability test with production-sized queues" — needs a physical tablet and phone, a real
  browser, and a screen reader (TalkBack, VoiceOver or NVDA) driven by a human;
- "queue reconciliation and **response-time targets**" — the binding measurement is against `docs/nfr/slo.md` row S2
  and `docs/nfr/traceability.md` **NFR-LT-01**, whose stated proof is a k6 scenario. `tests/load` does not exist in
  this repository, `playwright.config` and `tests/e2e` do not exist either, and `./scripts/dev` says the Playwright
  suite arrives with **#52**;
- the Largest Contentful Paint budget of `docs/nfr/capacity-and-performance.md` line 211 (p75 ≤ 2.5 s, cold cache),
  which is a Lighthouse figure on the reference device profile.

Every coding child of #33 has been written to hand these items here and to stay green without them. This issue is
scheduled **after** E06-F02-8, E06-F02-10, E06-F02-11, E06-F02-11b, E06-F02-12b and E06-F02-13 merge, and it is the
child that lets the wave gate say #33's verification is complete rather than partially waived.

## What is already built (do not re-build)

- `docs/nfr/a11y-checklist.md` — `A11Y-RJ-03` "Tailor: scan to queue and a phase of work" (section 6.8, nine steps) and
  `A11Y-RJ-04` "Tailor Master: workboard, assignment and QC" (nine steps). Both tables are **already in the document
  with empty Keyboard, Screen reader, Items failed, Severity and Defect columns** — this issue fills them in. Do not
  add a journey; do not restructure the tables. Section 6.2 describes the run sheet and section 1109 gives the run's
  own header fields (journey identifier, device, browser, assistive technology, date, tester).
- `docs/nfr/a11y-checklist.md` section 5.11 — the administration family, where the workflow editor's pass is recorded,
  because there is no `A11Y-RJ` journey for workflow administration and inventing one is a document change nobody
  asked for.
- The merged screens: `/production` (E06-F02-10), `/workboard` (E06-F02-11), `/workboard/planning` (E06-F02-11b),
  `/workboard/team` (E06-F02-13) and `/admin/workflows/:definitionId/versions/:versionId` (E06-F02-12b).
- The automated halves that are already green and are **not** repeated here: the axe runs, the
  `expectNoHorizontalOverflow` passes at 320/360/768/1024/1280 px and 100%/200% zoom, the keyboard-only assertions, the
  346-row virtualisation assertions, and the `EXPLAIN (ANALYZE, BUFFERS)` and query-count assertions from E06-F02-8.
  This issue cites them; it does not re-run them as its own evidence.
- `tests/Tailor360.IntegrationTests/Orders/OrdersHarness.cs` — how a production-sized data set is built, because
  `seed-synthetic` covers only Identity and Customers today.
- `docs/nfr/capacity-and-performance.md` section 2 — the volumes: 115 garment jobs on a mean day, **346 on a peak
  day**, 3,000 per month.
- `docs/prd/assumptions-and-open-decisions.md` **OD-07** — the device, browser and printer matrix, which is **open**.

## Scope

- [ ] Seed a production-sized branch: 346 open garment jobs across the six phases of the published workflow, a realistic
      spread of assigned, unassigned, overdue, due-soon, blocked and held, built by a repeatable script committed under
      `scripts/` or by a documented `OrdersHarness` fixture. **Synthetic data only** — no real customer name, phone
      number or measurement, in any fixture or screenshot (CLAUDE.md section 5).
- [ ] Run `A11Y-RJ-03` steps 1 to 8 on a **physical phone** with a screen reader, against that data set, and fill the
      five empty columns of its table. Step 9 (the offline replay) is **#51's** and is recorded as not applicable with
      that reason. Step 2's wedge scanner is recorded against what E06-F02-10 shipped — typed entry into the
      manual-entry field, which is what a keyboard wedge emits — with the dedicated `WedgeSource` named as #36's.
      Steps 4 and 5 (material and shortage) are **#39's** and are recorded not applicable naming it.
- [ ] Run `A11Y-RJ-04` steps 1 to 5 on a **physical tablet** with a screen reader, and once at desktop width for
      `/workboard/planning`, and fill the same columns. Steps 6 to 9 (QC, defect evidence, rework, the rework timeline
      entry) are **#34's** and are recorded not applicable naming it.
- [ ] Run a screen-reader pass of the workflow version editor's **transition matrix** on a desktop browser, recorded
      against the administration family of section 5.11 with the reason stated. The matrix is the one control in this
      feature whose accessible names cannot be judged from an axe run alone.
- [ ] Lighthouse on the reference device profile, cold cache, for `/production`, `/workboard` and `/workboard/planning`
      at 346 rows, against the p75 ≤ 2.5 s Largest Contentful Paint budget of `docs/nfr/capacity-and-performance.md`
      line 211, with the bundle figure against the 180 KB gzip budget of line 332.
- [ ] Measure the three Orders reads — `GET /api/v1/orders/work-queue`, `/planning-board` and `/workload` — at the
      346-job volume against `docs/nfr/slo.md` row S2 and NFR-LT-01's p95 under 400 ms, and the three commands
      (start production, the phase transition, the assignment) against NFR-LT-02's p95 under 800 ms. **State the
      harness used.** If #52's load harness has not landed, a documented `hey`/`oha`/`k6` invocation against a local
      stack is acceptable **provided the report says so**, names the hardware, and records that the binding
      measurement under the signed SLO is still #52's and #58's.
- [ ] Write the findings up as defects: every failed checklist item becomes its own GitHub issue with its severity and
      its `A11Y-…` identifier, linked to this one and to the epic. **A failure here does not reopen the coding child**;
      it becomes a new, scoped issue, which is what `docs/nfr/a11y-checklist.md` section 6.2 expects.
- [ ] Record the device, browser and assistive-technology versions used. **OD-07 is open**, so the matrix this run
      covers is recorded as *what was tested* and not as *what is supported* — and the run sheet says that in one line,
      so a reader does not mistake it for the support matrix.

## Out of scope

Every line of production code and every change to a screen. Fixing what the run finds — each finding is its own issue.
The penetration test and the security regression suites (#56, whose `E14-F01b` children own them). The thermal-printer
and hardware-scanner evidence, which is #35's `e07-f01-9` and #36's. The cross-browser matrix and the accessibility
gate in CI, which are **#52's**. The k6 load harness itself, which is #52's and #58's. Signing off the SLO, which is
**#19's** and is a separate open issue — this run produces measurements against a **draft** target, which
`docs/nfr/slo.md` itself says is "not binding until section 13 is signed".

## Acceptance criteria

- [ ] `docs/nfr/a11y-checklist.md` `A11Y-RJ-03` and `A11Y-RJ-04` have every applicable step's Keyboard and Screen
      reader columns filled, every not-applicable step marked with the issue number that owns it (#51, #39, #34, #36),
      and no step left blank without one of the two.
- [ ] A run sheet per journey carrying the header fields section 1109 lists — journey identifier, device, browser,
      assistive technology, date, tester — committed alongside the checklist, with the data-set size stated.
- [ ] The Lighthouse reports for the three routes at 346 rows, with the Largest Contentful Paint p75 and the gzip bundle
      size quoted against lines 211 and 332, and a verdict per route.
- [ ] The latency report for the three reads and the three commands at 346 jobs, naming the harness, the hardware and
      the page size, with a verdict against NFR-LT-01 and NFR-LT-02 and an explicit sentence that both rows remain
      "proposed, to be confirmed" until #19 signs the SLO and #52/#58 land the harness.
- [ ] **Negative:** every failure is a linked issue with a severity, and this issue's own body lists them. A run that
      found nothing says so explicitly — "no items failed" is a result, and an empty Items-failed column with no such
      statement is an unfinished run.
- [ ] **Negative, and this is the one that protects the gate:** where a measurement could not be taken at all — no
      device, no harness, OD-07 unanswered — it is recorded as **not measured** with the reason and the owning issue.
      It is **not** recorded as a pass, and it is **not** written up as a waiver in `docs/process/waivers.md`:
      `docs/process/waivers.md` is for a gate that cannot be met, and "we have not run it yet" is not that. If the
      wave gate needs to proceed without a measurement, the waiver is the gate owner's decision and is raised
      separately with a date and an owner.
- [ ] **Exception:** a step that the shipped screens genuinely do not cover because another issue owns the behaviour is
      marked not applicable **with that issue named**, never left blank and never marked passed. A reader must be able
      to tell "we checked and it was fine" from "nobody could check this yet".

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| The seeded 346-job data set | **Synthetic only** (`docs/nfr/data-classification.md` 5.20; CLAUDE.md section 5) | Measure at peak volume | Destroyed with the environment | The tester | No production data is ever copied into a fixture, a screenshot or a recording |
| Screenshots and screen recordings in the run sheet | Internal, and **only** if every name in them is synthetic | Evidence a reviewer can check | With the issue | The team | Check every frame before attaching: a customer name or a phone number in a screenshot is a disclosure, and a job card shows a customer name by design |
| Device, browser and assistive-technology versions | Internal (5.18) | Say what was tested | With the run sheet | The team | Safe |
| Tester name | Personal, staff (5.15) | Attribute the run | With the run sheet | The team | A name in a committed document is fine; it is not a metric dimension and not a log field |
| Latency and Lighthouse figures | Internal (5.18) | Judge against the objectives | With the run sheet | The team | Safe. They are about routes, not about people |

No secret, no credential and no personal data about a real customer appears anywhere in this issue's artefacts. The
measurement environment is **not** production and no production data is copied into it (CLAUDE.md section 4 rule 10).

## Permissions and roles

No endpoint, no permission and no code. The tester signs in as seeded synthetic principals holding `orders.read`,
`orders.phase_transition`, `orders.assign`, `orders.start_production` and `catalog.workflows.edit` in turn, because
several checklist steps are about **what a role cannot reach** as much as what it can — the forbidden-state rows of
E06-F02-11b and E06-F02-13 are walked here with a real screen reader, not just asserted in Vitest.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| — | — | — | — | — | — | **None declared.** This issue publishes no route and changes no grant. The keys above are exercised through existing seeded roles; **OD-13** is open, so the grants are as `docs/security/permission-matrix.md` proposes |

## Modules, contracts and events

No module, no contract, no event, no migration and no client code. The only files changed are
`docs/nfr/a11y-checklist.md` and the run sheets and reports committed beside it. DOR-05: no new trust boundary and no
new surface — this issue **tests** the boundaries the coding children declared.

## Screens and states

Deleted: this issue builds no screen. It walks the five screens E06-F02-10, E06-F02-11, E06-F02-11b, E06-F02-12b and
E06-F02-13 shipped.

## Migration and rollback

Deleted: no schema change and no deployable artefact. Rollback is reverting a documentation commit.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| `OD-07` — the device, browser and printer matrix, including hardware scanners and label printers (`docs/prd/assumptions-and-open-decisions.md`, needed **before W0 exit**) | **Open** — raised 2026-09-03 | The run uses whatever devices the team has and the run sheet records them as *what was tested*, explicitly **not** as what is supported. When OD-07 is answered, a second run may be required and that is a new issue, not a reopening of this one | Before #52's cross-browser gate can be called complete |
| `#19 [E01-F03]` — production NFRs and SLOs | **Open issue**, and `docs/nfr/slo.md` says so itself: "Draft — proposed targets; nothing here is binding until section 13 is signed" | Measure against the draft targets and label every verdict as against a draft. Do not record a pass as satisfying a signed objective | Before a latency figure can be a release gate |
| `#52 [E12-F03]` — accessibility, cross-browser and mobile performance budgets | **Open issue.** `tests/e2e`, `playwright.config` and `tests/load` do not exist; `./scripts/dev` says the Playwright suite arrives with #52 | Measure by hand with a named harness and say so. Do not create `tests/e2e` or `tests/load` here — founding either is #52's, and a second foundation would have to be reconciled | Before these measurements are automated in CI |
| `#58 [E14-F03]` — observability and SLO alerting | **Open** | The figures here are a point-in-time measurement, not a monitor. The continuous measurement is #58's | Before an SLO is alerted on |

No number is invented: every threshold quoted comes from `docs/nfr/slo.md`, `docs/nfr/traceability.md` or
`docs/nfr/capacity-and-performance.md`, and where a document gives none — the due-soon window, a capacity limit — this
issue measures nothing and says so.

## Required verification

- [ ] The filled `A11Y-RJ-03` and `A11Y-RJ-04` tables, reviewed by somebody who did not run them.
- [ ] `./scripts/dev docs` clean, so every relative link in the amended checklist and the new run sheets resolves.
- [ ] The seeding script or fixture re-run once from a clean database, proving the data set is reproducible and
      therefore that a second run can be compared with this one.
- [ ] Non-functional rows touched: the accessibility rows of `docs/nfr/traceability.md` reached through
      `docs/nfr/a11y-checklist.md`, plus **NFR-LT-01** and **NFR-LT-02**, which move from "proof scheduled" to
      "measured against the draft target, pending #19 and #52" (DOR-13).
- [ ] Environment: a physical phone, a physical tablet and a desktop browser with a screen reader. **This is the
      reason the issue exists** — no part of it runs in a container, and no coding child of #33 waits on it.

## Evidence the pull request will carry

- [ ] The rendered diff of `docs/nfr/a11y-checklist.md` showing both journey tables filled.
- [ ] The run sheets, one per journey, with their header fields complete.
- [ ] The Lighthouse reports for the three routes, attached.
- [ ] The latency report with the harness, the hardware, the page size and the verdicts.
- [ ] Screenshots and recordings from the devices, each checked for synthetic data only.
- [ ] The list of defect issues raised, with their severities, or an explicit "no items failed".
- [ ] The one-line statement that OD-07 is open and that the tested matrix is not the support matrix.

## Depends on

E06-F02-8 — the three reads whose latency is measured.

E06-F02-10, E06-F02-11, E06-F02-11b, E06-F02-13 — the four run-time screens walked.

E06-F02-12b — the workflow editor, for the transition-matrix screen-reader pass.

E06-F02-5 and E06-F02-7 — the phase transition and the assignment, whose command latency is measured and whose refusal
sentences are heard.

**It depends on nothing being in flight, and nothing depends on it.** It is the last child of #33 and it is the only one
a coding agent should not be given.

Refs #33
