Sub-issue of #34. The QC half of the catalogue does not exist. `ServiceType.QcChecklistTemplateId`
(`src/Modules/Catalog/Tailor360.Modules.Catalog.Domain/Catalogue/ServiceType.cs:92`) is already declared, already
required by `HasEveryRequiredLink` (same file, the clause at line 140), already carried through
`CatalogPublicationCandidate.cs:116`, `ICatalogAvailabilityQuery.cs:194`, `CatalogRequests.cs:91`,
`CatalogPayloads.cs:171` and `CatalogProjection`, and already checked for presence by
`BuiltInCatalogValidator.cs:404` — and it points at nothing. `grep -rn "qc_checklist" src/` returns only the
`qc_checklist_template_id` **column** on `service_types` (created by `20260909054521_InitialCatalogSchema.cs:169`)
and its mirrors in the migration designers; there is no `qc_checklist_templates` table, no `qc_checklist_versions`,
no `qc_criteria` and no `defect_codes`. [`docs/architecture/module-ownership.md`](../../architecture/module-ownership.md)
assigns all four to the `catalog` schema in its catalog table list, and plan Section 8 (#34) makes them catalogue
configuration under **D8**.

This is the **first of three catalog slices** and it carries the schema. It creates all four tables in **one**
expand-only migration and serves the template and version *lifecycle*: create a template, list templates, read one,
and start a draft version. Editing a version's criteria and defect codes is E06-F03-1b; publishing, retiring, the
read contract Orders pins against and the seeded drafts are E06-F03-1c.

Why three and not one: the merged analogue is the design-catalogue slice (#137), which added four `catalog` tables
with the same draft/published/retired and cloning discipline and came to roughly three thousand production lines —
`CatalogVersion.Design.cs` 568, `CatalogHandler.Design.cs` 666, `DesignRuleValidator.cs` 537, `DesignPayloads.cs`
261, `CatalogDesignEndpoints.cs` 498, the migration 345. The Customers analogue is the same size:
`MeasurementTemplateHandler.cs` alone is 613 lines and `MeasurementTemplateEndpoints.cs` 504. One session cannot
carry that and stay under the 1,500-line target of `CLAUDE.md` section 6.

## What is already built (do not re-build)

- `…Catalog.Domain/Catalogue/CatalogVersion.cs` (774 lines) and `CatalogVersion.Design.cs` (568) — the
  draft → published → retired version aggregate, with the code rules in `CatalogCode.cs` (`MaximumLength` is 40)
  and the presentation-only edit window in `CatalogPresentation.cs`. Covered by `tests/Tailor360.UnitTests/Catalog/`
  and `tests/Tailor360.IntegrationTests/Catalog/CatalogEndpointTests.cs`.
- `src/Modules/Customers/Tailor360.Modules.Customers.Domain/Measurements/{MeasurementTemplate,TemplateVersion,TemplateField,TemplateStatus}.cs`
  (250 / 526 / 366 / 31 lines) with `Application/Measurements/MeasurementTemplateHandler.cs` — **the shape to
  copy**: a template that owns numbered versions, each draft → published → retired, each holding typed child rows.
  Mirror it; do not reference it (ARCH-004 — Customers' types may not cross into Catalog).
- `…Catalog.Application/Abstractions/ICatalogStore.cs` with
  `…Catalog.Infrastructure/Persistence/CatalogStore.cs` — the store shape, and
  `…Catalog.Application/Catalogue/CatalogHandler.cs` (1,042 lines) — the handler shape, including its audit action
  constants. The convention for an action name is `catalog.<entity>.<verb>`, as
  `CatalogHandler.Design.cs:25-49` spells out (`catalog.design_option.added`, `catalog.design_rule.changed` …).
  **Do not write `catalog.qc_checklist_created`**; write `catalog.qc_checklist.created`.
- `…Catalog.Infrastructure/Persistence/CatalogDbContext.cs` and
  `…Catalog.Infrastructure/Migrations/20260912041738_DesignCatalogue.cs` (345 lines) — the mapping conventions,
  `xmin` row versions, and the `published_design_options_are_immutable` trigger family to model the two new
  triggers on.
- `…Catalog.Api/Catalogue/CatalogVersionEndpoints.cs` (606 lines) — every route in this slice is declared the way
  its routes are declared, and `CatalogEndpoints.GroupPrefix` is `/api/v1/catalog` (`CatalogEndpoints.cs:16`).
- `src/Platform/Tailor360.Platform.Security/Permissions/CatalogPermissions.cs:36,39` — `catalog.checklists.edit`
  and `catalog.checklists.publish` already exist, and
  `src/Modules/Identity/Tailor360.Modules.Identity.Application/Access/SystemRoles.cs:106-107` already grants both to
  `owner`. **No permission key and no grant is added by any of the three catalog slices.**
- `clients/pwa/src/stories/fixtures/production.ts:44,81,89` — `QC_CHECKLIST`, `DEFECT_CODES` and `REWORK_PHASE`,
  design fixtures the Storybook journeys already use. They are fixtures, not data; the vocabulary they show is a
  sanity check on the model's shape and nothing more.

## How an endpoint is declared in this repository

There is one accepted shape and it is not optional. Copy it from
`src/Modules/Billing/Tailor360.Modules.Billing.Api/Invoicing/InvoiceEndpoints.cs` (a write at lines 190-196, a read
at 139-141), which is the closest merged example:

```
    .RequirePermission(<PermissionKey>, BranchScope.CurrentBranch)   // ARCH-007
    .ScopedToResource(<ResourceKinds.Thing>, "<routeValueName>")     // ARCH-023, when the route names a row
    .RequireRateLimiting(RateLimitPolicyNames.Write)                 // ARCH-017, exactly one
    .Audited(<Handler>.<Something>Action, reasonRequired: true)      // ARCH-008, every write
    .RequireIdempotency()                                            // where a client may retry
    .RequireIfMatch()                                                // where the row carries an ETag
    .WithRequestTimeout(RequestTimeoutPolicies.Command)               // every endpoint in the repository has one
```

- Verbs: `Platform.Security/Endpoints/EndpointAuthorisationExtensions.cs` (`RequirePermission(key, scope)`,
  `Audited(action, reasonRequired)`, `AllowAnonymousWithJustification`);
  `ResourceScopeEndpointExtensions.cs` (`ScopedToResource(resourceKind, routeValueName, scope)`, and the
  deliberately noisy exemption verb for a route whose identifier names nothing branch-owned);
  `RequestSafetyEndpointExtensions.cs` (`RequireIdempotency`, `RequireIfMatch`);
  `StepUpEndpointExtensions.cs` (`RequireStepUp`).
- **Rate-limit policies**, all of them, from `Platform.Security/Endpoints/RateLimitPolicyNames.cs`:
  `DefaultUser` = `default-user` (ordinary authenticated read), `Write` = `write` (authenticated state change),
  `ScanBurst` = `scan-burst`, `ExportHeavy` = `export-heavy`, plus `AuthenticationAnonymous`,
  `MultiFactorChallenge`, `RecoveryAnonymous`, `DefaultIp`. There is no other policy, and inventing one is a
  change to the limiter configuration in the web host.
- **Request timeouts**: `Platform.Security/Endpoints/RequestTimeoutPolicies.cs` — `Read` = `read`,
  `Command` = `command`, `Report` = `report`, `Export` = `export`. **Every** endpoint in `src/Modules/` carries one
  today (grep `WithRequestTimeout` — there is no exception), so an endpoint without one is the odd one out.
- **Branch scopes** from `Platform.Abstractions/Multitenancy/BranchScope.cs`: `CurrentBranch` (the default),
  `AssignedBranches`, `Organisation`, and the member that demands no branch reach at all.
- **Resource kinds**: each module keeps its own constants class — `BillingResourceKinds`,
  `MeasurementResourceKinds`, `DesignSelectionResourceKinds` — in its `Application` project, with an
  `IResourceScopeResolver` per kind in its `Infrastructure` project, registered with `TryAddScoped`.
- **The matrix row** goes in `docs/security/permission-matrix.md` section 5, *inside* the
  `<!-- matrix:endpoints -->` … `<!-- /matrix:endpoints -->` markers (lines 311-512 today), with the columns
  `| Method | Route | Declaration | Permission | Branch scope | Resource | Audited as | What it is for |`.
  `AuthorisationMatrixTests` fails and names any published route with no row, and any row with no route.

## Scope

- [ ] `Domain/Quality/QcChecklistTemplate.cs`, `QcChecklistVersion.cs`, `QcChecklistStatus.cs`
      (`Draft`, `Published`, `Retired`) and the two child types the migration needs mapped but this slice does not
      yet edit, `QcCriterion.cs` and `DefectCode.cs`, with `QcCriterionKind.cs` (`PassFail`,
      `MeasurementTolerance`, `FitCheck` — the three names plan Section 8 uses). Domain references
      `Tailor360.Platform.Abstractions` only (ARCH-001, ARCH-002). A published version is immutable: every mutator
      refuses unless the status is `Draft`.
- [ ] A template carries `code` (a `CatalogCode`, 40 characters), `name`, `name_tamil`, `description`,
      `category_key` and the usual `created_at/by`, `updated_at/by`; a version carries `version_number`, `status`,
      `responsible_role_key`, `evidence_required`, `published_at/by` and `retired_at/by`. A version number is
      unique within its template and allocated as the previous maximum plus one.
- [ ] `Application/Abstractions/IQcChecklistStore.cs`, `Application/Quality/QcChecklistHandler.cs` and
      `Infrastructure/Quality/QcChecklistStore.cs`, following `ICatalogStore`/`CatalogStore`/`CatalogHandler` in
      style: `If-Match` precondition through the aggregate's `xmin`, the module's existing problem mapping, and the
      audit action constants on the handler (`catalog.qc_checklist.created`,
      `catalog.qc_checklist_version.drafted`). Registered in
      `CatalogModuleServiceCollectionExtensions.AddCatalogModule` with `TryAddScoped`, beside lines 65-68.
- [ ] **One** expand-only migration, scaffolded as `QcChecklistTemplates`, creating **all four** tables in the
      `catalog` schema — `qc_checklist_templates`, `qc_checklist_versions`, `qc_criteria`, `defect_codes` — with
      `xmin` concurrency, `created_at/by`, `updated_at/by`, a unique index on `(organisation_id, code)` for a
      template and on `(qc_checklist_template_id, version_number)` for a version, and the two triggers
      `published_qc_checklist_versions_are_immutable` and `published_qc_criteria_are_immutable`, modelled on
      `published_design_options_are_immutable`. The criteria and defect-code tables are created here and written by
      E06-F03-1b, which is why **this is the only catalog migration the three slices need** — 1b and 1c add none.
      Add the register row to [`docs/dev/migrations.md`](../../dev/migrations.md) with the evidence that `Down` ran
      (`src/Modules/CLAUDE.md` section 6).
- [ ] Four routes under `/api/v1/catalog`, each with `CatalogPermissions.EditChecklists` and
      `BranchScope.Organisation` — the scope `CatalogVersionEndpoints.cs:83` uses for every catalogue admin route —
      exactly one rate-limit policy, the audit filter on each write, `.RequireIdempotency()` on each `POST`, and
      `.WithRequestTimeout(...)`:
      `POST /qc-checklists` · `GET /qc-checklists` · `GET /qc-checklists/{templateId}` ·
      `POST /qc-checklists/{templateId}/versions`.
      None of the four needs `.ScopedToResource(...)`: `{templateId}` names an **organisation**-scoped
      configuration row, not a branch-owned one, so ARCH-023 is satisfied by the recorded exemption verb of
      `ResourceScopeEndpointExtensions.cs` with the same reasoning
      `CustomerEndpoints.cs:62` records — state that reason in the code, do not leave the rule unmentioned.
- [ ] `Api/Payloads/QcChecklistPayloads.cs` and `QcChecklistRequests.cs` following `CatalogPayloads.cs` (370 lines)
      and `CatalogRequests.cs` (141): public payload types live in the `Api` project and no `Domain` type is ever
      returned (ARCH-013).
- [ ] Rows for all four routes in the section 5 endpoints block of
      [`docs/security/permission-matrix.md`](../../security/permission-matrix.md), and the `identifier-editing` rows
      for `{templateId}` in `tests/Tailor360.IntegrationTests/Authorization/matrix.yaml`.

## Out of scope

Criteria, defect codes and the version content editor — **E06-F03-1b**. Publish, retire, `IQcChecklistQuery`, the
`catalog.qc-checklist-not-published` publication finding and the seeded drafts — **E06-F03-1c**. Everything in the
`orders` schema — E06-F03-2a onwards. A **checklist-administration screen**: no acceptance criterion of #34 needs
one, the seeded drafts and these routes are what #34 consumes, and the catalogue administration shell that would
host it is #141 and #142's; a checklist editor is a follow-on to those and is not carried anywhere in this parent.

## Acceptance criteria

- [ ] An Owner creates a checklist template over HTTP, lists templates, reads one back with its versions, and starts
      a first draft version whose `version_number` is 1; a second draft on the same template is numbered 2.
- [ ] Every route declares `catalog.checklists.edit` with `BranchScope.Organisation` (ARCH-007), exactly one
      rate-limit policy (ARCH-017), the audit filter on each write (ARCH-008), and either a resource scope or the
      recorded exemption (ARCH-023); the endpoint-inventory contract test shows each one.
- [ ] **Negative**: a caller holding `catalog.read` but not `catalog.checklists.edit` receives `403`
      `security.forbidden`; a stale `If-Match` is refused `412` before any write; a duplicate template `code` within
      the organisation is refused with a named Catalog problem code and no row is written; a replayed
      `Idempotency-Key` returns the first outcome rather than creating a second template.
- [ ] **Exception**: a template that already has a draft version refuses a second *draft* — one draft at a time,
      the rule `CatalogVersion` already applies to catalogue versions — and the refusal names the existing draft's
      version number rather than failing blankly.
- [ ] **Exception**: the two immutability triggers refuse an `UPDATE` of a published version and of a criterion
      belonging to one, issued directly from the application role, even though no route in this slice can reach
      either.

## Data and classification

The catalogue and its configuration have **no section 5 category** in
[`docs/nfr/data-classification.md`](../../nfr/data-classification.md) — its section 5.1 index runs 5.2 to 5.20 and
none of them covers configuration. The class therefore comes from the class table in **section 4**, whose
**Internal** row reads "Catalogue and workflow definitions …", with the retention row "Life of the configuration
version". Cite that, and do **not** cite section 5.6, which is QC, custody and delivery *evidence* and is
Sensitive Personal. Say in the pull request that no section 5 row was added and why, so the gap the index invites a
reviewer to find is answered rather than left.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| `catalog.qc_checklist_templates` (code, name, name_tamil, description, category_key) | Internal — section 4 class table, "Catalogue and workflow definitions" | Name the checklist a service type is judged against | Life of the configuration version (section 4) | `catalog.read` holders; edited by `catalog.checklists.edit` | Codes and identifiers may appear in logs and operational exports; no personal data exists on these rows |
| `catalog.qc_checklist_versions` (version_number, status, responsible_role_key, evidence_required, published_at/by, retired_at/by) | Internal | The immutable unit a QC result pins | As above | As above | Safe. `published_by` and `retired_by` are staff identifiers, handled under section 5.15 |
| `catalog.qc_criteria`, `catalog.defect_codes` | Internal | Created here, written by E06-F03-1b, classified in that issue | As above | As above | Safe |
| Audit actions `catalog.qc_checklist.created`, `catalog.qc_checklist_version.drafted` | Internal — evidentiary | Prove who changed the standard and when | Per section 5.16 | `auditor`, `owner` | Audit store only |

No new media class, no credential, no secret, no export column and no telemetry attribute.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `catalog.checklists.edit` | organisation | no | no | no | `owner` (Owner) | **Exists** — `docs/security/permission-matrix.md` line 158, attributed to #34, "Quality checklists are drafted alongside the workflow they check". Granted `SystemRoles.cs:106` |
| `catalog.read` | branch | no | no | no | `owner`, `admin`, `branch_manager`, `reception`, `measurement_staff`, `tailor_master`, `tailor`, `cashier` (matrix line 163) | **Exists** — unchanged; no route in this slice uses it |

`catalog.checklists.publish` is **not** used by this slice — it belongs to E06-F03-1c, which owns publish and
retire. **OD-13** (`docs/prd/assumptions-and-open-decisions.md` line 66) is open, so the grant above is the seeded
default rather than an approved one; this issue changes none of them.

## Modules, contracts and events

Catalog owns the schema and is the only module touched. No `Contracts` project of any module is consumed or published
by this slice — `IQcChecklistQuery` is E06-F03-1c's. No integration event is added: a checklist version's publication
is not a catalogue publication, and `catalog.catalog-version-published.v1` already announces the latter. Threat-model
coverage is **DOR-05's pre-#56a alternative**, not a written model:
[`docs/security/README.md`](../../security/README.md) section 3 records that only the authentication model exists (#23)
and that `abuse-cases.md` is **not written**, with the rest scheduled under #56a — the order-workflow model
specifically in the batch-1 child `E14-F01a-4`. So the abuse case is **named here and carried into that backlog
entry**: the catalogue administration flow carries the configuration-tampering abuse case, and
this slice adds no trust boundary, only rows behind an existing organisation-scoped permission.

## Screens and states

Deleted: this is a backend slice and adds no screen — see **Out of scope** for where a checklist editor would live.

## Migration and rollback

One expand-only migration, `QcChecklistTemplates`, creating four new tables and altering none. The previous release
tolerates it because nothing reads or writes the tables — `ServiceType.QcChecklistTemplateId` is already a plain
`Guid?` with no foreign key and stays one, so no existing row is invalidated and the link is still validated in
application code by `BuiltInCatalogValidator` rather than by the database. Rollback is redeploying the previous tag
with the four tables unused; `Down` drops them and the two triggers and must be executed at least once before merge
(`src/Modules/CLAUDE.md` section 6), proved by extending
`tests/Tailor360.IntegrationTests/Catalog/CatalogMigrationTests.cs` — which already migrates a scratch database up,
down and up for the design tables — with the same walk over this one. Per
[`docs/dev/migrations.md`](../../dev/migrations.md), with a register row in it.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-10** initial catalogue, templates, design options and **QC checklists**, reviewed from the seeded drafts | **Open** — `docs/prd/assumptions-and-open-decisions.md` line 63 | Nothing is seeded by this slice and nothing is published; it ships the mechanism only. E06-F03-1c seeds drafts | W2 per the decision's own row |
| **OD-13** approved role-to-permission grants | **Open** — line 66 | `catalog.checklists.edit` keeps the grant `SystemRoles.cs:106` already seeds; this issue proposes no change | W1 exit (already shipped under a documented default) |

## Required verification

- [ ] unit: the template and version lifecycle — a draft created, numbered, refused a second draft; every mutator on
      a published version refused; `QcChecklistStatus` and `QcCriterionKind` parsing rejecting a number that names
      no member (the defect #90 fixed elsewhere); `CatalogCode` validation on the template code.
- [ ] integration: the four routes end to end against real PostgreSQL, including the **negative** cases named above —
      `403` without the edit permission, `412` on a stale `If-Match`, duplicate code refused, idempotency replay —
      and the two immutability triggers refusing an `UPDATE` from the application role.
- [ ] contract: OpenAPI lint and diff over the four routes; the endpoint inventory shows a permission, one rate
      limit, a request timeout and an audit action for each write; `AuthorisationMatrixTests` green with the new
      section 5 rows.
- [ ] migration: `CatalogMigrationTests` up/down/up over this migration, and
      `dotnet ef migrations has-pending-model-changes` clean.
- [ ] NFR rows touched: **NFR-MQ-05** ("Configuration, not code: … quality-control checklists …") in
      [`docs/nfr/traceability.md`](../../nfr/traceability.md) line 276, which lists #34 as a proof contributor;
      read latency against the **S2** row of [`docs/nfr/slo.md`](../../nfr/slo.md) line 130 for the list route, with
      the measured figure recorded rather than the target restated.
- [ ] environment: `./scripts/dev doctor` first, to confirm the integration tier can run here; `CI=true` turns a
      skipped integration tier into a failure, so a skip is not an option (DOR-16).

## Evidence the pull request will carry

- [ ] `./scripts/dev test unit`, `architecture`, `contract` and `integration` output, run twice.
- [ ] `dotnet run --project src/Tools/Tailor360.Cli -- migrate --dry-run` then `migrate` output, and the up/down/up
      log from `CatalogMigrationTests`.
- [ ] The OpenAPI difference report for the four new operations, and `pnpm --dir clients/pwa generate:api:check`
      proving `clients/pwa/src/api/schema.d.ts` was regenerated.
- [ ] The new `permission-matrix.md` section 5 rows and the `matrix.yaml` diff, with the
      `AuthorisationMatrixTests` run that accepts them.
- [ ] An HTTP transcript creating a template and drafting two versions, synthetic data only, beside the audit rows.
- [ ] `dotnet format --verify-no-changes` clean.

## Depends on

**Nothing unmerged.** The catalogue version aggregate, `CatalogStore`, `CatalogHandler`, `CatalogMigrationTests`,
both checklist permission keys and both grants are merged today, which is what makes this the first child of #34 and
one of only two that can start immediately.

In flight on the same files: #91 (the INV-MTV-06 reconciliation) touches `CatalogReconciler.cs` and
`reference_breaches`, not the checklist tables; whichever merges second rebases and re-runs the Catalog integration
tier. #141 and #142 touch the catalogue **client** only.

Refs #34
