Sub-issue of #36, and the last of them. It is the parent's fifth acceptance criterion — "**Critical scan flows pass
on approved real-device/browser matrix**" — and the two items of its Required verification that no automated tier can
answer: "Real Android/iPhone/iPad and hardware-scanner tests" and "Accessibility and one-handed/touch usability
review". E07-F02-1 to E07-F02-5, inclusive of -2b and -3b, build the instrument; **this issue takes the readings.**

**Where this rehearsal stops, and why that is not a gap.** A scan that *changes custody* is a `POST
/api/v1/custody/scans` command, and that endpoint is **#37's**; #36 is sequenced before #37 in wave W3, and
E07-F02-3b ships the command port with a deliberately fail-visible default. So this rehearsal records the flow
**up to the commit boundary** — decode, resolve, confirmation card or undo window, feedback and announcement, by
camera, by hardware wedge and by manual entry — and the **committed transition is recorded by #37's own physical
rehearsal record** (its Section 6.3 row names exactly that). Where #37 has already merged when the sitting happens,
record the committed action as well and say so in the record. Where it has not, the record says "commit not
exercised — endpoint arrives with #37" rather than leaving a cell blank or implying a transition that never
happened.

> **This is not a coding session.** It is a rehearsal performed by a person with the shop's real hardware, and a
> coding agent cannot complete it: it needs an Android phone, an iPhone, an iPad, a counter tablet, a desktop
> browser on each of the three engines, a USB keyboard-wedge scanner, a Bluetooth keyboard-wedge scanner, printed
> synthetic label sheets, and a screen reader per platform (TalkBack, VoiceOver, NVDA). An agent may prepare the
> pull request's **shape** — the record directory, the record template, the index with one row per combination
> marked `pending`, and the unanswered `A11Y-SC` answer rows — and must then stop. **A result that was not observed
> is never written.** A fabricated row here is the one kind of evidence this project cannot recover from: every later
> release gate, and the W3 exit gate "labels printed and scanned on real devices", reads these records as proof that
> the shop's scanning works.

The gap is checkable and is structural rather than accidental: `docs/nfr/a11y-checklist.md` section 5.3 declares
**A11Y-SC-01 to A11Y-SC-11**, and its own answer table at lines 1282-1296 has every `Note`, `Severity` and `Defect`
cell **empty**, with the applicable pass pre-marked per row (blank means the pass applies, `—` means it does not).
`docs/nfr/support-matrix.md` section 9 requires "A recorded walkthrough per Tier 1 device class, in a browser tab
**and** in installed mode, with the device, operating system and browser versions logged", and no such record exists
anywhere in the repository.

## What is already built (do not re-build)

- **E07-F02-1, -2, -2b, -3, -3b, -4 and -5** — the whole scanning surface: `clients/pwa/src/scanning/` with the
  parser, the debounce, `KeyboardWedgeSource`, the camera engine and its permission ladder,
  `clients/pwa/src/routes/scanning/ScanRoute.tsx` at `scan` in single and continuous modes with the confirmation card
  and the undo window, the manual rungs with their reason, and
  `clients/pwa/src/routes/scanning/ScannerCheckRoute.tsx` at `settings/device`. **Nothing in this issue changes any
  of it** — a defect found here becomes a new issue linked to #8, per CLAUDE.md section 6's scope discipline.
- `clients/pwa/src/routes/scanning/ScannerCheckRoute.tsx` (E07-F02-2b) is the instrument for most of this work: it
  shows raw and normalised payload, namespace, check-character validity, source and decode latency **without resolving
  or committing**, which is exactly what a device-setup reading needs and why plan Section 8 for #36 describes it as
  being "used for device setup, print-quality checks and support".
- `docs/nfr/a11y-checklist.md` — section 5.3 for the eleven `A11Y-SC` questions, section 5 generally for the
  `K` / `SR` / `Env` pass vocabulary, line 327 for the fixture this rehearsal needs ("A printed synthetic label sheet
  carrying one pre-made payload per rule — wrong namespace, bad check character, unknown identity, wrong branch,
  wrong custodian. The check-character payload is generated by the seed tool, **never hand-crafted**"), line 296 for
  the per-platform Settings paths already written out, and the answer tables at lines 1282-1296.
- `docs/nfr/support-matrix.md` — section 2 (the lowest supported device and the performance reference), section 4 and
  4.1 (the engine matrix and the WebKit constraint on iOS), section 5 (orientation), section 6 (the capability matrix
  per class), section 7.1 (the scanning ladder), section 8 (assistive-technology pairings) and section 9 (how the
  matrix is enforced). **The matrix is the test plan; this issue does not invent a second one.**
- **E07-F01-8** (planned sibling of #35, **not yet filed as an issue**) — creates `docs/custody/`,
  `docs/custody/labels.md`, the `docs/custody/label-tests/` directory with its `README.md` index and `TEMPLATE.md`
  record, the committed sheets under `docs/custody/label-tests/sheets/`, and the repeatable `dotnet run --project
  src/Tools/Tailor360.Cli -- label-sheet` command. **Copy that convention; do not invent a second one**, and use its
  sheets rather than printing ad-hoc labels. Its invalid-payload sheet carries a bad check character, a wrong
  namespace (`S-`, `I-`, `R-`), a malformed string, the confusables and **a well-formed payload allocated to
  nothing** — which is three of A11Y-SC-05's five rules plus the confusable case, and **not** wrong branch or wrong
  custodian; those two are data conditions, not printing conditions, and the scope below says where each comes from.
- **E07-F01-9** (planned sibling of #35) — the printed-label rehearsal. Its records and this issue's share devices
  and should be run in the same sitting where the shop's hardware allows, but they answer different questions: that
  issue asks whether a label **prints** acceptably, this one asks whether it **scans** acceptably through each
  source on each class.

## What is missing

- No record of any real-device scan, anywhere. `docs/nfr/` contains only documents (`a11y-checklist.md`,
  `accessibility-localisation.md`, `capacity-and-performance.md`, `data-classification.md`, `reviews/`,
  `risk-review.md`, `security-operations-targets.md`, `slo.md`, `support-matrix.md`, `traceability.md`) and no
  evidence directory; `docs/evidence/`, which plan Section 6.4 names for epic closure, does not exist yet either.
- No answered `A11Y-SC` row. Eleven questions, zero answers.
- **SM-04** — which wedge-scanner models exist and whether each station is USB or Bluetooth — is an open decision
  (`support-matrix.md` section 10, "Before W3 — feeds #36"), so the nominated models are an input this rehearsal
  records rather than assumes.

## Scope

There is almost no production code here: the deliverable is **observed fact, recorded**. Estimated production lines:
under 50, and all of them Markdown scaffolding.

- [ ] **`docs/custody/scan-tests/README.md`** — the method and the index, in the shape E07-F01-8 establishes for
      `docs/custody/label-tests/README.md`: how a run is set up, which combinations must be covered, and an index
      table with one row per combination, each initially `pending` and each resolving to a record file. The
      combinations come from `support-matrix.md` sections 4 and 6, not from this issue's judgement: each Tier 1
      device class × each scan source the matrix marks Yes or Fallback for it, in a browser tab **and** in installed
      mode (section 9).
- [ ] **`docs/custody/scan-tests/TEMPLATE.md`** — the empty record shape: device, operating system version, browser
      and version, installed or tab, scanner model and connection where one is used, label sheet and printer used,
      lighting, the decode path actually taken (`BarcodeDetector` or ZXing, read off the Scanner check screen), the
      observed decode latency range, the number of attempts per successful read, whether torch and zoom were exposed,
      and the one-handed observations. Every field has a unit or an enumeration; none is a free-text verdict.
- [ ] **Take the readings**, one record file per index row, filled from observation, with the index row moved from
      `pending` to the record. A combination that could not be run is recorded as **not run, with the reason** — never
      as a pass, and never omitted.
- [ ] **Record the observed scan round trip as evidence, not as a target.** `docs/nfr/slo.md` S4 sets p95 under 1 s
      on Blink and under 1.5 s on WebKit, derived in `docs/nfr/capacity-and-performance.md` section 3.2 from a 400 ms
      decode allowance (900 ms on WebKit); **NFR-LT-03** in `docs/nfr/traceability.md` names "a real-device rehearsal
      on the shop network" as its evidence and schedules the proof against #36, #52 and #58. Record the decode
      latency read off the Scanner check screen and, where the resolve is reachable, the resolve round trip, against
      those published figures. **Set no new figure**: the k6 scenario and the field telemetry are #52's and #58's.
- [ ] **Answer `A11Y-SC-01` to `A11Y-SC-11`** in `docs/nfr/a11y-checklist.md`'s answer table at lines 1282-1296,
      filling the pre-marked `K` and `SR` cells with Pass, Fail or N/A plus the `Note`, `Severity` and `Defect`
      columns. A `Fail` is opened as a defect issue with the `bug` label and its number written into the `Defect`
      column; it is **not** fixed in this pull request.
- [ ] **The one-handed and touch usability review**, recorded against the concrete commitments of plan Section 8 for
      #36: viewfinder in the upper 60%, every control in the bottom bar within thumb reach and ≥ 48 × 48 px, no
      swipe-only and no long-press-only action. Record the measured reach on the largest and the smallest Tier 1
      phone, and whether a glove or a damp hand changes the answer — the shop floor is the environment this screen
      lives in.
- [ ] **The wedge-scanner reading that only hardware can give**: a real scan arriving while focus sits in an
      unrelated text field leaves the typing alone (**A11Y-SC-03**, verified in Vitest by E07-F02-1 and here by a
      real device), a Bluetooth scanner on iOS suppressing the on-screen keyboard does not hide the manual field
      (`support-matrix.md` section 7.1), and the terminator each nominated model actually emits.
- [ ] **Record the nominated hardware against SM-04** in the README, and note in
      `docs/prd/assumptions-and-open-decisions.md` only if the pull request that closes **OD-07** has not already
      done so — do not re-open a decision this rehearsal merely informs.
- [ ] **Link the index** from `docs/nfr/support-matrix.md` section 9's "Real-device evidence" row, so the matrix
      points at its own evidence, and from the epic closure file when `docs/evidence/e07-closure.md` is written
      (plan Section 6.4). Creating `docs/evidence/` is **not** this issue's.

## Out of scope

Every line of scanning code (**E07-F02-1** to **E07-F02-5**, inclusive of **-2b** and **-3b**); fixing anything
this rehearsal finds — each becomes its
own issue; the printed-label quality readings (**E07-F01-9**); the cross-browser Playwright suite, the Lighthouse
budgets and the full `tests/e2e` project (**#52**, which is where `playwright.config.ts` first exists); the
client-telemetry transport that will make fallback-rung usage measurable in the field (**#52** and **#58**); the
offline-queue readings (**#51**); the pen test and the security regression suite (**#56**).

## Acceptance criteria

- [ ] Every index row is either a record file with observed readings or an explicit **not run** with a reason. No row
      stays `pending` and no row is deleted.
- [ ] The same domain action is recorded as **reaching the commit boundary identically** through **camera**,
      **hardware wedge scanner** and **manual entry** on at least one Android phone, one iPhone or iPad and one
      desktop engine: the same payload resolves to the same job, the same confirmation card appears with the same
      job number and expected action, and the only recorded difference is the `source`. That is the parent's first
      acceptance criterion as far as #36 can observe it; the committed transition through each source is #37's
      physical rehearsal record, and each record here says which of the two it contains.
- [ ] A record exists for each Tier 1 class in **both** a browser tab and installed mode, as `support-matrix.md`
      section 9 requires, with device, operating-system and browser versions logged.
- [ ] **Negative:** A11Y-SC-05's five rejections are each presented, and the record names for each one the
      announcement the screen reader actually made and the remedy offered — but the five do **not** all come from a
      sheet, and the record says where each came from:
      **wrong namespace**, **bad check character** and **unknown identity** from E07-F01-8's printed invalid-payload
      sheet; **wrong branch** from a garment job confirmed in a *different* branch and scanned by an account scoped
      to this one, which is #194's branch-scoped refusal; and **wrong custodian**, which is a refusal of the scan
      **command** and therefore **#37's** — recorded as "not exercised — arrives with #37" when #37 has not merged.
      A sheet whose check-character payload was hand-crafted rather than generated by the tool is not acceptable
      evidence (`a11y-checklist.md` section 3.7, the "Five distinct scan rejections" row); if E07-F01-8's sheet is
      missing one of its three, that is raised against **E07-F01-8** rather than improvised here.
- [ ] **Negative:** a camera denial is exercised on each platform by actually refusing the permission in the browser,
      and the recorded outcome is that the operator completed the action by another rung — the parent's second
      acceptance criterion, and the one that cannot be faked because the platform Settings path either works or does
      not.
- [ ] **Exception:** a damaged or unreadable label is exercised deliberately — fold, smudge or tear one sheet
      label — and the record shows the by-job-number rung completing the action, which is exception **EX-07**.
- [ ] **Exception:** a device class the matrix marks **Fallback** for a source behaves as the ladder says — a
      camera-less desktop is never prompted for camera permission, a print station falls back — and the record says
      so rather than leaving the cell blank.
- [ ] All eleven `A11Y-SC` rows are answered, with every `Fail` carrying a defect number.
- [ ] No code under `src/` or `clients/pwa/src/` is modified by this pull request — `git diff --stat` proves it.

## Data and classification

The records are **Internal**, per `docs/nfr/data-classification.md` section 5.8: a barcode payload is opaque and
carries no personal data (plan D9), which is why it may be printed, logged and committed to a record. Two traps are
worth stating because a rehearsal is where they get broken.

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Record files under `docs/custody/scan-tests/` | Internal | Prove the shop's devices scan the shop's labels | Kept with the repository as release evidence | Anyone with repository access | Committed to the repository. **No screenshot may contain a real customer name, phone number, measurement or address**: every reading uses synthetic data (CLAUDE.md section 5), and a photograph of a real job card is not evidence, it is a disclosure |
| Device and browser versions, scanner models | Internal | The matrix of `support-matrix.md` sections 4 and 9 | As above | As above | Committed. A **personal** device's name or owner is not recorded — **SM-09** proposes shop-issued devices for Tier 1, and a personal phone's identifying label stays out of the record |
| Observed decode latency and attempt counts | Internal | Inform the budgets of `capacity-and-performance.md` | As above | As above | Committed as figures. They are observations, **not** new targets: a target that is not in `docs/nfr/slo.md` or `capacity-and-performance.md` is not created by this issue |
| Screen-reader transcripts | Internal | Answer **A11Y-43**, whose evidence is a transcript rather than a verdict (`a11y-checklist.md` section 3.8), and to support any `A11Y-SC` answer that is not a plain Pass | As above | As above | Committed in the `Note` column or the record file, quoting what was said. A transcript of a synthetic job only — a transcript naming a real customer is a disclosure |

## Permissions and roles

No permission is added, no endpoint is touched, and no matrix row changes. What this issue **does** need is a test
account per role it exercises: the recorded walkthroughs use a `tailor` account (camera and wedge, continuous mode),
a `reception` account (manual lookup with a reason) and a `branch_manager` account (the reprint action offered on a
superseded identity). All three hold `custody.scan`; `reception`, `tailor` and `branch_manager` hold
`custody.manual_lookup` per `docs/security/permission-matrix.md` line 172.

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `custody.scan` | branch | no | no | no | `branch_manager`, `reception`, `tailor_master`, `tailor`, `delivery_staff` | **Exists** — `permission-matrix.md` line 180 |
| `custody.manual_lookup` | branch | no | no | **yes** | as above | **Exists** — `permission-matrix.md` line 172 |

**OD-13** is open, so the grants exercised are the documented default. Accounts come from
`dotnet run --project src/Tools/Tailor360.Cli -- seed-synthetic`, which today covers **Identity and Customers only**,
so the garment jobs the rehearsal scans come from #201's confirmation path run by hand — record how they were
created, because the next rehearsal needs to repeat it.

## Modules, contracts and events

Documentation only: no module, no schema, no migration, no contract, no event. The modules whose behaviour is being
observed are Custody (#194's resolve and by-number endpoints) and the client. Threat-model coverage (DOR-05): the
rehearsal is where the client-side-only controls get tested against reality — a wrong-namespace label physically
presented to a real camera, a denial actually set in platform Settings — which is the evidence
`docs/architecture/sequences/barcode-handoff.md` section 2's "a client-side checksum pass is never trusted" asks for
in the field rather than in a unit test.

## Screens and states

No screen is added or changed. DOR-14 is answered by E07-F02-2b to E07-F02-5, each of which ships its own states
and its own axe run; **this issue answers the part of DOR-14 no automated check can** — the manual `K` and `SR`
passes of `a11y-checklist.md` section 5.3, on the real assistive technologies paired in `support-matrix.md`
section 8.

## Migration and rollback

No table, no column and no code. `migrate --dry-run` reports no outstanding migration. There is nothing to roll back:
the artefact is a record.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-07** device, browser and printer matrix | **Open** — `docs/prd/assumptions-and-open-decisions.md` section 3, needed before W0 exit | `docs/nfr/support-matrix.md` sections 2, 4, 5, 6 and 7.1 are treated as the approved matrix for the purposes of this rehearsal, and the records say so, so that a later approval either confirms them or names what must be re-run | Before the W3 exit gate |
| **SM-04** hardware scanner models and USB versus Bluetooth per station | **Proposed, to be confirmed** — `support-matrix.md` section 10, "Before W3 — feeds #36" | Keyboard-wedge HID only; at least one model nominated and tested. The nominated models are recorded in the README as the input they are | Before the W3 exit gate |
| **SM-02** / **SM-03** operating-system floors and browser version policy | **Proposed, to be confirmed** | The proposed positions of `support-matrix.md` section 10 rows SM-02 and SM-03 | Before W0 exit |
| **SM-09** personal phones | **Proposed, to be confirmed** | Shop-issued devices for Tier 1; a personal device is not identified in a record | Before W1 exit |
| **SM-08** manual-entry review threshold | **Proposed, to be confirmed** — above 2% of a branch's scans over seven days | Recorded as a proposal only. This rehearsal measures **attempts per successful read** as an observation and sets **no** threshold in code; the field measurement needs #52's telemetry transport | Before go-live |

No figure observed here becomes a target. A threshold, a latency budget or an attempt limit with no row in
`docs/nfr/slo.md` or `docs/nfr/capacity-and-performance.md` is a product decision and belongs in
`docs/prd/assumptions-and-open-decisions.md`.

## Required verification

This issue **is** the verification tier the others cannot reach, and it is the child that carries #36's hardware
obligation: `docs/process/definition-of-ready.md` line 86 makes **DOR-16 physical hardware** mandatory for "#35,
#36, #37, #48 and #61c always", so the hardware list at the top of this issue is the DOR-16 answer for the whole
feature and the five coding children inherit it by pointing here. `./scripts/dev doctor` is still run and recorded,
because the rehearsal needs a working local stack: PostgreSQL for the resolve endpoint, and a device on the
same network reaching the web host **over HTTPS**, since `getUserMedia` requires a secure context
(`support-matrix.md` section 7.1, "Secure origin"). Say in the pull request how that origin was provided. NFR
traceability (DOR-13): the rows of `docs/nfr/traceability.md` pointing at `support-matrix.md` sections 6, 7.1 and 9,
and at `a11y-checklist.md` section 5.3.

- [ ] Manual, per index row: the recorded walkthrough, in a tab and installed, with versions logged.
- [ ] Manual, screen reader: TalkBack with Chrome, VoiceOver with Safari and NVDA with Chrome or Edge — the three
      Tier 1 pairings of `support-matrix.md` section 8, set up per `a11y-checklist.md` sections 3.1 to 3.3 —
      answering the `SR` rows of section 5.3. Section 3.8 lists the items whose evidence **is** a transcript, and no
      `A11Y-SC` item is among them; **A11Y-43** (announced once, not doubled) is, and it applies to every scan
      announcement, so capture that transcript. For the `A11Y-SC` rows, quote what was said in the `Note` column
      anyway wherever the answer is not a plain Pass — "sounded right" is not an answer.
- [ ] Manual, keyboard: the `K` rows, including **A11Y-SC-01** (manual entry reachable without the camera),
      **A11Y-SC-03** (a real wedge scan not disturbing typing), **A11Y-SC-06** (`Escape` closes the overlay and
      focus returns) and **A11Y-SC-10** (the scan completes with no torch and no zoom).
- [ ] Manual, negative: the five printed rejections; a refused camera permission on each platform; a deliberately
      damaged label resolved by job number.
- [ ] Regression guard: `pnpm --dir clients/pwa test` and `pnpm --dir clients/pwa lint` are still run, to prove this
      pull request changed no behaviour.

## Evidence the pull request will carry

- [ ] The completed `docs/custody/scan-tests/` index with no `pending` row, and one record file per combination.
- [ ] Photographs or screen recordings per Tier 1 class showing a successful scan through each source available to
      it, with the device visible — synthetic data only.
- [ ] The answered `A11Y-SC-01` to `A11Y-SC-11` rows as a rendered diff of `docs/nfr/a11y-checklist.md`, with a
      defect number beside every `Fail`.
- [ ] The screen-reader transcripts for the items that ask for one.
- [ ] The one-handed reach measurements on the largest and smallest Tier 1 phone.
- [ ] The nominated scanner models and their emitted terminators, recorded against **SM-04**.
- [ ] `./scripts/dev doctor` output and a statement of how the HTTPS origin was provided to the devices.
- [ ] `git diff --stat` showing no change under `src/` or `clients/pwa/src/`.

## Depends on

**E07-F02-1**, **E07-F02-2**, **E07-F02-2b**, **E07-F02-3**, **E07-F02-3b**, **E07-F02-4** and **E07-F02-5** — all
merged. There is nothing to rehearse until the ladder is complete, and a partial rehearsal would have to be
repeated.

**E07-F01-8** (planned sibling of #35) — creates `docs/custody/`, the `label-tests` record convention this issue
mirrors, and the printed synthetic sheets, including the generated bad-check-character payload that
`a11y-checklist.md` line 327 forbids hand-crafting.

#194 — **[E07-F01-3]**, open: the resolve and by-number endpoints the readings exercise.

#201 — **[E06-F01-2]**, open: confirms the garment jobs that have identities to scan. `seed-synthetic` covers only
Identity and Customers today, so the setup is manual and must be written down.

Not blockers, but named so the sequencing is visible: **#37** owns the scan command and therefore the committed
half of every flow here, in its own physical rehearsal record; **#52** owns the Playwright suite and the
cross-browser automation that will eventually repeat part of this by machine; **#51** owns the offline readings; and
**E07-F01-9** is the printed-label rehearsal that shares the same hardware session.

Contended file: `docs/nfr/a11y-checklist.md` — its answer tables are filled by several issues (#50's and #52's
screens, and the E14 children), so **one session at a time**, and whoever merges second rebases.

Refs #36
