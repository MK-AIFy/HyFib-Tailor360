Sub-issue of #40. `E08-F03-7` values stock at a cut-off, records each run immutably with the method it used, and
publishes `IValuationQuery`; `E08-F03-11` builds the two report screens and the one transport helper a CSV download
needs. This child is the last screen #40 owes: **valuation**, which is the only screen in the parent that shows money.

It is separate from `E08-F03-11` for two reasons. The first is size: that child is two tables plus a change to
`clients/pwa/src/auth/apiClient.ts`, the file every request in the application goes through, and four screens in one
branch would put it well past one session — `CashierSessionRoute.tsx` is 440 production lines for one comparable
operational screen and `CatalogVersionEditorRoute.tsx` is 994 for one admin route. The second is that this screen is
not a table: it has a cut-off control, a **command** that writes an immutable run, a list of previous runs and one
run's lines, and it is the only #40 screen behind `inventory.view_valuation`, which exists precisely so cost figures
are separated from the balance report.

Checked in this working copy today: there is no `clients/pwa/src/routes/inventory/`, no `clients/pwa/src/inventory/`,
and `clients/pwa/src/i18n/messages/` holds no `inventory.ts`.

## What is already built (do not re-build)

- `clients/pwa/src/routes/billing/OutstandingBalancesRoute.tsx` (106 lines) — the smallest operational read screen in
  the tree. `clients/pwa/src/routes/billing/CashierSessionRoute.tsx` (440 lines) — the closest shape for a screen that
  reads a record, offers a command and shows the result. Billing's routes are flat children of `RequireSession` in
  `clients/pwa/src/app/router.tsx` from `:227`; the `admin` route with its own shell starts at `:289`. This is an
  operational route, **not** a child of the admin shell.
- `clients/pwa/src/admin/useAdminResource.ts` (83 lines) — the one read effect. A second data-fetching path is
  forbidden (`clients/pwa/CLAUDE.md` section 6).
- `clients/pwa/src/auth/apiClient.ts` — the only transport: `apiRequest`, `apiRequestVersioned`,
  `ApiRequestOptions.idempotencyKey` (`:168`) and `.ifMatch` (`:177`), automatic re-authentication and step-up renewal
  with an identical replay. **The non-JSON helper this child's CSV download needs is `E08-F03-11`'s** (or
  `E07-F01-7`'s `apiRequestBlob`, whichever landed first). **This child adds no helper and changes no line of that
  file** —
  it reuses what is there, and if neither has landed the download waits rather than forking a second path.
- `clients/pwa/src/admin/RequirePermission.tsx` (45 lines) — the `EmptyState` with `admin.forbidden.title` /
  `admin.forbidden.body`; it never redirects, and it is not the `Forbidden` component.
- `clients/pwa/src/admin/testing/storyTransport.tsx` (153 lines, note the `.tsx`) — `storyJson`, `storyProblem`,
  `storyPending`, `withAdminApi(element, routes, { path, at })`, `STORY_USER`, `StoryRoutes`. It supplies
  `SessionProvider`, a `MemoryRouter` and `RequireSession`, so it drives the real route at a real address.
  `STORY_USER.permissions` carries **no** inventory key, so every story stubs
  `'GET /api/v1/me': () => storyJson({ ...STORY_USER, permissions: [...] })`. This child needs **three** users: one
  holding `inventory.view_reports` only, one also holding `inventory.view_valuation`, and one holding
  `view_valuation` plus `reports.export`.
- `clients/pwa/src/design-system/testing/renderWithProviders.tsx` (45 lines) supplies `AppIntlProvider` and display
  preferences **only** — no session, no router — so a route rendered in it throws.
- `JourneyFrame` (`clients/pwa/src/stories/journeys/JourneyFrame.tsx:58`) wraps its children in its **own**
  `MemoryRouter`, so it cannot be combined with `withAdminApi`/`StoryRoutes`. Pick one; this screen uses `withAdminApi`.
- `clients/pwa/src/design-system/testing/axe.ts` — `expectNoAccessibilityViolations`, with
  `RULES_JSDOM_CANNOT_JUDGE = ['color-contrast', 'target-size']` (`:22`).
- `clients/pwa/src/i18n/formatters.ts` — **`formatQuantity` and the money formatters are not module exports.** The file
  exports `getFormatters(locale?, timeZone?)`, `createFormatters`, `DEFAULT_TIME_ZONE` and `CURRENCY_CODE = 'INR'`;
  the formatting functions are **methods on the `Formatters` interface** (`:104`; `formatQuantity` at `:117`, and a
  money formatter "with the ISO code, for exports and integration payloads" at `:112`). The call shape every route in
  the tree uses is `const formatters = getFormatters()` once per component — see
  `CashierSessionRoute.tsx:32,57` and `AllocateAdvanceRoute.tsx:19,39`. Importing `{ formatQuantity }` fails
  `pnpm typecheck`.
- `docs/architecture/conventions.md` section 1.1 rule 3 — "the client never computes an authoritative amount". Every
  figure on this screen is the server's; the client renders and never sums, never re-rounds and never derives a total.
- `docs/architecture/invariants.md` **INV-STK-08** (line 386) — "A valuation run is an immutable snapshot at a
  cut-off, **recording the method it used**." That is why the method and the cut-off are stated beside the total and
  not buried in a detail panel: a valuation figure without its method is meaningless.
- `docs/nfr/data-classification.md` section 5.9 — "**Cost, valuation and margin figures are Confidential** and are
  **not shown on shop-floor screens**", and "In logs: … **unit costs, valuations and supplier terms may not**" appear.
- `clients/pwa/src/components/dialogs/confirmTiers.ts:18` — `CONFIRM_TIERS = ['confirm', 'reason', 'typed']`. There is
  no tier called "irreversible". `typed` is "Reserved for desktop and tablet administration actions and never asked of
  somebody on a phone in a workshop" and `resolveConfirmTier` silently substitutes on a phone.
- The E08-F01 client foundation planned as `E08-F01-5`: `clients/pwa/src/inventory/{types,inventoryApi,
  inventoryPermissions}.ts`, `clients/pwa/src/i18n/messages/inventory.ts`,
  `clients/pwa/src/routes/inventory/inventory.css` — extended, not duplicated.

## What is missing

- No valuation screen, so the accountant's figures are reachable only with a command-line HTTP client, and #40's fifth
  acceptance criterion is invisible in the product.

## Scope

- [ ] Run `pnpm --dir clients/pwa generate:api` **first** and pin every local type in
      `clients/pwa/src/api/contract.ts` with `Conforms<Local, Immutable<Response200<'OperationId'>>>` against
      `E08-F03-7`'s operation ids (`Conforms` at `contract.ts:147`). A shape missing from the generated schema is a
      defect raised on `E08-F03-7`, not something to guess around.
- [ ] **`/inventory/valuation`** — one route, three views inside it: a **cut-off control** and a **Run valuation**
      action; the **list of previous runs** with their method, cut-off, actor, completion instant and total; and **one
      run's lines** with quantity, unit cost and value. Cursor paging on both lists where the server answers a
      `nextCursor`.
- [ ] **The method and the cut-off are stated beside every total**, on the list and on the run, because INV-STK-08
      makes the method part of what a figure *is*. The screen never chooses or implies a method: it renders the one
      the run recorded, and where `E08-F03-7` allows a run to be requested in the other method for comparison, the
      control says which it is asking for and the result says which it used.
- [ ] **The run command.** One `crypto.randomUUID()` minted when the person commits and **held across a refusal and a
      retry** (`clients/pwa/src/routes/catalog/CatalogVersionListRoute.tsx:164` and
      `clients/pwa/src/routes/admin/RoleDetailRoute.tsx:65` are the pattern), a `ConfirmDialog` at the **`confirm`**
      tier — a run is significant but recoverable, since it adds an immutable row and changes nothing that existed, so
      neither `reason` nor `typed` is warranted and `E08-F03-7` declares no `reasonRequired` on it — and
      `OfflineBlockedAction` when offline. A future cut-off is refused by the server with a field-level problem detail;
      render it beside the cut-off field, and **also** stop offering a future date in the control so the refusal is
      the second line of defence rather than the first.
- [ ] **Download CSV, reusing the helper and nothing more.** Call `E08-F03-7`'s `…/runs/{runId}/export` route through
      the non-JSON helper `E08-F03-11` (or `E07-F01-7`) already added, with `Accept: text/csv`. No link is constructed
      and nothing is stored. **The download is gated on `reports.export`, not on `inventory.view_valuation`** —
      `E08-F03-7` puts it there because `docs/nfr/data-classification.md` section 5.9's "In exports" row names
      `reports.export` for valuation exports by name — so the affordance is **absent, not disabled**, for a caller
      without that key, the reason the permission's `RequiresReason` flag demands is collected through
      `clients/pwa/src/routes/admin/AdminReasonField.tsx` (42 lines), and a second-factor demand renders as itself the
      way `ReconciliationApprovalRoute.tsx` and `DispatchExceptionApprovalRoute.tsx` render theirs (the defect #99
      fixed). Consequence to render honestly: an **Inventory Clerk can read a valuation and cannot download it**.
- [ ] **Permission gating.** Guard the route with `RequirePermission` on `inventory.view_valuation`; the navigation
      affordance that leads to it is **absent, not disabled**, for a caller without the key, and reaching
      `/inventory/valuation` directly renders the `RequirePermission` empty state without redirecting. The server
      re-checks on every request regardless (CLAUDE.md security rule 2).
- [ ] **`inventoryPermissions.ts` — a contradiction to name, not to discover in review.** `E08-F01-5` states it
      "declares all four keys now … **so no later child touches this file**". Every #40 client child does; this one
      adds `viewValuation`. Say in the pull request that `E08-F01-5`'s sentence holds for #38's own screens and not
      for #40's. The file is contended across all four #40 client children, so they merge one at a time.
- [ ] Register the route as a child of the existing `RequireSession` route in `clients/pwa/src/app/router.tsx`; add the
      calls to `clients/pwa/src/inventory/inventoryApi.ts`, the types to `types.ts`, and every string to
      `clients/pwa/src/i18n/messages/inventory.ts` under `inventory.valuation.*`, English and Tamil (operational
      vocabulary stays English pending native-speaker review — the recorded practice at the top of
      `clients/pwa/src/i18n/messages/catalog.ts`; `ta-IN` is not selectable, `clients/pwa/CLAUDE.md` section 5).
- [ ] **Five states** as Storybook stories through `withAdminApi` — loading, empty, error, offline, forbidden — plus
      one story of a **refused future cut-off**, one of a run whose method is FIFO rather than weighted average (so the
      method label is visibly load-bearing), and one pseudo-locale story (`clients/pwa/src/i18n/pseudo.ts`).
      `clients/pwa/src/routes/billing/` ships no stories today (#216 is open to add them); this child writes them
      rather than adding to that debt.

## The offline story, because this is where a reviewer and the tree disagree

`docs/process/definition-of-done.md` **DoD 7** asks, word for word, that "Storybook stories exist for the loading,
empty, error, **offline** and forbidden state of every new screen", and `clients/pwa/CLAUDE.md` section 6 repeats it.
But `clients/pwa/src/routes/catalog/catalogScreens.stories.tsx` ships **no** offline story and records why in its own
doc comment: an offline story there "would imply these screens queue what a person did, and they must not".

That exception is about a screen whose offline story would **imply queueing**. This is an operational screen whose
offline state is a *visible refusal* — `OfflineBlockedAction` saying the run and the download will not be queued,
which is what **NFR-CU-05** requires. So **write the offline story**, and put one sentence in the story file's doc
comment saying that it renders the refusal rather than a queue. Do **not** produce an offline *screenshot* at five
widths; the offline state is a story and a Vitest assertion, not a device photograph.

## Out of scope

The two report screens and the transport helper (`E08-F03-11` — this child **reuses** the helper and must not add a
second one); the alert screens (`E08-F03-8`); the stocktake screens (`E08-F03-9`, `E08-F03-10`); profitability,
costing assumption versions, cross-branch and historical valuation trends and the governed export service, all of
which plan Section 9 `### #46` assigns to #46; scheduling the monthly run, which is #44's scheduled-report machinery;
charts of any kind; the Owner dashboard and its tiles (#45, `A11Y-RJ-08` steps 1–3); the `/reports` destination
`roleNavigation.ts` points at, which belongs to Reporting (#44/#46); the `/inventory` index route, which is #39's
client work; the accountant's sign-off of the figures, which is **`E08-F03-13`** and is not a coding session.

## Acceptance criteria

- [ ] A caller holding `inventory.view_valuation` runs a valuation at a cut-off, sees it appear in the run list with
      its method and cut-off, opens it, and reads its lines with unit costs and values — the method and the cut-off
      stated beside the total on both views. One Vitest journey through `withAdminApi`, asserting the request bodies
      and the `Idempotency-Key`.
- [ ] **Negative (the separation the key exists for):** a caller holding `inventory.view_reports` but **not**
      `inventory.view_valuation` finds **no valuation affordance anywhere in the DOM**, and reaching
      `/inventory/valuation` directly renders the `RequirePermission` empty state with the message ids
      `admin.forbidden.title` / `admin.forbidden.body` **without redirecting**.
- [ ] **Negative:** a caller holding `inventory.view_valuation` but not `reports.export` reads a run and finds **no
      Download CSV affordance** — absent, not disabled. An Inventory Clerk is exactly this caller.
- [ ] **Negative:** a future cut-off is not offered by the control, and a forced request renders the server's
      field-level problem detail through `plainLanguageDetail` beside the cut-off field.
- [ ] **Negative:** a replayed run (the same `Idempotency-Key` after a refusal) shows the **first** run rather than
      producing a second — the key is unchanged on the retry.
- [ ] **Exception:** two runs at the **same** cut-off taken at different times both appear in the list, side by side,
      with their own completion instants — because a receipt recorded late does not alter an earlier run
      (`E08-F03-7`'s own exception criterion, INV-STK-08). The screen must not collapse, deduplicate or hide one of
      them, and it must not imply one of them is wrong. This is the behaviour a reader will most want to
      misunderstand, so there is a story for it.
- [ ] **Exception/offline:** with the network down the run list reads from its last value and both **Run** and
      **Download CSV** render `OfflineBlockedAction` saying the action will not be queued (**NFR-CU-05**).
- [ ] Accessibility: `expectNoAccessibilityViolations` on every state; every money value and every total is announced
      with its label **and its method**; the lines table has a caption naming the run, its method and its cut-off; and
      no value is conveyed by colour alone.
- [ ] **No unit cost and no valuation figure reaches any client telemetry field** — asserted over the telemetry the
      screen emits, not by inspection (`docs/nfr/data-classification.md` section 5.9: "unit costs, valuations and
      supplier terms may not" appear in logs; section 5.18 covers client telemetry).

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Valuation lines (quantity, **unit cost**, **value**) and run totals | **Confidential** — cost and valuation figures, explicitly "not shown on shop-floor screens" (`docs/nfr/data-classification.md` section 5.9) | Value the branch's stock at a cut-off | **Not cached client-side at all**: component state for the life of the view, no `localStorage`, no draft (offline drafts are #51's alone, plan Section 6.2 note 8) | `inventory.view_valuation` holders within their branch | **Never** in any log, metric, trace or client-telemetry field. Leaves the client only as the audited CSV download |
| Run metadata (method, configuration version, cut-off, actor, completion instant) | Confidential; the method name alone is Internal | Say what a figure means | Not cached | As above | The method and the cut-off are safe as low-cardinality telemetry attributes; **the totals are not** |
| The downloaded CSV file | Confidential — marked in its own header block by `E08-F03-6`'s writer, reused by `E08-F03-7` | A branch's working copy | Not stored by the client: handed to the browser and forgotten; no link constructed | `reports.export` holders in branch scope — `owner`, `branch_manager`, `auditor` | The download is audited server-side as `inventory.valuation_run.exported` with its reason (section 10: an export download is a read that is audited explicitly) |
| The reason typed for a download | Internal at best — staff free text | Satisfy `reports.export`'s `RequiresReason` flag | Sent and discarded client-side | As above | Never in a log, a trace or client telemetry (section 8) |

No personal data appears on the screen beyond the actor display name the server already answers for a run. Supplier
**contact** data (Personal, section 5.9) is not a column.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `inventory.view_valuation` | branch | no | no | no | `owner`, `branch_manager`, `inventory_clerk`, `auditor` | **Exists** — `docs/security/permission-matrix.md` line 212: "Valuation carries cost prices, which is why it is separate from the balance report". Granted in `SystemRoles.cs` to Owner (116), Branch Manager (225), Inventory Clerk (349), Auditor (407). Guards the route and the run command |
| `reports.export` | branch | **yes** | no | **yes** | `owner`, `branch_manager`, `auditor` | **Exists** — matrix line 245, `ReportingPermissions.Export`, flagged `RequiresMfa` and `RequiresReason`. Gates the **download only**, per `E08-F03-7` |

Both `inventory.view_reports` and `inventory.view_valuation` are granted to the **same four roles** today, so the
separation this screen enforces bites hardest on a **custom** role an administrator builds with one key and not the
other — which is exactly the case the forbidden-state test covers, and the reason it must be written even though no
seeded role exercises it. No permission, grant or matrix change here; **OD-13 is open**.

## Modules, contracts and events

Client only: `clients/pwa/`. No .NET project is touched. **No change to `clients/pwa/src/auth/apiClient.ts`** — the
non-JSON helper is `E08-F03-11`'s or `E07-F01-7`'s, and this child reuses it. DOR-05: no new trust boundary; every
authorisation, every branch filter, every figure and the reason requirement are the server's, and the export-leakage
and cost-leakage abuse cases are attacked from the outside by `E14-F01b-3b` (planned under #56).

## Screens and states

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| Valuation | `/inventory/valuation` | `LoadingState` for the run list, and again for one run's lines | "No valuation has been run for this branch", with the run action beside it | `RetryableError` with the server's words through `plainLanguageDetail`; a refused run and a refused future cut-off each render their own message beside the field | Run list readable from its last value; **Run** and **Download CSV** render `OfflineBlockedAction` ("will not be queued") | `RequirePermission` → `EmptyState` with `admin.forbidden.title` / `admin.forbidden.body`, never a redirect; the affordance that leads here is absent, not disabled, and the download affordance is absent without `reports.export` |

Message ids under `inventory.valuation.*` in `clients/pwa/src/i18n/messages/inventory.ts`, reusing the shared
`inventory.common.*` words `E08-F01-5` declares. Quantities through `formatters.formatQuantity` with their unit,
money and totals through the shared formatters and **never computed on the client**
(`docs/architecture/conventions.md` section 1.1 rule 3), instants through `formatters.formatDateTime`. No figure on
this screen is derived in the browser.

## Migration and rollback

Deleted deliberately: client-only change, no table and no column; no `src/Modules/**` migration appears in the diff.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| **OD-05** Valuation method, rounding and the statutory retention period | **Open** — raised 2026-09-03, "needed before W3 for rounding, **before W4 for valuation**" | The screen renders the method the run recorded and **never chooses or implies one**; the figures are the server's, computed under the register's recorded default (weighted average by default, FIFO optional). The screen is therefore correct under either answer | W4 exit |
| The accountant's sign-off of the figures | **Open, and not closable by a coding session** | The screen renders what `E08-F03-7` computes; the fixtures behind it are marked pending sign-off, and the sign-off is `E08-F03-13`. **No waiver** — `docs/process/waivers.md` is for a gate that cannot be met, not for an approval nobody has given | W4 exit |
| OD-07 Device, browser and printer matrix | Open | Screenshotted at the default width list of `expectNoHorizontalOverflow` (320, 360, 768, 1024, 1280 px at 100% and 200% zoom), the only width set written down anywhere today; the real device matrix is #52's | W0 gate (overdue) |
| OD-13 Permission matrix | Open | Keys consumed as published | W1 (overdue) |
| #50 (design system) and #19 (NFRs) are **open issues** | Open | Their components and rows exist in the tree and are consumed; neither may be described as merged | W1/W0 |

## Required verification

- [ ] Vitest: the run → list → read-lines journey through `withAdminApi`, asserting the request bodies, the held
      `Idempotency-Key` and `Accept: text/csv` on the download (`RecordedCall` in
      `clients/pwa/src/auth/testing/fixtures.ts` is how a request is asserted).
- [ ] Vitest: the three-user permission test — `view_reports` alone sees no valuation affordance and the forbidden
      state on direct arrival; `view_valuation` sees the screen and no download; `view_valuation` + `reports.export`
      sees the download.
- [ ] Vitest: the two-runs-at-one-cut-off state; the refused future cut-off; the replayed run; the five states.
- [ ] Vitest: the client-telemetry screen — no unit cost and no valuation value in any emitted field.
- [ ] Accessibility: `expectNoAccessibilityViolations` on every state, plus a recorded manual keyboard and
      screen-reader pass confirming every money value is announced with its label and its method.
- [ ] **Reflow is not asserted in jsdom.** `clients/pwa/src/design-system/testing/expectNoHorizontalOverflow.ts`
      (394 lines) takes a Playwright `PageLike` and measures inside a real page, and **no screen test in the tree
      calls it** — jsdom performs no layout. Its default widths (320, 360, 768, 1024 and 1280 px at 100% and 200%
      zoom) are the screenshot list, and **a lines table carrying quantity, unit cost and value at 320 px is the
      specific risk**; the automated check arrives with #52, and `tests/e2e` and `playwright.config` do not exist yet.
- [ ] `pnpm --dir clients/pwa lint`, `typecheck`, `test`, `build`, `format:check`, `generate:api:check`.
- [ ] DOR-13 rows: **NFR-CU-05** and the E12 accessibility rows in `docs/nfr/traceability.md`.
- [ ] DOR-16: `./scripts/dev doctor` attached; the client suite needs no database.

## Evidence the pull request will carry

- [ ] Screenshots at 320, 360, 768, 1024 and 1280 px, at 100% and 200% zoom, including the lines table at 320 px with
      no horizontal page scrolling. (`docs/process/definition-of-done.md` section 4 asks for three widths as the
      minimum; five is what `expectNoHorizontalOverflow` defaults to and what #52 will check.)
- [ ] The five state stories, the FIFO-method story, the two-runs-at-one-cut-off story, the refused-cut-off story and
      the pseudo-locale story — with the offline story's doc comment carrying the sentence described above.
- [ ] The accessibility run output and the manual keyboard and screen-reader pass.
- [ ] The three-user permission test output, pasted, because "no cost column for this caller" is the assertion a
      reviewer most needs to see rather than be told about.
- [ ] One downloaded valuation CSV opened in a spreadsheet, screenshotted with its marked header block visible.
- [ ] `generate:api:check` clean; the client suite run twice.
- [ ] A one-line statement that `clients/pwa/src/auth/apiClient.ts` is **unchanged** in this diff.

## Depends on

`E08-F03-7` — the run command, the run list, one run's lines, the export route, the recorded method and the future
cut-off refusal.
`E08-F03-11` — the non-JSON transport helper and the `inventory.reports.*` message family's shared words. If
`E07-F01-7` landed its `apiRequestBlob` first, that is the helper; either way this child adds none.
#38, through its client foundation slice `E08-F01-5` (planned in the parallel round, not yet numbered) — the five
client Inventory files, shared with every other #40 client child, plus `clients/pwa/src/app/router.tsx` and
`clients/pwa/src/api/contract.ts`. They merge one at a time.

Refs #40
