Sub-issue of #40. `E08-F03-1` to `E08-F03-3` give the branch an alert policy, a de-duplicated alert queue and the
acknowledge and snooze commands; none of it is reachable from the application. This child is the first of four client
slices for #40 and covers **one coherent screen group**: the low-stock alert queue the branch works from, and the
branch alert-policy editor behind it. Checked in this working copy today: there is no
`clients/pwa/src/routes/inventory/` directory, `grep -c "api/v1/inventory" docs/api/openapi.v1.json` returns `0`, and
`clients/pwa/src/components/layout/roleNavigation.ts` line 78 already sends the Inventory Clerk to `/inventory`,
which `clients/pwa/src/app/router.tsx` never defines — so the role's own destination is a client 404 today.

## What is already built (do not re-build)

- `clients/pwa/src/routes/billing/` — **the precedent for an operational (non-administration) screen group**: flat
  routes registered as children of `RequireSession` in `clients/pwa/src/app/router.tsx` (the billing routes run from
  `router.tsx:227` to `:287`; the `admin` route with its own shell and children starts at `:289`). Sizes, for
  calibration: `OutstandingBalancesRoute.tsx` 106 lines, `ReconciliationApprovalRoute.tsx` 172,
  `AllocateAdvanceRoute.tsx` 261, `DispatchExceptionApprovalRoute.tsx` 344, `TakePaymentRoute.tsx` 390,
  `CashierSessionRoute.tsx` 440, plus one `billing.css`.
- `clients/pwa/src/admin/useAdminResource.ts` (83 lines) — the **one** read effect (abort, reload keeps the previous
  value, key is not an array). `clients/pwa/CLAUDE.md` section 6 forbids a second data-fetching path; do not write one.
- `clients/pwa/src/auth/apiClient.ts` — the only transport. `apiRequest` and `apiRequestVersioned` return a
  `VersionedResponse<T>` carrying the `ETag`; `ApiRequestOptions` already carries `idempotencyKey` (sent as
  `Idempotency-Key`) and `ifMatch` (sent as `If-Match`), and the module re-authenticates, renews step-up in place and
  replays the identical request. Its body is serialised as JSON only.
- `clients/pwa/src/admin/RequirePermission.tsx` (45 lines) — renders an `EmptyState` with the message ids
  `admin.forbidden.title` / `admin.forbidden.body` for a caller without the key, and **never redirects**. It is not
  the `Forbidden` component. Reuse it; do not write a guard.
- `clients/pwa/src/components/states/index.ts` — `LoadingState`, `EmptyState`, `ErrorState`, `Forbidden`,
  `RetryableError`, `OfflineBlockedAction`, `NetworkStatusBanner`, `StateRegion`, plus `plainLanguageDetail` /
  `failureCauseForStatus` from `components/states/problemDetails.ts` for rendering an RFC 9457 problem as words.
- `clients/pwa/src/components/primitives/index.ts` — `DataTable`, `Filters`, `StatusBadge`, `Alert`, `Button`,
  `ButtonGroup`, `Card`, `Icon`, `IconButton`, `TextLink`; `clients/pwa/src/components/dialogs/` — `ConfirmDialog`
  with its three confirmation tiers, `Drawer`, `BottomSheet`, `UndoBar`.
- `clients/pwa/src/design-system/components/forms/` — `Field`, `TextField`, `Select`, `Checkbox`, `RadioGroup`,
  `SegmentedControl`, `Switch`, `NumericStepper`, `DateField`, `FormErrorSummary`, each with a persistent visible
  label by construction.
- `clients/pwa/src/i18n/formatters.ts` — **`formatQuantity` is not a module export.** The file exports
  `getFormatters(locale?, timeZone?)`, `createFormatters`, `DEFAULT_TIME_ZONE` and `CURRENCY_CODE`;
  `formatQuantity(value, unitSymbol)`, `formatNumber`, `formatShortDate` and `formatDateTime` are **methods on the
  `Formatters` interface** (`formatters.ts:104`, `formatQuantity` at `:117`). The call shape every route in the tree
  uses is `const formatters = getFormatters()` once per component, then `formatters.formatQuantity(value, unit)` — see
  `clients/pwa/src/routes/billing/CashierSessionRoute.tsx:32,57` and `AllocateAdvanceRoute.tsx:19,39`. Importing the
  bare name `formatQuantity` from that module fails `pnpm typecheck`. The set is cached per locale and timezone on
  purpose, so never call `createFormatters` in a row renderer and never format at a call site
  (`clients/pwa/CLAUDE.md` section 5).
  `clients/pwa/src/i18n/messages/index.ts` registers one family per
  file and `clients/pwa/src/i18n/en-IN.ts` derives `MessageKey` from the English catalogue, so a key missing from
  `ta-IN.ts` fails `pnpm typecheck`.
- `clients/pwa/src/admin/testing/storyTransport.tsx` (153 lines) — the real story and test harness: `storyJson`,
  `storyProblem`, `storyPending`, `withAdminApi(element, routes, { path, at })` and `STORY_USER`. It supplies
  `SessionProvider`, a `MemoryRouter` and `RequireSession`, so it drives a **real route** at a real address. Two
  things to know: it is transport-level and not administration-specific, so it works for `/inventory/...` as it does
  for `/admin/...`; and `STORY_USER.permissions` holds **no inventory key**, so every story here must stub
  `'GET /api/v1/me': () => storyJson({ ...STORY_USER, permissions: [...] })` with the keys that story is about.
- `clients/pwa/src/design-system/testing/renderWithProviders.tsx` (45 lines) supplies `AppIntlProvider` and display
  preferences **only** — no `SessionProvider` and no router. A route rendered in it throws; use the harness above.
- `clients/pwa/src/design-system/testing/axe.ts` — `expectNoAccessibilityViolations`, with
  `RULES_JSDOM_CANNOT_JUDGE = ['color-contrast', 'target-size']`, which is what jsdom cannot judge.
- `clients/pwa/src/stories/journeys/InventoryStockEntry.tsx` (492 lines) over
  `clients/pwa/src/stories/fixtures/inventory.ts` (240 lines) — the `A11Y-RJ-05` prototype. Read it for the
  vocabulary it already fixes (every quantity carries its unit; posting is online-only and says so) and **leave it
  alone**: it is `useState` over fixtures with no backend.
- The E08-F01 client foundation, planned in the same planning round as `E08-F01-5`: `clients/pwa/src/inventory/`
  (`types.ts`, `inventoryApi.ts`, `inventoryPermissions.ts`), `clients/pwa/src/i18n/messages/inventory.ts` and
  `clients/pwa/src/routes/inventory/inventory.css`. **Extend those five files if they exist; create them in the same
  shape if they do not** — and say in the pull request which happened.

## What is missing

- No alert screen: the queue `E08-F03-2` publishes and the commands `E08-F03-3` publishes have no caller.
- No policy screen, so the open decision on the policy's numbers (raised by `E08-F03-1`) has nobody to answer it —
  the server deliberately evaluates **no** branch until a policy row exists.

## Scope

- [ ] Run `pnpm --dir clients/pwa generate:api` **first** and read every shape out of
      `clients/pwa/src/api/schema.d.ts`. Field names, state values and the page shape belong to `E08-F03-1` to
      `E08-F03-3` and are **never invented**; pin each local type in `clients/pwa/src/api/contract.ts` with
      `Conforms<Local, Immutable<Response200<'OperationId'>>>`. A shape missing from the generated schema is a defect
      raised on the sibling, not something to guess around.
- [ ] **`/inventory/alerts`** — the queue: `DataTable` of item, location, state, shortfall (through
      `formatQuantity`, with its unit, never a bare number), age and the suggested replenishment where the payload
      carries one; `Filters` for state and location; cursor paging through `nextCursor`; state as a `StatusBadge`
      carrying an icon **and** a word. Row actions **Acknowledge** and **Snooze**, the snooze collecting an instant
      and a written reason (`AdminReasonField` at `clients/pwa/src/routes/admin/AdminReasonField.tsx`, 42 lines, is
      the existing reason control), each minting one `crypto.randomUUID()` when the person commits and **holding it
      across a refusal and a retry** — the pattern at `clients/pwa/src/routes/catalog/CatalogVersionListRoute.tsx:164`
      and `clients/pwa/src/routes/admin/RoleDetailRoute.tsx:65`.
- [ ] **`/inventory/alert-policy`** — the editor: evaluation basis, whether open purchase quantity counts,
      hysteresis margin, evaluation cadence, escalation delay and target roles, every field labelled, with
      `FormErrorSummary` at the top and field errors bound to their field. It sends `If-Match` from the `ETag` of the
      read that painted it, and it **offers no default value for any of the three numbers** — the field is empty
      until a person sets it, because the numbers are an open decision and a pre-filled suggestion would quietly
      become the answer.
- [ ] **An explicit empty state for an unconfigured branch**: when no policy exists the queue says, in words, that no
      alert can be raised until the policy is set, and links to the editor. That is the one piece of client behaviour
      that keeps the server's deny-by-default posture legible instead of looking like a broken screen.
- [ ] Guard `/inventory/alerts` with `RequirePermission` on `inventory.view_reports`, the row actions additionally on
      `inventory.acknowledge_alert` (hidden, not merely disabled, when absent — and the server re-checks regardless,
      CLAUDE.md security rule 2), and `/inventory/alert-policy` on `inventory.manage_reorder_rules`. Register both as
      children of the existing `RequireSession` route in `clients/pwa/src/app/router.tsx`, beside the billing routes.
- [ ] **`inventoryPermissions.ts` — a contradiction to name, not to discover in review.** `E08-F01-5` (#38's client
      foundation slice, planned in the parallel round) states that it "declares all four keys now —
      `manageItems`, `manageSuppliers`, `manageLocations`, `manageReorderRules` — **so no later child touches this
      file**". Every #40 client child *does* touch it, because #40's screens need `viewReports`, `stocktake`,
      `approveVariance`, `viewValuation` and the proposed `acknowledgeAlert`, none of which is in that four. Add the
      keys this child needs, and say in the pull request that `E08-F01-5`'s sentence holds for #38's own screens and
      not for #40's — so a reviewer citing it gets an answer rather than a rebuttal. The file is contended: every #40
      client child edits it, so they merge one at a time.
- [ ] Add the two calls and the three commands to `clients/pwa/src/inventory/inventoryApi.ts`, the types to
      `clients/pwa/src/inventory/types.ts`, and every string to `clients/pwa/src/i18n/messages/inventory.ts` (English
      and Tamil, following the recorded
      practice at the top of `clients/pwa/src/i18n/messages/catalog.ts`: operational vocabulary stays English pending
      native-speaker review, and `ta-IN` is not selectable — `clients/pwa/CLAUDE.md` section 5).
- [ ] **Five states per screen** — loading, empty, error, offline and forbidden — each with a Storybook story driven
      through `withAdminApi`, plus one pseudo-locale story (`clients/pwa/src/i18n/pseudo.ts`). Writes are
      **online-only**: an offline acknowledge or snooze renders `OfflineBlockedAction` saying it will not be queued,
      which is what `docs/nfr/traceability.md` **NFR-CU-05** requires of inventory actions. Note the local precedent
      and depart from it deliberately: `clients/pwa/src/routes/billing/` ships **no** stories (#216 is open to add
      them); this child writes them now rather than creating that debt.
- [ ] No change to `clients/pwa/src/auth/apiClient.ts` is needed or permitted here: every call is JSON.
- [ ] **Where the confirmation tiers actually are, if a dialogue is used at all.**
      `clients/pwa/src/components/dialogs/confirmTiers.ts:18` declares
      `CONFIRM_TIERS = ['confirm', 'reason', 'typed']` — there is no tier called "irreversible". Acknowledging needs
      no dialogue; **snoozing** collects a written reason because the server demands one, so if it is rendered as a
      dialogue it is the **`reason`** tier. `typed` is "Reserved for desktop and tablet administration actions and
      never asked of somebody on a phone in a workshop", and `resolveConfirmTier` silently substitutes on a phone, so
      never ask for it on an operational screen.

## The offline story, because this is where a reviewer and the tree disagree

`docs/process/definition-of-done.md` **DoD 7** asks, word for word, that "Storybook stories exist for the loading,
empty, error, **offline** and forbidden state of every new screen", and `clients/pwa/CLAUDE.md` section 6 repeats it.
But `clients/pwa/src/routes/catalog/catalogScreens.stories.tsx` ships **no** offline story and records why in its own
doc comment: "editing and publishing a catalogue are online-only acts, and the application says so through
`NetworkStatusBanner` and `OfflineBlockedAction`, which carry their own stories. An offline story here would imply
these screens queue what a person did, and they must not."

Read that exception for what it is: it is about a screen whose offline story would **imply queueing**. These are
**operational** screens whose offline state is a *visible refusal* — `OfflineBlockedAction` saying the action will not
be queued — which is exactly what **NFR-CU-05** requires of inventory actions and what a person on a shop floor needs
to see. So: **write the offline story**, and put one sentence in the story file's doc comment saying that it renders
the refusal rather than a queue, so it reads as following the catalogue's reasoning rather than ignoring it. Do **not**
produce an offline *screenshot* at five widths; the offline state is a story and a Vitest assertion, not a device
photograph.

## Out of scope

The CSV download and anything needing a non-JSON response from the transport (`E08-F03-11` carries the
`apiClient.ts` change, and that file is also being changed by `E07-F01-7` in the parallel planning round — whichever
lands first, the other rebases); the stocktake screens (`E08-F03-9`, `E08-F03-10`); the report and valuation screens
(`E08-F03-11` for the two reports, `E08-F03-11b` for valuation); the `/inventory` index route that
`roleNavigation.ts:78` points at (`href: '/inventory'`), which belongs to #39's
client work — **so until #39's client work lands, `/inventory/alerts` is reachable by address and from a link, and
not from the navigation**; say that in the pull request rather than adding a navigation entry, which is not this
child's; the Owner dashboard tile that counts open alerts, which is #45's
(`A11Y-RJ-08` step 1).

## Acceptance criteria

- [ ] An Inventory Clerk holding `inventory.view_reports` and `inventory.acknowledge_alert` opens
      `/inventory/alerts`, reads a queue of three alerts with their shortfalls in units, acknowledges one and snoozes
      another with a reason, and sees both leave the open list — proved by a Vitest journey against the stubbed
      transport, asserting the exact request bodies and headers sent (`RecordedCall` in
      `clients/pwa/src/auth/testing/fixtures.ts`).
- [ ] A Branch Manager opens `/inventory/alert-policy`, sets the basis, the three numbers and the target roles, and
      saves; the request carries the `If-Match` from the read that painted the screen and one `Idempotency-Key`
      where the generated schema declares the header.
- [ ] **Negative:** a caller without `inventory.view_reports` sees the `RequirePermission` empty state with the
      message ids `admin.forbidden.title` / `admin.forbidden.body` and is **not** redirected; a caller holding
      `view_reports` but not `acknowledge_alert` sees the queue with no row actions at all.
- [ ] **Negative:** a `409` from a second acknowledge and a `428`/`409` from a missing or stale `If-Match` each render
      the server's own words through `plainLanguageDetail`, and the action's `Idempotency-Key` is **unchanged** on the
      retry that follows.
- [ ] **Exception/offline:** with the network down, the queue still renders its last value and the two commands render
      `OfflineBlockedAction` saying the action will not be queued (**NFR-CU-05**).
- [ ] **Exception:** a branch with no alert policy renders the "no policy set, nothing can be raised" empty state
      with a link to the editor, rather than an empty table that reads as "no problems".
- [ ] Accessibility: `expectNoAccessibilityViolations` passes on every state of both screens, the shortfall is
      announced with its unit and its sign in words (the prototype's rule), and every control is reachable and
      operable from the keyboard in the order the screen reads.

## Data and classification

| Item | Class | Purpose | Retention | Who may read it | Logs, telemetry, export |
| --- | --- | --- | --- | --- | --- |
| Alert rows rendered on the screen (item, location, state, shortfall, age) | Confidential (`docs/nfr/data-classification.md` section 5.9) | Let the branch act before a stockout | Held in component state for the life of the view only — no `localStorage`, no draft | The signed-in caller, within their branch | No client telemetry carries a quantity or an item code; client telemetry redaction is #52's test and this child adds no new field to it |
| Snooze reason typed by the user | Internal at best — free text | Explain a deferral | Sent and discarded; never stored client-side | As above | Never in client telemetry or a log (section 8) |
| Alert policy values | Confidential | Configure the warning | Not cached client-side | `inventory.manage_reorder_rules` holders | As above |

No personal data is rendered by either screen, and no cost figure appears on either — cost and valuation are
`inventory.view_valuation`, which belongs to `E08-F03-11b`.

## Permissions and roles

| Permission | Scope | MFA | Step-up | Reason | Granted to | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `inventory.view_reports` | branch | no | no | no | `owner`, `branch_manager`, `inventory_clerk`, `auditor` | **Exists** — `docs/security/permission-matrix.md` line 211 |
| `inventory.acknowledge_alert` | branch | no | no | no | `owner`, `branch_manager`, `inventory_clerk` | **Proposed by `E08-F03-3`**, not by this child. If that sibling's reviewer renames or re-grants it, this screen follows the generated schema and the matrix, not this paragraph |
| `inventory.manage_reorder_rules` | branch | no | no | no | `branch_manager`, `inventory_clerk` | **Exists** — matrix line 207. Note the consequence for the screen: an **Owner** can read the queue and act on an alert but cannot open the policy editor, so the editor's link must be hidden for them rather than rendering a forbidden state on arrival |

This child changes no permission, no grant and no matrix row; it consumes them. **OD-13 is open**.

## Modules, contracts and events

Client only: `clients/pwa/`. No .NET project is touched, no module boundary is involved. The screens speak only to
`/api/v1/inventory` routes published by `E08-F03-1` to `E08-F03-3`, through the one transport. DOR-05: the client
surface adds no trust boundary; the authorisation decision is the server's on every request (CLAUDE.md security
rule 2).

## Screens and states

| Screen | Route | Loading | Empty | Error | Offline | Forbidden |
| --- | --- | --- | --- | --- | --- | --- |
| Low-stock alert queue | `/inventory/alerts` | `LoadingState` while the first page loads; the table keeps its previous value on a filter change (`useAdminResource`) | Two distinct empties: "no open alerts" and "no alert policy set, so nothing can be raised" with a link to the editor | `RetryableError` with the server's words through `plainLanguageDetail` | Queue readable from its last value; both commands render `OfflineBlockedAction` ("will not be queued") | `RequirePermission` → `EmptyState` with `admin.forbidden.title` / `admin.forbidden.body` |
| Branch alert policy | `/inventory/alert-policy` | `LoadingState` | Not applicable — a branch with no policy renders the empty form, and the body says so | `FormErrorSummary` plus field-bound errors; `428`/`409` rendered as the server's words | `OfflineBlockedAction` on save; the form stays filled | As above, on `inventory.manage_reorder_rules` |

Message ids live in `clients/pwa/src/i18n/messages/inventory.ts` under `inventory.alerts.*` and
`inventory.alertPolicy.*`, with the shared `inventory.common.*` words reused. Quantities go through
`formatQuantity`, instants through `formatDateTime`, durations through the message catalogue — never an inline
format call (`clients/pwa/CLAUDE.md` section 5). `docs/nfr/a11y-checklist.md` rows that apply:
`A11Y-RJ-08` step 4, verbatim: "Open a low-stock alert and hear item, location and shortfall in words and units", and
step 5, "Take the alert's action, and return focus to **the tile** it came from". The **tile** is the Owner dashboard's
and belongs to #45, so this child's half of step 5 is that focus returns to the row the action was taken from; record
it that way in the checklist rather than claiming the dashboard half.

## Migration and rollback

Deleted deliberately: a client-only change adds no table and no column. The proof is that no `src/Modules/**`
migration file appears in the diff.

## Open decisions in force

| Decision | Status | Default in force while it is open | Closes before |
| --- | --- | --- | --- |
| Alert policy numbers (raised by `E08-F03-1`) | Open | The editor pre-fills **nothing**; an unconfigured branch gets an explicit empty state rather than a guessed threshold | W4 exit |
| OD-13 Permission matrix | Open | The screen reads the keys as published; `inventory.acknowledge_alert` is its sibling's proposal | W1 (overdue) |
| #50 (the design system) and #19 (NFRs) are **open issues** | Open | Both are nonetheless present in the tree as code and documents, and this child consumes the existing components and rows. Nothing here waits on them, and neither may be described as merged | W1/W0 |

## Required verification

- [ ] Vitest: the two journeys above, driven through `withAdminApi`, asserting the request bodies, the
      `Idempotency-Key` held across a retry and the `If-Match` sent from the read's `ETag`.
- [ ] Vitest: the forbidden, error, empty, unconfigured-policy and offline states of both screens.
- [ ] Accessibility: `expectNoAccessibilityViolations` on every state of both screens, with
      `RULES_JSDOM_CANNOT_JUDGE` documented in the test as what jsdom cannot judge.
- [ ] **Reflow is not asserted in jsdom.** `clients/pwa/src/design-system/testing/expectNoHorizontalOverflow.ts`
      (394 lines) takes a Playwright `PageLike` and runs its measurement inside a real page, and **no screen test in
      the tree calls it** — jsdom performs no layout. Its default widths (320, 360, 768, 1024 and 1280 px at 100% and
      200% zoom) are nonetheless the list this child screenshots at; the automated check arrives with #52.
- [ ] `pnpm --dir clients/pwa lint`, `typecheck`, `test`, `build`, `format:check` and
      `generate:api:check` (which fails on drift).
- [ ] DOR-13 rows: **NFR-CU-05** (inventory actions online-only with an explicit blocked state) and the E12
      accessibility rows in `docs/nfr/traceability.md`.
- [ ] DOR-16: `./scripts/dev doctor` attached; the client suite needs no database.

## Evidence the pull request will carry

- [ ] Screenshots of both screens at phone, tablet and desktop widths — 320, 360, 768, 1024 and 1280 px, at 100% and
      200% zoom, the list `expectNoHorizontalOverflow` itself defaults to.
- [ ] The Storybook state stories for loading, empty, error, offline and forbidden, and the pseudo-locale story.
- [ ] The accessibility run output, plus a short manual keyboard and screen-reader pass against `A11Y-RJ-08` steps 4
      and 5, recorded as the checklist expects.
- [ ] `generate:api:check` clean, proving the local types match the published schema.
- [ ] The client suite output, run twice.

## Depends on

`E08-F03-1` — the alert-policy read and write routes and their `ETag`.
`E08-F03-2` — `GET /api/v1/inventory/alerts` and the shapes it answers in.
`E08-F03-3` — the acknowledge and snooze commands and the `inventory.acknowledge_alert` key.
#38, through its client foundation slice `E08-F01-5` (planned in the parallel planning round, not yet numbered) —
`clients/pwa/src/inventory/{types,inventoryApi,inventoryPermissions}.ts`,
`clients/pwa/src/i18n/messages/inventory.ts` and `clients/pwa/src/routes/inventory/inventory.css`. Those five files
and `clients/pwa/src/app/router.tsx` are **shared with that slice and with every other #40 client child**, so they
merge one at a time.

Refs #40
