{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hyfib.com/tailor360/events/billing.payment-recorded.v1.schema.json",
  "title": "billing.payment-recorded.v1",
  "description": "A payment was taken at the counter and recorded against an order, in the cashier's open session. Identifiers and figures only: the reference the mode required and the payer stay on the record.",
  "type": "object",
  "additionalProperties": true,
  "$comment": "additionalProperties is true on purpose. conventions.md section 5.5 permits additive changes within a major version, so a subscriber must tolerate a field it has not seen; a closed schema would make every such addition breaking. required lists the fields present when v1 was published — a field added within v1 is optional forever, because a dead-lettered message replayed later carries the payload as it was written.",
  "required": [
    "eventId",
    "occurredAt",
    "aggregateId",
    "organisationId",
    "branchId",
    "cashierSessionId",
    "customerId",
    "orderId",
    "modeCode",
    "amount",
    "currency",
    "eventType",
    "schemaVersion"
  ],
  "properties": {
    "eventId": {
      "type": "string",
      "format": "uuid",
      "description": "Identity of this occurrence. Delivery is at least once; consumers de-duplicate on it."
    },
    "occurredAt": {
      "type": "string",
      "format": "date-time",
      "description": "When it happened, in UTC."
    },
    "aggregateId": {
      "type": "string",
      "format": "uuid",
      "description": "The payment."
    },
    "organisationId": {
      "type": "string",
      "format": "uuid",
      "description": "The organisation."
    },
    "branchId": {
      "type": "string",
      "format": "uuid",
      "description": "The branch."
    },
    "cashierSessionId": {
      "type": "string",
      "format": "uuid",
      "description": "The cashier session it was recorded in."
    },
    "customerId": {
      "type": "string",
      "format": "uuid",
      "description": "The customer, the order's. An identifier and nothing more."
    },
    "orderId": {
      "type": "string",
      "format": "uuid",
      "description": "The order it was taken against."
    },
    "modeCode": {
      "type": "string",
      "minLength": 2,
      "maxLength": 60,
      "pattern": "^[A-Z][A-Z0-9_]+$",
      "description": "The payment mode's code, as configured: CASH, CARD, UPI, BANK_TRANSFER, OTHER or a mode the organisation added."
    },
    "amount": {
      "type": "number",
      "multipleOf": 0.01,
      "description": "How much, to the paisa, in currency."
    },
    "currency": {
      "type": "string",
      "minLength": 3,
      "maxLength": 3,
      "description": "ISO 4217, INR today (conventions.md section 1.1)."
    },
    "eventType": {
      "type": "string",
      "const": "billing.payment-recorded.v1",
      "description": "The wire name."
    },
    "schemaVersion": {
      "type": "integer",
      "const": 1,
      "description": "The payload's major version. Agrees with the .v suffix of eventType."
    }
  }
}
