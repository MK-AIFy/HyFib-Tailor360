{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hyfib.com/tailor360/events/billing.advance-applied.v1.schema.json",
  "title": "billing.advance-applied.v1",
  "description": "Part of a held advance was applied to a posted invoice of the order, by the rule when the invoice posted or by hand under authority. A payment-allocated event carries the allocation itself; this one carries what the advance still holds.",
  "type": "object",
  "additionalProperties": true,
  "$comment": "additionalProperties is true on purpose. conventions.md section 5.5 permits additive changes within a major version, so a subscriber must tolerate a field it has not seen; a closed schema would make every such addition breaking. required lists the fields present when v1 was published — a field added within v1 is optional forever, because a dead-lettered message replayed later carries the payload as it was written.",
  "required": [
    "eventId",
    "occurredAt",
    "aggregateId",
    "organisationId",
    "branchId",
    "paymentId",
    "invoiceId",
    "amount",
    "remaining",
    "currency",
    "eventType",
    "schemaVersion"
  ],
  "properties": {
    "eventId": {
      "type": "string",
      "format": "uuid",
      "description": "Identity of this occurrence. Delivery is at least once; consumers de-duplicate on it."
    },
    "occurredAt": {
      "type": "string",
      "format": "date-time",
      "description": "When it happened, in UTC."
    },
    "aggregateId": {
      "type": "string",
      "format": "uuid",
      "description": "The advance."
    },
    "organisationId": {
      "type": "string",
      "format": "uuid",
      "description": "The organisation."
    },
    "branchId": {
      "type": "string",
      "format": "uuid",
      "description": "The branch."
    },
    "paymentId": {
      "type": "string",
      "format": "uuid",
      "description": "The payment the advance belongs to."
    },
    "invoiceId": {
      "type": "string",
      "format": "uuid",
      "description": "The invoice it was applied to."
    },
    "amount": {
      "type": "number",
      "multipleOf": 0.01,
      "description": "How much was applied, to the paisa, in currency."
    },
    "remaining": {
      "type": "number",
      "multipleOf": 0.01,
      "description": "How much of the advance is still held after this application."
    },
    "currency": {
      "type": "string",
      "minLength": 3,
      "maxLength": 3,
      "description": "ISO 4217, INR today (conventions.md section 1.1)."
    },
    "eventType": {
      "type": "string",
      "const": "billing.advance-applied.v1",
      "description": "The wire name."
    },
    "schemaVersion": {
      "type": "integer",
      "const": 1,
      "description": "The payload's major version. Agrees with the .v suffix of eventType."
    }
  }
}
