{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hyfib.com/tailor360/events/billing.dispatch-exception-consumed.v1.schema.json",
  "title": "billing.dispatch-exception-consumed.v1",
  "description": "A dispatch exception was consumed by the dispatch authorisation it covered, exactly once (plan lines 1908-1916). Identifiers only.",
  "type": "object",
  "additionalProperties": true,
  "$comment": "additionalProperties is true on purpose. conventions.md section 5.5 permits additive changes within a major version, so a subscriber must tolerate a field it has not seen; a closed schema would make every such addition breaking. required lists the fields present when v1 was published — a field added within v1 is optional forever, because a dead-lettered message replayed later carries the payload as it was written.",
  "required": [
    "eventId",
    "occurredAt",
    "aggregateId",
    "organisationId",
    "branchId",
    "orderId",
    "consumedBy",
    "eventType",
    "schemaVersion"
  ],
  "properties": {
    "eventId": {
      "type": "string",
      "format": "uuid",
      "description": "Identity of this occurrence. Delivery is at least once; consumers de-duplicate on it."
    },
    "occurredAt": {
      "type": "string",
      "format": "date-time",
      "description": "When it happened, in UTC."
    },
    "aggregateId": {
      "type": "string",
      "format": "uuid",
      "description": "The dispatch exception."
    },
    "organisationId": {
      "type": "string",
      "format": "uuid",
      "description": "The organisation."
    },
    "branchId": {
      "type": "string",
      "format": "uuid",
      "description": "The branch the order belongs to."
    },
    "orderId": {
      "type": "string",
      "format": "uuid",
      "description": "The order it covers."
    },
    "consumedBy": {
      "type": "string",
      "format": "uuid",
      "description": "The dispatcher who consumed it; never the person who approved it."
    },
    "eventType": {
      "type": "string",
      "const": "billing.dispatch-exception-consumed.v1",
      "description": "The wire name."
    },
    "schemaVersion": {
      "type": "integer",
      "const": 1,
      "description": "The payload's major version. Agrees with the .v suffix of eventType."
    }
  }
}
