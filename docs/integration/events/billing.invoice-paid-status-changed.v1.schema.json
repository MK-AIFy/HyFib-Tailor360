{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hyfib.com/tailor360/events/billing.invoice-paid-status-changed.v1.schema.json",
  "title": "billing.invoice-paid-status-changed.v1",
  "description": "A posted invoice's derived paid status moved: Unpaid, PartlyPaid, Paid or Cancelled, computed from posted charges, credit and debit notes, allocations and refunds (INV-PAY-06). Published in the transaction that moved it.",
  "type": "object",
  "additionalProperties": true,
  "$comment": "additionalProperties is true on purpose. conventions.md section 5.5 permits additive changes within a major version, so a subscriber must tolerate a field it has not seen; a closed schema would make every such addition breaking. required lists the fields present when v1 was published — a field added within v1 is optional forever, because a dead-lettered message replayed later carries the payload as it was written.",
  "required": [
    "eventId",
    "occurredAt",
    "aggregateId",
    "organisationId",
    "branchId",
    "orderId",
    "previousStatus",
    "status",
    "outstanding",
    "currency",
    "eventType",
    "schemaVersion"
  ],
  "properties": {
    "eventId": {
      "type": "string",
      "format": "uuid",
      "description": "Identity of this occurrence. Delivery is at least once; consumers de-duplicate on it."
    },
    "occurredAt": {
      "type": "string",
      "format": "date-time",
      "description": "When it happened, in UTC."
    },
    "aggregateId": {
      "type": "string",
      "format": "uuid",
      "description": "The invoice."
    },
    "organisationId": {
      "type": "string",
      "format": "uuid",
      "description": "The organisation."
    },
    "branchId": {
      "type": "string",
      "format": "uuid",
      "description": "The branch."
    },
    "orderId": {
      "type": "string",
      "format": "uuid",
      "description": "The order the invoice is for."
    },
    "previousStatus": {
      "type": "string",
      "enum": [
        "Unpaid",
        "PartlyPaid",
        "Paid",
        "Cancelled"
      ],
      "description": "Where it stood before the change."
    },
    "status": {
      "type": "string",
      "enum": [
        "Unpaid",
        "PartlyPaid",
        "Paid",
        "Cancelled"
      ],
      "description": "Where it stands now."
    },
    "outstanding": {
      "type": "number",
      "multipleOf": 0.01,
      "description": "What it still owes after the change, to the paisa, in currency; never below zero."
    },
    "currency": {
      "type": "string",
      "minLength": 3,
      "maxLength": 3,
      "description": "ISO 4217, INR today (conventions.md section 1.1)."
    },
    "eventType": {
      "type": "string",
      "const": "billing.invoice-paid-status-changed.v1",
      "description": "The wire name."
    },
    "schemaVersion": {
      "type": "integer",
      "const": 1,
      "description": "The payload's major version. Agrees with the .v suffix of eventType."
    }
  }
}
